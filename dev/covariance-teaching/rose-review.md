# Rose final consistency review — covariance teaching

**Verdict: PASS — no actionable P1/P2 in the approved five-file slice.**

Reviewed the working-tree diff against `da6398a9d8df78c04dc4645dfa3fd4c3bd8d75e3`, the approved `PLAN.md`, the current `AGENTS.md` covariance contract, and the already-recorded Noether, Pat, and mechanical reviews. This was a source-only consistency review: no R, fitting, compilation, rendering, package check, commit, or source edit was performed. The current AGENTS 5 x 3 grid takes precedence over the stale 4 x 5 wording in this audit skill.

## Checks passed

1. **Approved scientific corrections.** The spatial article now distinguishes rotation ambiguity from the additional shared/diagonal decomposition ambiguity, explicitly gives *different* positive Psi companions, names total spatial covariance as the target, and makes no stability or recovery claim (`spatial-models.Rmd:368-393`). The cross-family summary is consistently called a residual-augmented latent-liability/model-scale association, never an observed-response correlation; the ordinal-probit refusal remains (`cross-family-correlations.Rmd:350-356,401-405,425-440`; `R/extract-correlations.R:809-825,927-930`). The ICC text permits a diagonal `indep()` level and does not require shared factors everywhere (`covariance-correlation.Rmd:379-386,454-477`). The article separates between-unit Psi, an OLRE, and the fixed family/link residual without advertising NB/Tweedie-plus-OLRE support (`covariance-correlation.Rmd:479-487,546-567`). The seeded example is explicitly a teaching realization rather than recovery or a sample-size guarantee (`cross-family-correlations.Rmd:323-332,442-448`).
2. **Reader cascade.** All added wide examples are `eval = FALSE` and say they are structural translations, not long/wide parity evidence (`covariance-correlation.Rmd:88-103,531-544`; `cross-family-correlations.Rmd:384-399`; `spatial-models.Rmd:330-351`). The four required figure alt strings are present and describe the displayed quantity and direction (`covariance-correlation.Rmd:290,375,435`; `cross-family-correlations.Rmd:332`). The spatial counterexample now states that the Psi companions differ (`spatial-models.Rmd:381-385`).
3. **Public API and documentation parity.** `extract_cross_correlations()` retains its formals and defaults: `level = "unit"`, `contrasts = FALSE`, `link_residual = c("auto", "none")`, `method = c("point", "bootstrap", "profile", "wald")`, `conf = 0.95`, `nsim = 500L`, and `seed = NULL` (`R/extract-correlations.R:886-888`; `man/extract_cross_correlations.Rd:7-16`). The changed `link_residual` prose and ordinal diagnostic agree between roxygen and generated Rd (`R/extract-correlations.R:809-825,927-930`; `man/extract_cross_correlations.Rd:27-43`). No export, signature, method list, keyword default, or executable extractor calculation changed.
4. **Scope and terminology.** The touched source introduces no primary deprecated keyword, obsolete grid framing, `gllvmTMB_wide()` claim, unqualified interval-calibration claim, or unsupported family capability. The spatial family remains explicitly partial, the cross-family rank-3 teaching result remains non-general, and no capability is promoted beyond recorded evidence.

## Still owed before PR readiness

Source review is complete only. The approved single renders of the three articles, rendered-page inspection of the four alt descriptions (including spatial equation wrapping), the bounded package check receipt, and final three-OS CI are still pending. Current `fit-ledger.md` records zero article-render attempts; its setup attempt 4 is a successful isolated install/document/pkgdown check, not a substitute for those gates.

## Deferred neighbour

`R/extract-sigma.R:545-548` retains the existing claim that the softmax residual makes a categorical block commensurable with any single-scale trait. That wording conflicts with its neighbouring scientific-assumption caveat and with this correction's model-scale boundary. It is outside the frozen approved five-file slice; record it as a separate documentation follow-up, with no expansion of this review or render gate.
