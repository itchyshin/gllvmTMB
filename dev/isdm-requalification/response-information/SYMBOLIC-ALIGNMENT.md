# Symbolic alignment: response information, not spatial replication

| DGP quantity | Fixed across a pair | `rep3` change | Recorded evidence |
| --- | --- | --- | --- |
| `u_c`, `Lambda Lambda'` | shared ecological surface | none | centered shared-surface nRMSE |
| `e_ct`, `Psi` | same cell-trait realization | none | three separate relative `Psi` errors |
| source/cell/trait support | source layout and offsets | none | baseline-data SHA-256 |
| response stream | baseline draw | append two conditional independent draws | immutable observation seeds |
| optimizer start | one paired seed | none | task record |

The intervention is additional conditional response information at fixed support.
It does not create independent `Psi` realizations or test spatial range/patch
replication. Negative `D` favours `rep3`.
