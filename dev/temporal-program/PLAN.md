# Native temporal programme: publication and additive source expansion

## GOAL

**Platform:** Codex

Deliver a corrected, cross-platform-verified native temporal covariance provider, then add the four named **additive** temporal-source combinations and individually specified temporal forecasting, direct inference, bootstrap, and selection routes. Each admitted route will have an executable ledger, independent dense Gaussian oracle, lifecycle evidence, user documentation, and its own claim boundary.

**Headline:** repair unconditional temporal simulation before any public or inferential expansion.

**Deferred:** source-by-time interaction kernels; estimated source attenuation with temporal terms; temporal rank above one; temporal slopes; non-Gaussian temporal fits; automatic selection; inferential calibration or coverage claims beyond campaigns that actually pass.

**Discipline:** no source-combination or lifecycle route becomes public merely because parsing or a generic method exists. Long recovery or coverage campaigns are timed first and routed under the compute policy.

## Frozen model meaning

A temporal plus source provider is an additive independent-process model:

\[
V_{ii'}=1_{g_i=g_{i'}}r_T(t_i,t_{i'})[\Sigma_T]_{j_i,j_{i'}}+
K_S(h_i,h_{i'})[\Sigma_S]_{j_i,j_{i'}}+[V_B+V_W+D_\epsilon]_{ii'}.
\]

`r_T` is AR1 with integer gaps or OU with supplied elapsed time. The second
source is one of spatial, phylogenetic, animal, or a fixed labelled kernel. Each
source keeps its existing `indep`, `dep`, or admitted rank-one `latent` trait
mode. Components are independent before observing data.

This is not a separable interaction \(K_S r_T\), an evolving spatial field, or
evolutionarily correlated temporal innovations. Those require another model
contract. The first combined cells fix source `rho = 1`, retain temporal
intercepts, and use Gaussian identity-link ML/Laplace only.

## Prior-work sweep receipt

| Surface | Evidence read | Finding and decision |
|---|---|---|
| Brain | `search_notes(... temporal combinations spatial phylogenetic animal kernel forecast interval profile selection bootstrap release, search_all_projects=TRUE)` and `rg -n -i temporal ... /Users/z3437171/.codex/memories/MEMORY.md` | The existing evidence was explicitly local and bounded. Resume, do not restate it as publication evidence. |
| Repository and branches | `git rev-list --left-right --count origin/main...8f013724b`, `git diff --name-status origin/main...8f013724b`, `git worktree list`, temporal log search | `8f013724b` is three commits ahead of its current base. Start a child lane from it; do not edit the unrelated current checkout. |
| Prior delivery | `git show 8f013724b:docs/dev-log/after-task/2026-09-09-temporal-sixth-source.md`; temporal tests and `dev/temporal-sixth-source/verify.R` | The sixth-source implementation, dense likelihood oracle and local check exist. They do not cover cross-platform, source pairs, forecast, inferential methods or bootstrap. |
| Sister/source implementation | `rg -n` over temporal, spatial, phylo, animal, kernel and lifecycle entry points; Luna reconnaissance | Existing native engine already adds source effects independently. Reuse that additive architecture; do not create product interactions by parser relaxation. |
| Independent math review | Astra review of `8f013724b`, especially `R/methods-gllvmTMB.R:1661-1698` and `src/gllvmTMB.cpp:1773-1838` | Current unconditional simulation subtracts innovations rather than recursive states. Correct and independently reverify it before every downstream route. |
| Lane state | `lane_preflight.sh` on 2026-09-09 and Luna branch/worktree map | Several live Codex lanes touch engine, methods, intervals and shared docs. Claim paths before each slice and use a fresh worktree. |

## Slices and dependencies

| ID | Owner / model / estimate | Output and owned surface | Depends on | Acceptance |
|---|---|---|---|---|
| S0 | Terra-high integration; Astra review; 4-8 h | Correct temporal simulation in `R/methods-gllvmTMB.R`; deterministic and dense-moment regressions | none | Correct fitted-state subtraction, conditional and unconditional contracts, all temporal modes and AR1/OU pass independent oracle. |
| S1 | Terra-high + Grace; 1-2 days excluding CI | Rebased corrected base, docs, PR/cross-platform receipts, merge/release readiness | S0 | Linux/macOS/Windows native temporal checks; local package/docs gates; no broader claim. |
| S2 | Astra contract review then Terra-high; 1-2 days | Additive source-pair grammar/admission contract and independent dense source-pair oracle | S0 | Explicit additive-not-interaction negative control; source/series labels and fixed source scale preserved. |
| S3a | Terra-high + Curie; 2-4 days | Temporal plus spatial pair | S2 | **Partial:** replicated AR1 `temporal_indep()` plus fixed in-keyword `spatial_indep()` has an independent dense additive NLL/gradient oracle, product-kernel control, unconditional SPDE redraw moment test, long/wide syntax, update replay, and a proportional time--space basis refusal. Its retained 30-attempt campaign failed the frozen strict gate: 10/10, 9/10, and 8/10 strict successes at `phi = -.4, 0, .6`, with unstable spatial range/scale estimates at positive persistence. The separate rank-one `temporal_latent(..., d=1, unique=FALSE) + spatial_indep()` cell rebuilds its mesh projection independently, covers all active dense gradients and redraw moments, and passes its retained nine-fit direct-DGP gate. Both are local fixed-fixture evidence only; OU and other temporal/spatial modes remain deferred. |
| S3b | Terra-high + Curie; 2-4 days | Temporal plus phylogenetic pair | S2 | **Partial:** replicated AR1 `temporal_indep()` plus fixed in-keyword `phylo_indep()` has independent additive dense, product-control, long/wide, update, simulation and tree/dense evidence. The initial retained 80-series recovery fixture failed two unchanged phylogenetic-variance criteria at `phi=.6`. Its sole predeclared 160-series follow-up completed on Fir DRAC job `59096255`: all 30 fits reached terminal success, but strict successes were 10/10, 9/10, and 8/10 at `phi=-.4, 0, .6` because three final outer gradients exceeded `1e-3`. The separately authorized six-cell Totoro damped-Newton qualification then accepted only two cells under its frozen all-six rule, so it also fails and supplies no recovery evidence. The rank-one `temporal_latent(..., d=1, unique=FALSE) + phylo_indep()` cell has a direct dense all-parameter NLL/gradient oracle and lifecycle checks, but its retained nine-fit DGP also fails at `phi=.6` (phylogenetic-variance medians `.529` and `.769` exceed `.35`). Both retained receipts are failed engineering gates, not recovery evidence. |
| S3c | Terra-high + Curie; 2-4 days | Temporal plus animal pair | S2 | **Partial:** replicated AR1 `temporal_indep()` plus fixed in-keyword `animal_indep()` has independent additive dense/product-control, `A`/pedigree/sparse-`Ainv` provenance, label-permutation, long/wide, update, and simulation tests. The retained 160-series, 30-fit recovery fixture completed with all optimizer convergence codes zero but failed its unchanged strict gate because 3/10, 3/10, and 2/10 fits at `phi = -.4, 0, .6` respectively exceeded the outer-gradient threshold; results are retained under `results/animal-recovery-160-20260909.csv`. The rank-one `temporal_latent(..., d=1, unique=FALSE) + animal_indep()` cell has a direct dense all-parameter NLL/gradient oracle, A/pedigree/Ainv lifecycle checks, and a passing retained nine-fit direct-DGP gate. It remains local fixed-fixture evidence only. |
| S3d | Terra-high + Curie; 2-4 days | Temporal plus fixed-kernel pair | S2 | **Partial:** replicated AR1 `temporal_indep()` plus one labelled `kernel_indep()` term has a passing retained fixture. The separate `temporal_dep() + kernel_indep()` cell fails its frozen nine-fit variance gate at `phi=.6`. The rank-one `temporal_latent(..., d=1, unique=FALSE) + kernel_indep()` cell has a direct DGP, dense all-parameter NLL/gradient oracle, lifecycle checks, and a passing retained nine-fit fixture. Each is local fixed-fixture evidence only. OU, temporal `unique=TRUE`, and other temporal/kernel modes remain deferred. |
| S4 | Terra-high + Emmy; 2-4 days | `forecast_temporal()` or precisely scoped `predict()` extension | S0, then S3a-d for composed version | **Partial:** temporal-only existing-series Gaussian forecast has a dense conditional-Gaussian oracle. Separately qualified composed routes admit replicated AR1 `temporal_indep() + kernel_indep()` future observations with a fixed labelled diagonal kernel, and replicated AR1 `temporal_dep() + phylo_indep()` future observations with one fixed phylogeny. New independent series and every other composed forecast remain refused. |
| S5 | Terra-high + Gauss/Fisher; 3-6 days | Direct temporal persistence/decay and diagonal-SD interval/profile routes | S0 | **Partial:** temporal-only and qualified kernel routes have direct profile support; replicated AR1 `temporal_dep() + phylo_indep()` also admits a direct persistence profile with marginal re-optimization, transformed-scale MLE closure and boundary diagnostics. Derived, covariance, phylogenetic, and interval profiles remain blocked. |
| S6 | Terra-high + Curie; 2-5 days | Temporal-only parametric bootstrap/refit, then one source pair at a time | S0, S3a-d, S5 | **Partial:** temporal-only and qualified kernel routes retain full-joint draw/refit attempts. Replicated AR1 `temporal_dep() + phylo_indep()` now also redraws and refits its saved public call, retaining every result. Sampling properties, intervals, and every other source pair remain separate work. |
| S7 | Terra-high + Noether/Fisher; 1-3 days | Supplied-candidate temporal model comparison | S0 | Same rows/likelihood; correct optimized-parameter count; AR1/OU equivalence and boundary-null refusals. No automatic or rank>1 selection. |
| S8 | Terra-high + Pat/Rose/Grace; 2-4 days plus measured campaigns | Documentation, recovery/coverage evidence, after-task reconciliation and release decision | S1-S7 | All ledgers reverified; documentation examples rendered; claims match retained evidence. |

S3a--S3d are logically parallel but share parser, fit plumbing, C++ engine and documentation paths. They are therefore executed sequentially unless disjoint ownership is proven by leases.

## Acceptance gates

- **G0:** isolated lane, explicit ownership and this frozen contract.
- **G1:** repaired temporal simulation has a deterministic zero-noise state test plus independent dense-moment oracle.
- **G2:** corrected bounded base passes local regression/documentation/package checks and platform-specific CI before merge/release.
- **G3:** each source pair matches independently constructed additive dense covariance and gradients; a product kernel demonstrably differs.
- **G4:** parser/admission/provenance: long/wide, permutation, source labels, series labels, all refused combinations and confounded designs.
- **G5:** source-pair simulation and lossless update/refit; no simulated mean shift.
- **G6:** temporal forecasting matches joint Gaussian conditioning; unsupported extrapolation refuses.
- **G7:** direct interval/profile routes maximize the marginal likelihood, transform correctly and expose boundaries.
- **G8:** bootstrap retains full model/source objects and all refit failures; fixed-seed recovery or coverage campaigns are separately timed and retained.
- **G9:** supplied-candidate selection preserves likelihood semantics and never assigns an invalid LRT distribution at a temporal boundary.
- **G10:** docs, generated help, pkgdown, package checks, validation register, after-task report and plan-versus-result reconciliation agree.

## Current reconciliation

On 2026-09-09, the first source-pair admission is replicated AR1
`temporal_indep() + kernel_indep()`. Its direct DGP uses 80 series, 16
occasions, three traits, fixed labelled non-proportional kernel coordinates,
`phi = -.4, 0, .6`, and seeds `2609151:2609153`. The executable runner checks
the dense oracle fixture and each retained final two-pass optimizer result.
This is local kernel-cell evidence only. A fixed in-keyword phylogenetic cell
completed its retained recovery campaign but failed the unchanged strict
gradient gate, so it remains partial. OU,
the dependent temporal-kernel recovery cell remains partial. The rank-one
temporal-latent kernel cell has its own passing fixed-fixture gate; its scope
does not widen to temporal Psi, OU, other sources, or source-pair inference.
The independent-variance spatial cell is limited to the replicated AR1 contract above and its retained campaign is partial. The separately admitted rank-one spatial cell has a passing nine-fit fixed-mesh receipt, but this is local fixture evidence only and does not support a general recovery claim. The animal cell remains partial because its retained recovery campaign did not meet the unchanged strict gradient gate.

## Compute

Cheap deterministic oracles and smoke fits run locally with one BLAS thread and at most four local cores. Before any recovery or coverage campaign, time one fixture for every source pair and lifecycle route. A retained run projected above 30 minutes requires the measured pre-run evidence and separate compute approval; use Totoro for a bounded rehearsal and DRAC arrays for claim-bearing campaigns.

## Publication sequence

The corrected bounded temporal base may proceed through a dedicated PR, three-OS CI and release decision after G1--G2. Each additional pair or lifecycle route gets a separate release note and validation-register row only after its own gates pass. No source pair or lifecycle route is silently included in the base release.
