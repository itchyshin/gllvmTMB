# Claims ledger — every substantive claim this arc made, and its current status

Written because the arc's headline changed **twice** under measurement, and several
commits are now on record carrying superseded framings. A reader arriving at any one
commit should be able to find out here whether it still stands.

**Convention:** STANDS · QUALIFIED · SUPERSEDED · RETRACTED.

| # | claim | where | status |
|---|---|---|---|
| 1 | Albert-Chib is an **objective substitution**, not an architecture change: `z` profiles out analytically to `y·logΦ(μ) + (n−y)·logΦ(−μ) − n·v/2` | `ALBERT-CHIB-DERIVATION.md`, `de255ba9` | **STANDS.** Confirmed against a 200-node GH reference to 6.2e-11, and by the working implementation. |
| 2 | The AC tier is correctly wired: strict lower bound, `he()` finite, family guard both ways, no silent tier fallback | `06-ac-tier-verify.R`, `478cc977` | **STANDS.** 21/21 checks; 245 existing VA tests green. |
| 3 | gllvm's `−v/2` is **not a valid lower bound** for `n > 1`; ours is | `de255ba9`, §6.2 | **STANDS** as a statement about the BOUND. Measured −15.584 nats at n=20. |
| 4 | …and that makes us "better than gllvm" | `478cc977` commit body | **QUALIFIED** by `1d10bca7`. It is a correctness property of the bound, **not** an accuracy claim. The two implementations reach the same optimum to 1e-5, so the corrected term barely moves this estimand. **Never quote (3) as an accuracy result.** |
| 5 | The AC evaluator is **15.5× cheaper** per evaluation; **17.7–26×** per fit | `fde6ca20`, `1ed59990` | **STANDS.** Clean, interleaved, serial. |
| 6 | AC is ~94–264× slower than gllvm | chat, mid-session | **SUPERSEDED.** That compared *different models* — ours carried a ψ tier gllvm does not fit. Like-for-like: **3.7×** (2.71 s vs 0.74 s). |
| 7 | GH "does not complete" at N=250/T=20 | `1ed59990` | **SUPERSEDED.** An artifact of `unique = TRUE`. With gllvm's model both complete in seconds. The commit already flagged it UNVERIFIED as a tier property; it was not one. |
| 8 | **The accuracy gate PASSES** — AC median 0.2259 ≤ 0.298 | `47d1fa22` | **QUALIFIED, and this is the important one.** Measured on a DGP planting **ψ = 0** — AC's single most favourable corner, since AC's known failure is collapsing ψ. It stands *for what it measured* and must not be read as general. |
| 9 | AC reproduces gllvm to 1.06e-04, independent of starting values | `47d1fa22`, `1d10bca7` | **STANDS.** Independence proven by construction: identical objective (1649.569276) and rel_frob spread 2.36e-06 from three starts. |
| 10 | …therefore AC is a success | implicit framing | **SUPERSEDED.** Agreement with the reference is **parity, not superiority.** Our **GH** tier beats gllvm (0.1974 vs 0.2259). |
| 11 | The ψ tier is B-tier, so D-28 forbids zeroing it | `6102e044` | **RETRACTED** by `ac3ed04e`. The level's *name* does not decide which tier is lowest — the data structure does. With one observation per (unit,trait) cell there is no `unit_obs` beneath it. |
| 12 | ψ is unidentified for probit, so fitting it is waste | implied by the 1e-05 measurement | **RETRACTED.** The DGP planted ψ = 0, so ~0 was correct. With ψ = 0.6 planted, ψ **is** recovered at `n_trials = 20` (0.5399) and the profiled objective has a clear minimum — identified, not degenerate. |
| 13 | **AC collapses a real ψ at low `n_trials`** — 0.0001 vs a planted 0.6 at n=6, where GH gives 0.6207 | `6cc84122` | **STANDS.** Same data through both tiers. AC also 29% worse on the loadings there. This is Risk R1 materialising. |
| 14 | The ψ = unique + link **conditional** is already correctly implemented | `6102e044`, `14-tier-rule-check.R` | **STANDS.** Only `gaussian_anchor` carries `log_sigma`; the B-tier ψ is family-agnostic. |
| 15 | **Warm-starting GH from AC** gives GH's accuracy in 36.8 vs 138.6 iterations | `17c03f4b` | **STANDS** as an ITERATION result (5 seeds, objectives agree to 4–5 s.f.). The **~3.0× whole-fit figure is ARITHMETIC**, not an end-to-end timing — confirm serially before quoting. |
| 16 | Our Laplace beats gllvm's Laplace by ~28% at `n_trials ≥ 6` | `775a9fdb` | **RETRACTED** by `22-four-way-results.txt`. It rested on a **single seed**. The 2×2 campaign has ours-LA worse in all four conditions. Neither run settles it — both are underpowered. The ψ-retention half of that commit **stands**. |
| 18 | Our GH tier beats gllvm's VA (0.1974 vs 0.2259) | 6-seed run, chat | **RETRACTED.** The 2×2 campaign has ours-GH worse in all four conditions. 6 seeds vs 2 seeds, per-seed range 0.13–0.46 — **underpowered either way**. Accuracy rank is UNRESOLVED. |
| 17 | Amdahl caps Item 1 at ~3.35× whole-fit | `fde6ca20` | **STANDS** for the GH→AC substitution in isolation, and is *why* (10) is true — but it is not the ceiling for the arc, because the warm start and the model right-sizing are separate levers. |
| 19 | The `A_i` collapse is "ours to take first" — framed as being first to the closed form itself | `21-WHY-GLLVM-IS-FAST.md`, pre-correction | **SUPERSEDED** by the dr25 prior-art finding. Ranga's distilled note (`dr25-gllvm-variational-implementation-distilled`, NotebookLM notebook `f329caa6` source 1) shows `A_i = (I_d + Σ_j λ_jλ_jᵀ)⁻¹` is already published VA literature. Corrected framing: **first to implement**, never first to derive — ours is the `N_ij` binomial generalisation and the implementation, not the closed form. |
| 20 | gllvm's TMB path spends ~57% of its outer parameters numerically rediscovering the `A_i` closed form instead of using it | `21-WHY-GLLVM-IS-FAST.md`, verified to 1.9e-9 against a real gllvm fit | **STANDS.** Our own measurement, not a literature claim. Regime: n=200, the probit branch, measured on this machine — restate the regime before reusing this number elsewhere. |
| 21 | Every claim in this arc must state the regime it was measured in | this file, "process lessons" §2; claims 8, 13 | **STANDS** as a standing discipline. "A gate passed on a favourable DGP is not a gate passed" — claim 8's accuracy gate looked like success until claim 13 showed the DGP had excluded AC's failure regime. Applied to claim 20 above: the 57% figure carries its regime explicitly for exactly this reason. |
| 22 | The warm route "ends on GH, so it inherits GH's variance recovery and **avoids §3's defect entirely**" — i.e. it does not collapse ψ | `2026-08-03` handover §4; restated in the lane-2 GOAL | **UNMEASURED, AND NOW WITH CONTRARY EVIDENCE.** The warm route's whole evidence base (`13-warmstart-gh.R`, claim 15's 36.8-vs-138.6) is generated by a DGP that **plants no ψ at all**: line 32 builds `eta <- sweep(a %*% t(lam), 2, rnorm(T0,0,0.3), "+")` with **no `u` term**, where `23-warm-route-confirm.R` adds `+ u` with ψ SD 0.6. The log's columns are seed/arm/objective/rel_frob/iterations/fn_evals/convergence/max_grad — **there is no ψ column**. So variance recovery was never measured; on that data there was no variance to recover. This is **claim 8's error repeating** on the very next result, against the process lesson this file already records. Contrary evidence: smoke at N=60, T=8, q=1, n_trials=6, ψ=0.6, **1 seed** — warm reached ψ **0.0007** and a *worse* objective (879.15 vs 863.20, rel diff 1.9e-2, i.e. **not** the same optimum) where cold GH recovered ψ **0.5264**. One small seed is not a refutation and is explicitly NOT claimed as one; the regime is smaller than any the arc has tested. **NOW REFUTED** by the 3-seed confirmation (`25-WARM-ROUTE-PSI-FINDING.md` §4b; N=100 T=10 n_trials=6 ψ=0.6, interleaved, order-rotated, on **Totoro** at load 1.0–1.2 with 0 competing R jobs, script verdict `USABLE`): warm returns ψ = **0.0001 / 0.2427 / 0.0000** where cold GH returns **0.6207 / 0.5023 / 0.5074** on the same data — 3 seeds of 3. Seed 1's pair reproduces claim 13's AC-vs-GH numbers exactly, i.e. **the warm route behaves like AC, not GH.** Mechanism (`25-…` §3): stage 2 hands `nlminb` AC's full parameter vector including `log_sd_tier`, and since ∂f/∂(log σ) = (∂f/∂σ)·σ the gradient is scaled by σ itself, so ψ→0 is a near-flat attracting boundary a local optimiser cannot leave; cold GH instead starts from the default ψ = 0.3 (`R/va-r3-proto.R:1096`). |
| 25 | The warm and cold arms reach the same optimum (claim 15's "objectives agree to 4–5 s.f.") | `17c03f4b`; `13-warmstart-log.txt` | **REFUTED at ψ > 0.** In all 3 confirmation seeds the warm arm is *worse*: 1890.30 vs 1838.44, 1734.82 vs 1715.90, 1862.54 vs 1810.82 — 19–52 nats apart, not 4–5 significant figures. The agreement held only on the ψ = 0 DGP. |
| 26 | The warm route is **14.34× faster** (median wall-clock 13.40 s vs 192.19 s) | `25-WARM-ROUTE-PSI-FINDING.md` §4b | **MEASURED, NOT QUOTABLE AS A SPEED RESULT.** Regime: N=100 T=10 q=1 n_trials=6 ψ=0.6, 3 seeds, interleaved, Totoro, load 1.0–1.2, 0 competing R jobs. It is a true wall-clock ratio, but the arms solve **different optimisation problems** (claim 24) and reach **different optima** (claim 25), and the fast arm **collapses a real variance** (claim 22). A 14× speedup to a wrong answer is not a speed result. This also supersedes the "~3× whole-fit" arithmetic, which was never end-to-end. |
| 27 | `rel_frob` is an adequate score for this comparison | implicit in `13-warmstart-log.txt`'s column set | **REFUTED, and this is why the defect survived.** In the confirmation the *collapsing* arm scored **better** on `rel_frob` (median 0.345 vs 0.377): with ψ driven to zero the variance is absorbed into the loadings, flattering the loadings metric while the variance decomposition is wrong. A loadings-only score is **blind to the one failure mode AC is known to have.** Any future VA comparison must report ψ alongside `rel_frob`. Separately: at this regime **neither arm meets the `rel_frob ≤ 0.298` accuracy gate** (0.377 / 0.345) — the gate was set on a different regime. |
| 29 | **The A_i collapse is IMPLEMENTED, and it does not move the optimum** | `07af7df3`; `27-ai-collapse-speed.R`; log in `results-lane2/aispeed.log` | **STANDS, regime stated.** Implemented as TMB `map=` parameter sharing (no C++ change, block-diagonal S untouched), opt-in and gated to AC + all-probit + complete data + constant `n_trials` + single dense tier. Measured, AC tier, 3 seeds, paired and order-rotated, Totoro at load 0.5 with 0 competing R jobs: **N=100 T=10 q=1 → 1.23×, free params 220→121 (45.0% removed), objective delta 6.87e-12**; **N=250 T=20 q=2 → 1.50×, free params 1309→562 (57.1% removed), objective delta 5.89e-12**. The maintainer's falsifier was ">1e-8 means it is wrong" — measured 3–4 orders of magnitude inside it; `rel_frob` moves 1e-5. 16/16 targeted tests, full VA suite 982 pass / 0 fail. The 57.1% is the same structural share gllvm leaves in its own outer problem. **UNTESTED and unclaimed:** q > 2, other `n_trials`, the structured/diagonal tiers (outside the derivation), and whether the speedup grows further with N. |
| 30 | "We have a better VA than gllvm" | not yet made anywhere — recorded here to keep it that way | **NOT ESTABLISHED. DO NOT CLAIM.** Two attempts already died: claim 16 (one seed) and claim 18 (six seeds) were both RETRACTED, and per-seed `rel_frob` spans 0.13–0.46, so the arc has twice mistaken noise for a ranking. What IS on record: (a) we fit a ψ tier gllvm does not have — a capability difference, not a win; (b) claim 20, gllvm leaves ~57% of its outer parameters unexploited, and claim 29, we now remove that share from ours — a structural advantage, **not yet a measured head-to-head**; (c) on gllvm's conditioning choice 1 we are **behind** them (`va-conditioning-audit-vs-gllvm.md`: our loadings diagonal is unconstrained where theirs is pinned with a separate scale). Establishing this needs a model-matched (`unique=FALSE` both sides), ≥10-seed, interleaved head-to-head on Totoro reporting speed **and** accuracy **and** ψ. Until that exists, the honest statement is "we exploit a closed form gllvm's implementation does not". |
| 31 | **The A_i collapse's benefit GROWS with N — 3.81x at N=1000** | `28-ai-collapse-n-ladder.R`; log in `results-lane2/ladder.log` | **STANDS, regime stated, and the middle rungs are NOISY.** Asked why halving the parameters gave only ~1.3x, the answer is that under TMB's **reverse-mode AD the whole gradient costs ~1-2x one objective evaluation regardless of parameter count**, and the objective is O(N*T) over observations either way — so removing parameters cannot cut per-evaluation cost, only iterations and the quasi-Newton approximation. The removed coordinates are also the cheapest (a quadratic projection, no quadrature). Measured, AC tier, T=20 q=2 n_trials=6, **2 seeds per rung**, paired and order-rotated, untimed warm-up, Totoro at load 0.7 with 0 competing R jobs: **N=100 1.28x - N=250 1.90x - N=500 1.56x - N=1000 3.81x**, with free params 559→262, 1309→562, 2559→1062, 5059→2062 (~53–59% removed throughout) and objective deltas 1.9e-11 / 2.7e-11 / 2.1e-11 / 7.7e-12 — all far inside the 1e-8 falsifier. **The ladder is NOT monotone**: N=500's per-seed values are 2.19x and 1.11x, so 2 seeds is thin and only the endpoints are trustworthy. At N=1000 both seeds agree (4.02x, 3.60x). **UNCLAIMED:** N>1000, other T/q, and whether the trend continues. |
| 28 | **The collapse is a BOUNDARY-START artefact, and resetting `log_sd_tier` before stage 2 repairs the route** | `43341784`; `26-warm-reset-probe.R`; log in `results-lane2/reset.log` | **STANDS, regime stated.** Mechanism predicted it (claim 22) and the probe confirmed it. Keeping the warm loadings/means/fixed effects but resetting only the tier SDs to the ordinary default (log 0.3) gives, at N=100 T=10 q=1 n_trials=6 ψ=0.6, 3 seeds, interleaved and order-rotated, Totoro at load 1.0–1.2 with 0 competing R jobs: **median ψ 0.5074 (vs cold 0.5074), median objective 1810.82 (vs cold 1810.82), median rel_frob 0.377406 (vs cold 0.377405) — matching cold GH to 4–5 s.f. per seed — and still 12.3× faster** (15.7 s vs 192.4 s). The unfixed arm's 14.5× bought ψ = 0.0001 and an optimum 19–52 nats worse. Shipped with a regression test that plants ψ > 0 and asserts recovery (`test-va-r3-warm-psi.R`, 4/4). **Full VA suite green afterwards: 966 pass, 0 fail, 0 warn.** **UNTESTED and therefore unclaimed:** other `n_trials`, other N/T, q > 1, the structured tiers, and whether 12.3× survives elsewhere. One cell, three seeds. |
| 23 | Claim 15's iteration result (36.8 vs 138.6, same optimum to 4–5 s.f.) | `17c03f4b` | **STANDS, NARROWED — and see claim 24, which may void the comparison entirely.** The iteration count and optimum agreement are real *for the ψ = 0 DGP they were measured on*. What does **not** follow is anything about ψ recovery (claim 22) — and the "same optimum" property was itself only ever established where ψ = 0. Re-measure both at ψ > 0 before quoting either. |
| 24 | The warm-vs-cold iteration comparison is like-for-like | implicit in claim 15 and the `~3×` arithmetic | **REFUTED — the two arms do not solve the same optimisation problem.** `.va_r3_fit` builds its objective with `profile_variational = profile_variational` (`R/va-r3-proto.R:2151-2155`), but `.va_r3_fit_warm()` stage 2 calls `.va_r3_make_objective(vd, H = H, eval_method = "gh")` (`:1358`) with **no `profile_variational` argument**, so it silently takes the default `FALSE` (`:1841`). With `profile_variational = TRUE` — which `13-warmstart-gh.R:82`, `23-warm-route-confirm.R` and the smoke all pass — the **cold** arm profiles the variational block out (outer vector ≈ the global block, ~30 params at N=100/T=10) while the **warm** arm carries the entire per-unit variational block in its outer vector (≈2,200 params). Iteration counts across problems of different dimension are not comparable, so "3.8× fewer iterations" and the `~3×` whole-fit arithmetic built on it are **not like-for-like**. Not a trivial oversight: under profiling `obj$par` excludes the profiled block (`:2216-2224`) while `ac$best$par` is the full vector, so the length guard at `:1362-1367` would abort the transplant — i.e. **the warm route cannot currently be profiled at all.** Fixing it means transplanting only the global block and seeding the inner solve with AC's variational block, not passing one more argument. |

## Open, and honestly unresolved

- **The `n·v/2` puzzle** (`12-INDEPENDENCE-AND-THE-NV2-PUZZLE.md`): two provably
  different objectives share an optimum to 1e-5. The inference — that the `v`
  coefficient steers `A_i` while `Λ` is driven by the `y`-term — is **unmeasured**.
  Four experiments named there would settle it; none is run.
- **Which tier is "lowest"** (`15-PSI-TIER-WHICH-LEVEL.md`): depends on replication
  and `n_trials`, not on the level's name. Experiment (2) — genuine `unit_obs`
  replication — is still open.
- **`extract_Sigma(part = "unique"/"psi")` returns an all-NA diagonal** where
  `total`/`shared` are finite. Filed, not fixed; different lane.
- **Overdispersed Poisson has no VA family code** — the one family that should carry
  both a unique and a link term. Gap, not defect.

## The two process lessons this arc paid for

1. **A comparison is only as good as its model match.** The 264× headline, and the
   Design 108 campaign this session refuted, were the *same error in two places*: arms
   fitted to different models, scored on one estimand. Check the model before the number.
2. **A gate passed on a favourable DGP is not a gate passed.** Claim 8 looked like the
   arc's success until claim 13 showed the DGP excluded the one regime where AC fails.
   **State the regime with the result, every time.**
