# Validation-Debt Register

**Maintained by:** Rose (validation-debt audit / overpromise
prevention) and Shannon (cross-team coordination + persona-
active row ownership).
**Ratified by:** Ada (orchestrator) on phase-boundary close.
**Reviewers:** the row-owner persona named per row, plus the
persona named in the lead column of the relevant design doc.

This is the **honest ledger** of advertised capability vs
test evidence. Every advertised capability in
`docs/design/00-vision.md`, `README.md`,
`vignettes/articles/*.Rmd`, `NEWS.md`, and roxygen has a row
here with one of four status states + test-evidence path +
diagnostic status + interval status.

**The register exists to prevent the overpromise crisis of
2026-05-15** (article-port batch overpromised capabilities;
/loop auto-pilot bypassed Pat + Rose reviews; maintainer
flagged repeated mistakes that the drmTMB team does NOT make).
drmTMB Doc #34 is the template; this register mirrors the
discipline.

## Vocabulary

The validation-debt register uses **drmTMB's 4-state
vocabulary** (`covered / partial / opt-in / blocked`), which
is different from the parser-syntax 4-state vocabulary
(`covered / claimed / reserved / planned`) used in
`docs/design/01-formula-grammar.md` and
`docs/design/06-extractors-contract.md`.

The two vocabularies coexist because they describe different
things:

- **Parser-syntax vocabulary** answers *"is this syntax
  accepted by the parser?"* — `claimed` means the parser
  takes it but no end-to-end test confirms the fit + extractor
  path.
- **Validation-debt vocabulary** answers *"is this advertised
  capability backed by evidence?"* — `covered` means a test
  file with concrete assertions; `partial` means tests exist
  but not at the depth advertised; `opt-in` means works with
  a non-default argument that must be explicit; `blocked`
  means advertised but currently broken / undefined / removed.

| Status | Meaning | When to use |
|--------|---------|-------------|
| `covered` | Tests exist with concrete assertions at the depth advertised | Most M0 single-family Gaussian capabilities |
| `partial` | Tests exist but coverage is shallower than the advertised claim | Most non-Gaussian / mixed-family extractors |
| `opt-in` | Works but only with a non-default argument; user must opt in explicitly | E.g. `link_residual = "auto"` until PR #101 made it default |
| `blocked` | Advertised but currently broken, undefined, or requires removal from public surface | E.g. delta-family mixed-family latent-scale correlation |

## Exact-aa ordinary production addendum (2026-08-09)

These rows are route-and-regime evidence. They do not replace broader grammar
or family rows elsewhere in the register. Evidence path for all rows:
`docs/dev-log/simulation-artifacts/2026-08-09-cran07-v4-exact-aa-production/`.

| ID | Capability | Status | Test evidence | Diagnostic status | Interval status |
|---|---|---|---|---|---|
| CRAN07-AA-01 | Native-Laplace Gaussian `indep()` point estimation, three traits, complete data, `n = 60/240` | `covered` | 3,200 exact-aa production attempts; both cells and all RMSE components passed | Fit-health checks remain triage; no general detector certificate | Not assessed; no interval claim |
| CRAN07-AA-02 | Native-Laplace Gaussian `dep()` point estimation, three traits, complete data, `n = 60/240` | `covered` | 3,200 exact-aa production attempts; both cells and all RMSE components passed | Fit-health checks remain triage; no general detector certificate | Not assessed; no interval claim |
| CRAN07-AA-03 | Native-Laplace Gaussian rank-1 `latent(unique = TRUE)` point estimation | `partial` | `n = 240` passed; `n = 60` was pilot-held; three admitted correlation-stress cells failed and three boundary/Psi challenges were held | No dependable-core or boundary-protection claim | Not assessed; no interval claim |
| CRAN07-AA-04 | Native-Laplace Poisson-log rank-1 `latent(unique = TRUE)` point estimation, three traits, clean data, `n = 100/300` | `partial` | 3,200 core attempts passed; missing-response cell passed; rare-level cell failed fixed-effect bias | Clean-data point evidence only; no family-wide robustness or detector certificate | Not assessed; no interval claim |
| CRAN07-AA-05 | Native-Laplace NB2-log rank-1 `latent(unique = TRUE)` point estimation | `partial` | `n = 300` passed, but `n = 100` and missing-response cells failed with catastrophic dispersion errors; rare-level cell was pilot-held | The broad dependable claim is blocked: the detector missed 61 catastrophic errors across tested NB2 cells; sensitivity 0.2375 overall | Not assessed; no interval claim |
| CRAN07-AA-05B | Dependable or missing-response-robust NB2 latent point-estimation claim | `blocked` | `n = 100` and missing-response cells failed; the rare-level cell was pilot-held | Ordinary diagnostics missed most catastrophic NB2 truth errors | Not assessed; no interval claim |
| CRAN07-AA-06 | Native-Laplace Binomial(10)-logit rank-1 `latent(unique = TRUE)` point estimation, three traits, clean data, `n = 100/300` | `partial` | 3,200 core attempts passed; n=150 missing/rare cells passed; small-sample silent-failure cells were held or failed | Clean-data point evidence only; no Bernoulli, separation, or family-wide detector claim | Not assessed; no interval claim |
| CRAN07-AA-07 | Observable terminal-status detection of catastrophic truth errors | `blocked` | 44,800 attempts: sensitivity 19/80 = 0.2375; specificity 43053/44720 = 0.9627236 | Display and triage only; not reliable protection | Not applicable |

## How the register is maintained

1. **Every PR that touches an advertised capability appends or
   updates a row.** The after-task report references the
   register row by ID.
2. **Phase-boundary close gates require** this register to
   reflect the state of the merged code. Shannon's coordination
   audit at each phase boundary cross-checks the register
   against the actual test suite.
3. **The overpromise-preventer rule**: if a row claims
   `covered` but Rose's pre-publish audit cannot point at a
   test file with concrete assertions, the row is downgraded
   to `partial` or `blocked` before publication.
4. **Phase 0B's job** is to walk every `partial` and `blocked`
   row to either `covered` (with new tests) or **honestly
   marked** (with public-surface tightening: NEWS entry +
   article revert + README matrix update).

## Status snapshot (Phase 0A close, 2026-05-16)

This snapshot is the input to Phase 0B. Every row marked
`partial` or `blocked` gets walked in Phase 0B; every row
marked `covered` gets a Rose audit confirming the test
evidence is real.

### Section 1 — Formula grammar (5×3 keyword grid plus modifiers)

Row-owner: **Boole** (formula-grammar parser).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| FG-01 | Long format with `traits(...)` LHS | `covered` | `test-traits-keyword.R`, `test-canonical-keywords.R` | M0 baseline |
| FG-02 | Long format with explicit `value`-stacked long data + `trait =` argument | `covered` | `test-canonical-keywords.R`, `test-keyword-grid.R`, `test-gllvmTMB-args.R` | Option A uniform naming. Issue #590 regression guard: fixed-effect transforms such as `(0 + trait):exp(env)` are no longer misclassified as unsupported glmmTMB-style covariance structures, while bar-style reserved names such as `exp(0 + trait \| site)` still fail loudly. |
| FG-03 | Wide format via `traits(t1, t2, ...) ~ ...` | `covered` | `test-traits-keyword.R`, `test-wide-weights-matrix.R` | Issue #604 regression guard: wide RHS expansion now preserves `animal_scalar()`, `animal_unique()`, and `animal_slope()` as source covariance markers instead of wrapping them in `(0 + trait):...`; the same grid test also checks the existing animal bar keywords. |
| FG-04 | `latent(0 + trait \| unit, d = K)` standalone | `covered` | `test-stage2-rr-diag.R`, `test-keyword-grid.R`, `test-canonical-keywords.R` | Ordinary Psi fold is covered by default `latent()` evidence; scalar common-Psi spelling added 2026-06-18 via `latent(..., common = TRUE)`, objective-equivalent to the legacy paired `latent(..., unique = FALSE) + unique(..., common = TRUE)` form for ordinary intercept-only terms. |
| FG-05 | `unique(0 + trait \| unit)` standalone | `covered` | `test-stage2-rr-diag.R`, `test-cross-sectional-unique.R` | |
| FG-06 | `latent + unique` explicit-Psi compatibility pair | `covered` | `test-stage2-rr-diag.R`, `test-mixed-response-sigma.R`, `test-canonical-keywords.R`, `test-unique-family-deprecation.R` | Compatibility pair remains accepted while ordinary `latent()` now emits Psi by default. Common scalar paired re-home added 2026-06-18: `test-canonical-keywords.R` asserts `latent(..., common = TRUE)` is objective-equivalent to legacy `latent(..., unique = FALSE) + unique(..., common = TRUE)`, estimates one shared ordinary Psi SD, and rejects `unique = FALSE, common = TRUE` plus augmented random-regression `common = TRUE`. Source-specific and `kernel_*()` paired-Psi folds remain future work. |
| FG-07 | `indep(0 + trait \| unit)` | `partial` | `test-canonical-keywords.R`, `test-stage3-propto-equalto.R`, `test-tiers-indep-dep-nongaussian.R` | Bare-keyword (no-V) non-Gaussian gap closed 2026-05-31 (`agent/distribution-validation-fills`): `test-tiers-indep-dep-nongaussian.R` recovers the per-trait diagonal (`indep_B` + `diag_B` flags, `extract_Sigma(part="unique")$s`) under poisson(log) within the inherited 0.30 relative count band (DGP + band from `test-tiers-poisson.R`, heavy-gated, honest-skip on non-convergence). Engine path is `unique()`-identical per `R/brms-sugar.R` (documentary keyword distinction). Common scalar standalone re-home added 2026-06-18: `test-canonical-keywords.R` asserts `indep(..., common = TRUE)` is objective-equivalent to legacy standalone `unique(..., common = TRUE)` and estimates one shared marginal SD across traits. Paired ordinary scalar-Psi re-home is recorded on FG-04 / FG-06 via `latent(..., common = TRUE)`. REMAINS `partial`: the known-V (`propto()`/`equalto()`) variant is still Gaussian-only verified -- non-Gaussian known-V is the harder phylo-dep identifiability item, not filled here. |
| FG-08 | `dep(0 + trait \| unit)` | `partial` | `test-stage3-propto-equalto.R`, `test-tiers-indep-dep-nongaussian.R` | Bare-keyword (no-V) non-Gaussian gap closed 2026-05-31 (`agent/distribution-validation-fills`): `test-tiers-indep-dep-nongaussian.R` recovers the full unstructured covariance (`dep_B` + `rr_B` flags, `extract_Sigma(part="shared")$Sigma`) under poisson(log) -- diagonal within the inherited 0.30 relative count band, off-diagonals within a measured 0.10 absolute band at n_unit=400 (heavy-gated, honest-skip). Engine path is `latent(d=n_traits)`-identical per `R/brms-sugar.R`. REMAINS `partial`: the known-V (`propto()`/`equalto()`) variant is still Gaussian-only verified -- non-Gaussian known-V is the harder phylo-dep item, not filled here. |
| FG-09 | `scalar(0 + trait \| unit)` | `partial` | `test-stage3-propto-equalto.R` | only Gaussian verified. NOT a cheap test-only gap: bare `scalar(0+trait\|unit)` has no standalone engine path (it is `phylo_scalar`/`spatial_scalar` known-V sugar per `R/brms-sugar.R`); the only non-Gaussian realisation is the known-V/phylo route, which is the genuinely-hard identifiability item. Left intentionally (audit 2026-05-31). |
| FG-10 | Two-tier nested `unit / unit_obs` | `covered` | `test-multi-random-intercepts.R`, `test-olre-separation.R` | |
| FG-11 | Crossed random effects (e.g. site × year) | `covered` | `test-multi-random-intercepts.R`, `test-stage1-stacked-fixed-effects.R` | Ordinary scalar `(1 \| site) + (1 \| year)` crossing is now covered by a balanced Gaussian recovery cell (local measurement 2026-06-02: convergence 0, both `log_sigma_re_int` entries recover empirical simulated SDs within 0.20, packed BLUP slices correlate with truth > 0.95). Trait-specific crossed diagonal grouping is the separate `cluster2` row RE-11; ordinary random slopes remain fail-loud and are not implied by this row. |
| FG-12 | `phylo_*` family (5 keywords) | `covered` | `test-stage35-phylo-rr.R`, `test-phylo-hadfield.R`, `test-phylo-mode-dispatch.R`, `test-phylo-q-decomposition.R`, `test-phylo-vcv-optional.R` | M0 baseline |
| FG-13 | `spatial_*` family (6 keywords) | `partial` | `test-stage4-spde.R`, `test-spatial-latent-recovery.R`, `test-spatial-mode-dispatch.R`, `test-spatial-orientation.R`, `test-spatial-orientation-parser.R`, `test-spatial-latent-unique-fold.R`, `test-scan-deprecated-namespace.R` | Base smoke + mode-dispatch remain partial as a family-level row. The 2026-07-03 `spatial_latent(..., unique = TRUE)` fold is covered for intercept-only Gaussian smoke/equivalence: parser marker, malformed-`unique` rejection, both `omega_spde_lv` and `omega_spde` random blocks, reported `Lambda_spde` / `sd_spde_unique` / total `Sigma_spde`, explicit-pair equivalence, duplicate-Psi rejection, and rank-1 total correlations not mechanically forced to +/-1. Broader non-Gaussian recovery and bootstrap calibration remain partial; nonlinear communality/correlation/proportion profile intervals are withdrawn and blocked, not a pending smoke gate. Augmented spatial-latent `unique = TRUE` also remains partial. The parser and deprecation guards retain the documented canonical/legacy boundary. |
| FG-14 | `meta_V(V = V)` | `partial` | `test-formula-grammar-smoke.R`, `test-traits-keyword.R`, `test-block-V.R` | V-only named and positional parser forms verified; wide `traits(...)` marker preservation verified; block-V helper verified; single-V inference validation remains partial |
| FG-15 | `phylo_slope()` random-slope keyword | `covered` | `test-phylo-slope.R`, `test-matrix-slope-*.R`, `test-phylo-column-slope-indep.R`, `test-phylo-slope-rhs-routing.R` | Design 130 reconciles two public routes without deprecating either. RHS equal to the declared trait column is response-column sugar: `||` expands to diagonal `phylo_indep(0 + predictors | trait)`, `|` to full `phylo_dep(...)`, including identical one-predictor models. RHS not equal to the trait column retains the historical group-indexed helper behaviour as runtime compatibility, not the new teaching model. Existing slope-only design-oracle, RHS-routing, recovery, and compatibility tests cover the phylogenetic route. |
| FG-16 | `gllvmTMB_wide(Y, ...)` legacy constructor | `partial` | `test-gllvmTMB-wide.R`, `test-wide-weights-matrix.R` | soft-deprecated in 0.2.0; new examples use `traits(...)`; removal is a later API-change decision while export remains live |
| FG-17 | Slash form `(1 \| g1/g2)` nesting | `blocked` | `test-augmented-lhs-guard.R` | parser rejects with snapshot-pinned error |
| FG-18 | `latent(..., lv = ~ x)` predictor-informed latent-score grammar | `partial` | `test-lv-parser-guard.R`; `test-julia-bridge.R`; `test-lv-gaussian-recovery.R`; `test-lv-bernoulli-depth.R`; `test-lv-native-nongaussian-guard.R`; `test-lv-family-boundary-guard.R`; `test-lv-mixed-family-first-cell.R`; `test-mixed-lv-family-harness.R`; `test-lv-reml-boundary-guard.R`; `test-lv-factor-runtime.R`; `test-lv-missing-response.R`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-28-local-r500-summary.csv`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-30-local-binomial-r500-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-source-contract.md`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-recovery-cell-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-pure-recovery-cell-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-calibration-target-summary.csv`; `test-lv-cross-family-predictor-bridge.R`; `test-mixed-gaussian-lognormal-scale.R`; `docs/dev-log/artifacts/cross-family-lv-predictor/2026-08-27-local-canary-pre-run-receipt.md`; `docs/dev-log/artifacts/cross-family-lv-predictor/2026-08-27-all-family-pre-run-receipt.md` | Ordinary unit-tier `latent(..., lv = ~ x)` now composes registered native response families under one complete-response block. Each trait has one family/link and repeated-family traits are allowed. Loadings-only rank is bounded by logical responses. Automatic `Psi` additionally passes a necessary joint mean-covariance dimension screen that counts physical loading rows, latent-predictor coefficients, and engine-free diagonal slots; an explicit extra diagonal term remains rejected. Construction tests cover IDs 0--16 and include the retained four-Gaussian rank-2 route plus verified multinomial contrast expansion. Live loadings-only rank-2/rank-3 canaries cover the five-family ordinal/multinomial route plus IDs 0--15 together. The five-family automatic-`Psi` rank-3 canary also passes the corrected necessary screen and numerical route-health checks; this does not establish generic identification, broad recovery, or calibration. The row remains partial for broad arbitrary-composition recovery/calibration, non-Gaussian masks, missing LV predictors, fixed `X + X_lv`, REML, structured tiers/sources, richer `lv` formulas, and Julia parity. Earlier Gaussian/binomial, named mixed/sentinel, pure-family HOLD, and Gaussian-anchor Wald conclusions retain their exact denominators and do not transfer. |

| FG-19 | Response-column slope helper family (`slope`, `phylo_slope`, `animal_slope`, `kernel_slope`, `spatial_slope`) | `partial` (all five helpers covered for Gaussian long-format point estimation; broader regimes deferred) | `test-fixed-column-slope-family.R`, `test-ordinary-column-slope-phylo-coexistence.R`, `test-phylo-column-slope-indep.R`, `test-phylo-slope-rhs-routing.R`, `test-spatial-column-slope.R`; Design 130 | The five helpers share `Cov(b) = K_column %x% Sigma_predictor`, add no random intercept, and use `||` for diagonal / `|` for full predictor covariance. The fixed-source gate covers one-predictor public-object identity, trait-major design, ordinary recovery, multi-predictor kernel `||`, custom response-column names, dense-kernel permutation alignment and scale metadata, malformed sources, and historical compatibility. A non-identity pedigree is compared with equivalent dense `A` and sparse `Ainv` inputs. A separate two-axis gate proves that ordinary `slope(x | trait)` retains its identity response-column source while coexisting with a species-axis `phylo_latent()` term, exposes both covariance extractors, and keeps structured-source two-axis combinations refused. The spatial gate adds opt-in trait-labelled mesh projections, exact projected unit-diagonal normalization, a `P != T` diagonal-map oracle, finite `log_kappa_spde` gradient, label-permutation and malformed-input tests, source coordinate/range/`K_column` metadata, and a small Gaussian known-DGP recovery. Its fail-closed local run passed 39/39 assertions. The current complete focused filter passed 221 assertions with no failures or warnings; six legacy recovery tests were skipped only by their explicit heavy-test environment gates. Wide grammar, latent predictor covariance, non-Gaussian or mixed families, multiple simultaneous response-column slope sources, and interval calibration remain deferred. |
| FG-20 | Response-column metadata, common fixed-effect marker, and `column_coef()` / `phylo_coef()` / `animal_coef()` / `kernel_coef()` / `spatial_coef()` | `partial` (all five public Gaussian long/wide point-model sources covered; broader regimes deferred) | Existing IID/phylo/animal/kernel evidence plus `test-column-coef-spatial-parser.R`, `test-column-coef-spatial-equivalence.R`, `test-column-coef-spatial-wide.R`, `test-column-coef-spatial-recovery.R`, `test-column-coef-spatial-edge.R`, `test-spatial-column-slope.R`, `data-raw/spatial-coef-recovery.R`; Designs 131--133 | `column_data` has exact long/wide key alignment and fixed-effect-only guards. Five public helpers accept explicit intercept/slope bases, one source, both bars, and ordinary long or `traits(...)` wide input. IID, tree/animal, and dense-kernel routes use the matrix-normal coefficient engine described in Design 131. The spatial route fixes `rho = 1` and uses the normalized projected-SPDE source from Design 130 while estimating `kappa` and `Sigma_coef`. Spatial evidence covers exact warning-free `spatial_slope()` identity for no-intercept bases under both bars, exact long/wide equality through objective, gradient, report and fitted values for the C3/C4 grand-intercept/grand-slope model, full/diagonal maps, label permutation, strict mesh validation, overlap and multiple-spatial-axis fences, missing-response and rare-level acceptance, extraction metadata, and small gradients. A retained 25-column deterministic campaign adds bounded point recovery of fixed effects, `Sigma_coef`, correlation, range, and `K_spatial`, plus near-zero, +/-0.8, and near-boundary correlations, low and mesh-extent-limited ranges, and small/large variances. The extent-limited large-range cell establishes ordering only, not precise range recovery. Estimated animal rho, spatial IID mixtures or estimated spatial rho, intervals, non-Gaussian regimes, latent coefficient covariance, simultaneous coefficient sources, and broad calibration remain unclaimed; every current `*_slope()` helper remains warning-free and non-deprecated. |

Parser addendum (2026-07-05): `test-canonical-keywords.R` now guards the
documented positional control arguments that issue #582 identified as a
silent-fallback class: `spatial_latent(0 + trait | coords, 2, TRUE)` keeps
`d = 2` plus the unique-spatial marker (FG-13 / SPA-02),
`animal_latent(id, 2, ...)` keeps `d = 2` (ANI-05),
`indep(0 + trait | unit, TRUE)` keeps `common = TRUE` (FG-07), and
`kernel_latent(unit, K = K, 2, ...)` keeps `d = 2` (KER-02). This is
parser-contract evidence only; it does not change family recovery,
source-specific `lv`, interval-calibration, or mixed-family CI claims.

### Section 2 — Response families (15 advertised)

Row-owner: **Gauss** (TMB likelihood per family).

| ID | Family | Status | Test evidence | Notes |
|----|--------|--------|---------------|-------|

**M1 canonical scope override (2026-07-20):** historical sentences in this
section that call the structured augmented-slope grid "complete for every
family" are superseded. They mean complete only for the then-registered,
route-specific core cells. Lognormal and Student-t remain C1-partial runtime
admissions under RE-14, and binomial cloglog remains reserved for structured
augmented slopes. Ordinary-family coverage such as FAM-04 is unaffected.

| FAM-01 | gaussian (identity) | `covered` | many tests | M0 baseline |
| FAM-02 | binomial (logit) | `covered` | `test-m2-2a-binary-recovery.R`, `test-m2-2b-binary-cis-extractors.R`, `test-m2-2b-glmmTMB-cross-check.R`, `test-multi-trial-binomial.R`, `test-stage33-non-gaussian.R`, `test-tmb-ad-safe-clamps.R` | M2.2-A: Σ recovery at d = 1. M2.2-B: CIs (Wald + Fisher-z + bootstrap) + 4 ratio extractors + glmmTMB cross-package agreement. 2026-07-05 issue #658 replaces AD-value ternary probability clamps with CppAD conditional clamps; `test-multi-trial-binomial.R` passed under `NOT_CRAN=true`. |
| FAM-03 | binomial (probit) | `covered` | `test-m2-2a-binary-recovery.R`, `test-stage33-non-gaussian.R`, `test-tmb-ad-safe-clamps.R` | M2.2-A walks; Σ recovery + identification (σ²_d = 1 by construction). The shared AD-safe probability clamp remains the logit/probit path; cloglog has its own log-scale density path. |
| FAM-04 | binomial (cloglog) | `covered` | `test-m2-2a-binary-recovery.R`, `test-stage33-non-gaussian.R`, `test-tmb-ad-safe-clamps.R`, `test-isdm-developer-fit.R` | M2.2-A walks; Σ recovery + σ²_d = π²/6 verified. The shared engine now evaluates cloglog binomial rows directly on the log scale, with focused left/right-tail objective, gradient, Hessian, and multi-trial checks; the generic binomial probability clip does not apply to this link. |
| FAM-05 | betabinomial | `covered` (intercept); slope C1 `partial` via RE-14 | `test-betabinomial-recovery.R`, `test-matrix-betabinomial.R`, `test-lme4-style-weights.R`, `test-family-slope-recovery.R` | Recovery green (local measurement 2026-05-30: 8 + 31 assertions, 0 fail). 2026-07-05 closes issue #634 for the single-column beta-binomial `weights = n_trials` contract: beta-binomial rows now route weights to `n_trials` and force `weights_i = 1`, matching binomial trial-count semantics instead of double-applying trials as likelihood multipliers. **2026-08-01:** `phylo_indep(1 + x \| species)` C1 slope admission under #388 / RE-14 — multi-trial `cbind(succ, fail)` DGP at large N (`n_sp = 200`, trials = 15); see RE-14. |
| FAM-06 | poisson (log) | `covered` | `test-stage33-non-gaussian.R`, `test-crosspkg-poisson-glmmTMB.R` | Cross-package light sanity check present on main (commit `41d80e3`): two-trait stacked Poisson(log) `value ~ 0 + trait + (1 \| site)` fixture agrees with glmmTMB on per-trait intercepts and shared random-intercept SD within 0.05 absolute bands; no replicates or grid, per cross-package policy. |
| FAM-07 | nbinom1 | `covered` | `test-matrix-nbinom1.R` (31 assertions, heavy-gated), `test-tiers-nbinom1.R` (38 assertions, heavy-gated) | Was exported-but-unwired (overclaim caught, spike 2026-05-30). **Wired 2026-05-30** on review-gated branch `agent/trackd-nbinom1-wiring` (`09d4f58`): `nbinom1` = fid 15, full R↔C++ lockstep, correct NB1 linear variance (Var = μ·(1+φ), verified flat-in-μ); φ recovers to −0.9% bias when identifiable (φ-vs-latent-variance confound documented honestly); `test-matrix-nbinom1.R` self-heals 3 skips -> 31 pass; all regression families green. **Un-skipped 2026-05-31** (`agent/nbinom1-unskip`): the stale "not wired" construct-fail skip in `skip_unless_healthy_nbinom1()` is removed (a construct failure now FAILS hard; the non-convergence/non-PD health skip is kept; the `skip_if_not_heavy()` env gate is kept). Re-verified on main wiring: conv == 0, PD Hessian, phi (true 2.0) recovers to mean ~1.87 (about -6% bias) on the unit cell, intercepts within 0.19 (tol 0.40); `devtools::test(filter = "nbinom1")` under `GLLVMTMB_HEAVY_TESTS=1` is 31 pass / 0 fail / 0 skip, and skips cleanly (3 skips) when the heavy gate is off. **Tier coverage extended 2026-05-31** (`agent/nbinom1-tier-coverage`, gap G3): `test-tiers-nbinom1.R` adds a representative structured-tier parity set mirroring the nbinom2 / poisson tier tests, one structural cell per `test_that`, all heavy-gated and honest-skip-guarded on construct-fail / non-convergence / non-PD Hessian: (a) UNIT `indep(0 + trait | unit)` -- the diagonal "clean trio" cell `test-matrix-nbinom1.R` does not already walk (it covers ordinary latent, explicit compatibility diagonal, and the explicit-Psi compatibility pair); asserts conv == 0, PD Hessian (`sd_report$pdHess`), `indep_B` flag, per-trait phi finite-positive with mean in the [phi/3, 3*phi] band, intercepts within 0.40; (b) PHYLO `phylo_unique(species)` (star tree, n_sp = 50, seed 101 -- the sibling's seed 2025 lands non-PD under the genuine NB1 draw `size = mu / phi`, a {101, 7, 42, 303, 404, 11} sweep fixed 101); asserts conv == 0, PD Hessian, `phylo_rr` flag, phi FINITE only (the phi<->phylo-variance confound legitimately pulls some per-trait phi to 0, matching the nbinom2-phylo mean-dependent convention), total phylo variance recovers via `extract_Sigma(level = "phy", part = "total")` inside the 4x trace band of `test-matrix-poisson-phylo.R`, intercept-mean within 0.6 (log); (c) SPATIAL `spatial_unique(0 + trait | site)` (per-trait independent SPDE, 100 sites, seed 20260529); asserts conv == 0, PD Hessian, `spde` flag, phi finite, kappa finite-positive, per-trait `log_tau_spde` finite, intercept-mean within 0.6 (log). All bands INHERITED from the nbinom2 / poisson / gamma sibling tier tests (no per-cell widening). `devtools::test(filter = "nbinom1")` under `GLLVMTMB_HEAVY_TESTS=1` is now 69 pass / 0 fail / 0 skip (31 matrix + 38 tiers); the 3 new tier cells skip cleanly when the heavy gate is off. Downstream extractor/simulate/profile NB1 wiring is a flagged follow-up. `truncated_nbinom1()` remains unwired. **Enum mirror repaired 2026-07-05** (`codex/r-bridge-grouped-dispersion`, #676): `R/enum.R` now mirrors the multivariate runtime `family_to_id()` encoding (`nbinom1 = 15L`; `poisson = 2L`; `lognormal = 3L`; delta and ordinal ids included) and carries only the binomial link ids (`logit = 0L`, `probit = 1L`, `cloglog = 2L`). `test-enum-runtime-ids.R` guards against reintroducing the stale sdmTMB/single-response enum or unsupported mixture/gengamma/truncated_nbinom1 ids. **Cross-package check added 2026-05-31** (`agent/distribution-validation-fills`): `test-crosspkg-nbinom1-glmmTMB.R` adds the NB1-vs-glmmTMB equivalence cell (the standalone fixture the poisson / nbinom2 cross-package tests had but nbinom1 lacked), mirroring `test-crosspkg-nbinom2-glmmTMB.R` exactly. On a shared 2-trait `value ~ 0 + trait + (1|site)` NB1 fixture (n = 300, glmmTMB given `dispformula = ~ 0 + trait` to match per-trait phi), gllvmTMB and glmmTMB agree to <1e-3 on per-trait intercepts and the shared random-intercept SD (0.05 absolute bands, inherited) and to ~1e-5 on per-trait dispersion phi (25% relative band, inherited). gllvmTMB's NB1 (Var = mu*(1+phi)) shares glmmTMB's nbinom1 parameterisation directly -- `exp(fixef$disp) == report$phi_nbinom1`, no NB2-style reciprocal. Honest-skip on glmmTMB-absent / non-convergence / weakly-identified phi (phi -> 0 or -> Inf in either engine). 3 cells, heavy not required (skip_on_cran-gated like its siblings); local measurement 2026-05-31 under `NOT_CRAN=true`: 3 pass / 0 fail. **Augmented slopes ADMITTED 2026-06-03** (`claude/slope-grid-residuals`, #350): nbinom1 was the last missing then-registered route-specific core family in the dated structured non-Gaussian random-slope grid. A new nbinom1 `*_VALIDATION` cell on the hardest (full-unstructured `phylo_dep`) path (`test-matrix-slope-phylo-dep.R`) drives the real API; the recovery gate decided it. nbinom1's augmented-slope identifiability was genuinely uncertain (smoke-only even intercept-only, board #340), so the outcome is evidence-based — and the gate came back green: the cell converges PD and recovers non-skipped at the escalated `n_sp = 400` (the prior `n_sp = 300` skipped non-PD). nbinom1 (id 15) is therefore ADMITTED and stays on all six slope-guard allowlists; see PHY-18 for the gate detail. The then-registered route-specific core grid was complete at that date; IDs 3/9 remain C1-partial under RE-14 and binomial cloglog remains reserved. |
| FAM-08 | nbinom2 | `covered` | `test-nb2-recovery.R`, `test-crosspkg-nbinom2-glmmTMB.R` | Recovery plus cross-package light sanity check present on main (commit `41d80e3`): two-trait stacked NB2 `value ~ 0 + trait + (1 \| site)` fixture with glmmTMB `dispformula = ~ 0 + trait` agrees on per-trait intercepts, shared random-intercept SD, and per-trait NB2 dispersion phi within inherited bands. |
| FAM-09 | gamma (log) | `covered` | `test-family-gamma.R`, `test-matrix-gamma-unit.R`, `test-matrix-slope-gamma.R`, `test-tiers-gamma.R`, `test-gamma-recovery-depth.R` | Recovery + slope + tiers green (local measurement 2026-05-30: 15 + 32, 0 fail). Recovery-DEPTH cell added (#443 / #348): joint trait fixed effects + CV + full between-unit `Sigma_B` (variances + off-diagonal correlation) recover non-skipped in CI at n_unit = 120. Prior "smoke only" under-claimed. 2026-07-04 closes issue #659 for observed responses: Gamma rows now fail loudly on `y <= 0` before TMB evaluation, while masked response sentinels remain exempt. The 2026-07-05 `rho:unit:1,2` penalty-profile smoke is retained only as historical/internal negative evidence: nonlinear rho profiles are withdrawn under CI-07 and it supplies neither a public route nor empirical interval calibration. |

FAM-09 addendum (2026-07-05, local #622): ordinary Gamma now uses per-trait
`phi_gamma` shape in native R/TMB instead of borrowing the shared
Gaussian/lognormal `sigma_eps`. The fast guard in `test-family-gamma.R`
checks that mixed Gaussian/Gamma fits estimate Gaussian `sigma_eps` and Gamma
CV independently; the 15-family residual fixture, unit/slope/tier/depth Gamma
tests, and likelihood docs now read Gamma residuals from `phi_gamma`.
| FAM-10 | beta (logit) | `covered` | `test-beta-recovery.R`, `test-matrix-beta-unit.R`, `test-matrix-slope-beta.R`, `test-tiers-beta.R`, `test-tmb-ad-safe-clamps.R` | Recovery + slope + tiers green (local measurement 2026-05-30: 15 + 36, 0 fail). 2026-07-05 issue #658 replaces the AD-value ternary response clamp with a CppAD conditional clamp; `test-beta-recovery.R` passed under `NOT_CRAN=true GLLVMTMB_HEAVY_TESTS=1`. |
| FAM-11 | lognormal | `covered` for ordinary family recovery; `partial` (C1) for structured augmented slopes under RE-14 | `test-family-lognormal.R`, `test-matrix-lognormal.R`, `test-missing-data-robustfix.R`; C1 admission only: `test-family-slope-recovery.R` | Recovery green (local measurement 2026-05-30: 6 + 37, 0 fail). Prior "smoke only" under-claimed. 2026-07-04 tightens the non-positive response regression so lognormal rows fail with the shared strict-positivity guard before TMB evaluation. The 2026-07-04 response-prediction repair closes issue #614: per-row inverse-link prediction for family id 3 now returns the conditional mean `exp(eta + sigma_eps^2 / 2)`, not the median `exp(eta)`. The augmented-slope fixture supplies one `phylo_indep(1 + x \| species)` seed and pooled slope-SD plausibility only; it does not cover other covariance modes or inference. |
| FAM-12 | student-t | `covered` for ordinary family recovery; `partial` (C1) for structured augmented slopes under RE-14 | `test-student-recovery.R`, `test-matrix-student.R`; C1 admission only: `test-family-slope-recovery.R` | Ordinary recovery is green (local measurement 2026-05-30: 13 + 49, 0 fail). The augmented-slope fixture supplies one `phylo_indep(1 + x \| species)` seed and pooled slope-SD plausibility only; it does not cover other covariance modes or inference. |
| FAM-13 | tweedie | `covered` | `test-tweedie-recovery.R`, `test-matrix-tweedie.R` | Recovery green (local measurement 2026-05-30: 13 + 37, 0 fail). |
| FAM-14 | ordinal_probit | `covered` | `test-ordinal-probit.R`, `test-matrix-ordinal-unit.R`, `test-matrix-slope-ordinal.R`, `test-tiers-ordinal.R`, `test-ordinal-recovery-depth.R`, `test-tmb-ad-safe-clamps.R`, `test-matrix-ordinal-kernel-animal.R`, `test-sanity-categorical.R`; `dev/ordinal-degeneracy/probe-criteria.md`, `dev/ordinal-degeneracy/pass-criteria-ordinal.md`, `dev/ordinal-degeneracy/pass-criteria-curvature.md` | Recovery (cutpoints + intercepts, K=2/3/4) + slope + tiers green (local measurement 2026-05-30: 21 + 30, 0 fail). Recovery-DEPTH cell added (#443 / #348): joint cutpoints + intercepts + full between-unit `Sigma_B` recover non-skipped in CI at n_unit = 150. 2026-07-05 issue #658 replaces the ordinal probability lower clamp with `CppAD::CondExpLt`; `test-ordinal-probit.R` passed under `NOT_CRAN=true`. Prior "smoke only" inaccurate. Cross-package mirt `graded` check still outstanding. 2026-08-17 (PA3, Design 123 paper alignment): `test-matrix-ordinal-kernel-animal.R` adds the kernel/animal source tiers for this family — `kernel_latent`/`kernel_indep`/`animal_latent` measured numerically equivalent to their `phylo_*` twins (matched TMB objective both directions, tol 1e-6; matched phy-tier Sigma to 1e-4), `animal_indep` construct/converge/diagonal-Sigma smoke, and a loose-band (3x) `animal_latent` recovery smoke; all 5 cells non-skipped in the 2026-08-17 local heavy run (0 fail, 0 skip). **DETECTOR COVERAGE (2026-08-17, issue #897 -- this row previously had NONE):** `check_gllvmTMB()` now emits an `ordinal_liability_loading` row for any fit carrying a fid 14 trait, with two loading arms (`ordinal_loading_runaway_thresh`, `ordinal_loading_absolute_thresh`) that ship **DISARMED at `Inf`/`Inf`** after calibration found no threshold that separates the classes (see CALIBRATION below). This matters for this row specifically because fid 14 traits drop the auto-Psi at parse time (`auto_unique_off_family`, `R/fit-multi.R`), so a pure-ordinal fit has no `report$sd_B` and every `near_zero_psi_*` row is dark by design -- these two arms are the ONLY degeneracy coverage a default ordinal fit gets, and they currently contribute none by default. **Mechanism CORRECTED 2026-08-17** (S1 probe, rule frozen at `e932cf37`, verdict at `b33d3b90`, corrected per `dev/ordinal-degeneracy/pass-criteria-dichotomised.md`; 60-fit grid, 24 degenerate): **"NOT link saturation" is solidly evidenced** -- flat-row share EXACTLY 0 on all 24 (the cutpoint underflow is never reached) -- **but "therefore category-level separation" is now the residual hypothesis, not a demonstrated one**. The third leg originally cited for separation (24/24 dichotomised refits firing the existing binomial detector) is ELIMINATED as evidence: scored properly on 315 fits across four arms, the same check fires on 86.3% of healthy fits (67.8% healthy, 93.3% transport, 100% mixed), so it discriminates nothing -- it fires on the 24/24 degenerate fits BY CONSTRUCTION. The runaway is single-column (loading 44.2 against a true `max\|Lambda\|` 4.79), descriptive of the pathology's shape, not evidence for either mechanism. A saturation arm has no empirical basis on the surviving evidence and was deliberately not built; that choice does not depend on the separation question being resolved. **CALIBRATION (`dev/ordinal-degeneracy/pass-criteria-ordinal.md`, frozen rule scored 2026-08-17; 315 fits, `n = 100/400`, four pre-registered arms; sensitivity on the `degenerate` arm's 41 `rel_frob > 10` fits, false positives ARM-LEVEL across `healthy`+`transport`+`mixed` combined, 255 fits, matching the frozen pre-registration -- a first scoring pass that relabelled truth per-fit instead of arm-level was caught by a D-43 panel and withdrawn; see the correction note in `pass-criteria-ordinal.md`):** the frozen conjunction (sensitivity $\ge$ 90% AND zero false positives) was **NOT achieved at any threshold, and none can be**: the healthy pool reaches `max_loading_unit` 216.9 while the degenerate arm starts at 13.5, so the classes are not separable on this statistic at all. At binomial's own threshold of 6 (**the binomial default at the time this campaign was scored; it moved to 8 on 2026-08-18 under #1098 -- see the DIA-08 row. The figure below is retained as measured, not restated against the new default**), O2 sensitivity 100.0% / FP **39.2%** and O1 61.0% / FP 28.6% -- worse than the 25% false-positive rate #897 complains about in binomial (**that 25% was subsequently attributed, 2026-08-18 under #1098: 232 of 232 healthy false positives came from the single `extreme_magnitude` arm, and the retune to 8 brought it to 15.5%; the comparison above stands against the 25% figure as it stood when scored**); at 20, O2 95.1%/27.8%, O1 43.9%/20.4%; at 40, O2 80.5%/11.0%, O1 36.6%/8.6%; the first zero-FP point is threshold 250, where sensitivity is 0.0%. The failure mode is heterogeneous per-trait loading scales, concentrated in the transport arm -- an absolute liability-scale threshold cannot transport across them; a future screen needs a scale-invariant statistic, not a better constant. **DISPOSITION: both arms ship DISARMED at `Inf`/`Inf`**, the pre-registered fallback; the row still computes and reports its statistics for a user to arm explicitly. Grid trim stated: `n = 1600` DROPPED and `n = 400` seeds halved for the D-139 budget, so there is **no `n = 1600` evidence**. The `cutpoint_span`/`loading_over_span` variant is computed for the campaign but NOT wired into `flag`/`status` (its circularity precondition was untested). **Fit-time warnings are NOT wired for fid 14** -- `check_gllvmTMB()` surface only; that behaviour change is left to the maintainer. This is **NOT a recovery-status change**: FAM-14 stays `covered` on the same recovery evidence as before. **DETECTOR-S2c (2026-08-17, `dev/ordinal-degeneracy/pass-criteria-curvature.md`, frozen 18:07, scored 18:32, 450 fits/0 errors, independently scored): TWO MORE CANDIDATES ELIMINATED -- BOTH ARMS FAIL.** Arm C (loading-block curvature: the Schur complement of the `theta_rr_B` block within `sd_report$cov.fixed`, i.e. the loading-only curvature profiled over every other fixed effect, not the naive conditional block) reaches sensitivity **1.1-5.3%** (reading-dependent on a genuine scoring-rule ambiguity, recorded as a post-hoc amendment in `pass-criteria-curvature.md` rather than a frozen-text edit) against the pre-registered $\ge$90% target. Arm D (multi-start objective disagreement across `n_init = 5` restarts, scale-relative jitter) reaches **7.3%**. The exploratory `flag_C | flag_D` OR-combination is **identical to Arm D alone (7.3%)** -- Arm C adds nothing incremental. Both fail by **83-89 percentage points under every scoring reading**, a margin the recorded ambiguities (FP count 0-4 out of 290/92) never come close to closing. **The independence precondition PASSES for both statistics** (correlation against the already-eliminated `max_loading_unit`: 0.538 and -0.452, refusal bar 0.8) -- this null is not a circularity artefact; two genuinely new, genuinely independent information sources were measured and neither distinguishes degenerate ordinal optima from healthy ones on this evidence. **Both arms remain UNIMPLEMENTED** (the pre-registered ship-disarmed fallback); this campaign made no change to `O1`/`O2`, which stay at `Inf`/`Inf` exactly as before. Full verdict: `dev/ordinal-degeneracy/results/scoring-verdict.md`; raw data: `dev/ordinal-degeneracy/results/campaign-curvature-scored.csv`. The count of eliminated ordinal degeneracy candidates is now **seven** (the four loading/cutpoint statistics and the dichotomisation refit counterfactual named in `pass-criteria-ordinal.md`'s "SEARCH STOPPED" section, plus these two) -- see `docs/design/123-multinomial-structured-surface.md` §8.2a for the consolidated list and what a future attempt should not retry. |
| FAM-15 | truncated_poisson / truncated_nbinom2 | `partial` | `test-truncated-recovery.R`, `test-matrix-truncated.R` | Runtime ids 10 and 11 are admitted and have focused recovery / structural smoke evidence. Local heavy verification 2026-07-05: `test-truncated-recovery.R` 15 pass and `test-matrix-truncated.R` 30 pass. `truncated_nbinom1()` is exported as a constructor but not runtime-admitted. |
| FAM-16 | truncated_nbinom1 | `blocked` | `test-enum-runtime-ids.R` | Constructor exists for compatibility, but there is no multivariate runtime id and `gllvmTMB()` must fail loud with "Unsupported family" until likelihood wiring and recovery tests land. `censored_poisson` MOVED to FAM-25 (Arc E, issue #1244, 2026-09-03; renumbered from FAM-24 on 2026-09-03 because the sibling `ordinal_logit` branch, PR #1250, also claimed FAM-24 -- ordinal_logit merges first and keeps it) -- it now has a runtime id (21) and is no longer blocked. |
| FAM-17 | delta_lognormal / delta_gamma standard hurdle families | `covered` for admitted fixed-effect routes; other delta constructors `blocked` | `test-delta-gamma-recovery.R`, `test-delta-lognormal-recovery.R`, `test-enum-runtime-ids.R` | Runtime ids 12 and 13 admit only the standard `delta_lognormal()` / `delta_gamma()` pair. Single-family fixed-effect delta recovery is green; the Poisson-link parameterisation and exported delta mixture/gengamma/beta/truncated-NB constructors fail loud until likelihood wiring and recovery tests land. **Superseded 2026-07-05 (Design 02 §Hurdle/delta):** the old "two-part = fixed-effect only; species correlation undefined" (Design 62) is lifted by constraining the latent to the positive submodel (occurrence fixed-effects-only) → single latent scale → cross-family correlation defined on the positive-part residual (now MIX-10 `partial` / route-only). **Build caveat, evidence-corrected (Codex lane):** the 2026-07-05 probe's apparent boundary (`convergence = 1`, `pdHess = TRUE`) read `pdHess` off the phantom `fit$sdr$pdHess` field (`NULL`, which reads `FALSE`), not the real `fit$sd_report$pdHess`. A 6-seed reproduction (`data-raw/diagnostics/2026-07-05-fam17-delta-latent-boundary-repro.R`, `data-raw/diagnostics/2026-07-05-fam17-delta-latent-boundary-repro-multiseed.R`) shows the latent delta-lognormal cell converges cleanly across 6/6 seeds under the real `fit$sd_report$pdHess`: `convergence = 0`, `pdHess = TRUE`, max|grad| ~1e-3 to 1e-2, intercepts and sigma recover. The earlier `convergence = 1` was an nlminb termination-code transient tied to a specific seed/start, not an identifiability failure, so no convergence fix is warranted. Per the hard guard, this remains NOT calibrated-CI evidence for advertising `latent`-on-delta-main, and non-Gaussian Psi stays un-zeroed. |
| FAM-18 | gamma_mix / lognormal_mix / nbinom2_mix | `blocked` | `test-enum-runtime-ids.R` | Mixture constructors are exported for API continuity, but there is no multivariate runtime id and `gllvmTMB()` must fail loud with "Unsupported family" until likelihood wiring and recovery tests land. |
| FAM-19 | gengamma | `blocked` | `test-enum-runtime-ids.R` | Constructor is exported for API continuity, but there is no multivariate runtime id and `gllvmTMB()` must fail loud with "Unsupported family" until likelihood wiring and recovery tests land. |
| FAM-20 | multinomial (baseline-category logit / softmax, unordered K>=3) | `covered` for fixed-effect recovery only; named Tier-2 routes recorded separately as `partial` | `test-multinomial.R`; `test-enum-runtime-ids.R`; `docs/dev-log/after-task/2026-07-16-multinomial-fam20-promotion.md`; `docs/dev-log/after-task/2026-07-16-multinomial-fam20-reaudit.md`; `docs/design/123-multinomial-structured-surface.md` | **IN:** fixed-effect recovery for one unordered response, baseline selection, probability prediction, and grouped softmax simulation. **PARTIAL:** Design 123's arc (Slices 1-4, 2026-08-16) admitted a bounded structured surface, each gated on a signed recovery campaign, recorded in FAM-20A through FAM-20F below and consolidated in `docs/design/123-multinomial-structured-surface.md`. **BLOCKED (post-arc remainder, 2026-08-16):** multiple multinomial traits per fit; augmented (intercept + slope) forms of every admitted keyword; explicit `dep()`/`unique()`/`indep()` at the unit tier (only the default-`unique = TRUE` `latent()` cell works there) and `latent()`/`dep()` at the cluster/cluster2 tiers; the `unit_obs` grouping tier; `meta_V()`/`equalto()` (confirmed Gaussian-only, no route on a categorical-contrast pseudo-trait); `integration = "va"` (a separate fence, DR-31); `mi()` predictor terms; multi-kernel (>1 `kernel_*()` name in one fit); missing responses or `weights` combined with any newly-admitted structured term (fixed-effects-only missing-response support is unaffected); calibrated intervals on any structured tier; and Julia parity. `phylo_scalar()`/`animal_scalar()`/`kernel_scalar()`/`spatial_scalar()` and `common = TRUE` at the cluster/cluster2 tier are **refused**, not merely blocked -- a single shared level across the K-1 contrasts has no interpretable null on the (I+J) contrast geometry (null-DGP evidence in FAM-20D/FAM-20F). AGHQ is orthogonal to this fence, not rejected by it. **DETECTOR COVERAGE (2026-08-17, issue #897 -- this family previously had NONE):** `check_gllvmTMB()` emits a `multinomial_contrast_degeneracy` row screening the `K-1` baseline-contrast pseudo-traits of each response, with three armed arms -- **M1** `contrast_variance_collapse` (`multinomial_collapse_floor = 1e-10`), **M2** `contrast_rail` (`multinomial_rail_thresh = 0.99`, evaluated only where the tier's rank is $\ge$ 2 so rank-1 row proportionality cannot fire it), **M3** `spatial_range_collapse` (`multinomial_range_collapse_thresh = 0.02`) -- the M1 sibling/relative sub-arm (`multinomial_collapse_rel_thresh`) stays disarmed at `Inf`, untested. Calibration (`dev/multinomial-structured/pass-criteria-detector-mn.md`, criteria frozen at `f6552ee9` BEFORE results, verdict at `6f34568e`; 128 fits, 122 conv+PD): **M1 6/7 labeled plus 7/7 fully out-of-sample** (FAM-20D's diagonal-V replication cell, 0/13 FP there), **M2 8/8 labeled plus 4/4 individually-railed fits hidden inside a cell whose AGGREGATE gate passed** (verified by refitting: $\hat\rho = \pm 1.00000$; two non-firing controls 0.48997 and -0.14534), **M3 3/3 after the scope fix** (0/3 before it). **Zero false positives on 40 informative healthy fits**, with the honest denominator stated: the s4 `re_int` cell's 20 fits are EXCLUDED because a bare `(1 \| group)` multinomial fit has no loading tier, so no row is emitted (`det_present` 0/20) and those fits carry zero specificity information -- rule-of-three bound **~7.5%, not a verified zero** (a $\ge$ 500-fit healthy arm would be needed to approach ~0.6%, and is outstanding). Rank-1 M2 suppression is proven out-of-sample (0/20 on healthy `d = 1` fits despite $\hat\rho = \pm 1$ by row proportionality). M1 fires 8/8 on a null DGP ($V_{true} = 0$) BY DESIGN -- a truly zero component is indistinguishable from a collapsed one at the fit level, which the row's action text says. **Fit-time warnings are NOT wired for fid 16** -- `check_gllvmTMB()` surface only; that behaviour change is left to the maintainer. This is a diagnostic-coverage addition, NOT a status change: every admitted-set and campaign verdict in FAM-20A-F stands exactly as recorded. |

#### FAM-20 named partial routes (2026-07-21)

The root FAM-20 row remains the covered fixed-effect claim. The bounded,
later-admitted Tier-2 routes are recorded separately below so neither can be
mistaken for universal multinomial covariance support.

| ID | Capability | Status | Test evidence | Notes |
|---|---|---|---|---|
| FAM-20A | Phylogenetic among-category multinomial covariance V | `partial` | `test-multinomial.R`; `test-link-residual-multinomial.R`; `docs/dev-log/after-task/2026-07-17-tier2a-phylo-multinomial-build-recovery.md`; `docs/dev-log/after-task/2026-07-17-tier2a-regularization-negative-result.md` | **IN (narrow):** `phylo_latent()` reports the fitted $(K-1)\times(K-1)$ contrast-scale $V$ with `part = "shared", link_residual = "none"`; the separate total/`"auto"` extraction adds the fixed $(pi^2/6)(I+J)$ softmax residual. **PARTIAL:** one-per-species recovery is data-hungry and a fixed regularizer did not make it reliable. No universal recovery or calibrated-interval claim. |
| FAM-20B | Ordinary shared-latent multinomial cross-family extraction and intervals | `partial` | `test-cross-family-multinomial.R`; `test-link-residual-multinomial.R`; `test-cross-family-intervals.R`; `docs/dev-log/after-task/2026-07-18-tier2b-item2aii-cross-family.md`; `docs/dev-log/after-task/2026-07-20-m1-heavy-baseline.md` | **IN (narrow):** one multinomial trait can share ordinary `latent()` factors with other families; extraction returns its explicit K-1 contrast block and the cross-family summary. Default `latent()` `Psi` is allowed for identified partners; the multinomial contrast diagonal is unidentified with one categorical draw per unit and is mapped off by the current engine even when replication could identify it in principle. Explicit multinomial `unique()`/`indep()` stays fenced. Wald/bootstrap interfaces are target-specific and uncalibrated. Nonlinear profile requests are withdrawn and typed-refusal tested. Multiple multinomials, slopes, and unlisted tiers remain blocked. |
| FAM-20C | Pedigree/kernel-sourced among-category multinomial covariance V (`animal_latent()` / single-name `kernel_latent()`) | `partial` | `test-matrix-multinomial-phylo.R`; `test-multinomial-fence.R`; `dev/multinomial-structured/pass-criteria-s1.md`; `dev/multinomial-structured/pass-criteria-s1b.md`; `docs/design/123-multinomial-structured-surface.md` | **IN (narrow):** Design 123 Slice 1 (2026-08-16) admits loadings-only (`unique = FALSE`) `animal_latent()` and SINGLE-NAME loadings-only `kernel_latent()` alongside `phylo_latent()` (FAM-20A) for the among-category surface. Both are PURE ENGINE SUGAR over the same `phylo_rr` route `phylo_latent()` uses -- no engine/C++ change; verified numerically identical to `phylo_latent()` (matched TMB objective at each other's converged parameters, both directions, tolerance 1e-6; matched $(K-1)\times(K-1)$ `V` from `extract_Sigma(level = "phy", part = "shared", link_residual = "none")`, tolerance 1e-4). **CAMPAIGN VERDICT (signed, run to completion 2026-08-16):** the one-categorical-draw-per-species recovery gate **FAILED**: rail rate 8/20 (>6/20 threshold), IDENTICALLY for `animal_latent()`/`kernel_latent()` and `phylo_latent()` by the proven engine identity above; non-railed median $\hat\rho$ 0.695. A pre-registered **replication rescue PASSED**: `n_sp = 300`, `n_rep = 5` categorical draws per species from the SAME species liability -> rails 4/20, median $\hat\rho$ 0.680 (non-railed filter, criterion 4) $\in$ [0.35, 0.75] (true 0.6), SD ratios 0.89/0.85 $\in$ [0.5, 2.0], direction-correct 15/16 of non-railed seeds (94% -- the proportion reading of the "$\ge$16/20" criterion passes; the strict-count reading is one seed short, both recorded). **Both filters disclosed:** median over ALL 20 conv+PD seeds (unfiltered) is 0.696, also inside [0.35, 0.75] -- the headline PASS does not depend on which median is quoted. A tree-vs-star check confirms the phylogeny genuinely enters the fit ($\Delta$ logLik 29.7). **Register statement: one categorical draw per species does not identify V; five draws per species does.** No calibrated-interval claim either way. **Detector (2026-08-17):** the railing this cell's gate measures is now visible on the `check_gllvmTMB()` surface -- M2 (`multinomial_contrast_degeneracy`, `\|rho\| >= 0.99` at tier rank $\ge$ 2) fired on 4 seeds of the s1b replicated cell that its AGGREGATE gate had passed, and refitting confirms all four are genuinely railed ($\hat\rho = \pm 1.00000$). The cell-level "healthy" label was the artifact, not the detector; the s1b PASS verdict above is unchanged, but a passing aggregate gate is now known to be able to contain individually railed fits. `unique = TRUE` on either keyword, augmented-slope forms, every other `animal_*`/`kernel_*` mode, and multi-kernel (more than one `kernel_latent()` name in one fit) stay BLOCKED (typed `gllvmTMB_multinomial_structured_not_admitted`). The $(pi^2/6)(I+J)$ fixed softmax residual note from FAM-20A applies identically. See `docs/design/123-multinomial-structured-surface.md` §1/§4 for the full derivation. |
| FAM-20D | The phylo MODE axis for multinomial: full-V (`phylo_dep()`) and diagonal-V (`phylo_indep()`/standalone `phylo_unique()`), plus their `animal_*`/`kernel_*` twins | `partial` | `test-matrix-multinomial-phylo.R`; `test-multinomial-fence.R`; `dev/multinomial-structured/pass-criteria-s2.md`; `dev/multinomial-structured/pass-criteria-diagonal-v-replication.md`; `docs/design/123-multinomial-structured-surface.md` §4/§8 | **IN (narrow):** Design 123 Slice 2 (2026-08-16) admits, for all three sources (phylo/animal/kernel, single-name for kernel), the intercept-only `*_dep(0 + trait | id)` full-unstructured $(K-1)\times(K-1)$ $V$ and `*_indep(0 + trait | id)` diagonal $V$ (no among-category correlation); standalone `*_unique()` is a soft-deprecated alias of `*_indep()`. All resolve `d = n_traits` and populate the SAME `phylo_rr`/`theta_rr_phy` slot as `phylo_latent(d = n_traits)` (FAM-20A) -- `*_dep()` is the IDENTICAL unconstrained packed-triangular parameterisation (`gll_unpack_rr_loadings()`, src/gllvmTMB.cpp), not merely V-equivalent. **CAMPAIGN VERDICT (signed, run to completion 2026-08-16):** `phylo_dep()`'s gate **FAILED**: rails 8/20, median $\hat\rho$ 0.781. The FAM-20C replication rescue (`n_sp = 300`, `n_rep = 5`) **transfers EXACTLY** to this cell, since `phylo_dep()` IS the identical parameterisation to the `phylo_latent(d = 2)` fit s1b ran. `phylo_indep()`'s AS-RUN cell had a DGP mismatch (correlated truth fed to a diagonal-truth-only cell) -- recorded openly rather than silently corrected; the corrected rerun (diagonal truth, sd 0.8/0.5) also **FAILED**: larger contrast variance recovers fine (median ratio 0.78, 17/20 in band), but smaller variance collapses (median ratio 0.24, 9/20 in band) -- **7/20 seeds collapse the smaller contrast variance to NUMERICAL ZERO (ratio $\le$ 1e-9), each with convergence = 0 AND a PD Hessian, and (as run) NO runtime degeneracy detector fired** (`R/diagnose.R`'s Heywood/degeneracy gate checked `family_id == 1` -- binomial -- only; issue #897's class of gap) -- and the planted-zero criterion fails outright -- a full-$V$ `phylo_latent()` refit on this diagonal-truth data rails to a median $\hat\rho$ magnitude of 1.0. **REPLICATION RESCUE VERDICT for the diagonal-V mode (2026-08-17): MEASURED, and it FAILS -- the rescue does NOT transfer.** The caveat this row previously carried as UNTESTED is now closed with a NEGATIVE result. The cell was pre-registered BEFORE any results (`dev/multinomial-structured/pass-criteria-diagonal-v-replication.md`, criteria frozen at `1925bc24`; verdict at `5e745dcd`), fit `phylo_indep()` on diagonal truth at `n_sp = 300`, `n_rep = 5` -- the exact regime that rescued FAM-20C -- and returned 20/20 conv+PD with **collapse 7/20, IDENTICAL to the unreplicated baseline of 7/20** (gate $\le$ 2/20, FAIL); median ratios 0.82/0.54 in band (PASS), per-seed in-band 16/20 and **12/20** (gate $\ge$ 14/20 each, FAIL on the second contrast). The mechanism split is the finding: replication rescues the FULL-RANK cell (s1b rails 8/20 $\to$ 4/20) because that pathology is *correlation railing*, which more information per species fixes; it does nothing here because the diagonal mode's pathology is *small-variance collapse*, which it does not. **This register must NOT extrapolate the s1b/FAM-20C pass to the diagonal-V mode, in either direction -- not as a pass, and not as an open question.** Amendment recorded rather than silently applied: the pre-registered `sd_true = c(0.8, 0)` is unrunnable (singular $V$, Cholesky fails) and `c(0.8, 0.05)` was substituted. Planted-near-zero sub-cell: variance ratio 0.0175 against a true 0.0039 **PASS** (the model does not invent variance where there is none); rails 10/10 **FAIL as scored**, with the honest reading attached and the criterion NOT retro-fitted -- a correlation over a ~zero variance is undefined, so railing is the expected numerical consequence, the same signature as the Arc-1 null probe below. **Detector cross-check on this cell, fully out-of-sample (these fits were never in the detector's calibration set): the M1 arm of `check_gllvmTMB()`'s `multinomial_contrast_degeneracy` row flags 7/7 collapse seeds and 0/13 non-collapsed fits** -- the silent-collapse half of this row's #897 complaint is closed on the `check_gllvmTMB()` surface (fit-time warnings are still NOT wired; see DIA-08). Null-DGP evidence ($V_{true}=0$): `phylo_indep()` correctly recovers near-zero variance (5/5 seeds), but `phylo_dep()`'s $\hat\rho$ rails toward $\pm1$ in 4/5 seeds despite a PD Hessian -- see `dev/multinomial-structured/probe-scalar-null.R`. **BLOCKED:** `phylo_scalar()`/`animal_scalar()`/`kernel_scalar()` (one shared level across contrasts -- the null-DGP probe evidences why a scalar summary is not interpretable on the (I+J) contrast geometry), `unique = TRUE` on the `*_latent()` trio, augmented-slope forms of every keyword, and multi-kernel remain BLOCKED (typed `gllvmTMB_multinomial_structured_not_admitted`). The $(pi^2/6)(I+J)$ fixed softmax residual note from FAM-20A applies identically. See `docs/design/123-multinomial-structured-surface.md` §1/§4/§5 for the full derivation. |
| FAM-20E | The spatial (SPDE) mode axis for multinomial: shared fields (`spatial_latent()`), per-contrast independent fields (`spatial_indep()`), and full field covariance (`spatial_dep()`) | `partial` | `test-matrix-multinomial-spatial.R`; `test-multinomial-fence.R`; `dev/multinomial-structured/gate-check-a-proj.R`; `dev/multinomial-structured/pass-criteria-s3.md`; `docs/design/123-multinomial-structured-surface.md` | **IN (narrow):** Design 123 Slice 3 (2026-08-16) admits intercept-only `spatial_latent(0 + trait \| coords, d = k)` (loadings-only, default `unique = FALSE`), `spatial_indep(0 + trait \| coords)`, and `spatial_dep(0 + trait \| coords)`. GATE CHECK (real fit, not classifier-only; this environment has fmesher but NOT INLA, verified sufficient): `expand_multinomial_response()` duplicates each observation into $K-1$ contrast rows BEFORE mesh/`A_proj` construction; a mesh built on the user's un-expanded per-site data fails LOUD, never silently misaligned, and a mesh built on a pre-expanded coordinate frame aligns EXACTLY (0/40 mismatched site-blocks). `spatial_dep()` is VERIFIED (parser desugar) and confirmed on real fits to be `spatial_latent(d = n_traits)` under a documentary keyword -- matched TMB objective both directions, tolerance 1e-6, identical `kappa_hat`/`range_hat`. A pre-existing multinomial gate bug was fixed alongside this admission (`extract_Sigma()`'s top-level fixed-effects-only refusal did not recognise `use$spde`). **CAMPAIGN VERDICT (signed, run to completion 2026-08-16): PASSED all three cells.** Median practical-range ratios ($\text{range} = \sqrt 8 / \kappa$) 1.75 / 1.12 / 1.75 among the 14 conv+PD seeds for `spatial_latent`/`spatial_indep`/`spatial_dep` respectively (band 0.33-3.0), rails 0 / 3 / 0 of those 14 against the frozen threshold restated in its pre-registered form -- **>6/20, not ">6/14"** (an earlier draft of this row re-expressed the denominator incorrectly; restored here), non-PD 6/20 per cell (reported, excluded from the ratio band per the frozen criteria). **Per-seed dispersion, disclosed rather than left implicit in the median:** 4 of the 14 PD seeds fall outside [0.33, 3.0] for `spatial_latent`/`spatial_dep` (worst ratio 4.56); `spatial_indep` collapses the range in 6 of all 20 seeds (ratios down to 7e-5): 3 of those 6 are PD -- the rails counted under the frozen criteria, at ratios 2.3e-4 to 3.4e-4 -- and the other 3 sit among the 6/20 non-PD exclusions; a collapsed field can pass the Hessian check, the same shape as `phylo_indep()`'s zero-collapse (FAM-20D). Cross-contrast field correlation is descriptive-only per the frozen criteria, never numerically gated (reduced-rank shared-field attenuation at small `n_site` is expected). **Two usability/calibration caveats, recorded rather than resolved:** the `A_proj` pre-expansion requirement above is a real usability burden (undocumented, the internal expansion function is not exported); `n_site = 300` was **NOT** calibrated against a prior spike, unlike S1/S2's `n_sp = 800` (the PASS is real but the design was not tuned the way S1/S2's was). **PARTIAL, pre-existing, NOT fixed here:** `extract_Sigma(level = "spatial")` on a `spatial_indep()`-ONLY fit still aborts -- a family-agnostic gap shared by every family, not introduced or fixed by this slice; `spatial_indep()`'s recovery is read off `fit$report$kappa`/`log_tau_spde` directly. **Detector (2026-08-17):** the collapsed-but-PD spatial fields this row records are now screened by M3 (`spatial_range_collapse`, range/domain < 0.02) in the `multinomial_contrast_degeneracy` row. M3's first measurement was **0/3 on the labeled `spatial_indep()` collapse cell -- a keyword-scope bug, not a calibration miss**: the arm gated on `Lambda_spde`, which the engine REPORTs only on the low-rank `spatial_latent()`/`spatial_dep()` route (`spde_lv_k > 0`), so on the diagonal route it emitted no row at all. After the fix (branch on the engine route; the diagonal branch reads `log_tau_spde`) the same 20 seeds give rows 20/20 (was 0/20), sensitivity 3/3 on the labeled collapse seeds (was 0/3), and 0/11 false positives on that cell's healthy fits. **BLOCKED:** `spatial_scalar()`, `spatial_latent(unique = TRUE)`'s paired diagonal Psi_spde companion, standalone `spatial_unique()`/deprecated bare `spatial()` (the paired-companion alias mechanism, NOT admitted as an independent diagonal cell -- unlike Slice 2's phylo mode axis), and every augmented (intercept + slope) `spatial_*()` form remain BLOCKED (typed `gllvmTMB_multinomial_structured_not_admitted`). See `docs/design/123-multinomial-structured-surface.md` §1/§4 for the full derivation. |
| FAM-20F | Ordinary GROUP random intercepts for multinomial: generic `(1 \| group)` and the non-phylogenetic `cluster`/`cluster2` diagonal tier (`indep(0 + trait \| g)`) | `partial` | `test-matrix-multinomial-unit.R`; `test-multinomial-fence.R`; `dev/multinomial-structured/pass-criteria-s4.md`; `docs/design/123-multinomial-structured-surface.md` | **IN (narrow):** Design 123 Slice 4 (2026-08-16) admits a generic `(1 \| group)` random intercept (`re_int` engine kind) and `indep(0 + trait \| g)`/standalone `unique(0 + trait \| g)` at the `cluster`/`cluster2` tiers (`use_diag_species`/`use_diag_cluster2` -- verified LITERALLY IDENTICAL engine math on two different grouping columns, co-admission approved by the maintainer 2026-08-16 on that identity). Both routes are pre-existing, family-agnostic engine code; no `src/gllvmTMB.cpp` change this slice. `(1 \| group)`'s semantics are BASELINE-VS-REST, not per-category: the engine adds one draw per group level to EVERY one of a multinomial observation's $K-1$ baseline-contrast rows, shifting $P(y=\text{baseline})$ vs $P(y\ne\text{baseline})$ without moving the odds between any two NON-baseline categories, WITHIN one fit, across groups -- so `sigma_re` is REFERENCE-CATEGORY-SPECIFIC. Pinned empirically: the log-odds between the two non-baseline categories has sd = 0 across every observation of a `G = 20`, `n_per_g = 5` fixture (tolerance 1e-6). Re-labelling `baseline` is NOT a reparameterisation of the same model: it fits a genuinely different constraint on which pair of categories' log-odds stays constant across groups, so a re-fit under a different `baseline` changes the fitted response-scale probabilities too, not only `sigma_re`. A whole-fit OLRE guard (`.multinomial_reint_group_olre_guard()`, typed `gllvmTMB_multinomial_olre_not_admitted`) aborts both routes when every level of the grouping factor covers exactly one categorical observation -- unidentifiable, since the softmax latent scale is fixed. **CAMPAIGN VERDICT (signed, run to completion 2026-08-16): PASSED for `(1 \| group)` ONLY.** The s4 campaign (`dev/multinomial-structured/results/s4-summary-20260816-183635.csv`) fit `re_int` exclusively -- 20/20 seeds converged with a PD Hessian; median `sigma_re` ratio 0.947, range [0.60, 1.51] $\subset$ [0.5, 2.0]. **Detector (2026-08-17): this route gets NO degeneracy row at all, structurally.** A bare `(1 \| group)` multinomial fit has no loading tier, so `multinomial_contrast_degeneracy` never emits a row (`det_present` 0/20 across the s4 cell) -- which is why those 20 fits are EXCLUDED from the detector's false-positive denominator (40, not 56) rather than counted as 20 clean specificity observations. **The cluster/cluster2 diagonal tier carries NO recovery-campaign evidence of its own** -- a correction to an earlier draft of this row, which wrongly attributed the same PASSED verdict to both routes. `test-matrix-multinomial-unit.R` verifies `indep(0 + trait \| g)` CONSTRUCTS and that `extract_Sigma(level = "cluster"/"cluster2")` returns a well-formed per-contrast diagonal against a DGP with no cluster-tier structure of its own to recover against (`pass-criteria-s4.md`'s own "Non-degenerate diagonal cell" section states this explicitly: "Construction-level only for this slice ... FAM-20F stays `partial` on this axis"). **PARTIAL:** the cluster/cluster2 route's recovery axis remains OPEN, pending a dedicated per-contrast recovery DGP and its own criteria block; `common = TRUE` (the `scalar()` modifier) at the cluster/cluster2 tier stays REFUSED (the generic engine has no common-pooling map for `use_diag_species`/`use_diag_cluster2`); no calibrated-interval claim for either route. **BLOCKED:** the `unit_obs` grouping tier, `latent()`/`dep()` at the cluster/cluster2 tiers, and everything FAM-20/FAM-20D already list as blocked remain BLOCKED (typed `gllvmTMB_multinomial_structured_not_admitted`). `meta_V()`/`equalto()` confirmed Gaussian-only, fail-closed for fid 16 (maintainer decision, 2026-08-16). See `docs/design/123-multinomial-structured-surface.md` §1/§4 for the full derivation. |

| FAM-21 | zi_poisson (zero-inflated Poisson, TRUE mixture) | `partial` | `test-zi-families.R`, `test-zi-recovery.R`, `test-enum-runtime-ids.R` | **IN:** runtime id 17 wired end-to-end -- density (exact TMB-vs-hand match, 1e-8), gradient (FD-checked), fit admission (long + wide), `fitted()`/`predict(type="response")` = `(1-zi)*mu`, `simulate()`, randomized-quantile residuals via the closed-form mixture CDF, `check_gllvmTMB()` boundary flag on `zi`, mixed-family fits with a plain `poisson()` trait. Count part carries the full grammar (fixed effects, `latent()`, every covariance tier); `zi` is per-trait, intercept-only, logit link, no covariates/random effects. Known-DGP recovery (rank-1 latent, n=200, 6 traits): intercepts within 0.15, `zi` within 0.08 absolute, loadings to rel. Frobenius 0.25 -- all met at seed 101 (the default test) AND re-verified at seeds 202/303/404 (review R6; n was raised from 150 to 200 because those bars did NOT hold at n=150 across seeds -- see dev/gapclose/arcD/D1-report.md "Review fixes" for the n=150 numbers that motivated the change). **PARTIAL:** no calibrated interval on `zi`; `integration = "va"` and `estimator = "mspl"` refuse this family id, but `aghq` DECLINES to a plain Laplace fit with a warning rather than erroring (consistent with AGHQ's existing decline-not-refuse architecture for every ineligible model, e.g. `multinomial()`; corrected 2026-09-02, review R3, was previously stated as a refusal); `link_residual_per_trait()` uses the conditional count family's own rule (Poisson, fid 2) so `extract_Sigma(link_residual = "auto")` reports a finite value, not NA (review R2); `predictive_check(type = "rootogram")` now admits fid 17 (review R1); Julia parity (`ZIPoisson` alias) not attempted. See `dev/gapclose/arcD/D1-report.md` and `dev/gapclose/arcD/alignment-zi.md`. **50-SEED CAMPAIGN, 2026-09-03 (`dev/gapclose/arcD/recovery/RESULTS.md`, 450 fits on Totoro from `main` @ 5855e2ad9):** the four bars together (convergence, intercepts, `zi`, loadings) hold in **54% of seeds at n=150, 82% at n=200, 98% at n=400**. The shipped test's n=200 is a single-seed REGRESSION GUARD, not a certified size: it was chosen on seeds 101/202/303/404 and does not reach a 90%-of-seeds standard. **n=400 is the size this family actually recovers at.** |
| FAM-22 | zi_nbinom2 (zero-inflated NB2, TRUE mixture) | `partial` | `test-zi-families.R`, `test-zi-recovery.R`, `test-enum-runtime-ids.R` | **IN:** runtime id 18, same wiring as FAM-21, reusing the ordinary per-trait `nbinom2()` `log_phi_nbinom2` dispersion vector rather than a new one (a deliberate departure from GLLVM.jl's shared-scalar `r` -- recon open question 2). Density/gradient exactness identical to FAM-21. Known-DGP recovery: intercepts/`zi`/loadings meet the same bars as FAM-21 (n_site raised to 400 for this family only -- see the NOTE below). **PARTIAL, with a MEASURED caveat, corrected 2026-09-02 (review R4 -- the first-draft caveat UNDERSTATED severity):** at n_site = 150 (matching FAM-21/23), the per-trait NB2 dispersion is NOT reliably recovered -- reproduces on plain `nbinom2()` under the identical rank-1-latent DGP too (confirmed not zero-inflation-specific; this package already documents the same class of per-trait-dispersion runaway for Gamma/Beta/student under a shared random-effect structure). At n_site = 400, re-measured across THREE seeds (101/202/303), **2 of 6 traits exceed 30% relative `phi` error in EVERY seed tried**, not "one trait can still exceed it" as first written; the median (not max) relative error across traits stays < 30% in all three (0.189, 0.219, 0.157). One seed in three produces a genuine runaway, not merely a moderate miss: seed 303 trait 4 reports `phi_hat = 2,663,000` against `phi_true = 6` (relative error 4.4e5), on a fit that reports `convergence = 0`, a PD Hessian, `max_gradient` well under threshold, and (before this review) ZERO non-PASS rows from `check_gllvmTMB()` -- the runaway was completely undetected. Fixed: `R/diagnose.R` gained a `boundary_phi_nbinom2_<trait>` row (WARN when `phi` sits at or above a numerical-ceiling threshold), analogous to the existing `boundary_zi_<trait>` row. Signed-error analysis across the pooled 18 (seed x trait) cells shows this is genuinely a small-sample VARIANCE fact, not a directional bias (median signed relative error +7.0% at n=400, -5.8% at n=150; both signs occur at comparable magnitude) -- the D1 report's original diagnosis of the MECHANISM was correct, only the reported SEVERITY at n=400 was wrong. `extract_Sigma(link_residual = "auto")` uses the conditional NB2 rule ($\psi'(\phi)$, fid 5) so it reports a finite value, not NA (review R2). No calibrated interval on `zi`; `integration = "va"` and `estimator = "mspl"` refuse this family id, but `aghq` DECLINES to a plain Laplace fit with a warning rather than erroring (same AGHQ-architecture note as FAM-21/23, corrected 2026-09-02); Julia parity (`ZINB`/`ZINegBin` alias) not attempted. See `dev/gapclose/arcD/D1-report.md` "Review fixes" section for the full per-seed, per-trait table. **50-SEED CAMPAIGN, 2026-09-03 (`dev/gapclose/arcD/recovery/RESULTS.md`, 450 fits on Totoro from `main` @ 5855e2ad9):** the point bars together hold in **34% of seeds at n=200, 82% at n=400, 98% at n=800**; the per-trait dispersion bar (median relative `phi` error < 0.30) holds in **50% / 74% / 98%** at the same sizes, with a mean of 3.04 / 2.02 / 1.16 traits out of 6 above 30%. The shipped test's n=400 is a single-seed REGRESSION GUARD and reaches neither bar at a 90%-of-seeds standard; **n=800 does, on both.** This sharpens rather than replaces the mechanism above: the dispersion problem is small-sample identifiability shared with Gamma/Beta/student, and it shrinks with n. **Independently corroborated 2026-09-03 by GLLVM.jl's own 6,000-fit ADEMP campaign** (branch `codex/core070-aghq-20260830`, `docs/dev-log/core070/zi-ademp-recovery-findings.md`): on a different grid (p in {5,25} x n in {50,200}) it finds the same shape -- recovery holds only once n is large relative to p (zinb convergence 70.0% at p=25/n=50, 98.6% at n=200) -- and attributes the dispersion failure to the same small-sample per-trait identifiability class, reached from a separate codebase. Neither campaign evaluated SEs or coverage. |
| FAM-23 | zi_binomial (zero-inflated binomial, TRUE mixture, multi-trial only) | `partial` | `test-zi-families.R`, `test-zi-recovery.R`, `test-enum-runtime-ids.R` | **IN:** runtime id 19, same wiring as FAM-21, logit link on the count part only. Single-trial (0/1) response data is REFUSED at parse time (`R/fit-multi.R`), not merely undertested: with $N=1$, $P(y=1) = (1-\text{zi}) p$ collapses the structural-zero probability and the count probability into one free product with no curvature to separate them; the refusal names `binomial()` as the working alternative and is itself tested. Admits any trait with at least one row carrying $N \ge 2$. Known-DGP recovery (n=250, 6 traits, $N=10$): intercepts/`zi`/loadings meet the same bars as FAM-21 at seed 101 AND re-verified at seeds 202/303/404 (review R6; n was raised from 150 to 250 for the same reason as FAM-21 -- at n=150, seed 303 breached both the `zi` and loadings bars). **PARTIAL:** no calibrated interval on `zi`; `integration = "va"` and `estimator = "mspl"` refuse this family id, but `aghq` DECLINES to a plain Laplace fit with a warning rather than erroring (same AGHQ-architecture note as FAM-21/22, corrected 2026-09-02); `link_residual_per_trait()` uses the conditional binomial rule ($\pi^2/3$, fid 1) so `extract_Sigma(link_residual = "auto")` reports a finite value (review R2); Julia parity (`ZIB` alias) not attempted. See `dev/gapclose/arcD/D1-report.md`. **50-SEED CAMPAIGN, 2026-09-03 (`dev/gapclose/arcD/recovery/RESULTS.md`, 450 fits on Totoro from `main` @ 5855e2ad9):** the bars together hold in **48% of seeds at n=150, 92% at n=250, 100% at n=500**. This is the only one of the three families whose shipped test size (n=250) clears a 90%-of-seeds standard, and it clears it thinly; n=500 is comfortable. |
| FAM-25 | censored_poisson (right-censored Poisson, log link only) | `partial` | `test-censored-poisson.R`, `test-enum-runtime-ids.R` | **REASSIGNED 2026-09-03: this row was FAM-24 and the family was runtime id 20 until the sibling `ordinal_logit` branch (PR #1250, maintainer-approved, merges first) was found to independently claim BOTH the same runtime id (20) and the same register id (FAM-24) -- censored_poisson loses the tie on both and moves to FAM-25 / runtime id 21. Density identity, the FD gradient, the all-uncensored-equals-`poisson()` check, and all four recovery seeds were re-measured after the renumber (numbers below are the re-measured ones, not the pre-renumber ones).** **IN (Arc E, issue #1244, 2026-09-03):** runtime id 21 wired end-to-end. Right-censoring only (not left- or interval-censoring); density via the Poisson-Gamma duality `log P(Y>=C) = log pgamma(mu, shape=C, scale=1)`, evaluated through TMB's AD-differentiable `pgamma` (no hand-coded score/weight needed, unlike GLLVM.jl's Newton-core oracle -- CppAD differentiates through `pgamma` directly). Density EXACT vs. a hand-computed R density (diff `0.000e+00`, bit-identical, 20-row fixed-effects-only fixture with 12 censored rows); FD gradient at genuine `b_fix = 0` starting values matches the TMB analytic gradient to max relative discrepancy `1.9e-10`. Response encoding: `cbind(y, censored) ~ ...` (column 2 a strict 0/1 right-censoring indicator; column 1 the count or the limit); a plain `y ~ ...` response is also accepted and is verified numerically IDENTICAL to `poisson()` (objective and `opt$par` match to 1e-6/1e-8) -- the "all-uncensored convenience path". `fitted()`/`predict(type="response")`/`.dlinkinv_per_row()` fall through to the existing log-link default (`exp(eta)`, the underlying-process mean, not a censoring-adjusted one) with NO new branch needed -- verified against `fit$report$eta` to 1e-10. `simulate()` draws the latent Poisson count and re-applies each row's OWN censoring limit (a fixed design property, not part of the random draw). Randomized-quantile residuals: exact Poisson CDF on uncensored rows, `Uniform(F(C-1), 1)` on censored rows (the same "known CDF interval" idiom the ordinal_probit and zi-mixture branches already use). `link_residual_per_trait()` reuses fid 2's Poisson rule (`log1p(1/mu_t)`) so `extract_Sigma(link_residual = "auto")` reports a finite value. `integration = "va"` refuses (fid 21 falls outside the `0:15L` allow-list, no code change needed); `estimator = "mspl"` refuses by omission (no `censored_poisson` row in the fixed enumerated registry, no code change needed); `aghq` DECLINES to a plain Laplace fit with a warning (same architecture as FAM-21/22/23, not a refusal). Known-DGP recovery (rank-1 latent, n_site=200, 6 traits, censoring limit C=6, ~26% of cells censored): intercepts max\|err\| < 0.15 and loadings rel. Frobenius < 0.25 -- both bars hold at seed 101 (the shipped test) AND re-verified at seeds 202/303/404 (a REGRESSION GUARD confirmed on 4 seeds, NOT a certified size -- no larger-N campaign was run for this family, unlike FAM-21/22/23's 450-fit Totoro sweep). **PARTIAL / NOT built:** left-censoring and interval-censoring (design doc `docs/design/105-va-family-densities.md` §7's fuller table describes them; this arc's scope, matching GLLVM.jl's own v1, is right-censoring only); no calibrated interval; rootogram (`predictive_check(type = "rootogram")`) does NOT admit fid 21 (deliberately, censored rows have no observed count for the histogram comparison); `check_gllvmTMB()` gains no new degeneracy row (this family introduces no new estimated parameter to flag); Julia parity beyond the alignment table not attempted (`GLLVM.jl`'s `censored_poisson_marginal_loglik_laplace` was read for the density derivation only, never cross-fit numerically). **Runtime id 20 was independently claimed by the sibling `o4-ordinal-logit` branch's `ordinal_logit()` (PR #1250) -- resolved 2026-09-03 by moving censored_poisson to 21, per this row's REASSIGNED note above; see `dev/gapclose/arcE/alignment-censored-poisson.md`.** See `dev/gapclose/arcE/alignment-censored-poisson.md` for the full symbolic derivation and 14-slot registry checklist. |

### Section 3 — Random-effects structures

Row-owner: **Boole + Fisher** (random-effects design lead).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| RE-01 | Random intercepts only (`s = 0`) | `covered` | `test-multi-random-intercepts.R` | M0 baseline |
| RE-02 | One random slope (`s = 1`) | `covered` | `test-phylo-slope.R`, `test-matrix-slope-{poisson,nbinom2,gamma,beta,ordinal,binomial-logit,binomial-probit}.R`, `test-family-slope-recovery.R` | Augmented random-regression slope recovers across all core families (local measurement 2026-05-30: 7 files, 0 fail) + Gaussian + animal (ANI-06) + `phylo_indep(1+x)` independent variant. C1 family-generalisation cells (lognormal, student, betabinomial) live in `test-family-slope-recovery.R` and are tracked as `partial` under RE-14 — not route-specific covered. |
| RE-03 | Two or more random slopes (`s ≥ 2`) | `partial` | `test-phylo-dep-slope-s2-gaussian.R`, `test-fixed-column-slope-family.R`, `test-phylo-column-slope-indep.R`, `test-phylo-slope-rhs-routing.R`, `test-spatial-column-slope.R` | Gaussian intercept-plus-slope `phylo_dep(1 + x1 + x2 \| sp)` is covered as before. Separately, Design 130's slope-only response-column model uses `K_column %x% Sigma_predictor` in trait-major order. Deterministic design-oracle/RHS-routing tests prove no random intercept is added and Gaussian recovery assesses the diagonal or full predictor matrix. Ordinary, phylogenetic, animal, dense-kernel, and spatial response-column sources are covered for Gaussian long-format point estimation at the evidence depth recorded in FG-19 and SPA-11. Non-Gaussian `s >= 2`, wide grammar, `s >= 3` recovery depth, latent predictor covariance, simultaneous column-slope sources, and intervals remain unadvertised. |
| RE-04 | Nested `unit / unit_obs` | `covered` | `test-multi-random-intercepts.R`, `test-olre-separation.R` | M0 baseline |
| RE-05 | Crossed (e.g. site × year) | `covered` | `test-multi-random-intercepts.R`, `test-stage1-stacked-fixed-effects.R` | Ordinary scalar `(1 \| site) + (1 \| year)` crossing is covered by the balanced Gaussian recovery cell in `test-multi-random-intercepts.R` (local measurement 2026-06-02: convergence 0, both SDs within 0.20 of empirical truth, BLUP correlations > 0.95). This does not advertise ordinary random slopes; trait-specific crossed diagonal components are covered separately by RE-11 / `cluster2`. |
| RE-06 | OLRE (observation-level random effect) | `covered` | `test-olre-separation.R`, `test-extract-omega.R`, `test-extractors-extra.R` | |
| RE-07 | `sigma_eps` auto-suppression for OLRE | `covered` | `test-sigma-eps-autosuppress.R` | |
| RE-08 | Cluster-level random effect (`cluster` argument) | `covered` | `test-cluster-rename.R` | |
| RE-09 | `latent + unique` explicit-Psi compatibility pair in within-unit tier | `covered` | `test-mixed-response-unique-nongaussian.R`, `test-tiers-{poisson,nbinom2,gamma,beta,ordinal}.R`, `test-re09-latent-unique-unit.R` | unit_obs explicit-Psi compatibility pair recovers across core families (local measurement 2026-05-30: tiers files green, 0 fail). `test-re09-latent-unique-unit.R` (PR #366, issue #347) adds the dedicated same-grouping `latent(0+trait\|unit,d=K) + unique(0+trait\|unit)` recovery cell behind the behavioural-syndromes vignette: Lambda loading shape (Procrustes `cor_per_factor > 0.95`), Psi diagonal (deliberately wide band, split weakly identified), and total `Sigma_unit = Lambda Lambda^T + Psi` off-diagonals all recovered; `skip_if_not_heavy()` gated. Prior tier tests exercised only diagonal-plus-diagonal / single OLRE cells, not a reduced-rank `latent()` block alongside explicit Psi. |
| RE-10 | Augmented LHS guard (engine-internal variable name clashes) | `covered` | `test-augmented-lhs-guard.R` | |
| RE-11 | Second independent diagonal grouping (`cluster2` argument) | `covered` | `test-cluster2-rename.R`, `test-cluster2-families.R` | Issue #342 (sub-issues #355, #356). cluster2 is a renamed copy of the `cluster` (`diag_species` / `q_sp`) diagonal tier on a disjoint grouping column, so two crossed/nested plain diagonal per-trait variance components fit at once (e.g. `cluster = "site"` + `cluster2 = "year"`). Family-agnostic (contribution added to `eta` before family dispatch; no per-family C++ branch). Equivalence gate: a `cluster2`-routed `unique(0+trait\|G)` fit is byte-identical (objective + `extract_Sigma` delta = 0) to the `cluster`-routed fit on the same G (local measurement 2026-05-31: 24 pass / 0 fail, heavy crossed-recovery cell included; Gaussian site+year variances recovered within 0.30 band). Diagonal-only: `latent`/`rr`/`dep` on the cluster2 column aborts with a `unit =` redirect. Per-family recovery sweep (Slice F, #356; `test-cluster2-families.R`): for each wired family the simulated cluster2 diagonal variance is recovered within that family's sibling tier band, mirroring `test-tiers-*.R` conventions (local measurement 2026-05-31, `GLLVMTMB_HEAVY_TESTS=1`: 70 pass / 0 fail / 0 skip across gaussian, poisson, binomial, nbinom2, beta, Gamma, ordinal\_probit -- every cell conv == 0, PD Hessian, finite `sd_c2`, `extract_Sigma(level = "cluster2")$s == sd_c2^2`). cluster2 sits one level above any per-row residual / OLRE / overdispersion (shared across many rows per level), so no family is the structurally degenerate per-row case -- all seven recover, none skipped. |
| RE-12 | Ordinary individual-level `latent(1 + x \| unit, d = K)` random-regression component | `partial` | `test-ordinary-latent-random-regression.R`, `test-example-behavioural-reaction-norm.R`, `test-augmented-lhs-guard.R` | Implemented 2026-06-08 for the Gaussian unit-tier augmented decomposition, then updated 2026-06-18 so Gaussian augmented `latent()` supplies the diagonal `Psi_B,aug` companion by default. Parser accepts the wide `latent(1 + x \| unit, d = K)` and long `latent(0 + trait + (0 + trait):x \| unit, d = K)` forms, routes them to dedicated TMB blocks with `Lambda_aug` and `Psi_B,aug` over the `2T` `(intercept, slope) x trait` coefficient vector, and `extract_Sigma(level = "unit_slope", part = "shared" / "unique" / "total")` returns `Lambda_aug Lambda_aug^T`, the augmented diagonal, or their sum with labelled rows. Evidence includes parser classification, parenthesized intercept-only LHS no-regression coverage, long and wide fits, Gaussian default composition and recovery, explicit `+ unique(1 + x \| unit)` compatibility composition, explicit compatibility diagonal extraction, behavioural reaction-norm long/wide fixture agreement, rank guard, unit_obs rejection, mismatched-slope rejection, and a Poisson latent-only smoke fit. The row remains `partial` because non-Gaussian augmented `latent()` remains low-rank-only, non-Gaussian augmented `unique()` is deliberately guarded, and delta / hurdle families remain out of scope per FAM-17. |
| RE-13 | Ordinary unit-tier predictor-informed latent-score mean `latent(..., lv = ~ x)` | `partial` | `test-lv-parser-guard.R`; `test-lv-gaussian-recovery.R`; `test-lv-bernoulli-depth.R`; `test-lv-native-nongaussian-guard.R`; `test-lv-family-boundary-guard.R`; `test-lv-mixed-family-first-cell.R`; `test-mixed-lv-family-harness.R`; `test-lv-reml-boundary-guard.R`; `test-lv-factor-runtime.R`; `test-lv-missing-response.R`; `test-julia-bridge.R`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-28-local-r500-summary.csv`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-30-local-binomial-r500-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-source-contract.md`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-recovery-cell-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-pure-recovery-cell-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-calibration-target-summary.csv`; `test-lv-cross-family-predictor-bridge.R`; `test-mixed-gaussian-lognormal-scale.R`; `docs/dev-log/artifacts/cross-family-lv-predictor/2026-08-27-local-canary-pre-run-receipt.md`; `docs/dev-log/artifacts/cross-family-lv-predictor/2026-08-27-all-family-pre-run-receipt.md` | The native model is `z_i = M_i alpha + e_i`, `e_i ~ N(0, I_K)`, with `B_lv = Lambda alpha^T` as the rotation-invariant trait-scale target. Registered native families now compose in the ordinary complete-response route. Loadings-only `unique = FALSE` admits rank through logical responses; automatic `Psi` additionally requires that physical loading rows, latent-predictor coefficients, and engine-free diagonal slots pass a necessary joint mean-covariance dimension screen. Passing the screen is not generic/local identification, recovery, or calibration evidence. Route-health covers IDs 0--16, including loadings-only rank-2/rank-3 five-family fits and a retained loadings-only rank-3 IDs-0--15 stress fit. The row stays partial because claim-bearing recovery/calibration remains cell-specific, while non-Gaussian masks, missing predictors, fixed `X + X_lv`, REML, structured tiers/sources, and broad Julia parity remain open. |
| RE-14 | Lognormal, Student-t, and betabinomial augmented-slope family generality | `partial` (C1 runtime admission only; not route-specific coverage) | `test-family-slope-recovery.R`; landed decision `a51e9704`; `docs/dev-log/after-task/2026-07-12-re-surface-arc-start.md`; `docs/dev-log/after-task/2026-08-01-slope-per-family-betabinomial-admission.md`; `test-augmented-slope-family-policy.R` | Runtime family IDs 3, 8, and 9 are intentionally permitted at the six family-agnostic structured augmented-slope guard sites via `.augmented_slope_family_contract()`. Direct evidence is one adequate-N `phylo_indep(1 + x | species)` seed per family with convergence, finite-positive reported SDs, and pooled slope-SD ratio between 0.5 and 1.7. Betabinomial (id 8, logit only) uses a multi-trial `cbind(succ, fail)` DGP at large N (`n_sp = 200`, trials = 15; n_sp = 90 failed to converge in the 2026-08-01 probe). The test does not establish fixed-effect recovery, positive-definite Hessian, gradient, intercept-variance recovery, per-trait recovery, replicated seeds, interval calibration, or direct phylo-dep/latent/spatial recovery. Therefore the family admission is C1 partial: other routes are mechanically permitted, not independently validated or covered. The policy test exhaustively joins runtime family/link admission to the six guards. Tweedie remains fail-loud; binomial cloglog is not included by this row.

### Section 4 — Phylogenetic GLLVM

Row-owner: **Noether + Boole** (phylo-specific math + parser).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| PHY-01 | Hadfield & Nakagawa sparse A⁻¹ | `covered` | `test-phylo-hadfield.R` | M0 baseline. Issue #611 guard: the `phylo_tree` route passes `log_det_A_phy_rr = -log_det_precision`, i.e. `+log|A_aug|` (built by `.gllvm_phylo_tree_precision()`; the `inverseA(tree)$dii` formulation predates the native builder), so the sparse tree path and dense `phylo_vcv` path agree on objective / logLik as well as MLEs. |
| PHY-02 | `phylo_latent + phylo_unique` paired | `covered` | `test-stage35-phylo-rr.R`, `test-phylo-q-decomposition.R` | M0 baseline |
| PHY-03 | Three-piece phylo fallback | `covered` | `test-phylo-q-decomposition.R` | |
| PHY-04 | `phylo_scalar(0 + trait \| sp)` | `covered` | `test-stage35-phylo-rr.R`, `test-phyloscalar-binary.R`, `test-stage3-propto-equalto.R` | Phase B-INF Lane 2 / B1 (Design 58): binary probit recovery on shared `sigma^2_phy_scalar` (3x band, n_sp = 40, 4 binary replicates per cell) + CI smoke (`confint(parm = "lambda_phy", method = "profile")` finite). Issue #636 guard: sparse `propto()` now preserves the tip-only precision path but, when sparse `Ainv` has extra nodes, inverts the full precision first, subsets the marginal covariance, and inverts that submatrix; `test-stage3-propto-equalto.R` checks the marginalization math directly. Note: `phylo_signal` parm does not apply -- the propto path sets `use$propto`, not `use$phylo_rr` / `use$phylo_diag`. |
| PHY-05 | `phylo_indep / phylo_dep` | `covered` | `test-stage35-phylo-rr.R`, `test-phylodepindep-binary.R` | Binary-probit structural/recovery evidence and finite point-correlation extraction are covered for both keywords. The nonlinear `rho:phy` profile route is explicitly withdrawn and tested through `gllvmTMB_nonlinear_profile_withdrawn`; this row makes no profile-interval claim. |
| PHY-06 | `phylo_slope()` helper | `covered` | `test-phylo-slope.R`, `test-phylo-column-slope-indep.R`, `test-phylo-slope-rhs-routing.R` | Under Design 130, RHS-equals-declared-trait calls, including one predictor, are response-column helpers: `||` selects `phylo_indep`, `|` selects `phylo_dep`, the tree supplies `K_column`, and `Sigma_predictor` describes predictor-slope covariance. RHS-not-trait calls retain the original group-indexed route as compatibility behaviour. The helper is not deprecated. |
| PHY-07 | `extract_phylo_signal()` Adams (2014) | `covered` | `test-extract-omega.R`, `test-extractors-extra.R`, `test-phylo-signal-categorical.R` | 2026-07-04 closes issue #677 by returning `NA`, not `NaN`, for per-trait zero-denominator `H2` / `C2_non` / `Psi` rows while preserving valid rows for other traits. **2026-08-17 (Design 123 PA2), a real defect found and fixed:** on a phylo-only `ordinal_probit()` or `multinomial()` fit the extractor returned **`H2 = 1.0` for every trait and every contrast** -- technically the species-level-latent proportion, silently nonsense against the conventional liability-scale heritability (the paper's own values for these data are ~0.37/0.35/0.27/0.41), because the fixed link residual never entered the denominator. `link_residual = c("none", "auto")` added: default `"none"` is byte-identical to the historical behaviour (every existing test untouched), `"auto"` gives `V_a/(V_a + 1)` per ordinal trait and `V_a(k)/(V_a(k) + pi^2/3)` per multinomial CONTRAST -- never collapsed to a scalar, documented as baseline-referenced -- hand-verified to 1e-10 against mocked cells, with a live MCMCglmm `family = "ordinal"` comparator (0.357 vs 0.436, band 0.15, truth 0.5). `ci = TRUE` with `"auto"` refuses typed rather than returning uncalibrated intervals. **Not covered:** calibrated intervals on the liability-scale ratio, any multi-seed recovery campaign (single-seed live cells only), and the replication-regime multinomial H^2. |
| PHY-08 | `extract_communality()` $H^2 + C^2 + \psi^2 = 1$ partition | `covered` | `test-extractors.R`, `test-extractors-extra.R` | |
| PHY-09 | Phylogenetic mode dispatch (paired vs three-piece) | `covered` | `test-phylo-mode-dispatch.R` | |
| PHY-10 | Optional `phyloVCV` argument | `covered` | `test-phylo-vcv-optional.R` | |

Phylo simulation addendum (2026-07-05): issue #596 is guarded in
`test-stage3-propto-equalto.R`. The unconditional `propto()` / `phylo_scalar`
simulation path now treats `lam_phy = exp(loglambda_phy)` as a variance
multiplier (`sqrt(lam_phy)` on the standard phylogenetic draw), matching the
TMB likelihood and extractor convention. This corrects bootstrap-style
simulation input; it is not new bootstrap calibration evidence.

| PHY-11 | `phylo_indep(1 + x \| sp)` augmented slope under **binomial** (probit + logit) | `partial` | `test-binomial-slope-recovery.R` | Design 79/80 structural contract only: one deterministic fixture per link constructs a numerically healthy `2T` fit with `3T` free `theta_dep_chol` parameters, unit diagonal, and zero cross-trait blocks. Each trait's intercept-slope correlation remains free. Replicated variance recovery, correlation recovery, intervals, and calibrated inference remain open; the former shared two-parameter / `rho = 0` evidence is not applicable to this engine. Families outside the runtime allowlist remain fail-loud in `test-matrix-slope-phylo-indep.R`. |

| PHY-12 | `phylo_indep(1 + x \| sp)` augmented slope under **poisson** (log) | `covered` | `test-phylo-indep-slope-nongaussian.R` | Current `2T` per-trait block-diagonal recovery gate: cross-trait blocks are exactly zero, each within-trait intercept-slope correlation is free and sign-checked, at least 5/6 seeds must converge with a positive-definite Hessian, and pooled intercept/slope variances must fall within a factor-5 band. This is family-specific recovery evidence, not interval calibration. |
| PHY-13 | `phylo_indep(1 + x \| sp)` augmented slope under **nbinom2** | `covered` | `test-phylo-indep-slope-nongaussian.R` | Current `2T` per-trait block-diagonal recovery gate: cross-trait blocks are exactly zero, within-trait correlations are free and sign-checked, at least 4/6 seeds must be healthy, and pooled intercept/slope relative error must be at most 0.50. This is family-specific recovery evidence, not interval calibration. |
| PHY-14 | `phylo_indep(1 + x \| sp)` augmented slope under **Gamma** (log) | `covered` | `test-phylo-indep-slope-nongaussian.R` | Current `2T` per-trait block-diagonal recovery gate: cross-trait blocks are exactly zero, within-trait correlations are free and sign-checked, at least 5/6 seeds must be healthy, and pooled intercept/slope variances must fall within a factor-4 band. This is family-specific recovery evidence, not interval calibration. |
| PHY-15 | `phylo_indep(1 + x \| sp)` augmented slope under **Beta** | `covered` | `test-phylo-indep-slope-nongaussian.R` | Current `2T` per-trait block-diagonal recovery gate: cross-trait blocks are exactly zero, within-trait correlations are free and sign-checked, at least 5/6 seeds must be healthy, and pooled intercept/slope relative error must be at most 0.45. This is family-specific recovery evidence, not interval calibration. |
| PHY-16 | `phylo_indep(1 + x \| sp)` augmented slope under **ordinal_probit** | `partial` | `test-phylo-indep-slope-nongaussian.R` | The `2T` structural route, `3T` free-parameter count, and cross-block-zero contract are exercised, but the frozen fixture records only 3/6 converged positive-definite-Hessian fits against `min_good = 4` and therefore deliberately skips recovery. No ordinal variance or within-trait-correlation recovery claim is admissible. |
| PHY-17 | `phylo_latent(1 + x \| sp, d = 1)` augmented reduced-rank slope | `covered` for the family-by-route cells exercised by the cited PHY-17 tests; lognormal (ID 3) and Student-t (ID 9) are `partial` under RE-14 | `test-matrix-slope-phylo-latent.R`, `test-phylo-latent-slope-gaussian.R`; C1 admission only: `test-family-slope-recovery.R` | Design 56 §9.5a. The block-diagonal reduced-rank slope contribution is accumulated before family dispatch and reports `Sigma_phy_slope_intercept` / `Sigma_phy_slope_slope`; it has no intercept-slope correlation. The route-specific tests cover gaussian, binomial logit/probit, poisson, nbinom2, Gamma, Beta, ordinal-probit, and nbinom1 at their recorded depths. Runtime IDs 3 and 9 are intentionally permitted, but RE-14 provides only one-seed `phylo_indep` C1 evidence; it provides no direct `phylo_latent` recovery, Hessian/gradient, replicated-seed, or interval-calibration evidence. Binomial cloglog and families outside the central augmented-slope contract stay fail-loud. |
| PHY-18 | `phylo_dep(1 + x \| sp)` augmented full-unstructured slope under non-Gaussian families | `covered` for the family-by-route cells exercised by the cited PHY-18 tests; lognormal (ID 3) and Student-t (ID 9) are `partial` under RE-14 | `test-matrix-slope-phylo-dep.R`, `test-phylo-dep-slope-gaussian.R`; C1 admission only: `test-family-slope-recovery.R` | Design 56 §9.5c. The `dep` engine reports a full interleaved `C x C` `Sigma_b_dep`, with `C = 2*n_traits`. Direct recovery cells cover gaussian, binomial logit/probit, poisson, Gamma, nbinom2, Beta, ordinal-probit, and nbinom1 at the sample sizes and family-specific bands recorded in the cited tests and historical after-task reports. Runtime IDs 3 and 9 are intentionally permitted, but RE-14 provides only one-seed `phylo_indep` C1 evidence and no direct `phylo_dep` recovery. Therefore no universal-family, every-mode, or 100%-complete structured-slope claim is admissible. Binomial cloglog and families outside the central augmented-slope contract stay fail-loud. |

### Section 5 — Spatial GLLVM

Row-owner: **Boole + Gauss** (SPDE implementation and provenance).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| SPA-01 | SPDE mesh construction via `make_mesh()` | `covered` | `test-mesh.R`, `test-stage4-spde.R`, `test-anisotropy.R`, `test-mesh-unused-1165.R`; `dev/verify-sdmtmb-spatial-oracle.R` | The current helpers were substantially rewritten against fmesher's public API after an earlier GPL-3 sdmTMB-derived implementation; `inst/COPYRIGHTS` retains that lineage. Tests cover cutoff, k-means, validity-first cutoff search, supplied fmesher meshes, projection/FEM invariants, malformed inputs, the temporary legacy-class bridge, one native R-to-TMB fit, and the unused-`mesh=` guard (#1165: a supplied mesh with no spatial term warns; a raw fmesher mesh is rejected). The plotting surface reports the isotropic practical range only; no directional anisotropy is estimated. |
| SPA-02 | `spatial_latent` + spatial unique total covariance | `partial` | `test-keyword-grid.R` (`spatial_latent(unique = TRUE)` parser / random blocks / report / extractor), `test-spatial-latent-recovery.R`, `test-spatial-pair-binary.R`; gap audit `docs/dev-log/audits/2026-07-03-spatial-derived-profile-gap.md` | `spatial_latent(..., unique = TRUE)` keeps shared and per-trait unique fields active, reports shared/unique/total spatial covariance, and supports Gaussian point extraction. The row remains partial because source/family depth is narrow. Nonlinear spatial correlation/communality profiles are withdrawn and blocked pending an exact constraint solver, optimizer-status ledger, and target-specific calibration; they are not awaiting only a heavier smoke test. |
| SPA-03 | `spatial_indep(..., common = TRUE)` (legacy `spatial_scalar`) | `covered` | `test-stage4-spde.R`, `test-spatial-scalar-binary.R` | Phase B-INF Lane 2 / B4 (Design 58): binary probit recovery + CI smoke (`confint(parm = "tau_spde", method = "profile")`) + tied-tau contract verified (`log_tau_spde` entries collapse to a single value via TMB `map`). |
| SPA-04 | `spatial_indep / spatial_dep` | `covered` | `test-stage4-spde.R`, `test-spatial-depindep-binary.R` | Binary-probit structural/recovery evidence is covered: healthy SPDE fits, finite-positive scale reports, and non-degenerate point correlations for `spatial_dep`. The nonlinear `rho:spatial` profile route is explicitly withdrawn and tested through `gllvmTMB_nonlinear_profile_withdrawn`. A spatial bootstrap request currently returns a labelled Wald fallback; it is not spatial bootstrap calibration. |
| SPA-05 | Spatial mode dispatch | `covered` | `test-spatial-mode-dispatch.R` | |
| SPA-06 | Spatial orientation handling (X/Y) | `covered` | `test-spatial-orientation.R`, `test-utm-conversions.R` | |
| SPA-07 | Spatial deprecation (legacy aliases) | `covered` | `test-spatial-deprecation.R` | |
| SPA-08 | `extract_Sigma(level = "spatial")` for augmented `spatial_unique` and current `spatial_indep` | `covered` for registered Gaussian/core cells; lognormal/Student-t `partial` via RE-14 | `test-extract-sigma-spde-base-slope.R`, `test-spatial-indep-slope-gaussian.R`, `test-spatial-indep-slope-nongaussian.R` | The two modes have distinct current contracts. `spatial_unique(1 + x \| coords)` retains the shared 2 by 2 `sd_spde_b`/`cor_spde_b` field covariance and returns `level = "spde_base_slope"`. Design 79/80 `spatial_indep(1 + x \| coords)` uses interleaved `2T` `theta_spde_dep_chol` with cross-trait blocks pinned, reports `Sigma_field`, and returns `level = "spde_indep_slope"`; `|` leaves one free within-trait intercept-slope correlation per trait and `||` also pins those correlations. `spatial_dep` remains `spde_dep`. All returned SPDE covariances state their field-versus-marginal scale. Lognormal/Student-t are mechanically permitted but lack direct route-specific recovery (RE-14). |
| SPA-09 | `spatial_latent(1 + x \| site, d = K)` augmented block-diagonal reduced-rank slope | `covered` for the family-by-route cells exercised by the cited SPA-09 tests; lognormal (ID 3) and Student-t (ID 9) are `partial` under RE-14 | `test-matrix-slope-spatial-latent.R`, `test-spatial-latent-slope-gaussian.R`; C1 admission only: `test-family-slope-recovery.R` | Design 64 §3. The route gives each augmented LHS column its own low-rank spatial covariance and no intercept-slope correlation. Route-specific tests cover gaussian, binomial logit/probit, poisson, nbinom2, Gamma, Beta, ordinal-probit, and nbinom1 at their recorded depths. Runtime IDs 3 and 9 are intentionally permitted, but RE-14 supplies no direct `spatial_latent` recovery, Hessian/gradient, replicated-seed, or interval-calibration evidence. Binomial cloglog and families outside the central augmented-slope contract stay fail-loud. |
| SPA-10 | `spatial_dep(1 + x \| site)` augmented full-unstructured 2T×2T field-covariance slope | `covered` for the family-by-route cells exercised by the cited SPA-10 tests; lognormal (ID 3) and Student-t (ID 9) are `partial` under RE-14 | `test-matrix-slope-spatial-dep.R`, `test-spatial-dep-slope-gaussian.R`, `test-spatial-dep-slope-nongaussian.R`; C1 admission only: `test-family-slope-recovery.R` | Design 64 §2. The route reports a full interleaved `C x C` `Sigma_field`, with `C = 2*n_traits`; the cited direct recovery tests retain their family-specific sample sizes and bands. Runtime IDs 3 and 9 are intentionally permitted, but RE-14 supplies only one-seed `phylo_indep` C1 evidence and no direct `spatial_dep` recovery. Therefore SPA-10 does not make a universal-family or every-mode claim. Binomial cloglog and families outside the central augmented-slope contract stay fail-loud. |
| SPA-11 | `spatial_slope()` response-column helper | `covered` (Gaussian long-format point estimation only) | `test-spatial-column-slope.R`; Design 130 §6 | A labelled mesh built with `make_mesh(..., id_col = trait_column)` supplies exactly one unique finite coordinate pair per declared response column and an aligned `A_column` of shape `T x n_mesh`; unlabelled, mismatched, or duplicated inputs fail before optimization. The TMB block factorizes sparse `Q(kappa)` and solves only the `T` projected right-hand sides, then reports exact `K_column = D^-1/2 A_column Q^-1 A_column' D^-1/2` with unit diagonal. `Sigma_predictor` owns marginal coefficient variance; `kappa = exp(log_kappa_spde)` and `sqrt(8) / kappa` are reported with coordinates in supplied units. Tests cover `P != T` diagonal-map cardinality, `|`/`||` one-predictor identity, no intercept, an independent normalization oracle, finite `log_kappa_spde` gradient, label permutation, malformed inputs, source metadata, and a small `P = 2`, `T = 25` Gaussian recovery. The fail-closed gate passed 39/39 locally; existing observation-space spatial fits remain on their separate SPA-08 to SPA-10 routes and their regression filter stayed green. Non-Gaussian/mixed families, wide grammar, simultaneous response-column slope sources, and intervals are not covered. |

### Section 6 — Meta-analysis (meta_V)

Row-owner: **Fisher + Boole** (meta-analysis with known V).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| MET-01 | Single-V `meta_V(V = V)` (additive `type = "exact"` default) | `partial` | `test-formula-grammar-smoke.R`, `test-traits-keyword.R`, `test-gllvmTMB-args.R` | V-only named and positional parser forms verified; wide `traits(...)` marker preservation verified; single-V smoke only; direct `glmmTMB::equalto()` LL comparator still needed |
| MET-02 | Block-V within-study correlation | `covered` | `test-block-V.R` | |
| MET-03 | `meta_V(V = V, type = "proportional")` (Nakagawa 2022) | `blocked` | `test-formula-grammar-smoke.R` | post-CRAN; parser errors explicitly rather than silently treating it as exact; unrecognised `type` values now use the package cli diagnostic rather than base `match.arg()` |
| MET-04 | `corvidae-two-stage` two-stage workflow | `partial` | n/a | article pulled to `dev/workshop-articles/` in PR-0C.PULL (Gaussian meta-analytical example; deferred per maintainer 2026-05-16 — restore once a live cross-check fixture exists) |

### Section 6.5 — Known-relatedness keyword family (animal models)

Row-owner: **Boole + Gauss + Rose** (formula grammar + engine +
A-vs-V boundary). Added M2.8 (2026-05-17) per
[`14-known-relatedness-keywords.md`](14-known-relatedness-keywords.md).
The animal_* keyword family is pure sugar over the existing
`phylo_*` engine path; byte-equivalence with `phylo_*(vcv = A)` is
the test contract.

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| ANI-01 | `animal_scalar(id, pedigree=/A=/Ainv=)` | `covered` | `test-animal-keyword.R`, `test-traits-keyword.R` | byte-equiv with `phylo_scalar(species, vcv = pedigree_to_A(ped))` to 1e-6; wide `traits()` pass-through guarded by issue #604 regression test |
| ANI-02 | `animal_unique(id, ...)` | `covered` | `test-animal-keyword.R`, `test-traits-keyword.R` | byte-equiv with `phylo_unique(vcv = A)`; wide `traits()` pass-through guarded by issue #604 regression test |
| ANI-03 | `animal_indep(0 + trait \| id, ...)` | `covered` | `test-animal-keyword.R` | byte-equiv with `phylo_indep(vcv = A)` |
| ANI-04 | `animal_dep(0 + trait \| id, ...)` | `covered` | `test-animal-keyword.R` | byte-equiv with `phylo_dep(vcv = A)` |
| ANI-05 | `animal_latent(id, d = K, ...)` | `covered` | `test-animal-keyword.R`, `test-matrix-animal-nongaussian.R`, `test-animal-latent-unique-fold.R` | `animal_latent(..., unique = TRUE)` folds the additive-genetic diagonal $\Psi_\text{animal}$ companion via the same `phylo_rr(.phylo_unique = TRUE, .auto_unique = TRUE, vcv = A)` path as `phylo_latent(..., unique = TRUE)`. The default `animal_latent(..., unique = FALSE)` is loadings-only. Evidence covers parser companion emission, malformed-`unique` rejection, Gaussian byte-identity against `animal_latent(..., unique = FALSE) + animal_unique()`, duplicate-Psi rejection, and Gaussian/non-Gaussian loadings-only equivalence with dense `phylo_latent(vcv = A, unique = FALSE)`. |
| ANI-06 | `animal_slope()` helper | `covered` | `test-animal-slope-recovery.R` (PR #313), `test-traits-keyword.R`, `test-phylo-column-slope-indep.R` | Design 130 preserves the original RHS-not-trait route as compatibility behaviour and admits RHS-equals-declared-trait response-column sugar, including one predictor. The latter shares the diagonal/full column-slope core, labels the source as animal, and accepts pedigree, `A`, or `Ainv`. Wide column-predictor grammar is not added. |
| ANI-07 | `pedigree_to_A()` Henderson formula | `covered` | `test-animal-keyword.R` | `nadiv::makeAinv()` cross-check available when nadiv installed |
| ANI-08 | Sparse `Ainv = ` direct engine path | `covered` (helper shipped PR #179 2026-05-18; engine auto-routing shipped 2026-05-18) | `tests/testthat/test-pedigree-sparse-ainv.R`, `tests/testthat/test-pedigree-sparse-ainv-engine.R`, `test-stage3-propto-equalto.R` | Design 47 §10. `animal_*(pedigree = ped)` now auto-routes through `pedigree_to_Ainv_sparse()` and the sparse-Ainv engine path in `R/fit-multi.R` (mirrors the `phylo_tree → MCMCglmm::inverseA` route). Engine path identified via `inherits(fit$phylo_vcv, 'sparseMatrix')`. Sparse-vs-dense equivalence tests cover scalar + unique routes. Issue #612 guard: when sparse `Ainv` has unphenotyped ancestors / extra nodes, the direct phylo engine now keeps the full precision and maps observed levels into `species_aug_id`; `test-pedigree-sparse-ainv-engine.R` checks `n_aug_phy > n_species`, full row retention, correct row mapping, and sparse full-`Ainv` vs dense marginal-`A` logLik equivalence. Issue #636 adds a sparse-precision marginalization guard for the dense `propto()` adapter: exact tip-only `Ainv` still uses the precision directly, while augmented/extra-node sparse precision is inverted to covariance before subsetting. **Pre-CRAN per maintainer 2026-05-18.** |
| ANI-09 | Multi-matrix animal models (G + permanent-environment + maternal) | `partial` | n/a | Achievable today by combining `animal_*` with sibling `(1 \| id)`; idiomatic article example v0.3.0 |
| ANI-10 | Cross-package agreement against MCMCglmm / WOMBAT | `partial` | n/a | Phase 5.5 grid work |
| ANI-11 | `animal_unique(1 + x \| id)` augmented correlated intercept+slope routing **and 2x2 read-out** | `covered` (fits / routes / 2x2 read-out) | `test-animal-unique-routing.R`, `test-extract-sigma-augmented-unique.R` | Issue #354 part (b), shipped PR #367; read-out gap closed by PR #373. The correlated reaction-norm LHS `vec(B) ~ N(0, Sigma_b (x) A)` (Sigma_b a 2x2 with FREE intercept-slope correlation) now routes through the `phylo_unique` augmented engine instead of the old fail-loud abort that misdirected users to `animal_slope`. `R/brms-sugar.R` `animal_unique` handler emits `.phylo_unique_augmented = TRUE` with `atanh_cor_b` free (no `.indep`). `test-animal-unique-routing.R` (13 checks) confirms it fits (`use_phylo_slope_correlated == 1L`, not the dep path, `n_lhs_cols == 2L`) and is byte-identical to `phylo_unique(1+x\|id, vcv = pedigree_to_Ainv_sparse(ped))` (logLik + report-reconstructed Sigma_b diff = 0) and matches the dense `pedigree_to_A(ped)` call to ~1e-4; bare `animal_unique(id)` and intercept-only `animal_unique(0+trait\|id)` forms unchanged; a genuinely unsupported bar LHS still fails loud. **Read-out (PR #373):** the correlated 2x2 (intercept, slope) covariance READ-OUT is now surfaced by `extract_Sigma(fit, level = "phy")` for both `phylo_unique(1 + x \| sp)` and the same-engine `animal_unique(1 + x \| id)`. `R/extract-sigma.R` adds an `extract_Sigma` branch (placed before the generic `phy` handler, guarded `!isTRUE(fit$use$phylo_dep_slope)` and keyed on the scalar `report$cor_b` that ONLY the closed-form unique/indep path emits) returning the 2x2 `Sigma = D R D` (`D = diag(report$sd_b)`, `R = [[1, cor_b], [cor_b, 1]]`) with `intercept`/`slope` dimnames, `level = "phy_unique_slope"`, `part = "slope"`. This mirrors the spatial analogue SPA-08 and surfaces the FREE-correlation `unique` 2x2 honestly (#373). `test-extract-sigma-augmented-unique.R` (27 checks) asserts the read-out equals the engine's own `D R D` reconstruction to < 1e-6 with correct dimnames (Gaussian), an `animal_unique` smoke cell on the long surface, and that `phylo_dep(1 + x \| sp)` still routes to its own interleaved 4x4 branch (guard regression). `extract_correlations` is intentionally NOT extended (matching the `phylo_dep` / `spde_dep` / SPA-08 precedent, whose tier detection keys on intercept-only RR flags). |
| ANI-12 | `animal_dep(1 + x \| id)` augmented full-unstructured dep slope under **non-Gaussian** families | `covered` — poisson confirmation cell green (2026-06-03: 0 failed / 0 errored / 0 skipped) | `test-animal-dep-slope-gaussian.R` | Confirmation recovery cell from the maintainer's relmat/animal decision (2026-06-03, `claude/slope-grid-residuals`). `animal_dep` non-Gaussian dep slopes were ALREADY ADMITTED via the shared `use_phylo_dep_slope` guard (PHY-18, #422/#424; the allowlist `animal_dep` shares with `phylo_dep`), but — unlike `phylo_dep` — they had NO dedicated recovery cell. `animal_*` stays **first-class** (NOT deprecated, unlike `relmat`, which is heading for kernel soft-deprecation per Design 65 C4 — so NO `relmat_dep` cell was added). The new ANI-12 cell adds ONE lightweight non-Gaussian (poisson) `animal_dep(1 + x \| id)` recovery: it builds the relatedness matrix `A` from a pedigree via `pedigree_to_A()`, fits the REAL `animal_dep` path (`value ~ 0 + trait + animal_dep(1 + x \| species, pedigree = ped)`, `family = poisson()`), and reads slope variances from `report$Sigma_b_dep` at the interleaved positions 2/4 within the inherited 4x poisson band. The math is IDENTICAL to the `phylo_dep` poisson VALIDATION cell — only the source of `A` differs (pedigree vs ape tree). Same interleaved `C = 4` `Sigma_b` (`.dep_L_animal`), `n_id = 150`. Honest-skip on non-convergence / non-PD / out-of-band recovery; NO force-pass. **Historical evidence:** the now-retired `slope-grid-residuals-recovery` Actions workflow confirmed the cell on 2026-06-03 with 0 failed / 0 errored / 0 skipped. D-50 preserves that receipt as provenance but routes any new multi-seed confirmation through local/Totoro/DRAC campaign bundles, never Actions. See `docs/dev-log/after-task/2026-06-03-slope-grid-residuals.md`. |

### Section 6.6 — Generic kernels and cross-lineage coevolution

Row-owner: **Boole + Fisher + Curie** (generic kernel grammar,
identifiability, and simulation recovery). Added Design 65
(2026-05-31). C0 validates the biological kernel and the existing
dense `phylo_latent(vcv=K)` route before any new engine work. C1 adds
the generic dense `kernel_*()` formula surface by reusing that
phylo-equivalent path; C3.1 adds fixed named multi-kernel tiers, but
scientific coevolution claims remain gated by component-recovery,
kernel-separation, and interval evidence. Guard: PR green != bridge
complete != release ready != scientific coverage passed.

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| KER-01 | `make_cross_kernel(A_H, A_P, W, rho)` builds a PSD cross-lineage block kernel | `covered` | `test-coevolution-prototype.R` | C0 helper only. Validates symmetry, supplied diagonal blocks, unit diagonal, PSD, and fail-loud guards for invalid bridge strength / matrix scale. No parser or TMB engine change. |
| COE-01 | Cross-lineage coevolution prototype via existing `phylo_latent(..., vcv = K_star) + phylo_unique(..., vcv = K_star)` | `covered` | `test-coevolution-prototype.R` (heavy); superseded by `test-coevolution-recovery.R` (heavy) | C0 prototype checks planted host-partner `Gamma` recovery through the existing dense phylo path on block-missing `traits(...)` data. C2 supersedes the prototype with the generic `kernel_*()` path and the `extract_Gamma()` recovery gate. |
| KER-02 | Generic dense `kernel_latent()` / `kernel_unique()` / `kernel_indep()` / `kernel_dep()` formula family | `covered` | `test-kernel-latent-unique-fold.R`, `test-kernel-equivalence.R` | Design 65 C1. Single named dense kernel tier reuses the phylo-equivalent dense `vcv` path. `kernel_latent(..., unique = TRUE)` folds the kernel-structured diagonal $\Psi_\text{kernel}$ companion via `phylo_rr(.phylo_unique = TRUE, .auto_unique = TRUE, .kernel_mode = "unique", vcv = K)`. The default `kernel_latent(..., unique = FALSE)` is loadings-only. Evidence covers parser companion emission, malformed-`unique` rejection, Gaussian byte-identity against `kernel_latent(..., unique = FALSE) + kernel_unique()`, duplicate-Psi rejection, loadings-only dense-phylo equivalence, and unique/indep/dep companion modes against dense `phylo_*()` `vcv = A` paths to less than `1e-6` for log likelihood and extracted Sigma. No `kernel_scalar()` surface in C1. |
| COE-02 | Validated cross-lineage coevolution engine with `extract_Gamma()` | `partial` (fast + heavy test evidence covered; the internal-workflow article this row cited was cut) | `test-coevolution-recovery.R` (fast extractor tests; heavy recovery/sensitivity gate); `test-example-coevolution-kernel.R` | Design 65 C2. `extract_Gamma()` slices the named kernel tier's shared Sigma block as `Gamma_shape = Lambda_H %*% t(Lambda_P)` by default. `make_cross_kernel()` now records the supplied fixed `rho` on `K_star`, the fitted tier records that metadata, and `extract_Gamma(scale = "effect")` returns the fixed-kernel transformation `Gamma_effect = rho * Gamma_shape` when that metadata exists; generic kernels without cross-kernel metadata fail loudly for the effect scale. Heavy evidence covers known-`Gamma_shape` recovery on block-missing host/partner data, a block-diagonal zero-`Gamma` null with lower logLik, rotation-invariant Gamma extraction, loading-orientation checks on the fitted dense-kernel recovery fixture, fixed-`rho` effect-scale extraction, and a sparse-versus-dense single-`W` sensitivity case. The regenerated internal example fixture carries the same fixed-`rho` metadata. Uncertainty intervals and in-engine `rho` estimation remain future claims. **Downgraded from `covered` 2026-09-02:** this row cited `vignettes/articles/cross-lineage-coevolution.Rmd` as a "buildable internal workflow" demonstrating paired long/wide syntax, null comparison, and fixed-`rho` sensitivity-grid code; that article was added 2026-06-02 (`50de43ee`) and cut at the 0.5.0 public-article-estate finalization (`eacbd0f65`, 2026-07-12) along with 14 other retired articles. No replacement article exists under a different name (`git log --all --diff-filter=A -- '*cross-lineage-coevolution*'` shows only the one add/delete pair). The test evidence stands; the workflow-demonstration claim does not. |
| KER-03 | Fixed named multi-kernel engine (`kernel_latent(..., name = r)` for two or more dense `K_r`) | `covered` | `test-coevolution-two-kernel.R` (fast C3.1 acceptance); `test-kernel-equivalence.R` (single-tier non-regression) | Design 65 C3.1 first wave. Two or more named fixed dense `kernel_latent()` tiers over the same grouping levels now route through a generic TMB block with per-tier `K_r`, `Lambda_r`, and latent field. The fitted object stores the aligned dense `K_r` matrices by component name for downstream point extractors such as `predict_cross_covariance()`; when a tier was built by `make_cross_kernel()`, that stored real-fit matrix retains host/partner metadata and fixed `rho` after level alignment. The one-name `kernel_*()` path still uses the phylo-equivalent KER-02 route to preserve `<1e-6` equivalence. First-wave restrictions: latent-only multi-kernel path; same grouping factor and level set; dense numeric PSD `K`; `kernel_dep()` remains single-tier; auto-generated `kernel_latent()` Psi companions are pruned when two or more named latent kernel tiers are present; explicit kernel-level Psi, crossed/different kernel groups, sparse generic kernel tiers, in-engine `rho`, and re-pointing `phylo_*`/`animal_*`/`spatial_*` onto this core are not claimed. |
| KER-04 | `kernel_slope()` response-column helper | `partial` (Gaussian long-format point route only) | `test-fixed-column-slope-family.R`, `test-kernel-slope-guard.R`; Design 130 | Public sugar over the single labelled dense-kernel `kernel_indep(0 + predictors | trait)` / `kernel_dep(...)` core. The fixed-source gate covers identity-kernel equivalence, label-based permutation invariance, malformed-matrix failures, and `source$scale = "as_supplied"`. The helper adds no random intercept and returns `Sigma_predictor` at `level = "column_slope"`. Wide grammar, multiple simultaneous slope kernels, latent predictor covariance, non-Gaussian families, and intervals are not admitted. |
| COE-03 | Two-kernel coevolution model (`K_phy` + `K_non`) and two-Psi identifiability guardrail | `partial` (fixed latent-only two-component engine, component-specific `Gamma_shape` / fixed-`rho` `Gamma_effect`, and pair-specific point covariance extraction covered; first near-orthogonal recovery evidence in COE-04; inference/Psi gates pending) | `test-coevolution-two-kernel.R` (fast C3.1 extraction + C3.2 guardrail; heavy legacy replicated-Psi recovery; heavy COE-04 near-orthogonal recovery); `test-coevolution-recovery.R` (`predict_cross_covariance()` contract) | Design 65 C3. Fast evidence now fits two independent named latent kernel tiers and extracts separate shared `Sigma`/`Gamma_shape` blocks by component name. When each component kernel comes from `make_cross_kernel()`, `extract_Gamma(scale = "effect")` returns the component-specific fixed-`rho` transformation `Gamma_effect_r = rho_r * Gamma_shape_r`. `predict_cross_covariance()` returns pair-specific point covariance as `Gamma_shape_r * K_r[i, j]`; for `make_cross_kernel()` tiers, `K_r[i, j]` already contains the fixed supplied `rho`, so the helper deliberately avoids multiplying by `Gamma_effect_r`. Real fitted multi-kernel `make_cross_kernel()` tiers now retain metadata after matrix subsetting/symmetrisation, so omitted `row_levels`/`col_levels` default to host/partner levels and `kernel_includes_rho` reports `TRUE`. This supports the Paper 2 fixed-Option-B shape/effect/pair-specific syntax, not scientific promotion. Explicit `kernel_unique()` / `*_unique()` Psi is deliberately not part of the Paper 2 multi-kernel first wave because it is a poor default for non-Gaussian and cross-family coevolution models; the current `*_unique()` surface remains compatibility syntax and should be redesigned/deprecated after this arc. **C3.2 (legacy guardrail still covered):** when two `kernel_unique` tiers are requested WITHOUT within-species replication, the two-Psi split (`Psi_phy + Psi_non`) is confounded, so `R/fit-multi.R` drops the extra uniqueness covstruct and warns before the multi-kernel gate. |
| COE-04 | Two-component recovery and kernel-separation diagnostics for Paper 2 Option B | `partial` (near-orthogonal Gaussian latent-only recovery, three-cell moderate-overlap recovery plus one harder moderate-edge boundary cell, high-overlap duplicate and near-duplicate collapse-equivalence gates, non-identical high-overlap failure-calibration, pre-fit raw/residualized kernel separability diagnostic plus deterministic pre-fit collinearity gate, two-sided selective absence, block-null smoke plus a 12-seed null diagnostic, empirical 95% null-tail threshold scaffold, and medium-signal grid, bounded Poisson construction fixtures plus two-cell Poisson recovery gate, mixed-family Gaussian+Poisson construction smoke plus two-cell mixed-family recovery gate, fixed-`rho` profile/sensitivity helper, fixed-`rho` effect-scale extraction, pair-specific point covariance extraction, point-estimate coevolutionary module extraction with a standardized-SVD oracle, and high-overlap fit/extraction warning covered; broader grid open) | `test-coevolution-two-kernel.R` (pre-fit `diagnose_kernel_separability()` raw/residualized candidate contract plus deterministic `K_tip` blend collinearity gate; fitted-object kernel-similarity diagnostic; high-overlap fit and `extract_Gamma()` warnings; heavy near-orthogonal two-component recovery with `extract_coevolution_modules()` real-fit contract, independent standardized-SVD oracle, `n_modules` truncation, and fixed-`rho` effect-scale module check; heavy three-cell moderate-overlap recovery plus one harder moderate-edge boundary cell; heavy high-overlap exact/near-duplicate collapse-equivalence; heavy non-identical high-overlap failure-calibration; heavy two-sided selective absence; heavy block-null smoke; heavy 12-seed null diagnostic, empirical 95% null-tail threshold scaffold, and medium-signal grid; heavy Poisson construction smoke plus heavy two-cell known-DGP Poisson recovery; heavy mixed-family Gaussian+Poisson construction smoke plus heavy two-cell Gaussian-host/Poisson-partner recovery; `profile_cross_rho()` contract and heavy fixed-`rho` sensitivity grid; fixed-`rho` `Gamma_effect` checks; `predict_cross_covariance()` real-fit object contract); `test-coevolution-recovery.R` (`predict_cross_covariance()` fake-fit contract; `extract_coevolution_modules()` fake-fit standardized-SVD contract) | First COE-04 slices landed 2026-06-18. A predeclared off-diagonal Frobenius-style kernel-similarity diagnostic is stored on multi-kernel fits as `fit$kernel_diagnostics`; it classifies low-overlap versus high-overlap kernel pairs, high-overlap fitted tier pairs now emit a warning that component-specific `Gamma_shape` separation is weak evidence, and `extract_Gamma(level = ...)` repeats that warning when the requested component participates in a high-overlap pair. The exported `diagnose_kernel_separability()` helper applies the same pre-fit claim-boundary rule to candidate kernels: the raw/aliased `K_tip` fixture is `high` overlap with recommendation `collapse_or_single_covariance`, while the residualized/opposed candidate is `near_orthogonal` with recommendation `separable_candidate`. A deterministic pre-fit collinearity gate blends the residualized `K_tip` candidate toward `K_phy`, verifies monotone off-diagonal similarity, and requires the rule to classify `near_orthogonal`, `moderate`, and `high` regimes with recommendations `separable_candidate`, `sensitivity_required`, and `collapse_or_single_covariance`. This is screening and claim-boundary evidence, not a formal identifiability proof. High-overlap collapse-equivalence gates cover an exact-duplicate kernel pair and a diagonal-shrink near-duplicate pair, verify the separated two-tier fit is not materially better than one collapsed rank-2 kernel, and keep the collapsed `extract_Gamma(level = "cross")` path quiet and finite. A separate non-identical high-overlap gate mixes `K_non` 85% toward `K_phy` before simulation (`similarity = 0.999876`), detects two-component signal over either one-component comparator, but fails promoted component separation (`Gamma_shape` own correlations 0.64 and 0.87, with one cross-component match 0.52); this is failure-calibration evidence only. The heavy DGP fits two latent-only named kernels (`phy`, `non`) with distinct low-overlap cross-lineage kernels; the full model beats either one-component model by >50 log-likelihood units, `extract_Gamma(level = "phy")` and `extract_Gamma(level = "non")` each recover their own `Gamma_shape` truth with correlation >0.95, `extract_Gamma(scale = "effect")` returns `rho_r * Gamma_shape_r` for each recorded fixed-`rho` component, `predict_cross_covariance()` multiplies `Gamma_shape_r` by fitted `K_r[i, j]` entries for pair-specific point covariance, `extract_coevolution_modules()` standardizes each component block as `R = Sigma_H^{-1/2} Gamma Sigma_P^{-1/2}`, and each component fails to match the other component's truth. The module gate now verifies the returned `R` matrices and singular values against an independent test-side inverse-square-root/SVD calculation, checks `n_modules` truncation, and confirms `scale = "effect"` scales the module matrix and singular values by the recorded fixed `rho`. The module helper is point-estimate output only; it does not provide module uncertainty, biological rank selection, `rho` estimation, or null-threshold calibration. A three-cell moderate-overlap grid blends the non association pattern 25%, 30%, and 35% toward the phy pattern, yielding overlap class `moderate`; each cell still beats one-component fits by >50 log-likelihood units, both component `Gamma_shape` correlations exceed 0.95, and cross-component matches remain below 0.25. A tested 40% moderate-edge cell also converges and strongly beats either one-component model, but `Gamma_phy` recovery falls below 0.95 and its cross-component match exceeds 0.25; that cell is therefore a boundary, not promoted separation evidence. A second heavy near-orthogonal selective-absence gate covers absence in both directions: the absent component's `extract_Gamma()` norm collapses below `1e-3`, the present component recovers its `Gamma_shape`, and the present-only model beats the absent-only model by >20 log-likelihood units while the full model does not materially improve over the present-only model. A third heavy block-null smoke gate sets both loading blocks to zero, verifies both extracted `Gamma_shape` norms collapse below `1e-3`, and checks the full model stays within 3 log-likelihood units of an intercept-only fit. A 12-seed heavy null diagnostic now keeps null component `Gamma_shape` norms bounded near zero and quantifies the full-vs-intercept overfit tail (median below `2`, at most two seeds above `3`, maximum below `8` log-likelihood units, empirical 95% tail marker below `8`), while two medium-signal fixtures retain full-versus-one-component likelihood separation, component-specific `Gamma_shape` correlations >0.90, and >10x separation from that fixed-grid tail marker. Poisson evidence now has two layers. Heavy construction smokes fit two bounded count fixtures with the same latent-only two-kernel syntax, verify convergence, finite log likelihood, near-orthogonal diagnostics, and finite component-specific `Gamma` point blocks. A heavy two-cell known-DGP recovery gate (`seed = 2801` and `2804`) then recovers both planted Poisson log-link component `Gamma_shape` blocks with correlations above `0.98`, beats either one-component Poisson comparator by more than `40` log-likelihood units, and keeps cross-component matches below `0.10`; this is narrow Poisson recovery evidence, not interval calibration or broader non-Gaussian recovery. Mixed-family evidence now also has two layers. A heavy construction smoke fits long-format Gaussian host traits plus Poisson partner traits through per-row family dispatch, verifies convergence, finite log likelihood, near-orthogonal diagnostics, finite component shared-Sigma blocks, and finite component `Gamma_shape` point blocks. A heavy two-cell Gaussian-host/Poisson-partner recovery gate (`seed = 2912` and `2913`) then recovers both planted component `Gamma_shape` blocks with correlations above `0.90`, beats either one-component comparator by more than `200` log-likelihood units, and keeps cross-component matches below `0.12`; this is narrow mixed-family shape-recovery evidence, not interval calibration or broad heterogeneous-trait coverage. `profile_cross_rho()` now formalizes the fixed-kernel rho profile: it rebuilds the phy component at `rho = c(0, 0.25, 0.55, 0.85)` while holding the non component fixed, and the heavy gate verifies the positive-`rho` fits plus the planted `rho = 0.55` fit strongly beat the block-null `rho = 0` fit. The best grid point may sit at the high edge, so this is fixed-kernel profile/sensitivity evidence only and not in-engine `rho` estimation or profile-interval coverage. Still open: harder moderate-overlap calibration beyond the promoted 0.25-0.35 grid, broader high-overlap truth-recovery/failure calibration beyond the collapse-equivalence, non-identical failure-calibration, and warning gates, formal reusable null-threshold / Type-I calibration beyond this diagnostic scaffold, in-engine `rho` estimation, `rho` profile intervals, broader interval coverage, module uncertainty/rank calibration, broader non-Gaussian/mixed-family recovery gates beyond the narrow Poisson and mixed-family cell pairs, and the separate post-arc Psi/deprecation grammar plan. Do not promote full scientific coverage until those rows move with evidence. |

COE-04 addendum (2026-06-19): `test-coevolution-two-kernel.R`
adds a fast reciprocal-dependence gate. A deterministic count-derived
`W_recip = sqrt(p(j|i) p(i|j))` raw tip kernel is `moderate` overlap
and requires sensitivity; a residualized-plus-opposed tip kernel is
`near_orthogonal` and a separable candidate. This is pre-fit diagnostic
evidence for the Paper 2 raw-vs-residualized `W` decision only.

COE-04 addendum (2026-07-04): `profile_cross_rho()` now resolves tied
log-likelihood maxima deterministically by the first maximum in the supplied
rho order, so `is_best` marks one row and `attr(best_rho)` remains scalar.
This is fixed-kernel profile/sensitivity plumbing only, not in-engine rho
estimation or interval coverage.

COE-04 addendum (2026-07-04): issue #686 is closed for the exported pre-fit
diagnostic. `diagnose_kernel_separability()` now aligns named kernels to the
first kernel's level order before computing off-diagonal similarity, and fails
loudly when named kernels do not share the same level set. Unnamed kernels keep
the old positional comparison contract. This is diagnostic hardening only, not
new coevolution recovery, interval calibration, or in-engine rho estimation.

### Section 7 — Mixed-family fits

Row-owner: **Boole + Fisher + Emmy** (mixed-family is the
vision-item-5 differentiator).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| MIX-01 | Engine accepts `family = list(...)` long format | `covered` | `test-stage37-mixed-family.R` | M0 baseline; 2026-07-04 adds named-list alignment for mixed-family lists so list names, when present, are authoritative over list order. Unnamed lists retain the legacy selector-level order contract. |
| MIX-02 | Per-row `family_var` column dispatch | `covered` | `test-stage37-mixed-family.R` | Local branch addresses issue #610: per-row `family_id_vec`, `link_id_vec`, and `family_per_row` are built after aligning named family lists to the selector levels, preventing silent wrong-family dispatch when list order differs from factor/sorted level order. GitHub issue closure still waits for push/PR/merge. The 2026-07-05 truth-lock adds fail-loud guards for duplicate and mismatched family-list names. |
| MIX-03 | `extract_Sigma()` on mixed-family fits | `covered` | `test-m1-3-extract-sigma-mixed-family.R`, `test-mixed-family-extractor.R`, `test-mixed-response-sigma.R` | M1.3 (PR #151) |
| MIX-04 | `extract_correlations()` on mixed-family fits | `covered` | `test-m1-4-extract-correlations-mixed-family.R`, `test-link-residual-15-family-fixture.R`, `test-fisher-z-correlations.R` | Point, Fisher-z, Wald, and bootstrap route shape/range are exercised; empirical mixed-family interval coverage remains uncalibrated. The nonlinear profile path is withdrawn and typed-refusal tested. `covered` applies to the extractor and admitted routes, not to profile intervals or calibrated coverage. |
| MIX-05 | `extract_communality()` on mixed-family fits | `covered` | `test-m1-5-extract-communality-mixed-family.R`, `test-mixed-family-extractor.R` | M1.5 (PR #154) |
| MIX-06 | `extract_repeatability()` on mixed-family fits | `covered` | `test-m1-6-extract-repeatability-mixed-family.R`, `test-mixed-family-extractor.R` | M1.6 (PR #154) — `vW` formula corrected to add per-family `sigma2_d` |
| MIX-07 | OLRE-bearing trait in mixed-family fits | `covered` | `test-mixed-family-olre.R`, `test-mixed-response-unique-nongaussian.R` | M0 baseline; M1.7 cross-tier integration via `test-m1-7-extract-omega-phylo-signal-mixed-family.R` |
| MIX-08 | `bootstrap_Sigma()` on mixed-family fits | `covered` | `test-m1-8-bootstrap-mixed-family.R`, `test-bootstrap-Sigma.R` | M1.8 (PR #157) — per-row family preserved via `fit$family_input`. Interval status (2026-07-05 audit): route/infrastructure only — `covered` marks per-row family preservation and refit plumbing; mixed-family bootstrap *calibration* is labelled PARTIAL/PLANNED in `R/bootstrap-sigma.R` (future M3 work). No calibrated mixed-family CI is claimed (gates CI-08/CI-10). |
| MIX-09 | `link_residual = "auto"` default (PR #101) | `covered` | `test-link-residual-auto-default.R`, `test-link-residual-15-family-fixture.R`, `test-link-residual-clamp.R` | M0 baseline |
| MIX-10 | Mixed-family with delta / hurdle family (latent-scale correlation) | `partial` (route-only) | `test-check-auto-residual.R` (asserts silence on single-family-per-trait mixed fits, line 47) | **Resolved 2026-07-05 (Design 02 §Hurdle/delta):** latent attaches to the positive submodel only, occurrence submodel fixed-effects-only → single latent scale → cross-family correlation defined on the **positive-part residual**, reported `interval_status = "route-only"` (uncalibrated, CI-08/CI-10). **Correction:** the prior `blocked` + class `gllvmTMB_auto_residual_delta_undefined` were never wired — that class exists in 0 files and `check_auto_residual()` is a MANUAL diagnostic that only aborts *within-trait* mixing (`gllvmTMB_auto_residual_incoherent`); the cited test asserts it is *silent* on single-family-per-trait mixed fits. **Completion (Codex lane):** guard random effects off the occurrence submodel (random slopes already blocked at `fit-multi.R:1495`) + use the positive-part σ²_d in `extract_correlations()`. |

### Section 8 — Extractors

Row-owner: **Emmy + Fisher** (extractor contract per
`06-extractors-contract.md`).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| EXT-01 | `extract_Sigma(level, part)` | `covered` | `test-extract-sigma.R`, `test-extractors.R`, `test-extractors-extra.R` | rotation-invariant; 2026-07-04 closes issue #682 boundary handling by preserving the non-degenerate covariance-to-correlation submatrix and returning `NA` only for zero-variance rows/columns. |
| EXT-02 | `extract_Sigma_B / W` legacy aliases | `covered` | `test-sigma-rename.R` | soft-deprecated via `lifecycle::deprecate_soft()` in 0.7.0 (`R/extractors.R`; #1194) |
| EXT-03 | `extract_Omega()` cross-tier | `covered` | `test-extract-omega.R`, `test-extractors-extra.R` | 2026-07-04 routes correlation conversion through the same safe zero-variance boundary helper as `extract_Sigma()`. 2026-07-05 adds a fail-loud summability guard: tiers that return non-trait covariance blocks, such as augmented intercept/slope structures, now direct users to `extract_Sigma(level = ..., part = "total")` instead of crashing with a non-conformable matrix addition. 2026-07-05 closes issue #587 locally: internally auto-detected `B` / `W` tiers now call `extract_Sigma(..., .skip_warn = TRUE)`, so users do not see deprecated-alias warnings they did not trigger. |
| EXT-04 | `extract_correlations()` methods | `covered` (point / Fisher-z / Wald) / `partial` (bootstrap) / `blocked` (profile) | `test-fisher-z-correlations.R`, `test-confint-bootstrap.R`, `test-spatial-depindep-binary.R`, `test-m1-4-extract-correlations-mixed-family.R` | Fisher-z/Wald preserve explicit unavailable-bound labels when effective sample size is missing. Bootstrap is route-specific and uncalibrated; unsupported spatial bootstrap requests return labelled Wald fallback rows. The nonlinear penalty-profile route is withdrawn and always aborts with its public class. |
| EXT-05 | `extract_communality()` | `covered` | `test-extractors.R`, `test-extractors-extra.R`, `test-derived-phylo-ci-audit.R`, `test-communality-ci.R` | Unit / unit_obs communalities remain covered; 2026-07-04 adds the phylogenetic-tier point route (`level = "phy"`) for `diag(Lambda_phy Lambda_phy') / diag(Lambda_phy Lambda_phy' + Psi_phy)`, guarded by the Ayumi #14 regression fixture. The extractor `ci = TRUE, method = "wald"` route now uses the scalar delta-method helper directly instead of the old bootstrap fallback. |
| EXT-06 | `extract_repeatability()` | `covered` | `test-extractors-extra.R` | |
| EXT-07 | `extract_phylo_signal()` | `covered` | `test-extractors-extra.R`, `test-extract-omega.R` | 2026-07-04 closes issue #677 by using elementwise-safe denominators for the three-component phylogenetic-signal decomposition. |
| EXT-08 | `extract_residual_split()` | `covered` | `test-extract-omega.R`, `test-extractors-extra.R` | |
| EXT-09 | `extract_ordination()` | `covered` | `test-ordiplot-VP.R`, `test-ordiplot-multi.R` | rotation-variant; warn |
| EXT-10 | `extract_cutpoints()` ordinal-probit | `covered` | `test-ordinal-probit.R`, `test-ordinal-recovery-depth.R` | Returns ordinal-probit cutpoints with planted-threshold recovery across `K = 2, 3, 4`, omitting binary `K = 2` traits that have no free cutpoint; mixed-family Gaussian + ordinal-probit + Poisson fits return cutpoint rows only for the ordinal trait. Heavy joint-depth evidence (`test-ordinal-recovery-depth.R`) covers cutpoints alongside intercepts and full between-unit `Sigma_B`. |
| EXT-11 | `extract_proportions()` delta-family | `blocked` | n/a | post-CRAN |
| EXT-12 | `extract_ICC_site()` legacy | `covered` | `test-extractors.R`, `test-extractors-extra.R` | superseded by `extract_repeatability()`; 2026-07-04 closes issue #683 by returning `NA`, not `NaN`, for zero total-variance denominators. |
| EXT-13 | `bootstrap_Sigma()` | `covered` (Gaussian) / `partial` (non-Gaussian) | `test-bootstrap-Sigma.R`, `test-stage3-propto-equalto.R` | M3.3b surface admission (Design 50) controls the next non-Gaussian evidence movement. Known-phi point diagnostics are not bootstrap coverage. Issue #596 guard: `propto()` / `phylo_scalar` unconditional simulation now redraws the phylogenetic effect with variance `lam_phy * Cphy` (`sqrt(lam_phy)` scale), so bootstrap-style refits no longer receive `lam_phy^2 * Cphy` random effects. This is simulation-input correctness, not bootstrap interval calibration. |
| EXT-14 | `getLoadings()` raw $\Lambda$ | `covered` | `test-rotate-compare-loadings.R` | rotation-variant; warn |
| EXT-15 | `rotate_loadings()` varimax / promax | `covered` | `test-rotate-compare-loadings.R`, `test-rotation-advisory.R` | Rotation is for interpretation of loading columns; covariance, correlation, communality, and uniqueness remain the primary rotation-invariant summaries. `plot(type = "ordination")` exposes the same order/sign-anchor convention for biplots. |
| EXT-16 | `getLV()` legacy ordination alias | `covered` | `test-extractors.R` | slated for `deprecate_soft()` 0.3.0 |
| EXT-17 | `getResidualCor / Cov()` glmmTMB-style | `covered` | `test-extractors.R` | |
| EXT-18 | `extract_Sigma_table()` report-ready Sigma/Psi/R table | `covered` | `test-extract-sigma-table.R`, `test-plot-gllvmTMB.R` | Point-estimate table view over `extract_Sigma()`; interval columns intentionally `none` / `NA`. The 2026-07-04 hardening closes issue #664 by forcing `part = "unique"` Psi tables to genuine diagonal entries, so default report tables no longer fabricate off-diagonal zero-Psi estimands. The 2026-07-05 routing guard closes issue #588 for table discovery by adding `cluster2` and named `kernel_*()` tiers to the available-level ledger; tests prove `level = "all"` reaches both `cluster2` and a named kernel tier. |
| EXT-19 | `plot_correlations()` / `plot_Sigma_table()` tidy covariance/correlation forest and confidence-eye plots | `covered` | `test-plot-covariance-tables.R` | Report-ready ggplot helpers over `extract_correlations()` and `extract_Sigma_table()` rows; intervals are displayed when input bounds are finite but not computed by the plotting helpers. Confidence eyes are soft filled frequentist compatibility displays with hollow estimate markers, not posterior densities; `style = "raindrop"` remains a compatibility alias. |
| EXT-20 | `extract_Sigma_table()` bootstrap interval rows | `covered` | `test-extract-sigma-table.R`, `test-plot-covariance-tables.R` | Converts `bootstrap_Sigma()` Sigma/R point estimates and percentile bounds to the same report-ready row schema; does not compute new bootstrap intervals or itself cover communality / repeatability rows. |
| EXT-21 | `extract_communality()` bootstrap object rows and communality plot intervals | `covered` | `test-extract-communality-bootstrap.R`, `test-plot-gllvmTMB.R`, `test-derived-phylo-ci-audit.R` | Reuses `bootstrap_Sigma(..., what = "communality")` point estimates and percentile bounds without rerunning refits; `plot(type = "communality", boot = boot)` overlays supplied `c^2` intervals. The bootstrap summary collector now stores `communality_phy` when `level = "phy"` is requested; broader bootstrap calibration remains governed by CI-08 / CI-10. |
| EXT-22 | `extract_repeatability()` bootstrap object rows and integration plot intervals | `covered` | `test-extract-repeatability-bootstrap.R`, `test-plot-gllvmTMB.R` | Reuses `bootstrap_Sigma(..., what = "ICC")` point estimates and percentile bounds without rerunning refits; `plot(type = "integration", boot = boot)` accepts raw `bootstrap_Sigma()` objects for repeatability and communality intervals. |
| EXT-23 | `plot(type = "correlation")` / `plot(type = "correlation_ellipse")` bootstrap correlation intervals | `covered` | `test-plot-gllvmTMB.R` | Merges `bootstrap_Sigma(..., what = "R")` percentile bounds into correlation heatmap and ellipse data; ellipse borders/stars mark supplied intervals that do not cross zero. |
| EXT-24 | `plot_correlations()` bootstrap object input | `covered` | `test-plot-covariance-tables.R` | Converts `bootstrap_Sigma(..., what = "R")` summaries to row-first correlation forest/confidence-eye plots without requiring hand-built pairwise rows. |
| EXT-25 | `compare_Sigma_table()` estimate-vs-truth table helper | `covered` | `test-extract-sigma-table.R` | Joins fitted/report-ready Sigma or R rows to a supplied truth matrix for simulation and teaching figures; table helper only, no plotting or calibration claim. |
| EXT-26 | `plot_Sigma_comparison()` estimate-vs-truth plot helper | `covered` | `test-plot-covariance-tables.R` | Plots `compare_Sigma_table()` rows as row-labelled error plots or estimate-vs-truth scatter plots, including optional `facet = "comparison"` panels for precomputed model/specification labels; visual comparison helper only, no simulation or calibration claim. |
| EXT-27 | `plot_Sigma_heatmap()` matrix-style Sigma/R heatmap helper | `covered` | `test-plot-covariance-tables.R` | Plots `extract_Sigma_table()` rows as trait-by-trait heatmaps for covariance or correlation matrices; point-estimate visual helper only, intervals are preserved in plot data but not displayed. |
| EXT-28 | `extract_rotated_loadings_table()` report-ready rotated loading rows | `covered` | `test-rotate-compare-loadings.R` | Row-first table over `rotate_loadings()` for ordination reports and figures; includes rotation, axis-ordering, sign-anchor, anchor-trait, raw axis-variance/share, and raw/standardized loading scale metadata. Point-estimate helper only; no loading uncertainty intervals. |
| EXT-29 | `plot_rotated_loadings()` rotated loading matrix helper | `covered` | `test-rotate-compare-loadings.R` | Plots fitted-model or `extract_rotated_loadings_table()` rows as a report-ready loading matrix with rotation/sign/loading-scale metadata preserved in `gllvmTMB_meta` / `gllvmTMB_data`; point-estimate visual helper only, no loading uncertainty intervals. |
| EXT-30 | `plot_correlations()` heatmap / ellipse matrix styles | `covered` | `test-plot-covariance-tables.R`, `test-plot-visual-snapshots.R` | Adds matrix-style correlation heatmap and ellipse/oval views over fitted-model, `bootstrap_Sigma()`, or `extract_correlations()` rows, with full/lower/upper triangle controls, diagonal control, optional estimate/CI labels, `matrix_layout = "estimate_ci"` for upper estimates plus lower interval bounds, `matrix_layout = "levels"` for two-level upper/lower matrices such as `unit` over `unit_obs`, and significance outlines/stars for supplied intervals that exclude zero. Snapshot guards cover the estimate-CI heatmap and two-level ellipse matrix layouts. Plotting helper only; it does not compute intervals or calibrate uncertainty. |
| EXT-31 | `extract_lv_effects()` and `extract_ordination(component = ...)` for predictor-informed latent scores | `partial` | `test-lv-parser-guard.R`; `test-julia-bridge.R`; `test-lv-gaussian-recovery.R`; `test-lv-bernoulli-depth.R`; `test-lv-factor-runtime.R`; `test-lv-missing-response.R`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-28-local-r500-summary.csv`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-30-local-binomial-r500-summary.csv`; `test-lv-cross-family-predictor-bridge.R`; `test-mixed-gaussian-lognormal-scale.R`; `docs/dev-log/artifacts/cross-family-lv-predictor/2026-08-27-local-canary-pre-run-receipt.md`; `docs/dev-log/artifacts/cross-family-lv-predictor/2026-08-27-all-family-pre-run-receipt.md` | `extract_lv_effects(type = "trait_effect")` returns rotation-invariant `B_lv = Lambda alpha^T`; `extract_ordination(component = c("total", "innovation", "mean"))` preserves total = innovation + mean. These point extractors operate on the admitted native registered-family composition route, and live all-family canaries return finite `B_lv_unit`. Raw axis `alpha` and `Lambda` remain orientation-dependent and are not cross-fit targets. Wald calibration remains limited to the previously certified Gaussian, binomial, and eight named Gaussian-anchor cells; arbitrary-composition intervals, factor intervals, non-Gaussian masks, Julia calibration, and structured-tier parity remain partial. |
| EXT-32 | `VP()` legacy variance-share residual semantics | `covered` | `test-ordiplot-VP.R` | 2026-07-04 closes issue #615: `VP()` no longer adds a fake Gaussian residual column to pure Poisson/non-Gaussian traits just because `sigma_eps` is reported. The legacy `residual` column is kept for Gaussian/lognormal observation residuals and is trait-specific in mixed-family fits. Family-aware latent-scale link residual shares remain the job of `extract_proportions()`, not `VP()`. |
| EXT-33 | `predict.gllvmTMB_multi(type = "response")` family/link dispatch | `covered` | `test-tidy-predict.R`, `test-missing-data-robustfix.R` | Local branch addresses issue #678 for the `newdata` family/link lookup: categorical family and link ids now use deterministic modal ids, not median/truncated numeric ids, so tied mixed ids cannot fabricate a family/link that never occurred. GitHub issue closure still waits for push/PR/merge. This is point-prediction dispatch only, not newdata simulation or prediction intervals. |
| EXT-34 | `extract_proportions()` zero-denominator boundary and wide-format contract | `covered` | `test-extractors-extra.R`, `test-extract-omega.R`, `test-mixed-response-sigma.R`, `test-proportions-ci.R` | 2026-07-04 closes issue #681 for the existing ordinary variance-share surface by returning `NA`, not `NaN`, when a trait has zero total variance. The same 2026-07-04 hardening closes issue #663 by making `format = "wide"` return per-component proportions that sum to one, with `total_variance` retained as the absolute denominator; multiplying a wide component column by `total_variance` recovers the absolute variance. This does not unblock the separate delta-family row EXT-11. |
| EXT-35 | `standard_errors()` — deferred TMB `sdreport()` for a fit made with `gllvmTMBcontrol(se = FALSE)` | `covered` | `test-standard-errors.R` | 2026-08-04. Parity with the eager path is verified **bit-exact** (`tolerance = 0`) on `par.fixed`, `cov.fixed`, and the fixed-effect summary, so this is the fit-time calculation deferred rather than a second route to standard errors — it adds no new inference and no new interval claim. **Same-session only**: a TMB ADFun's external pointers do not survive `saveRDS()`; the function raises a typed error rather than failing obscurely, but the limitation is shared by every `fit$tmb_obj` consumer and is not closed here. Measured while building it, and recorded so it is not over-read later: `sdreport()` reads `last.par.best`, which ordinary extractor calls do **not** move, so the internal-state replay mirrors fit time as fidelity, **not** as a guard — deliberate corruption of `last.par.best` changes the answer (max abs SE diff 0.20) and the replay does not recover from it. **Open, not closed by this row:** `.gllvmTMB_b_fix_se()` (`R/methods-gllvmTMB.R:209`) returns `NA` with no warning when `sd_report` is `NULL`, and `confint(method = "wald")` propagates that to an all-`NA` interval silently — originally confirmed by an ad hoc runtime probe that was never committed to `main` (its cited path, `dev/va-speed/60-se-false-consumer-probe.md`, does not exist in this repository's history), tracked separately. **→ CLOSED 2026-08-04 by EXT-36**, whose own row carries committed test evidence (`test-standard-errors.R`) for the closure; this row's `covered` status rests on that evidence, not on the uncommitted probe. |
| EXT-36 | Unusable standard errors are always reported, never returned as a silent all-`NA`/`NaN` answer | `covered` | `test-standard-errors.R` (6 gates: the abort on the fixed-effects path + its remedy text, the abort on the **variance-component target** path, both matching `se = TRUE` inverse guards, `summary()` surviving, and the note NOT firing when SEs exist) | 2026-08-04, closing the defect EXT-35 recorded OPEN. **Status is `partial`, not `covered`, and the scope is deliberately narrow — see the two carve-outs below.** Governed by shinichi-brain **D-33** — *"an error handler that converts 'cannot check' into 'fine' is the defect itself"*. **Split by whether the output is still useful:** `confint(method = "wald")` now **aborts** (`gllvmTMB_confint_no_sdreport`) naming both remedies, because an all-`NA` interval is a non-answer that flows onward unmarked; `summary()` and `extract_cutpoints()` **keep working but report why** the SE column is empty, because point estimates are the package's supported claim and erroring would punish a legitimate fast-fit workflow. Deliberate immediate behaviour change, not staged: pre-1.0 experimental, no released code can depend on the old return. **Verified with tests written BEFORE the fix and confirmed failing** (3 failures → 0). **Scope deliberately reduced from plan:** the `{value, status}` refactor of `.gllvmTMB_b_fix_se()` was dropped as unnecessary once no user-facing surface reports its NA silently — recorded as adaptive, not dropped. **Two carve-outs, both found by adversarial review AFTER this row first claimed `covered` — the correction is the honest part of this entry.** (1) **A near-miss on the closure claim itself:** the first version of the guard sat in the fixed-effects path only, while `.confint_wald_targets()` (`R/z-confint-gllvmTMB.R:1309-1312`) returns **nineteen lines earlier** at `:1694` and swallows a NULL `sd_report` into `NA_real_` — so `confint(parm = "sigma_eps", method = "wald")` still returned a silent all-`NA` interval for 7 of 7 non-`b_fix` targets while NEWS and this row said the defect was closed. Both routes now share `.confint_require_sdreport()`; a regression test covers the target path. (2) **Non-finite SEs — the second carve-out, now CLOSED the same day.** The gate originally keyed on `sd_report` being **missing**, not on the SEs being **usable**, so a non-positive-definite Hessian still produced bare `NaN`. Both surfaces now detect it: `summary()` reports it with **different advice** (`gllvmTMB_diagnose()`, because the numbers were already computed and `standard_errors()` cannot help), and `confint()` aborts with `gllvmTMB_confint_nonfinite_se`. Fixing `summary()` alone would have repeated carve-out (1) exactly. **The discriminator is ALL-non-finite, not any:** a single `NA` is the legitimate mapped-out-coefficient case, and an inverse guard asserts a healthy fit with one fixed coefficient still summarises and still returns intervals. **That coverage gap is now closed** (2026-08-04, same day): `extract_cutpoints()` gained an `ordinal_probit` fixture and two gates — it *reports and returns usable estimates* rather than aborting, and the note is **silent** where no `tau_se` column is displayed (`print()`/`tidy()` pass the new `quiet = TRUE`). The second gate closes a *noise* defect the same review found: the note had been firing from `print(fit)`, explaining a column the reader was not looking at. The **main regression risk is also now guarded explicitly**: a mapped-out `Xcoef_fixed` coefficient still returns `NA` **silently**, with no note, no warning, and no `confint()` abort — a fixed parameter legitimately has no standard error, and flattening that into the defect class was the way this change could have done harm. **Untouched and flagged, NOT part of this row:** `.wald_block()` (`R/profile-ci.R:487-512`) has the identical pattern but is **dead code** — zero callers in `R/`, `tests/`, `dev/` — while `dev/aghq-scope/06-consumers.md:44` still claims it is *"called by all Wald confint routes"* and cites stale line numbers; and **`vcov()` / `coef()` are not implemented for `gllvmTMB_multi`** — both exist only for `gllvmTMB_va`, where they deliberately refuse. Roxygen at `R/gllvmTMB.R` had claimed `vcov()` dispatched on `gllvmTMB`, which was **never true of any release**; the wider defect was that *no* method is registered on the bare `gllvmTMB` class at all — every one is on a subclass. **Documentation corrected 2026-08-04 to state what was true, and then — after maintainer approval the same day — the two methods were ADDED** (`R/vcov-coef.R`, row EXT-37). Raising the API change rather than making it unilaterally was the right order: the docs were made honest first, so the repository was never in a state where it promised something it did not do. |
| EXT-37 | `vcov()` / `coef()` for `gllvmTMB_multi` | `covered` | `test-standard-errors.R` (4 gates) | 2026-08-04, added on maintainer approval after EXT-36's audit found the roxygen had promised `vcov()` for releases in which it did not exist. Both read what the fit already carries; neither computes anything new. **`coef()` requires no `sdreport()`** — point estimates are the package's supported claim — and is asserted to agree with the eager fit on an `se = FALSE` fit. **`vcov()` reuses `confint()`'s gate verbatim**, so it raises the same `gllvmTMB_confint_no_sdreport` / `gllvmTMB_confint_nonfinite_se` conditions: a caller handling one handles the other. Cross-checked against the other surface — `sqrt(diag(vcov(fit)))` is asserted equal to `summary(fit)$fixef$Std.Err`, so the two cannot silently disagree about the same fit. Coefficients held fixed via `Xcoef_fixed` give `NA` rows/columns, with a test: a parameter that was not estimated has no sampling covariance, and that is the legitimate-`NA` case EXT-36 exists to protect. |
### Section 9 — Diagnostics

Row-owner: **Curie + Fisher** (diagnostic / identifiability).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| DIA-01 | `sanity_multi(fit)` | `covered` | `test-sanity-multi.R`, `test-penalised-gradient-1092.R` | #1092 (2026-08-17): the gradient row judges the objective the fit actually optimised — on a ridged fit (finite `aghq_ridge`) it reads the penalised gradient via `.gllvmTMB_penalised_gradient()` and labels the printed value "penalised objective". Previously it printed the raw TMB gradient, which sits at `lambda/tau^2` at a perfectly converged MAP optimum. |
| DIA-02 | `gllvmTMB_check_consistency(fit)` (PR #105) | `covered` | `test-check-consistency.R` | |
| DIA-03 | `check_identifiability(fit, sim_reps)` (PR #105) | `covered` | `test-check-identifiability.R` | |
| DIA-04 | `check_auto_residual(fit)` (PR #104) | `covered` | `test-check-auto-residual.R` | |
| DIA-05 | `gllvmTMB_diagnose(fit)` | `covered` | `test-gllvmTMB-diagnose.R` | |
| DIA-06 | Multi-start sdreport / report consistency (PR #100) | `covered` | `test-multi-start-sdreport-consistency.R` | |
| DIA-07 | Profile-curve shape inspection (`confint_inspect()`, PR #121) | `covered` | `test-confint-inspect.R` | |
| DIA-08 | `check_gllvmTMB(fit)` machine-readable fit-health table | `covered` | `test-sanity-multi.R` | Design 49 + #248 + #523. Returns stable rows for optimizer convergence, gradient, `sdreport()` availability, `pdHess`, Hessian rank, fixed-effect SEs, restart provenance, selected restart, simple boundary flags, loading-rotation convention, weak latent-axis share, near-zero `psi`, `sigma_eps` boundary, broad cross-loading structure, and a binomial prevalence/loading/saturation screen for near-constant binary traits. Treats `pdHess = FALSE`, weak latent-axis rows, and the binomial near-constant screen as inference / identifiability warnings rather than automatic point-estimate failure. **2026-08-17 (issue #897): two categorical degeneracy rows added** -- `ordinal_liability_loading` (fid 14; arms O1/O2 ship DISARMED at `Inf`/`Inf` after calibration found no threshold clearing the frozen sensitivity/FP conjunction, see FAM-14) and `multinomial_contrast_degeneracy` (fid 16; arms M1/M2/M3 armed at 1e-10 / 0.99 / 0.02, the M1 relative sub-arm disarmed at `Inf`). Both carry their measured sensitivity and specificity in the shipped roxygen; see FAM-14 and FAM-20 for the numbers (ordinal: no separable threshold, disarmed; multinomial FPR bound ~7.5%, 40 informative healthy fits -- a bound, not a verified zero). **Neither row is wired to a fit-time warning**: `check_gllvmTMB()` surface only, no `gllvmTMB()` call warns differently than before, and making them automatic is a maintainer decision. Also fixed here: `near_zero_psi_unit` no longer fires on entries the auto-Psi skip block deliberately pinned at `log(1e-6)` (single-trial Bernoulli and every multinomial contrast pseudo-trait), a structural false positive that predated the Design 123 arc and was live-confirmed on a healthy mixed multinomial+gaussian fit whose free trait had sd 0.312. **Still NOT re-calibrated: the binomial screen's own measured ~25% false-positive rate** (#897 point 2), which the ordinal campaign diagnosed as the same absolute-threshold transport failure -- spun out as a follow-up rather than rushed into this arc. **2026-08-17 (issue #1092): the gradient underlying `fit_health$max_gradient` -- and with it the `converged` conjunction and every row built on it -- is now the gradient of the objective the optimiser actually minimised.** On a ridged fit the raw `tmb_obj$gr()` omits the R-level loading penalty and reports `lambda/tau^2` at a genuinely stationary MAP optimum, so ridged fits were systematically misread as unconverged. `.gllvmTMB_penalised_gradient()` is the single accessor; `fit_health$gradient_is_penalised` discloses which objective the number describes; the raw gradient stays reachable via `tmb_obj$gr()`. Evidence: `test-penalised-gradient-1092.R` (fails against the pre-fix code). Warm-restart / ISDM gates were audited and left raw deliberately: they refuse ridged fits (`is.null(ridge_tau)` in their eligibility conjunctions), so their raw reads are unreachable on the ridged path.  **2026-08-17 (issue #1098, `partial` -- the binomial `binomial_prevalence_loading` arm specifically, not DIA-08 as a whole): #897's 25% figure was attributed by arm.** Two calibration pools disagree on RATE but agree on MECHANISM (`dev/heywood/fp-attribution.R`, `dev/heywood/fp-attribution-findings.md`): the original 3,944-fit pool never exercises the failing regime (0/551-2,499 healthy false positives, loading scale fixed at 0.7-1.0), while a pool built to cross `sigma_lambda` in `c(0.7, 3.0)` (928 healthy / 272 degenerate binomial-probit fits; `3.0` chosen to hit issue #847's `aghq_ridge` ridge-failure regime, not argued for realism) reproduces #897's 232/928 = 25% exactly, and attributes ALL 232 of those false positives to the single `extreme_magnitude` arm (`loading_absolute_thresh`) -- `runaway_loading` and the prevalence-gated branch contribute zero. An oracle exceedance calculation over the DGP's own `N(0, sigma_lambda^2)` loading draws independently predicts the same pattern (`P(max|Lambda_true| >= 6)` 0.67-0.92 at `sigma_lambda = 3`, falling to 0.17-0.34 at threshold 8) and closely matches the measured FPR at threshold 8, turning the correlation into a mechanism with a number attached. `loading_absolute_thresh` raised 6 -> 8 (this same evidence): FPR on that pool 0.2500 -> 0.1552, sensitivity on its degenerate fits 1.0000 -> 0.9963 (one additional missed fit of 272). **This is an interim retune, not a resolution**: the arm is measurably regime/effect-size dependent -- NOT response-scale dependent in the #851/#855 sense, since probit fixes the residual variance at 1 and there is no free response scale here to standardise away (FPR 3.85% at `sigma_lambda = 0.7` vs 49.08% at `sigma_lambda = 3.0`, the scale `aghq_ridge = 2` already struggles at per #847), so a fixed constant cannot be correct across every loading scale -- `aghq_ridge = 2` reduces but does not remove it (46.0% -> 13.5% at the larger scale). Also unmeasured: the calibration is probit-only, and logit loadings run ~1.6-1.8x larger for the same model, so this FPR should read as a lower bound for logit fits. Filed as a NEGATIVE scoping result for #851/#855, not a to-do item: the class's usual per-fit rescaling device has no Bernoulli analogue and the natural alternative collapses into the existing `loading_relative_thresh` ratio arm -- see `dev/heywood/fp-scale-dependence.md`. |
| DIA-09 | `sdreport()` failure degradation path | `covered` | `test-sanity-multi.R` | Design 49. Fit construction wraps `TMB::sdreport()` and records `fit$sdreport_error`; diagnostics are covered on a forced degraded object and on a deterministic in-fit `TMB::sdreport()` failure fixture using `testthat::with_mocked_bindings()` to fail the namespaced `TMB::sdreport()` call during `gllvmTMB()` construction. The returned fit keeps point estimates, stores the failure message, sets `fit_health$sdreport_ok = FALSE`, and `check_gllvmTMB()` reports a WARN `sdreport` row. |
| DIA-10 | `gllvmTMBcontrol(se = FALSE)` hard-fit point-estimate path | `covered` | `test-sanity-multi.R`, `test-gllvmTMBcontrol.R` | Design 49. Lets hard models skip `TMB::sdreport()` intentionally, return point estimates, and route uncertainty to bootstrap/profile workflows. Diagnostics report `sdreport` as `WARN` with a skipped-SE message. |
| DIA-11 | `predictive_check()` fitted-model diagnostic plots | `covered` (`rq_qq` inherits DIA-12's 13-of-17-family exact-residual coverage; `stat_grouped`/`dens_overlay` draw from `simulate()`, now family-correct for 16 of 17 families) / `partial` (rootogram stays scoped to Poisson/NB1/NB2 count families, and formal tests remain out of scope) | `test-predictive-diagnostics.R` | Design 51 + #228. Exports package-specific fitted-model predictive plots rather than a Bayesian `pp_check()` claim. Q-Q, rootogram, grouped-statistic, and density-overlay plots return `ggplot` objects with plotted data, `check_gllvmTMB()` rows, and `fit$fit_health` metadata in `attr(plot, "gllvmTMB_diagnostic")`. **2026-08-17:** `type = "rq_qq"` calls `residuals()` directly, so it inherits DIA-12's #1085 exact-CDF coverage (13 of 17 families) with no separate scoping of its own. `type = "rootogram"` (`.gllvmTMB_rootogram_data()`) is unaffected by #1085/#1086 and remains hard-filtered to `family_id %in% c(2L, 5L, 15L)` (Poisson, NB2, NB1) because it compares observed vs. expected *count* frequencies. `type = "stat_grouped"` and `type = "dens_overlay"` draw from `simulate()`-backed fitted-model draws (`.gllvmTMB_predictive_draws()`); per #1086 those draws are now family-correct for 16 of 17 families (all but tweedie) rather than silently substituting a wrong distribution for nine of them, so these plot types are more trustworthy than before even though their family scope is unchanged. Rootogram auto-`max_count` is bounded and pools extreme simulated tails unless the user supplies an explicit `max_count` (#693). Diagnostic display only; no interval calibration, latent-rank proof, or DHARMa-equivalent formal test. |
| DIA-12 | `residuals.gllvmTMB_multi()` randomized-quantile and simulation-rank residuals | `covered` (13 of 17 runtime families get exact family-CDF residuals; `simulation_rank` fallback for the rest) / `partial` (tweedie, delta_lognormal, delta_gamma, multinomial have no exact residual) | `test-predictive-diagnostics.R` | Design 51 + #228. **2026-08-17 (#1085):** exact family-CDF randomized-quantile residuals now cover gaussian (0), binomial (1, logit/probit/cloglog), poisson (2), lognormal (3), Gamma (4), nbinom2 (5), Beta (7), betabinomial (8), student-t (9), truncated_poisson (10), truncated_nbinom2 (11), ordinal_probit (14), and nbinom1 (15) — 13 of the 17 runtime family ids (`.valid_family` in `R/enum.R`), up from the 4 previously implemented (gaussian, poisson, nbinom2, nbinom1). Not implemented, per the design rationale recorded in `.gllvmTMB_exact_rq_residuals()` (`R/predictive-diagnostics.R`): tweedie (fid 6, no closed-form CDF without a new dependency), delta_lognormal / delta_gamma (fid 12/13, the hurdle point-mass-at-zero split is an undecided design choice), and multinomial (fid 16, unordered categories have no residual ordering to invert). `type = "simulation_rank"` is the fitted-model simulation fallback for rows the exact route does not cover. **2026-08-17 (#1086):** the fallback now draws from the true per-row family for 16 of the 17 families (all but tweedie) via `.draw_y_per_family()` (`R/methods-gllvmTMB.R`) — previously nine families silently fell through to a Gaussian-on-link-scale draw instead of their real distribution. Unsupported tweedie rows now return `NA` with a per-call `cli::cli_warn()` rather than a wrong number. Unsupported or non-finite rows are retained with explicit `status`, and residual objects carry `check_gllvmTMB()` / `fit_health` metadata. |
| DIA-13 | `diagnostic_table()` metadata table extraction | `covered` | `test-predictive-diagnostics.R` | Design 51 + #228 follow-up. Extracts plotted/residual data, row-status counts, fit-health status counts, and the attached `check_gllvmTMB()` rows from `predictive_check()` plots or diagnostic residual data frames without requiring article code to inspect `attr(x, "gllvmTMB_diagnostic")` directly. Table extraction only; no new diagnostics, formal tests, refits, or uncertainty calibration. |
| DIA-14 | `screen_gllvmTMB()` pre-fit response screening | `partial` overall; `covered` fixed-design binomial separation module | `test-screen-gllvmTMB.R`; `test-screen-separation.R`; `docs/design/2026-06-22-pre-fit-response-screening.md` | Formula-aware pre-fit screen for candidate binary/binomial traits before fitting a stacked-trait GLLVM. Covers wide `traits(...)` and long `trait =` paths, response-missing row dropping, Bernoulli / `cbind(success, failure)` / `weights = n_trials` binomial modes, denominator-aware prevalence and minority-count summaries, duplicate/complement pair flags, fixed-effect rank deficiency, and `d >= n_traits` warnings. The opt-in `separation = "fixed"` module certifies overlap/complete/quasi-complete separation for coefficient-connected fixed-design blocks under logit/probit/cloglog links, finite offsets, maps/ties, and structural-zero constraints; grouped binomial, constants, rank failure, missing predictors, missing detector versions, and solver failures are typed outcomes. It does not inspect latent loading/covariance divergence, select variables, delete traits, choose rank, or guarantee convergence. Overall screening remains partial because non-binary modules and high-dimensional systematic-map performance benchmarks remain planned. |

### Section 10 — Confidence intervals


Row-owner: **Fisher** (inference completeness lead).

**Current interval-calibration disposition (2026-08-25; supersedes stale
addenda inside CI-08--CI-15 rows below).** The release-wide campaign and exact
19-route/state oracle are retained in
`docs/dev-log/artifacts/interval-calibration/2026-08-25-terminal-campaign-evidence.md`,
`docs/dev-log/artifacts/interval-calibration/2026-08-25-target-recomputation.csv`,
and `docs/dev-log/artifacts/interval-calibration/public-route-census.csv`.
CI-08 has no certified cell: callable and historical rows are `limited`, every
computed `profile_ci_total_variance()` row is `route-only`, and the exact PVT-02
campaign is `blocked` because constrained-refit fidelity is not auditable from
retained endpoints. CI-09 is campaign-`blocked` by the frozen DGP's estimand
identifiability; its routing/algebra evidence remains separate. CI-10 is
`blocked` for nonlinear profile and `limited` for the named Wald/bootstrap/
contrast routes. CI-11/12 remain typed `refused` public nonlinear profiles.
CI-13 certifies only structurally free strict-lower symmetric joint-delta Wald
targets in native pinned unrotated ordinary-Gaussian three-trait cells
`(n=150,d=2)`, `(n=400,d=1)`, and `(n=400,d=2)`; `(n=150,d=1)` and the global
route remain `limited`. This is one frozen DGP, conditional on eligible fits:
trait intercepts `(-0.20, 0.10, 0.25)`, unique SDs `(0.70, 0.80, 0.90)`, and
loading vector `(0.80, 0.45, -0.35)` for `d=1`, with second column
`(0, 0.70, 0.40)` for `d=2`. No other truth-parameter regime inherits the
certificate. CI-14 and both CI-15 campaign routes are `blocked` by,
respectively, frozen-source provenance and the unrun predecessor dependency.
These campaign states do not remove already implemented point or route
plumbing; availability never widens a certificate.

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| CI-01 | Wald CI via `confint(method = "wald")` | `covered` for fixed effects and pure-diag Sigma; guarded for reduced-rank total Sigma | `test-tidy-predict.R`, `test-stage1-stacked-fixed-effects.R`, `test-profile-ci.R`, `test-confint-bootstrap.R` | M0 baseline. 2026-07-04 closes issues #620/#621 for Sigma Wald rows: when `latent()` is present at a tier, diagonal total-Sigma estimates no longer receive residual/Psi-only theta_diag bounds; pure-diagonal tiers still receive finite Wald bounds. |
| CI-02 | Direct/simple profile CI via `confint(method = "profile")` (PR #109) | `covered` | `test-profile-ci.R`, `test-profile-targets.R`, `test-confint-lambda.R` | Covered only for direct/simple targets such as fixed effects, direct scale parameters, and Lambda entries, including pinned-entry handling. It does not cover canonical repeatability or nonlinear communality/correlation/proportion routes; see blocked CI-04, CI-06, and CI-07. |
| CI-03 | Bootstrap CI via `confint(method = "bootstrap")` (PR #109) | `covered` | `test-confint-bootstrap.R` | |
| CI-04 | Canonical repeatability profile | `blocked` | `test-confint-derived.R`, `test-profile-route-matrix.R` | The former token estimated only a diagonal-companion ratio and omitted shared latent variance. Public ICC profile requests now abort with `gllvmTMB_repeatability_profile_withdrawn`. Restoration requires a target-explicit full-Sigma estimand, an exact or independently tolerance-certified constraint solver, an exposed optimizer-status ledger, retained failed endpoints, target-specific calibration, and explicit maintainer promotion. |
| CI-05 | `profile_ci_phylo_signal()` (PR #105) | `partial` for multi-component profile; covered for two-component profile and numeric-Wald fallback | `test-profile-ci.R`, `test-phylo-signal-ci.R`, `test-derived-phylo-ci-audit.R` | Two-component `phylo_diag + diag_species` remains direct profile via the log-SD contrast. For richer 3+ component PGLLVM fits, full fix-and-refit profile is still planned; the 2026-07-04 Ayumi #14 repair returns finite numerical delta-method Wald bounds labelled `wald(numeric)` instead of empty `NA` bounds. Do not describe 3+ phylogenetic-signal profile-LR as covered. |
| CI-06 | Nonlinear communality profile | `blocked` | `test-confint-derived.R`, `test-profile-route-matrix.R` | The former penalty-profile prototype is withdrawn across unit, unit_obs, phylogenetic, and spatial tiers. Public requests abort with `gllvmTMB_nonlinear_profile_withdrawn`. Restoration requires an exact or independently tolerance-certified constraint solver, an exposed optimizer-status ledger, retained failed endpoints, target-specific calibration, and explicit maintainer promotion. |
| CI-07 | Nonlinear correlation profile | `blocked` | `test-confint-derived.R`, `test-profile-route-matrix.R`, `test-phylodepindep-binary.R`, `test-spatial-depindep-binary.R` | The former penalty-profile prototype is withdrawn across ordinary, source-specific, and augmented tiers. Point/Fisher-z/Wald/bootstrap routes have separate statuses. Public profile requests abort with `gllvmTMB_nonlinear_profile_withdrawn`. Restoration requires an exact or independently tolerance-certified constraint solver, an exposed optimizer-status ledger, retained failed endpoints, target-specific calibration, and explicit maintainer promotion. |
| CI-08 | `coverage_study()` ≥ 94 % empirical coverage gate (PR #120) | `partial` (M3.3 production gate failed) | `test-coverage-study.R`; `dev/precomputed/coverage-gaussian-d2.rds` (R = 200, PR-0C.COVERAGE); `docs/dev-log/audits/2026-05-19-m3-production-grid-artifact-review.md` (R = 200 historical Actions run 26100827665) | M3.3 profile-psi production run completed 2026-05-19 before D-50: 15/15 jobs ran, but only Gaussian d=1 and Gaussian d=3 cleared the 94 % gate; 13/15 cells remain below gate and 236/3000 replicate fits failed. The receipt is retained as negative historical evidence, not a current compute route. No production RDS promoted to `inst/extdata/`; Design 50 now requires surface admission, target-explicit total `Sigma_unit[tt]`, and a diagnostic report before coverage claims move. New campaigns run on Totoro/DRAC and retain all attempts outside Actions artifacts. **Addendum 2026-07-29 — two cells now clear, on a different route; this row stays `partial`.** A fresh-seed 20,000-replicate campaign on the `profile_total` route (a genuine chi-square_1 profile on log V_t — *not* `coverage_study()`'s psi route) met a pre-registered `coverage >= 0.94` gate in both scoped cells: gaussian d=1 n=150 coverage 0.9467169 (2·MCSE band 0.9434896), gaussian d=2 n=150 coverage 0.9467216 (band 0.9435366), with complete failure accounting (n_attempted 20,000; n_failed 628 / 112). A three-lens D-43 panel returned **CERTIFY 3-0**. **The status does NOT move to `covered`, for three reasons:** the certified route `.profile_ci_total_variance()` is **unexported**, so no user-reachable capability changed — the exported `bootstrap_Sigma()` route for the same estimand covered **0.78** in this same campaign; the **psi target FAILS** on the same run (0.9384 d1, 0.8653 d2); and 13/15 of this row's original `coverage_study()` cells remain below gate. Scope of the certificate: two-sided only (upper-tail misses ≈1.53× lower, so one-sided use is invalid), a marginal average that fails in the smallest-V_t ventile (d1 0.9259, d2 0.9369), conditional on convergence (96.9% / 99.4%), at a **0.94 floor and never nominal 95%** — which both cells miss by ≈3.3 clustered SEs. The two cells share 19,000 of 20,000 seeds and are therefore not independent replicates. Full disposition: `docs/dev-log/2026-07-29-certificate-disposition.md`. **Addendum 2026-08-02 — TWO OF THE THREE REASONS ABOVE NO LONGER HOLD.** (1) *"the certified route is unexported, so no user-reachable capability changed"* is **stale**: commit `f04c066c` exported `profile_ci_total_variance()` behind a per-row `interval_status` fence (`NAMESPACE:174`, wrapper at `R/profile-derived.R:1010`, delegating to the certified `.profile_ci_total_variance()` at `:856`). The certified route is now user-callable. (2) *"the exported `bootstrap_Sigma()` route covered 0.78"* is **not a property of `bootstrap_Sigma()`** — it is a property of `bootstrap_Sigma(n_boot = 10)`. The campaign ran the bootstrap arm at **ten replicates** against a documented default of 200 (`docs/dev-log/2026-07-29-certificate-run-record-v2.md:9-13`, `NBOOT=10`). A percentile interval from B draws cannot cover more than (B−1)/(B+1) = **0.818** at B = 10, whatever the data are. Holding the draws fixed and varying only B: **0.8073 (B=10) → 0.9418 (B=200, the default)**, against 0.9491 for the profile route — the 17-point gap collapses to ~0.7 points, inside 1 MCSE. An analytic scaled-χ² check attributes ~16 of the 17 points to the measurement artifact and ~0.5–1 to the percentile method's known second-order inaccuracy. **Do not repeat the 0.78 figure without re-measuring at `n_boot >= 200`.** The third reason (13/15 `coverage_study()` cells below gate) also warrants care: those cells gated on rotation-**variant** per-trait `psi` (`theta_diag_B`), a proxy PR #364 retired on 2026-05-31 in favour of the rotation-invariant `Sigma_unit_diag` (`docs/design/66-capstone-power-study.md:167-176`); `psi` is now diagnostic-only, never gated. `bootstrap_Sigma()` now returns `$coverage_ceiling` so a campaign can assert `coverage_ceiling >= conf` before trusting its own numbers. Row still `partial` — the certificate's scope fences are unchanged and most cells remain unmeasured on the corrected estimand. Full analysis: `docs/dev-log/audits/2026-08-02-ci08-coverage-explained.md`. **CI-08 withdrawal addendum (2026-08-25):** The former profile-total certificate is withdrawn. The implementation is a penalty-profile approximation, and neither the historical `n=150,d=1/2` endpoints nor PVT-02 retain enough constrained-refit convergence and target-fidelity evidence to establish the exact likelihood-ratio contract. Every computed public row is now `route-only`; historical/callable census rows are `limited`, and the exact PVT-02 campaign is `blocked`. The numerical coverage measurements remain historical evidence, not certificates. Current terminal evidence: `docs/dev-log/artifacts/interval-calibration/2026-08-25-terminal-campaign-evidence.md`. |
| CI-09 | Fisher-z CI on correlations | `covered` | `test-fisher-z-correlations.R` | 2026-07-04 also guards `extract_correlations(method = "bootstrap", tier = "all")` so one shared `bootstrap_Sigma(what = "R")` call serves all bootstrappable requested tiers rather than rerunning identical refits per tier; this is runtime plumbing, not new interval calibration. 2026-07-05 removes the old hard-coded `n_eff = 30` fallback for missing or too-small tier counts; the same test file covers the unavailable-interval boundary and the explicit `n_eff = 4` acceptance case. **CI-09 calibration addendum (2026-08-25):** Routing/algebra tests remain `covered`, but the frozen six-cell calibration campaign is `blocked` because one Gaussian pair per unit identifies total covariance rather than the separately scored unit-tier correlation. The extreme coverage values are invalid calibration evidence, not a Fisher-z method certificate or clean method failure. |
| CI-10 | Mixed-family intervals | `blocked` (profile) / `partial` (Wald/bootstrap calibration) | `docs/dev-log/audits/2026-05-19-m3-production-grid-artifact-review.md` | The former mixed-family profile prototype failed its coverage gate and is withdrawn. Historical cells remain negative evidence. Wald/bootstrap require their own target-explicit calibration before stronger claims. |
| CI-11 | Internal profile route matrix and refusal ledger | `covered` for policy truth-table only | `test-profile-route-matrix.R`; `test-profile-ci.R`; `test-confint-derived.R`; `test-profile-proportions.R`; `test-proportions-cluster-components.R`; `docs/design/73-profile-likelihood-route-matrix.md`; `docs/design/74-augmented-profile-target-table.md` | The matrix covers direct/simple routes, structural-zero/point-only states, and explicit blocked states. Every nonlinear repeatability/communality/rho/proportion profile row is constructed as blocked; typed refusals are public behavior. Direct scale/Lambda and retained phylogenetic-signal routes keep their separate evidence. This row is not interval calibration. |
| CI-12 | Internal derived-profile curve/refit prototypes | `covered` for internal regression only; public nonlinear routes `blocked` | `test-profile-derived-curves.R`, `test-profile-derived-refit.R` | LR-baseline, failed-probe filtering, and finite-point inversion remain tested research machinery. They do not support a public repeatability, communality, correlation, or variance-proportion profile interval claim while the penalty constraint and optimizer-status contract are withdrawn. |
| CI-13 | Standardized-loading joint-delta inference and scale-labelled decision routes | `covered` deterministic algebra/routing; three exact target-specific cells `certified`; global route `partial` | `test-loading-ci.R`, `test-loading-ci-bootstrap.R`, `test-confint-lambda.R`, `test-rotate-compare-loadings.R`; `test-interval-calibration-ci13.R`; `2026-08-25-target-recomputation.csv` | Issue #921. Standardized loadings use `rho[t,k] = Lambda[t,k] / sqrt(Sigma_total[t,t])`, matching the existing standardized point extractor. The numerical Jacobian evaluates the complete standardized-loading vector against `cov.fixed`, including same-trait cross-axis covariance and fitted denominator uncertainty. The 5,000-replicate campaign certifies only structurally free strict-lower symmetric joint-delta Wald targets in native pinned unrotated ordinary-Gaussian three-trait cells `(n=150,d=2)`, `(n=400,d=1)`, and `(n=400,d=2)`; every free target passed both `coverage >= 0.94` and `coverage - 2*MCSE >= 0.94`. This is one frozen DGP, conditional on eligible fits: trait intercepts `(-0.20, 0.10, 0.25)`, unique SDs `(0.70, 0.80, 0.90)`, and loading vector `(0.80, 0.45, -0.35)` for `d=1`, with second column `(0, 0.70, 0.40)` for `d=2`; availability was 98.82%, 93.38%, and 96.18% in the three certified cells. The `(n=150,d=1)` cell failed. Pinned diagnostic rows, Fisher-z Wald, arbitrary constraints, raw/profile/bootstrap targets, rotated/unconstrained fits, neighbouring cells, and every other truth-parameter regime remain uncalibrated; exact-cell evidence never promotes the global route. |
| CI-14 | `slope_sd_ci()` -- Wald log-scale intervals on the augmented ordinary random-slope diagonal (Psi) companion, `theta_diag_B_slope` only | `partial` | `test-slope-sd-ci.R` | 2026-08-18 (Slice 1, design: `dev/fable-extractor-recommendation.md`). `theta_diag_B_slope` is a genuine univariate log-SD (`src/gllvmTMB.cpp:1606`), so `sd = exp(theta)`, `se(sd) = exp(theta) * se(theta)` is an exact transformed Wald interval -- no Jacobian. Test evidence: one recovery cell (`n_ind = 50`, `n_rep = 6`, 3 traits, `latent(0 + trait + (0 + trait):x \| unit, d = 2)`) with all 3 slope SDs finite and their CIs covering the true `psi_sd = 0.2`; plus deterministic mock-fit tests proving each kill-switch guard actually fires (non-PD Hessian, non-finite `se_theta` including `Inf`, `se_theta > 10` SE blowup, and a near-zero-relative-to-siblings collapse) rather than passing vacuously; plus the deferred-route refusals (see CI-15) and a `scale = "sd"`/`scale = "variance"` consistency check. `interval_status = "wald_uncalibrated"` on every row; `calibrated = FALSE` is hard-coded (never an argument). No repeated-sampling coverage campaign has been run (gated by CI-08/CI-10); `partial` reflects single-seed recovery evidence only. **2026-08-18 correction (adversarial review pass, `dev/S6-slope-sd-ci-review.md`):** when the fit's random-slope term also carries a shared loadings component (`theta_rr_B_slope`, the default alongside `theta_diag_B_slope`), the returned `estimate` is the per-trait UNIQUE (Psi) component of slope variance only, not the total marginal `Var(Lambda_B_slope z + psi)` -- measured to understate the total by 17-45% on the recovery fixture above. The review found a print-method-only caveat did not survive `$estimate`, `subset()`, or column selection, so this is now an IN-BAND signal: a `component` column (`"unique_psi"` / `"total"`) and a `total_sd` point-estimate column (from the already-`REPORT()`ed `fit$report$Sigma_B_slope`; no interval -- that needs the same multivariate delta method as the CI-15 deferred routes), plus a `cli::cli_warn()` fired on the call itself, not only in `print()`. The review also found the original `se_theta > 10` guard did not detect a genuine near-zero collapse at ordinary SEs (e.g. `theta = -20, se = 0.5` passed as `"ok"`); a second, independent `near_zero_relative` check (relative to this fit's other slope SDs, mirroring `psi_rel_thresh` / `near_zero_psi_*` in `R/diagnose.R`) now catches that case, and the SE-explosion status was renamed `se_blowup` to describe what it actually tests. **2026-08-19 update (Slice 2):** when both `theta_diag_B_slope` and `theta_rr_B_slope` are present, `total_sd` -- previously a point estimate only -- now also carries a genuine interval (`total_lower`/`total_upper`, `total_status`) via the `sd_B_slope_total` `ADREPORT()` described in CI-15 below, which differentiates through both parameter blocks jointly (not an R-side sum of two separately-computed SEs, which could not capture their Hessian correlation). `estimate` for this route is unchanged from Slice 1 (still the unique/Psi component only); the underlying `theta`/`se_theta` computation for `estimate`/`lower`/`upper` is byte-identical to Slice 1. **CI-14 calibration addendum (2026-08-25):** Existing `slope_sd_ci()` route/recovery evidence remains `partial`; the exact 10,000-attempt calibration campaign is `blocked` because every attempt stopped at the frozen-source provenance guard and no scientific aggregate exists. |
| CI-15 | Marginal slope-SD intervals for the phylogenetic Cholesky augmented-slope route (`phylo_dep()` / `phylo_indep(1 + x \| species)`, `theta_dep_chol`) and the loadings-only augmented random-slope route (`theta_rr_B_slope` with no diagonal companion) | `partial` (moved off `blocked`, 2026-08-19, Slice 2) | `test-slope-sd-ci.R` | 2026-08-18 Slice 1 deliberately refused both routes with a typed error (`gllvmTMB_slope_sd_ci_unsupported_route`) rather than a hand-indexed approximation, for the reason still recorded here: `theta_dep_chol`'s slope diagonal shares its 2x2+ Cholesky block with free within-trait off-diagonal entries, so `Var(slope_t) = L21_t^2 + exp(diag_slope_t)^2` (or the general `(1+s)T x (1+s)T` case) needs a MULTIVARIATE delta method, and `theta_rr_B_slope` alone has no per-element back-transform at all -- the marginal slope variance from loadings is a quadratic form in multiple loading entries. A first hand-indexed attempt at the phylo route got this wrong (indexed entries 2/5/8 instead of the correct 2/4/6, and exponentiated a raw off-diagonal entry as if it were a log-SD; `dev/slope-interval-feasibility-RESULTS.md`). **2026-08-19 (Slice 2) unblocks both routes via `ADREPORT()` in `src/gllvmTMB.cpp`** rather than an R-side numerical Jacobian: `sd_b` (already `REPORT()`ed in the `theta_dep_chol` branch) is now also `ADREPORT()`ed, so `sdreport()` runs the delta method against the TMB-authoritative packing directly; `sd_rr_B_slope` (`= sqrt(diag(Sigma_B_slope))`) and the combined `sd_B_slope_total` (`= sqrt(diag(Sigma_B_slope) + diag(Sigma_B_unique_slope))`, used by CI-14's `total_sd` interval) are added the same way. `R/slope-sd-ci.R` reads these by NAME from `summary(fit$sd_report, "report")` (`rownames(...) == name`), never by position. Test evidence: for the phylo route, one recovery cell (`n_sp = 70`, 2 traits, `phylo_dep(0 + trait + (0 + trait):x \| species)`, heavy-gated `GLLVMTMB_HEAVY_TESTS=1` following house convention for phylo_dep model fits) with both slope-SD CIs covering their true values; for the loadings-only route, one fitted cell (`latent(..., d = 2, unique = FALSE)`, not heavy-gated) with the reported intervals `ok` -- NOT a recovery cell: this route's fit is misspecified against its own fixture (a diagonal Psi companion was simulated but is absent from the `unique = FALSE` fit), so there is no known truth to recover against, only a plausible-order-of-magnitude finite estimate; both routes carry the four kill-switch guard tests (non-PD Hessian, non-finite SE, SE blow-up, near-zero-relative collapse) via hand-built `sd_report` mocks that dispatch through the real `TMB:::summary.sdreport` method. **The phylo route's ground-truth recovery assertion is what actually guards the C++ packing** (`ci$lower <= slope_sd_true & slope_sd_true <= ci$upper`, with `slope_sd_true` derived from the simulated `L` matrix wholly outside `slope_sd_ci()`'s own code path) -- a wrong position (e.g. the 2/5/8-vs-2/4/6 hazard) would show up here as a failed-to-cover interval. Both real-fit tests also carry a second, narrower check: the ADREPORTed point estimate, as read by `slope_sd_ci()`, is asserted equal (tolerance `1e-6`) to `sqrt(diag(...))` computed directly from the already-`REPORT()`ed `Sigma_b_dep` / `Sigma_B_slope`. This is NOT an independent construction -- `sd_b(j)` / `sd_rr_B_slope(j)` and `Sigma_b_dep(j,j)` / `Sigma_B_slope(j,j)` are the SAME C++ quantity -- so it guards R-side position selection (did `slope_sd_ci()` read the right ADREPORT rows?), not the C++ packing itself. `partial`, not `covered`: single-seed recovery evidence only, `interval_status = "wald_uncalibrated"` on every row, no repeated-sampling coverage campaign (gated by CI-08/CI-10, same as CI-14). Cost/compatibility check performed: an alternating-build A/B timing comparison (`TMB::sdreport()` on a representative fit, 4 rounds each) found no measurable slowdown from the added `ADREPORT()`s (both builds ~0.06-0.11s; the added vectors are length `O(n_traits)`, not `O(n_obs)`), and a repo-wide grep of every `R/` consumer of `fit$sd_report` found none that index the `"report"` (ADREPORT) component by position -- all filter by `rownames(...) == name`, so the added rows cannot silently misalign an existing extractor. **CI-15 calibration addendum (2026-08-25):** Existing ADREPORT route/recovery evidence remains `partial`; both exact calibration routes are `blocked` because the approved sequence stopped after CI-14 and no CI-15 campaign root or scientific attempt exists. |

### Section 11 — Lambda constraint (M2 binary IRT)

Row-owner: **Boole + Fisher** (lambda machinery is central to
M2 binary).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| LAM-01 | `lambda_constraint` argument accepted | `covered` | `test-lambda-constraint.R` | |
| LAM-02 | `lambda_constraint` Gaussian fits | `covered` | `test-lambda-constraint.R` | Gaussian confirmatory-loading fits pin requested entries exactly: diagonal pins, off-diagonal zero and non-zero pins, ignored upper-triangle pins, dimension-mismatch errors, W-level pins, and simultaneous B + W constraints. |
| LAM-03 | `lambda_constraint` on binary fits (confirmatory IRT) | `covered` | `test-m2-3-lambda-constraint-binary.R`, `test-m2-3-mirt-cross-check.R`, `test-m2-3-galamm-cross-check.R`, `test-lambda-constraint.R`, `test-loading-ci-bootstrap.R` | M2.3 walks: binary 2PL IRT recovery at d ∈ {1, 2} × n_items ∈ {20, 50} + mirt + galamm cross-checks. 2026-07-04 hardens the confirmatory loading bootstrap pathology screen with an absolute scale floor so weak-loading fits do not get a microscopic percentile-tail cutoff. |
| LAM-04 | `suggest_lambda_constraint()` / `suggest_lambda_constraints()` | `covered` | `test-m2-4-suggest-lambda-constraint-binary.R`, `test-suggest-lambda-constraint.R` | M2.4 walks: suggester output structure + suggester→fit recovery cycle on binary IRT at d ∈ {1, 2, 3}; d=3 n_items=10 boundary documented. The plural helper is a comparison wrapper around the same threshold / Wald / profile suggester surface. |

### Section 12 — Miscellaneous public surface

Row-owner: **Emmy** (S3 surface) / **Curie** (test integration).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| MIS-01 | `gllvmTMB()` long-format constructor | `covered` | many tests | M0 baseline |
| MIS-02 | `gllvmTMB(traits(...) ~ ...)` wide format | `covered` | `test-traits-keyword.R`, `test-wide-weights-matrix.R` | |
| MIS-03 | `gllvmTMB_wide(Y, ...)` legacy constructor | `partial` | `test-gllvmTMB-wide.R`, `test-wide-weights-matrix.R`, `test-missing-data-robustfix.R` | soft-deprecated in 0.2.0; retained for migration and matrix-first workflows. 2026-07-04 closes issue #589 for the retained-response path: `gllvmTMB_wide(..., missing = miss_control(response = "include"), weights = W)` now validates NA-aligned per-cell weights and sends finite sentinel weights for masked cells. |
| MIS-04 | Weight column unified handling | `covered` | `test-weights-unified.R`, `test-lme4-style-weights.R`, `test-missing-data-robustfix.R` | 2026-07-04 adds the internal `drop_masked = FALSE` contract for wide matrix weights so retained missing-response cells keep row identity while masked weights remain finite inside TMB. |
| MIS-05 | `simulate.gllvmTMB_multi()` family-aware draws (per-row family dispatch) | `covered` | `test-m1-8-bootstrap-mixed-family.R`, `test-simulate-site-trait.R` | M1.8 (PR #157) — `.draw_y_per_family()` dispatches by `family_id_vec`; 6 families (gaussian / binomial / poisson / lognormal / Gamma / nbinom2) covered; others fall back with one-time warning |
| MIS-06 | `tidy.gllvmTMB_multi()` broom-style output | `covered` | `test-tidy-predict.R` | |
| MIS-07 | `predict.gllvmTMB_multi()` link / response | `covered` | `test-tidy-predict.R`, `test-missing-data-robustfix.R`, `test-integration-tour.R` | Training-row predictions return one row per observed response on the link scale, and `type = "response"` dispatches through each row's trait/family inverse link for mixed Gaussian / binomial / Poisson fits. `newdata` response predictions map rows back to the training trait's link before transformation; 2026-07-04 (issue #645) restores training factor levels and aligns model-matrix columns by stored `X_fix_names`, failing loudly on unseen fixed-effect factor levels while unseen unit/species grouping levels remain allowed for population-mean rows; random-effect handling remains controlled by `re_form`, with known-level random effects added only when requested / available. Point-prediction only: `newdata` simulation, prediction intervals, and broader conditional/random-effect prediction contracts remain gated. |
| MIS-08 | `print.gllvmTMB_multi()` summary label discipline | `covered` | `test-print-labels.R` | |
| MIS-09 | `plot.gllvmTMB_multi()` dispatcher | `partial` | `test-plot-gllvmTMB.R`, `test-plot-visual-snapshots.R` | Seven dispatcher types are object-shape tested (`correlation`, `correlation_ellipse`, `loadings`, `integration`, `communality`, `variance`, `ordination`), including bootstrap interval overlays and rotated ordination metadata. Visual snapshots now cover Confidence Eye correlation plots, Sigma-table Confidence Eye plots, and anchored rotated ordination; still partial until broader rendered-figure QA covers the full dispatcher surface. |
| MIS-10 | brms-style sugar | `covered` | `test-brms-sugar.R` | |
| MIS-11 | `traits(...)` LHS expansion | `covered` | `test-traits-keyword.R` | |
| MIS-12 | `gllvmTMBcontrol()` control object | `covered` | `test-gllvmTMBcontrol.R` | |
| MIS-13 | Integration tour (end-to-end) | `covered` | `test-integration-tour.R` | M0 baseline |
| MIS-14 | `gllvmTMB-args.R` argument validation | `covered` | `test-gllvmTMB-args.R` | |
| MIS-15 | `profile_targets()` controlled vocabulary (PR #109) | `covered` | `test-profile-targets.R` | drmTMB-style |
| MIS-16 | `init_strategy = "single_trait_warmup"` (M3.4 Mitigation A) | `covered` | `test-m3-4-warmstart-phi-clamp.R` | Design 48 §2-A. Opt-in via `gllvmTMBcontrol(init_strategy = "single_trait_warmup")`. Fits an intercept-only univariate GLM per trait (with that trait's family) and seeds `log_phi_*` entries before MakeADFun. Phi-bearing families covered: nbinom1, nbinom2, beta, betabinomial, truncated_nbinom2, gamma_delta (intercept-only seed only; per-trait `b_fix` warmup deferred). |
| MIS-17 | Phi starting-value clamp `[0.01, 100]` (M3.4 Mitigation B) | `covered` | `test-m3-4-warmstart-phi-clamp.R` | Design 48 §2-B. Applied to all `log_phi_*` entries at init regardless of `init_strategy`. Defensive — clamps both default zero inits (no-op) and warm-started values (pulls pathological theta from glm.nb iteration-limit cases back into a safe range). |
| MIS-18 | `start_method = list(method = "res")` residual reduced-rank starts | `covered` | `test-start-method-residual.R`, `test-gllvmTMBcontrol.R` | Design 48 §2-A2. Opt-in via `gllvmTMBcontrol(start_method = list(method = "res", jitter.sd = 0.2))`. Seeds `theta_rr_*` and latent scores from grouped fixed-effect residual matrices, rotates loadings to the engine's lower-triangular convention, and seeds diagonal Psi residual terms when present. Contract-tested; convergence-rate claims remain M3 production-grid evidence, not unit-test evidence. **2026-07-31 scale fix:** the fallback loading start (`.gllvmTMB_default_rr_lambda`) hardcoded `0.5` with no reference to the response scale, while the companion Psi start in the same function was already data derived (`sd_rem`). That mixture became visible *inside a single Lambda* whenever the SVD recovered fewer directions than `d` requested: only columns `1:r_eff` are overwritten, the rest kept the `0.5`, and `theta_rr` is then packed from the whole matrix -- overriding the caller's `default_theta` with `usable = TRUE`, so the fit received it. On a response scaled by 1e4 this packed a literal `0.5` beside siblings of 7726 and 3863. The fallback now carries the residual scale, so every entry of the returned `theta_rr` scales with the response; guarded by the rank-deficient regression cell in `test-start-method-residual.R`, verified to fail on the unfixed code with ratios `10000, 1, 10000, 10000`. Note this is the `res` path's OWN yardstick and is applied for every family, because this function's Psi is data derived unconditionally -- unlike the engine default, where #851 scopes the scale to gaussian (MIS-35). |
| MIS-19 | `start_method = list(method = "indep")` and manual `start_from` simpler-fit starts | `covered` | `test-start-method-residual.R`, `test-gllvmTMBcontrol.R` | Design 48 §2-A3. Opt-in GLMM/GLLVM warm start for Gaussian two-level ordinary latent covariance fits: fit the matching independent diagonal model or a user-supplied simpler fit, then copy same-shaped estimated TMB parameters into the full model's starting list. Contract-tested; default-policy and convergence-rate claims require M3 target-explicit evidence. |
| MIS-20 | `restart_history` and `start_provenance` on fitted objects | `covered` | `test-stage39-multi-start.R`, `test-sanity-multi.R`, `test-multi-start-sdreport-consistency.R` | Design 49. Every fit records one row per attempted optimizer start, the selected restart, optimizer/start method, jitter scale, objective, convergence code, message, elapsed time, and start provenance. The 2026-07-04 restart-selection guard closes issues #684/#685 for the local loop contract: non-finite restart objectives are recorded as unsuccessful and cannot be selected as `best_opt`, so the returned optimizer result and `restart_history$selected` stay aligned. This is provenance only; it does not itself validate a start strategy's convergence-rate benefit. |
| MIS-21 | Missing response cells in long and wide data | `covered` | `test-missing-response.R`, `test-traits-keyword.R`, `test-wide-weights-matrix.R`, `test-missing-data-robustfix.R` | Response-missing rows/cells are handled as unobserved unit-trait cells; other observed traits for the same unit remain in the likelihood. 2026-07-04 covers the `gllvmTMB_wide()` per-cell weight-matrix interaction under `response = "include"`; NA weights must align with NA response cells and are replaced by finite sentinels only for masked rows. Ordinary predictor/design missingness still errors unless the predictor is explicitly modelled through a supported `mi()` slice. **2026-08-15: per-family evidence now spans ALL 17 admitted families under Laplace** — `test-missing-response-nongaussian.R` Tier-3b adds lognormal, Gamma, nbinom1, tweedie, Beta, student, truncated_poisson, truncated_nbinom2, delta_lognormal, delta_gamma, ordinal_probit, and betabinomial (cbind); `test-multinomial-missing-response.R` admits and pins NA categorical responses (group-uniform contrast masking; the former R-side refusal is lifted, the C++ anchor gate was already mask-ready). Measured invariant across the new families: abs dlogLik(include, drop) <= 2.5e-6. |
| MIS-22 | Morphometrics cached bootstrap correlation fixture | `covered` | `test-example-morphometrics.R` | Ships `inst/extdata/examples/morphometrics-bootstrap-r.rds` for article rendering and visual QA of the confidence-eye correlation display and `plot(type = "correlation_ellipse", boot = boot)`; the fixture is not interval-calibration evidence. |
| MIS-23 | `miss_control()` missing-data policy surface | `covered` | `test-miss-control.R`, `test-missing-data-robustness.R` | Default `response = "drop"`, `predictor = "fail"`, `engine = "laplace"` is the no-op complete-case surface; `response = "include"` and `predictor = "model"` are accepted; `engine = "em"` / `"profile"` and `estimator =` fail loudly as reserved / unsupported. |
| MIS-24 | `predict_missing()` response-mask extractor | `covered` | `test-missing-response-gaussian.R`, `test-missing-response-traits.R`, `test-missing-data-robustfix.R` | Returns fitted point predictions for masked missing response cells with original-row / cell accounting. It is response-only; modelled missing predictors use `imputed()`. |
| MIS-25 | `imputed()` extractor for modelled missing predictors | `covered` | `test-missing-predictor-gaussian.R`, `test-missing-predictor-binary.R`, `test-missing-predictor-ordered.R`, `test-missing-predictor-categorical.R`, `test-missing-predictor-phylo.R` | Returns one row per missing latent-bearing unit / group. Gaussian routes report conditional modes (EBLUPs) and Hessian SEs when available; discrete routes report fitted conditional probability / expected score / modal category with `std_error = NA` and `uncertainty_status = "discrete_no_se"`. |
| MIS-26 | One Gaussian fixed-effect missing predictor via `mi(x)` | `covered` | `test-missing-predictor-gaussian.R`, `test-missing-data-robustness.R`, `test-missing-data-robustfix.R` | Phase 2a / Design 67. One unit-level numeric predictor declared with `mi(x)`, `missing = miss_control(predictor = "model")`, and `impute = list(x = x ~ z)` or `impute_model(x ~ z, family = gaussian())`; missing predictor rows are retained and the latent `x_mis` is integrated by Laplace. |
| MIS-27 | One grouped Gaussian missing predictor via `mi(x)` | `covered` | `test-missing-predictor-gaussian.R`, `test-missing-data-robustness.R` | Phase 2b / Design 67. Supports one grouped random intercept in the Gaussian predictor model, e.g. `impute = list(x = x ~ z + (1 | group))`; random slopes, more than one grouping term, and missing grouping values fail loudly. 2026-08-01 adds the shared-group independence pin: response `(1 \| grp)` + covariate `(1 \| grp)` keeps the response `b_x` unbiased (issue #336 gate 2). |
| MIS-28 | One phylogenetic Gaussian missing predictor and `phylo_signal_mi()` | `covered` | `test-missing-predictor-phylo.R` | Phase 3 / Design 69. Supports one species-level Gaussian missing predictor with `phylo(1 | species, tree = tree)` in the covariate model; the phylogenetic field is independent of the response field, and `phylo_signal_mi()` flags weak borrowing. Joint response-covariate fields remain deferred. |
| MIS-29 | One binary missing predictor via exact two-state summation | `covered` | `test-missing-predictor-binary.R`, `test-missing-predictor-ordered.R`, `test-missing-predictor-categorical.R` | Phase 5a / Design 68. Supports one two-level predictor with fixed-effect Bernoulli-logit `impute_model(x ~ z, family = binomial())`; no latent `x_mis`; grouped / structured binary predictor models and non-logit binary links fail loudly. |
| MIS-30 | One ordered missing predictor via exact K-state summation | `covered` | `test-missing-predictor-ordered.R`, `test-missing-predictor-categorical.R` | Phase 5b / Design 68. Supports one ordered factor or integer-score predictor with fixed-effect cumulative-logit `impute_model(..., family = cumulative_logit())`; reports conditional expected score and per-level probabilities. Grouped / structured ordered predictor models fail loudly. |
| MIS-31 | One unordered categorical missing predictor via exact K-state softmax summation | `covered` | `test-missing-predictor-categorical.R` | Phase 5c / Design 68. Supports one unordered factor / character / integer-score predictor with fixed-effect baseline-softmax `impute_model(..., family = categorical())`; reports modal category and per-level probabilities; likelihood is invariant to baseline-level choice. |
| MIS-32 | Deferred missing-data extensions beyond the shipped v1 layer | `blocked` | n/a | Multiple simultaneous `mi()` terms; EM/profile/REML missing-data engines; simulated imputations; MI pooling; structured discrete predictor models; spatial / animal / relatedness covariate models; joint response-covariate fields; bounded / count / lognormal / Gamma missing-predictor families; dense known sampling-covariance matrices with partial multivariate response rows; MNAR sensitivity and bootstrap-SE cross-checks. |
| MIS-33 | Gaussian-only REML via `gllvmTMB(REML = TRUE)` | `covered` | `test-gaussian-reml.R`; `test-lv-reml-gaussian.R`; `test-lv-reml-boundary-guard.R`; `test-xcoef-fixed.R` | Gaussian linear random-intercept and ordinary `indep()` / `dep()` / `latent() + Psi` fits integrate `b_fix` through TMB's exact Gaussian Laplace block. A dense Patterson--Thompson oracle agrees at fitted and perturbed covariance parameters; matched random-intercept and rank-1 latent fixtures also agree with `glmmTMB(..., REML = TRUE)` likelihood and df metadata. Admitted contract: all-Gaussian, unweighted rows; dropped rather than retained missing responses; full-rank observed `X` with positive residual degrees of freedom; no `mi()`, predictor-informed `latent(..., lv = ~ x)`, or `Xcoef_fixed`. Source/tier/slope covariance forms remain engine-admitted but are not evidence-certified until named oracle and recovery cells land. Fixed-effect profile CIs remain ML-only. |
| MIS-34 | ML structural-zero fixed-effect coefficients via `Xcoef_fixed` | `covered` (native ML zero constraints; admitted Julia fixed-effect-X zero masks) / `partial` (REML, non-zero fixed values, unsupported Julia X rows) | `test-xcoef-fixed.R`; `test-julia-bridge.R`; paired GLLVM.jl PR #114 | `gllvmTMB(..., Xcoef_fixed = c("expanded_column" = 0))` pins selected expanded fixed-effect coefficients exactly at zero after normal `model.matrix()` expansion. This is intended for trait-specific mean-structure control: a predictor can be estimated for responses where it is meaningful and fixed to zero for responses where it is not. Native TMB fits use a parameter `map`, report all coefficient rows with pinned rows marked `status = "fixed"`, set fixed-row SE/CIs to `NA`, and reduce `logLik()` df by the number of fixed coefficients. The Julia bridge now translates the same named R-side vector into GLLVM.jl's positional zero mask for admitted complete fixed-effect-X rows and preserves returned `mean_coef_status` / `gamma_status` fields. Tests cover exact zero pinning, one-parameter df reduction, all-zero covariate-block equivalence to omitting that block, unknown-name rejection, non-zero fixed-value rejection, REML rejection, pure-R R-to-Julia mask routing, and a live JuliaCall/GLLVM.jl smoke when configured. This is not a formula-grammar change, not variable selection, not a response mask, and not a `lambda_constraint`; non-zero fixed values, REML, Julia per-trait intercept pinning, mixed-family-X, masks+X, NB1-X, ordinal-X, and unsupported Julia fixed-effect-X families remain gated. |
| MIS-35 | Scale-aware DEFAULT starting values for the ordinary unit-tier `latent()` structure (issue #851) | `covered` (single-tier ordinary `latent()`, gaussian) / `blocked` (every other tier, deliberately) | `test-scale-equivariance.R`; reproducers `dev/scale-equivariance-check.R`, `dev/851-scale-equivariance-comparators.R` | The loading start, its companion Psi start, and the latent-score start now follow the working residual scale instead of assuming `sd(y) ~ 1`. Advertised in `NEWS.md` with a comparative claim, hence this row. **Evidence:** `test-scale-equivariance.R` asserts the exact gaussian laws (`Lambda -> k*Lambda`, `Sigma -> k^2*Sigma`, correlations invariant) at k = 100 and k = 5000 within the 2% acceptance criterion, on the same n = 150 / p = 4 single-tier fixture the cross-package study used. Bite verified: on `origin/main` the k = 5000 block fails with Lambda rel.err 0.99 (the loadings do not move at all), while k = 100 passes on both -- so k = 100 alone would have guarded nothing. The 8-seed cross-package comparison (gllvmTMB 0/16 violations, worst 0.0105; gllvm 3/16, worst 0.998; glmmTMB 14/16, worst 2.00) remains **dev-script evidence, not suite-enforced**. **Scope, deliberately narrow:** the scale is applied only when every trait is **gaussian** (`family_id == 0`), and only at `theta_rr_B`. The within-unit (W), slope, kernel, phylogenetic and SPDE tiers keep the historical `0.5` start, and latent scores are not seeded when `latent(lv = ~)` gives them a modelled mean -- each is unmeasured at scale rather than verified, and `NEWS.md` says so. Non-gaussian responses are excluded on the same rule that excludes the other tiers -- 'multiply y by k' is only meaningful for an unbounded continuous response, so the collapse is unmeasured elsewhere; keying off the working residual anyway moved a binomial AGHQ cell for no evidenced reason (379.7134 -> 380.5439), and gating restores it exactly. A nested two-tier fit retains a ~1-2% residual at k = 5000; that is a likelihood-surface property tracked as its own issue, not a starting-value defect. |

| MIS-36 | Experimental scale-aware AGHQ ridge (`aghq_ridge = "auto"`, issue #847) | `opt-in` (pure single-trial Bernoulli, one ordinary unit-tier `latent()` block) / `partial` (runaway/failure avoidance only) | `test-aghq-auto-ridge.R`; `test-aghq-auto-psi-equivalence.R`; `dev/aghq-evidence/28-tau-rescore.R`; `dev/aghq-evidence/29-tau-cap-totoro.R`; `dev/aghq-evidence/30-tau-cap-analyse.R`; `dev/aghq-evidence/31-tau-transparent-fallback.R`; `docs/dev-log/artifacts/aghq-tau-847/`; `docs/dev-log/2026-08-01-scale-aware-tau-ultra-plan.md` | The explicit `"auto"` route uses an unpenalised 9-node multi-start AGHQ pilot and `tau_used = min(6, max(1, ||Lambda_pilot||_F / sqrt(pq)))`. A plain Laplace fit is never a pilot. If the pilot or scale-aware final fit is unusable, the returned fit is an independently started shipped-style `tau = 2` fit, and `fit$aghq$ridge_auto` records the pilot, raw/used scale, cap, selected route, and fallback reason. The 600-replicate Totoro selection grid did **not** pass the preregistered default-change gate: valid pilots were sparse (162/600; 1/100 to 79/100 by cell), and the strict pilot-dependent policy was materially worse than fixed `tau = 2` at the first gate. A separately labelled transparent-fallback sensitivity used the auto result in 135/600 replicates; no cell had a higher failure or runaway rate, every cell's loading-error upper bound stayed within the preregistered +0.02 non-inferiority margin, and the six-cell macro-mean loading-error difference was +0.002819895, so no broad accuracy claim is made. Fresh valid pilots never clipped at caps 5, 6, or 8; the stored wider-range rescore made cap 6 the smallest candidate with negligible clipping (1.1% in the hardest stored n=100, sigma_lambda=3 cell, versus 6.7% at cap 5). Numeric and `Inf` ridge controls retain their established path and the package default remains `tau = 2`. The measured grid used logit, `p = 6`, `q = 2`, and `n = 100`, `400`, or `1600`; other links and dimensions are extrapolations. Other families, structured/source/W/slope tiers, likelihood comparison, interval calibration, and broad loading accuracy remain outside the admitted claim. |
| MIS-37 | `predict_missing()` masked-cell reconstruction ACCURACY | `partial` | `dev/missing-accuracy-dgp.R`; `dev/missing-accuracy-arc0-recovery.R`; `dev/missing-accuracy/arc0-cells.csv`; `dev/missing-accuracy/RESULTS.md` | First accuracy evidence (2026-08-15; Design 70 §E.2 target S1, previously "no code, no runs"): gaussian + poisson × {MCAR 5%, MCAR 20%, trait-clustered, unit-clustered} × 10 seeds = 80/80 converged local fits. Reconstructions beat the trait-mean-fill baseline in every cell (RMSE ratio 0.44–0.57); accuracy holds under the structured-MAR clustered mechanisms (gaussian r 0.66–0.74, poisson Spearman 0.60–0.68). `partial` by register rule: dev-script evidence, not testthat assertions. **Wave-1 coverage (2026-08-15, Totoro, 1,600 fits, 100% conv): NOT calibrated by the pre-registered gate — confidence intervals over-cover (0.960–0.966 at 95%, 0.930–0.939 at 90%); prediction intervals near-nominal at 95% (0.948–0.953). se= stays heuristic_unvalidated. Wave-1b (R1-joint, exact joint-precision variance) UNDER-covers (conf 0.925–0.933 at 95%; prediction 3/4 mechanisms pass at 90%) — the two routes BRACKET nominal, and the diagnosed gap is the omitted loading block d(eta)/d(lambda)=u. Wave-1c (R1-joint+loadings, all three gradient blocks) is the best route measured and still fails: conf 0.935-0.939 at 95% (deficit halved). Error-to-SE ratios quad 0.82 / joint 1.03 / joint_load 1.10. No gradient block remains missing, so the residual is a delta-method limit (plug-in conditional variance, or normal quantiles); the five-route ladder is now CLOSED: wave-3 parametric bootstrap (B=200, full refits, 400 reps/cell) scores 0/16 (conf 0.926-0.931 at 95%). Four of five routes under-cover by 1-2 points including the bootstrap, so the deficit is the fitted-model plug-in at n=50 (ML variance/loading bias), not any interval construction. se= remains heuristic_unvalidated with no interval claim; remaining options (REML-corrected refits / double bootstrap / document ~0.93 honestly) are maintainer decisions per Design 119 §7e. PROGRAMME CLOSED — wave-4 (2026-08-16, `boot_dgp = "reml"`, same grid and seeds, 1,600/1,600 conv, Design 119 §7f): the pre-registered narrows-but-fails branch fired. The REML-corrected bootstrap DGP improves coverage in 16 of 16 cells (uniform sign, mean +0.36 points, conf 0.929-0.933 at 95%) but scores 0/16 against the ±2×MCSE gate, recovering ~18% of the deficit — so plug-in underdispersion is a real but minor part of the cause. FINAL LABEL, applied under a rule pre-registered before the data existed: no route is `calibrated`, `se =` stays `heuristic_unvalidated` at this scale, and the best-measured route is `sim` (0.941-0.946 at nominal 0.95) at ~1/300 of the bootstrap's cost. The measured coverage is documented rather than hidden; no further waves.** **MECHANISM IDENTIFIED (2026-08-16, waves 5-6, Design 119 §8), and it CORRECTS §7f's attribution. §7f called the residual "a small-sample property of the fitted model at n = 50 × p = 25" — naming two quantities and testing neither. Swept separately they give a DOUBLE DISSOCIATION: over a 32× range in n (50→1600, p = 25 fixed) the confidence deficit is FLAT (0.61 → 0.51 pt) while the prediction deficit halves (1.13 → 0.56) and pred-90 crosses nominal; over a 10× range in p (10→100, n = 200 fixed) the confidence deficit falls 78% (1.32 → 0.29 pt, monotone; conf-90 reaches −0.04) while the prediction deficit stays flat. Because a masked cell's `eta = x'b + lambda_t' u_i` leans on the unit score `u_i`, which is reconstructed from that unit's OTHER OBSERVED TRAITS, its information is O(p) and not O(n): sample size cannot fix it, trait count can. The residual is therefore an information limit rather than a variance-route defect — that half of §7f stands and justifies abandoning the sixth route — but it is NOT a small-n effect. The user-facing rule is about data SHAPE, not size: near-nominal when each unit carries many measured traits, ~1.3 pt optimistic at ~10 traits. Structurally corroborated by the AGHQ lane's independent 2026-07-28 finding that Laplace's bias is O(1/T) per CLUSTER and "16× more data does not touch it". Validity: wave-5's n = 50 grid reproduces wave-2 bit-for-bit (max |diff| = 0), and the shared (n = 200, p = 25) cell agrees across both sweeps to 0. NO STATUS CHANGE — `se =` stays `heuristic_unvalidated`; this is mechanism, not calibration.** Point predictions only — `predict_missing()` returns no SEs/intervals, so no interval or coverage claim exists; clustered arms are structured MAR, never an MNAR claim. **Arc0b (same date; `dev/missing-accuracy-arc0b-recovery.R`, `arc0b-cells.csv`) extends spot recovery to binomial, ordinal_probit, delta_lognormal, multinomial (80/80 fits): reconstruction skill is FAMILY-DEPENDENT — multinomial clearly beats its marginal-frequency baseline (modal accuracy 0.61 vs 0.51, Brier 0.50 vs 0.62), while binomial/ordinal/delta sit near their naive baselines at these scales (one masked Bernoulli/ordinal/hurdle cell carries little recoverable information). Do not advertise categorical/binary masked-cell reconstruction as accurate on this evidence.** **Design 119 Slice 1 (2026-08-15) adds an internal-only `predict_missing(se = TRUE)` for gaussian fits (`se_confidence`/`se_prediction`, R1-quad route, `R/methods-gllvmTMB.R:.gllvmTMB_predict_missing_se()`): `heuristic_unvalidated`, no coverage campaign has run, and no public interval claim exists.** |
| MIS-38 | Explicitly unused `unit_obs` / `cluster` grouping slots now warn (#1190) | `covered` | `tests/testthat/test-unused-grouping-slots.R` | Added for NEWS 0.7.1. Supplying `unit_obs` or `cluster` is useful only when a covariance keyword consumes that column; if no keyword does, `gllvmTMB()` now warns and names the omit-or-consume remedy instead of silently accepting an unused argument. Omitted and explicit-`NULL` slots stay silent, and the warning changes no likelihood, parameterisation, or fit. Filed after a real external user (`iwogross`, issue #1189) manufactured a meaningless grouping column to satisfy what he read as a required argument. |

| ISDM-01 | Integrated two-source model admitted through public `gllvmTMB()` (issues #945, #946) | `partial` (the route is reachable, documented, and contract-tested; recovery at the advertised scope is NOT certified) | `test-isdm-spatial-private-contract.R`; `test-isdm-developer-fit.R`; `test-isdm-contract.R`; `test-family-within-trait-boundary.R`; `test-offset-guard.R`; `vignettes/articles/integrated-two-source-example.Rmd` (renders through the public route); `vignettes/articles/integrated-survey-design.Rmd` (the reader-facing distillation of the domain-growth campaign); `docs/design/111-isdm-nonspatial-recovery-protocol.md`; `dev/isdm-package-recovery/2026-08-15-domain-growth-results.md` | Per-row family **within** one trait is admitted for exactly one contract: `family = list(gbif = poisson(), survey_pa = binomial("cloglog"))` with `attr(family, "family_var") <- "isdm_family"`, a `source` column of `gbif`/`survey` with both present, and an `isdm_family` column aligned to it. Within that contract a nonzero `offset()` is admitted on the Bernoulli-cloglog arm as a known change-of-support term. Admission is **structural** — `.gllvmTMB_integrated_two_source_contract()` is the single predicate, and both the unexported developer route and a public caller pass through it; the namespace token is retained for the developer route only. Poisson and Bernoulli are dispersion-free, so #945 wrinkle 1 (ambiguous per-trait nuisance parameters) does not arise; dispersion-carrying pairs stay refused. **What is NOT established:** this row certifies reachability and refusal-narrowness, not estimator quality. Design 111's recovery protocol is **nonspatial-only and its gates are not cleared for the spatial arm**, so the `spatial_latent(1 + isdm_gbif | ...)` variant is admitted at runtime on frontier-campaign experience (~7,200 fits) rather than on a cleared recovery gate. Everything the route reports is **relative intensity**: per Fithian et al. (2015) presence-only data cannot identify absolute abundance, occupancy, or detectability, and none are estimated. Source-specific spatial structure is weakly identified on small designs — the 2026-08-15 domain-growth result places reliable portal-only *field* recovery well beyond the article's design size, with identifiability bought by adding cells at maintained per-cell records, not by piling records into the same cells. Interval calibration, multi-source (`n_sources > 2`; see ISDM-02), and the weighted-joint-likelihood channel (#944) are outside this row. |

| ISDM-02 | Multi-source integrated model (`n_sources >= 2`) via `isdm_sources()` (Design 120, issue #941) | `partial` (reachable, documented, contract-tested, with first-pass nonspatial recovery evidence; not a certified capability) | `test-isdm-multisource.R`; `test-isdm-source-formula.R`; `tests/testthat/test-isdm-requalification-prediction.R`; `docs/design/120-multi-source-isdm-contract.md`; `vignettes/articles/integrated-multi-source-example.Rmd`; `dev/isdm-multisource/prerun-gamma-recovery.R` (12/12 converged, 12/12 `pd_hessian` PASS); `dev/isdm-multisource/2026-08-16-campaign-results.md` + `campaign-gamma-recovery-results.csv` (the completed 1,200-fit Totoro grid, **split-metric v2** — conv 1199/1200, pd 95%; per-arm gamma RMSE flat in `n_sources`; note **only the 600 PA-mix fits exercise `isdm_sources()`** — the all-PO half runs the ordinary route by design, and the results doc carries a retraction of a v1 pooled-metric reading) | Any number of named sources, each declaring its observation law through the exported constructor `isdm_sources()`, admitted through `.gllvmTMB_integrated_sources_contract()` — **one predicate, of which the ISDM-01 two-source shape is the `n = 2` case** (byte-compatibility asserted by test: identical objective and parameters on the same data through both routes). A source can wrap an admissible law with `isdm_source(law, observation = ~ ...)`; the source-filtered columns are source-masked and deterministically reference-coded only when aliased with the ecological design, so normal formulas such as `~ observer + method` need no compensating `0 +`. Bare laws remain byte-compatible and duplicate or encoding-colliding source-observation effects are refused. The admitted law set at any `n` is exactly `{Poisson-log, Bernoulli-cloglog}`, because coherence is an arm-by-arm property against the shared intensity (Design 120 §2: the joint likelihood has no cross-arm term, so each arm's inverse link must individually express its mean as a function of `a·exp(eta)`); logit/probit and dispersion-carrying laws are refused at declaration. Admission requires a MIXED declaration (≥1 count and ≥1 detection arm); an all-count multi-source declaration needs no relaxation and deliberately keeps ordinary behaviour, including `weights`. Every trait must carry every declared source, and every source-by-trait arm must contain at least one observed response; an all-`NA` arm is refused before fitting for mixed-law and all-Poisson `isdm_sources()` declarations. `weights` and multi-trial rows stay refused inside the admitted mixed contract. **What is NOT established:** source-formula tests are a fit/design contract, not a recovery or identifiability campaign; recovery evidence is nonspatial, one design size (150 cells, 3 species), reference-coded `gamma[d,j]` only; the campaign's arms are fully-crossed and balanced, so the realistic hard case — arms with *partial, overlapping* coverage — is unmeasured, and its PA arm is saturated at the equal-effort cells (mean p ≈ 0.81), so no healthy high-information detection cell exists in the grid. An all-detection (all-PA) declaration is refused at construction — deferred scope, not established. A declared `n > 2` fit with `spatial_latent(1 + isdm_gbif | ...)` is runtime-admitted but **inherits ISDM-01's two-source frontier experience as its only spatial evidence** — nothing spatial has been run at `n > 2`. Intervals and broader per-source observation-effect recovery are outside this row. Relative intensity only, as ISDM-01. |
| ISDM-03 | `predict()` on integrated (`isdm_sources()` / two-source) fits | `partial` (public point-map route implemented for the ordinary intercept-only `use_spde` tier; held-out accuracy and map intervals remain uncertified) | `tests/testthat/test-isdm-predict.R`; `tests/testthat/test-isdm-requalification-prediction.R`; `dev/isdm-predict-probe/`; `dev/isdm-requalification/2026-08-29-training-identity-repair-receipt.md`; `docs/design/126-isdm-prediction-api.md`; `docs/design/127-isdm-prediction-map-implementation.md` | In-sample `est == report$eta`; training-row `type = "response"` dispatches each row's own Poisson-log or Bernoulli-cloglog inverse link; mixed-family output carries its source column; exact nonspatial `newdata` round-trip, `re_form = ~0`/`NA`/numeric `0`, unseen-unit fallback, and the classed `newdata + se.fit` refusal are pinned. For ordinary intercept-only SPDE fits, `fmesher::fm_basis()` re-adds the field and reproduces `report$eta` on training rows to numerical identity; new in-hull coordinates project, while out-of-hull rows warn. Response-free `newdata` is supported. Source-observation formulas are reconstructed from retained terms, factor levels, contrasts, and fitted columns; transformed terms, factors, aliased columns, neutral absent-source blocks, and typed missing/unknown plus active-source unseen-level failures are covered. Zeroing the offset in `newdata` gives effort-free relative intensity. **Still partial:** training-row identity proves reconstruction, not held-out accuracy. The preregistered two-/three-source full-/weak-overlap campaign ran at exact source `c5bb0b80` and failed its all-attempt availability gate; held-out point accuracy therefore remains uncertified. `use_spde_slope`, `use_spde_latent_slope`, and other unsupported tiers remain warnings/refusals; no exported grid helper is planned. Fixed `trait:env` Wald coverage passed 48/48 cells over 22,200 fits (coverage 0.939--0.959; PD-Hessian availability 0.858--0.947), but this narrow coefficient result does not certify a map interval. The fixed-effect-only map `se.fit` negative control covered true `eta` only 0.23--0.82, so `newdata + se.fit` remains refused and calibrated SPDE map uncertainty is owed. |
| ISDM-RESP-INFO | Internal paired baseline-versus-`rep3` response-information study | `blocked` for any recovery conclusion | `dev/isdm-requalification/response-information/{PLAN.md,RESULTS.md,contract.R,classify.R,summarise-independent.R}`; `tests/testthat/test-isdm-response-information-*.R`; Tamia raw archive manifest `6ac8b1aa246f913e2403d3373d6a115542ce4b03b026fd8204f6facb732112d8` | Fresh 800-fit nonspatial campaign with byte-identical baseline plus two conditionally independent streams, paired starts, hash-bound qualifications, and immutable worker receipts. All 800 fits returned once, but only 398/400 pairs satisfied the predeclared fit-health gate: two `rep3` fits in cell 7 had gradients 0.01036609 and 0.01108690 above the 0.01 threshold. Its frozen classifier is `EVIDENCE_INCOMPLETE`, never a scientific null. This is internal evidence only and adds no API, recovery, replication, interval, spatial, or empirical capability claim. The post-campaign forensic audit re-read all 800 immutable Tamia records and verified the focal hashes. The two failures rank 49/50 and 50/50 among cell-7 `rep3` gradients; matched baseline gradients are ordinary and the cell's baseline maximum is 0.00999407. Focal surface and covariance errors, runtime, and memory are not systematic outliers. This supports a narrow near-threshold numerical tail, not a recovery conclusion or component-level mechanism. A successor campaign is deferred until a non-retained engineering qualification records component-level gradients and optimizer termination information. | Fisher / Rose |

**ISDM-01/02 completeness addendum (2026-08-29).** The public legacy
two-source route and `isdm_sources()` now require at least one observed
response in every declared source-by-trait arm. Under
`miss_control(response = "include")`, an all-`NA` arm fails before fitting
with `gllvmTMB_isdm_observed_source_incomplete`; row presence alone is no
longer sufficient. This safety repair does not change either row's `partial`
evidence status.

**ISDM-01/02/03 point-requalification addendum (2026-08-29).** Exact-main
source `c5bb0b80` passed three-OS CI and was bound to the installed Totoro
package/DLL before 2,600 public-route attempts. All 1,800 nonspatial and 800
spatial attempts started and terminated; no attempt was replaced. The frozen
nonspatial recovery gate failed despite 100% convergence, finite objectives,
and PD Hessians: centered-surface correlation was 0.812 (weak overlap 0.757),
normalized RMSE was 0.585, and two weak/full source-coefficient RMSE ratios
exceeded 2. V3 corrected the lost `Psi` dimnames without changing a raw fit:
availability was 100%, species 1 median relative error was 0.390 and failed the
0.35 gate, while species 2 and 3 passed at 0.182 and 0.210. All 200
disconnected-support attacks completed, but their warnings were generic
lifecycle warnings rather than targeted disconnected-support diagnostics, so
this slice is `STRESS_ONLY` and its diagnostic gate failed 0/200.
The spatial gate also failed: 302/800 fits (37.75%) were eligible versus the
85% requirement. Eligible fits had strong held-out point accuracy (median
correlation 0.9956, normalized RMSE 0.1144) and passed every deterministic
prediction oracle, but available-only accuracy does not certify the route.
ISDM-01/02/03 therefore remain `partial`; SPDE marginal-map uncertainty and
its calibration campaign remain blocked. The retained receipt and checksum
provenance are in
`dev/isdm-requalification/2026-08-29-point-campaign-terminal-receipt.md`.

## Honest scope statement

**Current tally (2026-06-28) — recounted from the leading status
label in the per-row status column:**

- **213 capability rows** (the register grew from 102 rows at
  Phase 0A close as the kernel/coevolution, augmented-slope,
  cluster2, missing-data, plot/extractor, diagnostic, Julia bridge,
  and predictor-informed latent-score sections were added).
- **173 `covered`** (82 %): test evidence exists at the primary
  depth advertised by the row. Five of these rows still carry an
  explicit `partial` sub-scope in their status text (`EXT-04`,
  `EXT-13`, `DIA-11`, `DIA-12`, `MIS-34`), so downstream prose must
  cite the covered regime rather than generalising across all regimes.
- **31 `partial`** (15 %): tests exist but coverage is shallower
  than advertised — every remaining leading-`partial` row is an
  honest, deliberate deferral (known-V non-Gaussian variants
  FG-07/08/09, spatial-family depth FG-13, single-V `meta_V`
  inference FG-14/MET-01, predictor-informed grammar FG-18,
  soft-deprecated `gllvmTMB_wide()` FG-16/MIS-03,
  truncated/censored recovery FAM-15/16,
  non-Gaussian s ≥ 2 slopes RE-03, ordinary random-regression
  surface RE-12, predictor-informed latent-score rows RE-13 /
  EXT-31 / LV-01 / LV-02 / LV-03 / LV-04 / LV-05, multi-matrix / cross-package animal models
  ANI-09/10, coevolution inference grids COE-03/COE-04,
  binary-only pre-fit screening DIA-14, coverage-study gates
  CI-08/CI-10, plot dispatcher MIS-09, meta-analytic two-stage
  article MET-04, and Julia bridge JUL-01/JUL-01A). None is a
  v0.2.0 *correctness* blocker.
- **0 `opt-in`**: the `link_residual = "auto"` default
  (PR #101) eliminated this category and it has stayed empty.
- **9 `blocked`** (4 %): advertised-but-undefined or
  deliberately-deferred surfaces, either post-CRAN by design or
  design-first lanes without parser/TMB support yet —
  slash-form nesting FG-17, mixture families FAM-18, gengamma
  FAM-19, proportional `meta_V` MET-03, delta/hurdle
  latent-scale correlation MIX-10, delta-family
  `extract_proportions()` EXT-11, and the missing-data v2
  extensions MIS-32, plus the Design 73 predictor-informed
  latent-score surfaces LV-06 and LV-07. Each live public
  surface carries or must carry a fail-loud / typed-error safeguard.

The headline therefore moved **40/48/0/14 over 102 rows
(Phase 0A) -> 173/31/0/9 over 213 rows (2026-06-29 LV-03 update)**. No
`partial` or `blocked` row is a v0.2.0 correctness blocker;
they are honestly-marked deferrals (power-study coverage,
mixture / gengamma families, proportional meta-V,
delta-correlation, missing-data v2, and Design 73 interval / tier /
structured-source work). The historical Phase 0A
snapshot below is preserved verbatim for provenance.

### Historical snapshot — Phase 0A close (2026-05-16)

This register's honest tally as of Phase 0A close (2026-05-16):

- **102 capability rows.**
- **40 `covered`** (39 %): test evidence exists at the depth
  advertised.
- **48 `partial`** (47 %): tests exist but coverage is
  shallower than advertised; Phase 0B walks each one.
- **0 `opt-in`**: the `link_residual = "auto"` default
  (PR #101) eliminated this category for now.
- **14 `blocked`** (14 %): advertised but currently
  broken / undefined / removed.

This is not a number to be proud of; it is the honest
starting point. Phase 0B's job is to walk the 48 `partial`
rows + audit every `covered` row + correctly mark every
`blocked` row in the public surface.

**The vision's claim of "unparalleled capability" depends on
walking the `partial` mixed-family rows (MIX-03 through
MIX-08) to `covered`.** M1 milestone delivers that walk.

### Update — M1 close (2026-05-17)

Six rows walked from `partial` → `covered` in M1.10 close
gate: MIX-03, MIX-04, MIX-05, MIX-06, MIX-08, and MIS-05.
EXT-07 stayed `covered` with extended test-file evidence
(M1.7 cross-tier composition). MIX-10 is now `partial` (route-only) after the 2026-07-05
resolution (latent on the positive submodel only; see the
MIX-10 row + Design 02 §Hurdle/delta). The earlier `blocked`
+ class `gllvmTMB_auto_residual_delta_undefined` were never
wired (that class exists in 0 files). Per
[`docs/dev-log/after-phase/2026-05-17-m1-close.md`](../dev-log/after-phase/2026-05-17-m1-close.md).

### Update — v0.2.0 tag-prep recount (2026-06-03)

**Superseded scope wording:** this historical recount is preserved as a dated
receipt, but its broad coverage phrase applies only to the family-by-route
cells registered in the 2026-06-03 snapshot. It does not cover
later C1-partial IDs 3/9 or binomial cloglog for structured augmented slopes;
RE-14 and the current per-row statuses are authoritative.

The stale Phase 0A headline (40 / 48 / 0 / 14 over 102 rows)
was recounted directly from the per-row status column for the
v0.2.0 editorial tag-prep. The register now holds **193 rows
at 166 `covered` / 20 `partial` / 0 `opt-in` / 7 `blocked`**;
the corrected headline lives in the "Current tally" block at
the top of this section. The growth from 102 to 193 rows is
the kernel/coevolution (KER / COE), augmented non-Gaussian
slope (PHY-11..18, SPA-08..10), `cluster2` (RE-11),
missing-data (MIS-21..32), plot/extractor (EXT-19..30), and
diagnostic (DIA-08..13) sections that landed after the Phase
0A snapshot. The non-Gaussian structured-slope grid in
particular walked the phylo and spatial slope cells to
`covered` across every family (#388 / #392 / #422 / #424 /
#427 / #429). No `partial` / `blocked` row is a v0.2.0
correctness blocker — all are honest deferrals.

**Rows held by in-flight branches (NOT recounted / not edited
here; they land via their own PRs):** PHY-18, FAM-07, and the
`nbinom1` / `animal_dep` line land via **PR #441**, and the
kernel C3 row (COE-03) lands via **PR #439**. The counts
above reflect the register state on `main` at tag-prep; those
PRs will append / settle their own rows on merge.

### Section 13 — Julia bridge

Row-owner: **Hopper + Karpinski + Rose** (R-Julia bridge
admission, paired engine capability, and claim wording).

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| JUL-01A | Julia bridge unit-tier covariance and ordination accessors | `partial` | `test-julia-bridge.R`; `test-plot-covariance-tables.R`; `docs/dev-log/after-task/2026-06-16-r-bridge-extractor-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-mixed-postfit-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-raw-extractor-payload-matrix.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-extractor-scale-semantics.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-link-residual-delta-guards.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-sigma-table-point-view.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-sigma-compare-point-view.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-sigma-plot-point-view.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-sigma-comparison-plot-point-view.md` | `extract_Sigma()`, `extract_Sigma_B()`, `getResidualCov()`, `getResidualCor()`, `extract_Sigma_table()`, `compare_Sigma_table()`, `plot_Sigma_table()`, `plot_Sigma_heatmap()`, `plot_Sigma_comparison()`, `extract_ordination()`, `getLoadings()`, and `getLV()` route for `gllvmTMB_julia` objects on the ordinary unit tier. Public covariance extractors now distinguish native scale from raw retained payloads: `link_residual = "none"` returns `Lambda Lambda^T` from retained loadings, `link_residual = "auto"` uses the retained GLLVM.jl residual-augmented Sigma/correlation where available, and Gaussian/lognormal rows follow native gllvmTMB's no-op residual convention by resetting those diagonals to `Lambda Lambda^T`. The table route is point-only: it reuses `extract_Sigma()` for unit-tier Sigma/correlation rows, labels `validation_row = "JUL-01A"`, and leaves `lower`/`upper` as `NA` with `interval_status = "none"`; the comparison helper only joins those point rows to one supplied truth matrix and reports `estimate - truth`; the plot helpers display those point rows as forest/heatmap and comparison-error views and do not compute or display new intervals for Julia bridge fits. Pure-R tests cover fake payloads with explicit residual diagonals, fallback `Lambda Lambda^T` reconstruction, derived correlations, default argument forcing, transposed score payloads, bad Sigma/correlation/loading/score/family gates, structured-level gates, rotation gates, point-only table schema, point-only comparison rows, point-only plot rows, point-only comparison-plot rows, and mixed-family raw accessor admission; live R tests cover grouped-dispersion, ordinal-probit, mixed-family, and Gaussian native-TMB parity rows through `GLLVM.jl-integration`, including labels, finite loadings/scores, symmetry, unit-diagonal correlations, retained-payload `auto` behavior, native `link_residual = "none"` covariance parity, explicit `auto - none` diagonal guards, and live grouped point-table rows. The current delta guards verify grouped NB2/NB1/Beta/Gamma rows still have zero retained residual diagonal, complete balanced mixed-family rows keep Gaussian residuals at zero while Poisson/Bernoulli diagonals are positive, and ordinal-probit rows add the probit residual diagonal of one. Still partial: `unit_obs`, structured tiers, augmented slopes, cluster tiers, rotated ordinations, interval-bearing extractors, `extract_correlations()` intervals, full residual-split reporting, and broad/native `link_residual = "auto"` extractor parity across all admitted families remain gated. |
| JUL-01 | Lean `engine = "julia"` reduced-rank bridge through `GLLVM.bridge_fit` | `partial` | `test-julia-bridge.R`; `../GLLVM.jl-integration/test/test_bridge_missing_mask.jl`; `../GLLVM.jl-integration/test/test_bridge_x.jl`; `../GLLVM.jl-integration/test/test_bridge_ci.jl`; `../GLLVM.jl-integration/test/test_bridge_grouped_dispersion.jl`; `docs/dev-log/2026-06-16-nb1-gamma-bridge-parameterisation-audit.md`; `docs/dev-log/2026-06-16-nb1-reduced-rank-parity-audit.md`; `docs/dev-log/2026-06-16-nb1-reduced-rank-fisher-fix.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-response-mask-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-coef-summary-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-fixed-x-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-fixed-x-ci-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-ci-status-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-main-confint-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-predict-fitted-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-residuals-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-grouped-postfit-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-ordinal-prediction-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-simulate-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-fit-time-ci-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-grouped-ci-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-masked-ci-admission.md`; `docs/dev-log/after-task/2026-06-16-r-bridge-mixed-postfit-admission.md` | Main-dispatch route admits one `latent(..., d = K)` block on complete balanced trait x unit data, plus no-latent (`d = 0`) rows where the paired Julia bridge admits them, with fixed-effect covariates routed for complete-response gaussian, poisson, Bernoulli binomial, NB2, Beta, and Gamma rows and no structured covariance terms. The 2026-06-16 response-mask slice routes one-part no-X point fits for Poisson, Bernoulli binomial, NB2, NB1, Beta, Gamma, ordinal, and ordinal-probit rows by passing an observed-cell mask through `gllvm_julia_fit(mask = ...)` to `GLLVM.bridge_fit`; direct-wrapper tests cover every mask-capable family and main-dispatch tests cover Poisson, NB1, Gamma, and ordinal-probit. Gaussian masks, mixed-family masks, and X+mask still stop loudly. Masked no-X Wald/profile/bootstrap CI payloads are routed for Poisson, Bernoulli binomial, NB2, NB1, Beta, and Gamma rows; per-trait ordinal masked CIs remain gated with the broader ordinal CI row. The 2026-06-16 fixed-effect-X slice routes complete-response main-dispatch fits with canonical `0 + trait + ...` fixed-effect design for Poisson, Bernoulli binomial, NB2, Beta, and Gamma by sending only the extra fixed-effect columns to `GLLVM.bridge_fit(X = ...)`; live R tests compare the main-dispatch object with direct `gllvm_julia_fit(..., X = ...)`, and the paired Julia runtime test `test_bridge_x.jl` compares the low-level bridge to `fit_gllvm_cov` for Poisson, binomial, NB2, Beta, and Gamma. NB1-X, ordinal-X, mixed-family-X, masks+X, and non-canonical fixed-effect designs remain gated. The 2026-06-16 X-row CI slice routes fit-time and post-fit Wald/profile/bootstrap payloads for complete-response Gaussian, Poisson, Bernoulli binomial, NB2, Beta, and Gamma X rows through retained bridge input; NB1-X CIs, ordinal-X CIs, mixed-family-X CIs, and masks+X CIs remain gated. The 2026-06-16 post-fit and CI-status slices route `coef()`, `summary()`, scoped no-X `confint()`, fit-time `gllvmTMB(..., engine = "julia", ci_method = "wald" / "profile" / "bootstrap")`, and retained-score `predict()` / `fitted()` / `residuals(type = "response" / "pearson")` / conditional in-sample `simulate()` methods for score-bearing gaussian, poisson, Bernoulli binomial, NB2, NB1, Beta, and Gamma `gllvmTMB_julia` rows, plus response-scale probability and modal-class prediction for per-trait ordinal and ordinal-probit bridge rows and response/Pearson ordinal-score residuals for retained-probability ordinal rows; direct `gllvm_julia_fit(..., ci_method = "wald" / "profile" / "bootstrap")` calls can request stored complete-response no-X CI payloads for gaussian, poisson, Bernoulli binomial, NB2, NB1, Beta, and Gamma rows, and direct `gllvm_julia_fit(..., family = "ordinal", ci_method = "wald")` can request no-X ordinal Wald CI payloads and stored masked no-X CI payloads for the non-ordinal non-Gaussian subset, and ordinary `gllvmTMB(..., engine = "julia")` fits retain their bridge input so `confint(fit, method = "wald" / "profile" / "bootstrap")` can recompute the same admitted no-X CI payloads post-fit. Ordinal profile/bootstrap CI endpoints, ordinal-probit CI/admission, NB1-X CIs, ordinal-X CIs, mixed-family CIs, and masks+X CIs remain gated. Pure-R tests cover CI payload normalisation, stored-payload `confint()` filtering, post-fit refit argument routing with a mock, fit-time `ci_*` argument forwarding through the main dispatch, response-mask and fixed-effect-X fit-time CI payload preservation with mocks, TMB-engine `ci_*` rejection, in-sample prediction reconstruction from retained no-X and fixed-effect-X payloads, score-payload orientation normalisation, response/Pearson residual reconstruction including binomial proportions and masked `NA` cells, conditional in-sample simulation for one-family rows including masked `NA` cells and binomial count-scale draws, mixed-family retained-payload prediction/residual/simulation reconstruction, ordinal probability/class prediction, ordinal-score residual reconstruction, ordinal simulation gates, and mixed-family CI/mask/X/newdata gates, and unsupported CI gates before Julia setup; live R tests request direct-wrapper complete, masked, and fixed-effect-X Wald CIs, main-dispatch fit-time and post-fit Wald CIs including masked Poisson/NB1/Gamma post-fit recomputation and fixed-effect-X recomputation, and main-dispatch in-sample prediction, response/Pearson residuals, and conditional simulation through JuliaCall for gaussian, poisson, Bernoulli, NB2, NB1, Beta, and Gamma rows plus complete balanced no-X/no-mask/no-CI mixed-family vector rows, and response-scale ordinal probabilities/classes through JuliaCall for ordinal-probit rows; the paired Julia runtime `test_bridge_ci.jl` verifies Wald/profile/bootstrap parity against native GLLVM.jl CI engines, `test_bridge_grouped_dispersion.jl` verifies grouped Wald/profile/bootstrap bridge CI payloads and finite grouped score payloads for NB2, NB1, Beta, and Gamma, and `test_bridge_missing_mask.jl` verifies masked CI sentinel invariance, masked Wald routing for Poisson, Bernoulli binomial, NB2, NB1, Beta, and Gamma, and masked profile/bootstrap smoke for Poisson. `newdata` prediction/simulation, unconditional random-effect redraws, ordinal simulation/profile/bootstrap, mixed-family CIs, mixed-family masks, mixed-family fixed-effect X, and richer extractor parity remain gated; raw unit-tier covariance / ordination accessors are tracked in JUL-01A. The grouped-dispersion slice maps R `nbinom2()` / `nbinom1()` / `Beta()` / `Gamma()` to the paired Julia grouped-dispersion payload, labels `dispersion`, `dispersion_group`, and `dispersion_group_id`, and adds public-scale fields for NB2 (`sigma = 1/sqrt(r)`, `phi = 1/r`), NB1 (`phi` identity), Beta (`sigma = 1/sqrt(phi)`), and Gamma (`sigma = 1/sqrt(alpha)`). The grouped-dispersion main-dispatch smoke exercises all four rows through `gllvmTMB(..., engine = "julia")`; NB2 and Beta carry selected small-fixture native objective parity against `engine = "tmb"`. NB1 is parameterisation-aligned with native `phi_nbinom1` (`Var = mu * (1 + phi)`), and `test-julia-bridge.R` now includes three NB1 evidence layers: a fixed-parameter kernel check where Julia `nb1_grouped_marginal_loglik_laplace()` with zero loadings matches the native linear-variance `dnbinom(mu = mu, size = mu / phi)` likelihood to `1e-10`, a no-latent fitted-object check where `value ~ 0 + trait` matches native `engine = "tmb"` log-likelihood, df, labels, and `phi` scale on the small fixture, and a reduced-rank fitted-object check where `value ~ 0 + trait + latent(0 + trait | unit, d = 1)` matches native log-likelihood/df and checks per-trait intercepts, loadings, and `phi` on the same small complete balanced fixture after the paired Julia tiny-`phi` Fisher-information fix. Gamma grouped dispersion remains bridge-scoped: native R/TMB ordinary Gamma now uses per-trait `phi_gamma`, so the old scalar-CV native parity sentence is retired and grouped-Gamma bridge parity is a follow-up. Pure-R bridge capability tests cover wording and gate drift; live Julia parity must be rerun before any renewed Gamma bridge parity claim. The paired ordinal slices label per-trait ordinal cutpoint matrices, per-trait category counts, `cutpoint_mode`, and `cutpoint_link`, and the 2026-07-04 ordinal drift-closure slice admits GLLVM.jl ordinal no-X Wald CI payloads plus ordinal-score residuals while keeping ordinal profile/bootstrap, ordinal-probit, and simulation gated. Still partial: broad native-vs-Julia parity rows beyond selected fixtures, ordinal profile/bootstrap endpoints, ordinal-probit CI/admission, NB1-X CIs, ordinal-X CIs, mixed-family-X, masks+X, mixed-family CI/mask/X promotion, structured terms, newdata prediction/simulation, unconditional random-effect redraws, ordinal simulation, and richer extractor parity remain planned/gated. |

JUL-01 / JUL-01A predictor-informed latent-score addendum (2026-06-25, updated
2026-06-28): the R bridge now admits only complete-response Gaussian, Poisson,
and binomial logit/probit/cloglog `X_lv` point routes for
`latent(..., unique = FALSE, lv = ~ x)`. `gllvm_julia_fit()` accepts an
`X_lv` matrix and `gllvmTMB(..., engine = "julia")` builds that matrix from the
Design 73 `lv` parser setup, then passes retained `lv_effects`, `alpha_lv`,
`scores_mean`, and `scores_innovation` through `extract_lv_effects()` and
`extract_ordination(component = c("total", "mean", "innovation"))`. The route
is point-only: `std.error` is `NA`, `uncertainty_status` is
`julia_bridge_point_estimate_only_no_ci_validation`, and CI/profile/bootstrap
requests stop at `GJL-GATE-XLV-CI`. Other non-Gaussian `X_lv` rows beyond
Poisson and the three binomial standard links, response masks with `X_lv`,
fixed-effect `X` plus `X_lv`, mixed-family `X_lv`, and broad native-vs-Julia
parity remain gated (`GJL-GATE-XLV-FAMILY`,
`GJL-GATE-MASK-XLV`, `GJL-GATE-X-XLV`).

JUL-01 gate-registry addendum (2026-06-16): primary R-side bridge admission
refusals now carry stable `GJL-GATE-*` ids, schema/exact-set tests, issue
linkage to `gllvmTMB#488`, and validation-row linkage to `JUL-01`. The
read-only `gllvm_julia_gate_registry()` export makes this table inspectable
without loading Julia. See
`docs/dev-log/after-task/2026-06-16-r-bridge-gate-id-registry.md` and
`docs/dev-log/after-task/2026-06-16-r-bridge-public-gate-registry.md`. This
addendum covers user-facing admission gates, not lower-level malformed-payload
validation errors.

JUL-01A correlation-interval gate addendum (2026-06-16): Julia bridge fits now
fail explicitly with `GJL-GATE-CORRELATION-INTERVALS` in
`extract_correlations()` and `plot_correlations()`. Point-only ordinary
unit-tier correlation rows remain available through `extract_Sigma_table(...,
measure = "correlation")`; interval-bearing correlation rows remain gated. See
`docs/dev-log/after-task/2026-06-16-r-bridge-correlation-interval-gate.md`.
The public gate-registry slice pins this row to `JUL-01A`.

JUL-01 drift-guard addendum (2026-06-16): R now has an internal
cross-surface drift guard that compares `gllvm_julia_capabilities()` against a
Julia `GLLVM.bridge_capabilities()` surface. Any R-broader row fails as
unregistered; any Julia-broader row must be listed as an intentional gate. The
current intentional drift is `binomial` / `cbind_binomial`, linked to
`GJL-GATE-CBIND-BINOMIAL`, `gllvmTMB#488`, and this partial `JUL-01` bridge
row. See
`docs/dev-log/after-task/2026-06-16-r-bridge-capability-drift-guard.md`.

JUL-01 / JUL-01A landing addendum (2026-06-19): the clean bridge-admission
split (`codex/bridge-admission-split-20260619` @ c061ce2; 5 commits on
`main` 0567cd7, 33 bridge-scoped files) is now the live admission lane as
**PR #492**, superseding the bridge portion of draft #489. Both rows stay
`partial`; this addendum only names the PR carrying the evidence and does not
move any row to `covered`. See
`docs/dev-log/after-task/2026-06-19-overnight-bridge-pr492-dashboard.md`.

JUL-01 malformed-payload guard addendum (2026-07-04): issue #696 is closed
for the R-side bridge postfit surface. `.gllvm_julia_dispersion_vector()` now
fails loudly when a retained dispersion payload has length neither 1 nor the
number of traits, reporting the observed and expected lengths instead of
replacing the vector with all `NA`. The pure bridge regression lives in
`test-julia-bridge.R`. This is a payload-shape guard only; it does not widen
Julia bridge family, CI, mask, mixed-family, or structured-term support.

JUL-01 Pearson residual degeneracy addendum (2026-07-04): issue #641 is closed
for the R-side bridge postfit surface. `.gllvm_julia_residual_variance()` now
marks individual non-positive or non-finite Pearson variance cells as
`NA_real_` instead of aborting the whole residual call. The pure bridge
regression covers a saturated binomial row where `plogis(eta)` is exactly one.
This is a postfit robustness guard only; it does not widen Julia bridge family,
CI, mask, mixed-family, or structured-term support.
### Section 14 -- Predictor-informed latent scores

Row-owner: **Boole + Gauss + Noether + Emmy + Curie + Fisher**
(formula grammar, TMB implementation, mathematical contract,
extractor contract, simulation recovery, and inference semantics).
Added by Design 73 (2026-06-24). These rows started blocked and move
row by row only when parser/TMB/extractor code and recovery tests exist.

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| LV-01 | Ordinary Gaussian unit-tier `latent(..., lv = ~ x)` end-to-end support | `partial` | `test-lv-parser-guard.R`; `test-julia-bridge.R`; `test-lv-gaussian-recovery.R`; `test-lv-reml-boundary-guard.R`; `test-lv-missing-response.R`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-28-local-r500-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-design73-c1-closure-receipt.md` | C1 ordinary Gaussian unit-tier support now reaches TMB, extractors, focused recovery tests, local r500 native Gaussian interval evidence, ordinary response-mask compatibility when `lv` predictors are observed and complete at the unit level, and a narrow Julia bridge point route: parser metadata and unit-level `X_lv_B` preflight are retained, `alpha_lv_B` is estimated on the TMB path, the zero-mean innovation `e_i` remains in the likelihood, the ordinary `Psi` companion is preserved on TMB unless the user asks for `unique = FALSE`, `B_lv_unit = Lambda alpha^T` is reported/ADREPORTed, extractor smoke tests verify score decomposition and trait-scale algebra, native TMB Gaussian recovery checks cover rotation-stable `B_lv` plus `Sigma = Lambda Lambda^T + Psi` targets, the local r500 Wald grid passes the 0.92--0.98 coverage band for both `wald_z` and `wald_t_unit` across the four ordinary Gaussian cells, and `test-lv-missing-response.R` proves `miss_control(response = "include")` masks response rows without changing the unit-level `X_lv_B`, log likelihood, or fitted parameters relative to the matching complete-case fit. The Julia bridge route is complete-response Gaussian only, requires no fixed-effect `X` and no response mask, and fits the reduced-rank `latent(..., unique = FALSE, lv = ~ x)` route with retained `X_lv`, `lv_effects`, `alpha_lv`, `scores_mean`, and `scores_innovation`. It provides point estimates with optional uncalibrated Wald plumbing; it has no calibrated bridge-interval, profile, or bootstrap claim. The row remains partial because calibrated Julia `X_lv` intervals, missing `lv` predictors, source/tier expansion, non-Gaussian response masks, and broad Julia parity are not admitted. `test-lv-reml-boundary-guard.R` verifies that `REML = TRUE`, `offset()`, `mi()`, smooth, and random-effect terms inside `lv` fail loudly; any fixed-effect RHS covariates (including exact fixed/LV overlap), unsupported tiers/sources, augmented random-regression combinations, and unsupported Julia bridge `X_lv` combinations also remain rejected. |
| LV-02 | Gaussian recovery of `B_lv`, `Sigma`, and `Psi` | `covered` | `test-lv-gaussian-recovery.R`; `dev/lv-wald-coverage.R`; `dev/lv-wald-coverage-slurm.sh`; `test-lv-wald-coverage-harness.R`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-28-local-r500-summary.csv`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-28-local-r500-excluded-replicates.csv`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-28-local-r500-t-vs-z.csv`; `docs/dev-log/after-task/2026-06-28-lv-wald-local-pilot.md` | Focused native TMB Gaussian recovery covers a CRAN-safe rank-1 DGP and a heavy rank-2 DGP. The rank-1 gate checks convergence, positive-definite `sdreport()`, finite `ADREPORT(B_lv_unit)` SEs, agreement between extractor SEs and report SEs, independent manual delta-method SEs for `B_lv`, primary trait-scale `B_lv` recovery, and secondary `Sigma = Lambda Lambda^T + Psi` recovery with finite `Psi`. The heavy rank-2 gate checks the same rotation-stable recovery targets while deliberately avoiding raw `alpha` or raw `Lambda` as pass/fail targets because they are rotation-dependent for `K > 1`. The dev-only Wald coverage campaign harness defines four ordinary Gaussian cells, assigns one recorded seed per replicate/SLURM array task, writes per-replicate RDS outputs, and summarises coverage, MCSE, bias, RMSE, convergence, positive-definite Hessian, `sdreport()` availability, CI availability, and failed-fit denominators for trait-scale `B_lv`; it emits both normal-critical Wald (`wald_z`) and unit-df t-critical Wald (`wald_t_unit`) comparator rows and has a login-node-safe SLURM wrapper for the same one-seed-per-array-task campaign. The 2026-06-28 local r500 grid attempted 500 fits/cell, all optimizer-converged, produced 14,000 long rows and 28 summary rows, and all target/method rows met `production_n_reps_met = TRUE` and `passes_coverage_band = TRUE` with coverage 0.9269--0.9610 and MCSE 0.0088--0.0119. Positive-definite-Hessian denominators were explicit: `n_eligible = 500` for `gaussian-d1-n144-t3`, 487 for `gaussian-d1-n72-t3`, 487 for `gaussian-d2-n96-t4`, and 479 for `gaussian-d2-n160-t4`. In this grid `wald_t_unit` was never worse than `wald_z` and improved 12/14 target rows by 0.0020--0.0063. Profile/bootstrap rescue is not required for these four ordinary Gaussian cells, but remains available as a future inference slice if broader cells under-cover. |
| LV-03 | Missing-response compatibility for `lv` predictors | `partial` | `test-lv-missing-response.R` | Ordinary Gaussian native TMB now has a response-mask compatibility gate for observed complete unit-level `lv` predictors. The test fits the same DGP with `miss_control(response = "include")` and the matching complete-case `miss_control(response = "drop")`, checks convergence and gradients, proves masked rows are retained with `is_y_observed = 0` and sentinel `y = 0`, verifies `X_lv_B` is unchanged, checks log-likelihood and fitted-parameter equality to the complete-case fit, records `missing_data$counts`, checks trait-scale `B_lv` recovery through `extract_lv_effects()`, and verifies `total = innovation + mean` through `extract_ordination()`. The row remains partial because missing `lv` predictors, `mi()` terms inside `lv`, non-Gaussian response masks, factor-valued masks, mixed-family masks, Julia bridge masks with `X_lv`, and source/tier-expanded masks remain gated. |
| LV-04 | Factor predictors and rank-deficiency guards in `lv` | `partial` | `test-lv-parser-guard.R`; `test-lv-factor-runtime.R` | Parser/API tests cover factor expansion, intercept dropping, equivalence of `lv = ~ x` and `lv = ~ 0 + x`, and typed rejection for rank-deficient or aliased `X_lv` designs. The ordinary Gaussian TMB path now has runtime evidence for a three-level factor DGP: `test-lv-factor-runtime.R` checks the resulting `X_lv_B` column names and dimensions, finite `alpha_lv_B`, `U_lv_mean_B`, `U_B_total`, and `B_lv_unit`, the algebra `B_lv_unit = Lambda_B %*% t(alpha_lv_B)`, `extract_lv_effects()` trait-by-level `B_lv` recovery, `total = innovation + mean` score decomposition, rare nonempty factor-level behaviour, and empty-level rejection before fitting. The row remains partial because factor-valued LV predictors are not yet validated for interval coverage, non-Gaussian native TMB families, missing LV predictors, missing-response compatibility for factor-valued predictors, mixed-family bridge rows, tier-expanded rows, or structured-source forms. |
| LV-05 | Non-Gaussian predictor-informed latent scores | `partial` | `test-lv-parser-guard.R`; `test-lv-bernoulli-depth.R`; `test-lv-native-nongaussian-guard.R`; `test-lv-family-boundary-guard.R`; `test-lv-mixed-family-first-cell.R`; `test-mixed-lv-family-harness.R`; `test-julia-bridge.R`; `dev/lv-wald-coverage.R`; `dev/lv-wald-coverage-slurm.sh`; `test-lv-wald-coverage-harness.R`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-30-local-binomial-r500-summary.csv`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-30-local-binomial-r500-excluded-replicates.csv`; `docs/dev-log/artifacts/lv-wald-coverage/2026-06-30-local-binomial-r500-t-vs-z.csv`; `docs/dev-log/artifacts/methods-superarc/lv-design73-c1-closure-receipt.md`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-source-contract.md`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-recovery-cell-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-pure-recovery-cell-summary.csv`; `docs/dev-log/artifacts/methods-superarc/lv-mixed-family-all-native-calibration-target-summary.csv`; `test-lv-cross-family-predictor-bridge.R`; `test-mixed-gaussian-lognormal-scale.R`; `docs/dev-log/artifacts/cross-family-lv-predictor/2026-08-27-local-canary-pre-run-receipt.md`; `docs/dev-log/artifacts/cross-family-lv-predictor/2026-08-27-all-family-pre-run-receipt.md` | Native non-Gaussian predictor-informed LV is compositionally admitted for registered family/link rows with complete responses, one numeric unit predictor, and no fixed RHS. Loadings-only `unique = FALSE` admits rank through logical responses; automatic `Psi` additionally requires that physical loading rows, latent-predictor coefficients, and engine-free diagonal slots pass a necessary joint mean-covariance dimension screen. Pure-logic tests cover IDs 0--16, repeated families, the retained four-Gaussian rank-2 route, and verified multinomial contrast expansion; live loadings-only route-health covers rank-2/rank-3 Gaussian/binomial/Poisson/ordinal/multinomial fits and one retained rank-3 fit containing IDs 0--15, with distinct Gaussian raw-scale and lognormal log-scale residual slots. Earlier retained campaigns remain the claim-bearing evidence: all 3,800 named mixed/sentinel r200 attempts, all 3,800 pure r200 attempts with Beta and ordinal HOLDs, and 4,000 mixed r500 attempts with eight target-wise Wald PASS cells. Those conclusions do not calibrate arbitrary compositions. The row remains partial for broad recovery/calibration, nonstandard binomial links, masks, structured tiers/sources, fixed `X + X_lv`, REML, and Julia interval parity. |
| LV-06 | Tier-expanded `lv` support (`unit_obs`, `cluster`, `cluster2`) | `blocked` | n/a | Planned only. `unit_obs` needs within-unit/session score-mean derivation. `cluster` needs an ordinary reduced-rank cluster slot or an explicit remap. `cluster2` is diagonal-only today and cannot carry `lv` without a new tier design and simulation recovery. |
| LV-07 | Structured-source `lv` support (`phylo_*`, `animal_*`, `spatial_*`, `kernel_*`) | `blocked` | n/a | Planned only. Dense `kernel_latent(..., lv = )` must first clear single-kernel equivalence; phylo/animal need observed-tip/animal-level covariate derivations; spatial needs the site-vs-mesh score-mean target; multi-kernel attribution needs diagnostics. No broad R-Julia parity claim until named Julia rows move. |
| LV-08 | Gaussian `phylo_*(..., lv = ~ x)` structured predictor-informed latent betas (`B_lv = Lambda_phy alpha^T` under phylogeny) | `blocked` | n/a | Planned only; the specific Gaussian-phylo cell under the LV-07 umbrella and the maintainer's headline structured-`X_lv` want. Design 76 sets the estimand, symbolic alignment, and ADEMP recovery/coverage gate. The frozen GLLVM.jl Gate 0-3 evidence covers only the revised `B_eta_realized` target, not this population `B_lv`; the R grammar stays fail-loud until high-risk grammar and likelihood work is authorized and evidence clears. The orthogonal Model A is a different estimand (`B_lv = Lambda_B alpha^T`); its named profile/bootstrap evidence is recorded in LV-09. Predictor-informed `lv` is excluded from the present Gaussian REML restriction because `alpha_lv_B` is not integrated. |
| LV-09 | **Orthogonal Model A** — Gaussian predictor-informed *ordinary* `B_lv = Lambda_B alpha^T`, recovered with a *separate orthogonal* `phylo_latent` term present (`latent(0 + trait \| species, d = K, lv = ~ x) + phylo_latent(...)`); historical profile and bootstrap interval evidence | `partial` | Coverage: `docs/dev-log/artifacts/lv-effects-ci-coverage/` (SUMMARY + per-rep CSVs); unit: `test-profile-ci-lv-effects.R`, `test-bootstrap-lv-effects.R`, `test-lv-reml-gaussian.R`; boundary: `docs/dev-log/artifacts/methods-superarc/lv-design73-c1-closure-receipt.md` | Design 76 §UPDATE re-scope (maintainer, 2026-07-06). **Historical production profile coverage passed for rank-1 Gaussian Model A:** 0.952 / 0.950 / 0.962 at S = 60 / 100 / 200, all at least 500 replicates per cell, band 0.92--0.98. A later diagonal-tier alias repair restored positive-definite `sdreport()` and finite Wald output in the named unit fixture, so the earlier narrative that the Wald leg was intrinsically unavailable is superseded. This does not create a current public profile/bootstrap route: the public extractor withdraws both, and current predictor-informed `lv` rejects `REML = TRUE` because the present Gaussian REML restriction integrates `b_fix` but not `alpha_lv_B`. The row remains partial and historical; its orthogonal Model-A interval evidence does not transfer to current ordinary C1, the blocked structured estimand in `LV-08`, or Julia bridge calibration. |

Guard note (2026-06-29): `test-lv-parser-guard.R` now verifies that
ordinary fixed-effect RHS covariates cannot be combined with
predictor-informed `lv` terms in C1, including exact overlap
(`x + latent(..., lv = ~ x)`) and non-overlap
(`z + latent(..., lv = ~ x)`) formulas. This is fail-loud evidence only;
the combined `X + X_lv` regime remains blocked until it has a derivation,
implementation, recovery tests, and interval evidence.

Guard note (2026-06-29): `test-lv-native-nongaussian-guard.R` now verifies
that native TMB rejects unsupported non-binomial `latent(..., lv = ~ x)` calls
for Poisson, NB1, NB2, lognormal, Gamma, Beta, Tweedie, Student-t, truncated
Poisson, truncated NB2, beta-binomial, delta-lognormal, and delta-Gamma. This
is fail-loud evidence only; native non-binomial `lv` support remains blocked
until family-specific derivations, recovery tests, and interval evidence land.

Guard note (2026-06-29): `test-lv-family-boundary-guard.R` now verifies that
binomial cauchit, ordinal-probit, and mixed Gaussian/binomial/Poisson
`latent(..., lv = ~ x)` calls fail loudly before fitting. This is fail-loud
evidence only; nonstandard binomial links, ordinal `lv`, and mixed-family `lv`
remain blocked until their own derivations, recovery tests, and interval
evidence land.

Guard note (2026-06-30): `test-lv-reml-boundary-guard.R` now verifies that
`REML = TRUE`, `offset()`, `mi()`, smooth terms, and random-effect terms inside
`lv` fail loudly before fitting. This is fail-loud evidence only; REML with
predictor-informed latent scores and richer `lv` formulas remain blocked until
their own derivations, recovery tests, and interval evidence land.

### Section 15 -- Variational integration route (`integration = "va"`)

Opt-in research route added 2026-07-31. Laplace remains the package default and
nothing about the default fitting path changes. The objective is an **ELBO** --
a lower bound -- never a marginal likelihood, so Design 85 §10's prohibitions
are enforced by class and by method, not by documentation.

| ID | Capability | Status | Evidence | Notes |
| --- | --- | --- | --- | --- |
| VA-01 | `integration = "va"` translation layer: formula/data -> the VA-R3 engine, returning a `gllvmTMB_va` fit | `covered` | `test-va-routing-oracle.R`; `test-integration-fence.R` | The correctness claim is an oracle identity, not a recovery claim: a routed fit equals `.approximation_engine_fit()` called directly on the same data, to `1e-8` on both the loadings and the ELBO. The engine contains no RNG, so its multi-starts are deterministic and the tolerance is meaningful. Runs under `NOT_CRAN=true` alone -- deliberately not behind `GLLVMTMB_HEAVY_TESTS`, since gating the only proof the layer is correct behind a normally-unset variable means routine CI never runs it. |
| VA-02 | Admission fence: `unique = FALSE`, all 18 scalar family/link cells, `q <= 2`, `p <= 80`, `n >= 100`, TMB engine | `covered` | `test-integration-fence.R`; `docs/dev-log/after-task/2026-08-06-va-gh-h7-arc1-public-closeout.md`; Design 110 | The fence enumerates every admitted family/link pair individually and errors outside the boundary. Gate E passed 18/18 arithmetic, compiled, and light-fit cells at H = 7. Multinomial (family 16) and every other non-scalar/coupled architecture remain separate. This row certifies reachability and fail-loud scope, not recovery or coverage. **Citation repointed 2026-09-02:** the row previously cited `docs/dev-log/audits/2026-08-06-va-gh-h7-gate-e.md`, which never existed on `main` — it lived only on the dormant `origin/codex/va-gh-all-families` branch and was not among the 8 files the 2026-08-17 VA-lane reconciliation (`b4ad164da`) salvaged, because that reconciliation notes the Gate-E findings were already ported into the register separately (`ae340bdd`, citing `7bf56c4a`). The present after-task report documents the same promoted Gate-E result (18-cell admission, `H = 7` default, `calibrated = FALSE` labelling) and is cited here instead. |
| VA-03 | No silent fallback and no silent DROP | `covered` | `test-integration-fence.R`; `test-va-missing-response.R` (mi refuse); `test-va-mixed-family.R` | The route wires only `(X, unit_id, trait_id, q, is_y_observed, family)`, so anything else in the model would be ignored while the fit still reported healthy. Refused explicitly: a latent term away from the unit grouping, a constrained ordination (`lv = ~ x`), a second latent tier, any other covstruct, `mi()` predictors, offsets, weights, `REML`, `lambda_constraint`, `Xcoef_fixed`, and non-dense (ragged) crossed designs. **Design 107 Stage 1 (2026-08-01):** dense response masks (`miss_control(response = "include")`) are no longer refused — they travel as `is_y_observed` (see VA-10). **Design 108 Stage 2 (2026-08-01):** mixed families among the admitted fence set are no longer refused (see VA-11); out-of-fence families still abort. Latent options are checked by **whitelist**, so an option added later fails loudly instead of being silently ignored. Two of these were live defects caught in adversarial review, not designed in. |
| VA-04 | Design 85 §10 enforcement on the fitted object | `covered` | `test-va-routing-oracle.R`; `test-va-intervals.R` | Class is `c("gllvmTMB_va","gllvmTMB")` and deliberately does not inherit `gllvmTMB_multi`. `logLik`/`AIC`/`BIC` and unsupported generic methods fail loudly. `vcov()` and `confint()` are the bounded exception: on a healthy fit retaining its objective they expose only the fixed-effect block from profiled Schur information, with `calibrated = FALSE`; otherwise they fail closed. Raw-loading intervals remain unavailable. |
| VA-05 | Historical pre-Design-110 `Sigma_B` recovery comparison, binomial-logit, `q <= 2` | `partial` | `docs/dev-log/2026-07-31-gate3-result-corrected.md`; `dev/va-gate3/`; results local per D-50 | Gate 3 compared the then-current JJ and GH estimators on 2,160 known-truth datasets. JJ passed its frozen point-recovery conjunction at `q <= 2`; that result motivated the old public JJ routing. Later attenuation and H-ladder work showed that JJ's loading scale plateaus while GH converges, and Design 110 therefore supersedes the routing decision after a separate 18-cell Gate E. Keep Gate 3 as historical evidence, not as the current default or an all-family/coverage certificate. The `q = 4`, small-`p` axis-collapse failure still supports the public `q <= 2` fence. |
| VA-06 | Fixed-effect VA-Wald uncertainty and interval coverage | `partial` | `test-va-intervals.R`; Design 110; Arc-2 closeout commit `7bf56c4a`; `docs/dev-log/audits/2026-08-07-va-gh-h7-arc2-totoro-adjudication.{csv,dcf,md}` at that commit | Fixed-effect `vcov()`/`confint()` use the profiled Schur information and are implemented, bounded to beta, and labelled `calibrated = FALSE`. The completed 36,000-fit Arc-2 confirmation classified the fixed-effect VA-Wald outcomes as 20 PASS and 16 FAIL under its cell-specific rules. This mixed result does not support a general nominal-coverage claim or removal of `calibrated = FALSE`. The fixture was ordinary loadings-only at n = 120, p = 8, and q in {2, 5}; loading/covariance intervals and other model axes remain blocked. Both evidence stages ran on Totoro (`cross_platform = FALSE`). |
| VA-07 | `q >= 3`, and the 5+ latent factors that `decisions.md` A3 names as the motivating regime | `blocked` | n/a | Gate 3 measured `q = 4` and refused it: every `va_jj` axis-collapse failure sits in the `q = 4`, `p = 8` corner at rates 0.26--0.77 against a 0.05 tolerance, with every lower 2*MCSE bound above the threshold. Four latent axes are not identifiable from eight responses. The shipped fence stops where the gate passed. Honest note: `q <= 2` is *further* from A3's target than the `q <= 4` that was hoped for -- this narrows the claim, it does not narrow the gap. |
| VA-08 | `integration = "eva"` | `blocked` | n/a | Not an admitted value of `integration`; the enum is `c("laplace","va")`. The EVA engine and template remain research-only. Existing grids diagnose serious covariance-recovery failures, but they do not establish general coefficient validity, interval calibration, or a public estimator claim. Design 110 also shows that none of the 18 scalar family/link cells requires EVA for implementation reachability: exact or one-dimensional GH evaluation is available throughout that registry. |
| VA-09 | All 18 scalar family/link expectations under public GH H = 7 | `partial` | `test-va-all-family-oracles.R`; `test-va-all-family-compiled.R`; `test-va-all-family-light-fits.R`; Gate-E audit; Design 110; Arc-2 closeout commit `7bf56c4a`; `docs/dev-log/audits/2026-08-07-va-gh-h7-arc2-totoro-adjudication.{csv,dcf,md}` at that commit | Every scalar cell passes its own arithmetic, compiled, and light-fit gate. Gaussian, Poisson, lognormal, and Gamma use exact expectation paths; delta-lognormal and delta-Gamma are hybrid; the remaining cells use ordinary one-dimensional GH. H = 7 and public auto-to-GH are implemented. Arc 2 completed all 36,000 confirmation fits but did not establish broad recovery: the independent family/rank point-route verdicts were 1 PASS (`poisson_log`, q = 5), 24 FAIL, and 11 INCONCLUSIVE. H = 7 stability was 16 PASS, 12 INCONCLUSIVE, and 8 exact-cell NOT_APPLICABLE. The row remains `partial`; implementation and light-fit support are not general accuracy evidence. Both evidence stages ran on Totoro (`cross_platform = FALSE`). |
| VA-10 | VA response-include via dense `DATA_IVECTOR(is_y_observed)` (Design 107 / Design 108 Gate A Stage 1) | `partial` | `test-va-missing-response.R`; Design 107 | The VA-R3 template gates density/`ell` on `is_y_observed`; routing admits `miss_control(response = "include")` under the scalar VA fence; sentinel invariance and a thin public smoke are covered. The original smoke used the then-default JJ tier; current public auto uses GH. **2026-08-15: the sentinel/admission contract is now parametrised across all 18 scalar cells** (exact fn/gr sentinel-invariance at masked cells; masked `expected_loglik_by_obs` exactly 0; real include-mask fit per cell; `betabinomial_logit` and `delta_gamma_log` recorded health-gate-marginal — multi-start polish fragility, not a masking defect). Still `partial`: this is not a public missing-data certificate, VA `mi()`, coverage, or recovery evidence, and multinomial has no VA route. |
| VA-11 | VA mixed-family + per-trait Gaussian SD (Design 108 Gate A Stage 2) | `partial` | `test-va-mixed-family.R`; `test-va-r3-prototype.R`; Design 108; Design 110 | Dense per-row family/link IDs and per-trait Gaussian SDs are routed, and public auto uses GH for pure and mixed scalar fits. Thin mixed-family public and prototype smokes are healthy. This is plumbing/light evidence, not a mixed-family recovery certificate; VA `mi()`, non-scalar families, and coverage remain outside scope. |
| VA-12 | Tail-safe binomial-probit under public GH H = 7 | `partial` | `test-va-probit-adsafety.R`; `test-va-all-family-oracles.R`; `test-va-all-family-compiled.R`; Design 108 §6; Design 110; Arc-2 closeout commit `7bf56c4a` | User-reachable after Gate E. The public route preserves family ID 1 plus probit link ID 1, selects GH, and defaults to H = 7. The log-scale `log Phi` primitive, derivatives, Hessians, routing separation from logit, independent H7 oracle, compiled value, and a public toy fit are covered. Arc 2 completed its multi-seed point and fixed-effect interval campaign, but its cell-specific outcomes do not establish a general recovery or coverage claim; the route remains `partial` and `calibrated = FALSE`. Ordinal-probit is a distinct scalar registry cell and multinomial remains a separate coupled architecture. |
| VA-13 | Point-estimate ordination plus latent-score variational posterior SD | `partial` | `test-va-ordination.R`; `test-va-intervals.R`; `dev/va-usability/A2-ATTENUATION.md`; Design 110; Arc-2 closeout commit `7bf56c4a`; `docs/dev-log/audits/2026-08-07-va-gh-h7-arc2-totoro-adjudication.{csv,dcf,md}` at that commit | `extract_ordination()`, `getLV()`, `getLoadings()`, and `extract_loadings()` unpack the VA optimum. `getLV(se = TRUE)` returns the fitted variational posterior SD, explicitly labelled `calibrated = FALSE` and refused when it is constant across units or the evaluator is not GH. This is not a frequentist random-effect SE and omits global-parameter uncertainty. The completed Arc-2 campaign classified latent posterior-SD outcomes as 15 PASS, 20 FAIL, and 1 INCONCLUSIVE across the 36 family/rank cells. That mixed result retains `calibrated = FALSE`; structured tiers, random slopes, mixed families, rotation-aware assessment beyond the frozen fixture, and global-parameter propagation remain unmeasured. Fixed-effect VA-Wald is a separate profiled-Schur object. Both evidence stages ran on Totoro (`cross_platform = FALSE`). |

### Section 16 -- Binary LA-MSPL estimator

Opt-in estimator work for gllvmTMB 0.7. The mathematical and API contract is
`docs/design/88-binary-mspl-estimator.md`; retained-attempt campaigns live in
`inst/sim/lane-b/` and never run on GitHub Actions.

| ID | Capability | Status | Test evidence | Notes |
|----|------------|--------|---------------|-------|
| MSPL-01 | `estimator = "mspl"`, ordinary complete-Bernoulli `latent(d=1:2)` with no free Psi, logit/probit/cloglog | `partial` pending frozen B2 campaign | `test-mspl-api.R`; immutable v8 numerical bundle; `inst/sim/lane-b/` | Compiled objective, positive Cauchy-Binet determinant oracle, penalty-off decomposition, q=1:2 and three-link construction are covered. The default Bernoulli Psi grammar is admitted only when every Psi coordinate is mapped off; `unique=FALSE` states the same fitted surface explicitly. Public point estimates only. Promotion requires the frozen ordinary and permutation B2 cells, including separated tails, stationary gradients, multistart covariance agreement, risk/prediction comparators, and retained failures. |
| MSPL-02 | `estimator = "mspl"`, complete-Bernoulli `spatial_indep()` and `spatial_latent(d=1:2, unique=FALSE)` | `partial` pending frozen spatial B2 campaign | `test-mspl-api.R`; spatial marginal-coordinate oracle; `inst/sim/lane-b/` | Three links and all three admitted spatial structures construct with exact outer/random maps, zero atom/tail status, penalty-off closure, and continuum-reference versus finite-mesh scale distinction. Promotion requires the 72-cell spatial campaign and whole-block prediction gate. Scalar/common, unique companion, slopes, anisotropy, and spatiotemporal forms are rejected. |
| MSPL-03 | Fail-closed fixed-design Jeffreys log determinant | `covered` on validated binary64 fixtures | `test-mspl-api.R`; external `lane-b-jeffreys-certified-maxvol-atomic-v8` SHA receipt | Numerically guarded weighted-maxvol atom with typed status, map/tie-aware `X_*`, positive Cauchy-Binet oracle, adversarial row/column scaling and catastrophic-tail fixtures. The inverse/exchange decisions carry a-posteriori bounds, but the returned value/derivatives do not claim a formal interval certificate. Explicit order-three requests are unsupported; the admitted fixed-only additive penalty uses order at most two under nested Laplace. |
| MSPL-04 | MSPL likelihood comparison and uncertainty (`logLik`, AIC/BIC/LRT, profile, `vcov`, `confint`, bootstrap intervals) | `blocked` | `test-mspl-api.R` typed-error tests | Planned 0.7 promotion is point estimation only. The stored penalty-off LA value at the MSPL point is provenance, not a maximized likelihood. Repeated-sampling estimator-specific calibration is required before any route is promoted. |
| MSPL-05 | `loading_ridge` alias and ridge/MSPL separation | `covered` API / `partial` risk comparison | `test-mspl-api.R`; B2 four-arm manifest | `loading_ridge` is the integration-neutral spelling of the existing Gaussian-prior MAP loading ridge. Explicit ridge plus public MSPL is an error. ML, ridge, MSPL, and private hybrid remain separate B2 arms; the campaign decides whether ridge is redundant rather than assuming it. |

## What this register does NOT do

- **It does not replace the test files.** Every `covered`
  entry must point at a test file; this register is the
  cross-reference, not the test.
- **It does not replace the design docs.** The design docs
  (`01` to `06`) define what the package promises; this
  register tracks whether the promise is backed by evidence.
- **It does not replace `R CMD check`.** A row marked
  `covered` can still have a failing test on a particular
  OS / R version; the per-PR after-task report records
  this.
- **It does not replace the README's stable-core feature
  matrix.** The README matrix is the user-facing
  presentation; this register is the developer-facing
  honest ledger.
- **It does not commit to milestone timings.** The
  function-first roadmap (M1 / M2 / M3 / M5 / M5.5) commits
  to ordering; this register tracks what each milestone
  delivers.

## How this register grows

Each new PR that touches an advertised capability:

1. **Identifies the row** affected (by row ID, e.g. MIX-03).
2. **Adjusts the status** with provenance: e.g. *"MIX-03
   walked from `partial` to `covered` via
   `tests/testthat/test-mixed-family-extractor-rigour.R`
   PR #XXX"*.
3. **Appends the row** (if new capability) with a row ID
   following the section-prefix convention (`FG-`, `FAM-`,
   `RE-`, `PHY-`, `SPA-`, `MET-`, `MIX-`, `EXT-`, `DIA-`,
   `CI-`, `LAM-`, `MIS-`, `JUL-`, `KER-`, `COE-`, `ANI-`,
   `LV-`).
4. **References the row** in the after-task report's
   "validation-debt update" section (new section added by
   Phase 0A step 9 `10-after-task-protocol.md` revision).

Phase-boundary close gates require Shannon's coordination
audit to cross-check the register against the merged code.
The audit is a Shannon-specific dev-log entry.

## Cross-references

- `docs/design/00-vision.md` — advertised capability list;
  vision item 5 is the headline differentiator backed by the
  mixed-family rows.
- `docs/design/01-formula-grammar.md` — Section 1 mirrors the
  parser-syntax status map.
- `docs/design/02-family-registry.md` — Section 2 mirrors the
  family-registry table; delta families' `blocked` status here
  matches the registry's deferred-to-post-CRAN section.
- `docs/design/03-likelihoods.md` — per-family likelihood
  contracts; per-family `partial` rows here have entries.
- `docs/design/04-random-effects.md` — Section 3 random-slope
  cap is reflected in RE-02 and RE-03.
- `docs/design/05-testing-strategy.md` — the test-file
  evidence column above points at files documented there.
- `docs/design/06-extractors-contract.md` — Section 8
  mirrors the extractor coverage matrix.
- `README.md` — Stable-core feature matrix (Phase 0A step
  10) is generated from this register's honest tally.
- `NEWS.md` — every public-surface tightening (e.g. `blocked`
  row added or `partial` → `blocked` downgrade) gets a NEWS
  entry naming the row ID.

## Persona-active engagement

- **Rose** (lead): validation-debt audit. Owns the
  overpromise-preventer rule. Every Phase 0A step 8 after-task
  report verifies a Rose-flagged inconsistency is resolved.
- **Shannon** (lead): cross-team coordination + row-ownership
  audit. Phase-boundary audits cross-check the register
  against the merged code.
- **Ada** (orchestrator): ratifies the register on every
  phase boundary. Phase 0A close, Phase 0B close, M1 close,
  M2 close, M3 close, M5 close, M5.5 close — Ada's
  ratification is the gate.
- **Row-owner personas** (per row): Boole for FG / RE / PHY
  / SPA / MET / LAM / LV grammar, Gauss for FAM / SPA / LV TMB,
  Fisher for CI / MET / MIX / DIA / LAM / LV inference, Emmy for
  EXT / MIS / LV extractors, Curie for DIA / MIS-05 / LV recovery,
  Pat for MIS user-facing surface.

Each row-owner persona is responsible for:

1. Confirming the status reflects current code.
2. Identifying the test file path that backs `covered`.
3. Flagging when an external audit (Rose, Shannon) marks the
   row inconsistent.

This is the function-first discipline made operational: every
row has a named owner; no row is anonymous.

## Structured source strength arc (2026-08-31, local candidate only)

| ID | Contract | Status | Evidence and remaining debt | Owner |
| --- | --- | --- | --- | --- |
| STR-RHO-FIX | One fixed attenuated phylo/animal/kernel/spatial trait-intercept block; preserve full Psi, source scale and ordinary components | covered | `test-structured-rho-fixed-oracle.R`: 60 direct Gaussian likelihood points across dense, sparse augmented, tree and kernel representations; common and both latent subsets. Spatial covariance and family references are in `test-structured-rho-spatial.R` and `test-structured-rho-spatial-family.R` (STR-RHO-SPA below). Legacy14-case and optimized endpoint compatibility, fixed ordinary components, and1116 nonspatial native-family endpoint comparisons pass; two optimized Poisson reference pairs pass as software equivalence only. Local full check and final panel completed; the Rd-link warning has an exact help-only repair and direct validator receipt. No three-OS/merge claim. | Gauss / Rose |
| STR-RHO-EST | Native Gaussian estimated source strength with complete replication, retained residual and observation-level identification | partial | `test-structured-rho-estimated-oracle.R`: 100 independent likelihood/gradient points, endpoints approached on logit scale, unit_obs and long/wide parity. `test-structured-rho-estimation-contract.R`: replication, source contrast and unused-level counterexample. Descriptive weak-loading and boundary flags implemented with workflow tests; 12 pre-run optimizations returned but40-tip recovery was weak; independent likelihood and physical-score audits pass (PRERUN.md). The approved frozen2400-attempt study completed:2248 numerical successes and152 gradient-rule failures, no retries/timeouts. Fisher assigns bounded recovery passes only to animal dep and kernel indep/dep/latent-plus-Psi; all other source/mode regimes remain partial (dev/structured-rho/reviews/fisher-retained-20260831.md). No general recovery pass. No rho interval claim. | Noether / Fisher |
| STR-RHO-WORKFLOW | Simulation, known-level prediction, source_strength extraction, typed source-allocation limitations and runnable long/wide teaching | covered | `test-structured-rho-workflow.R` covers221 point-object, simulation-covariance, prediction, metadata, diagnostic and typed-limitation assertions. Common, sparse/dense/kernel, fixed0/interior/estimated cases covered; ordinary intercept components and excluded augmented-slope combinations have18 checks; raw-rho profile requests and direct phylogenetic-signal intervals are fenced. The long/wide teaching article and keyword-grid note render locally; five article renders used22 teaching fits. First full local check has zero errors and three notes, with test warnings/skips retained; the permitted current-source repair failed on validation-startup output before its tests. The approved third check finished in1456.340s with0 errors,1 Rd-link warning/3 notes and23875 test passes; the exact help-only repair passes the actual cross-reference validator. Final independent panel accepts qualified local closure, not an exact-repaired full-check or three-OS claim. `dev/structured-rho/PLAN.md` defines the full arc and separate attempt budgets. | Boole / Pat / Rose |
| STR-RHO-SPA | Same attenuation for spatial indep/dep/latent, with separate estimated range and modeled-location IID fields | partial | `test-structured-rho-spatial.R`:184 assertions including42 joint Gaussian likelihood/gradient points, near endpoints, unequal Psi, known-level predictions, simulation covariance, long/wide and geometry guards. `test-structured-rho-spatial-family.R`:1128 assertions including every admitted native family and multinomial boundaries; complete Psi preserved through an equivalent full covariance reference. Eight engineering fits returned with small gradients/PD Hessians; JSON wrapper failures retained and saved RDS verified without reruns. The frozen 1,600-attempt Gaussian spatial study has 1,494 strict successes: 14 estimated-rho cells are partial and two irregular-long rho=0.3 cells are blocked; none passes the predeclared joint rho/range recovery gate (`dev/structured-rho/spatial-recovery/RESULTS.md`). This is evidence against a broad recovery claim, not a universal non-identifiability theorem. No intervals, unseen-location recovery, or non-Gaussian recovery claim. | Gauss / Noether / Fisher / Pat |
