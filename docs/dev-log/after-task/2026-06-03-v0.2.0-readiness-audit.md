# After-task: v0.2.0 readiness audit (READ-ONLY)

**Date:** 2026-06-03
**Scope:** Read-only gap-to-v0.2.0 audit against issue #344's four
sub-items (NEWS + version bump; validation-debt register clean;
pkgdown article surface #230/#347; `R CMD check --as-cran` clean on
3 OSes via `full-check.yaml`). No package code, NEWS, DESCRIPTION, or
register was changed. Evidence is cited inline.

---

## 1. Version + NEWS

- **`DESCRIPTION` `Version: 0.2.0`** is already set (not a `0.1.x` /
  `.9000` dev version). DESCRIPTION already carries the binomial-JSDM
  framing and the Pollock et al. 2014 `<doi:10.1111/2041-210X.12180>`
  citation (the #434 landing).
- **`NEWS.md` has TWO top-level headings:**
  1. `# gllvmTMB (development version)` (line 1) — ~40 entries dated
     2026-05-19 .. 2026-06-03.
  2. `# gllvmTMB 0.2.0 (first CRAN release)` (line 354) — the curated
     release notes (API, families, inference, phylo/spatial, citation).
- **Consequence:** the recent capability landings ARE recorded, but
  under "(development version)", NOT folded into the `0.2.0` section:
  phylo/spatial non-Gaussian slope grid (#422/#424 phylo_dep,
  #427 spatial_indep, #429 spatial_dep, #341 latent/indep sweeps),
  kernel C0-C2 (#361: `make_cross_kernel`, `kernel_*()`,
  `extract_Gamma()`), missing-data layer (#332/#365 `mi()` / `imputed()`
  / `predict_missing()`), and the diagnostics surface (#228, #248).
  The JSDM citation is present in DESCRIPTION; the 0.2.0 NEWS section's
  "Inherited code and citation" block covers `inst/CITATION` but does not
  separately call out the Pollock JSDM anchor.
- **Gap (soft, editorial):** before tagging, the "(development version)"
  block must be merged/renamed into the `0.2.0` heading (or the dev
  block retitled to a post-0.2.0 line) so the tag's NEWS reads as one
  coherent release. This is a documentation reconciliation, not a code
  blocker.

## 2. Validation-debt register (`docs/design/35-validation-debt-register.md`)

The register's own Phase-0A tally (line ~388) is stale (102 rows: 40
covered / 48 partial / 0 opt-in / 14 blocked) — many rows have since
been walked to `covered` in the row notes. Current per-row reading of
the non-`covered` surface:

**`partial` (tests exist, shallower than advertised) — not v0.2.0 blockers:**
- FG-07 / FG-08 / FG-09 — bare `indep` / `dep` / `scalar` known-V
  variant Gaussian-only (non-Gaussian known-V is the hard phylo-dep item).
- FG-13 spatial family (smoke + dispatch); FG-14 / MET-01 single-V
  `meta_V` inference smoke-only (direct glmmTMB comparator still wanted).
- FG-16 / MIS-03 `gllvmTMB_wide()` soft-deprecated (intentional).
- FAM-15 truncated_*, FAM-16 censored_poisson (recovery/smoke).
- RE-03 s>=2 slopes: Gaussian `phylo_dep(1+x1+x2)` covered; non-Gaussian
  s>=2 reserved (deliberate).
- SPA-09 spatial_latent non-Gaussian slope: 4 families covered, 3
  honest-skip at default seed (power/seed artifact, documented).
- ANI-09 / ANI-10 multi-matrix + MCMCglmm/WOMBAT cross-pkg (v0.3.0).
- EXT-04 / EXT-10 / EXT-13 / CI-08 / CI-10 — profile/bootstrap on
  mixed-family + ordinal cutpoints + `coverage_study` >=94% gate
  (M3 production-grid evidence; coverage gate explicitly did NOT clear
  for 13/15 cells — this is the power-study capstone, gated to CRAN+paper
  #345, NOT v0.2.0).
- LAM-02 Gaussian lambda_constraint smoke; MIS-07 / MIS-09 predict /
  plot dispatcher partial.

**`blocked` (advertised-but-undefined/deferred) — all deliberately post-CRAN:**
- FAM-18 mixture families (gamma_mix/lognormal_mix/nbinom2_mix),
  FAM-19 gengamma — exported but not validated.
- MET-03 `meta_V(type="proportional")` — parser errors explicitly.
- MIX-10 / FAM-17 / EXT-11 delta-/hurdle-family latent-scale correlation
  — two-scales-undefined by design (safeguard error class).
- MIS-32 deferred missing-data extensions (multiple `mi()`, EM/REML, MI
  pooling, MNAR, etc.).

**Verdict on the register axis:** no `partial`/`blocked` row is a
*correctness* blocker for v0.2.0. They are honestly-marked deferrals
(power-study coverage, mixture/gengamma families, proportional meta-V,
delta-correlation, missing-data v2). The only register-side action for
v0.2.0 is the editorial close: refresh the stale headline tally and have
Shannon/Rose ratify the snapshot against merged code.

## 3. `R CMD check --as-cran` (3-OS via `full-check.yaml`)

- The 3-OS `--as-cran` gate is **`.github/workflows/full-check.yaml`**
  (matrix: ubuntu-latest / macos-latest / windows-latest, R release;
  `r-lib/actions/check-r-package@v2`, which runs `rcmdcheck` with the
  r-lib default `--as-cran` + `error_on = "warning"`). It runs nightly
  (06:00 UTC), on `workflow_dispatch`, and on every `v*` tag. It sets
  `GLLVMTMB_HEAVY_TESTS=1`, so the full ~190-fit heavy suite runs inside
  the check.
- **Latest run = #4, id `26880216038`, event `schedule`, 2026-06-03
  10:55Z, head `ebf4ff03`: conclusion FAILURE on ALL THREE OSes**
  (macOS job 79278016986, ubuntu 79278017048, windows 79278017054 — each
  failed at the `check-r-package` step).
- **The failure is a testthat TEST-SUITE failure, NOT a CRAN
  NOTE/WARNING.** Ubuntu job log: `[ FAIL 19 | WARN 5 | SKIP 54 |
  PASS 8162 ]`. The 19 failures are all **stale test expectations** from
  this week's non-Gaussian slope guard relaxations (#422/#424/#427/#429):
  e.g. `test-matrix-slope-spatial-unique.R` reading phylo_dep slots
  (`n_lhs_cols`/`sd_b`) instead of SPDE slots (`n_lhs_cols_spde`/
  `sd_spde_b`); and `test-{phylo,relmat,animal}-dep-slope-gaussian.R`
  fail-loud probes that expected `poisson()` to abort (now admitted).
  The engine is correct; no `--as-cran` NOTE/WARNING was the cause.
- **Fix already merged:** PR #438 ("test: restore nightly full-check to
  green — stale guard-relaxation fallout") merged 2026-06-03 14:34Z as
  sha `266f7a06` (current `main` HEAD). It is **test-only — no `R/` or
  `src/` change** — re-points the stale slot/probe assertions and adds
  `nightly-stale-test-fixups-gate.yaml` as a heavy paths-filtered gate.
- **OPEN VERIFICATION ITEM:** run #4 ran on the PRE-fix head; runs #2
  (06-01) and #3 (06-02) were green; there is **no full-check run yet on
  the fixed head `266f7a06`**. The standard fast `R-CMD-check.yaml`
  (no heavy gate) is green on main (run #943, 266f7a06, success), and
  pkgdown is green (run #413, 266f7a06, success) — but neither exercises
  the heavy suite. The `--as-cran` 3-OS gate must be re-confirmed green
  on `266f7a06` via a manual `workflow_dispatch` of `full-check.yaml`
  (don't wait for the 06:00 UTC nightly) before tagging.
- **NOTE/WARNING content:** not separately visible in the tail log
  (the run halted on test failures before/within check; the standard
  CRAN NOTEs were not surfaced). A clean `workflow_dispatch` run is the
  way to read any residual NOTEs.

## 4. pkgdown / docs surface (#230 / #347)

- **pkgdown builds + deploys green on main:** `pkgdown.yaml` run #413
  (id 26892539581, head `266f7a06`, `workflow_run` event, conclusion
  success). Recent pkgdown runs are uniformly green.
- **#230 (article surface reset)** is OPEN — the user-first tooling gate
  is essentially satisfied (extractors, plot helpers, diagnostics,
  `check_gllvmTMB`, missing-data all shipped), but the issue itself is
  not closed.
- **#347 (article completion)** is OPEN, milestone `v0.2.0`. `_pkgdown.yml`
  still hides an **`internal` article section** (draft / validation-
  dependent, hidden from the public dropdown): `functional-biogeography`,
  `lambda-constraint`, `lambda-constraint-suggest`, `psychometrics-irt`,
  `simulation-recovery-validated`, `simulation-verification`, `roadmap`.
  #347's Wave-1 promotion list (phylogenetic-gllvm, animal-model,
  random-regression-reaction-norms, behavioural-syndromes,
  profile-likelihood-ci, troubleshooting-profile) is already in the
  visible navbar. Remaining promotions are "one at a time, register-
  covered + Florence figure/prose audit" — a parallel lane, not a hard
  ship blocker.

## 5. Open release-blocker issues

- **#344** ([roadmap] Release v0.2.0 gate) — OPEN, milestone v0.2.0,
  labels roadmap/release. The umbrella; its four sub-items are this
  audit's four axes.
- **#345** ([roadmap] CRAN readiness + paper) — OPEN, labels
  roadmap/release/CRAN-ish, milestone "CRAN + paper". **Explicitly gated
  on the power-study capstone** — this is the NEXT milestone after
  v0.2.0, not a v0.2.0 blocker. The `coverage_study` >=94% gate (CI-08)
  and cross-package family checks live here.
- **#347** ([roadmap] Article completion) — OPEN, milestone v0.2.0
  (parallel lane).
- **#340** (capability board) — OPEN. Random slopes Gaussian COMPLETE;
  non-Gaussian slopes are tracked but the board still lists them as
  "deferred (engine guards to gaussian())" — STALE vs the register,
  which now shows them `covered` via #422/#424/#427/#429. Board needs a
  Rose refresh but flags no v0.2.0-specific hard blocker beyond #344.

---

## v0.2.0 readiness verdict

**DONE**
- Version field is `0.2.0`; DESCRIPTION carries the JSDM/Pollock framing.
- Recent capability landings are all recorded in NEWS (under the dev
  heading).
- pkgdown site builds + deploys green on main HEAD (`266f7a06`).
- Fast `R-CMD-check.yaml` green on main HEAD.
- The full-check RED was diagnosed as stale-test fallout and FIXED by
  the test-only PR #438 (merged, now HEAD).
- No register row is a correctness blocker; all non-covered rows are
  honest, deliberate deferrals.

**LEFT (ordered by blocking-ness)**

1. **[HARD BLOCKER] Re-confirm the 3-OS `--as-cran` gate green on the
   fixed head `266f7a06`.** Run #4 failed on the pre-fix head; no
   full-check has run since #438. Manually `workflow_dispatch`
   `full-check.yaml` on main and confirm 3/3 OSes pass and read any
   residual CRAN NOTEs/WARNINGs. (Cannot ship "as-cran clean" on an
   inference rather than a green run.)
2. **[HARD-ish] NEWS reconciliation.** Fold/rename the
   "(development version)" block into the `0.2.0` heading so the tagged
   release notes are one coherent section. Editorial, but a CRAN
   submission reviewer reads NEWS.
3. **[SOFT] Register + board close.** Refresh the stale headline tally
   in Doc 35 and the #340 board (non-Gaussian slopes now covered);
   Shannon/Rose ratify against merged code. Confirms #344's "register
   clean" sub-item.
4. **[SOFT / parallel] #347 article surface.** Visible navbar already
   has Wave-1; promote remaining `internal` drafts one-at-a-time with
   Florence audits. Can ship v0.2.0 with the current visible set + the
   draft set documented as held.

**Deferrable (ship with documented PARTIAL — NOT blockers):**
- power-study coverage gate (CI-08/CI-10) — owned by #345 (CRAN+paper).
- mixture families (FAM-18), gengamma (FAM-19), proportional meta-V
  (MET-03), delta-/hurdle-correlation (MIX-10), missing-data v2
  (MIS-32) — all honestly `blocked`/deferred.
- non-Gaussian s>=2 slopes (RE-03), spatial_latent 3 seed-skip families
  (SPA-09) — honestly `partial`/reserved.

## Recommended sequence to cut v0.2.0

1. `workflow_dispatch` `full-check.yaml` on `main@266f7a06`; confirm
   3/3 OS green and capture any `--as-cran` NOTEs. (Gate item.)
2. Reconcile NEWS (merge dev block into the 0.2.0 heading; ensure the
   JSDM citation + the recent capability landings read as 0.2.0 notes).
3. Rose/Shannon refresh + ratify Doc 35 register tally and the #340
   board; mark #344's "register clean" sub-item done.
4. Decide the #347 visible-article set for launch (current Wave-1 is
   sufficient); leave the rest documented as held drafts.
5. Tag `v0.2.0` (the tag itself triggers `full-check.yaml` again as the
   final release gate) and close #344.

**One sentence:** v0.2.0 is essentially shippable — the only true hard
blocker is re-running the 3-OS `--as-cran` gate on the post-#438 head to
turn the inferred-green into an observed-green, after which a NEWS
reconciliation and a register/board ratification close out #344.
