# After-task: v0.2.0 editorial tag-prep (DOCS ONLY)

**Date:** 2026-06-03
**Branch:** `claude/v0.2.0-tag-prep` (off `origin/main` @ `aace276`)
**Scope:** Editorial close-out for the v0.2.0 release tag. DOCS ONLY —
no `R/`, `src/`, `tests/`, or `DESCRIPTION` changes (version is
already `0.2.0`). Driven by
`docs/dev-log/after-task/2026-06-03-v0.2.0-readiness-audit.md`.

## What changed

### 1. NEWS.md reconciliation

The development-version block held 44 dated `##` entries
(2026-05-17..06-03) that were never folded into the
`# gllvmTMB 0.2.0 (first CRAN release)` section, so the release notes
did not read as one coherent first-CRAN record.

Approach chosen (coherence over a destructive fold):

- A thin `# gllvmTMB (development version)` stub stays at the top for
  post-release work.
- A curated `## 0.2.0 release summary` is inserted as the first
  subsection of the `0.2.0` section. It bullets the headline
  capabilities by theme with PR refs (random-effect slopes; generic
  dense kernels / coevolution; animal-model keywords; missing data;
  meta-analysis; diagnostics / extractors; inference + robustness).
- The existing curated 0.2.0 subsections (User-facing API, Inference,
  Phylogenetic and spatial paths, Inherited code and citation,
  Source-tree notes, Relationship to a pre-0.2.0 development line) are
  unchanged.
- All 44 dated entries are preserved **verbatim** (every entry's
  substance and PR refs intact), demoted `##` → `###`, and regrouped
  by theme under a new `## Capability landings during the 0.2.0
  development cycle` section beneath the curated subsections.

No capability was invented; this is reorganization only. Verified
that every one of the 44 original entry headings still appears (as
`###`) in the rewritten file, and the file has exactly two H1
headings.

### 2. Validation-debt register tally refresh (Doc 35)

The headline "honest scope statement" tally was stale (40 covered /
48 partial / 0 opt-in / 14 blocked over 102 rows — a Phase 0A
snapshot from 2026-05-16). Recounted directly from the per-row status
column:

- **Old (Phase 0A): 40 / 48 / 0 / 14 over 102 rows.**
- **New (v0.2.0): 166 covered / 20 partial / 0 opt-in / 7 blocked
  over 193 rows.**

The growth 102 → 193 rows is the KER/COE, augmented non-Gaussian
slope (PHY-11..18, SPA-08..10), `cluster2` (RE-11), missing-data
(MIS-21..32), plot/extractor (EXT-19..30), and diagnostic (DIA-08..13)
sections added after the snapshot; the non-Gaussian structured-slope
grid walked the phylo/spatial slope cells to `covered` across every
family (#388/#392/#422/#424/#427/#429).

The historical Phase 0A snapshot is preserved verbatim under a
"Historical snapshot" subhead; the new "Current tally" block sits at
the top of the scope statement, and a dated "Update — v0.2.0 tag-prep
recount (2026-06-03)" entry was added after the M1-close update.

The 20 partial / 7 blocked rows are all honest, deliberate deferrals
(known-V non-Gaussian variants, spatial-family depth, single-V
`meta_V` inference, soft-deprecated `gllvmTMB_wide()`,
truncated/censored recovery, non-Gaussian s ≥ 2 slopes, multi-matrix /
cross-package animal models, coverage-study gate, ordinal cutpoint
smoke, Gaussian `lambda_constraint` smoke, predict/plot dispatchers,
proportional meta-V, mixture/gengamma families, delta-correlation,
missing-data v2). **None is a v0.2.0 correctness blocker.**

**Rows left untouched (in-flight on other branches):** PHY-18, FAM-07,
and the `nbinom1` / `animal_dep` line land via **PR #441**; the kernel
C3 row (COE-03) lands via **PR #439**. The recount reflects `main` at
tag-prep; those PRs settle their own rows on merge.

### 3. Capability board #340 refresh (GitHub issue body)

Edited the live status board body (it self-describes as "refreshed at
each phase close"), keeping its structure:

- **Random slopes (non-Gaussian)** flipped ❌ deferred → ✅ COMPLETE
  with a phylo/spatial × mode matrix and PR refs
  (#388/#392/#422/#424/#427/#429); `nbinom1` noted as #441 in-flight,
  `animal_dep` confirmed.
- **Families** table refreshed: gamma / beta / ordinal walked to C;
  nbinom1 → C (#441); poisson / nbinom2 cross-pkg → C (glmmTMB); added
  rows for lognormal/student/tweedie/betabinomial (C), truncated/
  censored (P), and mixture/gengamma (B).
- **Tiers** cluster2 planned → C (#342/#355/#356, RE-11).
- Added **Generic kernels & coevolution**, refreshed **Missing-data**,
  and added an **Articles** line (3 published via #433: joint-sdm,
  cross-lineage-coevolution, missing-data; #347 drafts held).
- Added a "last refreshed" line with the new register tally.

## Checks

- DOCS ONLY — no package code, tests, or DESCRIPTION touched.
- NEWS.md: all 44 dated entries preserved verbatim (script-verified);
  exactly two H1 headings; PR refs (#341/#361/#422/#427/#429/etc.)
  intact.
- Register recount done programmatically from the status column
  (193 rows total) and cross-checked section-by-section.

## v0.2.0 blocker read

The only real hard blocker (per the readiness audit) is observational:
re-confirm the 3-OS `R CMD check --as-cran` gate green on the post-#438
head via a `full-check.yaml` `workflow_dispatch` (run #4 failed on the
pre-fix head; the failure was stale test expectations, fixed test-only
by #438). Everything else — NEWS, register, board — is editorial and
now closed. The git tag itself is the maintainer's action.

## Follow-up

- Maintainer to dispatch `full-check.yaml` on `main` and read residual
  CRAN NOTEs before tagging.
- PHY-18 / FAM-07 / ANI-12 (#441) and COE-03 (#439) settle their
  register rows on merge.
