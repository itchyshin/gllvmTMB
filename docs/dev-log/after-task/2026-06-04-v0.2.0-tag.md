# After-task — v0.2.0 release: engine close-out, article publish, tag prep (2026-06-04)

## Scope

Maintainer authorized (chat, 2026-06-04) merging the two open engine
PRs **into** v0.2.0, publishing the two flagship article drafts, and
tagging v0.2.0. This report records the close-out.

## Outcome

- **Engine into v0.2.0.** #439 (kernel C3 — two-kernel test + C3.2
  two-uniqueness identifiability guardrail) and #441 (nbinom1 augmented
  slopes + animal_dep recovery cell) were each updated onto current
  `main`, re-validated on their heavy recovery gates against the
  combined tree (both edit `R/fit-multi.R`, different regions — clean
  auto-merge, no conflict), and merged. #439's C3.1 two-independent-tier
  engine slot stays `reserved` (STOP), accepted by the maintainer.
- **Articles published.** #448 (non-Gaussian structured random-slope
  grid — new article) and #449 (cross-lineage coevolution deepened to
  methods-paper grade) merged. Per maintainer call, the rendered-HTML
  review (#230) was waived at merge because R is unavailable in the
  agent environment; the next pkgdown deploy is the execution gate for
  #448's build-time chunks.
- **NEWS / pkgdown (this PR).** `simulate_unit_trait()` folded from the
  post-0.2.0 block into the 0.2.0 release notes (the tag includes it);
  the nbinom1 grid-completion (#350) and the kernel C3.2 guardrail
  (#361) added to the headline 0.2.0 capability bullets; the new
  `random-slopes-nongaussian` article indexed in `_pkgdown.yml`
  (Model guide list + navbar).
- **Tag.** `v0.2.0` is cut at the post-merge `main` after a fresh 3-OS
  `--as-cran` full-check comes back green (the tag re-triggers
  full-check as the final release gate).

## Checks

- #439 heavy gate: green (6/6) on the combined tree.
- #441 heavy gate: green (8/8) on the combined tree; nbinom1 `phylo_dep`
  recovers non-skipped at `n_sp = 400`.
- Pre-merge `--as-cran` full-check on the simulate_unit_trait fix
  (4ee6b8a): green, 0 failed jobs across ubuntu/macOS/windows.
- Final full-check on the post-engine `main`: gating the tag.

## Follow-up

- **NEWS detailed-landings reconciliation.** The headline 0.2.0 summary
  is accurate, but a dated landing entry in the "Capability landings"
  section (the PHY-17 entry's PARTIAL note) still describes non-Gaussian
  `phylo_dep` slopes as PLANNED — stale after #422/#424 and now #441.
  Recommend a focused NEWS detailed-entry pass to retire the stale
  PARTIAL/PLANNED notes; deliberately out of scope here to avoid
  inaccurate edits to release prose under time pressure.
- **pkgdown render of #448.** Confirm the random-slopes-nongaussian
  build-time chunks render on the next deploy; fix any chunk fallout.
