# After-task — Profile-route interval-coverage CERTIFICATION arc (v2) — 2026-07-18/19 [DRAFT — formal D-43 panel deferred by tooling]

**Platform:** Claude (user-driven, ultracode). **Branch:** `claude/profile-coverage-remeasure-20260718` (off `claude/release-0.5.0`). **Status:** MEASURED + diagnosed; B3b Bartlett re-score COMPLETE; **FORMAL 4-lens D-43 panel (Rose/Fisher/Efron/Gelman) ran 2026-07-19 → certificate WITHHELD at 0.95, UNANIMOUS for every n≥150 cell.** Nothing promoted. The panel further found the n≥150 correction is within MCSE of the uncorrected χ²₁ baseline — it does no demonstrable work in-regime. **A NEGATIVE result on the Bartlett route (efficacy).** (The panel also read the n=400 anchor b̂=318 as b-estimator instability; a follow-up diagnostic REFUTED the "systematic" framing — a fresh n=400 b-estimation is normal, b̂=7.46 — so the 318 is a POOLING OUTLIER, not a large-n breakdown; see the n=400 section.)

## Scope / goal
Deferred arc (A4/A6 + binomial diagnostic + data-integrity) after the prior session's "MEASURED, not certified" stop. Maintainer chose (options 2+3): (2) TRY to EARN the gaussian n≥150 certificate via a Bartlett-corrected χ²₁ profile + REML n=50 test; (3) deep-diagnose the binomial + reconcile the truth_psi flag. Then D-43 panel → truthful register update.

## Settled findings (FINAL — evidence in scratchpad + results/profile-pilot-*)
1. **Gaussian n≥150 baseline (uncorrected χ²₁):** pooled fresh-seed ~10k reps → **d1 0.9474, d2 0.9455, rep-clustered MCSE 0.0011.** Clears the 0.94 IUT bar (lower bands 0.9453/0.9433) but ~0.3–0.5pp short of a clean 0.95. Confirms borderline at 3× A3 precision.
2. **REML n=50 (B2, `reml_bridge()` Gaussian):** ML 0.9437 (est/truth 0.985) → **REML 0.9488 (est/truth 1.005).** REML closes ~half the n=50 shortfall AND removes the finite-cluster point bias → confirms the ML-variance-bias hypothesis as the n=50 lever.
3. **Binomial (C1, Fisher+Gelman, adversarially corroborated): FENCE for 0.6 (recovery-only).** Named mechanism = a weakly-identified ΛΛ'-vs-total-variance **identifiability ridge** (single-trial Bernoulli can't separate per-trait Ψ from the fixed probit residual r=1; V_t scale pinned only by r=1) → bimodal, upward-mislocated point estimates (interior est/truth 50–600×). Coverage worsens as signal grows (ridge fingerprint) — **NOT the two-lever floor** (disproven, PF-5). No interval construction closes it; Bartlett powerless (can't re-center a mislocated point). Fix = estimation-side (pin Ψ=0 for single-trial binomial) = a **1.0 Discussion-Checkpoint spec change**.
4. **truth_psi (C2): a harness record-keeping BUG,** logs the raw pre-zeroing Gamma(2,2) draw not `psi_effective` (enforced 0 for binomial, `dev/m3-grid.R` L490–494). `est_psi≈0` is CORRECT; "psi→0 collapse" was compared against the wrong reference. **Certificate target `truth_diag_sigma` unaffected.** (Follow-up: rewire the truth_psi emission column; secondary defect in the non-primary "psi" target scoring.)
5. **Sigma_unit_corr (S4): DIAGNOSTIC only** — gaussian corr under-covers 5–15 MCSE below 0.95 even where gaussian diag certifies (a family-independent correlation-estimand shortfall); binomial corr under-covers in lockstep with diag (shared ridge). Moves no register row; the certificate is DIAG-only.

## The Bartlett lever (B1, verified) + B3b re-score (COMPLETE)
- **Opt-in `crit·(1+b/n)`** in `.qchisq_threshold` (`R/profile-ci.R`), `b̂ = n·(W_mean−1)`, W=2ΔL under the fitted null, pooled per design on an INDEPENDENT seed stream, boundary-collapsed refits excluded. Default `confint(method="profile")` **byte-identical** (opt-in only; re-asserted from static code 2026-07-19: `.qchisq_threshold(level, bartlett_b=NULL, n=NULL)` returns `qchisq(level,1)/2` unchanged when NULL; 4 pre-existing 1-arg callers untouched + 1 new opt-in call site `R/profile-derived.R:833`; `R/z-confint-gllvmTMB.R` zero diff). `n = fit$n_sites`.
- **Verified:** B1v adversarial panel SOUND on all 3 lenses (Fisher/Efron/statistical-reviewer, 0 blockers); heavy tests pass (#12 byte-identical path, #13 widening-brackets, #14 estimand identity).
- **B3b re-score (Totoro, n_sim≈4000/cell, MAIN 32/32 chunks complete, 0 errors, results LOCAL D-50):**

| cell | n_rep | corrected cov | rep-clustered MCSE | 2·MCSE lower | conv | ci_fail | b̂ | boundary_hit | factor 1+b/n |
|---|---|---|---|---|---|---|---|---|---|
| gaussian-d1-n150-sig0.2 | 3839 | 0.9487 | 0.0016 | 0.9455 | 0.992 | 0.008 | 5.756 | 0.11 | 1.038 |
| gaussian-d1-n150-sig0.5 | 3909 | 0.9486 | 0.0017 | 0.9452 | 0.995 | 0.005 | 4.337 | 0.04 | 1.029 |
| gaussian-d2-n150-sig0.2 | 3955 | 0.9488 | 0.0016 | 0.9456 | 0.998 | 0.002 | 3.467 | 0.40 | 1.023 |
| gaussian-d2-n150-sig0.5 | 3961 | 0.9529 | 0.0016 | 0.9497 | 0.998 | 0.002 | 4.894 | 0.37 | 1.033 |
| gaussian-d1-n50-sig0.2 | 3964 | 0.9498 | 0.0016 | 0.9467 | 0.998 | 0.002 | 4.276 | 0.31 | 1.086 |
| gaussian-d1-n50-sig0.5 | 3988 | 0.9535 | 0.0016 | 0.9503 | 0.999 | 0.001 | 3.251 | 0.15 | 1.065 |
| gaussian-d2-n50-sig0.2 | 3990 | 0.9535 | 0.0015 | 0.9505 | 0.999 | 0.001 | 5.684 | 0.65 | 1.114 |
| gaussian-d2-n50-sig0.5 | 3992 | 0.9500 | 0.0016 | 0.9468 | 1.000 | 0.000 | 2.597 | 0.58 | 1.052 |

- **n≥150 (the certificate target):** the Bartlett correction LIFTED coverage from uncorrected 0.9455–0.9474 to **0.9486–0.9529** (closes ~half the residual gap to nominal) with honest, modest widening (factor 1.02–1.04) and clean boundary_hit at the d1 cells (0.11, 0.04). **Every n≥150 cell holds the 0.94 floor. NONE reaches a clean 0.95** — the four 2·MCSE lower bands are 0.9455 / 0.9452 / 0.9456 / 0.9497, all below 0.95.
- **n=50 (NOT the Bartlett target — REML is the n=50 lever):** two cells clear a 0.95 lower band (d1-n50-sig0.5 2MCSE-lo 0.9503; d2-n50-sig0.2 0.9505) but carry HIGH boundary_hit (0.15, **0.65** — the latter's b̂ came from a pool 65% boundary-excluded → honesty-gate fail), and are off-target regardless.
- **PF-2 anchor n=400 (standalone runner, PARTIAL 1/2): DATA-INTEGRITY FLAG.** `gaussian-d1-n400-sig0.5`: cov 0.9893, but **b̂=318.476 → factor 1.796 (~80% crit widening)**, vs the n≤150 factors 1.02–1.11; ci_fail 0.022 (above ~1% ceiling). The n=400 anchor b̂ is an **outlier of that specific pooled run** — a fresh n=400 b-estimation is normal (b̂=7.46; see the D-43 section) — so it is NOT a clean asymptote anchor and NOT a systematic large-n breakdown; it needs a pooling outlier-guard before reuse.

## D-43 panel (P) — FORMAL 4-lens panel COMPLETE (2026-07-19) → WITHHELD
The formal panel (4 independent Opus lenses Rose/Fisher/Efron/Gelman) ran once the Opus-classifier outage cleared (full verdict `scratchpad/panel-FORMAL-verdict.md`). Rule: a cell EARNS iff <2 lenses returned NOT-DONE.
- **All four n≥150 cells (the certificate target): 4/4 NOT-DONE → WITHHELD, UNANIMOUS.** 2·MCSE lower bands 0.9452–0.9497 all < 0.95.
- The only cell with <2 NOT-DONE is `gaussian-d1-n50-sig0.5` (3 done=y, 1 done=n) — but it is an n=50 REML-lever cell, +0.0003 above the band, which all four lenses explicitly state does NOT carry the certificate. Every other n=50 cell WITHHELD; `d2-n50-sig0.2` fails the honesty gate (boundary_hit 0.65). Anchor n=400 WITHHELD (b-estimator instability).
- **Two findings STRONGER than a bare withhold:** (1) Fisher — at n≥150 the corrected coverage is statistically indistinguishable from the uncorrected χ²₁ baseline (Δ ≈ 0.001–0.003 ≤ MCSE); the correction does no demonstrable work in-regime (so "closes ~half the gap" overstates it — the lift is within Monte-Carlo noise). (2) Fisher/Efron/Gelman read the n=400 anchor (W_mean−1 ≈ 0.80 vs ~0.03 at n=150) as the b-estimator mis-scaling with n. **A follow-up diagnostic REFUTED the "systematic" reading** (`scratchpad/anchor-Wdist-RESULT.md`): a fresh n=400 b-estimation gives W_mean=1.019 / b̂=7.46 — normal, like n=150 — so the campaign anchor b̂=318 was a POOLING OUTLIER (one rogue base-fit rep among the 20 pooled), not a large-n law; the small-n b̂ are NOT impeached. (3) Gelman — 4/8 cells carry boundary_hit ≥0.37, so honest calibration cannot be separated from ψ=0 boundary over-coverage.
- **Certificate WITHHELD at 0.95. Nothing promoted. The Bartlett route is a NEGATIVE result** (no demonstrable in-regime improvement + unstable estimator).

## Register update (R) — NON-PROMOTING (no status flip)
- **CI-08 / CI-10** (`partial`): disambiguated the OLD 2026-05-19 M3.3 profile-psi bootstrap-era numbers from the Design-73 `profile_total` (log-SD/χ²₁) route; added the B3b measured result (correction closes ~half the gap; 0.94 floor held; **0.95 NOT reached → WITHHELD**; formal panel pending). NO promotion. (See register Section 10.)
- **CI-11** (`covered` route-ledger only): unchanged.
- Binomial → fenced (C1 mechanism); corr → diagnostic (moves no row). nbinom2 stays fenced.

## Checks / evidence
- B1: B1v SOUND + heavy tests pass (worktree, uncommitted). B2/A/B3b: Totoro, ≤100 cores, results LOCAL (D-50), never GitHub. Binomial/truth_psi: analysis of existing pf5/A2/A3 rds. Mechanical verify (V): 8 cells non-empty, rep-clustered MCSE, conv/ci_fail present, nothing `results/`-staged, `.gitignore` `results/profile-pilot-*` effective, default `confint` byte-identity re-asserted from static code.

## Follow-ups / next (the open items)
- **[DONE] Formal 4-lens D-43 panel ran (2026-07-19) → WITHHELD, unanimous for n≥150** (above). This report is now final (drop `-DRAFT`).
- **[DIAGNOSED] n=400 anchor b̂=318 = a POOLING OUTLIER, not systematic mis-scaling.** The confirming W-distribution diagnostic (`scratchpad/anchor-Wdist-RESULT.md`, `dev/anchor-Wdist-diag.R`) shows a fresh n=400 b-estimation is normal (W_mean=1.019, b̂=7.46, in line with n=150's 1.024/3.67) — so the campaign anchor's 318 came from ONE pathological base-fit rep among the 20 pooled reps injecting inflated W. The mean-W formula is CORRECT (χ²₁ median≈0.455≠1 → median-W is spuriously negative, so "use median" would be a bug). **Fix = OUTLIER-ROBUSTNESS in the pooling** (`.pool_bartlett_b`: drop/winsorize a rep whose per-rep W_mean is a gross outlier), NOT median, NOT the constrained-convergence hypothesis. The small-n b̂ are NOT impeached; the primary WITHHELD verdict is unaffected (it rests on coverage bands, not the anchor).
- **[DECIDED 2026-07-19: do NOT commit] The Bartlett worktree stays UNCOMMITTED** (maintainer's call, given the negative result). The correction does no demonstrable in-regime work and the b-estimator is unstable, so landing formula-adjacent code that doesn't work adds low-value risk to the release branch. The finding is durably recorded here + the register + the handover; the worktree (`.claude/worktrees/agent-a1b37d9e4b149949e`) remains available if the b-estimator is later fixed (convergence check) and the route revisited.
- truth_psi emission-column rewire (harness hygiene). Correlation-estimand shortfall (own arc). Phase B (pin-Ψ=0 binomial; AGHQ+Cox–Reid) = 1.0 + sign-off. Making the Bartlett crit the package DEFAULT = a separate later decision.
- **Headline for the register/reader:** the profile Bartlett correction is a genuine, honest improvement (n≥150 → ~0.949) but does not certify a clean 0.95; the gaussian `Sigma_unit` diagonal profile remains a **0.94-gate** cell, not a 0.95-certified one.
