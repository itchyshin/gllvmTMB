# After-task — M1 unblock arc: verified state, Amendment 3, A12, and three closeout slices

**Date** 2026-07-22 · **Platform** Claude Code (runtime, per D-61 — the `codex/…` branch name is
leftover naming, not an owner) · **Branch** `claude/0.6-m1-close-20260722` @ `e367f57f`

## 1. Goal

Resume the gllvmTMB 0.6 M1 lane, establish what was actually true rather than inherited, and clear
every piece of agent work standing between the arc and M1's closing claim — leaving only decisions
that are genuinely the maintainer's.

## 2. Implemented

- **Re-derived the git ground truth.** Three documents claimed three different heads (`d74a6a08`,
  `21e04eb5`, `eea9761c`). Actual head was `d74a6a08`; the shipped-path diff against certified SHA
  `21e04eb5` was **empty**, so the certification transfers and nothing needed re-earning.
- **Rewrote `LOOP/checkpoint.md`** (`8f10003a`) — it claimed the wrong head and *five* panels when
  six had run. Rewritten, not patched.
- **Amendment 3 + Design 86 lane brief** (`142ff39f`): a design-only EVA feasibility lane,
  fenced to `docs/design/86-*.md`, in a worktree outside Dropbox, explicitly **not** gating 0.6.
- **Opened and merged [#779](https://github.com/itchyshin/gllvmTMB/pull/779)** (`509d5792`) after
  finding `142ff39f` had missed #778's merge — `main` had zero Amendment 3 and no brief.
- **A12** (vault `ceb2ab7`, `de1a764`): five stale "EVA is in 0.6" surfaces superseded.
- **R-11 wording dossier** (`fe10048a`), **C4 plan-vs-actual** (`4b1681dd`), **A-iss triage**
  (`e367f57f`).

## 3a. Decisions and Rejected Alternatives

- **Rewrote the checkpoint rather than patching two lines.** A surgical fix would have left "five
  panels" elsewhere in the file — the instance-not-class failure this arc has paid for twelve times.
- **Did not convene a seventh D-43 panel.** Brain **D-74** states D-43 fires *once per milestone
  claim* and records repeat panels as **drift**; its evidence is this lane's own telemetry. D-43's
  remedy is *"withheld until the uncovered cells are named explicitly"*, not "until a panel passes".
  **Recorded as needing the maintainer's confirmation, not acted on** — it is an agent reading of a
  decision, and this lane's agent readings have been wrong before.
- **Superseded the stale brain notes with dated banners; did not delete them.** Delta, not summary.
  One carries a paste-ready prompt that would relaunch the cut arc, so the banner sits *above* the
  content rather than in a footer.
- **Did not edit `Brain-Growth-Report.md:126`** despite it matching the EVA-in-0.6 grep. It is a
  dated historical log row — accurate as a record — and is regenerated by
  `tools/brain-growth-report.py`. Rejected editing it as an over-reach.
- **Merged #778/#779 as merge commits, not squashes**, preserving the arc history that this
  project's audit culture depends on; **declined to delete the branch** — it is the evidence anchor
  the receipts and register reference by name.
- **Committed only this session's files in the vault.** Rejected a blanket commit: the tree carried
  another writer's work (D-60).

## 4. Files Touched

**Repo (`claude/0.6-m1-close-20260722`, all documentation):**

- `LOOP/checkpoint.md` — rewritten, then amended for Amendment 3
- `LOOP/GOAL.md` — Amendment 3 appended (text above untouched)
- `docs/dev-log/2026-07-22-design86-lane-brief.md` — new
- `docs/dev-log/2026-07-22-r11-wording-review-dossier.md` — new
- `docs/dev-log/plan-actual/2026-07-22-m1-plan-vs-actual.md` — new
- `docs/dev-log/2026-07-22-a-iss-open-issue-triage.md` — new
- `docs/dev-log/after-task/2026-07-22-m1-unblock-arc.md` — new (this file)

**Vault (`~/shinichi-brain`, commits `ceb2ab7`, `de1a764`):**

- `docs/release/2026-07-20-gllvmTMB-EVA-codex-arcloop.md`, `…-drm-gllvm-crown06-finishing-plan.md`,
  `projects/gllvmTMB.md`, `memory/LA vs VA-EVA — …md`, `memory/PROJECT-NOTEBOOKS.md`

**No source file touched** — no `R/`, `src/`, `tests/`, `man/`, `vignettes/`, `tools/`,
`DESCRIPTION`, or `NAMESPACE`.

## 5. Checks Run

| Check | Result |
|---|---|
| `git diff --name-only 21e04eb5 HEAD -- R/ src/ tests/ man/ vignettes/ tools/ DESCRIPTION NAMESPACE` | **empty** — re-run after every commit; certification held for every commit in this arc |

> **⚠ CORRECTION (same day, after the above ran).** That path list — the canonical one used
> throughout this arc — is **incomplete**. It omits **`NEWS.md`, `README.md` and `inst/`**, all three
> of which **ship** (absent from `.Rbuildignore`). A NEWS-only or README-only edit would have
> forfeited the certification while this command reported "empty" — **a false PASS from the very
> check written to prevent one.** Found by enumerating the top-level tree against `.Rbuildignore`
> instead of trusting the list. It does **not** invalidate the results above: every commit in this
> arc was documentation-only under `docs/` and `LOOP/`, both `.Rbuildignore`d, so the verdict was
> right even though the instrument was blunt. Corrected list now in `LOOP/checkpoint.md` §0:
> `R/ src/ tests/ man/ vignettes/ tools/ inst/ DESCRIPTION NAMESPACE NEWS.md README.md`.
| `git status --porcelain` | clean at each commit |
| `git rev-list --left-right --count origin/…...HEAD` | `0 0` after each push |
| #779 CI, `gh pr checks 779` | `ubuntu-latest (release)  pass  18m38s` |
| Post-merge verification | `AMENDMENT 3` count in `main` = **1** (was 0); brief **PRESENT**; branch **alive** |
| `.Rbuildignore` inspection | `:23 ^LOOP$` (A0 confirmed), `:18 ^docs$`, `:29 ^vignettes/articles$` |

**Not run, and why:** `devtools::test()`, `R CMD check`, `document()`, `pkgdown::check_pkgdown()`.
This arc touched **no source file**, so the certified evidence at `21e04eb5` transfers unchanged;
re-running would have proved nothing new and re-minted nothing. Stated rather than silently skipped.

## 6. Tests of the Tests

The load-bearing check here is the certification-transfer diff, and it was **negatively controlled
by construction**: the same command returns *non-empty* whenever a shipped path changes — which is
exactly how the arc's five previous evidence chains were detected as forfeited (`git diff --stat
95f7d06a 21e04eb5` showed six source files). An always-empty check would be worthless; this one has
demonstrably fired.

A second control: my first correction-marker sweep **produced false negatives** — the filename with
spaces word-split, printing nine bogus `UNMARKED` lines. Caught by inspection and re-run
null-delimited. **The buggy check was not accepted as a result.**

## 7a. Issue Ledger

- **#750** — body still reads `Target release: **0.6**` while register **R-5** records the maintainer
  retargeting it to **0.7**. Register and public tracker disagree. **Deferred to the maintainer**
  (one-line edit).
- **#345** — CRAN umbrella gates on the power-study capstone (#349/#346), which is **not** in the
  M1→M3→M4→M5 sequence. Scope question. **Deferred to the maintainer.**
- **#778 / #779** — both merged; #779 opened by this arc to repair #778's missed commit.
- Six issues on the stale `v0.2.0` milestone. **Deferred.**
- **No issue blocks M1.**

## 8. Consistency Audit

The A12 sweep is the neighbourhood walk. The first grep (`"EVA is in 0.6"`) found **one** file; the
real class — anything placing EVA in 0.6, naming Codex as gllvmTMB's owner, or citing the void
~2026-08-03 target — had **five**. All five corrected.

Sweeping the amendment pointers likewise: adding Amendment 3 while leaving every "BOTH amendments"
reference would have manufactured a fresh stale reference, so those were swept too.

In the R-11 dossier, the audit ran the other way and **prevented** two false findings.
`R/fit-multi.R:151-155` and `R/julia-bridge.R:245` both look like the R-10/R-11 defect class —
strings pointing at a register that `.Rbuildignore:18` excludes from the tarball. Tracing each to
its consumer showed neither reaches a user (`.augmented_slope_family_allowed()` never reads the
`evidence` column; the roxygen block carries `@keywords internal` + `@noRd`). **A string's text does
not tell you whether it reaches a user.**

## 9. What Did Not Go Smoothly

- **The auto-mode classifier failed intermittently for most of the session** — roughly two calls in
  three refused, blocking `Bash`, `Workflow`, `Agent`, `Skill`, and every MCP tool. Three Workflow
  attempts and two subagent dispatches were abandoned; the work was done directly instead.
- **I concluded the brain was unreachable after the MCP layer failed, without trying the documented
  CLI fallback.** `AGENTS.md` names it explicitly. Corrected by the maintainer. This is
  inference-from-insufficient-evidence — the exact failure this lane exists to catch — and it is the
  most instructive mistake of the session.
- **`142ff39f` missed #778's merge window**, so `main` briefly carried an authorised lane brief
  that did not exist in `main`. Found by verifying after the merge rather than trusting it.
- **`ceb2ab7`'s commit message contained a false sentence** — it claimed `PROJECT-NOTEBOOKS.md` had
  already been swept into the automation's commit. Corrected in `de1a764` rather than amended,
  because rewriting landed history to hide a wrong sentence is worse than carrying the fix forward.
- The user's first attempt at the transfer check failed with *"Not a git repository"* — run from
  `~`. An operator error, not a result; the `cd` is now written into the checkpoint.

## 10. Known Residuals

- **M1 is still WITHHELD**, on three maintainer decisions: the one R-11 string
  (`R/julia-bridge.R:1671`), the `NEWS.md` boundary statement, and whether R-7's sign-off survives
  its retired causation evidence.
- **B2 is `CANNOT VERIFY`**; **C5** is not done.
- **D-41's mandatory experimental warning is unverified** — pkgdown callout, README badge,
  `lifecycle` badges, `.onAttach`, DESCRIPTION `Description`. gllvmTMB is **not** exempt. A release
  blocker if absent.
- The **D-74 reading is `AGENT-INFERRED`** and awaits the maintainer's confirmation.
- The honest release rung is **NOT READY, below `source-clean`** (D-49/D-66) — the gap is evidence,
  not capability.

## 11. Team Learning

**When one transport fails, try the documented second before concluding absence.** The MCP layer
being down is not the brain being unreachable; `AGENTS.md` names a CLI fallback and I did not use
it. Generalises: *a negative from one probe is not proof of absence* — the same rule the cross-repo
guards already state for capability checks.

**Trace the string to its consumer before calling it a defect.** Two perfect-looking matches for a
known defect class were both internal. Grep finds candidates; only the call graph finds defects.

**A commit message is a claim and can be false.** Two were, this session and previously. Correct
forward, never by amending landed history.

## 12. Cross-Product Coverage

The cross-cutting object this arc touched is **the certified evidence chain at `21e04eb5`** — a
transformation that silently qualifies every downstream release surface.

**Covers ✓** — the non-heavy suite (`0/779/7290`), durable `--as-cran` (`0/0/0`), the CRAN-config
check (`0/0/1`, `New submission`), Ubuntu CI, the three-OS matrix (ubuntu + macOS + Windows,
asserted by job name), the heavy run (`FAIL 0`), and `check-reader-surface.sh`. Each re-confirmed to
transfer to every commit this arc made, by empty shipped-path diff.

**Does NOT cover ✗:**

- **M3's version bump.** `0.5.0 → 0.6.0` is a source edit that **invalidates every receipt above**;
  M5 must price a second exact-tag three-OS cycle. M1's CI qualifies a *pre-bump* identity by design.
- **Interval calibration.** No cell is empirical-coverage-calibrated (`design/75:96-99`); CI-08's
  gate FAILED, 13/15 cells below 94%, only Gaussian d=1/d=3 cleared. Green checks say nothing here.
- **Prose truth.** No check establishes whether a user-facing string is honest — the whole reason
  M1 is withheld.
- **The claim-string class outside the five R-11 files** — `man/`, the shipped vignette, `NEWS.md`,
  `README.md`, `DESCRIPTION`. Not swept. An M4 item; **do not assume it is done.**
- **The heavy warning set.** `WARN n` is not a function of the code — six runs gave 8, 9 and 10.
  Only `FAIL` is a regression signal.
- **`pkgdown` site building.** `check_pkgdown()` validates configuration and does not build.
- **The Design 86 lane.** Fenced, design-only, and outside this arc entirely.

## Memory receipt

Loaded and used: hub `AGENTS.md` (sequential-tool rule, compute doctrine, memory reflex);
`LOOP/GOAL.md` all three amendments; `LOOP/checkpoint.md`; `known-residuals-register.md`;
`LOOP/arcs.md` + `ultra-plan.md`; `docs/design/75`. From the vault `memory/DECISIONS.md`, read
directly when the MCP failed: **D-74** (shaped the do-not-run-a-seventh-panel call, the single most
decision-relevant retrieval), **D-43** and **D-46** (the remedy clause and the designed panel
shape), **D-41** (surfaced the experimental-warning blocker nobody had listed), **D-66/D-49** (the
release rung), **D-61** (platform from runtime), **D-60** (dirty tree = prior session, never
blanket-commit), **D-69/D-77** (worktrees outside Dropbox), **D-42** (corroborated CI-08),
**D-37** (vault local commit is landed state).

**Golden Set:** not run — `tools/memory_regression.py` lives in the vault and the classifier blocked
most tool calls; the in-scope known-mistake classes (`concurrent-mislabel`,
`cran-readiness-partial-green`) were checked **manually** instead: the dirty vault tree was
attributed to the daily automation rather than a concurrent editor, and no readiness claim was made
at any rung. Stated as a gap rather than implied as done.

**Recall before scouting:** yes for the brain (`DECISIONS.md` read directly) — and the corrected
failure was concluding absence from one failed transport. The cross-team note this arc leaves is
this report plus the plan-vs-actual reconciliation.

> Related: `docs/dev-log/plan-actual/2026-07-22-m1-plan-vs-actual.md` ·
> `docs/dev-log/2026-07-22-r11-wording-review-dossier.md` ·
> `docs/dev-log/2026-07-22-a-iss-open-issue-triage.md` · `LOOP/checkpoint.md` · `LOOP/GOAL.md`
