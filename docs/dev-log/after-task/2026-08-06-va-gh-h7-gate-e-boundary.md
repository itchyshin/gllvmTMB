# After Task: VA(GH) H = 7 Gate E context-boundary preservation

**Verdict:** PARTIAL / PRESERVED. Gate E remains NOT PASS.

## 1. Goal

Collect the completed independent Arc 1 reviews, reconcile them with the live worktree, preserve
the minimum in-progress Gate E repairs, and stop at the D-76 context boundary without weakening
Gate E or launching public promotion, media work, research, campaign work, Totoro, DRAC, or Arc 2.

## 2. Implemented

The preserved repair attempt removes likelihood-changing clamps from four count/Tweedie branches,
uses stable log-probabilities for Beta-family shapes, carries fixed Tweedie power and Student
degrees of freedom into the mapped R3 objective, and makes public VA-Wald covariance profile all
fitted global nuisance parameters. The VA-Wald fix has a failure-discriminating Hessian test.

Nothing in this slice promotes H = 7 or expands the public fence. Seven scalar cells retain NOT
PASS verdicts until dedicated tests and a fresh independent review prove these attempted repairs.

## 3. Files Changed

Engine and routing: `R/approximation-engine.R`, `R/va-r3-proto.R`, `R/va-routing.R`,
`R/integration-fence.R`, and `inst/tmb/gllvmTMB_va_r3.cpp`.

Inference and tests: `R/va-intervals.R` and `tests/testthat/test-va-intervals.R`.

Durable records: this report,
`docs/dev-log/recovery-checkpoints/2026-08-06-073617-codex-gate-e-checkpoint.md`, and
`docs/dev-log/check-log.md`.

No README, NEWS, roxygen/Rd, vignette, pkgdown, roadmap, validation-register, campaign-scaffold, or
public-default file was changed.

## 3a. Decisions and Rejected Alternatives

> **Decision**: Preserve the seven-cell repair attempt but keep Gate E at NOT PASS.
> **Rationale**: Independent review found model-changing clamps and dropped fixed metadata, while
> the repair paths still lack dedicated regression tests.
> **Rejected alternative**: Treat successful compilation and mild-domain green tests as Gate E.
> **Confidence**: high.

> **Decision**: Stop before public promotion and Arc 2.
> **Rationale**: The D-76 context boundary was reached and the user explicitly required a clean
> fresh-task handoff without weakening Gate E.
> **Rejected alternative**: Continue widening into campaign repair or compute.
> **Confidence**: high.

## 4. Files Touched

Section 3 enumerates every touched path. The convention-change cascade is not triggered because
no public syntax, default, example, documentation, or capability claim changed. The implementation
files, one regression-test file, and the three preservation records are the complete scope.

## 5. Checks Run

Completed reviewers reported 180/180 ordination-routing-probit checks, 160/160 all-family
oracle/compiled checks, 174/174 all-family light-fit checks, and 804/804 on the likelihood
reviewer's combined target. The current repair diff parsed in all six changed R/test files and the
amended C++ template compiled twice during targeted `va-intervals`/`va-r3-prototype` runs; those
runs completed without emitted failures, but their capture omitted final counters. The earlier
post-VA-Wald `va-intervals` run was 109 pass, 0 fail/warn/skip.

`git log --all --oneline --since="6 hours ago"` found no foreign local commit. `gh pr list --state
open` failed because GitHub was unreachable. `ps` inspection was sandbox-denied. No remote compute
was launched.

## 6. Tests of the Tests

The new VA-Wald test satisfies failure-before-fix: its positive-definite Hessian correlates beta
with a family nuisance parameter, proves the old beta/theta-only Schur result differs, and checks
the public covariance against the full inverse. The seven likelihood/metadata repairs have no new
failure-before-fix tests yet; this is the principal reason Gate E remains NOT PASS.

## 7. Roadmap Tick

N/A. Gate E is still NOT PASS; no progress chip or public capability status was changed.

## 7a. Issue Ledger

No issue was created or changed. The remote issue/PR ledger could not be refreshed because GitHub
was unreachable; this is recorded as incomplete rather than inferred from local state.

## 8. Consistency Audit

`git diff -- R/va-routing.R R/approximation-engine.R R/va-r3-proto.R R/va-intervals.R
R/integration-fence.R inst/tmb/gllvmTMB_va_r3.cpp tests/testthat/test-va-intervals.R` confirmed the
diff is limited to Gate E repairs, VA-Wald profiling, and one stale-comment correction.

`git status --short --branch` and `git diff --stat` established the exact seven modified files
before the durable records were added. No public H/JJ/fence change appears in the diff.

## 9. What Did Not Go Smoothly

The original tests were too mild to expose the clamps. Independent extreme-tail probes then found
large discrepancies in the affected cells. Tweedie's R oracle also underflows in an extreme tail,
so its next regression test needs an honest finite-domain independent oracle or a clearly scoped
secondary comparator. GitHub state could not be refreshed, and sandbox policy prevented `ps`.

## 10. Known Residuals

Gate E remains 11 PASS / seven NOT PASS at the last independent review. The current seven-cell
repairs compile but remain unproved. Full tests, docs, pkgdown, `R CMD check`, cross-OS CI,
campaign repair, and Totoro/DRAC evidence were deliberately not run.

The single next action is a fresh Sol/high statistical-parent task: write adversarial tail and
fixed-metadata regressions for all seven attempted repairs, rerun the independent 18-cell review,
and record Gate E. Use Terra/medium or Luna/low only for bounded support tasks with self-contained
briefs. Public promotion and Arc 2 remain forbidden unless Gate E passes.

## 11. Team Learning

Gauss/Noether's independent likelihood pass established that 11 green cells do not license a
pooled all-family verdict: seven cells failed for concrete density or metadata reasons. Curie's
test pass showed broad light-fit health while also identifying missing adversarial-tail and
public-route coverage. Rose's fence audit kept the public H = 61/JJ/three-family hold and Arc 2
freeze visible. The parent review found the additional VA-Wald conditioning defect and supplied a
failure-discriminating test.

## 12. Cross-Product Coverage

This boundary covers all 18 scalar cell verdicts at the preserved commit and the attempted repair
paths for the seven failures. It does not cover multinomial, dedicated tests for those repairs,
public formula routing across every cell, all covariance providers, missing/structured combinations,
calibrated uncertainty, public promotion, package-level checks, or campaign evidence. Green light-fit
evidence must not be interpreted as evidence over those omitted axes.
