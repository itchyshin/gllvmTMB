# After Task: G2h 360-cell local diagnostic smoke

## 1. Goal

Run exactly one private local smoke on the frozen G2h 360-cell fixture, retaining the fit, three restarts, six `theta_diag_B` profiles, decision ledger, manifest, and failure-state evidence.

## 2. Implemented

The committed private wrapper `bdc3da6a` passed no-fit validation and ran once at result root `dev/isdm-package-recovery/results/g2h-smoke-20260811-001`. It retained the exact fit, all three restart rows, six five-offset profiles, root receipt, truth, decision ledger, and core-artifact manifest. The result is scientifically `GEOMETRY_RESPONSIVE` but operationally `G2H_SMOKE_HOLD` because the maximum gradient was `0.001290534`, exceeding the frozen `0.001` completion threshold.

## 3a. Decisions and Rejected Alternatives

No public API, likelihood, formula grammar, family, NAMESPACE, generated Rd, vignette, pkgdown navigation, or model parameterisation changed. The frozen model remains the six-species nonspatial relative-intensity iJSDM with GBIF Poisson observations and three conditionally independent PA-cloglog visits sharing one ecological state, rank-one `Lambda`, free diagonal `Psi`, and a GBIF-only bias gate. G2h changes only the cell geometry to 360 independent cells.

The frozen completion threshold is binding: a geometry-responsive scientific classification does not override `G2H_SMOKE_HOLD`. A retry, campaign, Totoro/DRAC run, public/package work, or a post-hoc threshold relaxation was rejected. The next possible work is a separately approved read-only diagnosis, not a rerun.

## 4. Files Touched

- `dev/isdm-package-recovery/run-g2h-360cell-smoke.R` (committed smoke wrapper, `bdc3da6a`)
- `dev/isdm-package-recovery/results/g2h-smoke-20260811-001/` (private retained output; not a public/package surface)
- `docs/dev-log/after-task/2026-08-11-g2h-360cell-smoke.md`
- `docs/dev-log/plan-actual/2026-08-11-g2h-360cell-smoke-reconciliation.md`
- `docs/dev-log/check-log.md`

No examples, user-facing documentation, generated files, `README.md`, `NEWS.md`, `ROADMAP.md`, `_pkgdown.yml`, or Issue #953 changed.

## 5. Checks Run

```sh
TMPDIR=/private/tmp/gllvmtmb-isdm-g2h-360cell-prep/.tmp \
  Rscript --vanilla dev/isdm-package-recovery/run-g2h-360cell-smoke.R \
  --mode=validate --output=dev/isdm-package-recovery/results/g2h-validation-unused \
  --pkg="$PWD"
# PASS: G2H smoke wrapper validation PASS (no fit).
```

The one authorised smoke used seed `86121`, commit `bdc3da6a36da407d22b9162afcdef38dfda42eea`, and three restarts. All profile rows had finite NLL and `convergence = 0`. The lower-offset profile deltas were `0.000000, 0.569917, 2.786129, 2.646356, 1.680058, 2.546995`; three exceed the frozen value 2 criterion. Maximum GBIF-bias error was `0.1149462`, below 0.30. The all-attempt optimizer objectives agree at `5228.673`; their elapsed times were `13.911`, `20.361`, and `19.893` seconds. The wrapper's fixed gradient criterion alone held admission.

The saved-manifest readback verified hashes for `decision-ledger.rds`, `fit.rds`, `profile-ledger.rds`, `root-receipt.rds`, `stage.txt`, and `truth.rds`. It intentionally precedes writing `file-manifest.csv` and `smoke-receipt.md`; those two terminal files are retained but cannot self-enumerate in that one-pass manifest. This is a provenance limitation, not a reason to mutate or rerun the completed smoke.

## 6. Tests of the Tests

The no-fit mode reconstructs and validates the frozen fixture before any package load or optimizer call. The successful smoke exercises the acceptance path: exact fit, all three restart records, all six profile ledgers, and decision classification. The wrapper also contains a fit-error failure path that writes `INVALID_FIT_ERROR`, a placeholder profile ledger, decision ledger, and hold receipt rather than mislabelling a runtime failure as scientific nonresponse.

## 7a. Issue Ledger

Issue #953 was explicitly out of scope and was not inspected, commented, or changed. No issue was created.

## 8. Consistency Audit

`rg -n 'G2H_SMOKE|G2h|isdm' README.md ROADMAP.md NEWS.md docs/design docs/dev-log/known-limitations.md _pkgdown.yml` found no public G2h claim; the only design-register hit was the existing covered cloglog-family row. `rg -n 'g2h-360cell|G2H_SMOKE' dev/isdm-package-recovery docs/dev-log` found the private fixture, runner, and prior G2h preparation records. G2c remains `G2C_SMOKE_ADMISSION_HOLD`; G2d--G2g evidence is unchanged. The required PR/history coordination check found no G2h collision.

## 9. What Did Not Go Smoothly

Increasing geometry resolves the frozen profile/bias-response rule but does not meet the predeclared maximum-gradient admission rule. The result must therefore remain a hold; it cannot be promoted to a recovery campaign by interpreting the scientific profile class alone.

## 10. Known Residuals

This is one synthetic local diagnostic only. It supplies no multi-seed recovery, coverage, empirical, spatial, detection-extension, count, comparator, absolute-intensity, zero-inflation, public API, or package claim. A recovery campaign remains separately approval-gated and is not admissible on this hold.

## 11. Team Learning

Curie's retained-profile perspective shows the G2h geometry makes several lower-tail variance directions identifiable even though one species remains flat in the lower tail. Fisher's admission perspective keeps that useful diagnosis separate from numerical eligibility: a narrowly missed gradient criterion is an operational hold, not evidence of recovery.

## 12. Cross-Product Coverage

G2h covers one private 360-cell nonspatial six-species diagnostic under the frozen GBIF-Poisson plus three-visit PA-cloglog model only. It does not cover a recovery campaign, repeated-visit detection extension, spatial latent or independent fields, other observation sources, counts, empirical inference, zero inflation, comparator benchmarking, package API, or reader-facing documentation.

### Next Actions

Perform a read-only retained-fit diagnostic on the G2h gradient failure, then return with a narrowly predeclared repair-or-no-repair plan. Do not rerun this smoke, launch a campaign, use Totoro/DRAC, or alter public/package surfaces without fresh approval.

**Roadmap tick**: N/A; this private diagnostic changes no public roadmap row.
