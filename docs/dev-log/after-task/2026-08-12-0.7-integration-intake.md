# After Task: refreshed 0.7 integration intake

## 1. Goal

Deliver a current, reviewable decision packet for the smallest
evidence-backed ordinary native-Laplace 0.7 candidate and fence every other
route.  No release action was in scope.

## 2. Implemented

Rebased the dedicated intake branch on live `origin/main` and replaced its
stale decision with a source-selection HOLD.  The packet now records that no
current 0.7 candidate is artifact-current and names the approval required to
create one.

## 3a. Decisions and Rejected Alternatives

The maintainer chose to retain LA-MSPL as a strategic opt-in lane.  The release
architecture is therefore two-track: ordinary native-Laplace claims remain
narrow, while LA-MSPL is independently adjudicated and fenced beyond its earned
point-estimation/separation scope.  Rejected: treating the 0.6 receipt as proof
for current main; merging evidence receipts as capability promotion; or
beginning #946, #941/#943, VA, AGHQ, EVA, ordinal, structured, or interval work
as release hygiene.

## 4. Files Touched

- `docs/dev-log/release/2026-08-12-0.7-integration-intake.md`
- `docs/dev-log/plan-actual/2026-08-12-0.7-integration-intake.md`
- `docs/dev-log/check-log.md`
- this report

No package-bearing path, public documentation, generated help, example, CI,
artifact, issue state, or compute job changed.

## 5. Checks Run

- `git fetch origin --prune`, `git status --short --branch`, worktree census,
  branch/PR/issue reconciliation, and recent-ref inspection.
- `git rebase origin/main` on the dedicated intake branch; it completed cleanly.
- `git diff --name-only cb312689..origin/main -- DESCRIPTION NAMESPACE R src inst man vignettes tests .Rbuildignore`.
- Direct reads of the exact 0.6 artifact receipt, #345, the issue ledger, and
  `CRAN07-AA-*` / `MIS-35` / VA / MSPL validation-register rows.
- `git diff --check` and scoped-path verification after editing.

## 6. Tests of the Tests

No package tests apply because no package implementation changed.  The
artifact applicability check compares exactly the paths that would alter an
installed package, rather than inferring continuity from a documentation diff.

## 7a. Issue Ledger

#345 remains owner-gated and open; package version remains 0.6.0.  #872 stays
`park/research`; #855, #897, #946, #945, #941, and #943 retain their stated
fences/deferments.  No issue was closed or commented on.

## 8. Consistency Audit

The old intake's claim that `cb312689` artifact evidence still applied was
false for live `2942b654`.  Commit `14650312` and its installed-byte diff make
the discontinuity observable.  The refreshed packet separates #945 (within-
trait family fence) from #941/#943 (integrated-SDM), avoiding a numbered-issue
scope error.

## 9. What Did Not Go Smoothly

The dedicated PR's remote head is stale after local rebase, so it requires a
reviewed force-with-lease push before review can see the refreshed packet.  No
push occurred in this phase.

## 10. Known Residuals

This is a decision packet, not a source candidate, artifact, platform matrix,
or release claim.  LA-MSPL retention needs a fresh bounded admission plan owned
by its active Lane B, including an exact-source artifact/platform ladder; this
intake does not infer code changes or evidence from its presence.

## 11. Team Learning

Rose's independent read-only review passed the HOLD and established the
technical meaning of a hypothetical MSPL quarantine.  The maintainer then
selected retention instead: MSPL remains a separately adjudicated opt-in route,
not an ordinary-Laplace claim.  The review also separated the fixed-design
separation screen from LA-MSPL.  The reusable lesson is that an exact artifact
receipt must be checked against all installed-byte paths before it is used as
release evidence.

## 12. Cross-Product Coverage

This internal documentation-only packet covers only source identity, artifact
applicability, the native-Laplace point-estimation claim boundary, and the
release-state decision.  It **does NOT cover** REML, penalties or LA-MSPL,
VA/AGHQ/EVA engines, missingness, aggregation, ordinal or non-Gaussian
recovery, structured/source tiers, intervals, public documentation behaviour,
CI/platform execution, or any API/provider implementation.  Shinichi's
requested decision is whether to prepare the recommended ordinary
native-Laplace-only source line; only then may a new code plan begin.
