# After-task: Paper 2 iJSDM evidence-to-reader A0–A2

## 1. Goal

Start the fresh private Codex Paper 2 arc from G2o and execute only A0–A2:
reconcile retained evidence, map conceptual neighbours, and freeze the Case-C
estimator/Psi recovery and scale specification.

## 2. Implemented

Created a lane-local loop/checkpoint, a cited non-ranking source map, a frozen
internal estimator/Psi information specification, reconciliation receipt, and
Gate-A review.  The worktree branch starts at exact G2o commit `0f668c46`.

## 3. Mathematical contract

The private G2d Poisson plus repeated PA-cloglog shared-state model, GBIF-only
bias map, rank-one Lambda, free diagonal Psi, `theta_diag_B=log(psi)` transform,
Case-C decision, five recovery metrics, profiles, thresholds, and all-attempt
denominators are unchanged.  S=6/20/60 are proposed recovery cells; S=250/1,000
are one-fixture measured implementation gates only; S=10,000 is architecture
HOLD.

## 4. Files touched

All files are private developer/dev-log artefacts listed in the paired recovery
checkpoint.  `R/`, `src/`, tests, NAMESPACE, man pages, README, NEWS, ROADMAP,
pkgdown, vignettes, public articles, and package metadata were untouched.

## 5. Checks run

- Read `AGENTS.md`, active-lane split, G2o after-task/reconciliation, and
  retained G2d/G2n/G2k materials.
- Ran lane preflight, exact-head/ancestry checks, worktree inventory, and the
  shared-file pre-edit local-git/PR checks (GitHub was unreachable).
- Read official package documentation for A1 and performed a separate Gate-A
  method checklist.
- `git diff --check`: PASS.
- Static scans confirmed the three protected HOLDs, Case-C `NO_CANDIDATE`, the
  S=250/1,000 stages and predicates, and the S=10,000 architecture HOLD.  The
  only `nlminb`/`MakeADFun` occurrences are frozen estimator/stage names in the
  specification, not executable invocations.

No tests were run: the task deliberately added no executable code and forbade
all fit/compute paths.

## 6. Tests of the tests

No test harness changed.  The Gate-A review is a document-level contract
check; it would fail if a scale row, numerical predicate, protected HOLD, or
negative-space fence were absent or contradictory.

## 7. Roadmap tick

N/A.  This is not a public capability or roadmap change.

## 8. Consistency audit

The source map is explicitly non-ranking.  The specification has no new
likelihood, DGP, map, threshold, recovery metric, spatial, count, empirical,
absolute-abundance, generic-zero-inflation, arbitrary-source, or 10k claim.

## 9. What did not go smoothly

The worktree began detached and the shared Git metadata was read-only; a
narrow, user-approved Git-ref permission created the requested continuation
branch.  Brain CLI retrieval was unavailable because its configuration directory
was not writable, so orientation used the repository record and local memory
index instead.

## 10. Known residuals

No A3 work is authorized.  The packet has not measured a scale row, run a
recovery cell, fitted a comparator, or established any public support.

## 11. Team learning

The useful boundary is to keep an estimator-information specification separate
from both a repair proposal and a performance claim.  Measuring construction,
objective, fit, inference, and one profile stage independently prevents a
single elapsed time from being called scalability.

## 12. Cross-product coverage

Private synthetic nonspatial iJSDM design only.  No public gllvmTMB feature,
family, formula, article, or cross-package performance surface is covered.
