# After Task: Destination B S4 public Tree workflow

## Scope

`gllvm_julia_phylo_rr()` is the single authorised public post-fit exception. It accepts only an already fitted native Gaussian ML ultrametric-Tree `phylo_latent(..., d = 1, unique = FALSE)` model with a `0 + trait` design.

## Evidence

The focused opt-in R-to-Julia suite passed using frozen gllvmTMB semantics and the hardened GLLVM.jl precision consumer. It checks the distinct public class, stored Wald endpoints, summary/coef/logLik readers, and continued refusal of generic `engine = "julia"` phylogenetic terms.

## Boundaries

This is one controlled Gaussian Tree workflow. Dense `vcv`, pedigrees, extra fixed effects, prediction, generic engine admission, interval coverage, recovery, 0.7 parity, FRK, release, and registration remain unqualified.
