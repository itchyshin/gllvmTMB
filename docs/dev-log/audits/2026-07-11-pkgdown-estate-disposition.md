# pkgdown estate disposition before gllvmTMB 0.5.0

**Date:** 2026-07-11  
**Branch:** `claude/release-0.5.0`  
**Status:** IN PROGRESS — release work is paused

## Completion boundary

The package code can be complete while the package remains unfit to release. The
0.5.0 release does not proceed until every row in this ledger has an explicit
disposition, every retained reader page has been reviewed individually, the public
navigation has been consolidated, and the rendered site has been inspected from a
reader's perspective.

The starting public estate contained the Get Started vignette plus 32 article files.
All thirty-three pages have completed the page-by-page review or disposition.
Every retained public page received an individual panel review; every retired topic
has a recorded return condition. The page-disposition phase is complete, while
maintainer visual approvals and the final whole-site audit remain separate gates.

## Disposition meanings

- **SHIP:** keep on the public site and review as a reader-facing page.
- **MERGE:** preserve useful material inside a stronger retained page, then remove the
  redundant public route.
- **HIDE:** retain as a developer artefact but remove it from the public navbar,
  article index, search, and sitemap.
- **CUT:** retire the page after checking and repairing every inbound link.
- **REWRITE:** retain the topic, but do not ship the current page without a substantial
  reader-first rewrite.

## A. Completed page reviews

These rows are complete only for the page-review stage. They remain subject to the
final cross-page navigation, link, render, and stale-wording audit.

| Page | Tier | Disposition | Review state | Notes |
|---|---:|---|---|---|
| `vignettes/gllvmTMB.Rmd` | 1 | SHIP | reviewed | Get Started path reviewed; recheck in final rendered estate. |
| `morphometrics.Rmd` | 1 | SHIP | reviewed | Canonical Tier-1 exemplar. |
| `model-selection-latent-rank.Rmd` | 1 | SHIP | reviewed | Rank-selection claims fenced to the demonstrated fixture. |
| `joint-sdm.Rmd` | 1 | SHIP | reviewed | Binary JSDM orientation and honesty fixes applied. |
| `covariance-correlation.Rmd` | 2 | SHIP | reviewed | Covariance/correlation framing and interval boundaries applied. |
| `fit-diagnostics.Rmd` | diagnostics | SHIP | panel fixes applied; maintainer visual check pending | Pat, Fisher, and Rose recommendations applied: authoritative convergence verdict, every warning exposed and interpreted, conditional-diagnostic scope, family-evidence boundary, plot interpretation, and responsive navigation. |
| `convergence-start-values.Rmd` | diagnostics | SHIP | panel fixes applied; maintainer visual check pending | Fisher, Rose, and Pat recommendations applied: scaled-gradient verdict, separate baseline/no-SE fits, convergence/inference/identifiability lanes, symptom-linked starts, restart interpretation, and narrower bootstrap scope. |
| `pre-fit-response-screening.Rmd` | diagnostics | SHIP | panel fixes applied; maintainer visual check pending | Fisher, Rose, and Pat recommendations applied: support/redundancy/formula-shape scope, canonical long formula, overlap denominators, visible thresholds, scope-specific statuses, and corrected candidate rerun. |
| `pitfalls.Rmd` | diagnostics | SHIP | panel fixes applied; maintainer visual check pending | Fisher, Rose, and Pat recommendations applied: six concise checks, corrected simulator and rotation language, advanced phylogenetic decomposition removed, `A`/`Ainv`/`V` distinction narrowed, and all internal evidence codes removed. |
| `profile-likelihood-ci.Rmd` | 2 | SHIP | panel fixes applied; maintainer visual check pending | Fisher, Rose, and Pat recommendations applied: target-specific route map, public `profile_targets()` workflow, true natural-boundary behavior, explicit full-covariance Wald fallback, Fisher-z default, and unsupported speed/coverage/bootstrap claims removed. |
| `troubleshooting-profile.Rmd` | 2 | MERGE then CUT | merged into page 11; source and public route removed | Pat and Rose recommended consolidation; Fisher's technically useful distinctions were retained in page 11's symptom-to-action table. Navbar, article configuration, help text, printed messages, and inbound links were repaired. |
| `missing-data.Rmd` | 1 | REWRITE then SHIP | final panel and desktop/mobile gates passed; maintainer visual check pending | Fisher, Rose, and Pat unanimously rejected the 1,169-line unevaluated design compendium. The final reader guide contains live Gaussian response-mask and missing-predictor workflows in long and wide form, explicit missingness assumptions, honest discrete-output and uncertainty limits, stable public join keys, supported predictor types, and next actions. The review also repaired true wide-row provenance in `predict_missing()`, removed a synthetic placeholder column, added `level` / `level_id` to `imputed()`, and removed EBLUP, phase, private-field, internal-code, and false automatic-congeniality claims. |
| `gllvm-vocabulary.Rmd` | 2 | REWRITE then SHIP | final panel and desktop/mobile gates passed; maintainer visual check pending | Fisher, Rose, and Pat independently retained the glossary but rejected its definitions and specialist sprawl. The final candidate distinguishes LV scores from loadings on the linear-predictor scale, states the ordinary `Var(u) = I` convention, separates Lambda/Psi/Sigma and grouping key/tier/source/mode, aligns fit health with the unscaled-gradient rule, labels Fisher-z as an effective-size sensitivity interval, and uses the public conditional-mode missing-predictor contract. Premature heritability, G-matrix, reaction-norm, phylogenetic-decomposition, SPDE, EBLUP, and internal process material was removed. The clean site/search/sitemap rebuild and desktop/mobile inspection passed. |
| `api-keyword-grid.Rmd` | 2 | REWRITE then SHIP | panel fixes applied; maintainer visual approval recorded 2026-07-12 | Fisher, Rose, and Pat retained the canonical syntax lookup. The reader-facing contract now teaches four modes only—Scalar, Independent, Dependent, and Latent—across ordinary, animal, phylogenetic, and spatial sources. Deprecated `unique()` / `*_unique()` functions were removed from the grid, article examples, ordinary reference topics, and vocabulary; the current `unique =` argument on latent helpers is explicitly distinguished from those deprecated functions. The separate current kernel interface retains `kernel_indep()`, `kernel_dep()`, and `kernel_latent()`; only `kernel_unique()` belongs in the deprecated reference set. The review also corrected fixed-operator versus spatial-GMRF distinctions, mesh requirements, spatial Psi wording, internal status prose, and responsive table behaviour. Shinichi visually approved the final local page after the kernel-reference correction. |
| `fixed-effect-zero-constraints.Rmd` | 2 | REWRITE then SHIP | panel fixes applied; maintainer visual check pending | Fisher, Rose, and Pat independently retained the narrow feature but rejected the old implementation-led framing. Rewritten as a design-based guide to response-specific predictor effects: exact-zero and ML-only boundaries, direct-coefficient interpretation, long/`traits(...)` wide fits, model-matrix name discovery, conditional-inference meaning, multiple constraints, and symptom-to-action recovery. Internal support language and unnecessary latent structure were removed. |
| `response-families.Rmd` | 2 | REWRITE then SHIP | panel fixes applied; maintainer visual check pending | Fisher, Rose, and Pat unanimously retained the canonical likelihood lookup but rejected its implementation-led status ledger and found a critical delta/hurdle contradiction. Rewritten as “Choose a response family”: response-domain and sampling-process routing, exact NB1/NB2 parameterizations, Student-t guidance, specialist truncation/hurdle boundaries, a live Poisson long/`traits(...)` wide equivalence example, safer named mixed-family syntax, covariance-scale distinctions, diagnostics, and input recovery. Internal mapping/status prose and the unsupported-constructor catalogue were removed; the live shared-predictor hurdle contract now matches the likelihood. |
| `choose-your-model.Rmd` | 3 | MERGE router then CUT | panel disposition applied; Get Started visual check pending | Fisher, Rose, and Pat independently rejected the 4,938-word ladder as a public article. Family, grouping, covariance source, mode, rank, space, and slopes are branching scientific choices rather than cumulative rungs. The page also contained false phylogenetic requirements, stale slope boundaries, raw fitted-object internals, weak simulations labelled as recovery, and unsafe interval advice. Its only useful asset—a compact destination router—was merged into Get Started; the standalone source, navbar/index entry, generated route, stale asset, and inbound links were removed. Git history preserves the retired draft. |
| `animal-model.Rmd` | 3 | CUT; future rewrite deferred | panel disposition applied; surviving reference destination visual check pending | Fisher, Rose, and Pat independently failed the article for publication. Its nominal half-sib pedigree created full-sib families; the bivariate example estimated a genetic correlation of -1.00 against a truth of +0.632; and the page treated fit-health checks as scientific recovery. It also relied on private TMB fields because no stable public heritability, animal-G, or breeding-value extractor exists. The article, navbar/index entry, generated route, and inbound links were removed. Current pedigree conversion and `animal_*()` call shapes remain in public reference material; a future quantitative-genetics article requires an informative design, explicit environmental components, stable extractors, and replicated recovery evidence. Git history preserves the retired draft. |
| `phylogenetic-gllvm.Rmd` | 1 | REWRITE then SHIP | panel fixes applied; maintainer visual check pending | Fisher, Rose, and Pat unanimously failed the old 825-line four-component page, whose clean diagnostics masked boundary collapse and poor recovery. Replaced with a 353-line Gaussian guide using 150 species with within-species replication, one full phylogenetic covariance target, a tree/data alignment check, long and `traits(...)` wide equivalents, every non-passing diagnostic row, public extractors, and explicit truth comparison. Reaction norms, behavioural tiers, two-Psi heuristics, heritability language, private fields, arbitrary thresholds, internal process prose, and unsupported uncertainty claims were removed. The retained example has 0.156 relative Frobenius error and the rendered figure shows correlation error without presenting it as uncertainty. |
| `behavioural-syndromes.Rmd` | 1 | REWRITE then SHIP | panel fixes applied; maintainer visual check pending | Fisher, Rose, and Pat independently retained the intercept-only behavioural question but rejected the 792-line draft's rotation-dependent axis labels, causal within-occasion story, circular comparison with a planted literature median, filtered diagnostics, uncalibrated interval emphasis, and clipped figures. Replaced with a 427-line Gaussian guide using six behaviours, 150 individuals, six uniquely nested occasions, explicit ranks, long and `traits(...)` wide calls, the scaled-gradient verdict plus every warning, truth checks for both total covariance tiers, and point repeatability. The example has relative Frobenius errors 0.173 between individuals and 0.094 within individuals, with repeatability MAE 0.032. Raw-axis ordination, communalities, four redundant figures, process prose, internal codes, and causal/generalisation claims were removed. |
| `random-regression-reaction-norms.Rmd` | 1 | REWRITE then SHIP | panel fixes applied; maintainer visual check pending | Fisher found that the shipped fixture assigned temperature across individuals rather than within individuals, invalidating the old reaction-norm design. Rose and Pat independently found incomplete diagnostics, private residual access, an unidentified residual split, missing centring and design guidance, and premature non-Gaussian advice. The fixture now gives every individual five temperatures over the same gradient and stores Celsius plus the centre and scale. Tests require within-individual coverage, fixed-slope recovery, recovery of all three augmented covariance blocks, total session covariance, pointwise repeatability, and passing curvature diagnostics. The rewritten Gaussian guide shows long and `traits(...)` wide fits, all health rows, population slopes, public covariance extraction, block-specific recovery, and repeatability only over common observed support. Private fields, internal codes, process prose, separate residual claims, and non-Gaussian analysis advice were removed. |
| `random-slopes-nongaussian.Rmd` | 3 | HIDE; current draft retired and future rewrite deferred | panel disposition applied; surviving keyword guide visual check pending | Fisher, Rose, and Pat unanimously rejected the validation-ledger page as a public article. Its lead Gaussian example simulated intercept--slope correlation but fitted `phylo_indep()`, its fitted formulas omitted the population-average slope, its Poisson design had little within-species context coverage, and it called a slope-variance estimate less than half the truth recovery. The full family/source/mode and animal-grid claims exceeded direct evidence; `latent`, `indep`, and `dep` were incorrectly presented as a uniform complexity ladder; heavy phylogenetic and spatial examples used undefined unevaluated objects; and private fields, internal codes, test tolerances, unsafe Hessian advice, and a generic Bayesian-refit recommendation dominated the reader path. The public source, route, navbar, and index entry were removed. A concise, exact distinction among structured slope covariance shapes, fixed-slope requirements, extractor levels, and design checks was retained in the formula keyword guide. Git history and the validation register preserve developer evidence; a future article requires one aligned applied question, runnable public-API code, and a scientific figure. |
| `cross-lineage-coevolution.Rmd` | 3 | HIDE; public draft retired to existing design records | panel disposition applied; surviving keyword guide visual check pending | Fisher, Rose, and Pat unanimously rejected the page as a 0.5.0 reader guide. A fixed kernel built from two phylogenies, one observed association matrix, and analyst-supplied `rho` estimates association-conditioned cross-lineage covariance, not a causal coevolutionary process. The article conflated `Gamma_shape`, fixed-rho effect, and pair-specific covariance; incorrectly said common loading rotation changes Gamma; treated a numerically zero null Gamma as structural; described multiple kernels as planned although they are implemented; mixed single-kernel Psi with latent-only multi-kernel syntax; and changed the model inside one rho-profile callback. Every load-bearing fit and likelihood table was unevaluated or illustrative, while the sole “fitted” heatmap was truth plus random noise. Internal codes, Paper-2 process prose, private fields, an unreviewed novelty claim, unsupported interval advice, and absolute effective-sample-size heuristics added further risk. The public source, route, navbar, and index entry were removed. The existing design/validation records retain the research programme; the keyword guide now preserves only the fixed-kernel, Gamma-scale, supplied-rho, separability, and non-causal interpretation boundaries. |
| `lambda-constraint.Rmd` | 3 | HIDE then CUT; future guide deferred | panel disposition applied; revised `joint-sdm` visual check pending | Fisher, Rose, and Pat independently rejected the standalone page. The simulation drew the two loading anchors below one but fitted them as exact `+1` restrictions, even though unit-variance latent scores make those values substantive. The page then named unaligned exploratory axes, treated their raw entries as biological leakage, used an unhealthy small-sample fit for loading intervals and variance partitions, and presented an uncalibrated chi-square likelihood-ratio test. The served likelihood and uncertainty output was pathological and contradicted the newer source. The source and generated route were retired. A concise caution now remains in `joint-sdm`: numeric pins are exact assumptions, the live route is lower-triangular rather than general free-form CFA, same-data suggestions remain exploratory, and rotation-invariant covariance is the primary interpretation target. |
| `lambda-constraint-suggest.Rmd` | 3 | MERGE boundary then CUT | panel disposition applied; revised `joint-sdm` visual check pending | The companion repeatedly selected zeros from an exploratory fit, refitted the same data, and invited confirmatory model comparison and loading inference without accounting for selection. Its profile-retention path treated failure to reject as evidence for an exact zero and described a non-positive-definite fit as rescued merely because a likelihood profile could be computed. Most consequential refits were unevaluated, private fitted-object fields drove figures, and the page duplicated the flawed main fixture. The standalone source and generated route were retired. Only the reader-facing boundary that data-derived constraints are descriptive orientations, not prior biological hypotheses, was merged into `joint-sdm`; the exported helper remains documented in its reference topic. |
| `mixed-family-extractors.Rmd` | 3 | MERGE then CUT | panel disposition applied; revised `response-families` visual check pending | Fisher, Rose, and Pat independently found a real capability buried inside an internal extractor catalogue. The page's “common, comparable latent scale” claim contradicted the package's per-trait/per-pair scale design; its internal routing fixture was not generated from the fitted likelihoods and produced a near-boundary Gaussian communality; it hid two fit warnings, misidentified unsupported residual rows, and used private/internal helpers. Tiny Fisher-z and bootstrap demonstrations looked inferential despite absent mixed-family calibration, while register tables, route status, delta/hurdle plans, and generic trait names displaced any biological question. The standalone source and route were retired. The retained `response-families` section now shows safe named-family dispatch, checks one family per trait, separates fitted-tier covariance from family/link-adjusted point correlations, explains mean-dependent approximations, and states that neither target is a raw-response correlation or universal common scale. |
| `ordinal-probit.Rmd` | 3 | MERGE then CUT; future applied rewrite deferred | panel disposition applied; revised `response-families` visual check pending | The ordinal family and cutpoint extractor are real, but all reviewers rejected the current article as a release page. Its one-observation-per-unit-and-trait fixture fitted a diagonal unit effect that behaved like an unidentifiable observation-level scale: planted variance 0.3025 collapsed to roughly 0.00007 and 0.00020 despite all numerical health rows passing. Both advertised structural and total Sigma calls used the default `link_residual = "auto"`, so they rendered identically; the article then introduced heritability without fitting phylogenetic or genetic variance and misstated the covariance grid. The page had no applied question, predictor, predicted category probabilities, figure, or sparse-category workflow. The standalone source and route were retired. `response-families` now retains the threshold equation, fixed-first-boundary convention, free-cutpoint interpretation, consecutive-coding and observed-maximum warning, proportional-threshold assumption, binary-probit redirect, and the requirement for a design that actually identifies any advertised variance component. |
| `psychometrics-irt.Rmd` | 3 | CUT; future guide deferred | panel disposition applied; no replacement page | Fisher, Rose, and Pat independently rejected the page. Its constraint matrix set the second lower-triangular identification loading to zero, collapsing the entire Mood factor; the rendered table nevertheless claimed recovery. The confirmatory fit had a non-positive-definite Hessian, zero Mood loadings, and a weak second axis. The likelihood-ratio calculation hard-coded six restrictions although the fitted-object degrees of freedom differed by five, and the singular boundary fit made the chi-square reference indefensible regardless. The article also called unbounded Gaussian responses Likert scores, described raw exploratory loadings as rotation-invariant, taught a private fitted-object field, mislabeled total correlation as latent-only, and offered malformed multilevel syntax. The public source, route, navbar, and index entry were removed. A future psychometrics guide must start from a validated item-response fixture, correct identification anchors, public extractors, psychometric estimands, an external comparator, and calibrated uncertainty; psychometrics is not advertised as a current package domain. |
| `cross-package-validation.Rmd` | 3 | CUT; evidence retained in tests/design records | panel disposition applied; no replacement page | Fisher, Rose, and Pat independently rejected the page. Its rendered binary comparison reported factor correlations of 0.999 and 0.017 with the second `gllvm` factor collapsed, yet the prose declared identical ordinations. The DGP was probit-thresholded while both fits used logit, the covariance decompositions were not aligned, and neither fit received a two-factor health gate. The Gaussian comparison used a one-observation-per-cell fixture, obtained a non-PD `glmmTMB` fit, bypassed `logLik()` through a private optimizer field, and claimed the result was exactly the CI test even though that test explicitly skips the unhealthy comparison. The matrix also labelled an absent standalone diagonal comparison as live and conflated parser smoke checks, shared-engine port regression, independent agreement, and missing comparators. The public source, route, navbar, and index entry were removed. Exact comparator evidence remains in tests and design records; any future reader guide requires versioned, estimand-aligned, healthy fixtures and predeclared agreement metrics. |
| `simulation-recovery-validated.Rmd` | 3 | CUT; historical failure evidence retained in design/audits | panel disposition applied; no replacement page | All reviewers rejected the page as public evidence. Its bundled v0.2.0 smoke artifact predates the rule that maps default Psi off for single-trial Bernoulli and ordinal traits, and it profiles diagnostic per-trait Psi rather than the current primary total-covariance target. The table counts trait rows as completions, reports zero failures even when rows are missing, conditions coverage on successful fits, and applies a hard pass flag to only ten replicate clusters. The page then calls Gaussian/binomial results near nominal despite the later production gate failing 13 of 15 cells with 236 of 3000 failed fits. It also gives speculative causes as established patterns, states the wrong number of free ordinal cutpoints, and exposes raw artifact slots, scripts, file paths, runtimes, milestone codes, and promotion machinery without a reader workflow or figure. The public source, route, navbar, and index entry were removed. Historical results remain in the production-artifact audit, target-scale audit, register, and design documents; any future evidence guide requires current target-explicit artifacts and failure-aware Monte Carlo summaries. |
| `simulation-verification.Rmd` | 3 | MERGE boundary then CUT; helper repairs queued | panel disposition applied; revised `fit-diagnostics` visual check pending | Fisher, Rose, and Pat independently rejected the standalone workflow. `simulate_site_trait()` generated random site-level environmental effects that the fit omitted, so the headline truth matrix did not match the fitted estimand; rendered errors reached +0.416 for a true variance of 0.94 and +0.205 for a true covariance of 0.32 while the prose claimed recovery. Only the basic fit and `sanity_multi()` ran; the four advanced checks were unevaluated. Review also found that `check_identifiability()` reverses the meaning of near-zero Procrustes residuals, accepts an unhealthy convergence disjunction, and leaves documented coverage uncomputed; `coverage_study()` conditions on successful refits/intervals and applies an uncalibrated small-R 94% flag. The page further treated profile heuristics and non-rejection of a consistency test as certificates. The source and route were retired. `fit-diagnostics` now distinguishes predictive simulation, fitted-model simulate-refit self-consistency, and known-DGP validation, while `pitfalls` and the profile guide retain the safer detailed material. |
| `roadmap.Rmd` | 2 | CUT public wrapper; retain root roadmap internally | panel disposition applied; revised homepage visual check pending | Fisher, Rose, and Pat unanimously rejected automatic publication of the 3,499-word contributor ledger. It exposed agent ownership, PR pacing, issue numbers, validation IDs, audit paths, restoration mechanics, and volatile planned work rather than answering user adoption questions. Its public inventory omitted several current guides, duplicated others, retained completed work in the active queue, and disagreed with the July estate ledger. It also taught obsolete `latent + unique` syntax even though ordinary `latent()` now includes diagonal Psi. The wrapper, route, top navbar item, Developer Notes menu, and article-index section were removed; `ROADMAP.md` remains a contributor-only GitHub document. README now treats linked guides as the reader-facing support boundary and labels the repository roadmap as internal planning rather than a support guarantee or schedule. |
| `functional-biogeography.Rmd` | 3 | CUT; future capstone requires from-scratch rewrite | panel disposition applied; no replacement page | Fisher, Rose, and Pat unanimously rejected the capstone. Its adjusted composite returned optimizer convergence 1, a non-positive-definite Hessian, and unavailable fixed-effect uncertainty, yet the article interpreted point correlations and concluded that a syndrome was not a geography or ancestry artefact. The core fit also poorly recovered planted covariance and communality, including collapsed diagonal components; one claimed block even changed sign. The article falsely called the phylogenetic and identity matrices orthogonal, offered unsupported sample-size and recovery thresholds, advised dropping plausible controls below those thresholds, and showed no uncertainty. Its spatial DGP did not match the fitted SPDE model, the phylogenetic extension formula changed the unit contract and errors under the current API, and both extension fits were unevaluated. The synthetic generic traits answered no real biological question, and the within-site heatmap was visibly broken. The source and direct route were retired. Existing public guides retain the safe component-level teaching; a future capstone must use healthy stagewise fits, a biologically named design, truth-aligned components, evidence-based guidance, and associational sensitivity claims. |

## B. Confirmed public keepers still requiring individual review

None. Every page initially classified as a confirmed public keeper has completed
its Fisher, Rose, and Pat review. Maintainer visual approval and the final whole-site
audit remain separate gates.

## C. Pages requiring maintainer disposition before detailed review

None. All 33 article sources in the starting estate now have a recorded
reader-first disposition.

## D. Package reference and non-article surfaces

Article cleanup alone is insufficient. These surfaces receive their own audit after
the article dispositions are settled:

| Surface | Required check | State |
|---|---|---|
| `README.md` / landing page | reader route, version/lifecycle wording, no stale or internal claims | reader/process and desktop/mobile visual gates passed |
| `_pkgdown.yml` navbar and article index | only retained reader routes visible; no developer drafts in search/navigation | route/search and desktop/mobile visual gates passed |
| desktop navbar breakpoint | at 1024 px, navigation collapses cleanly; all menus and search remain reachable through the toggle without horizontal overflow | browser verification passed at 1024 px and 390 px |
| exported roxygen and `man/*.Rd` | source/Rd agreement, useful examples, no internal register codes or stale claims | process/code, Rose parity, example, and output gates passed |
| `NEWS.md` / Changelog | plain-language release history without internal register codes | reader sweep, Rose gate, render, and final package check passed |
| printed examples and extractor tables | no hidden internal columns or provenance keys in rendered output | 339 focused tests and rendered-table scan passed |
| pkgdown Reference index | every exported topic placed once, with reader-shaped grouping and descriptions | `check_pkgdown()` parity plus desktop/mobile visual grouping review passed |
| site search and sitemap | no hidden/cut drafts or internal rule files discoverable | final clean rebuild passed; internal files, codes, and cut routes absent |
| cross-links | no dead, circular, or misleading links after merge/hide/cut decisions | 1,185 retained-article links and article-chooser visual review passed |
| full site and package gate | clean pkgdown build plus `--as-cran` package check | clean build passed; 229 HTML pages and 6,920 local links passed; `--as-cran` 0 errors, 0 warnings, 0 notes |

## E. Release gate

Do not open or advance the 0.5.0 release PR from this ledger alone. The gate opens
only after all rows have a final decision, all retained pages have completed review,
the site renders cleanly, visual inspection passes, and the after-task audit records
both what was completed and what remains outside 0.5.0.
