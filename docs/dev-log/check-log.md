# Check log

Append-only record of `R CMD check`, `devtools::test()`, and
`pkgdown` runs that produced meaningful evidence. Keep entries
date-stamped.

The first entry will be added when the bootstrap PR (this one)
lands its local rcmdcheck.
