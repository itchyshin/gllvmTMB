# Session Handoff: G3P V2 binding, pre-Gate B

`fdcb05cd` is the current exact generic-runner baseline. It binds the
preflight receipt's schema, source gate, root ID, attempt ID, time estimate,
and hard stop before a
future smoke can load the compiled package or enter the optimizer. The V2
proposal names the intended V2 identifiers but creates neither packet nor
root.

Resume on `codex/isdm-g3-provenance-amendment`, read the paired recovery
checkpoint and V2 proposal, and collect independent Gauss/Noether, Fisher,
and Rose verdicts. If all pass, ask the maintainer only for Gate B: creation of
the exact V2 packet and ignored root. Obtain separate explicit approvals for
any preflight and for exactly one smoke.
