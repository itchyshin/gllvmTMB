# Ultra-plan — g0_unlock fork B (G0 SIGNED 2026-08-18)

Frozen at approval. Binding detail for `/goal`.
Do **not** reopen repo-root `LOOP/` (Poisson \(W_*\) REPLACE **GOAL_MET**).

## 🎯 GOAL

```
🎯 GOAL
Solo platform: Cursor (this kit) — L0/L1 execute in sibling Cursor worktrees; decision/docs in forkB-decision
Deliverable: Design 125 G4c = fork B recorded; D-148→D-159 + PARK→REPLACE docs sync; L0 penalty-off path measurable (calibrated=FALSE, public refuse); L1 local coverage smoke logged
HEADLINE: Fork B is measurable on a local smoke — unpenalized Laplace profiled at fixed MSPL nuisance; A is ablation only
IN PARALLEL: decision/docs · L0 plumbing · L1 smoke · this kit (docs-only)
DEFER: Totoro · T* freeze · undraft #1077 · public se/vcov/confint · MSPL-04→covered · NEWS covered · L2 claim · fork C
DISCIPLINE: verify=log+artifact not exit code · compute=local only · closure=L1 receipt exists and hard OUTs held
```

ARC PROGRAM: N/A (no Arc Card; this is a `/goal` kit after G0).

---

## WHAT THE BRAIN ALREADY KNOWS

- Vault **D-159** is the gllvmTMB LA-MSPL interval withhold (Design 118
  pre-reg). It was committed as **D-148** on 2026-08-15 and renumbered
  2026-08-18 so the *never-ask-bare* decision could keep **D-148**.
  Live MSPL docs still say D-148 in places — that is the A2 cite fix
  (`memory/DECISIONS.md` numbering note; `journal/2026-08-18`).
- **D-149** authorises internal SE pins; public calibrated intervals stay
  withheld. **D-157** parks Design 118 / B1; later intervals = new
  construction (Design 125). **D-12** heroizes profile.
- Design 125 + ADEMP G4c was **FORK-DEFER**: A = penalised MSPL; **B =
  unpenalized Laplace at fixed MSPL nuisance**; C = hybrid.
  (`docs/design/125-mspl-profile-led-intervals.md`;
  `docs/dev-log/research/2026-08-17-mspl-profile-led-prereg-ademp.md` §M;
  `docs/dev-log/research/2026-08-17-mspl-profile-bootstrap-ci-next.md`.)
- Poisson \(W_*\) REPLACE is **done** (#1111 @ `3053fce3`). Design 125 §5
  still says **SIGNED PARK SE doors** — stale vs tape.

## WHAT SHINICHI TOLD US

AskQuestion 2026-08-18 chose **g0_unlock** (not docs-only, not full public
unlock). Then twice: **Fork B**. Parent: new `/goal`, do not reopen
REPLACE GOAL_MET.

## WHAT THE TEAM RAISED

```
TEAM RAISED
  Fisher — Fork B is the Q0-aligned profile (unpenalized Laplace at fixed
           MSPL nuisance). A is the Design 118-shaped objective and stays
           ablation. · Why: KF2021 already says profiling the penalised
           objective does not rescue coverage on binomial; B is the
           construction that can be compared to Q0. · Rec: lock B; do not
           smoke A as primary. · Q for Shinichi: none (signed). · Default: B.
  Rose   — Root LOOP/ is a closed GOAL_MET record (#1124). · Why: overwriting
           it reopens REPLACE. · Rec: new kit under
           docs/dev-log/lanes/cursor-mspl-fork-B/. · Q: none. · Default: archive-in-place.
  Gauss  — L0 must unlock penalty-off for internal/calibrated=FALSE only;
           #1090 hard-refuses that path. · Why: a silent public door is the
           failure mode. · Rec: keep .gllvmTMB_mspl_assert_inference. · Q: none.
  Ada    — Four sibling lanes; this kit is the conductor contract, not the
           implementer. · Rec: docs-only PR; do not touch R/ or Design 125 body.
```

## ADA'S RECOMMENDATION

Ship this kit as a docs PR. Let decision / L0 / L1 keep their files. Stop
after L1 local smoke.

## DECISIONS LOCKED

Fork **B** primary · A ablation · C deferred · local compute only · public
doors closed · REPLACE LOOP stays GOAL_MET.

## QUESTIONS STILL OPEN

None blocking. T\*, undraft #1077, public se, Totoro stay fenced (not
questions — hard OUTs).

---

## Phase 0.25 sweep receipt

| Surface | Evidence | Finding | Call |
|---|---|---|---|
| **repo git** | `git status -sb` on `~/local-scratch/lanes/gllvmTMB-mspl-forkB-goal` after `git fetch` + rebase skip of stub `16fd7c0d`; `git worktree list` showed forkB-goal / L0 / L1 / decision / g0-unlock / REPLACE; `branch_drift_check.sh` on Dropbox checkout is a different branch (ignore) | worktree now = `origin/main` @ `81d3a303`; root LOOP/ is REPLACE GOAL_MET; sibling stub was a placeholder that collided with #1124 | **resume kit write here**; do not rebuild REPLACE |
| **twin / sister** | drmTMB MSPL implementation guide in the vault; DRM.jl lanes exist under `~/local-scratch/lanes/` | no fork-B tape in the R twin | **n/a** — do not port |
| **brain** | MCP `search_notes` `"g0_unlock fork B Design 125 MSPL profile"` `search_all_projects=true` `after_date=7d`; grep `fork B\|g0_unlock\|G4c` on `memory/AGENT_LOG.md` (no hits); grep `D-148\|D-159` on `memory/DECISIONS.md` (renumber note); grep journal `2026-08-18` (D-148→D-159) | no prior g0_unlock note; D-159 is the MSPL-interval withhold; journal records the cite trap | **reuse** Design 125 + ADEMP + D-159; **build** this kit |
| **log/history grep** | `docs/dev-log/research/2026-08-17-mspl-profile-bootstrap-ci-next.md` fork A/B/C; ADEMP G4c FORK-DEFER; REPLACE overnight arc **OUT** included "invent fork B/C smoke" — now superseded by this G0 | fork B was named, not picked, until today | **build-the-gap** = pick + L0 + L1 |
| **Verdict** | | REPLACE closed; Design 125 exists; G4c was deferred | **reuse** stub/ADEMP · **resume** nothing from REPLACE · **build** kit + let siblings implement |

PREFLIGHT: `lane_preflight.sh` from Dropbox checkout → **FOREIGN LANE ACTIVE
(claude + codex + 9 other cursor)**. This session took
**`cursor/mspl-fork-B-goal-kit`** and owns only
`docs/dev-log/lanes/cursor-mspl-fork-B/` + after-task + check-log prepend.
Does **not** claim `R/`, `src/`, Design 125 body, or `docs/dev-log/decisions.md`.

---

## SLICE TABLE

| ID | member | model+effort | dispatch | Bar | time | detail | dep |
|---|---|---|---|---|---|---|---|
| RECON | Shannon / Ada | Grok · low | this sitting | Cursor Models | 15 m | git + brain + ADEMP + PR states | — |
| A0 | Ada | Grok · low | this sitting | Cursor Models | 20 m | write this kit | RECON |
| A1 | decision lane | sibling | `cursor/mspl-forkB-decision` | Cursor Models | — | G4c → B | — |
| A2 | decision lane | sibling | same | Cursor Models | — | D-159 + PARK→REPLACE | — |
| A3 | PR hygiene | done | — | — | 0 | #1100 CLOSED; #1124 MERGED | — |
| A4 | L0 | sibling | `cursor/mspl-forkB-l0-20260818` | Cursor Models | — | `R/mspl.R` penalty-off path | A1 wording |
| A5 | L1 | sibling | `cursor/mspl-forkB-l1-smoke-20260818` | Cursor Models | — | local ADEMP L1 | A4 |
| A6 | Rose | Grok · low | this sitting | Cursor Models | 15 m | docs PR; fence | A0 |
| MECH-V | Curie-shape | Grok · low | this sitting | Cursor Models | 5 m | paths exist; root LOOP/ untouched; #1077 still draft | A6 |
| RECONCILE | Melissa | N/A this kit | campaign close | Other Models | later | plan-vs-actual when L1 lands | A5 |

SEARCH: none (G0 already named the construction; NotebookLM not offered —
internal Design pick).  
SLICES: A0, A1, A2, A3✓, A4, A5, A6  
PARALLEL: {A1, A2, A0} · {A4 ∥ A6} · A5 ← A4  
SEQUENTIAL: A5 after A4 tape exists  
FAN-OUT: sibling lanes already spawned by parent (decision, L0, L1, this kit,
PR hygiene). This accelerator does **not** spawn further children.  
FAN-OUT BUDGET: checkpoint=`g0-unlock-forkB-20260818` · new children=0/6 ·
scout=this recon · build=0 (siblings) · ceiling=0 · reuse=parent siblings  
SCOUT SUITABILITY: yes — recon + kit write are read/docs  
ULTRA EFFORT: no  
CONTEXT BRAKE: parent input=unknown · fresh-task trigger=not reached  
COMPACTIONS: parent=0 · children max=0 · boundary=open  
LANE RECEIPT: **START A FRESH TASK** to *run* L0/L1 — this sitting is the
kit. Next-task prompt = `checkpoint.md` RESUME.  
AUTO-REVIEW: guardian calls unknown · action=none  
D-43 PANEL: milestone=not a milestone (kit only) · status=not fired  
MODELS: Cursor Grok (this kit). L0/L1 stay on their Cursor lanes. Hand
adversarial claim review to Claude Opus / Codex only if L1 will be cited.  
ESTIMATE: kit ~30–45 min · campaign L0+L1 is sibling wall-clock, not this PR  
TWO BARS: Settings → Usage **not read** this sitting (subagent). This slice
is Cursor Models / Grok as dispatched. Do not burn Other Models to even
meters.

REVIEW: Rose-on-plan — sweep receipt cites commands; root LOOP/ fence is
the load-bearing scope check.  
VERIFY: `test -f` kit files; `git diff --name-only` must not include
`LOOP/GOAL.md` at repo root; `gh pr view 1077 --json isDraft` still true.  
CONSOLIDATE: this folder + after-task + check-log prepend.  
RECONCILE: **N/A for this docs-kit sitting** (no campaign close yet).
Campaign close (after A5) writes
`docs/dev-log/plan-actual/2026-08-18-mspl-forkB-g0-unlock.md`.

---

## Construction (binding)

**Fork B.** Profile the **unpenalized Laplace** objective, holding
**nuisance coordinates at the fitted MSPL point** \(\tilde\theta\).
Closer to \(Q_0\) (unpenalized observed \(J\) at \(\tilde\theta\)) than
to a penalised-objective profile.

**Fork A.** Penalised MSPL objective — **ablation only**. Not the L1
primary path. Not Design 118 relaunch.

**Fork C.** Hybrid — **not picked**.

**L0.** Unlock the internal penalty-off / fork-B measurable path that
#1090 currently hard-refuses. `calibrated = FALSE`. Public
`confint` / `vcov` / `se=TRUE` still hit
`.gllvmTMB_mspl_assert_inference`. Do not rebuild #1090 as a claim.

**L1.** Local only. ADEMP frozen L\* (G4d): on ≥1 binary-first anchor
cell, \(n_{\mathrm{rep}}\) 50–100:
\(\widehat{\mathrm{cov}}_{\mathrm{eff}}\) Wilson not entirely below 0.80;
availability ≥ 0.90; refusal ≤ 0.15 — or typed refuse (R-SAT / R-PIN /
R-NAVL / R-Q0 / R-FENCE). Estimands E1 intercept, E2 loading.
Do **not** escalate to L2 or Totoro from a pass.

---

## File-ownership fence

| Path | Owner |
|---|---|
| `docs/dev-log/lanes/cursor-mspl-fork-B/**` | **this kit** |
| `docs/design/125-mspl-profile-led-intervals.md` | decision lane |
| `docs/dev-log/research/2026-08-17-mspl-profile-led-prereg-ademp.md` | decision lane |
| `docs/dev-log/decisions.md` | decision lane |
| `R/mspl.R` (and other `R/` MSPL) | L0 |
| L1 smoke scripts / receipts | L1 |
| repo-root `LOOP/**` | **frozen GOAL_MET** — nobody |

---

## Hard OUT (repeat)

Totoro · T\* · undraft #1077 · public se/vcov/confint · MSPL-04→covered ·
NEWS covered · Design 118/B1 reopen · rebuild #1090 · KF2021 beyond
binomial · `git add -A` · isdm-package-recovery · overwrite root `LOOP/`.
