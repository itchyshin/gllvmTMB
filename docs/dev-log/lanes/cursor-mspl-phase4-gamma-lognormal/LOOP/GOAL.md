# GOAL — cursor-mspl-phase4-gamma-lognormal (IMMUTABLE — re-read every cycle)

Read this first, every cycle. Auto-compact eats messages, not this file.

This kit lives at `docs/dev-log/lanes/cursor-mspl-phase4-gamma-lognormal/LOOP/`.
**Do not write repo-root `LOOP/`.**

This is **LA-MSPL**, not EVA, not VA, not AGHQ-MSPL.

## Mission

```text
Solo: Cursor
Deliverable: Gamma(log) + lognormal(log) LA-MSPL Phase-4-style prep — derivation notes, pure-R oracles, registry rows status=planned only. NOT admitted.
HEADLINE: pin that Gamma W=phi is mean-inert and lognormal is Gaussian-on-log-y with shared sigma_eps, not Gaussian identity or Poisson all-zero
DEFER: admitted flip; NEWS covered; prepare widen to family_id 3 or 4; C++ tape; public se=TRUE; Poisson-admit / SE sibling files; R/fit-multi.R; R/mspl-curvature-pin.R
DISCIPLINE: planned only; se=FALSE; OMP=1; no src/; no git add -A; no Dropbox; no shared worktree /private/tmp/gllvmtmb-mspl-estimator-programme-roadmap
```

## Headline

Earn Gamma(log) and lognormal(log) on paper + oracles as
**planned** ordinary q=1/q2 cells. Do not admit.

## Invariants

- Workspace ONLY `/tmp/gllvmtmb-mspl-gamma-lnorm`.
- Branch `cursor/mspl-phase4-gamma-lognormal`.
- Lane LOOP only under this folder.
- Do NOT flip `admitted`. Do NOT write NEWS covered.
- Do NOT widen `.gllvmTMB_mspl_prepare()` to 3 or 4.
- Do NOT edit `R/fit-multi.R`, `R/mspl-curvature-pin.R`, or Poisson admit tests.
- Never `git add -A`. Never write repo-root `LOOP/`.

## Authoritative WHAT

- `LOOP/ultra-plan.md`
- Programme constitution:
  `docs/dev-log/after-task/2026-08-14-laplace-mspl-estimator-programme.md`

## Definition of done

1. Research notes pin Gamma \(W=\phi\) (mean-inert) and lognormal
   \(W=1/\sigma_{\varepsilon}^2\) on \(\log y\).
2. Pure-R oracles + registry `planned` / `phase4_prep` pins; no live
   `estimator = "mspl"`.
3. Prepare fence still `{0,1,2}`.
4. After-task; commit explicit paths; push; DRAFT PR titled
   planned/not admitted.
