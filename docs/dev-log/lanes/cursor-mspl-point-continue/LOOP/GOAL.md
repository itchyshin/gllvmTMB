# GOAL — cursor-mspl-point-continue (IMMUTABLE — re-read every cycle)

Read this first, every cycle. Auto-compact eats messages, not this file.

This kit lives at `docs/dev-log/lanes/cursor-mspl-point-continue/LOOP/`.
**Do not write repo-root `LOOP/`.** That path is the 0.6 EVA/VA kit on `main`.

Closed kits (`cursor-mspl-catchup`, `cursor-mspl-gaussian`, `cursor-mspl-arc-1a`)
are historical — do not reopen their GOALs.

This is **LA-MSPL**, not EVA, not VA, not AGHQ-MSPL.

## Mission

```text
Solo: Cursor
Deliverable: (A) multi-seed failure-inclusive Gaussian LA-ML vs LA-MSPL point evidence on ordinary cells, evidence thickened beyond single-seed oracle_local; (B) Poisson LA-MSPL Phase-4 prep: symbolic atom + pure-R oracles + planned registry rows (NOT admitted until smoke); (C) durable handover + Mission Control updates
HEADLINE: finish the point-estimate MSPL arc for Gaussian depth + Poisson derivation without touching SE
DEFER: binary SE (Codex PROTECTED); Gaussian SE; NEWS covered; Totoro/DRAC campaign unless ≤30min local first then STOP for Shinichi if larger; EVA; free-ε reopen
DISCIPLINE: se=FALSE; OMP=1; verify by logs; failure-inclusive denominators; apples-to-apples; closure=after-task+PRs+Melissa
```

## Headline

Thicken Gaussian ordinary LA-MSPL point evidence beyond single-seed
`oracle_local`, and earn Poisson Phase-4 on paper + oracles +
`planned` rows — without SE, without covered claims, without
admitting Poisson.

## Invariants

- Workspace ONLY `/private/tmp/gllvmtmb-mspl-estimator-programme-roadmap`.
- Branch `cursor/mspl-point-programme-continue` (stick to this name).
- Lane LOOP only under `docs/dev-log/lanes/cursor-mspl-point-continue/LOOP/`.
- Do NOT edit Dropbox checkout; Design 117 / iSDM / G3P / #872 / #855 /
  AA-03; Codex `codex/lane-b-mspl-interval-feasibility`.
- Do NOT implement Gaussian SE or binary SE/intervals.
- Do NOT flip Poisson `planned` → `admitted` without smoke + Shinichi.
- Do NOT write NEWS “covered”. Do NOT reopen free-ε / pick B.
- Uniqueness pick C stays pinned for Gaussian.
- Local fits only; `OMP_NUM_THREADS=1`; any fit/job >30 min STOP for
  Shinichi unless a pre-run receipt already exists.
- Never `git add -A`. Stage explicit paths. Never write repo-root `LOOP/`.

## Authoritative WHAT

- `LOOP/ultra-plan.md` — binding arc detail for this run.
- Programme constitution:
  `docs/dev-log/after-task/2026-08-14-laplace-mspl-estimator-programme.md`
- Handover:
  `docs/dev-log/handover/2026-08-15-cursor-handover.md`

## Definition of done

All of A + B + C landed (committed + PR’d as appropriate):

1. **(A)** Multi-seed failure-inclusive Gaussian LA-ML vs LA-MSPL
   research note (healthy + near-Heywood; q=1 and q=2; several seeds);
   optional test fixtures; denominators retain failures; `se = FALSE`.
2. **(B)** Poisson information/coercivity derivation note; pure-R
   oracles; registry rows `planned` only (not admitted).
3. **(C)** Handover updated for a fresh chat; Mission Control
   `gllvmTMB.json` MSPL strip refreshed; after-task + Melissa
   plan-actual; Rose claim fences intact.

Finish line is **not** a single smoke. Keep working until A+B+C are
done or a HARD STOP fires.
