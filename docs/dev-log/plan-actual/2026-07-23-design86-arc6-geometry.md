# Plan versus actual — Design 86 Arc 6 geometry diagnosis

## Planned

Diagnose a loading–variational escape/ridge, response separation, or
block-coordinate conditioning with deterministic no-DGP evidence; permit V2
drafting only after a complete Gate-A `GO`.

## Actual

Completed the immutable-artifact sweep, a q=1 symbolic/profile diagnostic,
local scalar-oracle/AD and finite-difference checks, reference-point curvature,
and raw/preconditioned trace comparison. Independent Gauss and Rose reviews
both set Gate A to `PARK`.

## Reconciliation

The q=1 ray is coercive because the KL contribution rises at both scale
extremes. It therefore does not establish the proposed loading–variational
escape. The intercept-only response controls cannot establish logistic
separation, and the diagonal preconditioner fails the same-target and health
conditions. The retained ledger has useful raw evidence but is incomplete for
a promotion decision. These findings require a conservative PARK; they do not
justify rerunning a diagnostic to manufacture a favourable result.

## Routing deviation

The planned Luna S0 dispatch could not start because the tiered dispatcher
could not write its readonly Codex state database and its app-server operation
was denied. The provenance sweep was performed manually as a read-only
mechanical check. This is recorded as routing drift, not represented as a Luna
receipt.

## Deferred

V2 amendment drafting, Gate B, every runner call, input/DGP construction,
smoke root, campaign, historical rescore, threshold/start/seed change,
Laplace, public/API/C++ work, Gate-3/4, Totoro, DRAC, push, and PR remain
deferred.
