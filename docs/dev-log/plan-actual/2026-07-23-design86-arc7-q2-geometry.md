# Plan versus actual — Design 86 Arc 7 q=2 diagnosis

## Planned

Build a complete deterministic q=2 Gate-A record, then conditionally draft V2
only after a `GO`.

## Actual

Completed immutable rehashing, q=2 mapping/rank/reflection/scaling controls,
local scalar/AD/finite-difference comparisons, fixed-design separation/rank
controls, raw four-stage traces, and two independent reviews. Gate A is
`PARK`; the conditional V2 path did not begin.

## Material reconciliation

The separable control supplied the predeclared differential pattern, but it is
a constructed fixed-design control rather than evidence about the immutable
smokes. The q=2 map is locally full rank and the tested scaling path has KL
cost. These findings do not identify a historical mechanism. The ledger also
falls short of the plan's promotion-complete A0--A4 schema, particularly A1
full mapping and A4 same-target evidence. That is a substantive PARK condition,
not a reason to retry or fill the record retrospectively.

## Routing deviation

The planned Luna mechanical dispatch was not available: the prior tiered
dispatcher route had a readonly Codex-state/app-server failure. Manual
mechanical rehashing was performed and recorded as routing drift, not a Luna
receipt. One Terra implementation worker did not produce files and was
interrupted; Ada completed the two owned dev files directly.

## Deferred

V2, Gate B, all runner calls, input/DGP construction, smoke roots, campaigns,
historical rescore, threshold/start/seed change, Laplace, public/API/C++ work,
Gate-3/4, Totoro, DRAC, push, and PR remain deferred.
