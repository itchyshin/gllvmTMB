# Plan versus actual — Design 86 Arc 8 historical observability

## Planned

Rehash the two immutable smoke chains, map the numerical state needed for a historical mechanism claim, audit provenance and claim boundaries, and make one terminal read-only decision.

## Actual

All six manifest/result/receipt files rehashed and their recorded cross-links reconciled. The observability matrix found that neither record contains the common labelled optimizer/coordinate/derivative/curvature/response state needed to identify or exclude a historical mechanism. The terminal category is `HISTORICAL_MECHANISM_UNOBSERVABLE`; the current admission path is retired.

## Material reconciliation

No material scope deviation occurred. The planned Luna mechanical route was not available because the tiered dispatcher remains unable to write its Codex state/app-server record; the mechanical rehash was performed inline and this is recorded as routing drift, not as a Luna receipt.

## Deferred

All runners, inputs/DGPs, compilation, probes, rescoring, V2/Gate B/smoke work, starts/seeds/threshold changes, campaigns, Laplace, public/API/C++ work, Gate-3/4, Totoro, DRAC, rebase, merge, push, and PR remain deferred.
