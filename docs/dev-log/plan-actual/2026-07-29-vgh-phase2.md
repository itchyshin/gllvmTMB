# Plan-vs-actual reconciliation — gllvmTMB VGH Phase 2

Plan: `~/.claude/plans/dazzling-inventing-coral.md`. Actual: branch
`claude/vgh-phase2-20260730` (worktree `/private/tmp/gllvmtmb-vgh-p2`), 12 commits
`8d7d0f27..ea4a26c6`, origin/main..HEAD. Reconciled by Melissa, 2026-07-29.

## Material deviations

| Axis | Planned | Actual | Tag | Owner |
|---|---|---|---|---|
| Scope (S2) | "Derive+justify" a `Psi` init for VGH's warm start | Declined to derive one; fails closed whenever a free `theta_diag_B` tier is present, falls back to cold start (`docs/dev-log/2026-07-29-vgh-phase2-psi-scope.md`) | **adaptive** — rationale recorded (splitting `Lambda Lambda'` into `Lambda*Lambda*' + Psi` is "the factor-analytic estimation problem," not a hand-off detail); matches the arc's own fail-closed posture on nbinom2/structured/spatial | domain reviewer |
| Scope (S3 blocker) | S3 blocked on PR #818 | Collision map (`2026-07-29-vgh-phase2-wiring-collision-map.md`) showed #818's sole `R/fit-multi.R` hunk (`:2495-2516`) is 1,200+ lines from every Phase-2 insertion point — no overlap; S3 proceeded | **adaptive** — evidence-based correction of the handover's own claim, done before acting, not after a collision | Ada |
| Scope (extra work) | Not in plan | (a) Head-to-head benchmark vs the `gllvm` package; (b) six-defect repair of the S4 verification harness after adversarial review | **adaptive** — (a) surfaced and then self-corrected a real overclaim (extraction bug, then a second correction retracting the "beats gllvm 273x" framing) before it reached the after-task doc; (b) fix landed (10:51) before the fixed harness was relied on for the real e2e measurements (11:13 on) — correct sequencing, no measurement built on the unsound version | Ada / domain reviewer |
| Evidence/verification (S5) | Totoro campaign, 384 cores, grid × seeds | **Not run.** Local benchmarks only (18 end-to-end cells across gaussian/poisson/binomial). Totoro verified reachable (384 cores, load 0.23), never used | **adaptive** — justified in the after-task doc: the hypothesis was disproved at local scale in every admitted family, so a larger campaign "would only make a disproved hypothesis more precise" | domain reviewer |
| Evidence/verification (headline target) | ≥1.5× end-to-end at identical optimum | Optimum half **passes** (loglik agreement 1.65e-13–1.96e-11, 18/18 cells, start confirmed landed every time); speed half **fails** in all three admitted families (gaussian ≈0.99×, poisson ≈1.05× erratic, binomial ≈0.73× and consistently slower) | **adaptive** — this is the verification contract working as designed (per-seed, not averaged) and reported honestly as a negative result rather than reframed | — (finding, not a process defect) |
| Model routing | Slice table specifies **Sonnet** for S1–S6, S8; **Opus** for S7 (fresh-context adversarial gate); Haiku for S5 execution | Commit co-author tags show **Fable** for 11/12 commits and **Opus** for 1 (the initial S4 harness build, `f18e71b5` — not S7). No commit is tagged Sonnet or Haiku. No discrete, fresh-context S7 gate exists as a separate step — adversarial-review passes (six-defect repair, head-to-head refutation) are folded inline into the same lane instead | **unclear** — no rationale for the Sonnet→Fable substitution appears anywhere in the dev-log, and the planned independent S7 gate (explicitly meant to catch "silently didn't land / different optimum / measurement artefact" with fresh eyes) never ran as its own step. The inline adversarial passes were substantively good (six real defects found and fixed) but are not the same guarantee as an independent fresh-context re-run of S1's oracle | **Ada** |
| Handoff state (S6) | "Land the branch": reconcile divergence vs origin/main, prune `dev/vgh` scratch, `rcmdcheck --as-cran` | `rcmdcheck --as-cran` **done** (4 local runs, final clean 0 errors/0 warnings/1 note, catching and fixing a real `ave()` CRAN NOTE along the way). Scratch pruning **declined** (`dev` is `.Rbuildignore`d; `c18ccc51` deliberately preserved those receipts — documented as a correction to the agent's own earlier claim). Divergence vs origin/main: **not reconciled by explicit action** — PR #819 (Phase 1) was merged upstream by someone else during/after this arc, which incidentally collapsed the gap to 1 commit; the Phase 2 branch itself never merged or rebased against origin/main | Scratch-pruning: **adaptive** (documented, justified). Divergence: **adaptive-by-luck** — the risk evaporated externally, not through a reconciliation action the plan called for | Ada / Rose |
| Handoff state (branch landing) | "Land the branch" (S6), implying the branch reaches a mergeable, closed state | Branch is pushed to origin (`origin/claude/vgh-phase2-20260730` exists, matches HEAD) but **no PR was opened** and it is not merged into `origin/main`. The after-task doc closes with "not done: S5, scratch pruning" and gives no explicit status or CARRIED-OVER declaration for the branch/PR itself | **unclear** — given the negative result, "land" may reasonably mean "document and stop" rather than "merge," but that is not stated anywhere; the closing doc is silent on PR/merge intent rather than explicitly deciding it | Rose (closeout) / Ada (scope) |

## What's unclear

Two items above are genuinely ambiguous rather than confidently adaptive or drift. First, the model-routing substitution (Sonnet → Fable, and no standalone S7) has no recorded justification — it may reflect a deliberate, reasonable choice (Fable as the operative build model, adversarial review folded inline rather than deferred) but nothing in the dev-log says so, so it cannot be waved through as adaptive on the current evidence. Second, the branch's terminal state is undeclared: it is clean, pushed, and rcmdcheck-green, but whether "land the branch" is satisfied by that alone (given the target was disproven) or still requires a PR/merge/explicit CARRIED-OVER note is a decision the after-task doc doesn't make. Both are process/closeout questions, not evidence problems — the underlying scientific work (transform, harness, benchmarks, negative result) is well-documented and internally consistent wherever I cross-checked it (warmstart-result.md numbers match the after-task-close.md summary table; rcmdcheck log timestamps confirm the final clean run postdates all package-code commits).

## Fences

Held. `git diff origin/main..HEAD -- NAMESPACE` is empty; no diff touches nbinom2, structured, or spatial code paths; no Phase 3 (degenerate-fit screen) or Phase 4 (EDA surface) files appear in the 15 changed files (`R/fit-multi.R`, `R/vgh-verify.R`, `R/vgh-warmstart.R`, `dev/vgh/*`, and `docs/dev-log/*`) — the fenced items were not touched.
