# Plan vs actual — #856 per-trait `sigma_eps` (2026-07-30)

**Reconciler:** Melissa (S6), Sonnet · low–med. **Plan:**
`~/.claude/plans/validated-kindling-meerkat.md`. **Branch:**
`claude/856-sigma-eps-archaeology-20260730`, worktree
`/private/tmp/gllvmtmb-856-sigma-eps`. **Snapshot:** commit `d404d94b`
(2026-07-30 20:43:03 -0600), 18 commits since fork point `16448745`; 3 files
uncommitted at snapshot time (background work still live — see axis 2/6).

## Summary

Process fidelity was high: the SLICE TABLE's content was delivered (recon,
failing test, engine+guard, consumer surface, docs, adversarial gate,
CI-visible regression tests), the DEFER fence held at the file level, models
matched the plan where agents were actually dispatched (Haiku 4.5 / Sonnet 5 /
Sonnet 5 / Opus 5, confirmed directly by each agent), smoke-first was honored,
and the maintainer merge gate was respected (nothing merged, no PR opened).
The adversarial gate's DO-NOT-SHIP verdict and the three fixes that followed
are **adaptive**, not scope creep — argued below.

Two real process gaps: S4 (mechanical verify) was never dispatched as its own
agent and its deliverable (full suite + `--as-cran`) is still not confirmed
complete as of this snapshot; S5 ran without checking S4's state at all
(confirmed directly by S5). Both trace to routing, not to the content of the
work.

**The material finding is external to the plan's own mechanics.** GitHub
issue #856 was closed by the maintainer (itchyshin) at 2026-07-31T00:29:03Z —
comments at `#issuecomment-5137707329` and `#issuecomment-5137725863` — titled
"Correction — this issue is largely WRONG. Filed on a false premise," arguing
the scalar `sigma_eps` is a **deliberate** identifiability choice (`sd_g[t]^2
+ sigma_eps^2` identified only as a sum) served by the existing per-trait
`theta_diag` under the documented `indep(0+trait|g)` grammar, not an
incidental capability gap. That is the precise opposite of this branch's
central verdict (commit `65430b38`, "the pooled scalar sigma_eps is
INCIDENTAL"). Cross-checked precisely: both comments landed **~20–23 minutes
before this branch's first commit** (`65430b38` at 2026-07-31T00:49:26Z) and
before the plan file's own last-saved mtime (00:41:46Z). The correction
appears to originate from a concurrent, unrelated lane —
`docs/dev-log/audits/2026-07-30-scale-constant-class-sweep.md` lives on
`claude/scale-equivariance-harness-20260730` (and
`claude/fix-loading-start-scale-20260730`, already flagged DO-NOT-MERGE in
`CLAUDE.md`), not on this branch. Ada (orchestrator) independently confirmed
this timing and volunteered the sharper self-diagnosis: Ada's own archaeology
had already found the maintainer's point 1 (sigma_eps IS documented, at
`R/unique-keyword.R:127`) mid-arc and treated it as a footnote rather than a
reason to stop and re-ask GitHub directly. Nothing is merged and no PR was
opened, so nothing public is at risk — but the branch's ~3,300-line arc, and
the after-task report that presents it, rest on a premise the maintainer had
already withdrawn before the work began. This is recorded under axes 4–6
below and is item 1 of "what to carry forward."

## Deviations

| axis | planned | actual | tag | owner |
|---|---|---|---|---|
| 1 — scope | DEFER fence: no #855, #851 scale-constant class, logLik Jacobian, or back-transform surface | Confirmed clean at `git diff 16448745..HEAD`: no touch to `R/extract-sigma.R` (back-transform surface) or any #851/#855 file. One `git diff` hit for "Jacobian" is a pre-existing, unchanged context line (`src/gllvmTMB.cpp` lognormal density's own change-of-variables term), not new work | **adaptive** (fence held) | Ada |
| 2 — evidence | `dev/scale-equivariance-check.R` before/after, "not expected to move" | Run both blocks; after-task (`21948a2a`) records k=100/k=5000 output identical to the `origin/main` baseline in the script header | **adaptive** | — |
| 2 — evidence | `test-sigma-eps-autosuppress.R` passes unchanged | Confirmed twice independently: S5's review (§6, git log/diff empty for that path) and the after-task's own re-check; 7/7 pass | **adaptive** | — |
| 2 — evidence | VERIFY: `devtools::test()`, then `--as-cran`; SLICE TABLE S4←S3, S5←S4 | Full suite / `--as-cran` still **not confirmed complete** at snapshot (`d404d94b`); after-task §4 explicitly says PENDING as of `21948a2a`. S5 itself states directly: "I did not run against an S4-green build, and I never checked S4's state... that link does not exist." A background `devtools::test()` run was reported in progress via S2-engine at time of writing, with 3 files uncommitted (2 test-oracle updates + the after-task doc) | **drift** | Ada (gate design) + Rose (closeout — report not yet closeable) |
| 3 — routing | S3 = its own Agent dispatch, Sonnet·medium | Folded into S2-engine's already-warm context via a same-conversation follow-up message — no new Agent/Task call. Confirmed directly by S2-engine. The plan's own Receipt-fields text pre-authorized "send S3 corrections back to S2's agent"; folding the whole slice in (not just corrections) extends that but doesn't contradict it | **adaptive** | Ada |
| 3 — routing | S4 = its own Agent dispatch, Haiku·low | Never separately dispatched. Mechanical verification was absorbed ad hoc by the orchestrator ("Ada... independent verification" per after-task role list) and by S2-engine's background test run — and per the row above, still isn't finished | **drift** | Ada |
| 3 — routing | S0 Haiku·low / S1 Sonnet·medium / S2 Sonnet·high / S5 Opus·high | Confirmed directly by each agent on request: S0-recon = Haiku 4.5 (`claude-haiku-4-5-20251001`); S1-test = Sonnet 5; S2-engine = Sonnet 5; S5-adversary = Opus 5 (1M context, `claude-opus-5[1m]`). All match. Every commit's `Co-Authored-By` trailer reads "Claude Fable 5" regardless of slice — this reflects the **orchestrator's own commit identity** (confirmed: S0-recon and S1-test both state they never ran `git commit` themselves), not the dispatched agent's model. Orchestrator running at Fable-5 (ceiling) for orchestration matches both the plan's own Phase 0.3 roster ("Opus 5 / Fable 5 (ceiling)") and AGENTS.md's routing doctrine ("Opus for orchestration... where being wrong is expensive") | **adaptive** (no drift) | — |
| 3 — routing | FAN-OUT BUDGET: 6 new children max, 1 ceiling max | 4 distinct new children confirmed live (S0-recon, S1-test, S2-engine, S5-adversary), 1 ceiling (S5) — under budget on count, but 2 of the 6 planned slots (S3, S4) were never separately dispatched rather than economized elsewhere | **adaptive** re: budget ceiling; see the two rows above for the shape deviation | Ada |
| 4 — safety gates | Phase 0.25 sweep receipt present and non-vacuous before decomposition | Present, substantive (4 rows, git/twin/brain citations). Structural gap, confirmed by Ada directly: the sweep template has rows for repo git state, twin repos, brain, and external prior art, but **no row for live issue/PR state on the subject being worked**. Ada: "I ran the gate the plan specified; the gate was incomplete." This is the proximate cause of the headline finding above | **drift — against the method/template, not against execution** (Ada's framing, adopted) | Ada (owns the sweep template / ultra-plan method) |
| 4 — safety gates | SMOKE-FIRST: S1's failing test must fail on `main` before S2 starts | Confirmed: commit `17d656d6` states "The test FAILS on main, as it must," with the reproduced failure message; chronologically precedes `b986c779` (S2) | **adaptive** | — |
| 4 — safety gates | Maintainer merge gate: implement + push + report, do not merge | Confirmed: `gh pr list` for this branch returns empty (no PR exists at all); after-task explicitly states not merged | **adaptive** | — |
| 5 — public claims | No coverage/interval claim moves; NEWS entry fenced | NEWS entry (diff on `NEWS.md`) describes a capability change and its costs (boundary-collapse disclosure, `confint_inspect` index break, per-trait auto-suppression), not an interval/coverage guarantee. Technically within the letter of the fence; committed to the branch only, not merged, not released | **adaptive** (narrow reading) | — |
| 5/6 — public claims / handoff | (not anticipated by the plan) | The premise the NEWS entry and the whole arc rest on was withdrawn by the maintainer ~20 min before the arc's first commit (see Summary). Nothing merged, so no released claim is wrong — but the after-task and NEWS entry, as currently drafted, present the promotion as settled when the maintainer's own record says the opposite | **drift** (root cause = the axis-4 row above; consequence is here) | Rose (closeout — do not present as ready) + domain reviewer (adjudicate the identifiability argument on its merits) + Shinichi directly (only he can reconcile his own two positions) |
| 6 — handoff | After-task issue ledger should reflect current state | Committed after-task (`21948a2a` and earlier) §7a states "#856 ... Left OPEN pending maintainer review and merge." Live GitHub state is **CLOSED**. The committed record is stale as of this snapshot | **drift** | Rose |
| 6 — handoff | #622 corrected by one comment, issue left closed | Confirmed via `gh issue view 622`: a comment matching the after-task's description (clause two never implemented) is posted, `issuecomment-5137858662` | **adaptive** | — |
| 6 — handoff | Everything committed and pushed | True at every commit boundary checked, but **not** at the final snapshot: 3 files uncommitted (`docs/dev-log/after-task/2026-07-30-856-per-trait-sigma-eps.md`, `tests/testthat/test-family-gamma.R`, `tests/testthat/test-gaussian-reml.R`) — live, in-progress work (test oracles being made trait-aware; not abandoned cruft), not yet landed | **unclear** — reconciliation is a snapshot against a moving target; needs a follow-up check once the background run finishes | Ada |
| 6 — handoff | S0's consumer map: "update every scalar-assuming reader" | `R/extract-sigma.R:123` (`sigma_eps <- as.numeric(fit$report$sigma_eps %||% 1)`) and `R/check-identifiability.R:295-296` are SCALAR-ASSUMING sites from S0's own audit that are not in the "MUST CHANGE" shortlist, not in the after-task's "Files Changed" list, and — unlike `R/julia-bridge.R`, which has an explicit "checked, deliberately not changed" note — have no recorded justification either way | **unclear** | domain reviewer (Gauss/statistical-reviewer hat) |

**On the mid-course change (adversarial DO-NOT-SHIP → 3 fixes, argued as
requested):** **Adaptive.** The plan built S5 explicitly for this outcome —
"the load-bearing gate... default to NOT-DONE" — and the Risks section named
the guard as "the hazard... S5 exists for this." All three fixes
(`95cfa10a`, `95e41882`, `0e633f5b`) stay inside the DEFER fence (row 1
above), are each independently re-measured against a recompiled pre-fix
worktree rather than asserted, and are transparently narrated in both their
own commit messages and after-task §2/§8. Two were S5's named "blockers"
(items 7, 4); the third (item 3, mixed-family warning) was S5's "non-blocking
but owed" and follows an existing in-repo precedent
(`link_residual_per_trait()`'s NA+warn pattern) rather than inventing new
design. A fourth follow-up (`d404d94b`, CI-visible regression tests)
converts S5's own coverage caveat (item 6: load-bearing tests double-gated
out of routine CI) into two `skip_on_cran()`-only fixtures — closing a gap
the gate raised, not opening a new one. No new agents were dispatched for any
of this (S2-engine's existing context handled it). Elapsed time from first
commit to the last one reviewed here is under 2 hours, inside the plan's
4–6 h estimate. This is the ceiling gate doing its job, recorded honestly,
not scope creep.

## What to carry forward

1. **Do not open a PR or present this arc as ready for review.** GH #856 was
   closed by the maintainer as "filed on a false premise," directly
   contradicting this branch's central verdict, ~20 minutes before the
   branch's first commit. Reconcile against
   `docs/dev-log/audits/2026-07-30-scale-constant-class-sweep.md`
   (`claude/scale-equivariance-harness-20260730` @ `aa22516d`) and get
   Shinichi's explicit read before any further action.
2. **Finish S4 for real**: a full `devtools::test()` + `--as-cran` pass, then
   update after-task §4 (still PENDING) and §7a's issue ledger (still says
   #856 is OPEN; it is CLOSED) before the report is treated as closed.
3. **Add "live issue/PR state for the subject issue" as a standing Phase 0.25
   sweep row** in the ultra-plan method — Ada's own request, and the
   proximate cause of item 1.
4. **Resolve the two unclear consumer-map sites**: `R/extract-sigma.R:123`
   and `R/check-identifiability.R:295-296` — confirm reachability/safety or
   document why they're intentionally untouched, the way `julia-bridge.R`
   already is.
5. **Minor cleanup**: remove or keep-with-a-reason the leftover comparison
   worktree `/private/tmp/gllvmtmb-856-PREFIX` (detached at `16aeb208`) once
   it's no longer needed for re-verification.
