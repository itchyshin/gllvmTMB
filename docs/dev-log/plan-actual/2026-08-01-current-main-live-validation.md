# Plan-versus-actual: current-main live validation

**Baseline:** `074387156888c915d07de445988c516b5f99d09f`
**Disposition:** bounded arc completed as a diagnostic-and-one-repair lane; it is **not** a release, merge, or public-claim verdict.

| Axis | Approved plan | Actual and reconciliation | Deviation owner / tag | Scope changed? | Evidence-quality effect |
| --- | --- | --- | --- | --- | --- |
| Scope | Classify two profile checks, the spatial convergence assertion, and ellipse snapshot; run the ordinary check; admit at most one narrow repair. Exclude snapshot acceptance, VA-09, AGHQ, scale-start, PR, merge, release, and public claims. | The two profiles were non-reproduced in two clean processes; spatial line 54 reproduced 3/3, but an instrumented equivalent fit converged and the built-package route passed; ellipse stayed an observational classification. The ordinary check exposed one independent, two-file `stats::weights` S3-registration defect and that single repair was made. | Ada — `adaptive`: the pre-authorized one-repair gate was exercised for a newly discovered, localized defect. | No: the cap and exclusions held. | Increased: direct, built-package, and base-only namespace evidence distinguish the repair from the deferred numerical issue. |
| Evidence | Use immutable clean worktrees and admitted fresh-process evidence; preserve failures and separate known noise from regressions. | Baseline was pinned and clean. Profile passed twice. Spatial failed 3/3 at line 54; retained diagnostics localize a test-execution/object-observation contradiction, not a numerical cause. Baseline ordinary check was **1E/4W/4N**; repaired branch was **1E/1W/2N**. The ellipse diff is exactly four 0.01-unit SVG coordinates. | Numerical reviewer — `adaptive`: retained candidate SVG and the built-check comparison added discriminating evidence after the planned direct diagnostics. | No. | Increased; no failure was erased or recast as a pass. |
| Routing | Keep a clean, separate Codex diagnostic lane; scouts may inspect but parent owns executable evidence. | A read-only Luna provenance child could not write or create its R temporary directory; parent repeated the checks with `TMPDIR=/private/tmp`. One dirty-primary-checkout profile attempt was quarantined; only clean detached runs were admitted. The Luna audit briefly cited a stale receipt/brief contradiction, then parent ground truth corrected it. | Ada — `adaptive` (read-only provenance write failure); Numerical reviewer — `adaptive` (dirty attempt quarantined); Luna V2 scout + Ada — `drift` (stale read and current-file correction). | No. | Parent repetition and quarantine preserved or improved quality; the stale citation was not admitted evidence. |
| Safety gates | Keep sources clean, retain check artifacts outside source, use one localized repair only after cause and closure checks, and do not overwrite snapshots. | Initial and retained check reruns contaminated the source with `main-check.log`; both were quarantined/moved out and a clean authoritative rerun was used. Repair touched only `R/va-methods.R` and `NAMESPACE`; disposable install, base-only `loadNamespace()`, VA routing oracle, and `git diff --check` passed. No snapshot was accepted. | Grace + Ada — `adaptive`: the check owner and parent corrected artifact retention after contamination. | No. | Increased: authoritative counts exclude the self-inflicted NOTE while retained contaminated artifacts remain inspectable. |
| Public claims | Do not publish, claim readiness, or alter public API/docs/navigation in this arc. | No NEWS, README, vignette, Rd, API, likelihood, grammar, validation-debt, issue, PR, merge, release, or capability action occurred in this lane. Its pkgdown check found `gllvmTMB_va-methods` absent on pinned baseline `07438715`; PR #882 independently fixed that exact omission and merged to remote `main` during closeout. This branch did not absorb or duplicate it. | Ada — `adaptive`: the closeout finding was superseded upstream by a separate owner while the arc stayed immutable. | No. | Narrows attribution and prevents duplicate repair; it does not weaken the live numerical classifications. |
| Handoff state | Leave a durable, bounded record with residuals explicit; do not silently transfer excluded work. | Receipts 00–07, the after-task report, and this reconciliation preserve the baseline, classifications, repair evidence, quarantines, and limits. Primary checkout was untouched. | Ada — `adaptive`: the repair branch now carries two code files plus closure records, still within the five-file cap. | No. | Increased: the next owner can reproduce the exact remaining surfaces without interpreting contaminated runs. |

## Melissa disposition

**RECONCILED WITH CONTROLLED ADAPTATION.** The only implemented change is the planned-at-most-one, evidence-localized `stats::weights` registration repair. All other apparent deviations either improved evidence handling (`adaptive`), were corrected before admission (`drift`), or remain explicitly unresolved (`unclear`). Nothing in this reconciliation supports a green check, release/merge readiness, snapshot acceptance, or a public capability claim.

## CARRIED-OVER (exact items)

1. Spatial direct `load_all()` route: investigate the 3/3 line-54 `fit_health$converged` failure versus the healthy equivalent fit and built-package pass by comparing loaded namespace/DLL, object construction, RNG state, and health fields; do not change convergence semantics, starts, tolerances, optimizer, or seed before localization.
2. Dispatcher ellipse snapshot: retain the four-coordinate 0.01-unit diff as `INTERPRETABLE_VISUAL_DRIFT`; no acceptance or rounding/geometry policy is authorized.
3. Profile-derived curves: two clean non-reproductions on this host/SHA do not close issue #837 or establish profile coverage.
4. Package checks: repaired branch remains red (**1E/1W/2N**) because of the unchanged ellipse snapshot; repository-index/fixture and environmental diagnostics remain classified, not repaired.
5. Pkgdown navigation: PR #882 already added `gllvmTMB_va-methods` to `_pkgdown.yml` on remote `main`; do not duplicate it. Main CI for `0b898266` was still in progress at Shannon's checkpoint. Do not rebase or merge during this arc; reconcile only the append-only `docs/dev-log/check-log.md` overlap after the pinned closeout and a CI/ownership checkpoint.
6. Excluded lanes remain excluded: Poisson VA-09 recovery, AGHQ/scale-start design, cross-platform evidence, PR #881, merge/release readiness, and all public claims.
