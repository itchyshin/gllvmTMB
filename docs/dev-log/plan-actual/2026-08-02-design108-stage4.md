# Plan vs Actual — Design 108 Gate A Stage 4 (probit AD-safety spike)

**Reconciler:** Melissa (Phase 4.5) · **Date:** 2026-08-02
**Plan:** `/Users/z3437171/.claude/plans/recursive-moseying-llama.md`
**Actual:** worktree `/private/tmp/gllvmtmb-design108-stage4`, branch `claude/design108-stage4-probit`,
commit `f8ae751b` (parent `910ebd54`, the pinned `origin/main` at plan time).

Scope note: `git diff origin/main..f8ae751b` today shows 15 files because `origin/main` has since
advanced by 2 unrelated commits (`d58552e8`, a Codex spatial-docs PR, `#895`). The commit's true
diff is `git show f8ae751b --stat` — 12 files, 1843 insertions / 46 deletions — which is what this
reconciliation checks against.

| # | PLANNED | ACTUAL | TAG | OWNER |
|---|---|---|---|---|
| 1 | Slice table: S2 (primitive) and S4 (AD-safety verify) as one Opus child; S3 (wiring) a separate Sonnet child; S5 (toy) a separate Sonnet child | S3's wiring absorbed into the S2/S4 child. S5's toy test (`test_that("a Bernoulli-probit toy fit tracks the shipped Laplace probit engine")`, `test-va-probit-adsafety.R:405`) also folded into the same file and re-verified by the V2 adversarial pass rather than run as its own slice/file | **adaptive** | Ada |
| 2 | FAN-OUT BUDGET: ceiling (Opus) = 1 | 2 Opus children used: the S2+S4 builder and the V2 adversarial reviewer (Rose). Reason recorded in after-task §11 ("Ada — the fan-out budget was exceeded by one ceiling child... Recorded, not taken silently: the AD-SAFE verdict decides whether ~26–42 days happen, and letting the author's own priors grade it is the failure D-43 exists to prevent") | **adaptive** — the recorded reason is sufficient: it names the specific risk (self-graded, programme-deciding verdict) that D-43 exists to prevent, not a generic "needed more compute" excuse | Ada |
| 3 | P1 wall-clock ~15–25 min at 96 workers | Plan's own EXECUTION LOG already revises this before launch — final grid grew from the original P1 scope to 3,600 cells (Part A 2,880 + Part B 720) after the sigma_lambda/seed-count fix (row 5 below), with the 15–25 min estimate carried forward from the *revised* grid, not the original. External report (user) states P1 was still ~89% complete after ~40 min wall-clock — no completion artifact exists in the worktree to confirm final wall-clock (`dev/design108-stage8/` contains only the harness + a new untracked `analyse-silent-divergence.R`, no results file) | **unclear** — the estimate-vs-actual gap is plausible generic underestimation of I/O-bound grid work, but nothing in the repo lets me confirm P1's actual finish time or outcome; Stage 8 results are explicitly out of this arc's scope per §12 ("not folded into VA-12; reported separately") | domain reviewer (Fisher) for the timing; not blocking for R1 since Stage 8 is a separate deliverable |
| 4 | Verification item 5: register row `partial`, evidence path attached; fence stays refused | VA-12 row added, status `partial` exactly (`docs/design/35-validation-debt-register.md:734`). Fence comment-only in `R/integration-fence.R` (9 lines added, no admitted-set change); `dev/design108-stage4/va-claim-fence.sh HEAD` re-run independently → 0 leak lines, exit 0, confirming the claim against the actual committed state (not just `origin/main`) | **adaptive** — matches the plan precisely; "comment added, fence not widened" was the explicit decision, correctly executed and independently re-verifiable | — |
| 5 | Plan approved with the original P1 DGP (loading SD 0.7, n 1500, 3 seeds, no sigma_lambda axis) | Design changed post-approval: `sigma_lambda ∈ {0.7, 3.0}` added, n 1500→1600, seeds 3→30, `p` axis restored, cells 780→3600 — caught pre-launch because the original design would very likely have concluded "the ridge fixes it" by never visiting the regime where `#847` already measured the ridge to fail | **adaptive** — the EXECUTION LOG documents the specific pre-launch defect found and the fix, in-band before the campaign ran, not after a bad result | Ada / Fisher |
| 6 | Design docs stay accurate, or corrections get applied | Two corrections identified and left **unapplied, explicitly recorded**: Design 105 §1.3 / 108 §6's stated H=61 SD figure (says ±15.7, actual max node is ±14.4985), and §6's stale "add family == 3" instruction (code 3 is already nbinom2; probit correctly claimed code 4 instead) — both listed under after-task §10 "Owed design-doc corrections... outside this arc's write scope" | **adaptive** — correctly diagnosed and not silently absorbed into an out-of-scope doc edit; flagged for a separate hygiene pass as the plan's own philosophy requires ("Separately finishable... flagged, not silently absorbed") | Rose (closeout/claims — should ensure these get filed) |
| 7 | Closure: after-task report + register row + Melissa reconcile | Commit `f8ae751b` is staged on the feature branch, **not pushed, no PR opened** (`git ls-remote` / `gh pr list --head claude/design108-stage4-probit` both empty); after-task report explicitly states "Not committed at time of writing" in its own header even though a commit now exists — a stale header line, not a false claim about push state | **adaptive** — the plan's DELIVERABLE was "landed as a fenced, opt-in spike," which a local commit on a feature branch satisfies; pending a maintainer merge decision is consistent with `CLAUDE.md`'s merge-authority rules (probit is a TMB/family change = high-risk, needs maintainer sign-off before merge) — correctly NOT self-merged | Rose (to update the after-task header's stale "not committed" line before/at merge) |
| 8 | Slice-table artifacts: each slice produces its named file | S1 recon → folded into plan text (§"S1 VERDICT", no separate file, as intended for a Haiku scout slice). S2 → `inst/tmb/gllvmTMB_va_r3.cpp` primitive, present. S3 → `R/va-r3-proto.R`, `R/va-routing.R`, `R/integration-fence.R` wiring, present. S4 → `tests/testthat/test-va-probit-adsafety.R`, present (460 lines, 56 source-level `expect_`/loop constructs; register claims 114 *runtime* assertions, consistent with the file's `for`/`vapply` loops over multi-cell grids — not independently re-counted, since re-verifying the maths is out of this reconciler's scope). S5 → toy test folded into the S4 file (see row 1). P1 → `dev/design108-stage8/` harness (README, `laplace-silent-divergence.R`, `analyse-silent-divergence.R`); results not in-tree (LOCAL per D-50, as required). V1/V2 → no separate artifact file; findings folded into the after-task report and commit message (six wording-defect corrections, 3,744-cell break grid, analytic re-derivation) | all named deliverables traced to an actual artifact except the standalone S5 test file and a P1 results file, both explained above | **adaptive** (S1/S5/V1/V2 folding), **unclear** (P1 results, row 3) | — |
| 9 | DEFER + FENCE: no Stage 3/5/6 code, no export, no `method=`, no NEWS/README/article claim, no VA `mi()` | Verified directly, not assumed: `git diff origin/main f8ae751b -- R/ inst/` has zero lognormal/ordinal_probit/Stage-5/Stage-6 hits; `git diff ... -- NAMESPACE` empty (no export); `git diff ... -- NEWS.md README.md` empty; the one `mi(` hit in the diff is a *register-row prose* reference ("Not VA mi()"), not code; the one `method` hit is the pre-existing `eval_method` plumbing param, unrelated to a public `method=` argument | **adaptive** — every fence held; confirmed by direct diff inspection, not by trusting the after-task's own claim | — |
| 10 | Phase 0.25 sweep receipt: present, each row citing an actual command | Present in the plan document itself, six rows, each citing a concrete command/query (`git status -sb`, `gh pr view`, `git show origin/main:...`, `lane_preflight.sh`, `ssh` probe) and a specific finding, not a bare "none found" | **adaptive** (receipt passes) | — |

## Recurring drift classes (for the monthly ledger)

1. **Slice-table absorption without a table update.** Twice in this arc (S3 into S2/S4, S5 into S4)
   a planned standalone slice was folded into another agent's output. Both times the reasoning was
   sound and recorded in prose (after-task §3a, §11), but the plan's slice table itself was never
   struck through or annotated — a reader comparing only the table to the file list would see three
   "missing" files. Recommend: when a slice is absorbed, add a one-line strike-through in the plan's
   slice table at absorption time, not just a narrative mention in the eventual after-task report.
2. **Stale "not committed" language surviving into a landed after-task report.** The after-task
   header still reads "Not committed at time of writing" under a commit hash that plainly exists.
   Minor, but it is exactly the kind of receipt a future audit trusts literally.
3. **Campaign wall-clock estimates keep being optimistic** (P1: 15–25 min planned, ≥40 min actual
   per report) even after a mid-flight design correction already grew the grid 780→3,600 cells. The
   grid-size correction was caught pre-launch; the wall-clock re-estimate was not redone after that
   correction, which is a small underlying gap in COMPUTE cost estimation.

## Overall verdict

No unjustified drift found: every material deviation from the plan is either explicitly recorded
in-band (fan-out budget, DGP redesign, absorbed slices, deferred doc corrections, hold-for-maintainer
merge) or independently re-confirmed against the commit and register (fence holds at `HEAD`, DEFER/FENCE
items untouched, VA-12 = `partial`); the one open item is P1's actual completion state, which sits
outside this arc's committed scope and is not blocking.
