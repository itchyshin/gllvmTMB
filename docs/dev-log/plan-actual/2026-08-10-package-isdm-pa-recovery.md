# Plan versus actual: package-native iSDM PA recovery

## Planned

Freeze a developer-only PA known-truth protocol, run one local smoke, then a
30-fixture Totoro campaign (20 ordinary, five disconnected, five weak-overlap),
and close only with a named information/recovery verdict.  Deferred: count,
comparator, spatial fields, source data, empirical fits, public API/docs, and
article claims.

## Actual

- **Scope:** matched.  The private package-native route was used; no deferred
  model/data/public scope was entered.
- **Evidence:** local smoke and Totoro retry completed.  The first smoke and
  first remote deployment were retained failed preflights, not overwritten.
  The retry retained all 30 fixtures.
- **Verdict:** adaptive HOLD.  All 20 ordinary fixtures were eligible but 0/20
  met the frozen joint recovery rule (18/20 required).
- **Verification:** D-43 composition was two fresh Terra reviews plus one Sol
  adversarial review.  It agreed on the ordinary HOLD and found a false
  disconnected attack plus incomplete/mutable root receipt.
- **Repair:** the future harness now makes the disconnected support truly
  disjoint, requires complete attack panels, writes a file manifest, rejects
  summary overwrite, and rejects an existing Totoro result root.  No rerun was
  authorised or performed.
- **Routing:** the campaign used Totoro with 30 one-core workers, below the
  150-core limit, with BLAS/OpenMP pinned to one thread.

## Reconciliation

**Adaptive deviations:** a first Totoro launch failed before fitting because an
isolated R library hid `assertthat`; the retry used a separate worktree/result
root after a narrowly committed library-path correction.  A local DGP/extractor
smoke defect was similarly retained and repaired before remote execution.

**Drift prevented:** the completed attack panel did not satisfy its declared
disconnected-support definition.  It is explicitly invalidated rather than
reported as a diagnostic success.  The code repair applies only to a future,
fresh campaign.  No scientific or public claim was widened.

## Next boundary

`LANE: START A FRESH TASK` — a fresh approval must choose and freeze the next
information design before running another recovery campaign.  The first-read
files are the after-task report, PA protocol, and runner; do not redo the old
standalone G2/G2a work or treat this ordinary HOLD as count/spatial evidence.
