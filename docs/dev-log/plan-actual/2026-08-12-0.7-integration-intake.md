# Plan actual: refreshed 0.7 integration intake

## Planned

Reconcile live GitHub/source state, map all plausible release candidates to
their evidence and claim boundaries, independently review the result, and stop
with one ranked implementation decision.  No package/release/compute mutation
was authorised.

## Actual

- Rebased the prior #956 intake branch from its old `cb312689` base onto live
  `origin/main` `2942b654`.
- Re-read live #345 and open PRs #955--#960, the 0.6 exact artifact receipt,
  issue ledger, validation register, current #872 after-task evidence, and
  installed-byte diff.
- Found the decisive artifact discontinuity: current main includes installed
  LA-MSPL changes, so the 0.6 tarball and three-OS receipt cannot support a
  current 0.7 candidate.
- Updated the release decision matrix to a source-selection HOLD.  The
  maintainer selected a two-track boundary: retain LA-MSPL as a separately
  adjudicated opt-in lane while keeping ordinary native-Laplace claims narrow.

## Material deviation

The earlier packet assumed its docs-only branch described current main.  It did
not.  This refresh replaces that obsolete positive applicability statement with
the evidence-backed HOLD; no release-scope expansion was attempted.

## Reconciliation verdict

**PASS WITH HOLD.**  The next state transition is the active MSPL lane's
bounded admission plan, followed by a new artifact/platform ladder for the
exact two-track source candidate.
