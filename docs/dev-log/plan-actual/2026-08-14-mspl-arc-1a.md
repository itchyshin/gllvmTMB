# Plan versus actual — MSPL Arc 1A

**Plan:** `docs/dev-log/plans/2026-08-14-cursor-mspl-one-arc-ultra-plan.md`
**G0:** approved 2026-08-14 (stacked Cursor branch + new PR; do not wait on #961)
**Branch:** `cursor/mspl-arc-1a-provenance`

| Slice | Planned | Actual | Drift |
|---|---|---|---|
| S0 | Recon of `estimator_id` 0/1/2 sites + MSPL fixtures | Done. Sites: `R/fit-multi.R` (assign 0/1/2), `R/mspl.R` (prepare list field 1), `src/gllvmTMB.cpp` (DATA integer; unread for edit), `tests/testthat/test-mspl-api.R` | none |
| S1 | New `R/estimator-provenance.R` table + resolver, no behavior | File written with table, resolver, tape helper, attach helper | S1 and S2 landed in one sitting; table existed before the wire |
| S2 | Wire `fit-multi.R` / VA path; attach provenance | `estimator_id` derived via adapter; attach on Laplace/AGHQ fits and on `gllvmTMB()` including VA | `R/mspl.R` left unchanged (prepare still returns `estimator_id = 1L` as a list field, not a tape write) |
| S3 | Parity tests | New `test-estimator-provenance.R`; two expects added to existing ML-identity test | did not duplicate the full spatial/link grid in the new file; `test-mspl-api.R` remains that grid |
| S4 | Targeted run; stop on drift | LOG: provenance 75/75; mspl-api 223/223; 0 fail / 0 warn / 0 skip | none |
| S5 | Gauss/Noether/Rose, preferably Claude Opus | Written review in the after-task (Opus not spawned from this conductor) | review venue, not verdict |
| S6 | After-task + check-log + stacked PR; do not merge | this closeout | PR URL filled after `gh pr create` |
| V | Mechanical verify | no C++ / NEWS / register / foreign-lane paths in the diff | none |
| R | This file | this file | none |

**Not done (declared):** Arc 1B; Arcs 2–8; NEWS; register promotion; C++ comments; julia-path provenance; `R CMD check`; merge to main.

**Surprise that did not invalidate the plan:** historical `LOOP/` on `main` is the 0.6 release kit. Replacing it on this stacked branch is the `/goal` scaffold, not a 0.6 rewrite.
