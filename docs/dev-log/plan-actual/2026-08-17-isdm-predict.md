# Plan versus actual — iSDM predict() probe arc (2026-08-17)

Plan: `~/.claude/plans/read-agents-md-and-docs-dev-log-handover-steady-nebula.md`
(ultra-plan, S0–S9). Actual: worktree `/private/tmp/gllvmtmb-isdm-predict`,
branch `claude/isdm-predict-20260817`, 8 commits ahead of `origin/main`
(`def280fe`..`84bc8fb3`), PR #1125 (open) + PR #1113 (merged, S1). Reconciler:
Melissa (Sonnet, S9). Standard applied: a slice is DONE only against a named
artifact (commit, file, test run) — a document mentioning a slice is not
evidence it ran.

## Deviation table

| Deviation | Axis | Tag | Owner | Note |
|---|---|---|---|---|
| S4 fork took the hybrid branch: S4a (tests) ran, S4c (scoping doc) ran, S4b (map article) did NOT run | Scope | adaptive | Ada (scope) | Plan's own DISCIPLINE clause ("verify = probe evidence before any claim") required exactly this: the probe (S3) returned MIXED, not DEFENSIBLE, so the map article is the one slice the plan's own fork logic forbids. No `vignettes/articles/isdm-prediction-maps.Rmd` exists in the diff; `docs/design/126-isdm-prediction-api.md` does. Matches plan row "S4c IF NOT DEFENSIBLE" run alongside the still-viable S4a (in-sample core was certifiable even though newdata was not) — a within-plan fork the table anticipated, not an ad hoc one. |
| S5 / S5b gated on a live AskUserQuestion approval mid-execution rather than running unconditionally | Safety gates | adaptive | — (gate held as designed) | The plan text itself already records "ADA'S RECOMMENDATION / DECISIONS LOCKED (Shinichi answered 2026-08-17): S5 is IN, S5b added" — i.e. approval was sought and logged as part of planning, and D-139's "estimate + pre-run test + approval" contract was honoured at execution too. This is the gate functioning, not drift. |
| 7th child (Melissa, S9) spawned against a plan that names FAN-OUT BUDGET "≤6 new children ... 6/6 at cap" | Model routing | unclear | Ada (routing) | Producer fan-out matches the plan exactly at 6/6 (Haiku recon S0 at plan-time, Sonnet S4a, Sonnet S5, Sonnet S5b, Opus S6, Haiku S7). Melissa (S9, reconcile) is a 7th child. Ultra-plan convention treats completion/reconcile panels as separate from the production fan-out budget, which would make this clean rather than a deviation — but the plan's own FAN-OUT BUDGET line does not state that carve-out explicitly for this arc, so the classification is recorded as unclear rather than silently assumed clean. No corrective action indicated; flagged for Ada to confirm the convention applies here as it has elsewhere. |
| Opus adversarial verify (S6) surfaced a THIRD defect (`newdata` response applies a per-trait modal family — wrong arm) plus two attribution overclaims the producer (S4a/Ada) had not caught; verdict PASS-WITH-CORRECTIONS, R1–R8 applied in commit `84bc8fb3` | Evidence/verification | adaptive | — (gate did its job) | This is exactly what the plan's S6 slice was for ("one Opus adversarial pass on the semantics claim before any merge"). The correction commit (`84bc8fb3`, 05:41) predates PR #1125's open time (11:42) — nothing was published, register-promoted, or claimed before the corrections landed. Also resolved a minor numeric wobble the verify pass itself introduced: the after-task's assertion count reads 16 (matches current `grep -c "expect_"` = 16) because R7 added one exact-equality assertion to `test-isdm-predict.R`, superseding the check-log's earlier "15 pass" entry written before that commit — not an unresolved inconsistency. |
| Unplanned: a check-log entry was committed with the file mid-rebase, then a rebase (`384abd3e`) landed on top of it, and the entry was re-committed via amend (`315de5ea`, reflog shows `commit (amend)` following `rebase (finish)`) before push | Handoff state | adaptive | — (self-caught, self-repaired pre-push) | Not in the plan table at all. Current tree has zero conflict markers in `check-log.md` (the two hits from `grep` are unrelated historical lines quoting a conflict-marker grep command, not markers themselves). The incident never reached `origin` in a broken state — caught and repaired locally before the push that opened PR #1125. |
| Mid-plan `/ultra-plan "the arc"` invocation resolved by Shinichi's explicit choice "continue current arc" rather than triggering a re-plan | Scope | adaptive | — (explicit maintainer decision, recorded in plan) | Recorded in the plan document itself under "Coordination note — the Ayumi programme"; the alternative (pivot to Ayumi execution order) was offered and declined. No silent scope substitution occurred. |

## Clean axes

- **Public claims** — no premature publication. PR #1125 opened (11:42) strictly after the correction commit `84bc8fb3` (05:41); no NEWS/README/article claim; register row ISDM-03 states `partial` with the corrected (post-R1–R8) defect list, not the pre-correction understatement.
- **Safety gates** (remaining items) — D-139/D-157 respected (OWED-2 is a proposal document only, nothing launched, no Totoro/DRAC); D-50 respected (no Actions compute); map article fenced (S4b did not run, folded into the Scope row above); no `R/` or `src/` change anywhere in the 12-file, 1,484-insertion diff; PR #1113 merged as planned (S1); PR #1125 flagged for maintainer read despite qualifying for docs-class self-merge authority — a more conservative choice than the plan required, not a deviation from it.
- **Model routing** (remaining items) — every slice that ran used the model the table specified (Fable inline for S1/S2/S3/S4c/S8; Sonnet medium for S4a/S5/S5b; Opus high for S6; Haiku low for S7/S0); no model substitution.

## Summary

The arc tracked its plan closely. The one real scope fork (S4 hybrid, map article withheld) is the fork the plan itself specified for a MIXED probe verdict, not an improvisation — tag **adaptive**. The verification layer (S6 Opus, S7 Haiku) did exactly the job the plan assigned it: it caught a third defect and two overclaims the producer missed, and every correction landed in the same pre-publication commit, so the evidence and public-claims axes stay clean end to end. Safety gates held without exception (D-139/D-157/D-50, no `R/`/`src/` touch, article fence). The one item left **unclear** is bookkeeping, not substance: whether the reconcile child (this one) sits inside or outside the plan's stated 6/6 fan-out cap — worth a one-line clarification in the ultra-plan convention doc, not a correction to this arc. The one unplanned incident (a mid-rebase check-log commit needing an amend) was self-caught and repaired before anything reached the remote, so it cost nothing beyond one local amend.
