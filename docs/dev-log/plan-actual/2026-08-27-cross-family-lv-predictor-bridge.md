# Plan vs Actual: Cross-Family LV Predictor Bridge

Date: 2026-08-27

## Scope outcome

The bounded implementation milestone was delivered as planned: registered
native family/link rows compose in a complete-response ordinary unit-tier
predictor-informed `latent()` block; rank-2/rank-3 route health is retained;
Gaussian and lognormal have separate family-scale slots in joint fits; all
affected scale consumers, tests, help, articles, designs, NEWS, and pkgdown
navigation agree.

The broader evidence programme was deliberately not completed. The proposed
two-cell r200 campaign is 400 planned, 0 started, and 0 attempted. General
arbitrary-composition recovery therefore remains `partial`.

## Plan-to-actual ledger

| Planned slice | Actual result | Classification |
|---|---|---|
| Source/ownership and prior-work sweep | Rebased once onto verified main `870944744`; exact path lease held; prior 3,800/4,000-attempt evidence reused | matched |
| Engine/estimand audit | Identified the one-slot Gaussian/lognormal scale defect and fixed family-level dispatch; preserved `B_lv` and shared covariance as targets | adaptive, within scope |
| Rank-2/rank-3 compositional bridge | Loadings-only registered family/link rows admitted through logical-response rank; automatic `Psi` receives an additional free-parameter-versus-covariance-moment guard after independent review | adaptive correctness repair |
| All-family stress | IDs 0--15 passed in one rank-3 fit; family 16 passed inside the five-family coupled-response fit | adaptive, scientifically necessary |
| Tier-1 article and status cascade | Evaluated article, long/wide syntax, NEWS, Rd, navigation, Designs 01/03/35/61/73 completed | matched |
| r200 production recovery | Pre-run and immutable harness completed; production campaign not launched | adaptive scope cut; claim remains partial |
| Verification | Focused suites, corrected evaluated article, pkgdown, and final 21m23.1s full check passed; both the built-package harness failure and the later four-fixture identifiability failure were retained and repaired | matched with attributable repairs |
| Completion panel | Gauss/Emmy and Rose/Grace passed candidate `2350c5d`; Noether/Fisher failed its automatic-`Psi` identifiability boundary, triggering the current repair and a required fresh three-reviewer panel | adaptive correctness repair; pending re-review |
| Landing | Protected PR, three-OS CI, normal merge, exact-main verification, and lease release remain at this draft | pending |

## Safety and compute actuals

Every fit had a written estimate. All local work remained below 30 minutes.
The full-check attempts took 20m44.4s (failed attributable harness path),
20m26.9s (green before final review), 20m36.8s (four attributable stale
fixtures after the identifiability repair), 21m23.1s (green before the final
error-priority audit), and 20m12.3s (exact-current-source green: 0 errors, 0
warnings). No Totoro, DRAC, or GitHub Actions science
compute ran. GitHub Actions will be used only for package CI.

## Deviations

- The plan anticipated that a claim-bearing r200 campaign might follow a
  measured pre-run. Integration proceeded without it because the plan also
  defined a smaller implementation milestone and remote authority was not
  granted for this exact launch. This is adaptive, not drift, because claims
  remain partial and all 400 unstarted attempts are explicit.
- The family-scale repair remained one slot per active continuous family, not
  one slot per trait. This follows the locked within-family sharing contract;
  per-trait observation scales would be a new model.
- The build-package harness defect and trailing whitespace were not foreseen.
  Both were attributable, preserved, repaired narrowly, and reverified.
- The original rank rule accounted for rotational degrees of freedom but not
  the additional automatic diagonal parameters. Noether/Fisher caught the
  saturated rank-3 case before landing. The correction preserves the full
  logical-response rank for `unique = FALSE` and fail-loud rejects only
  automatic-`Psi` shapes whose free covariance parameters exceed the moments.
- A first fixture repair made the offset test loadings-only, but completion
  audit rejected that as masking an error-priority regression. The final code
  instead runs formula/design validation before the covariance dimension gate;
  the original offset test is restored and passes. This was adaptive
  correctness work, not a scope change.

## Melissa verdict

**PASS WITH DECLARED RESIDUAL.** Scope, estimand, implementation, tests, reader
claims, and safety gates match the approved bounded destination. The declared
residual is the unrun r200 recovery programme; it limits claim strength but
does not block landing the verified implementation milestone. Final success
still requires protected merge and exact-main verification.
