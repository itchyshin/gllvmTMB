# Plan vs Actual — gllvmTMB gap closure (ARC A/B/C), 2026-09-02

**Plan:** `/Users/z3437171/.claude/plans/read-agents-md-and-docs-dev-log-handover-lovely-grove.md`
**Branch:** `claude/gapclose-20260902` (worktree `/Users/z3437171/local-scratch/lanes/gllvmTMB-gapclose-20260902`, from `origin/main` `a15f9e46a`)
**Commits (11, `origin/main..HEAD`, oldest first):** `edbdc85b4` docs(parity) B0 · `a79b048bc`
docs(board) lane announce · `df2e10357` arcA(docs) plain-language + tau evidence · `e8068bfa8`
arcB(parity) ledger + tool · `b71bc167b` arcC(hygiene) · `c6d2dcc6e` arcA(code) signposting ·
`253d0e842` docs(gapclose) after-task draft + B3 issue drafts · `7141bad24` arcC(news) review fix ·
`685c03c29` arcA(docs) review fix · `510ea6037` arcB(parity) review fix · `aa704f8ed` arcA(code)
review fix (HEAD).
**Reconciler:** Melissa (this report). Not an implementation review; no escalation performed.

## Six-axis table

| Axis | Planned | Actual | Tag | Owner | Note |
|---|---|---|---|---|---|
| **Scope** | A/B/C in parallel, disjoint files, one working day; ARC D deferred; B1 depends on C1; A2 fixes "~30" bare aborts | A/B/C landed on one worktree/branch with `arcA:`/`arcB:`/`arcC:` commit prefixes instead of three integration branches; ARC D untouched (correctly deferred); RECON-0 found 495 abort/stop calls (318 bare, not ~30) and A2 was re-scoped to the user-reachable set + a package-wide ratchet; B1 dispatched before C1 landed (generator re-run after); A4 ran a second, harder dataset after the first failed to reproduce the pathology; gate `CHECK` lines switched `rg`→`grep -E` | adaptive | — | All five deviations are stated in the plan's own EXECUTION LOG at the time they occurred, with a reason (TMB compile cost, RECON-0 measurement, dependency-inversion rationale, first dataset "too easy", missing `rg` on PATH). ARC D staying out of scope matches the plan's DEFER line. |
| **Evidence / verification** | DISCIPLINE: "ledger gates re-run (`gate-check --reverify`) + one fresh Opus adversarial reviewer per arc"; slice table: VERIFY-A/B/C (Opus) + MECH-VERIFY (Haiku) as a completion set; G-ALL (`devtools::test()` + `check --no-manual`) required before a draft PR opens | Opus ran once (`verify-opus.md`, 07:56) and found 2 BLOCKING + 8 REQUIRED + 4 SUGGESTION issues, un-ticking G-A1/G-A2/G-B2; the fix round (07:58–08:24) that followed was re-verified only by the orchestrator re-running the `CHECK` shell commands in `GATES.md` — no second Opus pass exists on disk. `MECH-VERIFY` (`mech-verify.md`, 07:39) is stale: it still cites the pre-fix-round commit `c6d2dcc6e` as HEAD and was never re-run after the four review-fix commits (`685c03c29`…`aa704f8ed`). `G-ALL` never completed: `.unlazy/gapclose/GATES.md` still shows `[ ]` with `EVIDENCE: pending`, and the only capture of it, `scratchpad/g-all/test.log`, is 20 lines long — the suite starting up, no `FAIL` count, no completion | **drift** | domain reviewer (+ Rose) | The plan named a real literal tool `gate-check.mjs --reverify` that does not exist anywhere in this repo or `~/.claude`/`~/shinichi-brain` (boilerplate carried over from the ultra-plan template, not itself material) — the substantive re-check (running the actual `CHECK` commands) did happen, so that part is not counted as drift. What is counted: two BLOCKING findings were fixed and marked done by the same process that shipped them, with no independent adversarial confirmation, and the full-suite gate the plan gated the draft PR on was never finished. |
| **Model routing** | 6 producer children (RECON-0 haiku, A1/A3/A4/C1/B1 sonnet, reused for A2/B2-B3/C2) + 3 completion (MECH-VERIFY haiku, VERIFY opus, Melissa sonnet) = 9 agents in the checkpoint, stated explicitly in the FAN-OUT BUDGET | Session's agent roster (`recon-0-abort-inventory`, `a1-a2-signposting`, `a3-plain-language`, `a4-tau-prerun`, `c1-hygiene`, `b1-b2-ledger-parity`, `mech-verify-gapclose`, `verify-opus-gapclose`, + this Melissa agent) = 9, one-to-one with the plan's roles and models; the fix round reused the same four producer agents (A1, A3, B1, C1) rather than spawning new children, matching the "no new children" instruction. Two additional agents in the roster (`scout-forgotten-07`, `scout-reverse-parity`, `scout-user-holes` — 3 scouts) and `rose-boole-plan-review` belong to Phase 0 planning, outside the checkpoint's stated 9 | none | — | Routing matches the plan's stated budget exactly once the pre-execution scouting/review agents (correctly outside the "9 agents total in the checkpoint" claim) are excluded. |
| **Safety gates** | Pre-authorised: scoped edits in ARC globs, `devtools::document/test/check`, `pkgdown::check_pkgdown`, article renders, local commits, Totoro pre-run for A4 (≤30 min, ≤8 cores); MUST STOP: merge, any "covered" public claim, API/grammar change beyond decisions 1-4, new family, compute beyond estimate, foreign-lane file edits | A4 finished locally in ~15 min and never used Totoro (stricter than authorised, not a violation); no merge occurred (branch/commits only); no foreign-lane files touched per the log; the one new `covered` register row (MIS-38) cites a test that already exists on `origin/main`, and Opus confirmed the only register status **change** was a downgrade (COE-02 `covered`→`partial`), not a promotion | adaptive | — | No MUST STOP condition was tripped that is visible in the tree. The one item bordering the "API/grammar change beyond decisions 1-4" line is Decision 3's implementation — see next row. |
| **Public claims** | No NEWS/public "covered" claim without gate evidence; register edits reviewed by Rose; deprecations/removals documented | Opus's R6 finding (export removal + register downgrade + two new deprecations missing from NEWS) was fixed in the review round (`7141bad24`, "arcC(news)"), and register status changes are evidence-backed per Opus's own credit pass. But Opus's **S2** finding — `README.md:225` still hard-codes `0.7.1` even though `inst/CITATION` was made version-dynamic via `meta$Version`, so the exact defect C1 was fixing will drift again at the next version bump — was **never routed to any producer** in the fix-round dispatch list (`A1: B2,R2,R3,R4-msg,R7,R8,S1` · `A3: R5,S3,S4` · `B1: B1` · `C1: R6+R4-NEWS` — S2 absent from all four), and it is still unfixed: `README.md:225` reads `R package version 0.7.1` verbatim on HEAD | **drift** | Rose | A Suggestion-severity finding, but it is exactly the class of item the brief flags — "a deferred item that vanished" — since it was never even acknowledged as deferred; it simply dropped out of the routing list between the VERIFY report and the fix-round dispatch. |
| **Handoff state** | DISCIPLINE/closure: "arc-level integration branch green, after-task report, check-log entry, board refresh, Melissa reconcile"; draft PR opens once G-ALL is clean locally | Board refresh done (`docs/dev-log/coordination-board.md:22-`, committed `a79b048bc`). After-task report exists (`docs/dev-log/after-task/2026-09-02-gapclose-arcs-abc.md`, committed `253d0e842`) but **predates the entire fix round** — it still reads "G-ALL RESULT: _pending_" and contains no mention of the VERIFY findings, the un-ticked gates, or the 4-commit fix round that followed it. **No `check-log.md` entry exists** for this lane at all (`grep -i gapclose docs/dev-log/check-log.md` → no hits). Draft PR was attempted (`gh pr create`) but hit the GitHub API rate limit; a background job was to retry after reset — `gh pr list --head claude/gapclose-20260902` still returns nothing (still rate-limited at the time of this reconciliation, so unconfirmed either way) | **drift** | Rose | Two of the four named closure artifacts (check-log entry; an after-task report that reflects the state actually shipped) are missing or stale as of HEAD, despite the checkpoint having visibly moved past the point they describe. |

## Items the plan promised with no evidence in the tree

- `docs/dev-log/check-log.md` entry for this lane — required by DISCIPLINE, absent.
- A completed `G-ALL` run (`devtools::test()` FAIL 0 + `check --no-manual` 0E/0W) — acceptance-ledger gate, still `[ ]`/pending; the only log is a 20-line, in-progress capture.
- A second, independent verification pass (Opus or otherwise) confirming the fix round actually resolved B1/B2/R1-R8 — only self/orchestrator re-checks exist.
- A post-fix-round `MECH-VERIFY` run — the one on disk cites a superseded commit.
- The `README.md:225` version-string fix (Opus S2) — dropped from the fix-round routing, still `0.7.1` literal.
- A confirmed draft PR — attempted, rate-limited, not verifiable as created at reconciliation time.

## Verdict

The ARC A/B/C work itself is substantively sound and the plan's own verify-then-fix loop caught and closed two genuine blocking defects (B1's status-inflating parity tool, B2's route-that-refuses redirect) plus most of the required findings, with deviations from the plan mostly self-declared and reasoned rather than silent. But the checkpoint's closure discipline did not finish what it started: the fix round shipped with no independent re-verification, one suggestion-level finding vanished from the routing table, and the after-task report and check-log entry that were supposed to certify this state either predate it or don't exist — so this branch is accurately a well-evidenced draft, not yet the closed, reconciled checkpoint the plan describes.
