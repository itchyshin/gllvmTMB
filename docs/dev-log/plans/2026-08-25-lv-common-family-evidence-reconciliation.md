🎯 GOAL
Solo platform: Codex
Deliverable: A source-pinned, independently auditable gllvmTMB receipt that issues exactly one LV common-family evidence verdict and reconciles only the status that verdict earns.
HEADLINE: Reuse or reject the existing GLLVM.jl predictor-informed `B_lv` evidence before any new family compute.
IN PARALLEL: Luna-low mechanical commit/artifact provenance; Sol-high mathematical, parameterisation, payload, and claim adjudication.
DEFER: Native-TMB count/Gamma/Beta admission; masks; mixed families; source tiers; profile/bootstrap; public claims; any new 500-replicate campaign.
DISCIPLINE: verify=Unlazy `--reverify` plus independent Rose/Grace review · compute=static local audit only; no fit planned · closure=one exact verdict, bounded docs, after-task, Melissa reconciliation, handover, narrow local commit

# Ultra Plan: LV common-family sister-evidence reconciliation

Date: 2026-08-25
Branch: `codex/lv-family-evidence-reconcile`
Exact base: `482c9d372c7dc100f988f41f80d1b4cc3ce8a8e4`, equal to `origin/main` when the branch was created
Lane: `predictor-informed-lv-evidence`
G0 authority: the coordinator explicitly authorised execution through a local commit, with a hard stop before any remote or over-30-minute compute.

## What the brain and repository already know

- Ask-Brain MCP query (with `search_all_projects = true`): `gllvmTMB Design 73 predictor-informed latent variables GLLVM.jl B_lv Lambda alpha coverage bridge Wald Poisson generator finite-difference Hessian methods superarc`. It surfaced the GLLVM.jl parity note and the durable rule that non-Gaussian Wald/profile routes and bridge exposure remain target-specific and partial. The repository, not the index summary, is load-bearing.
- Deterministic brain grep: `rg -n -i "predictor-informed|B_lv|Design 73|GLLVM\\.jl.*X_lv|X_lv.*GLLVM\\.jl" ~/shinichi-brain/memory/AGENT_LOG.md`; journal grep found the 2026-07-09 `B_lv` coverage campaign note. Decisions/open-questions/deep-research greps returned no task-specific decision that supersedes the repo.
- Repo state sweep: `git status -sb`, `git log --oneline -20`, `git branch -a`, `git worktree list`, `git stash list`, and `branch_drift_check.sh`. The worktree began detached and clean at `origin/main`; the dedicated branch above was created before any file edit.
- Lane preflight verdict: `SECOND codex LANE ACTIVE`; active PRs #1208/#1209 are random-slope lanes. This lane holds an exact-path lease on nine documentation files and does not touch their code or evidence paths.
- Sister receipt: `codex/methods-superarc-plan:docs/dev-log/artifacts/methods-superarc/lv-route-and-gap-audit.md` says the current R bridge has complete-response Poisson/NB2/Gamma/Beta point routes and optional uncalibrated Wald payloads. The requested audit must now establish whether the cited GLLVM.jl evidence is reusable and lineage-complete.

## Phase 0.25 sweep receipt

| Surface | Concrete evidence | Finding | Forced call |
| --- | --- | --- | --- |
| gllvmTMB git state | `git status -sb`; `git rev-parse HEAD origin/main`; `git branch -a`; `branch_drift_check.sh` | clean exact-main base; other live lanes are unrelated | new isolated branch; exact lease |
| Prior plan lane | `git show codex/methods-superarc-plan:.../2026-08-25-prior-work-receipt.md`; `.../lv-route-and-gap-audit.md`; commits `d1176054`, `b10bb3b3` | anti-duplication boundary already identified | reuse plan evidence; deepen lineage/raw audit only |
| GLLVM.jl twin | read-only `git show`, `git branch --contains`, `git merge-base --is-ancestor`, and `rg` for `1dc42d57`, `b7bc2acb`, `2b44b6a9`, `4e4a9547`, `6c96b758`, `cd3c110f`, `2ce6e29f` | candidate `origin/main@8c9acc76` contains the squashed implementation but lacks seed-level common-family results, failed-attempt rows, earned MCSEs, and a retained K=2 driver | HOLD; no status edit or duplicate campaign |
| Brain | Ask-Brain query above with `search_all_projects=true`; deterministic `AGENT_LOG`/journal/decisions greps | parity/status pointers, no superseding decision | repo commits and retained artifacts remain load-bearing |
| External prior art | none | no novelty or literature claim is being made | NotebookLM/web search is not warranted |
| Verdict | exact three-way adjudication | genuinely new work is source-pinned sister-evidence reconciliation | no duplicate family campaign |

## Route check

The route is knowable. The destination is one auditable receipt and one of three fixed verdict strings; every slice has an explicit output path or review return. No slice contains a design choice that requires a decision map.

## Slice table

| Slice | Member and lens | Model + effort | Dispatch | Estimate | Output | Dependency |
| --- | --- | --- | --- | --- | --- | --- |
| S0 recon | Jason: commit/artifact provenance | Luna low | native/explicit, fresh read-only child | 10–20 min | structured return to Ada | none |
| S1 audit | Ada + Jason: integrate lineage, raw results, DGP, denominators, bug/fix, endpoint | Sol high parent judgment with Luna map | parent + child receipt | 25–40 min | `docs/dev-log/artifacts/methods-superarc/lv-common-family-evidence-reconciliation.md` | S0 |
| S2 adjudicate | Gauss + Noether + Fisher + Emmy: parameterisation, rotation-stable estimand, coverage, payload | Sol high | native/explicit fresh reviewer | 15–25 min | review return; corrections incorporated in S1 | S1 candidate |
| S3 reconcile | Ada: verdict-warranted status only | Sol high | parent | 15–25 min | Design 73, validation register, capability status only if REUSABLE; otherwise receipt only | S2 |
| S4 mechanical verify | Ada: exact markers, diff scope, whitespace | local commands | parent | 5–10 min | Unlazy evidence | S3 |
| S5 close review | Rose + Grace: scope and reproducibility | Terra high | native/explicit fresh reviewer | 10–20 min | review return; repairs if needed | S4 |
| S6 closeout | Ada + Melissa | Sol high integration | parent | 15–25 min | after-task, plan-vs-actual, handover, local commit | S5 |

Parallel: S0 may run while Ada reads the source-pinned R-side contract.
Sequential: S1 ← S0; S2 ← S1; S3 ← S2; S4 ← S3; S5 ← S4; S6 ← S5.

FAN-OUT: three children total: one Luna provenance scout, one Sol load-bearing adjudicator, one Terra close reviewer.
FAN-OUT BUDGET: checkpoint=`lv-common-family-20260825` · new children=3/6 · scout=1 · build/review=1 · ceiling=1 · reuse=Jason for follow-up.
SCOUT SUITABILITY: yes — commit/file/artifact inventory is mechanical and runs on Luna-low.
ULTRA EFFORT: no.
CONTEXT BRAKE: parent input below fresh-task threshold; no compaction observed.
D-43 PANEL: N/A after adjudication. The exact HOLD verdict makes this a
source-audit receipt with no implementation, status promotion, or public claim;
it is not a D-43 milestone. Independent Sol adjudication plus Rose/Grace close
review remain the proportionate scientific and reproducibility gates.

## Exact scope and leases

Owned paths are limited to:

- `docs/dev-log/plans/2026-08-25-lv-common-family-evidence-reconciliation.md`
- `docs/dev-log/artifacts/methods-superarc/lv-common-family-evidence-reconciliation.md`
- `docs/design/73-predictor-informed-latent-scores.md`
- `docs/design/35-validation-debt-register.md`
- `docs/design/61-capability-status.md`
- `docs/dev-log/check-log.md`
- `docs/dev-log/after-task/2026-08-25-lv-common-family-evidence-reconciliation.md`
- `docs/dev-log/plan-actual/2026-08-25-lv-common-family-evidence-reconciliation.md`
- `docs/dev-log/handover/2026-08-25-lv-common-family-evidence-reconciliation.md`

`/Users/z3437171/Dropbox/Github Local/GLLVM.jl` is strictly read-only. No package source, tests, public article, NEWS, README, workflow, fit, simulation, campaign, remote host, issue, PR, push, merge, or release is in scope.

## Verdict and edit rule

The receipt must return exactly one:

1. `LV_COMMON_FAMILY_EVIDENCE_REUSABLE__R_STATUS_RECONCILIATION_ALLOWED`
2. `LV_COMMON_FAMILY_HOLD__RAW_OR_LINEAGE_GAP`
3. `LV_COMMON_FAMILY_BLOCKED__LIVE_BRIDGE_MISMATCH`

REUSABLE permits only source-pinned internal Design 73/register/status reconciliation. HOLD or BLOCKED permits only the receipt plus a smallest missing-evidence pre-run design and estimate; it does not permit a campaign.

## Compute and fit gate

No fit, simulation, or benchmark is planned. Static git/source/artifact checks are expected to take less than 90 minutes total but are not science compute. If a live route-health fit becomes necessary, Ada must first record a clean pinned GLLVM candidate, a correctness-oriented pre-run check, and a wall-clock estimate. At most four Poisson/NB2/Gamma/Beta fits may run locally only when the estimate is at most 30 minutes. Any over-30-minute estimate, Totoro/DRAC use, or campaign stops for explicit approval. GitHub Actions is never a science-compute target.

## Team raised

- Jason — exact ancestry and retained raw artifacts decide reuse; narrative tables alone force HOLD.
- Gauss — family/dispersion parameterisations and the Poisson generator must match the validated endpoint.
- Noether — only `B_lv = Lambda alpha^T`, conditional covariance, and unit innovation are cross-fit targets; raw axes are not.
- Fisher — coverage needs all-attempt denominators, exclusions, MCSE, and a predeclared failure policy.
- Emmy — the R bridge must expose the exact validated endpoint and preserve labelled payloads.
- Rose — no internal evidence reconciliation may leak into broader native-TMB or public claims.
- Grace — commit hashes, candidate identity, environment/seed receipts, and retained failures must be reproducible.
- Ada — default recommendation is REUSABLE only if lineage, raw evidence, parameterisation, and bridge endpoint all join without inference; otherwise choose the narrower HOLD/BLOCKED verdict.

## Pre-authorised after G0

PRE-AUTHORISED AFTER G0: scoped edits to the nine leased paths; read-only git/source inspection in both repositories; routine local text checks; Unlazy approval/reverification; review agents; after-task, plan-actual, handover; narrow local commit.
OPTIONAL REMOTE AUTHORITY: none.
MUST STOP: any fit/simulation/benchmark above 30 minutes; any Totoro/DRAC work; push/PR/merge/release/public claim; GLLVM.jl mutation; scope expansion; or evidence that makes the route scientifically ambiguous.

## Acceptance and closure

The executable ledger is `.unlazy/lv-common-family-reconcile/GATES.md`. Completion requires its runnable gates to pass under `--reverify`, the scientific manual gate to carry Sol evidence, the Rose/Grace manual gate to be resolved, Melissa's six-axis plan-vs-actual record, and a narrow local commit. Final lane receipt: `START A FRESH TASK` after this bounded milestone closes.
