🎯 GOAL
Solo platform: Codex
Deliverable: A source-pinned, reader-ready closure of the bounded Design 73 C1 predictor-informed latent-variable programme, carried through verified documentation, article, status reconciliation, after-task, handover, and one narrow local commit.
HEADLINE: Make the named ordinary predictor-informed LV cells genuinely complete without promoting the broader partial/blocked LV surface.
IN PARALLEL: Provenance reconciliation, mathematical/evidence audit, and Tier-1 article production on disjoint paths.
DEFER: Julia interval calibration; structured-source LV; extra tiers; native extra families; mixed-family LV; broader masks; missing LV predictors; fixed `X + X_lv`; REML broadening; profile/bootstrap promotion; remote compute.
DISCIPLINE: verify=Unlazy `--reverify` + fresh 2-Terra/1-Sol completion panel · compute=local only, estimate before every fit; stop above 30 minutes · closure=all gates pass, residuals remain explicit, artifacts are committed locally, and the lane is handed over.

# Ultra Plan — Design 73 C1 LV closure

Date: 2026-08-25
Platform: Codex
Branch: `codex/lv-family-evidence-reconcile`
Current HEAD: `93020c790728462c4f27f86a82fc6b9e80d370ec`
Exact base/origin main: `482c9d372c7dc100f988f41f80d1b4cc3ce8a8e4`
Durable plan path: `docs/dev-log/plans/2026-08-25-lv-design73-c1-closeout-ultra-plan.md`

This displayed Markdown is the complete plan. Because the current task is in Plan Mode, the durable file is created byte-for-byte as the first action after approval.

## 1. Destination and locked decisions

The programme is complete when `gllvmTMB` has a coherent, tested, reader-facing account of these named cells:

- Native ordinary unit-tier predictor-informed `latent(..., lv = ~ x)`:
  - Gaussian, rank 1 and rank 2 where existing evidence supports them.
  - Pure binomial logit, probit, and cloglog, with interval claims limited to the existing rank-1 multi-trial cells.
  - \(z_i=M_i\alpha+e_i\), \(e_i\sim N(0,I_K)\).
  - Native ordinary covariance \(\Sigma=\Lambda\Lambda^\top+\Psi\).
  - Rotation-invariant trait-scale \(B_{lv}=\Lambda\alpha^\top\) is the cross-fit scientific target.
- The R-to-Julia bridge remains:
  - complete-response;
  - `unique = FALSE`;
  - point-estimate support with optional uncalibrated Wald plumbing;
  - no profile/bootstrap or calibrated bridge-interval claim.
- Factor-valued LV predictors remain point/recovery-supported and interval-uncalibrated.
- The missing Tier-1 article is published in the package source and pkgdown navigation.

“C1 closed” means these named cells are complete. It does not change Design 73’s broader overall status:

- `FG-18`, `RE-13`, `LV-01`, `LV-04`, and `LV-05` remain `partial`.
- `LV-06`, `LV-07`, and `LV-08` remain `blocked`.
- `LV-02` remains the covered native Gaussian recovery/coverage cell.
- No umbrella row is promoted merely because the bounded C1 deliverable closes.

## 2. Prior-work sweep receipt

| Surface | Evidence already inspected | Finding | Plan consequence |
|---|---|---|---|
| gllvmTMB state | `git status`, recent/all-branch history, worktrees, stashes, branch drift, lane preflight | Current branch is clean and one local commit ahead of `origin/main`; four other Codex lanes are active | Recheck HEAD before every commit and claim exact path leases |
| Lane preflight | `SECOND codex LANE ACTIVE`; no live lease; duplicate design-number slots exist | Shared files are collision-prone | Allocate no new design number; edit only leased paths |
| Ask-Brain | Hybrid `search_notes(..., search_all_projects=true)` for Design 73, `B_lv`, bridge intervals, raw evidence, and structured LV | Prior records consistently call predictor-informed bridge support partial/point-oriented; broader bridge CI plumbing is parity, not calibration | Reuse prior implementation/evidence; do not launch another campaign |
| Deterministic brain history | `AGENT_LOG`, journal, `DECISIONS`, `OPEN_QUESTIONS`, and deep-research greps for `B_lv`, `X_lv`, Design 73, profile, bridge, and coverage | Profile is preferred only where its target-specific evidence applies; serious compute remains estimate/smoke/approval gated | Keep inference claims cell-specific |
| GLLVM.jl twin | Existing source-pinned reconciliation receipt and read-only commit ancestry audit | Historical common-family narratives lack retained all-attempt raw rows, earned MCSEs, and a retained complete K=2 driver | Preserve the exact bridge interval HOLD; no new GLLVM.jl worktree or fit |
| Historical profile evidence | LV-effects profile campaign, Design 76, current source, current tests | Campaign concerns historical rank-1 Gaussian orthogonal Model A with a separate `phylo_latent()` term and historical REML assumptions; current predictor-informed LV rejects REML | Do not transfer that evidence to current ML-only ordinary C1 |
| External prior art | Grounded search was offered; no novelty/priority claim is planned | Repository and retained evidence are authoritative for this closure | No NotebookLM or web campaign |

Genuinely new work is limited to the C1 cell-level closure matrix, stale inference/status correction, and the reader-facing Tier-1 article.

## 3. Route check

The route is now knowable:

- The destination is one sentence and has a falsifiable stopping condition.
- Every slice has an exact output path or review return.
- No slice contains an unresolved method-design choice.
- Julia interval calibration and structured-source LV have been explicitly assigned to future fresh tasks.

Phase 1 decomposition is therefore authorized once this plan is approved.

## 4. Slice table

| Slice | Member and lens | Model + effort | Dispatch | Estimate | Output | Dependency |
|---|---|---|---|---:|---|---|
| G0 activation | Ada + Shannon: ownership and gates | Sol high parent | parent | 15–25 min | Plan file, exact leases, `.unlazy/lv-design73-c1-closeout/` | approval |
| S1 provenance recon | Jason: commits, artifacts, stale surfaces | Luna low | native/explicit, fresh read-only child | 20–35 min | `.unlazy/.../recon.md` | G0 |
| S2 cell matrix | Gauss + Noether + Fisher: parameterisation, estimand, evidence | Terra high | native/explicit, fresh read-only child | 30–50 min | `.unlazy/.../scientific-review.md` | G0 |
| S3 closure receipt | Ada: integrate S1/S2 into one source-pinned matrix | Sol high parent | parent | 45–75 min | `docs/dev-log/artifacts/methods-superarc/lv-design73-c1-closure-receipt.md` | S1, S2 |
| S4 Tier-1 article | Boole + Pat: API grammar and reader path | Terra medium | native/explicit worker, article-only ownership | 60–100 min | `vignettes/articles/explaining-latent-ecological-axes.Rmd` | S3 |
| S5 reconciliation | Ada: internal/public status cascade | Sol high parent | parent | 45–90 min | Design/status/NEWS/pkgdown/test-comment corrections | S3; parallel with S4 where paths do not overlap |
| S6 mechanical verification | Grace mechanical lens | Luna low | native/explicit, fresh read-only child | 15–30 min | `.unlazy/.../mechanical-verify.md` | S4, S5 |
| S7 completion panel | Gauss/Emmy; Rose/Grace; Noether/Fisher | 2× Terra high + 1× Sol xhigh | fresh D-43 panel | 30–60 min | Three frozen-diff verdicts | S6 and `--reverify` |
| S8 repairs and reverify | Original producer or Ada | original tier | follow-up, no new child | 20–60 min | Corrected diff and fresh gate evidence | S7 if needed |
| S9 closeout | Rose + Ada | Terra high + Sol integration | reviewer + parent | 30–50 min | after-task, check-log, handover | S7/S8 |
| S10 reconciliation | Melissa | Terra medium | native/explicit | 15–25 min | plan-vs-actual report | S9 |
| S11 landing | Ada | Sol high parent | parent | 10–20 min | One narrow local commit; clean tree; lease release | S10 |

Parallel execution:

- Batch A: S1 and S2.
- Batch B: S4 article work may run while Ada performs non-overlapping parts of S5.
- Verification and closeout are sequential because they require a frozen candidate.

Expected wall clock: approximately 4–7 hours, with no science-compute campaign.
Producer fan-out: three children—one Luna scout and two Terra contributors.
Completion panel: separately, exactly two Terra reviewers plus one Sol reviewer.
Ultra effort: not used.
Usage bars: not exposed by the current Codex app; no balance claim is made.
Context state: one compaction; scope is frozen to this C1 milestone. The next LV programme begins in a fresh task.

## 5. Exact owned paths

The initial lease claims exactly:

- `.gitignore`
- `docs/dev-log/plans/2026-08-25-lv-design73-c1-closeout-ultra-plan.md`
- `docs/dev-log/artifacts/methods-superarc/lv-design73-c1-closure-receipt.md`
- `docs/dev-log/artifacts/lv-effects-ci-coverage/README.md`
- `docs/design/73-predictor-informed-latent-scores.md`
- `docs/design/76-structured-xlv-phylo.md`
- `docs/design/35-validation-debt-register.md`
- `docs/design/61-capability-status.md`
- `tests/testthat/test-lv-parser-guard.R`
- `vignettes/articles/explaining-latent-ecological-axes.Rmd`
- `_pkgdown.yml`
- `NEWS.md`
- `docs/dev-log/check-log.md`
- `docs/dev-log/after-task/2026-08-25-lv-design73-c1-closeout.md`
- `docs/dev-log/plan-actual/2026-08-25-lv-design73-c1-closeout.md`
- `docs/dev-log/handover/2026-08-25-lv-design73-c1-closeout.md`

Before touching any shared file:

1. Re-run lane preflight.
2. Run `gh pr list --state open`.
3. Run `git log --all --oneline --since="6 hours ago" -- <exact paths>`.
4. Claim the paths through `lane_lease.sh`.
5. Re-measure each file immediately before editing.
6. If a lease is refused, pause only the colliding slice and continue disjoint work.

`/Users/z3437171/Dropbox/Github Local/GLLVM.jl` remains strictly read-only. No branch, worktree, edit, cleanup, fit, or commit is permitted there.

## 6. Implementation requirements

### 6.1 Cell-level closure receipt

The receipt will pin:

- Current branch HEAD and `origin/main`.
- Every implementation, test, driver, raw artifact, and historical commit used.
- The symbolic contract:
  [
  z_i=M_i\alpha+e_i,\quad e_i\sim N(0,I_K),\quad
  \Sigma=\Lambda\Lambda^\top+\Psi,\quad
  B_{lv}=\Lambda\alpha^\top.
  ]
- Native Gaussian and binomial coverage cells, including:
  - attempted fits;
  - eligible denominators;
  - failed/non-PD/CI-unavailable counts;
  - coverage;
  - MCSE;
  - rank and link restrictions.
- The distinction between:
  - native ordinary `latent()` with its default `+\Psi`;
  - Julia `unique = FALSE`;
  - point-estimate support with optional uncalibrated Wald plumbing.
- Historical Model-A profile evidence as historical, rank-1 Gaussian, orthogonal, and non-transferable to current ML-only C1.
- Bootstrap as implemented plumbing without a production C1 coverage claim.
- The current REML refusal.
- The exact retained Julia bridge interval verdict:
  `LV_COMMON_FAMILY_HOLD__RAW_OR_LINEAGE_GAP`.

Raw \(\alpha\) and \(\Lambda\) must never be used as cross-fit recovery or coverage targets.

### 6.2 Status reconciliation

Update internal surfaces so they agree:

- Design 73: mark the named C1 cells closed while keeping the overall programme partial.
- Design 76: remove stale wording that implies the historical profile/bootstrap routes remain publicly reachable or support structured `lv`.
- Validation register:
  - preserve umbrella partial rows;
  - preserve `LV-06/07/08` blocked;
  - preserve `LV-09` as historical orthogonal Model-A evidence with its exact boundary.
- Capability status: replace “next C1 work” language with a split:
  - bounded C1 closure is complete;
  - bridge calibration, structured sources, extra tiers, and broader families are separate future programmes.
- Historical evidence README: append a superseding provenance note; do not rewrite or erase historical results.
- Parser test comment: correct the stale statement suggesting Gaussian predictor-informed `lv + REML` is admitted. Test behaviour remains unchanged.
- NEWS: add only the article/status closure and plain-language boundary; no new capability promotion.

No source, likelihood, exported function, signature, family, grammar, or return type changes.

### 6.3 Tier-1 article

Create “Explaining Latent Ecological Axes With Predictors” with:

- A biological question first: whether an observed environmental gradient is associated with shared multivariate trait axes.
- A deterministic Gaussian teaching fixture based on the already healthy rank-1, three-trait regime.
- Long-format and `traits(...)` wide-format calls side by side through `gllvmTMB()`.
- One ordinary numeric LV predictor distinct from any ordinary fixed-effect predictor.
- Extraction and interpretation of rotation-invariant trait-scale \(B_{lv}\).
- Wald uncertainty only, using the currently covered native route.
- Score decomposition showing total = mean + innovation.
- Associational language—no causal claim.
- A plain-language boundary box:
  - Native ordinary Gaussian and pure binomial standard-link C1 support.
  - Factors have point/recovery evidence but not interval calibration.
  - Julia supplies point estimates with optional uncalibrated Wald plumbing.
  - REML, fixed `X + X_lv`, Julia calibration, broader masks, native extra families, mixed families, extra tiers, and structured sources are not supported by this article.
- No internal validation-row codes on the public page.
- No profile/bootstrap demonstration.
- No raw-axis comparison across fits.

The article is added to `_pkgdown.yml` and rendered with code evaluation enabled.

## 7. Unlazy acceptance ledger

Before any implementation, add `.unlazy/` and `.unlazy-hook-state.json` to `.gitignore`, then create:

- `.unlazy/lv-design73-c1-closeout/GATES.md`
- `gates/leaf-s1-recon.md`
- `gates/leaf-s2-science.md`
- `gates/leaf-s3-receipt.md`
- `gates/leaf-s4-article.md`
- `gates/leaf-s5-status.md`
- `gates/leaf-s6-verify.md`
- `gates/leaf-s9-closeout.md`

Required gates:

- G1: exact branch, base, and lease receipt are recorded.
- G2: the closure matrix names every covered, partial, blocked, and historical cell.
- G3: every evidence claim resolves to a real source/commit/artifact.
- G4: no cross-fit acceptance criterion targets raw \(\alpha\) or \(\Lambda\).
- G5: Julia wording includes “optional uncalibrated Wald plumbing” and excludes calibrated/profile/bootstrap claims.
- G6: REML remains fail-loud for predictor-informed `lv`.
- G7: `LV-06/07/08` remain blocked and umbrella rows remain partial.
- G8: the new article runs long and wide examples and uses \(B_{lv}\).
- G9: the public boundary box contains no internal row IDs.
- G10: focused tests, article render, pkgdown checks, and stale-wording scans pass.
- G11: the D-43 panel reviews the exact frozen candidate SHA/diff.
- G12: after-task, Melissa reconciliation, handover, and local landing state all validate.

Every runnable command is inspected before `--approve`. Final verification uses `--reverify`, never `--status`.

## 8. Fit and compute gate

No new recovery, coverage, simulation, or benchmark campaign is authorized.

Before the focused test/article batch, record:

- Expected work: two equivalent Gaussian article fits plus existing focused test fixtures.
- Estimated runtime: 10–25 minutes locally.
- Correctness smoke:
  - one evaluated article fit;
  - non-empty finite \(B_{lv}\);
  - expected trait/predictor labels;
  - total = mean + innovation;
  - article output written and non-empty.

Rules:

- If the measured projection exceeds 30 minutes, stop with the receipt.
- If the run itself overruns 30 minutes, stop and re-report.
- Preserve every failed attempt and exact command in `check-log.md`; do not silently tune away an unhealthy fixture.
- No Totoro, DRAC, GitHub Actions science compute, or new 500-replicate run.
- Test or article failures may be repaired within the approved C1 scope; evidence changing the estimand or capability boundary triggers a stop.

## 9. Verification commands and scenarios

Run, in order and with exact results recorded:

1. Static integrity:
   - `git diff --check`
   - exact-path staged/diff audit
   - stale wording scans for `lv =`, `B_lv`, REML, profile/bootstrap, Julia CI, and `LV-06/07/08`.
2. Focused native tests:
   - parser long/wide and rejection paths;
   - Gaussian recovery;
   - binomial logit/probit/cloglog recovery and family guards;
   - factor runtime;
   - Gaussian response masks;
   - REML refusal;
   - tier/source refusal.
3. Inference-boundary tests:
   - withdrawn profile/export route;
   - bootstrap route boundary;
   - current extractor Wald behaviour.
4. Julia bridge tests:
   - pure-R mocks, capability guards, payload labels, and retained Wald status;
   - `GLLVM_JL_PATH` unset;
   - no live Julia fit.
5. Artifact validation:
   - Gaussian r500 summaries retain 500 attempted per cell, explicit eligibility, MCSE, and passed declared bands.
   - Binomial r500 summaries retain all 1,500 attempts, zero exclusions, MCSE, and named links.
   - Historical profile evidence is identified as orthogonal Model A, not current ordinary C1.
6. Documentation:
   - evaluated render of the new article;
   - `pkgdown::check_pkgdown()`;
   - inspect the rendered article from the reader’s seat.
7. Final:
   - Unlazy `--reverify` for every leaf;
   - a negative-control stale-wording check;
   - frozen-diff D-43 panel;
   - rerun affected gates after any repair.

A full package test/check is not planned because no package implementation or exported API changes. If an implementation file changes unexpectedly, stop, widen the test estimate, and obtain scope approval before proceeding.

## 10. Independent reviews

Plan review completed before approval:

- Method map: the finite destination is Design 73 C1, not every theoretical LV extension.
- Noether/Fisher/Rose plan review: `PASS WITH REQUIRED CHANGES`.
- Required changes—cell-level status, historical profile boundary, withdrawn inference status, Julia wording, REML comment correction, explicit tests, exact article claims, and frozen candidate review—are incorporated above.

Completion panel after the candidate exists:

1. Terra high — Gauss + Emmy:
   parameterisation, native/Julia decomposition, payload labels, and API boundary.
2. Terra high — Rose + Grace:
   repeated-staleness sweep, reproducibility, files touched, commands, and landing state.
3. Sol xhigh — Noether + Fisher:
   estimand, rotation invariance, coverage denominators/MCSE, historical-evidence non-transfer, and final claims.

Any `FAIL` returns to the original producer. The panel is fired once for the milestone and repeated only after material repair evidence exists.

## 11. Closeout and landing

Rose’s after-task report must contain all required sections, including:

- decisions and rejected alternatives;
- every file touched;
- exact checks and failures;
- tests of the tests;
- issue ledger;
- consistency audit;
- known residuals;
- explicit “does NOT cover” negative space;
- Ask-Brain/route memory receipt.

Melissa writes:

`docs/dev-log/plan-actual/2026-08-25-lv-design73-c1-closeout.md`

She reconciles scope, evidence, routing, safety gates, public claims, and handoff state, classifying deviations as adaptive, drift, or unclear.

Then:

1. Run the after-task validator.
2. Run `handoff_gate.sh`.
3. Record the exact candidate diff and final SHA.
4. Stage only the leased paths.
5. Recheck current branch immediately before commit.
6. Create one narrow local commit.
7. Verify the working tree is clean.
8. Release the lease.
9. Do not push, open a PR, merge, or release.

Final lane receipt:

`LANE: START A FRESH TASK` — this C1 milestone is closed; bridge calibration or structured-source LV is mathematically distinct and begins fresh.

## 12. Pre-authorisation envelope

PRE-AUTHORISED AFTER G0:

- Create the exact Markdown plan file shown here.
- Claim/release the exact path lease.
- Add the Unlazy ignore entries and run-state ledger.
- Perform scoped edits to the leased files.
- Run read-only provenance/history checks.
- Run the listed local focused tests and evaluated documentation build within the 30-minute compute gate.
- Repair failures that remain inside the approved C1 destination.
- Run subagents/review panels within the stated model and child budgets.
- Write checkpoints, check-log, after-task, plan-vs-actual, and handover.
- Create one narrow local commit.

OPTIONAL REMOTE AUTHORITY: none.

MUST STOP:

- Any fit/simulation/benchmark projected above 30 minutes or any overrun.
- Any Totoro, DRAC, or other remote compute.
- Any mutation or new worktree in GLLVM.jl.
- Any lease collision on a required shared path.
- Any evidence that changes the estimand, admitted families, API, or approved scope.
- Push, PR, merge, release, external/public message, or capability announcement.
- Credential/security changes or destructive action.
- A second context compaction before the milestone is safely landed.

## Approval

Approval requested: reply exactly

`APPROVE LV C1 ULTRA PLAN`

That authorizes the pre-authorised envelope above. After Plan Mode ends, Codex will carry the reversible work through without requiring prompts at each intermediate step.
