# Recovery Checkpoint: M3.3a nbinom2 Stress Controls

**Timestamp**: 2026-05-19 21:48:11 MDT
**Agent**: Ada / Codex

## Branch And Status

Current branch:

```sh
codex/m3-3a-nbinom2-stress-2026-05-19
```

`git status --short --branch`:

```text
## codex/m3-3a-nbinom2-stress-2026-05-19
```

## Changed Files

`git diff --stat origin/main...HEAD`:

```text
 dev/m3-grid.R                                      | 126 +++++++++++++++--
 dev/precompute-m3-grid.R                           |  59 +++++++-
 .../2026-05-19-m3-3a-nbinom2-stress-smoke.md       | 150 +++++++++++++++++++++
 .../2026-05-19-m3-3a-nbinom2-stress-smoke.md       |  92 +++++++++++++
 docs/dev-log/check-log.md                          |  62 +++++++++
 docs/dev-log/coordination-board.md                 |   7 +-
 6 files changed, 480 insertions(+), 16 deletions(-)
```

## Commands Already Run

- `Rscript --vanilla -e 'invisible(parse(file="dev/m3-grid.R")); invisible(parse(file="dev/precompute-m3-grid.R")); cat("parse ok\n")'`
  - Outcome: `parse ok`.
- CLI `nbinom2-d1` scenario-control smoke with `--n-reps=1`,
  `--n-units=20`, `--n-traits=3`, `--phi=0.4`,
  `--lambda-scale=0.5`, `--psi-scale=1.5`, `--n-boot=2`,
  BFGS, residual starts, and `se=false`.
  - Outcome: completed and saved artifacts under
    `/tmp/gllvmtmb-m3-3a-stress-smoke/`.
- Four-scenario direct stress smoke with `n_reps = 2`, `n_boot = 4`,
  BFGS, residual starts, `n_init = 5`, and `se = FALSE`.
  - Outcome: completed and saved
    `/tmp/gllvmtmb-m3-3a-stress-grid/nbinom2-four-scenario-smoke.rds`.
- Focused two-scenario stress smoke with `n_reps = 5`, `n_boot = 6`,
  BFGS, residual starts, `n_init = 5`, and `se = FALSE`.
  - Outcome: completed and saved
    `/tmp/gllvmtmb-m3-3a-stress-grid/nbinom2-two-scenario-r5.rds`;
    baseline had 4/30 bootstrap failures and coverage 0.12, while
    `lowphi_n120_r5` had 0/30 bootstrap failures and coverage 0.00.
- Direct API sanity check for fixed `phi`, scale controls, and
  scenario-aware summary grouping.
  - Outcome: `dgp controls ok`.
- `rg -n 'lambda_scale|psi_scale|truth_phi|phi_shape|phi_rate|n_units = n_units' dev/m3-grid.R dev/precompute-m3-grid.R`
  - Outcome: controls are present in truth sampling, dispatch, row
    metadata, CLI parsing, and artifact metadata.
- `rg -n 'scenario' dev/m3-grid.R docs/dev-log/audits/2026-05-19-m3-3a-nbinom2-stress-smoke.md docs/dev-log/after-task/2026-05-19-m3-3a-nbinom2-stress-smoke.md`
  - Outcome: optional scenario grouping is implemented and documented.
- `git diff --check`
  - Outcome: clean.
- `git commit --amend --no-edit`
  - Outcome: local commit `dev: add M3 nbinom2 stress controls`.

## Still Need To Run

- Post-merge main R-CMD-check run `26139437409` from PR #208 passed
  after this checkpoint was first written.
- Push this branch and open a small PR.

## Next Safest Action

Push `codex/m3-3a-nbinom2-stress-2026-05-19` and open a small PR for
the stress-controls lane.

## Blocking Question

None. The branch is locally committed and ready to push once main CI
settles.
