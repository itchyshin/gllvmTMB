# Recovery checkpoint: Ada, 2026-05-20 03:30 MDT

## Branch and status

- Branch: `main`
- `git status --short --branch` before writing this checkpoint:
  `## main...origin/main`
- Open PRs at checkpoint: none (`gh pr list --state open --repo
  itchyshin/gllvmTMB` returned no rows).

## Changed files and diff stat

- No local file changes before writing this checkpoint.
- At creation time, this checkpoint file was intentionally left local
  and unpushed so it did not start another CI run after the
  maintainer-requested 03:30 stop. It was later included with the
  coordination-board closeout once the post-merge checks passed.

## Commands and outcomes already run

- PR #213 (`Add M3 nbinom2 fitted diagnostics`) merged to `main` as
  squash commit `b65206307b215ad2b1ced33512dd0ace1792b018`.
- PR #213 R-CMD-check run `26150065112` passed on ubuntu, macOS, and
  Windows.
- Local branch was switched to `main` and fast-forwarded to `b652063`.
- Post-merge main R-CMD-check run `26151851845` was still in progress
  at 03:30:10 MDT:
  - macOS: completed, success
  - Windows: completed, success
  - Ubuntu: in progress
- The coordination-board closeout was deliberately not edited yet
  because the post-merge main run had not completed.
- Follow-up status checked after the stop: post-merge main
  R-CMD-check run `26151851845` completed successfully on ubuntu,
  macOS, and Windows; pkgdown run `26153970065` completed
  successfully.

## Commands still needed

1. Update `docs/dev-log/coordination-board.md` to move PR #213 from
   active to recently resolved, set WIP to 0, and record the merge SHA
   plus post-merge run IDs.
2. Commit and push the coordination-board closeout.
3. Watch the board-closeout commit's main R-CMD-check and pkgdown runs.

## Next safest action

Wait for the Ubuntu job in `26151851845`. Do not open a new lane until
the PR #213 post-merge verification and coordination-board closeout are
complete.

## Blocking question

None. The only blocker is GitHub Actions still running at the requested
03:30 MDT stop time.
