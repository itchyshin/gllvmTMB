# Recovery Checkpoint: Ada Covariance Article Closeout Wave 3

**Date:** 2026-05-24 14:21 MDT
**Branch:** `codex/covariance-article-closeout-wave3-2026-05-24`
**Spawned subagents:** none

## Current Status

`git status --short --branch`:

```text
## codex/covariance-article-closeout-wave3-2026-05-24
```

`git diff --stat`: no output; branch started clean from `main` at
`1a5d46a`.

## Context

Wave 2 PR #251 was merged to `main` as
`1a5d46ada10e2af46efcaa23c550338d789989f4`. The PR R-CMD-check run
`26370813615` passed on Ubuntu, macOS, and Windows.

The post-merge main R-CMD-check run `26371757078` is still in progress.
This branch may be edited locally, but it must not be pushed or opened as
a PR until the main run and downstream pkgdown run are green.

## Commands Already Run

- `gh pr list --repo itchyshin/gllvmTMB --state open --json ...`
  -> `[]`.
- `git log --all --oneline --since="6 hours ago" --decorate`
  -> recent Wave 1 / Wave 2 commits only; no competing open PR.
- `gh run list --repo itchyshin/gllvmTMB --limit 6 --json ...`
  -> main R-CMD-check run `26371757078` active for Wave 2 merge.
- Read the tail of `docs/dev-log/check-log.md`.
- Rendered `articles/covariance-correlation` locally during the #251 CI
  wait. The current HTML references three active figures:
  `corr-comparison-1.png`, `sigma-table-plot-1.png`, and
  `communality-correlation-matrix-1.png`.
- Viewed those three rendered figures. They are legible; the main
  remaining closeout issue is to make the matrix-display and interval
  provenance wording unmistakably display-only, not calibration evidence.

## Commands Still Needed

- Patch `vignettes/articles/covariance-correlation.Rmd` narrowly.
- Add a final rendered figure/prose audit for the covariance/correlation
  article.
- Update `ROADMAP.md`, the article gate matrix, `docs/dev-log/check-log.md`,
  and an after-task report for Wave 3.
- Re-render `articles/covariance-correlation` and `articles/roadmap`.
- Run `pkgdown::check_pkgdown()`, stale-wording scans, rendered figure
  reference scan, and `git diff --check`.
- Wait for main R-CMD-check run `26371757078` and downstream pkgdown to
  pass before pushing/opening the Wave 3 PR.

## Next Safest Action

Patch only the covariance/correlation article captions/prose plus its
status ledgers. Do not change R source, roxygen, generated Rd,
likelihoods, formula grammar, exported APIs, validation-debt status, or
`_pkgdown.yml`.

## Blocking Question

None.
