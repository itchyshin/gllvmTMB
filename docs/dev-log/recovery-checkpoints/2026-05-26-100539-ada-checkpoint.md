# Recovery Checkpoint: Ada Phase 56.2 Preflight Rehydration

**Date:** 2026-05-26 10:05 MDT
**Branch:** `main`
**Spawned subagents:** none

## Current Status

`git status --short --branch`:

```text
## main...origin/main
```

`git diff --stat` before this checkpoint:

```text
```

Local `main` is clean and equals `origin/main` at `6f413cf`
(`A6 prep memo: articles inventory + NEWS pre-draft + register row
pre-draft (#291)`). PR #289 is still open, mergeable, and 3-OS
R-CMD-check green, but its Phase 56.1 dormant TMB stub is not on
`main` yet.

## Commands Already Run

- Rehydration after context loss:
  - `git status --short --branch`
  - `git diff --stat`
  - `git diff`
  - `tail -n 180 docs/dev-log/check-log.md`
  - `ls -t docs/dev-log/recovery-checkpoints`
  - `git log --oneline --decorate -12`
- PR status:
  - `gh pr view 289 --repo itchyshin/gllvmTMB --json number,title,state,isDraft,mergeable,reviewDecision,statusCheckRollup,headRefName,headRepositoryOwner,baseRefName,url,comments`
    -> #289 open, not draft, all three R-CMD-check jobs successful.
  - `gh pr list --repo itchyshin/gllvmTMB --state open --json number,title,headRefName,baseRefName,isDraft,mergeable,updatedAt,url`
    -> only #289 open; mergeable.
- PR-head inspection without switching branches:
  - `git fetch origin pull/289/head:refs/remotes/origin/pr-289`
  - `git show origin/pr-289:docs/dev-log/recovery-checkpoints/2026-05-26-081743-ada-checkpoint.md`
  - `git show origin/pr-289:docs/dev-log/check-log.md | tail -n 220`
  - `git grep -n -E "Phase 56\\.2|56\\.2|n_traits|n_lhs_cols|audit|nine|9 sites|site" origin/pr-289 -- docs/design/56-augmented-lhs-engine-stage3.md R/fit-multi.R docs/design/03-likelihoods.md tests/testthat/test-phase56-1-phylo-augmented-stub.R`
  - `git show origin/pr-289:R/fit-multi.R | nl -ba | sed -n "1110,1340p"`
  - `git show origin/pr-289:docs/design/56-augmented-lhs-engine-stage3.md | nl -ba | sed -n "150,190p"`
  - `git show origin/pr-289:docs/design/56-augmented-lhs-engine-stage3.md | nl -ba | sed -n "456,478p"`
  - `git show origin/pr-289:src/gllvmTMB.cpp | nl -ba | sed -n "554,650p"`
  - `git show origin/pr-289:src/gllvmTMB.cpp | nl -ba | sed -n "776,792p"`
  - `git show origin/pr-289:R/fit-multi.R | nl -ba | sed -n "1600,1630p"`
- Pre-edit lane checks before writing this checkpoint:
  - `gh pr list --repo itchyshin/gllvmTMB --state open --json number,title,headRefName,baseRefName,author,updatedAt,url,files`
    -> only #289 open; it touches `R/fit-multi.R`,
    `src/gllvmTMB.cpp`, `docs/design/03-likelihoods.md`,
    `docs/dev-log/check-log.md`,
    `docs/dev-log/after-task/2026-05-26-phase56-1-dormant-tmb-promotion.md`,
    `docs/dev-log/recovery-checkpoints/2026-05-26-081743-ada-checkpoint.md`,
    and `tests/testthat/test-phase56-1-phylo-augmented-stub.R`.
  - `git log --all --oneline --decorate --since="6 hours ago"`
    -> current `main` includes #290/#291 after #289's head; #289 remains
    the only open PR.

## Read-Only Findings So Far

- Design 56 §4 still names nine historical `n_traits` audit sites.
- PR #289's `R/fit-multi.R` adds a dormant
  `use_phylo_slope_correlated = FALSE`, `n_lhs_cols = 1L`,
  `Z_phy_aug`, `b_phy_aug`, `log_sd_b`, and `atanh_cor_b` path.
- The active default path still uses legacy `b_phy_slope` and maps
  the augmented path off when `use_phylo_slope_correlated` is false.
- The old `theta_rr_phy`, `Lambda_phy`, `g_phy`, `log_sd_phy_diag`,
  and `g_phy_diag` sites remain per-trait paths in PR #289 and need
  site-by-site classification before any Phase 56.2 edit.

## Commands Still Needed

- Finish the read-only Phase 56.2 preflight table: exact current
  `R/fit-multi.R` line, Design 56 §4 row, keep `n_traits` versus
  promote to `n_lhs_cols`, and reason.
- Do not edit `R/fit-multi.R`, `src/gllvmTMB.cpp`, check-log, or the
  after-task ledger until #289 lands or the maintainer explicitly
  chooses a stacked Phase 56.2 branch.
- If edits begin, re-run the shared-file lane check first because
  #289 touches the same code and dev-log files.

## Next Safest Action

Continue the Phase 56.2 preflight read-only against `origin/pr-289`.
The likely useful output is a small audit table that can become the
Phase 56.2 implementation checklist after #289 merges.

## Blocking Question

None for read-only preflight. A real edit branch needs the #289 merge
or an explicit maintainer choice to stack Phase 56.2 on #289.
