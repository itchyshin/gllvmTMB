# Recovery Checkpoint: Ada Phase 56.3 Parser Start

**Date:** 2026-05-26 13:00 MDT
**Branch:** `codex/phase56-3-parser-2026-05-26`
**Spawned subagents:** none

## Current Status

`git status --short --branch`:

```text
## codex/phase56-3-parser-2026-05-26
```

`git diff --stat` before this checkpoint:

```text
```

Local `main` was clean and equal to `origin/main` at `1108d3b`
(`Phase 56.2 merge close-out: #293 cross-reference + coord-board
sync (#294)`) before this branch was created. Open PR census was
empty.

## Commands Already Run

- Rehydration after context compaction:
  - `git status --short --branch`
  - `gh pr list --repo itchyshin/gllvmTMB --state open --json number,title,headRefName,baseRefName,author,updatedAt,url`
  - `git log --all --oneline --since="6 hours ago" --decorate`
  - `gh run list --repo itchyshin/gllvmTMB --limit 8 --json databaseId,displayTitle,workflowName,status,conclusion,headBranch,headSha,url,createdAt,updatedAt`
- CI pulse:
  - #293 post-merge `R-CMD-check` and `pkgdown` on `72f67de` both
    completed successfully.
  - #294 close-out `R-CMD-check` and `pkgdown` on `1108d3b` both
    completed successfully.
- Shannon coordination gate:
  - `gh pr list --repo itchyshin/gllvmTMB --state open ...` -> `[]`.
  - `git log --all --oneline --since="6 hours ago" --decorate`
    showed #289, #292, #293, and #294 merged on `main`; no open PR
    overlap.
- Context reads:
  - `tail -n 180 docs/dev-log/check-log.md`
  - `ls -1t docs/dev-log/recovery-checkpoints | head -8`
  - `nl -ba docs/dev-log/recovery-checkpoints/2026-05-26-100539-ada-checkpoint.md | sed -n '1,220p'`
  - `rg -n "56\\.3|9\\.3|parser|guard|augmented|wide|long|trait|n_traits|structural" docs/design/55-* docs/design/56-*`
  - `nl -ba R/brms-sugar.R | sed -n '1500,2515p'`
  - `nl -ba R/parse-multi-formula.R | sed -n '1,230p'`
  - `nl -ba R/fit-multi.R | sed -n '520,610p;960,1115p;1120,1325p;1585,1630p;1845,1880p;2050,2188p'`
  - `nl -ba tests/testthat/test-augmented-lhs-guard.R | sed -n '1,240p'`
  - `nl -ba tests/testthat/test-phase56-1-phylo-augmented-stub.R | sed -n '1,260p'`

## Findings So Far

- Phase 56.3 is the parser bridge, not the Phase 56.4 recovery-test
  slice.
- Design 56 §9.3 requires:
  - classify `lhs_form` / `slope_col` in `parse_covstruct_call()`;
  - extend `phylo_unique` rewrite to accept bar-form
    `phylo_unique(1 + x | species)` and the long equivalent;
  - build `Z_phy_aug` with `n_lhs_cols = 2L` for supported
    intercept+slope forms;
  - keep unsupported augmented LHS forms rejected.
- Existing `phylo_slope(x | species)` must stay byte-compatible on
  the legacy path unless explicitly routed through the new augmented
  flag later.

## Commands Still Needed

- Edit `R/brms-sugar.R`, `R/parse-multi-formula.R`, and
  `R/fit-multi.R`.
- Add focused parser / TMB-data tests.
- Update `docs/design/01-formula-grammar.md`, `docs/dev-log/check-log.md`,
  and the Phase 56.3 after-task report.
- Run focused tests and stale-pattern scans.

## Next Safest Action

Implement the narrow Phase 56.3 parser slice for `phylo_unique`
augmented LHS only, leaving A6, NEWS, validation-debt promotion,
deprecations, article rewrites, and Phase 56.4 recovery tests for
later PRs.

## Blocking Question

None.
