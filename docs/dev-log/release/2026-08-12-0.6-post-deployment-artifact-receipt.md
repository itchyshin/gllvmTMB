# 0.6 post-deployment exact artifact receipt

Date: 2026-08-12
Scope: bounded 0.6 hardening evidence only. This receipt does not authorise a
version change, source freeze, release, publication, upload, or CRAN submission.

## Exact source and artifact

The source was `origin/main` at
`cb3126893883ff9fb0c6114129c158fe0e649be8` (merged PR #951), with package
version `0.6.0`.

```text
/private/tmp/gllvmtmb-mainline-06-artifact.OQP3po/gllvmTMB_0.6.0.tar.gz
SHA-256 9706809b9e2f52b130c21843a0396cafcfea602db74bb676eaa232f667f2e05a
tar members 702
```

The artifact was built and checked with:

```sh
R CMD build .
R CMD check --as-cran --run-donttest \\
  /private/tmp/gllvmtmb-mainline-06-artifact.OQP3po/gllvmTMB_0.6.0.tar.gz \\
  --no-manual --output=/private/tmp/gllvmtmb-mainline-06-artifact.OQP3po
```

The installed-package check passed its documentation, vignette, example, and
full `testthat` phases (`[244s/275s]`). Final status was **1 NOTE**, solely
CRAN incoming feasibility's expected `New submission` notice; there were no
warnings or errors. In particular, no URL NOTE was emitted.

## Deployed reader surface and platform continuity

The public Current limitations page returned HTTP 200 on 2026-08-12:

```text
https://itchyshin.github.io/gllvmTMB/articles/current-limits.html
```

The prior exact three-OS full matrix is Actions run `31321069365`. Its macOS,
Ubuntu, and Windows jobs all succeeded. The installed-byte comparison between
the matrix source `ae340bdd50c5eee5cbe0b093b5ebf14930bf855f` and current
`cb3126893883ff9fb0c6114129c158fe0e649be8` returned zero paths across
`DESCRIPTION`, `NAMESPACE`, `R`, `src`, `inst`, `man`, `vignettes`, `tests`,
and `.Rbuildignore`; that platform evidence therefore applies to this exact
installed package surface. This fresh macOS artifact closes the earlier
undeployed-URL limitation without claiming a new release rung.

## Explicit exclusions

The earlier `--no-build-vignettes` diagnostic archive is not used as release
evidence. LA-MSPL B2 and other experimental compute campaigns are likewise
outside this receipt and remain non-blocking to the 0.6 hardening lane.
