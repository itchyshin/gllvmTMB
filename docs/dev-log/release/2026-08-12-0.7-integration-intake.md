# 0.7 integration intake — bounded release path

**Decision status:** ready for Shinichi's approval of one implementation slice.
This is an internal release-planning record. It does not authorise a version
change, source freeze, public-claim change, release, upload, or CRAN
submission.

## Live-state reconciliation

The intake branch is `codex/mainline-06-issue-closeout` at `148623f4` and is
documentation-only relative to `origin/main` `cb312689`. Its package-bearing
paths (`DESCRIPTION`, `NAMESPACE`, `R`, `src`, `inst`, `man`, `vignettes`,
`tests`, `.Rbuildignore`) have no diff from `cb312689`; consequently the
0.6 exact-artifact receipt still applies to the installed package surface.

The 2026-08-09 ledger's count of 49 open issues is historical. Live review
confirmed that PR #956 is this clean, draft documentation-only intake (Ubuntu
check succeeds); PR #952 is draft, dirty, has no check runs, and targets the
older base `7af5cf00`. Issue #953 remains an open Paper-2 planning issue, not a
package-integration candidate. Reconcile issue state again immediately before
any later issue action.

## Candidate matrix

| Candidate | Owner / evidence | Package-byte and claim effect | Required before any promotion | Decision |
|---|---|---|---|---|
| Bounded ordinary-Laplace release hygiene | `origin/main` `cb312689`; exact 0.6 artifact receipt; `CRAN07-AA-01` / `CRAN07-AA-02` covered point-estimation rows | No new bytes in this intake; preserves the current narrow reader boundary | Select one owner-approved, cell-specific test/debt slice; then rerun the artifact ladder if package bytes change | **Integrate before 0.7** |
| Remaining intentional fences | Issue ledger: #945, #944, #935, #904, #897; Current limitations | Keeps unsupported inference and routes visible as limits | Preserve fail-loud behaviour and boundary wording; do not convert a fence into a claim | **Fence / defer** |
| VA, AGHQ, EVA, ordinal, SPDE/structured routes, and broad intervals | Validation register and issue ledger record partial, opt-in, or blocked evidence | None may be promoted from fitting or convergence alone; Laplace remains default | Route-specific validation and a separate maintainer decision; a broad class label never substitutes for a cell-specific register row | **Fence / defer** |
| LA-MSPL / separation reconciliation (PR #952; branch `codex/lane-b-mspl-reconcile-951`) | Draft, dirty 77-file / +10,994-line experimental PR, no check runs, old base `7af5cf00`; partial artefacts only | Integration would change `DESCRIPTION`, R/TMB, tests, docs, and vignettes, invalidating the `cb312689` artifact/platform receipt; it does not establish a separation claim | Separate admission programme: corrected-link tests, genuine both-tail data, independent Gauss/Noether/Fisher/Curie review, and a new artifact/platform ladder | **Experimental-only** |
| Cross-repo MSPL findings (PR #955) | Clean docs-only cross-repo note with successful Ubuntu check | Planning input only, not gllvmTMB validation or an integration vehicle; it requires link-specific C++ penalty-difference tests and rejects mirrored-cloglog data for estimand claims | Retain as input only if a separate MSPL admission programme is authorised | **Fence / defer** |
| Integrated-SDM branches | Separate experimental worktrees and issue-ledger deferral | No first-release evidence or stable integration contract | Complete its own owner-approved recovery/adjudication route | **Experimental-only** |
| Validation-card pilot (#954) | Parked governance issue | No package or public-surface change | Resume only as an internal provenance pilot | **Defer** |

## Ranked implementation order

1. **Choose one ordinary-Laplace validation-debt slice.** Scope it to a
   reportable estimand and one existing register boundary; specify the exact
   tests and reader wording before code changes.
2. **Implement and independently verify that one slice.** If it changes any
   installed package byte, rebuild the normal-vignette artifact and re-establish
   platform evidence for that new source identity.
3. **Reconcile the issue ledger only after the source evidence is landed.**
   Close completed/superseded issues narrowly; leave deliberate fences and #345
   open.
4. **Only then consider a 0.7 identity decision.** Shinichi alone authorises a
   version bump, freeze, release, or CRAN upload.

## Explicit non-goals

This intake neither merges nor interprets LA-MSPL, starts a campaign, changes
the default estimator, treats convergence as calibrated inference, or makes a
new public capability claim. The existing 0.6 artifact remains evidence for
`cb312689` only.

## Approval requested

Approve the ranked order above and name the first ordinary-Laplace
validation-debt slice. Any proposed simulation expected to exceed 30 minutes
will return with an estimate and a smoke result before it is launched.
