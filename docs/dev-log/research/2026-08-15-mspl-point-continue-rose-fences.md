# Rose claim fences — LA-MSPL point-continue (2026-08-15)

**Status:** claim-boundary checklist for the running lane
`cursor/mspl-point-programme-continue`. Not a capability promotion.

**Reader:** Rose / maintainer before any merge that thickens Gaussian
evidence or adds Poisson planned rows.

## Supported (may say)

- LA-MSPL = Laplace integration + soft outer criterion (not EVA/VA/AGHQ-MSPL).
- Bernoulli LA-MSPL: experimental opt-in point estimation; B2 incomplete.
- Gaussian ordinary identity q=1/2: admitted experimentally as
  `oracle_local` via #967 (Hirose, uniqueness pick C, `se = FALSE` smoke).
- Multi-seed LA-ML vs LA-MSPL local grids are **point evidence** that can
  thicken denominators beyond a single seed; they do **not** flip
  `oracle_local` → `covered`.
- Poisson Phase-4 prep may add **planned** registry rows + pure-R oracles
  + a derivation note.

## Not established (must not say)

- Calibrated SEs / CIs / coverage for any MSPL cell.
- NEWS or public “covered” claim for Gaussian or Poisson MSPL.
- Poisson (or NB) **admitted** LA-MSPL fits.
- Family transfer of Bernoulli Jeffreys/`V_loading` or Gaussian Hirose Ψ
  atoms without a new proof.
- Free-ε / uniqueness pick B reopened.
- Totoro/DRAC campaign results (none authorized in this lane without a
  separate receipt).

## Protected foreign surfaces

- `codex/lane-b-mspl-interval-feasibility` — binary SE / intervals.
- Repo-root `LOOP/` — 0.6 EVA/VA kit; never overwrite from this lane.
- Dropbox primary checkout — foreign lanes; not the MSPL workspace.

## Merge gate (Rose)

Before merge of evidence/registry PRs: confirm README/NEWS/articles were
**not** edited to claim covered; registry Poisson rows stay `planned`;
Gaussian evidence note uses failure-inclusive denominators and does not
assert programme superiority as a covered result.
