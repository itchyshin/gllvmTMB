# GOAL — Paper 2 iJSDM evidence-to-reader arc

**Lane:** `codex/isdm-paper2-evidence-reader-a0`
**Base:** `0f668c469228f1799a989e112176fd931f2f88a8` (G2o closure)
**Owner:** Codex only.  This is a private developer-evidence lane, not the
separate article-staging lane.

## Headline

Turn retained G2 evidence into a reviewable, reader-oriented evidence packet
without upgrading the private iJSDM to a capability claim.  The first deliverable
is an immutable estimator/Psi information and scale protocol; it is not a fit,
repair, recovery campaign, public article, or package change.

## Immutable boundaries

- Preserve `G2N_LOCAL_PRERUN_HOLD`, `G2K_CALIBRATION_HOLD`, and
  `G2C_SMOKE_ADMISSION_HOLD` verbatim.
- Preserve the likelihood, DGP, maps, parameter transforms, profiles,
  eligibility thresholds, and all-attempt recovery denominator defined by the
  G2d/G2n/G2o record.
- No fitting, simulation, profiling, local/DRAC/Totoro compute, estimator
  repair, public API/docs/pkgdown/article work, empirical data, spatial field,
  count-survey outcome, detection parameter, absolute-abundance interpretation,
  generic zero inflation, arbitrary-source model, comparator run, or Issue #953
  work is authorized here.
- S = 10,000 is an architecture HOLD, not a scale target.  It cannot be opened
  without a later approved matrix-free/block trait-separable fixed-effect
  design, limited-memory optimisation, sparse/block-Schur Laplace strategy, and
  selected/low-rank rather than dense S-by-S output and inference.

## Approved A0–A2 deliverables

1. A reconciliation/handover record rooted at the exact G2o commit.
2. A non-ranking source map locating this private estimand among `gllvm`, Hmsc,
   `spOccupancy`, `glmmTMB`, and `sdmTMB`.
3. A frozen Case-C estimator/Psi information protocol with recovery cells at
   S = 6, 20, 60 and measured implementation gates at S = 250, 1,000.

## Stop condition

After Gate A method review, reconciliation, checkpoint, and after-task record,
return to Shinichi for explicit approval.  A3 or any fit needs a new approval.
