% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/column-coef-foundation.R
\name{animal_coef}
\alias{animal_coef}
\title{Animal response-column coefficients}
\usage{
animal_coef(formula, pedigree = NULL, A = NULL, Ainv = NULL, rho = 1)
}
\arguments{
\item{formula}{A coefficient-basis bar expression such as
\code{1 + x | trait}, \code{0 + x | trait}, or \code{1 + x || trait}.}

\item{pedigree}{A pedigree accepted by the animal covariance helpers.
Mutually exclusive with \code{A} and \code{Ainv}.}

\item{A}{A labelled animal relationship covariance matrix. Mutually
exclusive with \code{pedigree} and \code{Ainv}.}

\item{Ainv}{A labelled animal relationship precision matrix. Mutually
exclusive with \code{pedigree} and \code{A}.}

\item{rho}{One numeric value in \verb{[0, 1]} fixing the animal source mixture.}
}
\value{
A formula marker; never evaluated directly.
}
\description{
Fit response-column-specific random intercepts and/or slopes with an animal
covariance supplied as a pedigree, relationship covariance \code{A}, or inverse
relationship matrix \code{Ainv}. The fitted covariance across the coefficient
basis is full for \code{|} and diagonal for \code{||}.
}
\details{
This point-model route is covered for Gaussian multivariate data in long or
\code{traits(...)} wide form. The first public version accepts a fixed numeric
\code{rho} in \verb{[0, 1]}. Estimated source strength, interval inference, and
non-Gaussian coefficient models are not available. Existing
\code{\link[=animal_slope]{animal_slope()}} remains current and warning-free.

For exact compatibility with the released slope engine, a no-intercept
dense-\code{A} fit with \code{rho = 1} uses the existing \code{\link[=animal_slope]{animal_slope()}} conditioning
seam, \verb{A + 1e-8 I}. Pedigree and sparse-\code{Ainv} endpoints use their released
sparse precision. Interior \code{rho} and intercept-bearing \code{rho = 1} fits use
the raw covariance-scale mixture.
}
\examples{
set.seed(3)
dat <- expand.grid(unit = factor(1:12), trait = factor(paste0("sp", 1:3)))
dat$x <- rnorm(12)[dat$unit]
dat$value <- rnorm(nrow(dat))
A <- 0.4 ^ abs(outer(1:3, 1:3, "-"))
dimnames(A) <- list(levels(dat$trait), levels(dat$trait))
fit <- gllvmTMB(value ~ 1 + animal_coef(1 + x | trait, A = A, rho = 0.5),
  data = dat, trait = "trait", unit = "unit", family = gaussian(),
  control = gllvmTMBcontrol(se = FALSE), silent = TRUE)
extract_Sigma(fit, level = "column_coef")
}
\seealso{
\code{\link[=animal_slope]{animal_slope()}}, \code{\link[=column_coef]{column_coef()}}, \code{\link[=phylo_coef]{phylo_coef()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}
}
