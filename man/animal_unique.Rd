% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animal-keyword.R
\name{animal_unique}
\alias{animal_unique}
\title{Per-trait independent animal-model random intercepts: \code{animal_unique(id)}}
\usage{
animal_unique(id, pedigree = NULL, A = NULL, Ainv = NULL)
}
\arguments{
\item{id}{Bare column name of the individual factor.}

\item{pedigree}{A 3-column data frame with columns \code{id}, \code{sire},
\code{dam} (unknown parents encoded as \code{NA}). Converted internally
to \strong{A} via Henderson's recursive formula. Only one of
\code{pedigree}, \code{A}, or \code{Ainv} should be given.}

\item{A}{Dense relatedness matrix (\eqn{n \times n}); rownames /
colnames must match levels of \code{id}.}

\item{Ainv}{Sparse precision matrix (inverse of A). Currently
densified for v0.2.0; sparse engine path is a v0.3.0 follow-up.}
}
\value{
See \code{\link[=animal_scalar]{animal_scalar()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}
}
\details{
\code{animal_unique()} is soft-deprecated as compatibility syntax in gllvmTMB
0.2.0. Use \code{\link[=animal_indep]{animal_indep()}} for standalone marginal diagonal animal-model
terms. Paired explicit-Psi use remains accepted until the latent-Psi fold
lands.

Canonical name for the \strong{D independent} animal-model random
intercept on a shared relatedness matrix \eqn{\mathbf A}. Each
trait gets its own variance \eqn{\sigma^{2}_{\text{a},t}}; traits
share the same per-individual draws (rows of A act on each trait
independently). Mathematical parallel to \code{\link[=phylo_unique]{phylo_unique()}}.
}
\section{Correlated intercept + slope reaction norm}{

Used with an intercept+slope bar,
\code{animal_unique(1 + x | id, pedigree = ped)} fits a \strong{correlated}
additive-genetic random-regression (reaction norm)
\eqn{\mathrm{vec}(\mathbf B) \sim N(0, \Sigma_b \otimes \mathbf A)},
where \eqn{\Sigma_b} is a \eqn{2 \times 2} (intercept, slope)
covariance with a \strong{free} intercept-slope correlation. This routes
through the same augmented engine as \code{\link[=phylo_unique]{phylo_unique()}} with a
\code{1 + x | sp} bar; no separate likelihood. For the diagonal
(\eqn{\rho = 0}) special case use \code{\link[=animal_indep]{animal_indep()}} with the same
\code{1 + x | id} bar. Recover \eqn{\Sigma_b} with
\code{extract_Sigma(fit, level = "phy")}.
}

\seealso{
\code{\link[=animal_scalar]{animal_scalar()}}, \code{\link[=animal_indep]{animal_indep()}}, \code{\link[=animal_latent]{animal_latent()}},
\code{\link[=animal_dep]{animal_dep()}}, \code{\link[=phylo_unique]{phylo_unique()}}.
}
