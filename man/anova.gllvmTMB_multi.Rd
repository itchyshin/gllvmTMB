% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aghq-report.R
\name{anova.gllvmTMB_multi}
\alias{anova.gllvmTMB_multi}
\alias{print.anova.gllvmTMB_multi}
\title{Likelihood-ratio comparison of nested \code{gllvmTMB} fits}
\usage{
\method{anova}{gllvmTMB_multi}(object, ..., test = c("chibar", "chisq", "none"))

\method{print}{anova.gllvmTMB_multi}(x, digits = 4, ...)
}
\arguments{
\item{object, ...}{Two or more fits from \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{test}{One of \code{"chibar"} (default: chi-bar-square for rank steps,
plain chi-square for interior fixed-effect steps), \code{"chisq"} (plain
chi-square everywhere -- \strong{conservative} (its p-value is too large,
understating the evidence) at a rank/boundary step, and flagged as such
in the returned table's notes), or \code{"none"} (report the table with no
p-values).}

\item{x}{An \code{"anova.gllvmTMB_multi"} object.}
}
\value{
A \code{data.frame} of class \code{c("anova.gllvmTMB_multi", "data.frame")}
with columns \code{model}, \code{formula}, \code{d} (latent rank, \code{NA} if not
applicable), \code{npar}, \code{logLik}, \code{deviance}, \code{df} (parameter-count
change from the previous row), \code{LRT}, \code{test} (method actually used, or
\code{"refused"}), and \code{p.value}. An attribute \code{"note"} carries a per-row
explanation (recommended alternative when refused; the approximation
caveat for chi-bar rows).
}
\description{
Sequential nested likelihood-ratio comparison of two or more ordinary
(ML, unpenalised, non-MSPL) \code{gllvmTMB} fits, sorted by increasing free
parameter count. Each adjacent pair is classified as either an
\strong{interior fixed-effect step} (plain Wilks chi-square applies) or a
\strong{rank (boundary) step of exactly one new latent dimension} (the
Self-Liang chi-bar-square mixture is used, as an explicitly documented
approximation -- see Details). Anything else -- more than one new latent
dimension in a step, a change to covstruct routes/family, or a fixed-effect
change combined with a rank change in the same step -- is refused with a
named reason rather than guessed at.
}
\section{Comparability (refused outright)}{
\code{anova()} aborts, naming the reason, if the fits: are not all
\code{gllvmTMB_multi} objects; include an LA-MSPL fit; include a \code{REML = TRUE}
fit; use different integration engines (Laplace vs AGHQ) or a loading
ridge; were fit to different data (row count, or the response itself), or
different families/links; or are not fixed-effect-nested when ordered by
parameter count. It also refuses (per-step, in the returned table's
\code{test}/\code{note} columns rather than aborting the whole call) any adjacent
pair that changes more than the latent rank \code{d} by more than one
dimension, or that changes both the fixed effects and the rank in the
same step -- compare those in separate steps instead.
}

\section{The rank-step chi-bar-square is an approximation}{
The Self & Liang (1987) chi-bar-square mixture (\code{\link[=chibar2_pvalue]{chibar2_pvalue()}}) is
derived for \code{q} INDEPENDENT scalar boundary variance components with
regular Fisher information elsewhere. A newly added latent-loading column
is \code{p - d} free parameters (this package's \code{rr()} lower-triangular
identifiability convention), but they are not literally independent
scalar variances: at the null (the new column exactly 0) the associated
latent score itself becomes unidentified, which is closer to the
"testing the number of factors" / reduced-rank-testing literature (known
to have non-standard LRT asymptotics in general) than to Self & Liang's
setting. \code{anova()} uses the chi-bar-square mixture anyway, exactly as
GLLVM.jl's oracle implementation does for K-selection, but labels every
rank-step p-value with this caveat in the table's notes rather than
presenting it as an exact result. Its empirical size was measured by
simulation (\code{tests/testthat/test-select-lv-anova.R},
\code{dev/gapclose/arcD/O5-report.md}); consult that evidence, not this
docstring, for whether it is close to nominal.
}

\seealso{
\code{\link[=chibar2_pvalue]{chibar2_pvalue()}}, \code{\link[=select_lv]{select_lv()}}
}
