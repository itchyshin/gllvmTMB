% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-stage.R
\name{block_V}
\alias{block_V}
\title{Build a block-diagonal sampling-variance matrix V}
\usage{
block_V(study_id, sampling_var, rho_within = 0.5)
}
\arguments{
\item{study_id}{A factor (or coercible to one) identifying which
study each row belongs to. Rows with the same \code{study_id} form a
block in V.}

\item{sampling_var}{Numeric vector of per-row sampling variances,
length \code{length(study_id)}.}

\item{rho_within}{Either a single numeric in (-1, 1) used for every
study's off-diagonals, or a named numeric vector with one entry
per \code{study_id} level (allowing study-specific within-study
correlations). Default 0.5 - a common ad-hoc choice in ecology
meta-analyses when the true rho is unknown but plausibly moderate.}
}
\value{
A symmetric positive-definite \verb{n x n} matrix.
}
\description{
Constructs a block-diagonal \verb{n x n} matrix V suitable for the
\code{\link[=meta_known_V]{meta_known_V()}} / \code{equalto()} covstruct in \code{\link[=gllvmTMB]{gllvmTMB()}}, where
rows belonging to the same \code{study_id} are correlated and rows
from different studies are independent. Each block is a compound-
symmetric covariance matrix
\eqn{\mathbf{V}_s = \mathbf{D}_s^{1/2} \mathbf{R}_s \mathbf{D}_s^{1/2}}
with \eqn{\mathbf{D}_s = \mathrm{diag}(\sigma^2_{s1}, \ldots)} the
per-row sampling variances within study \eqn{s} and \eqn{\mathbf{R}_s}
the within-study correlation matrix (1 on the diagonal,
\code{rho_within} on the off-diagonal).
}
\details{
Use this when you have multiple effect sizes per study (multiple
outcomes, multiple traits per individual, etc.) and the within-study
sampling errors share a common cause. The independent-rows / single-
effect-per-study case is \code{block_V(..., rho_within = 0)}, which
equals \code{diag(sampling_var)}.
}
\examples{
set.seed(1)
df <- data.frame(
  study = factor(rep(paste0("s", 1:3), each = 2)),
  var   = c(0.04, 0.05, 0.06, 0.04, 0.05, 0.07)
)
V <- block_V(df$study, df$var, rho_within = 0.5)
round(V, 3)
}
\seealso{
\code{\link[=meta_known_V]{meta_known_V()}} for the canonical keyword that consumes
the matrix produced here.
}
