% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap-sigma.R
\name{bootstrap_Sigma}
\alias{bootstrap_Sigma}
\title{Bootstrap covariance, correlation, communality, and ICC summaries}
\usage{
bootstrap_Sigma(
  fit,
  n_boot = 200,
  level = c("unit", "unit_obs", "phy", "B", "W"),
  what = c("Sigma", "R", "communality", "ICC"),
  conf = 0.95,
  seed = NULL,
  n_cores = 1,
  progress = TRUE,
  keep_draws = FALSE,
  link_residual = c("auto", "none")
)
}
\arguments{
\item{fit}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{n_boot}{Integer; number of bootstrap replicates. Default 200.}

\item{level}{Character vector; which tier(s) to bootstrap.
Use the canonical levels \code{c("unit", "unit_obs", "phy")}; legacy
aliases \code{"B"} and \code{"W"} are still accepted. Levels absent from
the fit are silently dropped. Default: all available levels.}

\item{what}{Character vector; which summaries to compute.
Subset of \code{c("Sigma", "R", "communality", "ICC")}. Default: all
four. \code{"ICC"} only makes sense at the site level and requires both
\code{B} and \code{W} tiers in the fit.}

\item{conf}{Numeric in \verb{(0, 1)}; confidence level for percentile CIs.
Default 0.95.}

\item{seed}{Optional RNG seed for reproducibility.}

\item{n_cores}{Integer; number of cores for parallel refits.
Default 1 (sequential). \verb{>= 2} uses \code{future::multisession}.}

\item{progress}{Logical; print a one-line status message at each
replicate (sequential only). Default \code{TRUE}.}

\item{keep_draws}{Logical; if \code{TRUE}, the full \code{n_boot} x ...
matrices of bootstrap draws are returned as \verb{$draws}. Default
\code{FALSE} (CIs only — saves memory for large n_boot).}

\item{link_residual}{How to treat family-specific link-implicit
residual variance when extracting \code{Sigma}, \code{R}, \code{communality}, and
\code{ICC}.
\code{"auto"} (default) matches \code{\link[=extract_Sigma]{extract_Sigma()}} and adds the
family/link residual to non-Gaussian trait diagonals; \code{"none"}
returns the fitted model covariance without link residuals. Use \code{"none"}
when validating against a DGP target defined as
\eqn{\Lambda\Lambda^\top + \Psi}.}
}
\value{
A list with components:
\describe{
\item{\code{point_est}}{Named list of point estimates for each
requested summary at each requested level (e.g. \code{Sigma_B},
\code{R_B}, \code{communality_B}, \code{ICC_site}).}
\item{\code{ci_lower}, \code{ci_upper}}{Named lists of percentile CI bounds,
element-wise the same shape as the corresponding \code{point_est}.}
\item{\code{ci_method}}{Character; currently \code{"percentile"}.}
\item{\code{link_residual}}{Character; the link-residual convention used
in point estimates and bootstrap refit summaries.}
\item{\code{conf}, \code{n_boot}, \code{n_failed}}{Configuration metadata.}
\item{\code{draws}}{\code{NULL} unless \code{keep_draws = TRUE}; otherwise a
named list of bootstrap draw arrays.}
}
}
\description{
Use \code{bootstrap_Sigma()} when Hessian, Wald, or profile intervals are
unavailable or unsafe but the fitted model still has useful point
estimates. A \code{pdHess = FALSE} or skipped \code{sdreport()} is an
inference warning, not automatic proof that the fitted mean or
rotation-invariant covariance summaries are unusable. Inspect
\code{\link[=check_gllvmTMB]{check_gllvmTMB()}} / \code{\link[=gllvmTMB_diagnose]{gllvmTMB_diagnose()}} first, then use this
helper for parametric simulate-refit uncertainty.
}
\details{
The function generates \code{n_boot} parametric bootstrap replicates of a
fitted model and returns percentile confidence intervals for the
canonical biological summaries: trait covariance matrices
\eqn{\hat\Sigma_\mathrm{unit}}, \eqn{\hat\Sigma_\mathrm{unit\_obs}};
the corresponding correlation matrices; per-trait communalities
\eqn{c_t^2 = (\Lambda \Lambda^\top)_{tt} / \Sigma_{tt}}; and
per-trait site-level ICCs
\eqn{R_t = (\Sigma_\mathrm{unit})_{tt} /
[(\Sigma_\mathrm{unit})_{tt} + (\Sigma_\mathrm{unit\_obs})_{tt}]}.

Scope boundary (EXT-13 / CI-03 / CI-10): IN, Gaussian bootstrap
summaries and mixed-family refit plumbing covered by current tests.
PARTIAL, non-Gaussian bootstrap calibration remains experimental
until the M3 target-explicit grid is rerun. PLANNED, production
calibration evidence for mixed-family intervals remains future M3
work.

Each bootstrap replicate (1) draws a new response vector from
\code{simulate(fit, nsim = 1)}, (2) refits the model with the same formula
on the simulated data, (3) extracts the requested summaries via
\code{\link[=extract_Sigma]{extract_Sigma()}}, \code{\link[=extract_communality]{extract_communality()}}, and \code{\link[=extract_ICC_site]{extract_ICC_site()}}.
Replicates whose refit fails to converge are recorded but excluded
from CI calculation.

Multicore is dispatched via \code{future} + \code{future.apply}; pass
\code{n_cores >= 2} to enable parallel refits. When parallel, replicates
use \code{future.apply}'s L'Ecuyer-CMRG seed stream so the answers are
reproducible given a fixed \code{seed}, but they are NOT bit-identical to
an \code{n_cores = 1} run with the same seed (different RNG streams).
}
\section{Caveats}{

\itemize{
\item Uses the existing \code{\link[=simulate.gllvmTMB_multi]{simulate.gllvmTMB_multi()}} method, which
conditions on the fitted random effects (\eqn{\eta = \hat\eta})
and redraws the response from the fitted family where implemented.
CIs reflect parametric simulate-refit variability, not a
Bayesian posterior distribution for variance components.
Unsupported families fall back through the simulator's own
warning path.
\item Refits use the same \code{formula} reconstructed from
\code{fit$formula} and \code{fit$covstructs}. Auxiliary arguments such as
\code{phylo_vcv}, \code{mesh}, \code{lambda_constraint} are NOT currently
forwarded; pass \code{formula} and \code{extra_args} explicitly via the
low-level path if you need them.
\item Convergence: replicates whose refit fails or whose
optimiser does not return \code{convergence == 0} are counted in
\code{n_failed} and excluded from CIs.
}
}

\examples{
\dontrun{
set.seed(1)
s <- simulate_site_trait(n_sites = 30, n_species = 1, n_traits = 3,
                         mean_species_per_site = 1,
                         Lambda_B = matrix(c(1, .5, -.4), 3, 1),
                         psi_B = c(.2, .15, .1))
fit <- gllvmTMB(value ~ 0 + trait + latent(0 + trait | site, d = 1),
                data  = s$data,
                trait = "trait",
                unit  = "site")
boot <- bootstrap_Sigma(fit, n_boot = 50, level = "unit",
                        what = c("Sigma", "R"), seed = 42)
boot$point_est$Sigma_B
boot$ci_lower$Sigma_B
boot$ci_upper$Sigma_B
}
}
