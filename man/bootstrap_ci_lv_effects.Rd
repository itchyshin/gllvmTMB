% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap-lv-effects.R
\name{bootstrap_ci_lv_effects}
\alias{bootstrap_ci_lv_effects}
\title{Parametric bootstrap confidence intervals for predictor-informed latent effects}
\usage{
bootstrap_ci_lv_effects(
  fit,
  n_boot = 200,
  conf = 0.95,
  seed = NULL,
  n_cores = 1,
  progress = FALSE
)
}
\arguments{
\item{fit}{A fitted \code{gllvmTMB} model with a predictor-informed latent term.}

\item{n_boot}{Number of bootstrap replicates (default 200).}

\item{conf}{Confidence level (default 0.95).}

\item{seed}{Optional integer seed for reproducible replicates.}

\item{n_cores}{Cores for parallel refits (requires \pkg{future.apply} when > 1).}

\item{progress}{Logical; print per-replicate progress in the sequential path.}
}
\value{
A data frame with one row per \eqn{B_{lv}} entry: \code{trait},
\code{predictor}, \code{estimate}, \code{lower}, \code{upper}, \code{level},
\code{method}, \code{n_boot} (converged replicates used).
}
\description{
Percentile bootstrap CIs for the trait-scale effects
\eqn{B_{lv} = \Lambda_B \alpha^\top} of a \code{latent(..., lv = ~ x)} term.
Each replicate simulates a response from the fitted model
(\code{simulate(fit)}), refits with the same formula (tree / mesh / REML
preserved), and extracts \eqn{B_{lv}}; the CI is the empirical percentile
interval across converged replicates.
}
\seealso{
\code{\link[=profile_ci_lv_effects]{profile_ci_lv_effects()}}, \code{\link[=extract_lv_effects]{extract_lv_effects()}}, \code{\link[=bootstrap_Sigma]{bootstrap_Sigma()}}.
}
