% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-predictor.R
\name{categorical}
\alias{categorical}
\title{Baseline-category softmax unordered categorical missing-predictor family}
\usage{
categorical()
}
\value{
A \code{gllvmTMB_impute_family} object for the \code{family} argument of
\code{\link[=impute_model]{impute_model()}}.
}
\description{
\code{categorical()} declares the predictor-model family for an \strong{unordered
categorical} missing predictor used inside \code{\link[=mi]{mi()}} with
\code{impute_model(family = categorical())} (Phase 5c, the gllvmTMB analogue of
the drmTMB MD6c path). The missing unordered predictor is marginalised
EXACTLY by a finite-state baseline-category softmax sum over its \code{K}
categories (\code{K >= 3}); there is no latent variable. The predictor link is a
baseline-category softmax with the FIRST level as baseline
(\code{eta_state(1) = 0}); for state \verb{k = 2, ..., K},
\verb{eta_state(k) = X_x beta_x[block (k-1)]}, and
\verb{Pr(x = k | z) = exp(eta_state(k)) / sum_j exp(eta_state(j))}. The
coefficient vector \code{beta_x} is packed as \code{K - 1} blocks of the covariate
design width. The missing predictor must be an \strong{unordered factor},
\strong{character}, or \strong{integer category scores} with at least three levels, and
every category must appear among the observed values; a two-level predictor
uses \code{\link[=binomial]{binomial()}} (the binary route) and an ordered predictor uses
\code{\link[=cumulative_logit]{cumulative_logit()}}.
}
\details{
This is a predictor-model family tag consumed by \code{impute_model(family = )},
NOT a response family for \code{\link[=gllvmTMB]{gllvmTMB()}}. It is named and shaped to align with
the drmTMB \code{categorical()} constructor (the shared missing-data contract).
}
\examples{
impute_model(habitat ~ z, family = categorical())
}
\seealso{
\code{\link[=impute_model]{impute_model()}}, \code{\link[=mi]{mi()}}, \code{\link[=cumulative_logit]{cumulative_logit()}} for an ordered
missing predictor, and \code{\link[=binomial]{binomial()}} for a binary missing predictor.
}
