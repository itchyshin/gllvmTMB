% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-auto-residual.R
\name{check_auto_residual}
\alias{check_auto_residual}
\title{Check whether \code{link_residual = "auto"} is coherent for this fit}
\usage{
check_auto_residual(fit)
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}
}
\value{
Invisibly, a list with components \code{status} (one of \code{"ok"},
\code{"warn"}, or \code{"err"} -- though the \code{"err"} path is unreachable
from a successful return because \code{cli::cli_abort} halts) and
\code{messages} (a character vector of human-readable findings; empty
when \code{status = "ok"}).
}
\description{
Inspects the fit's per-row family vector and flags two configurations
that make the \code{link_residual = "auto"} path incoherent: within-trait
family mixing (errors) and ordinal-probit traits (warns; the latent
residual is already standardised at 1, so adding 1 again over-counts).
}
\section{Side effects}{

\itemize{
\item \code{"err"} path: \code{cli::cli_abort} is fired and the function does
not return.
\item \code{"warn"} path: \code{cli::cli_warn} is fired before the function
returns the list invisibly.
\item \code{"ok"} path: silent.
}
}

\seealso{
\code{\link[=extract_correlations]{extract_correlations()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}, \code{\link[=extract_Omega]{extract_Omega()}}.
}
