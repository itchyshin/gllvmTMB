% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-two-U-via-PIC.R
\name{compare_PIC_vs_joint}
\alias{compare_PIC_vs_joint}
\title{Cross-check: PIC-MOM vs joint-REML two-U (Gaussian / BM only)}
\usage{
compare_PIC_vs_joint(
  fit_joint_REML,
  tree,
  threshold = 0.5,
  d_phy = 1L,
  d_non = 1L
)
}
\arguments{
\item{fit_joint_REML}{A \code{gllvmTMB_multi} fit with a phylogenetic
component. Either \code{phylo_latent + phylo_unique} co-fit (Option B)
or the workaround \code{phylo_latent(species, d = T)} full-rank fit.}

\item{tree}{An \code{ape::phylo} object with tip labels matching the
species levels.}

\item{threshold}{Numeric (default \code{0.5}): relative-disagreement
threshold above which the \code{flag} is \code{TRUE}.}

\item{d_phy, d_non}{Integers passed through to
\code{\link[=extract_two_U_via_PIC]{extract_two_U_via_PIC()}}.}
}
\value{
A list with components:
\describe{
\item{\code{joint}}{Joint-REML estimates: \code{Sigma_phy}, \code{U_phy},
\code{Sigma_non}, \code{U_non} (legacy component names for the four-component
decomposition extracted from the fit).}
\item{\code{pic}}{Output of \code{\link[=extract_two_U_via_PIC]{extract_two_U_via_PIC()}}.}
\item{\code{agreement}}{Data frame with one row per component,
containing component name, RMSE, joint magnitude, and a
relative agreement score.}
\item{\code{flag}}{Logical: \code{TRUE} if any component disagrees beyond
\code{threshold}, suggesting an identifiability concern.}
}
}
\description{
Compares the PIC-MOM estimates (from \code{\link[=extract_two_U_via_PIC]{extract_two_U_via_PIC()}}) with
the corresponding components extracted from a joint-REML two-U fit.
Agreement = trustworthy joint estimate; disagreement = identifiability
concern, and the user is invited to investigate which axes
(Lambda_phy / psi_phy / Lambda_non / psi_non) disagree.
}
\details{
\strong{Scope.} This diagnostic inherits the Gaussian / Brownian-motion
restriction of \code{\link[=extract_two_U_via_PIC]{extract_two_U_via_PIC()}}. It is a fast complement to
the canonical likelihood-based PGLMM-stacked cross-check
(\code{\link[=compare_indep_vs_two_U]{compare_indep_vs_two_U()}} / \code{\link[=compare_dep_vs_two_U]{compare_dep_vs_two_U()}}), which works for every
family the engine supports.

Returns per-component RMSE between PIC-MOM and joint estimates of
\eqn{\boldsymbol\Sigma_\text{phy}}, \eqn{\boldsymbol\Sigma_\text{non}},
\eqn{\mathbf S_\text{phy}}, and \eqn{\mathbf S_\text{non}}. The
component-wise RMSE is the Frobenius norm of (PIC - joint), divided
by sqrt(T*T) for matrices and sqrt(T) for vectors. The \code{flag} is
\code{TRUE} when any RMSE exceeds \code{threshold} times the magnitude of the
joint estimate, suggesting weak identifiability.

If the parallel "Option B" engine extension that registers separate
joint slots for \code{phylo_latent} and \code{phylo_unique} is not yet merged,
a workaround is used: the joint-REML estimates are read from the
existing \code{phylo_latent(d = T)} full-rank fit + post-hoc principal-
factor split. Once Option B merges, this function will dispatch to
the proper two-U slots without API change.
}
\keyword{internal}
