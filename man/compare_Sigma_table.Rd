% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-sigma-table.R
\name{compare_Sigma_table}
\alias{compare_Sigma_table}
\title{Compare fitted Sigma-table rows with a known truth matrix}
\usage{
compare_Sigma_table(
  x,
  truth,
  level = "unit",
  part = c("total", "shared", "unique"),
  measure = c("covariance", "correlation"),
  entries = c("unique", "all", "upper", "lower", "offdiag", "diag"),
  link_residual = c("auto", "none")
)
}
\arguments{
\item{x}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}, an admitted \code{engine = "julia"}
bridge fit, or a data frame returned by \code{\link[=extract_Sigma_table]{extract_Sigma_table()}}.}

\item{truth}{Square numeric covariance or correlation matrix. Row and column
names should match the trait names in \code{x}; unnamed matrices are accepted
only when their dimension matches the traits in \code{x}.}

\item{level, part, measure, entries, link_residual}{Passed to
\code{\link[=extract_Sigma_table]{extract_Sigma_table()}} when \code{x} is a fitted model.}
}
\value{
A data frame with the columns from \code{\link[=extract_Sigma_table]{extract_Sigma_table()}} plus
\code{truth}, \code{error}, \code{abs_error}, and \code{comparison_status}.
}
\description{
\code{compare_Sigma_table()} joins report-ready \code{\link[=extract_Sigma_table]{extract_Sigma_table()}} rows to
a known covariance or correlation matrix. It is designed for simulation and
teaching articles that need estimate-vs-truth tables without hand-indexing
matrices inside the article.
}
\details{
Scope boundary: IN, the helper compares fitted or precomputed
\code{extract_Sigma_table()} rows against one supplied truth matrix (covered
by the package's Sigma-table validation tests).
PARTIAL, it is a table helper only: it does not compute uncertainty,
simulate data, or validate calibration. For the first visual comparison
layer, use \code{\link[=plot_Sigma_comparison]{plot_Sigma_comparison()}}; richer article-specific calibration
summaries remain future visualization work.
}
\examples{
rows <- data.frame(
  estimand = "R_unit[length,mass]",
  trait_i = "length",
  trait_j = "mass",
  i = 1L,
  j = 2L,
  level = "unit",
  component = "total",
  matrix = "R",
  estimate = 0.62,
  lower = NA_real_,
  upper = NA_real_,
  interval_method = "none",
  interval_status = "none",
  scale = "correlation",
  diagonal = FALSE,
  triangle = "upper"
)
truth_R <- matrix(c(1, 0.6, 0.6, 1), 2,
  dimnames = list(c("length", "mass"), c("length", "mass"))
)
compare_Sigma_table(rows, truth_R, measure = "correlation")
}
\seealso{
\code{\link[=extract_Sigma_table]{extract_Sigma_table()}}, \code{\link[=plot_Sigma_comparison]{plot_Sigma_comparison()}},
\code{\link[=plot_Sigma_table]{plot_Sigma_table()}}.
}
