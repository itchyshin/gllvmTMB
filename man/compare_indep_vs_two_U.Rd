% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-two-U-cross-check.R
\name{compare_indep_vs_two_U}
\alias{compare_indep_vs_two_U}
\title{Cheap diagonal cross-check for the paired phylogenetic decomposition (large T)}
\usage{
compare_indep_vs_two_U(
  fit_two_U,
  threshold = 0.1,
  phylo_vcv = NULL,
  phylo_tree = NULL
)
}
\arguments{
\item{fit_two_U}{A \code{gllvmTMB_multi} joint two-U fit, e.g. produced by
\code{gllvmTMB(value ~ 0 + trait + phylo_latent(species, d = K_phy) + phylo_unique(species) + unique(0 + trait | species), ...)}.
The cross-check refits the same data and family with
\code{phylo_dep + dep} and compares.}

\item{threshold}{Numeric (default \code{0.10}): relative-disagreement
threshold (Frobenius RMSE / Frobenius magnitude of the unstructured
estimate) above which a component is flagged.}

\item{phylo_vcv, phylo_tree}{Optional. The phylogenetic correlation
matrix or \code{ape::phylo} tree, only needed if \code{fit_two_U} was
produced by an older package version that did not store the
phylogeny on the fit. Default is to recover them from the fit
(\code{fit_two_U$phylo_vcv} / \code{fit_two_U$phylo_tree}).}
}
\value{
A list with components:
\describe{
\item{\code{joint}}{Joint two-U fit's implied \code{diag(Sigma_phy)} and
\code{diag(Sigma_non)} (named numeric vectors of length T).}
\item{\code{indep}}{The marginal-only baseline's \code{diag(Sigma_phy)} and
\code{diag(Sigma_non)} (same shape; may contain \code{NULL} if alt fit
failed).}
\item{\code{agreement}}{Data frame with rows for \code{Sigma_phy_diag} and
\code{Sigma_non_diag}, columns \code{rmse}, \code{indep_mag},
\code{rel_disagreement}, and \code{flag}.}
\item{\code{flag}, \code{threshold}, \code{alt_fit}}{As in
\code{\link[=compare_dep_vs_two_U]{compare_dep_vs_two_U()}}.}
}
}
\description{
Refits the user's data with \code{phylo_indep + indep} -- a stacked
multivariate generalisation of T univariate phylogenetic GLMMs in
the spirit of Williams et al. (2025) Eq. 3 -- using the same engine,
family, link, and grouping as the supplied two-U fit. Compares the
per-trait diagonals of the joint two-U fit's implied
\eqn{\boldsymbol\Sigma_{\mathrm{phy}}} (and \eqn{\boldsymbol\Sigma_{\mathrm{non}}})
against the marginal-only baseline. Tests \strong{only} the diagonals
(cheap); does not test the off-diagonal cross-trait covariances.
}
\details{
Use this diagnostic when T is large (T \eqn{\geq} 30) and the
unstructured fit needed by \code{\link[=compare_dep_vs_two_U]{compare_dep_vs_two_U()}} is intractable.
Otherwise prefer \code{\link[=compare_dep_vs_two_U]{compare_dep_vs_two_U()}} (gold standard).

Cross-check intent: the per-trait diagonals are the simplest summary
the joint two-U fit and the marginal baseline both target. If the
diagonals already disagree beyond the threshold, no inspection of
the cross-trait off-diagonals is warranted.
}
\examples{
\dontrun{
diag <- compare_indep_vs_two_U(fit)
diag$flag
diag$agreement
}
}
\references{
Williams, M. J., McGillycuddy, M., Drobniak, S. M., Bolker, B. M.,
Warton, D. I., & Nakagawa, S. (2025). Fast phylogenetic generalised
linear mixed-effects modelling using the glmmTMB R package.
\emph{bioRxiv} 2025.12.20.695312.
\doi{10.1101/2025.12.20.695312}
}
\seealso{
\code{\link[=compare_dep_vs_two_U]{compare_dep_vs_two_U()}} (canonical, full Sigma);
\code{\link[=extract_Sigma]{extract_Sigma()}}.
}
