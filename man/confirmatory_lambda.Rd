% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confirmatory-lambda.R
\name{confirmatory_lambda}
\alias{confirmatory_lambda}
\title{Build a confirmatory \code{lambda_constraint} matrix from group membership}
\usage{
confirmatory_lambda(
  species,
  group,
  d,
  loads_on,
  anchors = NULL,
  axis_labels = NULL
)
}
\arguments{
\item{species}{Character vector of species names. Becomes the
rownames of the returned matrix. Must match the species (i.e.
\code{trait} levels) used in the \code{gllvmTMB()} fit.}

\item{group}{Character or factor vector parallel to \code{species}; the
functional-group code for each species.}

\item{d}{Integer; number of latent axes (matches the \code{d} argument
of \code{latent(0 + trait | site, d = d)}).}

\item{loads_on}{Named list or named integer vector mapping group code
to latent axis index. For example, \code{list(A = 1L, B = 2L)} means
species in group A load on axis 1 only (zero on all other axes),
species in group B load on axis 2 only. Groups present in \code{group}
but absent from \code{loads_on} remain free on all axes.}

\item{anchors}{Optional length-\code{d} character vector; one anchor
species per axis (pinned at \code{+1}). If \code{NULL} (the default), anchors
are auto-picked as the first species belonging to the group that
loads on each axis. Use \code{NA} to skip the anchor on a specific axis.}

\item{axis_labels}{Optional length-\code{d} character vector for the
matrix column names. Defaults to \code{c("LV1", "LV2", ...)}.}
}
\value{
An \code{length(species)} by \code{d} numeric matrix with \code{NA} entries
(estimated), \code{0} entries (pinned to zero), and \code{1} entries (pinned
at the anchor), ready to pass to \code{gllvmTMB()} as
\code{lambda_constraint = list(unit = M)}.
}
\description{
Construct the \code{n_species} by \code{d} confirmatory loading-constraint matrix
\code{M} from a discrete functional-group hypothesis. Each named group is
pinned to load on a specific latent axis (zero on all other axes), and
one anchor species per axis is pinned at \code{+1} to fix the scale. Groups
present in \code{group} but absent from \code{loads_on} remain free on all axes.
}
\details{
This is the recommended starting point for confirmatory JSDMs where
prior knowledge takes the form "species in group A respond to
gradient 1, species in group B respond to gradient 2, ...". For
free-form constraint patterns (psychometric CFA, idiosyncratic
species-by-species pins), build the matrix directly; for purely
statistical identification scaffolding when no biology is in play,
see \code{\link[=suggest_lambda_constraint]{suggest_lambda_constraint()}}.
}
\examples{
species <- c(paste0("A_", 1:3), paste0("B_", 1:3), paste0("C_", 1:4))
group   <- c(rep("A", 3), rep("B", 3), rep("C", 4))

# Group A loads on axis 1; group B loads on axis 2; group C is free.
M <- confirmatory_lambda(
  species  = species,
  group    = group,
  d        = 2L,
  loads_on = list(A = 1L, B = 2L)
)
M

}
\seealso{
\code{\link[=suggest_lambda_constraint]{suggest_lambda_constraint()}} for the lower-level statistical
identification scaffold, \code{\link[=gllvmTMB]{gllvmTMB()}} for the \code{lambda_constraint}
argument.
}
