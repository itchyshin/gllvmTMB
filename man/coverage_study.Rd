% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage-study.R
\name{coverage_study}
\alias{coverage_study}
\title{Empirical coverage-rate study for a fitted gllvmTMB_multi}
\usage{
coverage_study(
  fit,
  parm = NULL,
  n_reps = 30L,
  methods = c("wald", "profile"),
  level = 0.95,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} fit.}

\item{parm}{Character vector of profile-target labels (see
\code{\link[=profile_targets]{profile_targets()}}). Default \code{NULL} selects all
profile-ready direct targets except packed Lambda entries
(which are rotation-ambiguous and would falsely look
under-covered). For ratio quantities (communality, repeatability,
phylogenetic signal, trait correlations), use the matching
\code{extract_*(method = "profile")} extractor with \code{nsim} for
bootstrap coverage instead -- those have their own machinery.}

\item{n_reps}{Integer number of parametric-bootstrap replicates.
Default \code{30L} for a routine local check. Audit recommendation
for a publication-quality validation is \code{n_reps = 50L}
(or higher) per the Phase 1b validation milestone.}

\item{methods}{Character vector of CI methods to evaluate.
Default \code{c("wald", "profile")}. Each rep computes the CI via
each method and counts coverage separately. Bootstrap is not
evaluated here because it would recurse parametric bootstrap
inside parametric bootstrap (slow + statistically ambiguous);
use \code{bootstrap_Sigma()} separately for the canonical bootstrap
coverage path.}

\item{level}{Confidence level. Default \code{0.95}. Coverage is
measured against this nominal level.}

\item{seed}{Optional integer seed.}

\item{progress}{Logical. Default \code{TRUE} prints a one-line
progress note per 10\% of \code{n_reps}.}
}
\value{
An object of class \code{gllvmTMB_coverage_study} with
components:
\describe{
\item{\verb{$coverage}}{Data frame, one row per (parm x method):
\code{parm}, \code{method}, \code{n_reps}, \code{n_covered}, \code{n_excluded},
\code{rate} (coverage rate, between 0 and 1), \code{passes_94pct}
(logical, the audit's exit-gate flag).}
\item{\verb{$intervals}}{Long-format data frame, one row per
(rep, parm, method): \code{rep}, \code{parm}, \code{method}, \code{truth},
\code{lower}, \code{upper}, \code{covered}. Useful for diagnostic plots
and re-aggregation.}
\item{\verb{$n_failed_refits}}{Integer; how many of \code{n_reps}
replicates had a refit that failed to converge. Replicates
are NOT included in the coverage rate denominator if their
refit failed (a separate \code{n_excluded} column tracks this).}
\item{\verb{$call}}{The \code{match.call()} of the invocation.}
}
}
\description{
For each of \code{n_reps} parametric-bootstrap replicates, simulates
a new dataset from the fitted model, refits the same formula,
computes confidence intervals via the requested \code{methods}, and
counts the fraction of CIs that contain the \strong{original fit's}
point estimate (the "truth" for this parametric-bootstrap study).
}
\details{
This is the empirical-validation counterpart to
\code{gllvmTMB_check_consistency()} (which tests whether the Laplace
marginal score is centred) and \code{check_identifiability()} (which
tests Procrustes-aligned loading recovery). Per the audit's
Phase 1b validation milestone, \strong{empirical coverage >= 94\%} on
the parameters that matter is the recommended sanity check
before treating CIs as publication-ready.

When coverage rates fall below 94\%, common causes are:
\itemize{
\item \strong{Boundary parameter}: variance pinned at zero -> Wald CIs
under-cover by construction; profile CIs are one-sided.
\code{confint_inspect()} confirms this visually.
\item \strong{Weak identifiability}: data don't constrain the parameter
well; rates can drop to near zero. \code{check_identifiability()}
confirms this for the loading entries.
\item \strong{Non-quadratic profile surface}: Wald assumes symmetry, so
under-covers on the long-tail side. Profile CIs are accurate
here. \code{confint_inspect()} shows the curve shape.
\item \strong{Mis-specified model}: the bootstrap DGP doesn't match the
fitted model. Refit and retry.
}
}
\examples{
\dontrun{
fit <- gllvmTMB(value ~ 0 + trait +
                latent(0 + trait | site, d = 1) +
                unique(0 + trait | site),
                data  = sim$data,
                trait = "trait",
                unit  = "site")
res <- coverage_study(fit, n_reps = 30L, seed = 1)
res$coverage
# passes_94pct column flags which targets meet the audit's
# >= 94\% empirical-coverage exit gate.
}

}
\seealso{
\code{\link[=profile_targets]{profile_targets()}} (the parm vocabulary),
\code{\link[=confint.gllvmTMB_multi]{confint.gllvmTMB_multi()}} (the CI machinery being audited),
\code{\link[=confint_inspect]{confint_inspect()}} (visual diagnosis when coverage falls
short), \code{\link[=check_identifiability]{check_identifiability()}} and
\code{\link[=gllvmTMB_check_consistency]{gllvmTMB_check_consistency()}} (complementary
simulation-based validations).
}
