% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-predictor.R
\name{cumulative_logit}
\alias{cumulative_logit}
\title{Cumulative-logit ordered categorical missing-predictor family}
\usage{
cumulative_logit()
}
\value{
A \code{gllvmTMB_impute_family} object for the \code{family} argument of
\code{\link[=impute_model]{impute_model()}}.
}
\description{
\code{cumulative_logit()} declares the predictor-model family for an \strong{ordered
categorical} missing predictor used inside \code{mi()} with
\code{impute_model(family = cumulative_logit())}. The missing ordered predictor is marginalised
EXACTLY by a finite-state cumulative-logit sum over its \code{K} categories
(\code{K >= 3}); there is no latent variable. The predictor link is cumulative
\strong{logit}, with \code{K - 1} free cutpoints \verb{c_1 < ... < c_\{K-1\}} and a cell
probability \verb{Pr(x = k | z) = F(c_k - eta_x) - F(c_\{k-1\} - eta_x)}, where
\verb{eta_x = X_x beta_x} (no separate predictor intercept; the cutpoints carry
the location). The missing predictor must be an \strong{ordered factor} (or
integer category scores) with at least three levels; a two-level predictor
uses \code{\link[=binomial]{binomial()}} (the binary route).
}
\details{
This is a predictor-model family tag consumed by \code{impute_model(family = )},
NOT a response family for \code{\link[=gllvmTMB]{gllvmTMB()}}. The predictor cumulative-logit link is DISTINCT from the
\code{\link[=ordinal_probit]{ordinal_probit()}} RESPONSE family (a probit threshold model).
}
\examples{
impute_model(score ~ z, family = cumulative_logit())
}
\seealso{
\code{\link[=impute_model]{impute_model()}}, \code{\link[=binomial]{binomial()}} for a binary missing predictor,
and \code{\link[=ordinal_probit]{ordinal_probit()}} for the ordinal RESPONSE family.
}
