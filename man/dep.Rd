% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{dep}
\alias{dep}
\title{Full unstructured trait covariance: \code{dep(0 + trait | g)}}
\usage{
dep(formula)
}
\arguments{
\item{formula}{\code{0 + trait | g} style formula (LHS is the trait
factor, typically \code{0 + trait}; RHS is the grouping factor).}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for the \strong{always-alone full-unstructured} trait
covariance term. Fits a full \eqn{T \times T} \eqn{\boldsymbol\Sigma}
with \eqn{T(T+1)/2} free parameters via a Cholesky parameterisation
\eqn{\boldsymbol\Sigma = \mathbf{L}\mathbf{L}^\top}. Mathematically
identical to \code{latent(0 + trait | g, d = T)} standalone (where \eqn{T}
is the number of traits) -- the engine's existing packed-triangular
\eqn{\boldsymbol\Lambda} at full rank IS the Cholesky factor of an
unstructured \eqn{\boldsymbol\Sigma}. The keyword choice is
documentary: \code{dep} declares user intent that the full unstructured
trait covariance is the model.
}
\details{
This completes the structural-mode quartet:

\describe{
\item{\strong{Decomposition} (\code{latent + unique})}{Shared low-rank
loadings plus trait-specific residual:
\eqn{\boldsymbol\Sigma = \boldsymbol\Lambda \boldsymbol\Lambda^\top + \boldsymbol\Psi}.
\eqn{TK + T} parameters (rotation-removed).}
\item{\strong{Marginal} (\code{indep} standalone)}{Per-trait variance with
identity correlation: \eqn{\boldsymbol\Sigma = \mathrm{diag}(\sigma^2_t)}.
\eqn{T} parameters.}
\item{\strong{Full / unstructured} (\code{dep} standalone)}{Free unstructured
\eqn{\boldsymbol\Sigma}, PSD via Cholesky.
\eqn{T(T+1)/2} parameters.}
}

Use \code{dep()} when you want to commit to the full unstructured
interpretation explicitly. Maintainer's framing: this is just a
standard multivariate response model. Computational scope: tractable
for \eqn{T \le \sim 30} with the default optimiser; for larger
\eqn{T}, prefer \code{latent(d = K)} with \eqn{K \ll T} as the
rank-reduced approximation.
\subsection{Mutual exclusion}{

Combining \code{dep(0 + trait | g)} with \code{latent(0 + trait | g, d = K)}
on the same grouping is \strong{over-parameterised}. Combining \code{dep} with
\code{unique} or \code{indep} on the same grouping is \strong{redundant} (\code{dep}
standalone already includes the trait-level diagonal). The parser
raises \code{cli::cli_abort()} in any of these cases.
}
}
\examples{
\dontrun{
# Full unstructured trait covariance fit:
fit <- gllvmTMB(value ~ 0 + trait + dep(0 + trait | site),
                data = df, trait = "trait", unit = "site")

# The mathematically-equivalent decomposition form (full rank):
fit <- gllvmTMB(value ~ 0 + trait + latent(0 + trait | site, d = T),
                data = df, trait = "trait", unit = "site")

# ERROR: dep + latent on the same grouping is over-parameterised.
# gllvmTMB(value ~ 0 + trait +
#           dep(0 + trait | site) +
#           latent(0 + trait | site, d = 2), data = df)
}
}
\seealso{
\code{\link[=latent]{latent()}}, \code{\link[=unique]{unique()}}, \code{\link[=indep]{indep()}}, \code{\link[=phylo_dep]{phylo_dep()}},
\code{\link[=spatial_dep]{spatial_dep()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}.
}
