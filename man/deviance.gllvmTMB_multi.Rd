% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-gllvmTMB.R
\name{deviance.gllvmTMB_multi}
\alias{deviance.gllvmTMB_multi}
\title{Deviance of a fitted gllvmTMB model}
\usage{
\method{deviance}{gllvmTMB_multi}(object, ...)
}
\arguments{
\item{object}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{...}{Currently unused.}
}
\value{
A single numeric value, \code{-2 * as.numeric(logLik(object))}. On a
ridged fit this carries the same MAP-point caveat as \code{logLik()}: the
value is \code{-2} times the unpenalised likelihood at the penalised
estimate, not at its own maximum, and the warning issued by \code{logLik()}
is inherited unchanged.
}
\description{
\code{-2 * logLik(object)}, delegating through \code{\link[=logLik.gllvmTMB_multi]{logLik.gllvmTMB_multi()}}
rather than recomputing anything independently. This is deliberate: on a
penalised (ridged) fit -- \code{aghq_ridge} set to a finite value --
\code{logLik()} already discloses, with a warning, that the returned value is
the unpenalised log-likelihood evaluated AT a penalised MAP point, not at
its own maximum. Because \code{deviance()} is defined purely in terms of
\code{logLik()} here, that same disclosure fires exactly once and is not
repeated or re-derived.
}
