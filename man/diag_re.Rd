% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unique-keyword.R
\name{diag_re}
\alias{diag_re}
\alias{diag-keyword}
\title{Trait-specific unique variance term: \code{unique(0 + trait | group)}}
\arguments{
\item{formula}{A formula of the form \code{0 + trait | group} (LHS
is the response factor — typically \code{0 + trait}; RHS is the
grouping factor over which the trait-specific variances are iid).}
}
\value{
A formula marker; never evaluated as a call.
}
\description{
A formula keyword for adding \strong{trait-specific independent random
effects} to a \code{gllvmTMB()} fit, complementing the reduced-rank
shared-variance term \code{latent(0 + trait | group, d = K)}. Using the two
together implements the standard psychometric / behavioural-syndrome
decomposition
}
\details{
\deqn{\Sigma_g = \Lambda \Lambda^\top + S,}

where \eqn{\boldsymbol\Lambda \boldsymbol\Lambda^\top} is the
low-rank shared component (from \code{latent()}) and \eqn{\mathbf S} is a
diagonal matrix of trait-specific unique variances (from \code{unique()}).
This is the decomposition every published GLLVM treatment uses
(Bartholomew et al. 2011; McGillycuddy et al. 2025; Nakagawa et al.
\emph{in prep}, Eq. 30).
\subsection{Why you almost always want \code{unique()} for Gaussian / lognormal / Gamma fits}{

If your formula has only \code{latent(0 + trait | site, d = K)} and no
\code{unique(0 + trait | site)}, the engine fits only the latent-implied
\eqn{\boldsymbol\Lambda \boldsymbol\Lambda^\top}. Each trait's
between-site variance is then constrained to come \strong{entirely} from
the K shared latent factors — there is no slot for trait-specific
between-site variance not captured by those factors. Two consequences:
\enumerate{
\item \strong{Correlations are inflated.} The reported correlation matrix
\eqn{R_B = D^{-1/2} \boldsymbol\Lambda \boldsymbol\Lambda^\top D^{-1/2}}
uses too small a diagonal (it omits the unique component), so
cross-trait correlations come out larger than the true
\eqn{R_B = D^{-1/2}(\boldsymbol\Lambda \boldsymbol\Lambda^\top + \mathbf S) D^{-1/2}}.
\item \strong{Communality reaches 1 by construction.} Communality
\eqn{c_t^2 = (\Lambda \Lambda^\top)_{tt} / \Sigma_{tt}}
is identically 1 when \eqn{\mathbf S = \mathbf 0}; you cannot
quantify how much of trait \eqn{t}'s variance is shared with the
others.
}

Adding \code{+ unique(0 + trait | site)} gives the engine a per-trait
variance parameter \eqn{s_{B,t}^2}, restoring the full decomposition.
}

\subsection{When you do \emph{not} need \code{unique()}}{
\itemize{
\item \strong{Binary responses} with a probit / logit / cloglog link. The
link function fixes an implicit residual variance on the latent
scale (1 for probit, \eqn{\pi^2/3} for logit, \eqn{\pi^2/6} for
cloglog), which acts as the implicit unique component. Adding an
explicit \code{diag()} term on top is typically not identified.
\item \strong{Phylogenetic shared term}. The \code{phylo_latent(species, d = K)} term
has no associated \code{unique()} because the phylogenetic prior is
already structured on tip × tip via the tree; trait-specific
\emph{unique} variance lives separately at the non-phylogenetic
species tier.
\item \strong{Confirmatory factor models}. Sometimes domain knowledge tells
you the latent-only model is correct (no trait-specific residuals);
confirm with a likelihood-ratio test.
}
}

\subsection{Two-level (between + within) models}{

For repeated-measures / behavioural-syndrome data, the recommended
pattern is \strong{two \code{latent() + unique()} pairs} (Nakagawa et al. \emph{in prep}):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{value ~ 0 + trait +
        latent(0 + trait | individual, d = d_B) + unique(0 + trait | individual) +
        latent(0 + trait | obs_id,     d = d_W) + unique(0 + trait | obs_id)
}\if{html}{\out{</div>}}

giving \eqn{\boldsymbol\Sigma_B = \boldsymbol\Lambda_B \boldsymbol\Lambda_B^\top + \mathbf S_B}
and \eqn{\boldsymbol\Sigma_W = \boldsymbol\Lambda_W \boldsymbol\Lambda_W^\top + \mathbf S_W}.
}

\subsection{Phylogenetic + non-phylogenetic species-level models}{

For a species-level fit with phylogeny (Nakagawa et al. \emph{in prep},
Eq. 19), the natural three-component decomposition is

\deqn{\Omega = \Sigma_\mathrm{phy} + \Sigma_\mathrm{non,shared} + U.}

In gllvmTMB syntax:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{value ~ 0 + trait +
        phylo_latent(species, d = K_phy) +             # Sigma_phy
        latent(0 + trait | species, d = K_non) +       # Sigma_non,shared
        unique(0 + trait | species)                    # U
}\if{html}{\out{</div>}}

\code{\link[=extract_Sigma]{extract_Sigma()}} with \code{level = "phy"} returns \eqn{\Sigma_\mathrm{phy}};
\code{level = "B"} returns \eqn{\Sigma_\mathrm{non,shared} + U}
(the non-phylogenetic species-level covariance). Their sum is
\eqn{\boldsymbol\Omega}.
}

\subsection{Per-row \code{unique()} and \code{sigma_eps}: auto-suppression}{

For Gaussian / lognormal / Gamma fits, the engine also estimates a
single observation-scale residual \code{sigma_eps} (the σ_ε of the response).
If you place \code{unique(0 + trait | g)} at a grouping \code{g} that has \strong{one
row per (trait, g) cell} (i.e. the unique random effects are at the
per-row / per-observation level), the unique-S parameters and
\code{sigma_eps} are jointly unidentifiable — only the sum
\eqn{\mathrm{sd}_g[t]^2 + \sigma_\varepsilon^2} is identified.

In that case the engine \strong{auto-suppresses} \code{sigma_eps} (fixed at
\eqn{\approx 10^{-3}} of \code{sd(y)}) so the unique(S) random effects fully
absorb the row-level residual variance, and emits a one-shot message
announcing the suppression. This matches the user's intent when they
write a per-row \code{unique()} term: they want the unique-S to \emph{be} the
row-level residual, not to compete with \code{sigma_eps} for it.

If you have multiple rows per (trait, g) cell (e.g. \code{unique(0 + trait | site)} with several species per site), \code{sigma_eps} is the
\emph{within-cell} residual and the unique-S random effects are the
\emph{between-cell} per-trait variance — both are separately identified and
both are estimated.
}

\subsection{Family-aware interpretation}{

The same \code{unique(0 + trait | g)} formula keyword has a slightly
different meaning across families, depending on whether the response
carries an observation-layer residual:

For Gaussian / lognormal / Gamma fits, \code{unique()} estimates the
trait-specific residual variance on the (log-)response scale. For
binomial fits with probit / logit / cloglog links, the link
function fixes a distribution-specific implicit residual; an
explicit \code{unique()} is identifiable only when there are repeated
rows per cell. For Poisson and other log-link families,
\code{unique(0 + trait | unit_obs)} doubles as an observation-level
random effect (OLRE / additive overdispersion).

Across families the unifying rule is: \code{unique(0 + trait | g)} is the
"effective per-trait unique-variance parameter at tier \code{g}", with the
family determining what counts as observation-layer vs latent-scale
residual. See the \code{link_residual} argument of \code{\link[=extract_Sigma]{extract_Sigma()}} for
how the family-specific implicit residual is added to the diagonal of
the reported Σ.
}

\subsection{Note on the function name}{

R's base \code{\link[base:diag]{base::diag()}} returns the diagonal of a matrix. The
formula keyword \code{unique(0 + trait | group)} inside a \code{gllvmTMB()}
formula is recognised by the formula parser at fit time; it is
\strong{not} a call to \code{\link[base:diag]{base::diag()}}. This help page documents the
formula keyword.
}
}
\examples{
\dontrun{
# Behavioural-syndrome / two-level pattern:
fit <- gllvmTMB(
  value ~ 0 + trait +
          latent(0 + trait | individual, d = 2) + unique(0 + trait | individual) +
          latent(0 + trait | obs_id,    d = 1) + unique(0 + trait | obs_id),
  data = df, unit = "individual"
)
extract_Sigma(fit, level = "B", part = "shared")$Sigma   # Lambda_B Lambda_B^T
extract_Sigma(fit, level = "B", part = "unique")$s       # diag(S_B)
extract_Sigma(fit, level = "B", part = "total")$Sigma    # both, summed
}
}
\references{
\itemize{
\item \strong{Bartholomew, Knott & Moustaki} (2011) \emph{Latent Variable Models
and Factor Analysis: A Unified Approach.} Wiley.
ISBN 978-0-470-97192-5.
\item \strong{McGillycuddy, Popovic, Bolker & Warton} (2025) Parsimoniously
Fitting Large Multivariate Random Effects in glmmTMB.
\emph{J. Stat. Softw.} 112(1).
\url{https://doi.org/10.18637/jss.v112.i01}
\item \strong{Nakagawa et al.} (\emph{in prep}) Quantifying between- and
within-individual correlations and the degree of trait
integration: leveraging latent variable modelling to study
behavioural syndromes and other phenotypic integration.
}
}
\seealso{
\code{\link[=extract_Sigma]{extract_Sigma()}} — pull \eqn{\boldsymbol\Sigma},
\eqn{\boldsymbol\Lambda \boldsymbol\Lambda^\top}, or
\eqn{\mathbf S} from the fit; \code{\link[=phylo_scalar]{phylo_scalar()}}, \code{\link[=phylo_latent]{phylo_latent()}}, \code{\link[=spatial_unique]{spatial_unique()}},
\code{\link[=re_int]{re_int()}}.
}
\keyword{internal}
