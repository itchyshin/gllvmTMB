% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-helpers.R
\name{diagnose_kernel_separability}
\alias{diagnose_kernel_separability}
\title{Diagnose fixed-kernel separability before fitting}
\usage{
diagnose_kernel_separability(
  ...,
  thresholds = c(near_orthogonal = 0.25, high = 0.7)
)
}
\arguments{
\item{...}{Two or more named numeric square kernel matrices with the same
dimensions and level order.}

\item{thresholds}{Named numeric vector with entries \code{near_orthogonal} and
\code{high}. Similarity below \code{near_orthogonal} is labelled
\code{"near_orthogonal"}; similarity below \code{high} is \code{"moderate"}; similarity
at or above \code{high} is \code{"high"}.}
}
\value{
A list of class \code{gllvmTMB_kernel_separability} with:
\describe{
\item{similarity}{A symmetric matrix of off-diagonal Frobenius-style
similarities between kernels.}
\item{pairs}{A data frame with pair labels, similarity, overlap class, and
a conservative recommendation.}
\item{thresholds}{The thresholds used for the classes.}
\item{note}{A short claim-boundary note.}
}
}
\description{
\code{diagnose_kernel_separability()} compares two or more dense fixed kernels on
the same levels before they are used in a multi-kernel model. It is a pre-fit
diagnostic rather than evidence that the components are separately
identifiable: when candidate
kernels are highly overlapping, component-specific \code{Gamma} blocks are weak
evidence and should be treated as descriptive unless simulations justify the
split.

What is covered (partially): use this helper to screen candidate \code{K_phy} and
\code{K_tip} definitions, including raw-network and residualized-network choices,
before fitting \code{kernel_latent(..., name = ...)} tiers. What is not covered:
this is a diagnostic, not recovery evidence, interval calibration, or an
in-engine identifiability proof.
}
\examples{
A_H <- diag(2)
A_P <- diag(2)
rownames(A_H) <- colnames(A_H) <- c("H1", "H2")
rownames(A_P) <- colnames(A_P) <- c("P1", "P2")
W_phy <- matrix(c(1, 0.2, 0.2, 1), 2, 2,
  dimnames = list(rownames(A_H), rownames(A_P)))
W_tip <- matrix(c(0.2, 1, 1, 0.2), 2, 2,
  dimnames = list(rownames(A_H), rownames(A_P)))
K_phy <- make_cross_kernel(A_H, A_P, W_phy, rho = 0.5)
K_tip <- make_cross_kernel(A_H, A_P, W_tip, rho = 0.5)
diagnose_kernel_separability(phy = K_phy, tip = K_tip)

}
