% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proportions-ci.R
\name{.proportions_bootstrap_ci}
\alias{.proportions_bootstrap_ci}
\title{Parametric-bootstrap CIs for per-(trait, component) variance proportions}
\usage{
.proportions_bootstrap_ci(
  fit,
  components = NULL,
  trait_idx = NULL,
  level = 0.95,
  nsim = 500L,
  seed = NULL
)
}
\arguments{
\item{fit}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{components}{Character vector of component names. \code{NULL}
(default) uses all components present.}

\item{trait_idx}{Integer indices of traits, or \code{NULL} for all.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{nsim}{Number of bootstrap replicates. Default \code{500}.}

\item{seed}{Optional RNG seed for reproducibility.}
}
\value{
A data frame with columns \code{trait}, \code{component}, \code{proportion},
\code{lower}, \code{upper}, \code{method}. \code{method = "bootstrap"} for percentile
rows, \code{"(unavailable)"} for link-residual rows.
}
\description{
Simulate \code{nsim} response vectors via \code{simulate(fit, nsim = nsim)},
refit the same formula on each, and recompute \code{extract_proportions()}.
Percentile-quantile bounds across replicates that converged. Reuses
the formula-reconstruction and auxiliary-args plumbing from
\code{\link[=bootstrap_Sigma]{bootstrap_Sigma()}} so phylogenetic / spatial / lambda-constraint
fits refit cleanly.
}
\keyword{internal}
