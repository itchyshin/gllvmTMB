% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proportions-ci.R
\name{.proportions_wald_ci}
\alias{.proportions_wald_ci}
\title{Wald (delta-method) CIs for per-(trait, component) variance proportions}
\usage{
.proportions_wald_ci(fit, components = NULL, trait_idx = NULL, level = 0.95)
}
\arguments{
\item{fit}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{components}{Character vector of component names. \code{NULL}
(default) uses all components present in
\code{\link[=extract_proportions]{extract_proportions()}}.}

\item{trait_idx}{Integer indices of traits, or \code{NULL} for all.}

\item{level}{Confidence level. Default \code{0.95}.}
}
\value{
A data frame with columns \code{trait}, \code{component}, \code{proportion},
\code{lower}, \code{upper}, \code{method}. \code{method = "wald"} for delta-method
rows, \code{"(unavailable)"} for link-residual rows (bounds collapse to
point for fixed-scale families; bounds are NA for mean-dependent
families).
}
\description{
Numerical-Jacobian Wald CI on the logit-transformed proportion
\verb{p_c,t = Sigma_c[tt] / Sigma_total[tt]}. The Jacobian is built by
perturbing each fixed parameter and re-reading the relevant
\verb{Lambda_*} / \verb{sd_*} slots from \code{fit$tmb_obj$report()}; the asymptotic
covariance is \verb{J Cov(theta_fix) J^T} from
\code{fit$sd_report$cov.fixed}. Bounds are computed on \code{z = qlogis(p)}
(symmetric Wald) and back-transformed via \code{plogis()}, so they always
lie in \eqn{[0, 1]} and respect the bounded-support asymmetry.
}
\keyword{internal}
