% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{extract_ICC_site}
\alias{extract_ICC_site}
\title{Site / individual-level ICC per trait}
\usage{
extract_ICC_site(fit, link_residual = c("auto", "none"))
}
\arguments{
\item{fit}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{link_residual}{For binomial fits: \code{"auto"} (default) adds the
link-specific implicit residual to \eqn{(\boldsymbol\Sigma_W)_{tt}};
\code{"none"} returns ICC on the latent/Psi-implied scale only.}
}
\value{
Numeric vector indexed by trait, or \code{NULL} if either Sigma_B or
Sigma_W is unavailable.
}
\description{
\deqn{\mathrm{ICC}_t \;=\; \frac{(\boldsymbol\Sigma_B)_{tt}}{(\boldsymbol\Sigma_B)_{tt} + (\boldsymbol\Sigma_W)_{tt}}.}
}
\details{
Calls \code{\link[=extract_Sigma]{extract_Sigma()}} internally for both levels with \code{part = "total"},
so the diagonal of each \eqn{\boldsymbol\Sigma} includes the
\eqn{\boldsymbol\Psi} component when it is present. Ordinary \code{latent()}
includes \eqn{\boldsymbol\Psi} by default; if either level uses
\code{latent(..., residual = FALSE)}, the corresponding advisory message
fires and the ICC is computed against the no-Psi diagonal.

For binomial fits the implicit link residual is included in the
within-unit variance by default (matching the marginal latent-scale
ICC convention); set \code{link_residual = "none"} to suppress.
}
\seealso{
\code{\link[=extract_proportions]{extract_proportions()}} for the canonical per-trait variance
decomposition (B / W / phy / link-residual shares); \code{\link[=extract_Sigma]{extract_Sigma()}}
for the unified covariance API; \code{\link[=extract_communality]{extract_communality()}}.
}
\keyword{internal}
