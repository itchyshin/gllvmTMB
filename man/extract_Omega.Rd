% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-omega.R
\name{extract_Omega}
\alias{extract_Omega}
\title{Total trait covariance Omega summed across requested tiers}
\usage{
extract_Omega(fit, tiers = NULL, link_residual = c("auto", "none"))
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} fit.}

\item{tiers}{Character vector. Subset of \code{c("B", "W", "phy")}. Default
\code{NULL} auto-detects: includes \code{"phy"} if \code{phylo_latent()} is in the
formula, \code{"B"} if any \code{latent()}/\code{unique()} at \code{unit}, \code{"W"} if any
\code{latent()}/\code{unique()} at \code{unit_obs}.}

\item{link_residual}{For non-Gaussian fits: \code{"auto"} (default) adds a
per-trait link-specific implicit residual to the diagonal of the
summed \code{Omega} (once, not per tier — see "Family-aware link residuals"
in \code{\link[=extract_Sigma]{extract_Sigma()}}); mixed-family fits get the residual implied by
each trait's family/link. \code{"none"} returns the latent+unique-implied scale.
Gaussian / lognormal-only fits are unaffected.}
}
\value{
A list with \code{Omega} (T × T summed covariance), \code{R_Omega}
(correlation), \code{tiers_used} (which tiers were actually summed),
\code{note} (notes from each underlying \code{\link[=extract_Sigma]{extract_Sigma()}} call), and
(when \code{link_residual = "auto"}) \code{residual_split} — the per-trait
\eqn{\sigma^2_d / \sigma^2_e / \sigma^2_\text{total}} data frame
from \code{\link[=extract_residual_split]{extract_residual_split()}}.
}
\description{
Sum of \eqn{\boldsymbol\Sigma_\text{tier}} matrices across the user-
selected tiers. The canonical PGLLVM use case is the species-level
three-piece decomposition (Nakagawa et al. \emph{in prep}, Eq. 19):
\deqn{\boldsymbol\Omega \;=\; \boldsymbol\Sigma_\text{phy} \;+\; \boldsymbol\Sigma_\text{non,shared} \;+\; \mathbf S_\text{non}.}
Here \code{tiers = c("phy", "B")} returns
\eqn{\boldsymbol\Sigma_\text{phy} + \boldsymbol\Sigma_\text{B}} where
\eqn{\boldsymbol\Sigma_B = \boldsymbol\Lambda_B \boldsymbol\Lambda_B^{\!\top} + \boldsymbol\Psi_B}
aggregates the non-phylogenetic shared and unique components.
}
\details{
For two-level behavioural-syndrome fits, \code{tiers = c("B", "W")}
returns the \strong{phenotypic} trait covariance
\eqn{\boldsymbol\Sigma_P = \boldsymbol\Sigma_B + \boldsymbol\Sigma_W}
(Nakagawa et al. \emph{in prep}, Eq. 28).
}
\examples{
\dontrun{
# PGLLVM three-piece total
om <- extract_Omega(fit, tiers = c("phy", "B"))
round(om$R_Omega, 2)

# Phenotypic covariance from a two-level behavioural-syndromes fit
om2 <- extract_Omega(fit, tiers = c("B", "W"))
}
}
\seealso{
\code{\link[=extract_Sigma]{extract_Sigma()}}; \code{\link[=extract_phylo_signal]{extract_phylo_signal()}};
\code{\link[=extract_proportions]{extract_proportions()}}; \code{\link[=extract_residual_split]{extract_residual_split()}}.
}
