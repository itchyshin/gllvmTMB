% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-sigma-table.R
\name{extract_Sigma_table}
\alias{extract_Sigma_table}
\title{Extract a report-ready table of Sigma or correlation entries}
\usage{
extract_Sigma_table(
  fit,
  level = "unit",
  part = c("total", "shared", "unique"),
  measure = c("covariance", "correlation"),
  entries = c("unique", "all", "upper", "lower", "offdiag", "diag"),
  link_residual = c("auto", "none")
)
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} fit.}

\item{level}{Character vector of covariance levels, or \code{"all"} for every
level present in the fit. Canonical levels are \code{"unit"}, \code{"unit_obs"},
\code{"cluster"}, \code{"phy"}, and \code{"spatial"}; legacy aliases \code{"B"}, \code{"W"},
and \code{"spde"} are accepted.}

\item{part}{One of \code{"total"} (default), \code{"shared"}, or \code{"unique"}, passed
to \code{\link[=extract_Sigma]{extract_Sigma()}}.}

\item{measure}{One of \code{"covariance"} (default) or \code{"correlation"}.
Correlation tables are available only for \code{part = "total"} because the
report-ready correlation target is based on the full
\verb{Lambda Lambda^T + Psi} covariance.}

\item{entries}{Which symmetric matrix entries to return. \code{"unique"}
(default) returns the diagonal plus upper triangle, one row per unique
estimand. \code{"all"} returns every cell, useful for heatmaps. \code{"upper"},
\code{"lower"}, \code{"offdiag"}, and \code{"diag"} return the corresponding subsets.}

\item{link_residual}{Passed to \code{\link[=extract_Sigma]{extract_Sigma()}}. \code{"auto"} (default) adds
family/link implicit residual variances to non-Gaussian trait diagonals;
\code{"none"} returns only the fitted latent + unique covariance.}
}
\value{
A data frame with one row per requested entry and stable columns:
\code{estimand}, \code{trait_i}, \code{trait_j}, integer indices \code{i} and \code{j}, \code{level},
\code{component}, \code{matrix}, \code{estimate}, \code{lower}, \code{upper}, \code{interval_method},
\code{interval_status}, \code{scale}, \code{validation_row}, \code{diagonal}, and
\code{triangle}. Interval columns are \code{NA} with \code{interval_method = "none"}
because this helper is point-estimate only.
}
\description{
\code{extract_Sigma()} returns matrices because that is the most convenient
interactive representation. \code{extract_Sigma_table()} returns the same
covariance target as one row per matrix entry so articles, tables, and
plot helpers can work without hand-indexing matrices.
}
\details{
Scope boundary: IN, the helper is a report-ready point-estimate table for
the same covariance and correlation matrices returned by \code{\link[=extract_Sigma]{extract_Sigma()}}
(EXT-18; backed by EXT-01 and MIX-03 for the underlying extractor).
PARTIAL, interval columns are present but not computed here. PLANNED,
interval-aware Sigma and correlation table joins remain future plotting
infrastructure; use \code{\link[=extract_correlations]{extract_correlations()}} or \code{\link[=bootstrap_Sigma]{bootstrap_Sigma()}} when
intervals are needed today.

The table is a point-estimate view over \code{\link[=extract_Sigma]{extract_Sigma()}}. It does not
compute confidence intervals. Use \code{\link[=extract_correlations]{extract_correlations()}} when you need
pairwise correlation intervals.
}
\examples{
\dontrun{
fit <- gllvmTMB(
  value ~ 0 + trait +
          latent(0 + trait | unit, d = 2) + unique(0 + trait | unit),
  data  = df,
  trait = "trait",
  unit  = "unit"
)
extract_Sigma_table(fit, level = "unit")
extract_Sigma_table(fit, level = "unit", measure = "correlation")
extract_Sigma_table(fit, level = c("unit", "unit_obs"), entries = "all")
}
}
\seealso{
\code{\link[=extract_Sigma]{extract_Sigma()}} for the underlying matrix extractor;
\code{\link[=extract_correlations]{extract_correlations()}} for pairwise correlation intervals;
\code{\link[=bootstrap_Sigma]{bootstrap_Sigma()}} for bootstrap uncertainty.
}
