% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-sigma.R
\name{extract_coevolution_modules}
\alias{extract_coevolution_modules}
\title{Extract cross-lineage coevolutionary modules}
\usage{
extract_coevolution_modules(
  fit,
  level,
  row_traits,
  col_traits,
  scale = c("shape", "effect"),
  n_modules = NULL,
  tol = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{fit}{A fitted \code{gllvmTMB_multi} object.}

\item{level}{Character scalar naming the fitted covariance tier.}

\item{row_traits, col_traits}{Character vectors naming the row-lineage and
column-lineage traits.}

\item{scale}{Character scalar. \code{"shape"} (default) uses
\verb{Gamma_shape = Lambda_row Lambda_col^T}; \code{"effect"} uses the fixed-rho
\code{Gamma_effect} available for kernels built by \code{\link[=make_cross_kernel]{make_cross_kernel()}}.}

\item{n_modules}{Optional positive integer limiting the number of returned
singular axes. By default all axes are returned.}

\item{tol}{Numerical tolerance for the generalized inverse square roots of
the within-lineage shared covariance blocks.}
}
\value{
A list with \code{R}, \code{modules}, \code{row_axes}, and \code{col_axes}. \code{R} is the
standardized cross-lineage correlation-like block. \code{modules} has one row
per singular axis with its singular value and squared-value share.
\code{row_axes} and \code{col_axes} contain trait loadings for each coupled axis.
}
\description{
\code{extract_coevolution_modules()} standardizes a component-specific
cross-lineage covariance block and decomposes it into coupled trait axes.
For a named coevolution kernel tier, the helper computes
\verb{R = Sigma_row^\{-1/2\} Gamma Sigma_col^\{-1/2\}} from the shared covariance
returned by \code{\link[=extract_Sigma]{extract_Sigma()}}, then applies a singular-value decomposition.

IN (\code{COE-04}): this is a point-estimate derived-output helper for fitted
dense-kernel coevolution models whose component-specific \code{Gamma} blocks are
already extractable with \code{\link[=extract_Gamma]{extract_Gamma()}}. PARTIAL: it does not report
uncertainty, choose the biological rank, estimate \code{rho}, or calibrate null
thresholds. Use simulation, bootstrap, and kernel-separability checks before
treating the returned axes as scientific evidence.
}
\examples{
\dontrun{
mods <- extract_coevolution_modules(
  fit,
  level = "phy",
  row_traits = c("host_size", "host_defence"),
  col_traits = c("partner_size", "partner_attack")
)
mods$modules
mods$row_axes
mods$col_axes
}

}
