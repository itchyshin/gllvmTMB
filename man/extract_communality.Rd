% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{extract_communality}
\alias{extract_communality}
\title{Communality of each trait (manuscript Eq. 32)}
\usage{
extract_communality(
  fit,
  level = c("unit", "unit_obs", "B", "W"),
  link_residual = c("auto", "none"),
  ci = FALSE,
  conf_level = 0.95,
  method = c("profile", "wald", "bootstrap"),
  nsim = 500L,
  seed = NULL
)
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} object.}

\item{level}{\code{"unit"} (between-unit) or \code{"unit_obs"} (within-unit).
Legacy aliases \code{"B"} and \code{"W"} are accepted with a deprecation warning.}

\item{link_residual}{For binomial fits: \code{"auto"} (default) adds the
link-specific implicit residual to the denominator; \code{"none"} returns
communalities on the latent+unique-implied scale only.}

\item{ci}{Logical. When \code{TRUE}, returns a tidy data frame with
confidence-interval columns; when \code{FALSE} (the default), returns a
plain named numeric vector for backward compatibility.}

\item{conf_level}{Confidence level when \code{ci = TRUE}. Default 0.95.}

\item{method}{One of \code{"profile"} (default), \code{"wald"}, \code{"bootstrap"}.
Only used when \code{ci = TRUE}. Prefer profile or Wald-style intervals where
available; bootstrap is a slower option for deliberate sampling checks.}

\item{nsim}{Number of bootstrap replicates when \code{method = "bootstrap"}.
Default 500.}

\item{seed}{Optional RNG seed for the bootstrap.}
}
\value{
When \code{ci = FALSE}: a numeric vector indexed by trait.
When \code{ci = TRUE}: a data frame with columns \code{trait}, \code{tier}, \code{c2},
\code{lower}, \code{upper}, \code{method}.
}
\description{
\deqn{c_t^2 \;=\; \frac{(\boldsymbol\Lambda \boldsymbol\Lambda^{\!\top})_{tt}}{(\boldsymbol\Lambda \boldsymbol\Lambda^{\!\top})_{tt} + S_{tt}}.}
}
\details{
The proportion of trait \eqn{t}'s variance that is \emph{shared} with the
other traits via the latent factors. Bounded between 0 and 1. Calls
\code{\link[=extract_Sigma]{extract_Sigma()}} internally for the chosen level, so the diagonal
uses the full \eqn{\boldsymbol\Sigma = \boldsymbol\Lambda \boldsymbol\Lambda^{\!\top} + \boldsymbol\Psi}
decomposition when both \code{latent()} and \code{unique()} are in the formula.
\subsection{Caveat: communality with latent-only fits}{

If the fit has \code{latent()} but \strong{no} \code{unique()} at the requested level (for
Gaussian / lognormal / Gamma responses), then \eqn{\boldsymbol\Psi = \mathbf 0}
and \code{c_t^2 = 1} for every trait — this is mathematically correct given
the model spec but tells you nothing about trait integration. The
\code{\link[=extract_Sigma]{extract_Sigma()}} advisory message will fire to flag this. To get
meaningful communalities, refit with \verb{+ unique(0 + trait | <group>)}.

For binomial fits the link-specific implicit residual (\eqn{\pi^2/3}
for logit, 1 for probit, \eqn{\pi^2/6} for cloglog) is added to the
denominator by default; pass \code{link_residual = "none"} to suppress.
}
}
\examples{
\dontrun{
  sim <- simulate_site_trait(
    n_sites = 20, n_species = 6, n_traits = 4,
    mean_species_per_site = 4, seed = 1
  )
  fit <- gllvmTMB(
    value ~ 0 + trait +
            latent(0 + trait | site, d = 2) +
            unique(0 + trait | site),
    data = sim$data
  )
  ## Per-trait between-unit communality.
  extract_communality(fit, level = "unit")
  ## With profile-likelihood CIs.
  extract_communality(fit, level = "unit", ci = TRUE)
}
}
\seealso{
\code{\link[=extract_Sigma]{extract_Sigma()}}; \code{\link[=extract_ICC_site]{extract_ICC_site()}};
\code{\link[=extract_correlations]{extract_correlations()}}; \code{\link[=extract_repeatability]{extract_repeatability()}};
\code{\link[=confint.gllvmTMB_multi]{confint.gllvmTMB_multi()}}.
}
