% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output-methods.R
\name{extract_loadings}
\alias{extract_loadings}
\title{Extract the trait loading matrix}
\usage{
extract_loadings(fit, level = "unit", rotate = c("none", "varimax", "promax"))
}
\arguments{
\item{fit}{A fitted multivariate model returned by \code{\link[=gllvmTMB]{gllvmTMB()}}. Admitted
\code{engine = "julia"} bridge fits expose raw unit-tier loadings and scores;
rotated ordinations remain gated for Julia bridge fits.}

\item{level}{\code{"unit"} (between-unit) or \code{"unit_obs"} (within-unit).
Deprecated aliases \code{"B"} and \code{"W"} are still accepted with a warning.}

\item{rotate}{Optional \code{"varimax"} or \code{"promax"} rotation after fitting.
Default \code{"none"} returns the engine's native lower-triangular Lambda.
For Julia bridge fits only \code{"none"} is currently routed.}
}
\value{
An \verb{n_traits x d} numeric matrix.
}
\description{
Returns the trait loading matrix from a fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}. This is
the canonical snake_case accessor in the \verb{extract_*()} family; \code{\link[=getLoadings]{getLoadings()}}
is an accepted compatibility spelling for readers coming from \code{gllvm}. Both
return the same matrix.
}
\examples{
\dontrun{
extract_loadings(fit, level = "unit", rotate = "varimax")
}
}
\seealso{
\code{\link[=extract_ordination]{extract_ordination()}} for scores and loadings together;
\code{\link[=getLoadings]{getLoadings()}} for the \code{gllvm}-style spelling.
}
