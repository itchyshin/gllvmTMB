% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{extract_ordination}
\alias{extract_ordination}
\title{Extract ordination scores and loadings from a fitted multivariate model}
\usage{
extract_ordination(
  fit,
  level = "unit",
  component = c("total", "innovation", "mean")
)
}
\arguments{
\item{fit}{A fitted multivariate model returned by \code{\link[=gllvmTMB]{gllvmTMB()}}. Admitted
\code{engine = "julia"} bridge fits expose raw unit-tier loadings and scores.
Gaussian bridge fits with \code{latent(..., lv = ~ x)} also expose retained
\code{"mean"} and \code{"innovation"} score components. Within-unit,
structured-tier, and rotated ordinations remain gated for Julia bridge
extractors.}

\item{level}{\code{"unit"} (between-unit) or \code{"unit_obs"} (within-unit).
Deprecated aliases \code{"B"} and \code{"W"} are still accepted with a warning.}

\item{component}{Score component to return. \code{"total"} returns the latent
score entering the linear predictor. \code{"innovation"} returns the zero-mean
latent innovation. \code{"mean"} returns the predictor-informed score mean and
is non-zero only for Design 73 \code{latent(..., lv = ~ x)} fits.}
}
\value{
A list with \code{scores} (units or within-unit observations in rows,
latent axes in columns) and \code{loadings} (traits in rows, axes in columns).
}
\description{
Extract ordination scores and loadings from a fitted multivariate model
}
\examples{
\dontrun{
  sim <- simulate_site_trait(
    n_sites = 20, n_species = 6, n_traits = 4,
    mean_species_per_site = 4, seed = 1
  )
  fit <- gllvmTMB(
    value ~ 0 + trait +
            latent(0 + trait | site, d = 2),
    data  = sim$data,
    trait = "trait",
    unit  = "site"
  )
  ord <- extract_ordination(fit, level = "unit")
  head(ord$scores)
  ord$loadings
}

}
