% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-omega.R
\name{extract_phylo_signal}
\alias{extract_phylo_signal}
\title{Phylogenetic-signal proportions per trait}
\usage{
extract_phylo_signal(
  fit,
  ci = FALSE,
  conf_level = 0.95,
  method = c("profile", "wald", "bootstrap"),
  nsim = 500L,
  seed = NULL
)
}
\arguments{
\item{fit}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}} with a \code{phylo_latent()} term.}

\item{ci}{Logical. When \code{TRUE}, adds confidence-interval columns to
the output for the H^2 column. Default \code{FALSE} for backward
compatibility.}

\item{conf_level}{Confidence level when \code{ci = TRUE}. Default 0.95.}

\item{method}{One of \code{"profile"} (default), \code{"wald"}, \code{"bootstrap"}.
Only used when \code{ci = TRUE}. For 2-component decompositions
(a phylo diagonal vs species-level diagonal component only) profile uses a linear
contrast; for 3-component decompositions (PGLLVM with
phylo_latent plus a species-level latent decomposition with Psi) the full
profile path is not yet implemented and falls back to numerical
delta-method Wald bounds labelled \code{"wald(numeric)"}.}

\item{nsim}{Number of bootstrap replicates when
\code{method = "bootstrap"}. Default 500.}

\item{seed}{Optional RNG seed for the bootstrap.}
}
\value{
A data frame with columns \code{trait}, \code{H2}, \code{C2_non}, \code{Psi},
\code{V_eta} (the denominator), one row per trait. The three proportions
sum to 1.0 by construction. When \code{ci = TRUE}, three additional
columns are added: \code{H2_lower}, \code{H2_upper}, \code{H2_method}.
}
\description{
For a phylogenetic species-level GLLVM, decomposes each trait's
between-species latent variance into three additive components that
sum to one:
\deqn{H_t^2 \;=\; \frac{[\boldsymbol\Sigma_\text{phy}]_{tt}}{V_{\eta,t}}, \qquad C^2_{\text{non},t} \;=\; \frac{[\boldsymbol\Sigma_\text{non,shared}]_{tt}}{V_{\eta,t}}, \qquad \psi_t \;=\; \frac{[\boldsymbol\Psi_\text{non}]_{tt}}{V_{\eta,t}},}
where \eqn{V_{\eta,t} = [\boldsymbol\Sigma_\text{phy}]_{tt} + [\boldsymbol\Sigma_\text{non,shared}]_{tt} + [\boldsymbol\Psi_\text{non}]_{tt}}
is the total between-species latent variance for trait \eqn{t}
.
}
\details{
Interpretation:
\describe{
\item{\eqn{H_t^2}}{phylogenetic signal — proportion of between-
species latent variance attributable to phylogenetically structured
variation ("evolutionary conservatism"). When the model uses the folded
\code{phylo_latent(..., unique = TRUE)} decomposition, \eqn{\boldsymbol\Sigma_\text{phy}}
is the sum \eqn{\boldsymbol\Lambda_\text{phy} \boldsymbol\Lambda_\text{phy}^{\!\top} + \boldsymbol\Psi_\text{phy}}
and \eqn{H_t^2} reflects the \emph{total} phylogenetic variance.}
\item{\eqn{C^2_{\text{non},t}}}{non-phylogenetic communality —
proportion of variance attributable to shared non-phylogenetic
axes ("coordinated tip-level lability" across traits).}
\item{\eqn{\psi_t}}{uniqueness — proportion of variance not captured
by any shared axis ("relative modularity").}
}

Requires \code{phylo_latent()} (optionally with \code{unique = TRUE} for a
phylogenetic Psi) plus a species-level \code{latent()} term, which carries its
diagonal Psi companion by default. If the species-tier Psi is absent (a
\code{latent(..., unique = FALSE)} subset), \eqn{\psi_t = 0} for all traits and a
\code{cli::cli_inform()} advisory fires.
}
\section{Interval calibration}{

The point estimates are the supported claim. When \code{ci = TRUE}, the interval
methods are provided for exploration: their empirical coverage has \strong{not}
been calibrated for this estimand outside the Gaussian cases that cleared the
coverage gate, so the intervals must not be reported as coverage-calibrated.
See \code{NEWS.md} for the current coverage status.
}

\section{Which variances enter the denominator}{

\code{V_eta} is the \strong{species-level} latent variance: only components whose
grouping is the \code{cluster} column contribute. Concretely
\eqn{V_\eta = \sigma^2_{phy} + \sigma^2_{non}}, where \eqn{\sigma^2_{non}}
collects the \emph{species-grouped} non-phylogenetic variance.
\itemize{
\item When \code{unit == cluster} (the usual \code{unit = "species"} setup) the ordinary
\code{latent()} term and its \code{Psi} companion are species-level, so both enter.
\item When \code{unit != cluster} (a crossed \verb{site x species} design) \code{Lambda_B} and
its \code{Psi} are \strong{site}-level and do \strong{not} enter; the species-level
non-phylogenetic variance is the cluster-tier \code{q_it} term
(\verb{indep(0 + trait | <cluster>)}, reported as \code{sd_q^2}).
}

The denominator is defined at the species level. \strong{Other defensible
definitions exist} — for instance a \emph{total}-variance denominator
that also absorbs site-level variation, which would give a smaller \code{H2} for
the same fit. Before 2026-07-08 this function used the unit tier
unconditionally, which silently reported \code{H2 = 1} for every trait in a
crossed design because the \code{q_it} variance was never read. Compare
\code{\link[=extract_proportions]{extract_proportions()}} if you want every component reported separately
rather than folded into a species-level ratio.
}

\examples{
\dontrun{
fit <- gllvmTMB(
  value ~ 0 + trait + phylo_latent(species, d = 2) +
                      latent(0 + trait | species, d = 2) +
                      indep(0 + trait | species),
  data       = df,
  trait      = "trait",
  unit       = "species",
  cluster    = "species",
  phylo_tree = tree
)
extract_phylo_signal(fit)
}
}
\seealso{
\code{\link[=extract_Sigma]{extract_Sigma()}}; \code{\link[=extract_Omega]{extract_Omega()}}; \code{\link[=extract_proportions]{extract_proportions()}};
\code{\link[=extract_repeatability]{extract_repeatability()}}; \code{\link[=extract_communality]{extract_communality()}};
\code{\link[=extract_correlations]{extract_correlations()}}.
}
