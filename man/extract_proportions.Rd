% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-omega.R
\name{extract_proportions}
\alias{extract_proportions}
\title{Per-trait proportion-of-variance decomposition across all model components}
\usage{
extract_proportions(
  fit,
  link_residual = c("auto", "none"),
  format = c("long", "wide")
)
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} fit.}

\item{link_residual}{For non-Gaussian fits: \code{"auto"} (default) adds the
link's implicit residual as its own per-trait component (e.g.
\eqn{\pi^2/3} for binomial logit, \eqn{\log(1 + 1/\hat\mu_t)} for
Poisson log; see "Family-aware link residuals" in \code{\link[=extract_Sigma]{extract_Sigma()}}
for the full per-family table). Mixed-family fits get the residual
implied by each trait's family/link. \code{"none"} omits the link-implicit
component.}

\item{format}{\code{"long"} (default) returns a tibble-like data frame
with one row per (trait, component); \code{"wide"} returns one row per
trait with one column per component.}
}
\value{
Long format: data frame with columns \code{trait}, \code{component}
(e.g. \code{"shared_unit"}, \code{"unique_unit"}, \code{"shared_phy"}, \code{"link_residual"}),
\code{variance} (the absolute variance), \code{proportion} (the share of
the total). Wide format: data frame with one column per component
plus a \code{total_variance} column; the per-trait proportions sum to 1.

\strong{OLRE interpretation:} for fits with a genuine observation-level
\code{unique()} term (see \code{\link[=extract_residual_split]{extract_residual_split()}}), the \code{unique_W}
component in this output corresponds to \eqn{\sigma^2_e} (the estimated
OLRE variance) and the \code{link_residual} component corresponds to
\eqn{\sigma^2_d} (the distribution-specific latent residual).
}
\description{
For each trait, returns the proportion of total latent variance
attributable to each component present in the model:
shared (rr) and unique (diag) at each tier (B, W, phy), plus
optionally the binomial link's implicit residual.
}
\details{
This is the most general proportion-decomposition function in the
package. \code{\link[=extract_phylo_signal]{extract_phylo_signal()}} is the focused PGLLVM
\eqn{H^2 / C^2_\text{non} / \psi^2} convenience wrapper for the
species-level case; \code{\link[=extract_communality]{extract_communality()}} gives the rr-only
"shared" proportion at one tier; \code{\link[=extract_ICC_site]{extract_ICC_site()}} gives the
between-vs-within proportion. \code{extract_proportions()} returns all of
these in one tidy frame.
}
\examples{
\dontrun{
extract_proportions(fit)                           # long format
extract_proportions(fit, format = "wide")          # one row per trait
}
}
\seealso{
\code{\link[=extract_phylo_signal]{extract_phylo_signal()}} — the PGLLVM-specific shortcut;
\code{\link[=extract_communality]{extract_communality()}}; \code{\link[=extract_ICC_site]{extract_ICC_site()}};
\code{\link[=extract_residual_split]{extract_residual_split()}} — explicit \eqn{\sigma^2_d / \sigma^2_e}
decomposition for OLRE fits.
}
