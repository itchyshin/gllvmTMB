% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-omega.R
\name{extract_residual_split}
\alias{extract_residual_split}
\title{Separate OLRE residual variance from the distribution-specific latent
residual}
\usage{
extract_residual_split(fit)
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} fit.}
}
\value{
A data frame with one row per trait and columns:
\describe{
\item{\code{trait}}{Factor of trait names.}
\item{\code{sigma2_d}}{Theoretical / parameter-dependent distribution-specific
latent residual (computed by the internal \code{link_residual_per_trait()}
helper; see the per-family table above; zero for \code{gaussian} and
\code{lognormal}).}
\item{\code{sigma2_e}}{Estimated OLRE variance per trait — the per-trait
diagonal of \eqn{\boldsymbol{\Psi}_W} when the fit has a genuine
observation-level \code{unique()} term, else 0.}
\item{\code{sigma2_total}}{\code{sigma2_d + sigma2_e}.}
}
}
\description{
For an additive overdispersion (OLRE) model
\deqn{\eta_{it} = \mathbf{X}\boldsymbol\beta + \ldots + e_{it},
       \quad e_{it} \sim N(0, \sigma^2_e),}
the total latent-scale residual variance for trait \eqn{t} is
\deqn{\sigma^2_{d,t} + \sigma^2_{e,t},}
where \eqn{\sigma^2_d} is the \strong{distribution-specific (theoretical)}
component that depends only on the family/link, and \eqn{\sigma^2_e}
is the \strong{estimated OLRE variance} — the per-trait diagonal of the
within-unit unique covariance \eqn{\boldsymbol{\Psi}_W}.
}
\details{
The function detects whether the fit includes a genuine observation-level
random effect: a \verb{unique(0 + trait | <obs-level>)} term where every
(trait, obs) cell is unique (i.e. one row per observation level per
trait). When this cell-uniqueness condition holds, \code{sigma2_e} is
populated; otherwise it is zero.
\subsection{Terminology note}{

Nakagawa & Schielzeth (2010) use \eqn{\sigma^2_d} for both components.
Nakagawa, Johnson & Schielzeth (2017) §7 refine the terminology:
\eqn{\sigma^2_d} (distribution-specific) applies only to binomial-type
families whose link function introduces a fixed latent-scale variance;
\eqn{\sigma^2_\varepsilon} (observation-level) applies to
overdispersed Poisson / NB / Gamma and is estimated from the data.
\strong{gllvmTMB} keeps the colloquial \code{sigma2_d} column name for
compatibility but documents the distinction here (NJS 2017 §7).
}

\subsection{Per-family \eqn{\sigma^2_d} table}{

\tabular{lll}{
Family \tab Link \tab \eqn{\sigma^2_d} \cr
\code{gaussian} \tab identity \tab 0 \cr
\code{binomial} \tab logit \tab \eqn{\pi^2/3 \approx 3.290} \cr
\code{binomial} \tab probit \tab \eqn{1} \cr
\code{binomial} \tab cloglog \tab \eqn{\pi^2/6 \approx 1.645} \cr
\code{poisson} \tab log \tab \eqn{\log(1 + 1/\hat{\mu}_t)} (lognormal-Poisson approx.) \cr
\code{lognormal} \tab log \tab 0 \cr
\code{Gamma} \tab log \tab \eqn{\psi_1(\hat\nu)}, \eqn{\hat\nu = 1/\hat\sigma_\varepsilon^2} \cr
\code{Beta} \tab logit \tab \eqn{\psi_1(\hat\mu_t \hat\phi) + \psi_1((1 - \hat\mu_t)\hat\phi)} (Smithson & Verkuilen 2006) \cr
\code{betabinomial} \tab logit \tab \eqn{\pi^2/3 + \psi_1(\hat\mu_t \hat\phi) + \psi_1((1 - \hat\mu_t)\hat\phi)}
}
}
}
\examples{
\dontrun{
## Add a site_species column (one level per row) as the obs-level grouping.
df$site_species <- factor(seq_len(nrow(df)))
fit <- gllvmTMB(
  value ~ 0 + trait + unique(0 + trait | site_species),
  data     = df,
  trait    = "trait",
  unit     = "site",
  unit_obs = "site_species",
  family   = poisson()
)
extract_residual_split(fit)
}
}
\references{
Nakagawa, S. & Schielzeth, H. (2010) Repeatability for Gaussian and
non-Gaussian data: a practical guide for biologists. \emph{Biological
Reviews} \strong{85}(4): 935-956. \doi{10.1111/j.1469-185X.2010.00141.x}

Nakagawa, S., Johnson, P. C. D. & Schielzeth, H. (2017) The coefficient
of determination \eqn{R^2} and intra-class correlation coefficient from
generalized linear mixed-effects models revisited and expanded.
\emph{Journal of the Royal Society Interface} \strong{14}(134): 20170213.
\doi{10.1098/rsif.2017.0213}
}
\seealso{
\code{\link[=extract_Omega]{extract_Omega()}} (returns \code{residual_split} as a list component
when \code{link_residual = "auto"}); \code{\link[=extract_Sigma]{extract_Sigma()}}; \code{\link[=extract_proportions]{extract_proportions()}}
(the \code{unique_W} component in its output is \eqn{\sigma^2_e} and the
\code{link_residual} component is \eqn{\sigma^2_d} for OLRE-style fits).
}
\keyword{internal}
