% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotate-loadings.R
\name{extract_rotated_loadings_table}
\alias{extract_rotated_loadings_table}
\title{Extract a tidy rotated loading table}
\usage{
extract_rotated_loadings_table(
  fit,
  level = "unit",
  method = c("varimax", "promax", "none"),
  order_axes = TRUE,
  sign_anchor = c("auto", "none"),
  anchor_traits = NULL,
  loading_scale = c("raw", "standardized")
)
}
\arguments{
\item{fit}{A fitted multivariate model returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{level}{\code{"unit"} (between-unit) or \code{"unit_obs"} (within-unit).
Deprecated aliases \code{"B"} and \code{"W"} are still accepted with a warning.}

\item{method}{One of \code{"varimax"}, \code{"promax"}, or \code{"none"}; passed to
\code{\link[=rotate_loadings]{rotate_loadings()}}.}

\item{order_axes}{Logical. When \code{TRUE}, reorder rotated axes by decreasing
raw shared variance before tabulation. Ignored when \code{method = "none"}.}

\item{sign_anchor}{One of \code{"auto"} or \code{"none"}. \code{"auto"} flips each rotated
axis so its anchor trait has a positive loading. Ignored when
\code{method = "none"}.}

\item{anchor_traits}{Optional character vector of trait names used for
sign anchoring. Supply one trait per axis after ordering. Axes without a
supplied anchor use the trait with the largest absolute loading.}

\item{loading_scale}{One of \code{"raw"} or \code{"standardized"}. \code{"raw"} returns
the rotated loadings on the fitted response scale. \code{"standardized"}
divides each trait loading by the square root of that trait's
model-implied total variance, matching \code{plot(type = "ordination", standardize_loadings = TRUE)}.}
}
\value{
A data frame with columns \code{level}, \code{trait}, \code{axis}, \code{loading},
\code{abs_loading}, \code{axis_variance}, \code{axis_share}, \code{rotation}, \code{order_axes},
\code{sign_anchor}, \code{anchor_trait}, and \code{loading_scale}. \code{axis_variance} and
\code{axis_share} are computed from the rotated raw loading matrix before any
optional standardization; these are the quantities used to order axes.
}
\description{
Returns one row per trait and latent axis from a fitted \code{\link[=gllvmTMB]{gllvmTMB()}} model.
This is the report-ready companion to \code{\link[=rotate_loadings]{rotate_loadings()}} and to
\code{plot(type = "ordination")}: the same rotation, axis-ordering, sign-anchor,
and optional loading-standardization conventions are recorded in columns
instead of left inside a matrix.
}
\details{
Scope: covered for fitted \code{latent()} components at \code{level = "unit"} or
\code{level = "unit_obs"} with raw or standardized point-estimate loadings.
This table does not attach uncertainty intervals to loadings. Bootstrap
or simulation-based uncertainty is not yet implemented and remains
possible future work for a later inference slice.
}
\examples{
\dontrun{
sim <- simulate_site_trait(
  n_sites = 60, n_species = 12, n_traits = 4,
  Lambda_B = matrix(c(1.0, 0.7, -0.3, 0.5,
                      0.3, -0.5, 0.8, 0.2),
                    nrow = 4, ncol = 2),
  psi_B = c(0.3, 0.3, 0.3, 0.3),
  seed = 1
)
fit <- gllvmTMB(value ~ 0 + trait +
                        latent(0 + trait | site, d = 2),
                data  = sim$data,
                trait = "trait",
                unit  = "site")
extract_rotated_loadings_table(
  fit,
  level = "unit",
  method = "varimax",
  loading_scale = "standardized"
)
}
}
\seealso{
\code{\link[=rotate_loadings]{rotate_loadings()}}, \code{\link[=extract_ordination]{extract_ordination()}}, \code{\link[=extract_Sigma]{extract_Sigma()}},
\code{\link[=plot.gllvmTMB_multi]{plot.gllvmTMB_multi()}}.
}
