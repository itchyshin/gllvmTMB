% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading-ci.R
\name{flag_unreliable_loadings}
\alias{flag_unreliable_loadings}
\title{Flag loadings whose CI overlaps a "biologically negligible" region}
\usage{
flag_unreliable_loadings(
  fit,
  null_region = c(-0.1, 0.1),
  level = c("unit", "unit_obs"),
  method = "wald",
  conf_level = 0.95
)
}
\arguments{
\item{fit}{A multivariate \code{gllvmTMB()} fit, or a data frame already
produced by \code{\link[=loading_ci]{loading_ci()}}.}

\item{null_region}{Length-2 numeric, sorted ascending. The
"negligible" band. Defaults to \code{c(-0.1, 0.1)}.}

\item{level, method, conf_level}{Forwarded to \code{\link[=loading_ci]{loading_ci()}} when
\code{fit} is a fit object. Ignored when \code{fit} is already a
\code{loading_ci()} data frame.}
}
\value{
The \code{loading_ci()} data frame with one extra logical column
\code{unreliable}: \code{TRUE} if the CI overlaps \code{null_region}, \code{FALSE} if
it lies entirely outside, \code{NA} for pinned entries.
}
\description{
A simple decision aid in the spirit of Zientek & Thompson (2007,
doi:10.3758/BF03193163): given per-entry CIs on Lambda, flag the
entries whose \code{conf_level} confidence interval \strong{does not exclude}
a user-supplied "null region" — a band around zero considered
biologically negligible. Loadings flagged as \code{unreliable = TRUE}
are the ones for which the data do not provide evidence that the
species responds non-trivially to the axis.
}
\details{
Pinned entries (set explicitly by \code{lambda_constraint}) are reported
with \code{unreliable = NA} because no inference is being made about them.
}
\examples{
\dontrun{
## Grounded in tests/testthat/test-loading-ci.R. flag_unreliable_loadings()
## accepts either a fit or a loading_ci() data frame directly.
fit <- gllvmTMB(
  value ~ 0 + trait + latent(0 + trait | site, d = 2L),
  data = df, family = stats::binomial(link = "probit")
)
## From a fit (computes per-entry Lambda CIs internally):
flag_unreliable_loadings(fit, null_region = c(-0.1, 0.1))
## Or pass a precomputed loading_ci() data frame:
ci <- loading_ci(fit, level = "unit")
flag_unreliable_loadings(ci, null_region = c(-0.1, 0.1))
}

}
\seealso{
\code{\link[=loading_ci]{loading_ci()}}.
}
