% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal-forecast.R
\name{forecast_temporal}
\alias{forecast_temporal}
\title{Forecast a native temporal covariance model}
\usage{
forecast_temporal(object, newdata, se.fit = FALSE)
}
\arguments{
\item{object}{A fitted native temporal \code{\link[=gllvmTMB]{gllvmTMB()}} model.}

\item{newdata}{A complete trait panel for one or more future
series--occasion pairs. Each series must occur in the fitted data and each
requested time must be strictly after that series' final fitted time.}

\item{se.fit}{Return the fitted-parameter conditional predictive standard
deviation.}
}
\value{
\code{newdata}, in its input row order, with \code{est} on the Gaussian
identity-link scale and, when requested, \code{se.fit}.
}
\description{
Computes a Gaussian response forecast at future occasions of already
observed series. The forecast conditions on the complete observed response
vector at fitted parameter values. \code{se.fit} is the corresponding
conditional predictive standard deviation; it excludes uncertainty in the
fitted parameters and is not a calibrated prediction interval.
}
\details{
This helper supports an unreplicated \code{temporal_indep()} Gaussian identity-link
model, the separately qualified replicated Gaussian AR1
\code{temporal_dep() + spatial_indep()} route with one fixed mesh, and the
separately qualified replicated Gaussian AR1 \code{temporal_dep() + phylo_indep()}
route with one fixed phylogeny. Other temporal-source combinations remain
refused pending separate conditioning contracts. It forecasts future
observations rather than latent state means.
}
\examples{
d <- expand.grid(series = c("a", "b"), occasion = 1:3,
  trait = c("t1", "t2", "t3"), KEEP.OUT.ATTRS = FALSE)
d$value <- with(d, occasion + as.numeric(factor(trait)) / 10)
fit <- gllvmTMB(value ~ 0 + trait +
  temporal_indep(0 + trait | series, time = occasion),
  data = d, unit = "series", family = gaussian(), silent = TRUE,
  control = gllvmTMBcontrol(se = FALSE))
future <- expand.grid(series = c("a", "b"), occasion = 4,
  trait = c("t1", "t2", "t3"), KEEP.OUT.ATTRS = FALSE)
forecast_temporal(fit, future, se.fit = TRUE)
}
