% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/re-uncertainty.R
\name{getREsd}
\alias{getREsd}
\title{Standard errors of random-effect blocks without a scores accessor}
\usage{
getREsd(
  fit,
  block = c("diag_unit", "diag_unit_obs", "diag_species", "phylo", "re_int", "equalto")
)
}
\arguments{
\item{fit}{A fitted multivariate model returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{block}{Which random-effect block to read. One of:
\itemize{
\item \code{"diag_unit"} -- the unit-level (site) diagonal random effect
from an \verb{indep(0 + trait | <unit>)} / \code{diag()} term (\code{s_B} in the
TMB template). Returns an \verb{n_traits x n_sites} matrix, matching the
orientation \code{\link[=predict.gllvmTMB_multi]{predict.gllvmTMB_multi()}} already uses when it reads
this block's point estimate.
\item \code{"diag_unit_obs"} -- the within-unit diagonal random effect
from an \verb{indep(0 + trait | <unit_obs>)} term (\code{s_W}). Returns an
\verb{n_traits x n_site_species} matrix in the same orientation.
\item \code{"diag_species"} -- the species-level diagonal random effect
from an \verb{indep(0 + trait | <species>)} term (\code{q_sp}). Returns an
\verb{n_traits x n_species} matrix.
\item \code{"phylo"} -- the \code{propto()} per-species phylogenetic random
effect (\code{p_phy}). Returns an \verb{n_species x n_traits} matrix, matching
the orientation \code{\link[=predict.gllvmTMB_multi]{predict.gllvmTMB_multi()}} already uses when it
reads this block's point estimate.
\item \code{"re_int"} -- the \code{(1 | group)} random intercept(s) (\code{u_re_int}).
Returns a named list, one numeric vector per bar term (named by
that term's grouping-column name), because multiple \code{(1 | group)}
terms are packed into a single flat parameter vector.
\item \code{"equalto"} -- the \code{equalto()} known-covariance random effect
(\code{e_eq}). Returns a plain numeric vector, one entry per observation
row (the block's native shape; there is no unit/trait grid to
reshape into).
}}
}
\value{
As described per \code{block} above. Standard errors are always
\code{NA} (with a warning) when the fit's Hessian is not positive-definite.
}
\description{
\code{\link[=getLV]{getLV()}} exposes standard errors for the ordinary latent-variable score
blocks (\code{z_B} / \code{z_W}). Several other random-effect families fitted by
\code{\link[=gllvmTMB]{gllvmTMB()}} have the identical machinery available -- a marginal
variance for every random-effect coordinate, already computed by the
fit's TMB \code{sdreport()} -- but no accessor exposed it. \code{getREsd()} closes
that gap for the blocks whose point-estimate reshape convention is
already established elsewhere in the package (see \code{block} below).
}
\section{What this is, and is not}{

Every number returned here is a \strong{Wald / \code{sdreport()} marginal standard
error}, propagated through the delta method the same way \code{\link[=getLV]{getLV()}}'s
\code{se = TRUE} already is -- not a resampled, profiled, or simulation-based
quantity. Coverage of intervals built from it has not been measured for
these blocks; do not describe or advertise it as exact, and do not
construct a CI from it and call the CI "certified" without separately
measuring its coverage. \code{diag.cov.random} reflects fixed-effect-uncertainty
-propagated marginal variance for each coordinate individually; it does
NOT give cross-block or random-vs-fixed covariance (that needs
\code{sdreport(getJointPrecision = TRUE)}, which this fit's single production
\code{sdreport()} call does not request).

Because of that fixed-effect-uncertainty propagation, a near-saturated or
otherwise degenerate design (e.g. an \code{indep()} diagonal term at a grouping
where every group has one observation, with its residual variance driven
to the boundary) can return SEs one or more orders of magnitude larger
than the block's own conditional (GMRF) variance would suggest -- in that
regime the random effect deterministically tracks the fixed-effect
residual, so its delta-method SE inherits the fixed effect's own
uncertainty almost entirely. This is correct Wald behaviour, not a bug,
but it means the magnitude is not always "the block's own noise" in an
intuitive sense; treat a surprisingly large SE as a cue to check whether
the corresponding variance component sits at or near a boundary.

Blocks not covered here (\code{s_B_slope}, \code{s_W_slope}, \code{r_c2} / diagonal
\code{cluster2}, \code{g_phy}, \code{g_phy_diag}, the SPDE spatial fields
\verb{omega_spde*}, the dense-kernel blocks \verb{g_kernel*}, every augmented
random-slope block, and the missing-predictor latent blocks \code{x_mis} /
\code{u_mi_group} / \code{g_x}) are, in principle, reachable the same way -- the
underlying \code{sdreport()} call already covers their marginal variances too
-- but either their point-estimate reshape convention is not yet
established anywhere else in the package (so getting the row/column
order right cannot be cross-checked against existing code), or their
dimensions are not carried as top-level fields on the fit object. Adding
them safely needs that groundwork first; this function raises a clear
error naming the block rather than silently mis-reshaping.
}

\examples{
\dontrun{
getREsd(fit, block = "re_int")
getREsd(fit, block = "diag_unit")
}
}
\seealso{
\code{\link[=getLV]{getLV()}} for the ordinary latent-score standard errors this
function generalises.
}
\keyword{internal}
