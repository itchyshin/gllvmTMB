% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/julia-bridge.R
\name{gllvmTMB_julia-methods}
\alias{gllvmTMB_julia-methods}
\alias{logLik.gllvmTMB_julia}
\alias{print.gllvmTMB_julia}
\alias{coef.gllvmTMB_julia}
\alias{predict.gllvmTMB_julia}
\alias{fitted.gllvmTMB_julia}
\alias{residuals.gllvmTMB_julia}
\alias{simulate.gllvmTMB_julia}
\alias{confint.gllvmTMB_julia}
\alias{summary.gllvmTMB_julia}
\alias{print.summary.gllvmTMB_julia}
\title{Methods for Julia bridge fits}
\usage{
\method{logLik}{gllvmTMB_julia}(object, ...)

\method{print}{gllvmTMB_julia}(x, ...)

\method{coef}{gllvmTMB_julia}(object, ...)

\method{predict}{gllvmTMB_julia}(
  object,
  newdata = NULL,
  type = c("link", "response", "prob", "class"),
  ...
)

\method{fitted}{gllvmTMB_julia}(object, type = c("response", "link", "prob", "class"), ...)

\method{residuals}{gllvmTMB_julia}(object, type = c("response", "pearson"), ...)

\method{simulate}{gllvmTMB_julia}(
  object,
  nsim = 1,
  seed = NULL,
  newdata = NULL,
  condition_on_RE = TRUE,
  ...
)

\method{confint}{gllvmTMB_julia}(
  object,
  parm,
  level = 0.95,
  method = c("stored", "wald", "profile", "bootstrap"),
  ci_nboot = 200L,
  ci_seed = 0L,
  ...
)

\method{summary}{gllvmTMB_julia}(object, ...)

\method{print}{summary.gllvmTMB_julia}(x, digits = 3, ...)
}
\arguments{
\item{object, x}{A fit returned by \code{gllvmTMB(..., engine = "julia")} or
\code{\link[=gllvm_julia_fit]{gllvm_julia_fit()}}.}

\item{...}{Unused.}

\item{newdata}{Unsupported for Julia bridge fits; current prediction and
simulation methods return in-sample values from the retained bridge payload.}

\item{type}{Prediction or residual scale. For \code{predict()} and \code{fitted()},
\code{"link"} returns the fitted linear predictor and \code{"response"} applies the
inverse link for supported non-ordinal bridge families. For per-trait
ordinal bridge fits, \code{"response"} and \code{"prob"} return fitted category
probabilities and \code{"class"} returns the modal category. For \code{residuals()},
\code{"response"} returns observed-minus-fitted residuals on the response scale
and \code{"pearson"} divides by the family-specific standard deviation.
Binomial rows use the observed proportion, \code{y / N}, on the response scale.}

\item{nsim}{Number of replicate response vectors to draw for \code{simulate()}.}

\item{seed}{Optional RNG seed for \code{simulate()}.}

\item{condition_on_RE}{Logical for \code{simulate()}. The Julia bridge currently
routes only conditional, in-sample simulation from retained fitted values,
so \code{FALSE} stops with a not-yet-routed message.}

\item{parm}{Optional integer or character vector of CI terms for \code{confint()}.}

\item{level}{Confidence level requested by \code{confint()}. Stored Julia bridge
payloads can only be read at their stored level; post-fit requests recompute
the admitted Julia CI payload at the requested level.}

\item{method}{CI route for \code{confint()}. \code{"stored"} reads an existing payload.
\code{"wald"}, \code{"profile"}, and \code{"bootstrap"} recompute from retained bridge
input for admitted no-X Gaussian, Poisson, Bernoulli binomial, NB2, NB1,
Beta, and Gamma rows, including masked non-Gaussian rows when \code{X = NULL}.
Complete-response fixed-effect-X recomputation is routed for Gaussian,
Poisson, Bernoulli binomial, NB2, Beta, and Gamma rows.
If omitted, \code{confint()} reads a stored payload when present and otherwise
uses \code{"wald"} for current fits that retain their bridge input.}

\item{ci_nboot}{Number of parametric bootstrap replicates when
\code{method = "bootstrap"}.}

\item{ci_seed}{Seed passed to the Julia bootstrap CI route.}

\item{digits}{Number of digits printed by summary methods.}
}
\value{
\code{logLik()} returns a \code{"logLik"} object. \code{coef()} returns a named list
of available point-estimate components. \code{confint()} returns a conventional
confidence-interval matrix for stored or recomputed Julia CI payloads.
\code{predict()} returns an in-sample data frame. Non-ordinal rows use
\code{trait}, \code{unit}, and \code{est} columns; ordinal probability rows use \code{trait},
\code{unit}, \code{category}, and \code{prob}; ordinal class rows use \code{trait}, \code{unit},
and \code{est}. \code{fitted()} returns the in-sample fitted matrix with traits in
rows and units in columns for non-ordinal rows, and an array of
category probabilities or a modal-class matrix for ordinal rows.
\code{residuals()} returns an in-sample residual matrix with the same shape as
\code{fitted()} for non-ordinal rows and keeps masked response cells as
\code{NA}. \code{simulate()} returns an \verb{n_obs x nsim} matrix in the same trait-major
cell order as \code{predict()} and keeps masked response cells as \code{NA}.
\code{summary()} returns a list with header, coefficients, covariance, and
status fields.
}
\description{
Small S3 surface for fits returned by \code{gllvmTMB(..., engine = "julia")} or
\code{\link[=gllvm_julia_fit]{gllvm_julia_fit()}}. These methods expose the flat bridge payload that has
already passed the R admission gates. They are point-estimate summaries:
prediction and ordinary response/Pearson residuals are in-sample
retained-payload reconstructions only. Simulation is conditional on retained
fitted values for admitted one-family rows and complete balanced mixed-family
rows. Unit-tier covariance honors native \code{link_residual} scale semantics,
and raw ordination accessors are routed; richer extractor parity remains a
separate bridge row.
Confidence intervals are
routed for admitted no-X Gaussian, Poisson, Bernoulli binomial, NB2, NB1,
Beta, and Gamma rows; response-mask CIs are routed for the same non-Gaussian
rows when \code{X = NULL}; complete-response fixed-effect-X CIs are routed for
Gaussian, Poisson, Bernoulli binomial, NB2, Beta, and Gamma rows. They may
be requested at fit time through
\code{gllvmTMB(ci_method = ...)}, or retrieved and recomputed through \code{confint()}.
Predictor-informed \code{latent(..., lv = ~ x)} rows admit \code{ci_method = "wald"}
at fit time; those Wald intervals are surfaced as \code{std.error}, \code{lower}, and
\code{upper} through \code{extract_lv_effects()} rather than \code{confint()}.
}
