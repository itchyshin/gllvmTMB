% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/julia-bridge.R
\name{gllvmTMB_julia-methods}
\alias{gllvmTMB_julia-methods}
\alias{coef.gllvmTMB_julia}
\alias{tidy.gllvmTMB_julia}
\alias{nobs.gllvmTMB_julia}
\alias{vcov.gllvmTMB_julia}
\alias{predict.gllvmTMB_julia}
\alias{fitted.gllvmTMB_julia}
\alias{residuals.gllvmTMB_julia}
\alias{summary.gllvmTMB_julia}
\alias{print.summary.gllvmTMB_julia}
\title{Post-fit methods for Julia-engine GLLVM fits}
\usage{
\method{coef}{gllvmTMB_julia}(object, ...)

\method{tidy}{gllvmTMB_julia}(
  x,
  effects = c("fixed", "ran_pars", "cutpoint"),
  conf.int = FALSE,
  conf.level = 0.95,
  ...
)

\method{nobs}{gllvmTMB_julia}(object, ...)

\method{vcov}{gllvmTMB_julia}(object, ...)

\method{predict}{gllvmTMB_julia}(
  object,
  newdata = NULL,
  type = c("link", "response"),
  re_form = ~.,
  ...
)

\method{fitted}{gllvmTMB_julia}(object, type = c("response", "link"), re_form = ~., ...)

\method{residuals}{gllvmTMB_julia}(object, type = c("response"), ...)

\method{summary}{gllvmTMB_julia}(object, ...)

\method{print}{summary.gllvmTMB_julia}(x, digits = 3, ...)
}
\arguments{
\item{object, x}{A \code{gllvmTMB_julia} object.}

\item{...}{Currently unused.}

\item{effects}{For \code{tidy()}, one of \code{"fixed"}, \code{"ran_pars"}, or \code{"cutpoint"};
only \code{"fixed"} is currently wired for Julia-engine fits.}

\item{conf.int}{For \code{tidy()}, whether to add confidence limits. Currently
unsupported for Julia-engine fits; use \code{\link[=confint]{confint()}} where routed.}

\item{conf.level}{Confidence level requested by \code{conf.int}.}

\item{newdata}{Optional new data. Currently unsupported for Julia-engine fits;
only in-sample predictions are returned.}

\item{type}{Prediction scale. \code{"link"} returns the fitted linear predictor and
\code{"response"} returns the inverse-link mean.}

\item{re_form}{Random-effect formula controlling whether conditional latent
scores are included. Use \code{~ 0} or \code{NA} for fixed-effects-only predictions.}

\item{digits}{Decimal digits to print.}
}
\value{
\code{coef()} returns a named list of bridge coefficients. \code{tidy()} returns
a coefficient data frame with \code{term}, \code{estimate}, and \code{component} columns for
the currently routed fixed-effect bridge payload. \code{predict()} and
\code{residuals()} return data frames in the original training-row order when the
object came from \code{gllvmTMB(..., engine = "julia")}. \code{fitted()} returns a
trait x unit matrix. \code{nobs()} returns the number of likelihood-contributing
cells. \code{vcov()} currently errors with an explicit status because covariance
matrices are not routed through the Julia bridge yet. \code{summary()} returns a
list of class \code{summary.gllvmTMB_julia}. \code{print()} methods return the input
invisibly.
}
\description{
Basic inspection methods for objects returned by \code{\link[=gllvm_julia_fit]{gllvm_julia_fit()}} or
\code{\link[=gllvmTMB]{gllvmTMB()}} with \code{engine = "julia"}. These methods expose the flat bridge
payload in ordinary R shapes; they do not perform new Julia computations.
}
