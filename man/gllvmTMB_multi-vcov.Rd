% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov-coef.R
\name{gllvmTMB_multi-vcov}
\alias{gllvmTMB_multi-vcov}
\alias{coef.gllvmTMB_multi}
\alias{vcov.gllvmTMB_multi}
\title{Fixed-effect estimates and covariance for a multi-trait fit}
\usage{
\method{coef}{gllvmTMB_multi}(object, ...)

\method{vcov}{gllvmTMB_multi}(object, ...)
}
\arguments{
\item{object}{A fitted multivariate model returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{...}{Ignored, present for S3 consistency.}
}
\value{
\code{coef()}: a named numeric vector, one entry per fixed-effect term
(\code{object$X_fix_names}).

\code{vcov()}: a square numeric matrix with those same names on both margins.
Rows and columns for coefficients held fixed via \code{Xcoef_fixed} are \code{NA} --
a fixed parameter was not estimated, so it has no sampling covariance.
}
\description{
\code{coef()} returns the fixed-effect point estimates; \code{vcov()} returns their
covariance matrix, taken from the fit's TMB \code{sdreport()}.
}
\section{Point estimates need no standard errors}{

\code{coef()} works on any fit, including one made with
\code{gllvmTMBcontrol(se = FALSE)} -- point estimates are this package's supported
claim and do not depend on \code{sdreport()}. \code{vcov()} does depend on it, and
raises the same typed errors \code{\link[=confint]{confint()}} does when it is missing
(\code{gllvmTMB_confint_no_sdreport}) or non-finite
(\code{gllvmTMB_confint_nonfinite_se}), naming \code{standard_errors()} as the remedy
in the first case. The conditions are shared deliberately: a caller that
handles one should handle the other.
}

\section{What this covariance is}{

The fixed-effect block of the single TMB \code{sdreport()} the fit already
carries -- a Wald covariance, with exactly the caveats any Wald quantity
from this package carries. It is not a resampled or profiled quantity, and
no interval built from it has certified coverage. \code{vcov()} refuses a fit
with non-unit likelihood weights: that fit targets a weighted estimating
objective and has no certified sandwich covariance.
}

\examples{
\dontrun{
coef(fit)
vcov(fit)
}
}
\seealso{
\code{\link[=standard_errors]{standard_errors()}} to compute the \code{sdreport()} after fitting;
\code{\link[=confint]{confint()}} for intervals; \code{\link[=summary]{summary()}} for a coefficient table.
}
