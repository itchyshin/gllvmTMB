% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gllvmTMB-wide.R
\name{gllvmTMB_wide}
\alias{gllvmTMB_wide}
\title{Fit a GLLVM from a wide unit × trait matrix}
\usage{
gllvmTMB_wide(
  Y,
  X = NULL,
  d = 2,
  family = gaussian(),
  phylo_vcv = NULL,
  formula_extra = NULL,
  weights = NULL,
  ...
)
}
\arguments{
\item{Y}{A \verb{n_units × n_traits} numeric matrix of responses
(presence / absence, abundance, continuous measurements, item
scores). Rows must be unique units; columns must be unique
traits.}

\item{X}{Optional \verb{n_units × n_predictors} data frame of
unit-level predictors. Row order must match \code{Y}.}

\item{d}{Integer; the number of latent factors. Default 2.}

\item{family}{A \code{family} object (default \code{gaussian()}); for
presence/absence matrices use \code{binomial()}.}

\item{phylo_vcv}{Optional \verb{n_traits x n_traits} phylogenetic
correlation matrix (rownames must match colnames of \code{Y}). When
supplied, a \code{phylo_latent()} term with \code{d} factors is added.
For the canonical site × species use case the "traits" are
species, so this is the species-level phylogenetic correlation.}

\item{formula_extra}{Optional formula RHS to splice into the fixed
effects, e.g. \code{~ env_temp + env_precip}. Defaults to \code{~ 1}.}

\item{weights}{Optional per-cell observation weights, parallel to \code{Y}.
At wide level the semantic is always lme4 / glmmTMB-style: each
cell's log-likelihood contribution is multiplied by its weight.
Accepted shapes are normalised to the same long-format vector used
by \code{\link[=gllvmTMB]{gllvmTMB()}} and the wide data-frame \code{\link[=traits]{traits()}} path:
\itemize{
\item \code{NULL} (default): unit weights; byte-identical to the no-weight fit.
\item \code{numeric(nrow(Y))}: row vector. Each cell \verb{(i, j)} inherits
\code{weights[i]} for every column \code{j}. The common case (per-row sample
size, per-individual study weight, per-site sampling intensity).
\item \code{matrix} of \code{dim(Y)}: per-cell weights. NA cells in \code{Y} MUST be NA
in \code{weights} (the only meaningful alignment); a mismatch errors.
Use this for meta-analytic / per-cell-uncertainty weighting.
\item single positive numeric (length 1): broadcast to all cells.
Disambiguation when \code{nrow(Y) == ncol(Y)}: \code{length(dim(weights))}
decides — \code{NULL} (1-d) → vector, row-broadcast; \code{c(n, m)} (2-d) →
per-cell. Values must be non-negative and finite (NA-aligned cells
excepted). For binomial trial-count semantics use the long-format
API (\code{\link[=gllvmTMB]{gllvmTMB()}} with \code{weights = n_trials}) instead.
}}

\item{...}{Passed to \code{\link[=gllvmTMB]{gllvmTMB()}}.}
}
\value{
A \code{gllvmTMB_multi} fit. The column dimension of \code{Y} is
exposed as the "trait" axis of the engine (so
\code{\link[=extract_ordination]{extract_ordination()}} returns the trait loadings). For the
site × species special case the columns are species, and the
loadings are species loadings; the same machinery returns
trait loadings for individual × trait morphometrics, study
loadings for paper × outcome meta-analysis, etc.
}
\description{
Convenience wrapper that lets users supply a wide \verb{unit × trait}
response matrix \code{Y} (rows = units, columns = traits) and an
optional unit-level predictor data frame \code{X}. Pivots to long
format internally and dispatches to \code{\link[=gllvmTMB]{gllvmTMB()}}.
}
\details{
The "unit × trait" framing is generic: \verb{site × species} (joint
species distribution modelling), \verb{individual × trait}
(morphometrics / behavioural syndromes), \verb{species × trait}
(phylogenetic comparative), \verb{paper × outcome} (meta-analysis),
or any similar layout. The function does not assume the
ecological special case; it is the unified matrix-in entry point
for the stacked-trait GLLVM engine.
}
\section{Deprecation}{

\code{gllvmTMB_wide()} is \strong{soft-deprecated} as of gllvmTMB 0.2.0. The
recommended replacement is \code{\link[=gllvmTMB]{gllvmTMB()}} with the \code{\link[=traits]{traits()}} LHS
marker, which is more general (formula-native predictors, full
covariance grammar, mixed-family fits) and matches the
formula-first idiom used by \code{lme4}, \code{glmmTMB}, \code{brms}, \code{drmTMB}.

Migration:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{## Old:
fit <- gllvmTMB_wide(Y, X = X_df, d = 2,
                     formula_extra = ~ env_temp + env_precip)

## New:
df_wide <- cbind(data.frame(unit = rownames(Y)),
                 as.data.frame(Y), X_df)
fit <- gllvmTMB(
  traits(<colnames of Y>) ~ 1 + env_temp + env_precip +
    latent(1 | unit, d = 2),
  data = df_wide,
  unit = "unit"
)
}\if{html}{\out{</div>}}

Per-cell weight matrices (the one path \code{gllvmTMB_wide()} uniquely
supports) remain available via the long-format API by passing a
long-format \code{weights} column aligned with \verb{(unit, trait)} rows.
See \code{docs/design/02-data-shape-and-weights.md}.
}

\seealso{
\code{\link[=gllvmTMB]{gllvmTMB()}} for the recommended formula-API entry point
(long format or wide data frames via \code{\link[=traits]{traits()}} LHS sugar);
\code{\link[=extract_Sigma]{extract_Sigma()}} for post-fit covariance summaries;
\code{\link[=extract_ordination]{extract_ordination()}} for scores and loadings. The source-tree
contract is \code{docs/design/02-data-shape-and-weights.md}.
}
\keyword{internal}
