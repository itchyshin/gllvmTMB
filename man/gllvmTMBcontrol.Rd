% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gllvmTMB.R
\name{gllvmTMBcontrol}
\alias{gllvmTMBcontrol}
\title{Control parameters for \code{\link[=gllvmTMB]{gllvmTMB()}}}
\usage{
gllvmTMBcontrol(
  d_B = NULL,
  d_W = NULL,
  spde_mode = c("per_trait", "shared"),
  n_init = 1L,
  optimizer = c("nlminb", "optim"),
  optArgs = list(),
  init_jitter = 0.3,
  init_strategy = c("default", "single_trait_warmup"),
  start_method = list(method = NULL, jitter.sd = 0),
  start_from = NULL,
  se = TRUE,
  verbose = FALSE,
  integration = c("laplace", "va"),
  va_H = 7L,
  va_eval_method = c("auto", "jj", "gh"),
  aghq = FALSE,
  aghq_iter_cap = 1L,
  aghq_n_adapt = 400L,
  aghq_ridge = 2,
  aghq_continuation = TRUE,
  aghq_shift_tol = 1e-04,
  aghq_grad_tol = 1e-04,
  aghq_grad_tol_rel = 1e-06,
  aghq_multistart = TRUE,
  aghq_f_tol = 1e-09,
  aghq_escalate_patience = 3L,
  aghq_rho_min = 1/64,
  warn_runaway = TRUE,
  allow_nongaussian_reml = FALSE,
  loading_ridge = NULL,
  ...
)
}
\arguments{
\item{d_B, d_W}{Latent dimensions for the between-unit and within-unit
reduced-rank components. Set to a positive integer to enable
\code{latent()} / \code{indep()} covariance structures at the corresponding tier.}

\item{spde_mode}{\code{"per_trait"} (default) fits one independent SPDE field
per trait when a \code{spatial()} term is present; \code{"shared"} fits one shared
SPDE field with trait-specific scalar loadings.}

\item{n_init}{Number of random-start replicates. Reduced-rank GLLVMs
are often multimodal because the latent-factor likelihood has many
equivalent local maxima; running several restarts with different
starting values and keeping the fit with the lowest \code{-logLik}
reduces the risk of settling on a suboptimal solution. Default
1 (single fit). Increase to 5–10 for two-level rr models.}

\item{optimizer}{One of \code{"nlminb"} (default) or \code{"optim"}. Use the
latter together with \code{optArgs} for finicky two-level rr fits.}

\item{optArgs}{A list of arguments passed to the optimiser. For
\code{optim} the most useful is \code{list(method = "BFGS")}.}

\item{init_jitter}{Standard deviation of N(0, sigma) jitter applied to
the starting parameter vector across the \code{n_init} restarts.
Default 0.3.}

\item{init_strategy}{One of \code{"default"} (current behaviour) or
\code{"single_trait_warmup"}. The warmup option fits an intercept-only univariate GLM
per trait — with that trait's family — and seeds the matching
\verb{log_phi_*} entries before \code{MakeADFun()}. Recommended for count
families (especially \code{nbinom2}) where the default initialisation
can leave the optimiser walking the
\eqn{(\psi_t, \phi_t)} trade-off ridge. No effect for traits whose
family doesn't carry a \code{phi} parameter (Gaussian, Poisson, binomial).
Default \code{"default"}.}

\item{start_method}{Optional reduced-rank starting-value method, modelled
after \code{glmmTMB::glmmTMBControl(start_method = ...)} but extended for
two-level gllvmTMB models. Use \code{list(method = "res", jitter.sd = 0.2)}
to seed \code{latent()} loadings and latent scores from a reduced-rank
decomposition of fixed-effect residuals. Use \code{list(method = "indep")}
to first fit the matching independent diagonal GLMM/GLLVM and
copy its estimated fixed effects, per-trait variance starts, and random
effects into the full latent covariance fit. \code{jitter.sd} adds Normal jitter
to residual-start latent scores. Default
\code{list(method = NULL, jitter.sd = 0)} keeps the historical starts.}

\item{start_from}{Optional fitted \code{gllvmTMB} object, usually a simpler
model such as one \code{latent()} tier or an independent diagonal model
(\code{indep()}). Any estimated TMB
parameters with shapes matching the current model are copied into the
starting parameter list before optimisation.
This implements the "fit simpler, then use it as starting values"
workflow recommended for complex reduced-rank models.}

\item{se}{Logical; if \code{TRUE}, compute \code{TMB::sdreport()} after
optimisation so Wald standard errors and Hessian diagnostics are
available. If \code{FALSE}, skip standard-error calculation and return the
point-estimate fit with \code{sd_report = NULL} and a diagnostic note. This
is useful for hard models where the point estimate is needed and
uncertainty will be obtained by bootstrap or profile methods. Default
\code{TRUE}.}

\item{verbose}{If \code{TRUE}, prints a one-line summary per restart so
the user can see which seed led to the winning fit. Default
\code{FALSE}.}

\item{integration}{Which method evaluates the latent-variable integral.
\code{"laplace"} (default) is the Laplace approximation and is the \strong{only}
route that yields a marginal likelihood, so it is the only one for which
\code{\link[=logLik]{logLik()}}, \code{\link[=AIC]{AIC()}}, \code{\link[=BIC]{BIC()}} and likelihood-ratio tests are defined.

\code{"va"} selects an opt-in \strong{research} route whose objective is an evidence
lower bound, not a marginal likelihood. Fixed-effect \code{\link[=vcov]{vcov()}} and
\code{\link[=confint]{confint()}} use profiled-Schur VA-Wald information and are labelled
\code{calibrated = FALSE}; \code{\link[=getLV]{getLV()}} can return a variational posterior SD,
which is not a frequentist random-effect standard error. No nominal
coverage claim is made. A bound must not be compared across ranks or
models, so it cannot be used for model or rank selection.

It is admitted only inside the Gate-E implementation/light-fit region —
\code{latent(..., unique = FALSE)}, the 18 scalar family/link cells in the
response-family registry, \code{d} up to 2, up to 80 responses, at least 100
units, and the native TMB engine — and
requesting it outside that region is an \strong{error}, not a warning. The \code{d}
limit is where a pre-registered recovery gate actually passed: \code{d = 4} was
measured and refused, because with few responses the planted axes collapse
far more often than the gate's tolerance allows.
It cannot be combined with \code{aghq}, which is an alternative evaluation of
the same integral rather than an additional layer.

Offering this value advertises nothing about its accuracy.

\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}} The \strong{ordination surface is
available} for a \code{"va"} fit: \code{\link[=extract_ordination]{extract_ordination()}}, \code{\link[=getLV]{getLV()}},
\code{\link[=getLoadings]{getLoadings()}} and \code{\link[=extract_loadings]{extract_loadings()}} return latent scores and
loadings as POINT ESTIMATES. Fixed-effect \code{\link[=confint]{confint()}} and \code{\link[=vcov]{vcov()}} expose
uncalibrated VA-Wald uncertainty; \code{getLV(se = TRUE)} returns a variational
\emph{posterior} SD, not a frequentist standard error, and only when
\code{eval_method} resolves to \code{"gh"}. Loading/covariance intervals remain
unavailable.

Recovery of that ordination was measured against planted truth at the
admitted cells (\code{d = 2}, 8 responses, n = 150 and 400, 50 seeds per cell;
\code{dev/va-usability/A2-ATTENUATION.md}), with the Laplace route run on the
same simulated data as a control:
\itemize{
\item \strong{Gaussian}: the loading-implied variance is recovered at 0.98–1.02 of
truth, and the mean paired difference in latent-score correlation
against Laplace is below \code{2e-07} at both \code{n}.
\item \strong{Poisson}: also 0.98–1.02 of truth, with a paired latent-score
difference of ~\code{4e-04} — negligible in magnitude but statistically
detectable, and the loading scale sits ~1\% below Laplace.
\item \strong{Binomial}: latent scores recover at r ≈ 0.59. That ceiling is a limit
of \strong{binary data} at these cells rather than of the variational route —
the Laplace default reaches r ≈ 0.56–0.59 on the same data, and a third
estimator (Gauss-Hermite) lands in the same place. The loading SCALE,
however, is biased low (0.58–0.67 of truth) and this \strong{is} a real bias
relative to Laplace, whose median brackets 1. Read a binomial
ordination's axes and relative positions, not the absolute magnitude of
its loadings.
}

The comparison is not effort-matched: the variational arm ran four starts
behind an agreement-and-gradient health gate, the Laplace arm a single
start admitted on a positive-definite Hessian.

\code{"va"} returns an object of class \code{"gllvmTMB_va"} (see
\link{gllvmTMB_va-methods}) rather than an ordinary fit, so that every method
which would treat its objective as a likelihood fails loudly instead of
returning a number. Because the engine runs its own multi-start and
optimiser policy, the search settings of \code{\link[=gllvmTMBcontrol]{gllvmTMBcontrol()}} — \code{n_init},
\code{optimizer}, \code{optArgs}, \code{start_from}, \verb{init_*} and \code{se} — have no effect
on this route. Any model structure the route cannot represent (a latent
term away from the unit grouping, a constrained ordination, an offset,
weights, \code{REML}, \code{lambda_constraint}, \code{Xcoef_fixed}, or a further random
effect) is an \strong{error}, never a silent omission.

There is no \code{"eva"} value. The EVA engine exists in this package and is
reachable as a research route, but it is not wired to \code{\link[=gllvmTMB]{gllvmTMB()}}, and an
argument value that could only ever raise an error would advertise a
capability the package does not have. Its own measurements are the reason
it is not a candidate here: EVA delivers valid inference for the
regression coefficients but not for \verb{Lambda Lambda'}, which is the
between-response covariance this package exists to estimate.}

\item{va_H}{Gauss-Hermite quadrature order for the variational route, a
single odd integer \verb{>= 3} (default \code{7}). Ignored unless
\code{integration = "va"}.

Quadrature dominates the cost of a variational fit and the work is linear
in \code{H}, so this is the main speed control on that route. On a binomial
probit cell with 20 traits, \code{H = 7} was indistinguishable from \code{H = 61} in
both the recovered loading scale and the recovered linear predictor, at
two and at five latent dimensions, while running 3.4-6.7 times faster.
\code{H = 5} was measurably worse at five latent dimensions. Gate E therefore
promoted \code{H = 7}; \code{H = 61} remains available as a diagnostic.}

\item{va_eval_method}{How the variational route evaluates the per-observation
expectation. \code{"auto"} (default) selects Gauss-Hermite quadrature for every
admitted scalar family/link cell and is what you want unless you are
comparing methods.

\code{"gh"} uses Gauss-Hermite quadrature. \code{"jj"} uses the Jaakkola-Jordan
bound, which is defined only for pure binomial-logit fits and is
substantially cheaper. The two differ in more than speed: on binary data
the bound's recovered loading magnitudes are biased downward and do not
improve as the sample grows, whereas quadrature's converge. Latent-variable
\emph{scores}, and therefore ordinations, are effectively unaffected by the
choice. Ignored unless \code{integration = "va"}.}

\item{aghq}{Adaptive Gauss-Hermite quadrature for the between-unit latent
block. \code{FALSE} (default) fits by Laplace approximation. A positive integer
requests that many quadrature nodes. \code{"auto"} lets the package decide, and
it declines to Laplace whenever the model is ineligible or the expected
gain does not justify the cost. \strong{Opt-in and experimental}: no capability
claim is made for quadrature-fitted models, and eligibility is narrow — the
random part must be a single ordinary \code{latent()} block on the unit tier.}

\item{aghq_iter_cap}{Optimiser iteration budget for the first adaptation
pass. Default \code{1L}. Later passes escalate when continuation is enabled.}

\item{aghq_n_adapt}{Maximum number of adaptation passes. Default \code{400L}.}

\item{aghq_ridge}{Ridge penalty on the loadings, as the scale \code{tau};
\code{Inf} disables it. Default \code{2}. Naming a numeric value explicitly also
makes the \code{Laplace + ridge} control reachable without quadrature.
\code{"auto"} is an \strong{opt-in experimental} route for pure single-trial
Bernoulli models with one ordinary unit-tier \code{latent()} block. It uses an
unpenalised 9-node multi-start AGHQ pilot, then
\verb{tau = min(6, max(1, ||Lambda_pilot||_F / sqrt(pq)))}. If the pilot or
scale-aware final fit is unusable, the returned fit transparently falls
back to the shipped \code{tau = 2} route; inspect
\code{fit$aghq$ridge_auto}. Evidence supports failure/runaway avoidance in this
scope, not a broad loading-accuracy improvement. The measured grid used a
logit link with \code{p = 6}, \code{q = 2}, and \code{n = 100}, \code{400}, or \code{1600}; other
links and dimensions are extrapolations. The
\code{"auto"} route always uses 9-node multi-start AGHQ for both pilot and final
fits; a conflicting node/start control is replaced with a warning, while an
explicit \code{aghq = FALSE} is incompatible.}

\item{aghq_continuation}{If \code{TRUE} (default), the adaptation loop may raise
\code{aghq_iter_cap} across passes. \code{FALSE} pins the cap and disables escalation.}

\item{aghq_shift_tol, aghq_grad_tol, aghq_f_tol}{Convergence tolerances for the
adaptation loop: the quadrature-point shift, the gradient at the adaptation
point, and the change in objective. Defaults \code{1e-4}, \code{1e-4}, \code{1e-9}.}

\item{aghq_grad_tol_rel}{Relative gradient tolerance, tested as
\verb{max|grad| / max(1, |objective|)}. Default \code{1e-6}. The gradient of a
likelihood summed over many observations grows with the sample, so the
absolute \code{aghq_grad_tol} on its own becomes unreachable at scale. The two are
combined with \code{OR}, so this can only ever admit more fits as converged, never
fewer; set it to \code{0} to test the absolute tolerance only. Read
\code{fit$aghq$converged} for the verdict — \strong{not} \code{fit$opt$convergence}, which
on the quadrature path records the optimiser's per-pass iteration cap and so
reports a limit even on a healthy fit.}

\item{aghq_multistart}{If \code{TRUE} (default), the quadrature is run from two
starting points — the Laplace optimum and a data-driven alternative that uses
no knowledge of the truth — and the fit with the better final objective is
kept. \code{FALSE} uses the Laplace warm start only, reproducing the behaviour of
earlier versions. The extra start costs one additional adaptation run, and
exists because the Laplace optimum is sometimes itself a runaway that the
quadrature would otherwise inherit.}

\item{aghq_escalate_patience}{Number of accepted passes before the iteration
cap is escalated. Default \code{3L}.}

\item{aghq_rho_min}{Minimum trust-region-style acceptance ratio for a pass.
Default \code{1/64}.}

\item{warn_runaway}{If \code{TRUE} (default), warn once per session when a
binomial latent-variable fit triggers the package's existing runaway-loading
diagnostic. Set \code{FALSE} to silence the fit-time warning; the diagnostic
remains available through \code{\link[=gllvmTMB_diagnose]{gllvmTMB_diagnose()}}.}

\item{allow_nongaussian_reml}{Permit \code{REML = TRUE} for non-Gaussian families.
Default \code{FALSE}; the restricted likelihood is defined by a Gaussian
marginalisation, so this is an escape hatch, not an endorsement.}

\item{loading_ridge}{Integration-neutral alias for \code{aghq_ridge}. Use this
spelling to request the same loading MAP penalty on a Laplace fit without
suggesting that the penalty belongs to AGHQ. The default \code{NULL} preserves
historical unpenalised Laplace fits. Supply at most one of
\code{loading_ridge} and \code{aghq_ridge}; neither may be combined with
\code{estimator = "mspl"}.}

\item{...}{Reserved for future use. Currently ignored with a warning.}
}
\value{
A \code{gllvmTMBcontrol} list.
}
\description{
Control parameters for \code{\link[=gllvmTMB]{gllvmTMB()}}
}
\details{
\strong{Recommended workflow for two-level latent models.} Reduced-rank
likelihoods are notoriously multimodal in two-level fits like
\code{value ~ 0 + trait + latent(0+trait|ID, d_B) + latent(0+trait|obs_ID, d_W)}.
The recommended workflow is:
\enumerate{
\item Run several restarts (\code{n_init = 5} to \code{10}) and keep the one
with the lowest \code{-logLik}.
\item If that fails to converge cleanly, switch the optimiser to
\code{optim} with \code{BFGS}:
\code{gllvmTMBcontrol(optimizer = "optim", optArgs = list(method = "BFGS"))}.
\item \code{start_method = list(method = "res", jitter.sd = 0.2)} is
\strong{soft-deprecated as of 0.6.0} and warns once per session. It still
fits: it decomposes the fixed-effect residual matrix into starting
values for \eqn{\Lambda} and the latent scores. Do not reach for it.

It was retired on measurement. Across 89 simulated fits — Gaussian,
Poisson and negative-binomial, \code{d = 1} to \code{3}, three and five traits —
it was \strong{never materially better} than the default start (its three
best margins were 0.07, 0.29 and 0.66 log-likelihood units) and was
materially worse eight times, once by 14.6 units. At \code{d >= 2} it made
no difference at all. Every failure reported \code{convergence == 0} and a
positive-definite Hessian on both sides, so the worse fit was silent,
and restarts did not detect it: in one such fit all five \code{n_init = 5}
restarts returned the same worse optimum.

If you use it anyway, compare \code{fit$opt$objective} against a
default-start fit and keep the lower value.
\item For Gaussian two-level models, prefer
\code{start_method = list(method = "indep")} or manually fit a simpler
model and pass it through \code{start_from = simpler_fit}. This is a GLMM
warm start rather than a fixed-effect-only GLM residual start.
}
}
\examples{
# gllvmTMBcontrol() is a pure-R constructor: it builds and validates a
# control list without fitting anything.
gllvmTMBcontrol()                                  # historical defaults

# Multi-start to guard against multimodal reduced-rank likelihoods, with
# jitter on the starting parameter vector across restarts.
gllvmTMBcontrol(n_init = 3, init_jitter = 0.2)

# Switch the optimiser to optim + BFGS for finicky two-level rr fits.
gllvmTMBcontrol(optimizer = "optim", optArgs = list(method = "BFGS"))

# Experimental, opt-in scale-aware ridge for the calibrated Bernoulli AGHQ
# scope. The route uses the calibrated 9-node multi-start rule.
gllvmTMBcontrol(aghq_ridge = "auto")
# The same fixed loading MAP penalty, named independently of integration.
gllvmTMBcontrol(loading_ridge = 2)

# Experimental scalar variational route: public auto uses GH with H = 7.
gllvmTMBcontrol(integration = "va")
# H = 61 remains a diagnostic; JJ is explicit binomial-logit only.
gllvmTMBcontrol(integration = "va", va_H = 61, va_eval_method = "gh")

}
