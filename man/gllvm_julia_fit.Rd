% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/julia-bridge.R
\name{gllvm_julia_fit}
\alias{gllvm_julia_fit}
\title{Fit a GLLVM with the Julia engine (GLLVM.jl \code{bridge_fit}).}
\usage{
gllvm_julia_fit(
  y,
  family = "gaussian",
  num.lv = 2L,
  N = NULL,
  X = NULL,
  units_are_rows = FALSE,
  ci_method = c("none", "wald", "profile", "bootstrap"),
  ci_level = 0.95,
  ci_nboot = 200L,
  ci_seed = 0L,
  ...
)
}
\arguments{
\item{y}{Response matrix, p x n (traits x units), or n x p (set \code{units_are_rows}).}

\item{family}{A family object/string, or a list of them (one per trait -> mixed).}

\item{num.lv}{Number of latent variables (K).}

\item{N}{Binomial trials (matrix or scalar), or \code{NULL}.}

\item{X}{Gaussian-only fixed-effect design (p x n x q array), or \code{NULL}.}

\item{units_are_rows}{If \code{TRUE}, \code{y} is n x p and is transposed to p x n.}

\item{ci_method}{Confidence-interval method routed to the Julia engine: one of
\code{"none"} (default, no CIs), \code{"wald"}, \code{"profile"}, or \code{"bootstrap"}. When not
\code{"none"}, the returned object gains the bridge \verb{ci_*} fields (\code{ci_method},
\code{ci_level}, \code{ci_param_names}, \code{ci_estimate}, \code{ci_lower}, \code{ci_upper},
\code{ci_note}), reusing GLLVM.jl's native CI engines (no CI math in R).}

\item{ci_level}{Nominal coverage for the CIs (default \code{0.95}).}

\item{ci_nboot}{Parametric-bootstrap replicates when \code{ci_method = "bootstrap"}
(default \code{200L}).}

\item{ci_seed}{Bootstrap RNG seed (default \code{0L}; fixed for reproducibility).}

\item{...}{Passed to \code{\link[=gllvm_julia_setup]{gllvm_julia_setup()}} (\code{jl_path}, \code{julia_home}).}
}
\value{
A list of class \code{gllvmTMB_julia} with the bridge contract fields
(\code{loadings}, \code{Sigma}, \code{correlation}, \code{loglik}, \code{aic}, \code{bic}, \code{converged}, ...).
When \code{ci_method != "none"} the object also carries the bridge \verb{ci_*} fields.
}
\description{
Fit a GLLVM with the Julia engine (GLLVM.jl \code{bridge_fit}).
}
