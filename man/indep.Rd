% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{indep}
\alias{indep}
\title{Per-trait marginal variance: \code{indep(0 + trait | g)}}
\usage{
indep(formula)
}
\arguments{
\item{formula}{\code{0 + trait | g} style formula (LHS is the trait
factor, typically \code{0 + trait}; RHS is the grouping factor).}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for the \strong{always-alone marginal-only} per-trait
diagonal variance term. Mathematically identical to
\code{unique(0 + trait | g)} standalone (both produce
\eqn{\boldsymbol\Sigma = \mathrm{diag}(\sigma^2_t)} with identity
off-diagonals); the keyword choice is documentary, not operational.
}
\details{
The package's covstruct API organises around two mutually exclusive
modes, distinguished by convention:

\describe{
\item{\strong{Decomposition} (\code{latent + unique})}{Shared cross-trait
covariance plus trait-specific residual:
\eqn{\boldsymbol\Sigma = \boldsymbol\Lambda \boldsymbol\Lambda^\top + \mathbf S}.}
\item{\strong{Marginal} (\code{indep} standalone)}{Per-trait total variance
with no cross-trait decomposition: \eqn{\boldsymbol\Sigma = \mathrm{diag}(\sigma^2_t)}.}
}

Use \code{indep()} when you want to commit to the marginal-only
interpretation explicitly. Use \code{unique()} paired with \code{latent()} when
you want the cross-trait decomposition; \code{unique()} standalone (e.g.
for observation-level random effects in mixed-response fits) also
remains legitimate.
\subsection{Mutual exclusion with \code{latent()}}{

Combining \code{indep(0 + trait | g)} with \code{latent(0 + trait | g, d = K)}
on the same grouping is \strong{over-parameterised} (the model cannot
decide whether trait variance lives in the shared component or the
marginal component). The parser raises a \code{cli::cli_abort()} in this
case. Combining \code{indep} with \code{unique} on the same grouping is
similarly redundant and also errors.
}
}
\examples{
\dontrun{
# Marginal-only fit (per-trait variance, identity correlation):
fit <- gllvmTMB(value ~ 0 + trait + indep(0 + trait | site),
                data = df, unit = "site")

# The mathematically-equivalent decomposition form:
fit <- gllvmTMB(value ~ 0 + trait + unique(0 + trait | site),
                data = df, unit = "site")

# ERROR: indep + latent on the same grouping is over-parameterised.
# gllvmTMB(value ~ 0 + trait +
#           indep(0 + trait | site) +
#           latent(0 + trait | site, d = 2), data = df)
}
}
\seealso{
\code{\link[=unique]{unique()}}, \code{\link[=latent]{latent()}}, \code{\link[=phylo_indep]{phylo_indep()}}, \code{\link[=spatial_indep]{spatial_indep()}},
\code{\link[=extract_Sigma]{extract_Sigma()}}.
}
