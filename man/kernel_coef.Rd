% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/column-coef-foundation.R
\name{kernel_coef}
\alias{kernel_coef}
\title{Dense-kernel response-column coefficients}
\usage{
kernel_coef(formula, K, name = "kernel", rho = NULL)
}
\arguments{
\item{formula}{A coefficient-basis bar expression such as
\code{1 + x | trait}, \code{0 + x | trait}, or \code{1 + x || trait}.}

\item{K}{A labelled dense numeric positive-definite covariance matrix whose
row and column names exactly match the response columns.}

\item{name}{One non-empty name for the kernel source.}

\item{rho}{\code{NULL} to estimate one interior source-strength value, or one
numeric value in \verb{[0, 1]} to fix it.}
}
\value{
A formula marker; never evaluated directly.
}
\description{
Fit response-column-specific random intercepts and/or slopes with source
covariance \code{K_rho = rho * K + (1 - rho) * diag(diag(K))}. Supply a numeric
\code{rho} in \verb{[0, 1]} to fix the mixture or use \code{rho = NULL} (the default) to
estimate one interior value. The supplied labelled dense kernel retains its
marginal scale. The fitted covariance across the coefficient basis is full
for \code{|} and diagonal for \code{||}.
}
\details{
This point-model route is covered for Gaussian multivariate data in long or
\code{traits(...)} wide form, with one dense positive-definite kernel and bare
numeric row predictors. Interval inference, non-Gaussian coefficient models,
simultaneous coefficient sources, and sparse kernel inputs are not
available. Existing \code{\link[=kernel_slope]{kernel_slope()}} remains current and warning-free.

A no-intercept \code{rho = 1} fit uses the released \code{\link[=kernel_slope]{kernel_slope()}} route exactly
and therefore uses raw \code{K} without the dense phylogenetic ridge.
}
\examples{
set.seed(4)
dat <- expand.grid(unit = factor(1:12), trait = factor(paste0("sp", 1:4)))
dat$x <- rnorm(12)[dat$unit]
dat$value <- rnorm(nrow(dat))
K <- 0.4 ^ abs(outer(1:4, 1:4, "-"))
dimnames(K) <- list(levels(dat$trait), levels(dat$trait))
fit <- gllvmTMB(value ~ 1 + kernel_coef(1 + x | trait, K = K, rho = 0.5),
  data = dat, trait = "trait", unit = "unit", family = gaussian(),
  control = gllvmTMBcontrol(se = FALSE), silent = TRUE)
extract_Sigma(fit, level = "column_coef")
}
\seealso{
\code{\link[=kernel_slope]{kernel_slope()}}, \code{\link[=column_coef]{column_coef()}}, \code{\link[=phylo_coef]{phylo_coef()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}
}
