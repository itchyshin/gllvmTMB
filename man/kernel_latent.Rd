% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-keywords.R
\name{kernel_latent}
\alias{kernel_latent}
\alias{kernel_unique}
\alias{kernel_indep}
\alias{kernel_dep}
\title{Generic dense-kernel covariance keywords}
\usage{
kernel_latent(unit, K, d = 1, name = "kernel")

kernel_unique(unit, K, name = "kernel")

kernel_indep(unit, K, name = "kernel")

kernel_dep(unit, K, name = "kernel")
}
\arguments{
\item{unit}{Unquoted grouping column whose levels align with \code{rownames(K)}.}

\item{K}{Numeric dense positive-semidefinite covariance/correlation matrix.
In C1 this routes through the existing phylo-equivalent dense \code{vcv}
path.}

\item{d}{Integer latent rank for \code{kernel_latent()}.}

\item{name}{Character scalar used as the extractor level, e.g.
\code{extract_Sigma(fit, level = "cross")}.}
}
\value{
A formula marker; never evaluated as a regular R function.
}
\description{
\code{kernel_latent()} and \code{kernel_unique()} fit a named random-effect tier
with a user-supplied between-unit covariance matrix \code{K}. The IN scope
(\code{KER-02}) for this C1 dense-kernel slice is the phylo-equivalent path:
\code{kernel_latent(unit, K = A, d = q)} plus \code{kernel_unique(unit, K = A)}
must match \code{phylo_latent(unit, vcv = A, d = q)} plus
\code{phylo_unique(unit, vcv = A)} to less than \code{1e-6} for log likelihood
and extracted \code{Sigma}. The PARTIAL scope (\code{COE-02}) is coevolution:
users can pass a \code{K_star} from \code{\link[=make_cross_kernel]{make_cross_kernel()}} and extract the
host-partner shared covariance block with \code{\link[=extract_Gamma]{extract_Gamma()}}. C2 recovery
evidence covers known-\code{Gamma} recovery, a zero-\code{Gamma} block-diagonal null,
and a sparse-versus-dense association sensitivity case; uncertainty and
\code{rho} profiling remain later example / workflow layers.
}
\examples{
\dontrun{
A <- diag(5)
dat <- data.frame(
  unit = factor(rep(paste0("u", 1:5), each = 2), levels = paste0("u", 1:5)),
  obs = factor(seq_len(10)),
  y1 = rnorm(10),
  y2 = rnorm(10)
)
rownames(A) <- colnames(A) <- levels(dat$unit)
fit <- gllvmTMB(
  traits(y1, y2) ~
    1 + kernel_latent(unit, K = A, d = 1, name = "known") +
    kernel_unique(unit, K = A, name = "known"),
  data = dat,
  unit = "obs",
  cluster = "unit",
  family = gaussian()
)
extract_Sigma(fit, level = "known")
}

}
