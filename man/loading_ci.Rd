% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading-ci.R
\name{loading_ci}
\alias{loading_ci}
\title{Confidence intervals on individual entries of the loading matrix}
\usage{
loading_ci(
  fit,
  level = c("unit", "unit_obs"),
  method = c("wald", "wald_asym", "profile"),
  conf_level = 0.95,
  sigma_d2 = 1,
  loading_scale = NULL
)
}
\arguments{
\item{fit}{A multivariate \code{gllvmTMB()} fit object.}

\item{level}{Which loading matrix to summarise: \code{"unit"} (default)
or \code{"unit_obs"}. Legacy aliases \code{"B"} / \code{"W"} are accepted with a
one-shot deprecation warning.}

\item{method}{CI method:
\describe{
\item{\code{"wald"} (default)}{Symmetric Wald via the joint delta method
on the requested \code{loading_scale}.}
\item{\code{"wald_asym"}}{Asymmetric Wald via the Fisher-z transformation
on \eqn{\rho_{tk} = \Lambda_{tk}/\sqrt{\Sigma_{total,tt}}}.
This method is available only with
\code{loading_scale = "standardized"}; its bounds stay in \verb{(-1, 1)}.
It captures bounded-support asymmetry but not higher-order
log-likelihood curvature.}
\item{\code{"profile"}}{Profile-likelihood inversion through
\code{\link[=loading_profile]{loading_profile()}}. This refits across a grid for each free
loading entry and can be used when Wald inference is blocked by a
non-positive-definite Hessian. The current profile target is raw
\code{Lambda}, so \code{loading_scale = "standardized"} is refused.}
}}

\item{conf_level}{Confidence level. Defaults to 0.95.}

\item{sigma_d2}{Deprecated compatibility argument; ignored. Standardised
inference now derives each denominator from model-implied total variance
via \code{\link[=extract_Sigma]{extract_Sigma()}} instead of accepting one scalar residual variance.}

\item{loading_scale}{\code{NULL}, \code{"raw"}, or \code{"standardized"}. \code{NULL} uses
\code{"raw"} for \code{"wald"} / \code{"profile"} and \code{"standardized"} for
\code{"wald_asym"}. Estimates, SEs, bounds, and downstream null regions are
always on this recorded scale.}
}
\value{
A data frame (one row per Lambda entry) with columns
\code{trait}, \code{axis}, \code{estimate}, \code{se}, \code{lower}, \code{upper}, \code{method},
\code{loading_scale}, and \code{pinned}. \code{pinned = TRUE} records that the raw
loading was fixed by \code{lambda_constraint}. Its raw SE is zero, but its
standardised SE can be nonzero because total variance can still vary.
}
\description{
Return per-entry CIs on either the raw reduced-rank loading matrix
\verb{Lambda_<level>} or the standardised loading
\code{rho[t,k] = Lambda[t,k] / sqrt(Sigma_total[t,t])} of a confirmatory
\code{gllvmTMB()} fit. A numerical Jacobian of the complete requested loading
vector is combined with the TMB \code{sdreport()} covariance. On the
standardised scale this includes covariance among axes and uncertainty in
fitted denominator components such as \code{Psi}.
}
\details{
Per-entry CIs are \strong{only well-defined for confirmatory fits} — i.e.
fits supplied with a \code{lambda_constraint} that fixes enough entries
to pin the rotation. Exploratory fits leave Lambda identified only
up to a \verb{d x d} orthogonal rotation, so the SE on any single
\code{Lambda[i, k]} depends on the rotation convention and is not a
biological quantity. This function therefore errors on exploratory
fits and points the user at \code{\link[=confirmatory_lambda]{confirmatory_lambda()}} /
\code{\link[=suggest_lambda_constraint]{suggest_lambda_constraint()}}, or at \code{\link[=extract_communality]{extract_communality()}} /
\code{\link[=extract_Sigma]{extract_Sigma()}} for rotation-invariant summaries.
}
\section{Interval calibration}{

Standardized symmetric joint-delta Wald intervals have an exact empirical
certificate only for the structurally free strict-lower targets in native
Laplace, pinned, unrotated, lower-triangular ordinary Gaussian unit-tier
three-trait cells \verb{(n_units=150,d=2)}, \verb{(n_units=400,d=1)}, and
\verb{(n_units=400,d=2)}, at \code{conf_level = 0.95}. The \verb{(n_units=150,d=1)} cell
failed its lower-band gate. This is one frozen DGP: trait intercepts
\verb{(-0.20, 0.10, 0.25)}, unique standard deviations
\verb{(0.70, 0.80, 0.90)}, and loading vector \verb{(0.80, 0.45, -0.35)} for \code{d=1},
with the second column \verb{(0, 0.70, 0.40)} added for \code{d=2}. Coverage is
conditional on eligible fits (optimizer convergence, converged fit health,
an available \code{sdreport()}, and a positive-definite Hessian); availability
was 98.82\%, 93.38\%, and 96.18\% in the three certified cells, respectively.
The certificate does not extend to another loading, unique-variance, or
intercept regime. Pinned diagnostic rows, Fisher-z Wald
(\code{method = "wald_asym"}), arbitrary confirmatory constraints, raw-loading
intervals, profiles, bootstraps, other rotations, and every neighbouring
family, tier, rank, sample size, or confidence level remain exploratory.
The worked example below is binomial-probit and is outside the certified
cells. See \code{NEWS.md} for the current coverage status.
}

\examples{
\dontrun{
# Build a confirmatory fit
M <- confirmatory_lambda(
  species  = species_names,
  group    = species_group,
  d        = 2L,
  loads_on = list(A = 1L, B = 2L)
)
fit <- gllvmTMB(
  value ~ 0 + trait + latent(0 + trait | site, d = 2L),
  data              = df_long,
  family            = binomial(link = "probit"),
  lambda_constraint = list(unit = M)
)
loading_ci(fit, level = "unit")
loading_ci(fit, level = "unit", loading_scale = "standardized")
}

}
\seealso{
\code{\link[=flag_unreliable_loadings]{flag_unreliable_loadings()}} for a decision-aid summary;
\code{\link[=confirmatory_lambda]{confirmatory_lambda()}} to build a confirmatory constraint matrix;
\code{\link[=extract_communality]{extract_communality()}} for rotation-invariant alternatives.
}
