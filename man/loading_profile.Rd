% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading-profile.R
\name{loading_profile}
\alias{loading_profile}
\title{Profile-likelihood curve(s) for entries of the loading matrix}
\usage{
loading_profile(
  fit,
  level = c("unit", "unit_obs"),
  entries = NULL,
  n_grid = 11L,
  grid_extent = 6,
  conf_level = 0.95
)
}
\arguments{
\item{fit}{A confirmatory \code{gllvmTMB()} multi-trait fit.}

\item{level}{Which loading matrix: \code{"unit"} (default) or
\code{"unit_obs"}. Legacy \code{"B"}/\code{"W"} accepted with deprecation.}

\item{entries}{Optional matrix or data.frame of \verb{(i, k)} index
pairs specifying which Lambda entries to profile. Default \code{NULL}
profiles every entry that is NOT pinned (i.e. every free entry
the data informs). Pinned entries are skipped.}

\item{n_grid}{Integer; number of grid points per entry. Default 11.}

\item{grid_extent}{Numeric; total grid width as a multiple of a
robust scale estimate of the loading (default 6 — i.e. estimate
± 3 scale units on each side). At pdHess-OK fits the scale is
the Wald SE; otherwise a heuristic based on \verb{|Lambda|/2 + 0.5}.}

\item{conf_level}{Confidence level for the eventual CI inversion;
stored on the output for downstream consumers.}
}
\value{
A data.frame of class \code{profile_loadings} with columns:
\code{trait}, \code{axis}, \code{i}, \code{k}, \code{profile_value}, \code{objective}
(the negative log-likelihood evaluated at that value),
\code{delta_deviance} (= 2 * (objective - min(objective))),
\code{estimate}, \code{conf_level}. Pinned entries do not appear in the
output.
}
\description{
For each requested entry \verb{(i, k)} of \verb{Lambda_<level>}, refit the
model with that entry pinned to a grid of values and return the
resulting log-likelihood curve. The output is a long data.frame
with class \code{profile_loadings}; pass it to \code{\link[=plot]{plot()}} to visualise the
classic LR U-shape, or to \code{\link[=loading_ci]{loading_ci()}} (via \code{method = "profile"})
to invert the curve and get CIs that do NOT depend on the fit's
Hessian being positive-definite.
}
\details{
This is \strong{expensive}: each entry requires \code{n_grid} partial refits,
so a 20-species 2-factor fit at default \code{n_grid = 11} is ~440 fits.
For binary probit at the scale of this article's fixture, expect
minutes. Use \code{entries} to restrict to a subset when iterating.
}
\examples{
\dontrun{
pf <- loading_profile(fit, level = "unit")
plot(pf)                              # all entries faceted
loading_ci(fit, method = "profile")   # inverts pf internally
}

}
\seealso{
\code{\link[=loading_ci]{loading_ci()}} for the inverted-CI version,
\code{\link[=plot.profile_loadings]{plot.profile_loadings()}} for the U-shape visualisation.
}
