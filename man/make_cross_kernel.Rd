% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-helpers.R
\name{make_cross_kernel}
\alias{make_cross_kernel}
\title{Build a cross-lineage relatedness kernel}
\usage{
make_cross_kernel(A_H, A_P, W, rho = 0.5, eps = 1e-08)
}
\arguments{
\item{A_H, A_P}{Numeric square correlation matrices for the host and
partner lineages. Both must be symmetric, positive semidefinite, and
have unit diagonal.}

\item{W}{Numeric association matrix with \code{nrow(W) == nrow(A_H)} and
\code{ncol(W) == nrow(A_P)}. If \code{W} has row or column names, they are
aligned to \code{A_H} / \code{A_P} names before the kernel is built.}

\item{rho}{Scalar bridge strength in \verb{[-1, 1]}. Larger absolute values
put more covariance in the off-diagonal host-partner block.}

\item{eps}{Positive numerical floor used when taking symmetric square
roots and scaling an all-zero or near-zero \code{W}.}
}
\value{
A numeric correlation matrix with the host block first and the
partner block second. The matrix carries lightweight metadata for
downstream extractors, including the fixed \code{rho} used to build the
host-partner bridge.
}
\description{
\code{make_cross_kernel()} builds the block relatedness matrix for the C0
coevolution prototype. The IN scope (\code{KER-01}) is a correlation-scale
positive-semidefinite matrix
\code{K_star = rbind(cbind(A_H, C_HP), cbind(t(C_HP), A_P))}, where \code{A_H}
and \code{A_P} are within-lineage relatedness matrices and \code{C_HP} is the
cross-lineage bridge induced by the association matrix \code{W}. The PARTIAL
scope (\code{COE-01}) is the prototype path: use \code{K_star} through the
existing \code{phylo_latent(..., vcv = K_star, unique = TRUE)} engine. The
generic \verb{kernel_*()} surface (\code{KER-02}) and
validated \code{extract_Gamma()} coevolution gate (\code{COE-02}) are now covered
separately; this helper only builds the input kernel.
}
\examples{
A_H <- matrix(c(
  1.0, 0.3, 0.1,
  0.3, 1.0, 0.2,
  0.1, 0.2, 1.0
), 3, 3, byrow = TRUE)
A_P <- matrix(c(1.0, 0.25, 0.25, 1.0), 2, 2)
W <- matrix(c(1, 0, 0.5, 0, 1, 0.25), 3, 2, byrow = TRUE)
K_star <- make_cross_kernel(A_H, A_P, W, rho = 0.4)
min(eigen(K_star, symmetric = TRUE, only.values = TRUE)$values)

}
