% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{meta}
\alias{meta}
\title{Known sampling-error term (brms-style, deprecated alias)}
\usage{
meta(value, sampling_var)
}
\arguments{
\item{value}{The response column name (typically the column called
\code{value} in a stage-1 summary, or an effect-size column in a
meta-analytic dataset).}

\item{sampling_var}{Either an unquoted column name holding the
per-row sampling variance, or a length-1 numeric used for every row.
Deprecated in favour of \code{meta_known_V(value, V = V)}.}
}
\value{
A formula marker; never evaluated.
}
\description{
A formula keyword for the two-stage meta-regression workflow's
stage-2 entry, where the responses are stage-1 estimates (or
pre-computed effect sizes such as Hedges' g) and their marginal
sampling variances are known. This keyword desugars to
\code{equalto(0 + obs | grp_V, V)} inside \code{gllvmTMB()}.
}
\details{
\strong{Deprecated alias.} Use \code{\link[=meta_known_V]{meta_known_V()}} in new code:
\code{meta_known_V(value, V = V)}.
\subsection{Diagonal vs block-diagonal V}{

\code{meta(value, sampling_var = vi)} describes the \strong{diagonal} case --
rows independent. For multivariate meta-analyses with multiple
effect sizes per study (multiple outcomes, multiple traits per
individual, etc.), within-study sampling errors are correlated and
V is \strong{block-diagonal}. The engine supports any positive-definite
\eqn{n \times n} V via \code{solve(V)} internally. Build a block-diagonal
V with \code{\link[=block_V]{block_V()}} and pass it to \code{gllvmTMB()} as \code{known_V = V_block}.
}
}
\examples{
\dontrun{
# Preferred (canonical) form -- use meta_known_V():
V    <- diag(stage1_summary$sampling_var)
fit2 <- gllvmTMB(value ~ 0 + trait +
                   latent(0 + trait | site, d = 2) +
                   meta_known_V(value, V = V),
                 data = stage1_summary, known_V = V)

# Deprecated form (still works, emits a warning):
fit2 <- gllvmTMB(value ~ 0 + trait +
                   latent(0 + trait | site, d = 2) +
                   meta(value, sampling_var = sampling_var),
                 data = stage1_summary, known_V = V)
}
}
\seealso{
\code{\link[=meta_known_V]{meta_known_V()}} (canonical replacement); \code{\link[=block_V]{block_V()}} for
constructing a block-diagonal sampling-V from a study factor;
\code{\link[=gllvmTMB]{gllvmTMB()}} for the stage-2 fit.
}
