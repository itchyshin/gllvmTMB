% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordination-uncertainty.R
\name{ordination_uncertainty}
\alias{ordination_uncertainty}
\title{Per-unit covariance of ordination (latent) scores}
\usage{
ordination_uncertainty(fit, level = "unit")
}
\arguments{
\item{fit}{A fitted multivariate model returned by \code{\link[=gllvmTMB]{gllvmTMB()}}. Must be
a native TMB fit with \code{gllvmTMBcontrol(se = TRUE)} (the default);
\code{engine = "julia"} bridge fits, \code{integration = "va"} fits, and
\code{estimator = "mspl"} fits are refused (see Details).}

\item{level}{\code{"unit"} (between-unit, \code{z_B}) or \code{"unit_obs"} (within-unit,
\code{z_W}). Deprecated aliases \code{"B"} and \code{"W"} are still accepted with a
warning.}
}
\value{
\code{NULL} if the fit has no \code{latent()} term at the requested level
(matching \code{\link[=extract_ordination]{extract_ordination()}}). Otherwise a list of class
\code{"gllvmTMB_ordination_uncertainty"}:
\itemize{
\item \code{scores} -- the \verb{n x K} score matrix (identical to
\code{getLV(fit, level)}, native \code{rotate = "none"} orientation).
\item \code{se} -- an \verb{n x K} matrix of per-(unit, axis) standard
deviations; \code{sqrt(diag(cov[, , s]))} for row \code{s}, and numerically
identical to \code{getLV(fit, level, se = TRUE)$se}.
\item \code{cov} -- a \verb{K x K x n} array; \code{cov[, , s]} is unit \code{s}'s
latent-score covariance matrix, dimnamed by axis on the first two
margins and by unit on the third.
\item \code{level} -- the canonical level name (\code{"unit"} / \code{"unit_obs"}).
}
\code{se} and \code{cov} are filled with \code{NA} (with a warning) when the fit's
Hessian is not positive-definite, matching \code{getLV(se = TRUE)}.
}
\description{
\code{getLV(se = TRUE)} reports a standard error for every latent-score cell,
one axis at a time. \code{ordination_uncertainty()} reports the full covariance
of a unit's score VECTOR across its axes -- the object needed to draw an
uncertainty ellipse (rather than an axis-aligned error bar) around a site
in an ordination biplot.
}
\section{Estimand (read this before using the output)}{

Latent scores are random effects, not fixed parameters. What this
function returns is the model's best guess at how far each site's score
could plausibly have landed given (a) the data actually observed at that
site and (b) the package's uncertainty about the fitted parameters
(loadings, dispersion, etc.) -- \strong{not} a sampling-distribution standard
error of a fixed number, the way \code{std.error} on a regression coefficient
is. Two model fits refit on two different datasets would not be expected
to recover this quantity "on average" the way a coefficient SE is; it is
closer to a prediction interval than a confidence interval. Practically:
it answers "how sure is the model about THIS site's position on the
ordination plot," not "how much would the whole ordination move if I
recollected the data."

Concretely, it is the covariance of \eqn{\mathbf u_s} (site \eqn{s}'s
latent score vector) given the data and the fitted model, read off the
fit's joint (fixed effects + random effects) precision matrix
(\code{TMB::sdreport(getJointPrecision = TRUE)}), so it propagates the
package's uncertainty about the loadings and other fixed parameters into
the score covariance, not just the Laplace curvature at the fixed-effect
point estimate.
}

\section{Rotation / identifiability}{

Scores are reported in gllvmTMB's native fitting orientation
(\code{rotate = "none"}) only. That orientation is pinned by a structural
lower-triangular constraint on the loading matrix, which fixes the axes
up to a per-axis SIGN FLIP (a discrete choice), not a continuous
rotation. Per-axis variance (\code{se}) is unaffected by that sign choice.
The cross-axis covariance in \code{cov} is the one that belongs with the
returned \code{scores} from THIS fit, so an ellipse built from them is
correctly oriented for this fit's own biplot -- but do not compare the
\emph{sign} of a cross-axis entry, or of an axis, across independent (re)fits;
that sign is this fit's own arbitrary convention, exactly as in ordinary
PCA or factor analysis. Rotated scores (\code{getLV(..., rotate = "varimax")}
/ \code{"promax"}) are not supported -- the covariance's transform under a
post-hoc rotation is not implemented, matching \code{getLV(se = TRUE)}'s
existing \code{rotate = "none"}-only restriction.
}

\section{What this is not}{

A Wald quantity read from asymptotic (delta-method / Laplace) theory, not
a resampled, profiled, or simulation-based interval. Its repeated
-sampling coverage has not been measured for this function; \code{se} and
\code{cov} are reported as-is, with no \code{lower}/\code{upper} bound, so that no
unmeasured coverage claim is implied by the return shape (the same
choice \code{getLV(se = TRUE)} already makes). A confidence ellipse built from
\code{cov} at some nominal level (e.g. via \code{stats::qchisq(level, df = 2)}) is
a standard asymptotic-normal construction, not a certified interval.
}

\examples{
\dontrun{
u <- ordination_uncertainty(fit, level = "unit")
u$se           # same numbers as getLV(fit, "unit", se = TRUE)$se
u$cov[, , 1]   # site 1's 2x2 (or dxd) score covariance
}
}
\seealso{
\code{\link[=getLV]{getLV()}} for the marginal per-cell standard error this
function generalises to a full per-unit covariance;
\code{\link[=ordiplot]{ordiplot()}}'s \code{ellipse} argument, which draws the covariance this
function returns.
}
