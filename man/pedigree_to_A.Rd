% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animal-keyword.R
\name{pedigree_to_A}
\alias{pedigree_to_A}
\title{Pedigree to additive-genetic relatedness matrix \code{A}}
\usage{
pedigree_to_A(pedigree)
}
\arguments{
\item{pedigree}{A data frame with one row per individual and
columns identifying the individual, sire, and dam. \strong{Column
names are resolved BY NAME} (MCMCglmm-style) using these
synonyms:
\itemize{
\item Individual ID column: \code{id} or \code{animal}.
\item Sire (father) column: \code{sire} or \code{father}.
\item Dam (mother) column: \code{dam} or \code{mother}.
}
If none of the named synonyms is present, the function falls
back to \strong{positional} access — column 1 = id, column 2 =
sire, column 3 = dam — with a soft note. Unknown parents
encoded as \code{NA} or \code{0} (both treated as missing).}
}
\value{
Dense numeric matrix \eqn{n \times n} with \code{rownames} /
\code{colnames} equal to the individual IDs.
}
\description{
Computes the dense numerator-relationship matrix \eqn{\mathbf A}
from a 3-column pedigree (\code{id}, \code{sire}, \code{dam}) using Henderson's
(1976) recursive formula:
}
\details{
\itemize{
\item \eqn{A_{ii} = 1 + F_i}, where \eqn{F_i} is the inbreeding
coefficient of individual \eqn{i}.
\item \eqn{A_{ij} = \tfrac{1}{2}(A_{i,\text{sire}(j)} + A_{i,\text{dam}(j)})}
for \eqn{j} younger than \eqn{i} (pedigree is sorted oldest
first).
}

Founder individuals (both parents unknown) are assumed unrelated
and non-inbred: \eqn{A_{founder, founder} = 1}.

Users typically don't call \code{pedigree_to_A()} directly — pass
\code{pedigree = ped} to an \code{\link[=animal_scalar]{animal_scalar()}} / \code{\link[=animal_unique]{animal_unique()}} /
\code{\link[=animal_latent]{animal_latent()}} / \code{\link[=animal_indep]{animal_indep()}} / \code{\link[=animal_dep]{animal_dep()}} keyword in
the formula, and the keyword's parser handles the conversion
internally. This function is exported as a public helper for users
who want the matrix for their own diagnostics (e.g. inspection of
inbreeding coefficients or pre-computation for repeated fits).

For very large pedigrees (\eqn{n > 5000}) the cubic-time cost of
Henderson recursion becomes noticeable; a sparse-\code{Ainv} direct
engine path is a v0.3.0 follow-up. For comparison against
established implementations, see \code{nadiv::makeAinv()} (sparse
\eqn{\mathbf A^{-1}}).
}
\seealso{
\code{\link[=animal_scalar]{animal_scalar()}} and siblings.
}
