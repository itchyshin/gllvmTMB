% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/column-coef-foundation.R
\name{phylo_coef}
\alias{phylo_coef}
\title{Phylogenetic response-column coefficients}
\usage{
phylo_coef(formula, tree = NULL, vcv = NULL, rho = NULL)
}
\arguments{
\item{formula}{A coefficient-basis bar expression such as
\code{1 + x | trait}, \code{0 + x | trait}, or \code{1 + x || trait}.}

\item{tree}{An \code{ape::phylo} tree whose tip labels match the response columns.
Mutually exclusive with \code{vcv}.}

\item{vcv}{A labelled positive-definite covariance matrix, or a labelled
sparse precision matrix, for the response columns. Mutually exclusive
with \code{tree}.}

\item{rho}{\code{NULL} to estimate an interior phylogenetic mixture, or one
numeric value in \verb{[0, 1]} to fix it.}
}
\value{
A formula marker; never evaluated directly.
}
\description{
Fit response-column-specific random intercepts and/or slopes with source
covariance \code{K_rho = rho * K + (1 - rho) * diag(diag(K))}. Supply a numeric
\code{rho} in \verb{[0, 1]} to fix the mixture or use \code{rho = NULL} (the default) to
estimate one interior value. The fitted covariance across the coefficient
basis is full for \code{|} and diagonal for \code{||}.
}
\details{
This point-model route is covered for Gaussian multivariate data in long or
\code{traits(...)} wide form, with a labelled positive-definite tree covariance
source and bare numeric row predictors. Numeric \code{rho} and one estimated
interior \code{rho} are supported. Interval inference, non-Gaussian coefficient
models, and animal, kernel, or spatial coefficient helpers remain planned.
Existing \code{phylo_slope()} remains current and warning-free.

For exact compatibility with the released slope engine, a no-intercept
dense-\code{vcv} fit with \code{rho = 1} uses the existing \code{\link[=phylo_slope]{phylo_slope()}}
conditioning seam, \verb{K + 1e-8 I}. Tree sources use their released sparse
precision. Interior fixed \code{rho}, estimated \code{rho}, and intercept-bearing
\code{rho = 1} fits use the raw covariance-scale mixture shown above.
Estimating \code{rho} requires genuine between-column correlation contrast in
the standardized source; a diagonal source cannot identify the mixture and
is rejected. Supply exactly one of \code{tree} or \code{vcv}, never both.
}
\examples{
set.seed(2)
dat <- expand.grid(unit = factor(1:12), trait = factor(paste0("sp", 1:4)))
dat$x <- rnorm(12)[dat$unit]
dat$value <- rnorm(nrow(dat))
K <- diag(4); dimnames(K) <- list(levels(dat$trait), levels(dat$trait))
fit <- gllvmTMB(value ~ 1 + phylo_coef(0 + x | trait, vcv = K, rho = 0.5),
  data = dat, trait = "trait", unit = "unit", family = gaussian(),
  control = gllvmTMBcontrol(se = FALSE), silent = TRUE)
extract_Sigma(fit, level = "column_coef")
}
\seealso{
\code{\link[=column_coef]{column_coef()}}, \code{\link[=phylo_slope]{phylo_slope()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}
}
