% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{phylo_indep}
\alias{phylo_indep}
\title{Per-trait phylogenetic marginal variance: \code{phylo_indep(0 + trait | species)}}
\usage{
phylo_indep(
  formula,
  tree = NULL,
  vcv = NULL,
  A = NULL,
  Ainv = NULL,
  common = FALSE,
  rho = 1
)
}
\arguments{
\item{formula}{Intercept-only \code{0 + trait | species}, the supported
intercept-and-slope form described above, or the Gaussian long-format
slope-only response-column form \code{0 + x1 + ... | trait}.}

\item{tree}{An \code{ape::phylo} object. \strong{Canonical.}}

\item{vcv}{A tip-only phylogenetic correlation matrix
(\verb{n_species x n_species}) for the ordinary forms. For the slope-only
response-column form it is instead \verb{n_traits x n_traits}, with row names
matching the resolved RHS \code{trait} levels. Legacy alias of \verb{A =}.}

\item{A}{Tip-level relatedness matrix (\verb{n_species x n_species}) for the
ordinary forms; for the slope-only response-column form, the corresponding
trait-level matrix. Alias of \verb{vcv =}, aligned with the \verb{animal_*} family's
argument naming.}

\item{Ainv}{Sparse precision matrix (inverse of \code{A}).}

\item{common}{\code{FALSE} (default) for a separate phylogenetic variance per
trait; \code{TRUE} ties all traits to one shared phylogenetic variance
(intercept-only). \code{phylo_indep(0 + trait | species, common = TRUE)} is the
canonical one-shared-variance spelling and fits the same model as the
soft-deprecated \code{phylo_scalar(species)}.}

\item{rho}{Source strength: a number in \verb{[0,1]} fixes
\code{rho * K + (1-rho) * diag(diag(K))} on the legacy-resolved source scale.
Omitted or explicit \code{1} preserves the existing model. \code{NULL} estimates
strength for one Gaussian structured trait-intercept block with complete
replicated multivariate observations and no competing ordinary covariance.
Estimated latent terms require rank one and at least four traits. The same
strength applies to the entire latent-plus-Psi covariance. This parameter
is not a variance-share summary. Fixed attenuation and the admitted Gaussian
estimator have implementation checks; recovery is regime-specific.}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for \strong{T per-trait phylogenetic variances coupled by
the phylo correlation matrix \eqn{\mathbf A}}; standalone =
T univariate phylogenetic mixed models stacked.
}
\details{
Each trait \eqn{t} gets its own variance
\eqn{\sigma^2_{\text{phy},t}} on the same phylogenetic correlation
matrix \eqn{\mathbf A_{\text{phy}}}; trait-specific random vectors
are otherwise independent.

\deqn{\mathbf p_t \sim \mathcal{N}(\mathbf 0,\, \sigma^2_{\text{phy},t}\,\mathbf A_{\text{phy}}), \qquad t = 1, \dots, T.}

Use \code{phylo_indep()} for an explicit marginal-only phylogenetic fit
(no cross-trait phylogenetic decomposition). Use
\code{phylo_latent(..., unique = TRUE)} for the paired
phylogenetic decomposition
\eqn{\boldsymbol\Sigma_{\text{phy}} = \boldsymbol\Lambda_{\text{phy}}\boldsymbol\Lambda_{\text{phy}}^\top + \boldsymbol\Psi_{\text{phy}}}.
\subsection{Mutual exclusion with \code{phylo_latent()}}{

Combining \code{phylo_indep(0 + trait | species)} with
\code{phylo_latent(species, d = K)} is \strong{over-parameterised} and the
parser raises a \code{cli::cli_abort()}.
}

\subsection{Intercept-and-slope form}{

A single-covariate random regression uses
\code{phylo_indep(1 + x | species)} in wide syntax or
\code{phylo_indep(0 + trait + (0 + trait):x | species)} in explicit long syntax.
It estimates independent trait-specific intercept--slope blocks. The
intercept--slope correlation is estimated within each trait; cross-trait
blocks are structurally zero.
}

\subsection{Slope-only response-column form}{

For a long-format Gaussian model, \code{phylo_indep(0 + lat + temp | trait)}
adds response-column deviations to the fixed slopes without adding a random
intercept. The phylogeny is indexed by the response-column factor on the
right of \code{|}; \code{indep} makes the covariance among \code{lat} and \code{temp} slopes
diagonal. With slope coefficient matrix \eqn{\mathbf B} (rows are
response columns and columns are predictors), let
\eqn{\mathbf b=\mathrm{vec}(\mathbf B^\mathsf{T})} order coefficients by
response column: \code{lat}, \code{temp} for column 1, then \code{lat}, \code{temp} for column
2, and so on. Then
\eqn{\mathrm{Cov}(\mathbf b) = \mathbf A_{\mathrm{phy}}
\otimes \mathrm{diag}(\sigma^2_{\mathrm{lat}},
\sigma^2_{\mathrm{temp}})}. (The native implementation stores the
permutation-equivalent column-major vector.) Retrieve that predictor-basis covariance with
\code{extract_Sigma(fit, level = "column_slope")}. It is not a trait-level
\eqn{\boldsymbol\Sigma}. This V1 route requires bare finite numeric
predictors, exactly the resolved \code{trait} column on the RHS, and Gaussian
responses; wide-format and non-Gaussian column slopes are planned work.

Pass the phylogeny via \code{tree = phylo} (canonical, sparse \eqn{\mathbf{A}^{-1}}) or
\code{vcv = Cphy} (\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#superseded}{\figure{lifecycle-superseded.svg}{options: alt='[Superseded]'}}}{\strong{[Superseded]}}, dense). See
\code{\link[=phylo_latent]{phylo_latent()}} for the full discussion of the two paths.
}
}
\examples{
\dontrun{
library(ape)
tree <- rcoal(20); tree$tip.label <- paste0("sp", 1:20)
fit <- gllvmTMB(value ~ 0 + trait +
                  phylo_indep(0 + trait | species, tree = tree),
                data    = df,
                trait   = "trait",
                unit    = "species",
                cluster = "species")
}
}
\references{
\itemize{
\item \strong{Williams et al.} (2025) Phylogenetic generalised linear mixed
models for multi-trait comparative analyses. \emph{bioRxiv}
2025.12.20.695312. The marginal \code{phylo_indep + indep} standalone
form stacked across traits matches their PGLMM Eq. 3.
}
}
\seealso{
\code{\link[=phylo_latent]{phylo_latent()}}, \code{\link[=phylo_dep]{phylo_dep()}}, \code{\link[=indep]{indep()}},
\code{\link[=spatial_indep]{spatial_indep()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}.
}
