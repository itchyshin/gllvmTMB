% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{phylo_latent}
\alias{phylo_latent}
\title{Reduced-rank phylogenetic latent factors: \code{phylo_latent(species, d = K)}}
\usage{
phylo_latent(species, d = 1, tree = NULL, vcv = NULL, A = NULL, Ainv = NULL)
}
\arguments{
\item{species}{Unquoted column name for the species factor.}

\item{d}{Integer; number of phylogenetic latent factors.}

\item{tree}{An \code{ape::phylo} object. \strong{Canonical.} Use this if
you have a tree.}

\item{vcv}{A tip-only phylogenetic correlation matrix
(\verb{n_species x n_species}). Legacy alias of \verb{A =}.}

\item{A}{Tip-level relatedness matrix (\verb{n_species x n_species})
-- alias of \verb{vcv =}, aligned with the \verb{animal_*} family's
argument naming (M2.8b, 2026-05-17). Supply one of \code{tree},
\code{vcv}, or \code{A} / \code{Ainv}.}

\item{Ainv}{Sparse precision matrix (inverse of \code{A}). Densified
via \code{solve()} internally for v0.2.0; sparse direct engine
path is a v0.3.0 follow-up.}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for the reduced-rank phylogenetic random effect.
Formerly \code{phylo_rr(species, d = K)} -- same engine, new name.
}
\details{
\subsection{Two phylogeny inputs: \verb{tree =} (canonical) and \verb{vcv =} (legacy)}{

Pass the phylogeny inside the keyword via one of two arguments:
\itemize{
\item \strong{\code{tree = phylo}} (canonical, recommended) -- the full
\code{ape::phylo} object. Triggers the sparse \eqn{\mathbf{A}^{-1}}
path (Hadfield & Nakagawa 2010, appendix eqs. 26-29) which
scales linearly in \code{n_species}. About 24x faster than the dense
path at \code{n_species = 1000} on the published benchmark.
\item \strong{\code{vcv = Cphy}} (\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#superseded}{\figure{lifecycle-superseded.svg}{options: alt='[Superseded]'}}}{\strong{[Superseded]}}) -- a
tip-only \verb{n_species x n_species} correlation matrix. Forces the
dense O(n^2) memory / O(n^3) Cholesky path. Provided for users
who have a Cphy in hand and no Newick tree (e.g. comparing
against \code{nlme::corPagel}); soft deprecation in 0.2.0.
}

Both arguments give the \strong{same MLE} -- they differ only in
computational cost. Internal-node ancestral states are estimated
as latent random effects on the sparse path; on the dense path
only tip-level loadings are estimated.

See \code{vignette("phylogenetic-gllvm")} for the benchmark and
\code{dev/design/01-phylo-api-canonical-tree.md} for the technical
justification.
}
}
\examples{
\dontrun{
  tree <- ape::rcoal(20); tree$tip.label <- paste0("sp", seq_len(20))
  sim <- simulate_site_trait(
    n_sites = 1, n_species = 20, n_traits = 4,
    mean_species_per_site = 20,
    Cphy = ape::vcv(tree, corr = TRUE),
    sigma2_phy = rep(0.3, 4), seed = 1
  )
  sim$data$species <- factor(sim$data$species, levels = tree$tip.label)
  fit <- gllvmTMB(
    value ~ 0 + trait + phylo_latent(species, d = 2),
    data       = sim$data,
    trait      = "trait",
    unit       = "species",
    cluster    = "species",
    phylo_tree = tree
  )
}
}
\references{
Hadfield JD, Nakagawa S (2010). General quantitative
genetic methods for comparative biology: phylogenies, taxonomies
and multi-trait models for continuous and categorical characters.
\emph{J. Evol. Biol.} 23: 494-508. \doi{10.1111/j.1420-9101.2009.01915.x}
}
\seealso{
\code{\link[=phylo_scalar]{phylo_scalar()}}, \code{\link[=phylo_unique]{phylo_unique()}}, \code{\link[=phylo_indep]{phylo_indep()}},
\code{\link[=phylo_dep]{phylo_dep()}}, \code{\link[=phylo_rr]{phylo_rr()}} (deprecated alias).
}
