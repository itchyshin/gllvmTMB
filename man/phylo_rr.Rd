% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{phylo_rr}
\alias{phylo_rr}
\title{Reduced-rank phylogenetic random effect (PGLLVM)}
\usage{
phylo_rr(species, d = 1)
}
\arguments{
\item{species}{An unquoted column name giving the species factor.
Levels must match the rownames of \code{phylo_vcv} passed to
\code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{d}{Integer; the number of phylogenetic latent factors.}
}
\value{
A formula marker; never evaluated.
}
\description{
Adds a reduced-rank phylogenetic random effect on the species
dimension. For each of \code{d} phylogenetic latent factors, the
species-level scores are drawn from a multivariate normal with
the user-supplied phylogenetic correlation matrix; the factors
are then linearly combined into trait-specific contributions via
a lower-triangular \verb{n_traits x d} loading matrix. Mathematically:
}
\details{
\deqn{p_{it} = \sum_{k=1}^{d} \Lambda_{\mathrm{phy},tk}\, g_{ik},
      \qquad g_{\cdot k} \sim \mathcal{N}(\mathbf{0}, \mathbf{A}_{\mathrm{phy}}).}

This is the model of Nakagawa et al. (\emph{in prep}).
When the user supplies \code{phylo_tree} (an \code{ape::phylo} object), the
implementation builds the sparse inverse \eqn{\mathbf{A}_{\mathrm{phy}}^{-1}}
over tips + internal nodes via \code{MCMCglmm::inverseA(tree)} and
evaluates the prior through the quadratic form
\eqn{g^\top \mathbf{A}_{\mathrm{phy}}^{-1} g}, exploiting tree-topology
sparsity (Hadfield & Nakagawa 2010 \emph{Journal of Evolutionary
Biology} 23:494-508; Hadfield 2010 \emph{Journal of Statistical Software}
33(2):1-22). When the user supplies only \code{phylo_vcv} (the dense
tip-only covariance matrix), the package falls back to inverting it
densely via \code{Matrix::solve()} -- correct, but does not exploit
sparsity.

Used inside a \code{gllvmTMB()} formula:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# recommended: pass tree
gllvmTMB(value ~ 0 + trait + phylo_rr(species, d = 2),
         data       = df,
         trait      = "trait",
         unit       = "species",
         cluster    = "species",
         phylo_tree = tree)

# legacy: pass dense covariance only
gllvmTMB(value ~ 0 + trait + phylo_rr(species, d = 2),
         data      = df,
         trait     = "trait",
         unit      = "species",
         cluster   = "species",
         phylo_vcv = Cphy)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
library(ape)
tree   <- rcoal(20); tree$tip.label <- paste0("sp", 1:20)
Cphy   <- vcv(tree, corr = TRUE)
fit <- gllvmTMB(value ~ 0 + trait + phylo_rr(species, d = 2),
                data      = df,
                trait     = "trait",
                unit      = "species",
                cluster   = "species",
                phylo_vcv = Cphy)
}
}
