% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-predictor.R
\name{phylo_signal_mi}
\alias{phylo_signal_mi}
\title{Phylogenetic-signal diagnostic for a modelled missing predictor}
\usage{
phylo_signal_mi(object, variable = NULL, threshold = 0.1, warn = FALSE)
}
\arguments{
\item{object}{A \code{gllvmTMB} fit with a phylogenetic missing-predictor model.}

\item{variable}{Optional missing-predictor name; defaults to the only
modelled missing predictor.}

\item{threshold}{Effective-lambda threshold below which the signal is flagged
as weak (default \code{0.1}).}

\item{warn}{Logical; when \code{TRUE}, warn against over-interpreting conditional
modes when phylogenetic signal is weak. Default \code{FALSE} (the function
returns the statistic silently).}
}
\value{
A list with \code{variable}, \code{lambda} (effective Pagel lambda), \code{sd_x},
\code{sigma_x}, \code{weak} (logical), and \code{threshold}.
}
\description{
For a \code{\link[=gllvmTMB]{gllvmTMB()}} fit with a phylogenetic missing-predictor covariate model
(\code{mi(x)} with \code{impute = list(x = x ~ ... + phylo(1 | species, tree = tree))}),
\code{phylo_signal_mi()} reports an effective phylogenetic-signal statistic for the
covariate model and flags the weak-signal case. Phylogenetic imputation helps
when the signal in \code{x} is strong and degrades toward the independent
(no-borrowing) model when it is weak; forcing a phylogenetic prior on a
phylogenetically-unstructured trait adds noise rather than information
(Penone et al. 2014 \emph{Methods Ecol. Evol.} 5:961-970; Molina-Venegas 2024
\emph{Methods Ecol. Evol.}; Goolsby, Bruggeman & Ane 2017 \emph{Methods Ecol. Evol.}
8:22-27, \code{Rphylopars}).
}
\details{
The statistic is an effective Pagel's lambda for the covariate,
\code{lambda = sd_x^2 / (sd_x^2 + sigma_x^2)}, the fraction of species-level
variance in \code{x} explained by the phylogenetic field (\code{sd_x}) relative to the
i.i.d. residual (\code{sigma_x}) -- the Pagel partition of the covariate model.
\code{lambda} near 1 = strong signal (informative borrowing); \code{lambda} near 0 =
weak signal (the covariate model is approximately equivalent to an
independent model). This is a verification / interpretation aid, not part of
the fit; it does not change estimates.
}
\seealso{
\code{\link[=imputed]{imputed()}} for conditional modes; \code{\link[=gllvmTMB]{gllvmTMB()}} for the \verb{impute =} phylo
covariate model.
}
