% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{phylo_unique}
\alias{phylo_unique}
\title{Per-trait independent phylogenetic random intercepts: \code{phylo_unique(species)}}
\usage{
phylo_unique(species, tree = NULL, vcv = NULL)
}
\arguments{
\item{species}{Unquoted column name for the species factor.}

\item{tree}{An \code{ape::phylo} object. \strong{Canonical.}}

\item{vcv}{A tip-only phylogenetic correlation matrix
(\verb{n_species x n_species}). Legacy / superseded.}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for the \strong{D independent} phylogenetic random
intercepts. Each trait \eqn{t} gets its own variance
\eqn{\sigma^{2}_{\text{phy},t}} on the same phylogenetic correlation
matrix \eqn{\mathbf{C}_{\text{phy}}}; the per-trait random vectors are
otherwise independent. Mathematically:
}
\details{
\deqn{\mathbf p_t \sim \mathcal{N}(\mathbf{0},\, \sigma^{2}_{\text{phy},t}\,\mathbf{C}_{\text{phy}}),\qquad t = 1,\dots,T,}

giving a phylogenetic counterpart of \code{\link[=unique]{unique()}} -- one independent
variance per trait, all sharing the same phylogenetic precision.

Compare to \code{\link[=phylo_scalar]{phylo_scalar()}} (one shared variance), which collapses
the T variances to a single \eqn{\sigma^{2}_{\text{phy}}}; and
\code{\link[=phylo_latent]{phylo_latent()}} (K factors), which induces \emph{across-trait}
correlation through a low-rank loading matrix.
\subsection{Two co-fitting modes}{

\code{phylo_unique()} has two complementary modes:

\describe{
\item{\strong{Alone} (legacy)}{When written without a paired
\code{phylo_latent(species, d = K)}, the engine implements the term
internally as \code{phylo_latent(species, d = T)} with a diagonal
\eqn{\boldsymbol\Lambda_{\text{phy}}}. The diagonal entries of
\eqn{\boldsymbol\Lambda_{\text{phy}}} are the per-trait
phylogenetic SDs. This path stays for backward compatibility.}
\item{\strong{Paired with \code{phylo_latent()}} (two-U PGLLVM, recommended)}{
When written together with \code{phylo_latent(species, d = K)}, the
two terms co-fit as \strong{separate covariance components}:
\deqn{\boldsymbol\Sigma_\text{phy} \;=\; \underbrace{\boldsymbol\Lambda_\text{phy}\boldsymbol\Lambda_\text{phy}^{\!\top}}_{\text{shared (rank K)}} \;+\; \underbrace{\boldsymbol\Psi_\text{phy}}_{\text{per-trait unique}}.}
\eqn{\boldsymbol\Lambda_\text{phy}} is filled by the rank-K shared
latent factors; \eqn{\boldsymbol\Psi_\text{phy}} carries
the per-trait phylogenetic variances not absorbed by the K shared
axes. This is the manuscript-aligned PGLLVM decomposition
(Hadfield & Nakagawa 2010; Meyer & Kirkpatrick 2008; Halliwell et
al. 2025). Replication (multiple sites per species) is required
to break the Psi_phy / Psi_non confound at the species level.}
}

Use \code{\link[=extract_Sigma]{extract_Sigma()}} with \code{level = "phy"} and \code{part = "shared"},
\code{"unique"}, or \code{"total"} to pull each component, the diagonal psi_phy,
or their sum.

Pass the phylogeny via \code{tree = phylo} (canonical, sparse \eqn{\mathbf{A}^{-1}}) or
\code{vcv = Cphy} (\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#superseded}{\figure{lifecycle-superseded.svg}{options: alt='[Superseded]'}}}{\strong{[Superseded]}}, dense). See
\code{\link[=phylo_latent]{phylo_latent()}} for the full discussion of the two paths.
}
}
\examples{
\dontrun{
  tree <- ape::rcoal(8); tree$tip.label <- paste0("sp", seq_len(8))
  sim <- simulate_site_trait(
    n_sites = 1, n_species = 8, n_traits = 3,
    mean_species_per_site = 8,
    Cphy = ape::vcv(tree, corr = TRUE),
    sigma2_phy = rep(0.3, 3), seed = 1
  )
  sim$data$species <- factor(sim$data$species, levels = tree$tip.label)
  fit <- gllvmTMB(
    value ~ 0 + trait + phylo_unique(species),
    data = sim$data, phylo_tree = tree
  )
}
}
\references{
\itemize{
\item \strong{Hadfield, J. D. & Nakagawa, S.} (2010) General quantitative
genetic methods for comparative biology: phylogenies, taxonomies and
multi-trait models for continuous and categorical characters.
\emph{Journal of Evolutionary Biology} 23(3): 494-508.
\doi{10.1111/j.1420-9101.2009.01915.x}
\item \strong{Meyer, K. & Kirkpatrick, M.} (2008) Perils of parsimony:
properties of reduced-rank estimates of genetic covariance matrices.
\emph{Genetics} 178(4): 2223-2240.
}
}
\seealso{
\code{\link[=phylo_scalar]{phylo_scalar()}}, \code{\link[=phylo_latent]{phylo_latent()}}, \code{\link[=phylo_indep]{phylo_indep()}},
\code{\link[=phylo_dep]{phylo_dep()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}.
}
