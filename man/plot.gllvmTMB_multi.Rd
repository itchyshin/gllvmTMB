% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-gllvmTMB.R
\name{plot.gllvmTMB_multi}
\alias{plot.gllvmTMB_multi}
\title{Plot a fitted multivariate gllvmTMB model}
\usage{
\method{plot}{gllvmTMB_multi}(
  x,
  type = c("correlation", "correlation_ellipse", "loadings", "integration",
    "communality", "variance", "ordination"),
  level = c("unit", "unit_obs"),
  boot = NULL,
  axes = c(1L, 2L),
  rotation = c("varimax", "none", "promax"),
  order_axes = TRUE,
  sign_anchor = c("auto", "none"),
  anchor_traits = NULL,
  standardize_loadings = FALSE,
  ...
)
}
\arguments{
\item{x}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{type}{One of \code{"correlation"}, \code{"correlation_ellipse"}, \code{"loadings"},
\code{"integration"}, \code{"communality"}, \code{"variance"}, \code{"ordination"}.}

\item{level}{\code{"unit"} (between-unit) or \code{"unit_obs"} (within-unit).
Legacy aliases \code{"B"} and \code{"W"} are accepted with a deprecation warning.
Used by \code{"loadings"} (which level to plot; the default
\code{c("unit", "unit_obs")} means "both available levels, faceted
side-by-side"; pass a length-1 string to plot one tier) and
\code{"ordination"} (single level required; omitted \code{level} defaults to
\code{"unit"}). Ignored for \code{"correlation"} (which always shows both if
available), \code{"integration"}, and \code{"variance"}.

\emph{Note}: the default \code{level = c("unit", "unit_obs")} is intentionally a
length-2 vector, not the usual \code{match.arg} shortcut. The dispatcher
does \strong{not} call \code{match.arg(level)} itself; each helper inspects
\code{level} and decides whether to plot one tier or both. If you copy
one of these helpers into your own code, mirror that pattern rather
than reflexively calling \code{match.arg(level)} (which would silently
collapse the default to \code{"unit"} and drop the \code{unit_obs} panel).}

\item{boot}{Optional bootstrap object. This can be either a
\code{bootstrap_Sigma()} result or a list with elements \code{repeatability},
\code{communality_B}, \code{communality_W}, each a data frame with columns
\code{trait}, \code{lower}, \code{upper}. A \code{bootstrap_Sigma()} object can add
correlation intervals to \code{"correlation"} / \code{"correlation_ellipse"},
whiskers to \code{"integration"}, and \code{c^2} boundary intervals to
\code{"communality"}. Default \code{NULL} skips interval overlays.}

\item{axes}{Length-2 or length-3 integer vector for \code{"ordination"} when
\code{d >= 2}. Length 2 draws a single biplot. Length 3 draws a static
pair-grid of the three axis pairs. For \code{d = 3}, the default
\code{c(1, 2)} is promoted to \code{c(1, 2, 3)} so all three axes are visible.
Ignored when \code{d = 1}.}

\item{rotation}{One of \code{"varimax"}, \code{"none"}, or \code{"promax"} for
\code{"ordination"} plots. The default \code{"varimax"} uses rotated,
shared-variance-ordered, sign-anchored axes for interpretation.
Use \code{"none"} to show the raw computational orientation. Rotation
makes the biplot easier to label; use \code{Sigma}, correlations,
communality, and uniqueness as the primary quantitative summaries.}

\item{order_axes}{Logical. For rotated \code{"ordination"} plots, reorder
latent axes by decreasing shared variance after rotation. Default
\code{TRUE}. Ignored when \code{rotation = "none"}.}

\item{sign_anchor}{One of \code{"auto"} or \code{"none"}. For rotated
\code{"ordination"} plots, \code{"auto"} flips each axis so its anchor trait has
a positive loading. Default \code{"auto"}. Ignored when \code{rotation = "none"}.}

\item{anchor_traits}{Optional character vector of trait names used for
sign anchoring in rotated \code{"ordination"} plots. Supply one trait per
axis after any \code{order_axes} step. Missing axes fall back to the trait
with the largest absolute loading. Ignored when \code{rotation = "none"}.}

\item{standardize_loadings}{Logical. For \code{"ordination"} plots, divide
each trait loading by the square root of that trait's model-implied total
variance before drawing arrows. This puts arrows on a correlation-like
scale for mixed-scale traits. It changes the displayed arrow scale, not
the model's communality or variance decomposition. Default \code{FALSE}.}

\item{...}{Currently unused.}
}
\value{
A \code{ggplot} object with a \code{gllvmTMB_meta} attribute describing
the plot type, source extractor, covariance level, interval status, and
rotation status. Metadata also carries extractor notes when a plotted
summary has an important caveat. Plots also carry a \code{gllvmTMB_data}
attribute with the prepared plotting data; ordination stores separate
\code{scores} and \code{loadings} tables.
}
\description{
Produces a variety of \code{ggplot2} visualisations for a stacked-trait
multivariate GLLVM. Dispatches on \code{type} to one of seven panels:
}
\details{
\describe{
\item{\code{"correlation"}}{Combined heatmap of trait correlations.
Upper triangle = between-unit correlations (\code{level = "unit"}),
lower triangle = within-unit correlations (\code{level = "unit_obs"}),
diagonal = 1. Falls back to whichever level is present if the
other tier is absent. Optional \code{boot} intervals are carried in the
plot data when supplied.}
\item{\code{"correlation_ellipse"}}{Ellipse matrix of trait correlations.
Ellipse direction and eccentricity encode the sign and strength of
the correlation. This is the Figure-3-style alternative to the tile
heatmap. Optional \code{boot} intervals mark correlations whose interval
does not cross zero with black borders and stars. These bootstrap
intervals are recovery-grade, and their coverage properties are not
yet established: read a marked cell as "the interval excludes zero
at the fitted point", not as a calibrated significance test.}
\item{\code{"loadings"}}{Tile heatmap of \code{Lambda_B} (and \code{Lambda_W} if
present), faceted by level. Rows = traits, columns = factors.
Pinned cells (from \code{lambda_constraint}) are drawn with a heavy
outline.}
\item{\code{"integration"}}{Dot-and-whisker plot of repeatability (ICC),
between-tier communality and within-tier communality per trait,
sorted by repeatability. Optional whiskers from a \code{boot} object
(skipped if \code{boot = NULL}).}
\item{\code{"communality"}}{Figure-3-style stacked bars of per-trait
communality (\code{c^2}, shared latent proportion) and uniqueness
(\code{1 - c^2}) for the available latent tiers. Optional \code{boot}
intervals are drawn on the \code{c^2} boundary when supplied.}
\item{\code{"variance"}}{Stacked-bar variance partition per trait, using
\code{extract_proportions(format = "long")}. One bar per trait,
stacks summing to 1.}
\item{\code{"ordination"}}{Dimension-aware latent-score ordination.
1D fits (\code{d = 1}) get a horizontal score strip with trait loading
lollipops; 2D fits get a standard biplot; 3D fits get a static
pair-grid biplot for the three axis pairs. For \code{d > 3}, pick two
or three axes via \code{axes}.}
}
}
