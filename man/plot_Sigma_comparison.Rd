% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-covariance-tables.R
\name{plot_Sigma_comparison}
\alias{plot_Sigma_comparison}
\title{Plot Sigma-table estimates against a known truth matrix}
\usage{
plot_Sigma_comparison(
  x,
  truth,
  level = "unit",
  part = c("total", "shared", "unique"),
  measure = c("covariance", "correlation"),
  entries = c("upper", "unique", "all", "offdiag", "lower", "diag"),
  link_residual = c("auto", "none"),
  include_diagonal = FALSE,
  facet = c("level", "comparison", "none"),
  sort = c("abs_error", "error", "estimate", "truth", "trait", "level"),
  style = c("difference", "scatter")
)
}
\arguments{
\item{x}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}, an admitted \code{engine = "julia"}
bridge fit, a data frame returned by \code{\link[=extract_Sigma_table]{extract_Sigma_table()}}, or a data
frame already returned by \code{\link[=compare_Sigma_table]{compare_Sigma_table()}}.}

\item{truth}{Square numeric covariance or correlation matrix passed to
\code{\link[=compare_Sigma_table]{compare_Sigma_table()}}. May be omitted only when \code{x} already contains
\code{truth}, \code{error}, \code{abs_error}, and \code{comparison_status} columns.}

\item{level, part, measure, entries, link_residual}{Passed to
\code{\link[=compare_Sigma_table]{compare_Sigma_table()}} when \code{truth} is supplied.}

\item{include_diagonal}{Logical. Include diagonal rows if they are present?
The default is \code{FALSE} because variances are usually on a different scale
from pairwise covariance/correlation rows.}

\item{facet}{One of \code{"level"} (default), \code{"comparison"}, or \code{"none"}.
Use \code{"comparison"} when precomputed rows contain a \code{comparison} column,
for example to compare two model specifications against the same truth.}

\item{sort}{Row ordering for \code{style = "difference"}: \code{"abs_error"}
(default), \code{"error"}, \code{"estimate"}, \code{"truth"}, \code{"trait"}, or \code{"level"}.}

\item{style}{One of \code{"difference"} (default) or \code{"scatter"}.}
}
\value{
A \code{ggplot2} plot object with \code{gllvmTMB_meta} and \code{gllvmTMB_data}
attributes.
}
\description{
\code{plot_Sigma_comparison()} turns \code{\link[=compare_Sigma_table]{compare_Sigma_table()}} rows into a
row-labelled comparison plot. The default \code{style = "difference"} shows
\code{estimate - truth} for each covariance or correlation row; \code{style = "scatter"} shows estimate versus truth with a one-to-one reference line.
Segments in these plots are comparison residuals, not confidence intervals.
}
\details{
Scope boundary: IN, the helper plots \code{\link[=compare_Sigma_table]{compare_Sigma_table()}} rows or builds
them from a fitted model / Sigma table plus one supplied truth matrix
(EXT-26; built on EXT-25). PARTIAL, it is a visual comparison helper only:
it does not run simulations, compute intervals, or validate calibration.
PLANNED, article-specific simulation summaries and richer calibration plots
remain future work.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  rows <- data.frame(
    level = "unit",
    trait_i = c("length", "length", "mass"),
    trait_j = c("mass", "wing", "wing"),
    estimate = c(0.62, -0.10, 0.28),
    lower = NA_real_,
    upper = NA_real_,
    matrix = "R",
    component = "total",
    diagonal = FALSE,
    triangle = "upper",
    scale = "correlation"
  )
  truth_R <- matrix(
    c(1, 0.60, -0.05, 0.60, 1, 0.20, -0.05, 0.20, 1),
    3,
    byrow = TRUE,
    dimnames = list(
      c("length", "mass", "wing"),
      c("length", "mass", "wing")
    )
  )
  plot_Sigma_comparison(rows, truth_R, measure = "correlation")
}
}
\seealso{
\code{\link[=compare_Sigma_table]{compare_Sigma_table()}}, \code{\link[=plot_Sigma_table]{plot_Sigma_table()}}.
}
