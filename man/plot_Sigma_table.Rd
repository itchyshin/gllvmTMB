% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-covariance-tables.R
\name{plot_Sigma_table}
\alias{plot_Sigma_table}
\title{Plot report-ready Sigma table rows}
\usage{
plot_Sigma_table(
  x,
  level = "unit",
  part = c("total", "shared", "unique"),
  measure = c("covariance", "correlation"),
  entries = c("upper", "unique", "all", "offdiag", "lower", "diag"),
  link_residual = c("auto", "none"),
  include_diagonal = FALSE,
  facet = c("level", "none"),
  sort = c("estimate", "magnitude", "trait", "level"),
  show_intervals = NULL,
  style = c("interval", "eye", "raindrop"),
  eye_level = 0.95,
  raindrop_level = NULL
)
}
\arguments{
\item{x}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}, a \code{bootstrap_Sigma} object, or a data
frame returned by \code{\link[=extract_Sigma_table]{extract_Sigma_table()}}. Data frames must contain \code{level},
\code{trait_i}, \code{trait_j}, \code{estimate}, \code{lower}, \code{upper}, \code{matrix},
\code{component}, \code{diagonal}, and \code{triangle}.}

\item{level, part, measure, entries, link_residual}{Passed to
\code{\link[=extract_Sigma_table]{extract_Sigma_table()}} when \code{x} is a fitted model. The plotting default
\code{entries = "upper"} shows each pairwise covariance/correlation once; pass
\code{entries = "diag"} for variances or \code{entries = "unique"} for the diagonal
plus upper triangle.}

\item{include_diagonal}{Logical. Include diagonal rows if they are present
in \code{x}? The default is \code{FALSE} because covariance diagonals are variances
and are usually on a different interpretive scale than pairwise
covariances.}

\item{facet}{One of \code{"level"} (default) or \code{"none"}.}

\item{sort}{Row ordering: \code{"estimate"} (default), \code{"magnitude"}, \code{"trait"},
or \code{"level"}.}

\item{show_intervals}{Logical or \code{NULL}. If \code{NULL} (default), finite
\code{lower}/\code{upper} bounds are drawn as horizontal intervals for
\code{style = "interval"} and omitted for \code{style = "eye"}. Set \code{TRUE} to
overlay interval lines on confidence eyes. Rows without finite bounds remain
visible as points.}

\item{style}{One of \code{"interval"} (default), \code{"eye"}, or \code{"raindrop"}.
\code{"eye"} draws a confidence eye: a pale frequentist compatibility shape
reconstructed from the estimate and finite interval bounds, plus a hollow,
sign-coloured estimate circle. Correlation rows use Fisher's z scale and
covariance rows use the displayed estimate scale. The shape is not a
posterior density. \code{"raindrop"} is accepted as a compatibility alias.}

\item{eye_level}{Confidence level represented by the supplied interval
bounds when \code{style = "eye"}. Default \code{0.95}.}

\item{raindrop_level}{Compatibility alias for \code{eye_level}.}
}
\value{
A \code{ggplot2} plot object with \code{gllvmTMB_meta} and \code{gllvmTMB_data}
attributes.
}
\description{
\code{plot_Sigma_table()} turns rows from \code{\link[=extract_Sigma_table]{extract_Sigma_table()}} into a
forest-style covariance or correlation plot. It is designed for reports and
articles that need to show selected covariance entries without indexing the
\code{Sigma} matrix by hand. Rows without finite interval bounds are drawn as
open points. To display Sigma uncertainty, pass interval-bearing rows, for
example from a bootstrap workflow; \code{\link[=extract_Sigma_table]{extract_Sigma_table()}} itself currently
leaves interval columns as placeholders.
}
\details{
Scope boundary: IN, the helper plots point-estimate rows from
\code{\link[=extract_Sigma_table]{extract_Sigma_table()}} or extracts those rows from a fit returned by
\code{\link[=gllvmTMB]{gllvmTMB()}} (EXT-19; built on EXT-18). PARTIAL, interval columns are
displayed when present and finite, but this helper does not compute Sigma
intervals. PLANNED, uncertainty propagation for arbitrary Sigma entries
belongs in bootstrap/profile infrastructure.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  sigma_rows <- data.frame(
    level = "unit",
    trait_i = c("length", "length", "mass"),
    trait_j = c("mass", "wing", "wing"),
    estimate = c(0.22, -0.08, 0.15),
    lower = NA_real_,
    upper = NA_real_,
    matrix = "Sigma",
    component = "total",
    diagonal = FALSE,
    triangle = "upper"
  )
  plot_Sigma_table(sigma_rows)
}
}
\seealso{
\code{\link[=extract_Sigma_table]{extract_Sigma_table()}}, \code{\link[=plot_correlations]{plot_correlations()}},
\code{\link[=plot.gllvmTMB_multi]{plot.gllvmTMB_multi()}}.
}
