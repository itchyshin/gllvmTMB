% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-loadings-confidence-eye.R
\name{plot_loadings_confidence_eye}
\alias{plot_loadings_confidence_eye}
\title{Confidence Eye plot for per-species loading uncertainty}
\usage{
plot_loadings_confidence_eye(
  fit,
  level = c("unit", "unit_obs"),
  method = "wald",
  conf_level = 0.95,
  null_region = NULL,
  sort_by = c("trait_order", "magnitude"),
  ylim = NULL
)
}
\arguments{
\item{fit}{A multivariate \code{gllvmTMB()} fit, OR a data frame already
produced by \code{\link[=loading_ci]{loading_ci()}} / \code{\link[=flag_unreliable_loadings]{flag_unreliable_loadings()}}.}

\item{level, method, conf_level}{Forwarded to \code{\link[=loading_ci]{loading_ci()}} when
\code{fit} is a fit object. Ignored when \code{fit} is already a
\code{loading_ci()} data frame.}

\item{null_region}{Optional length-2 numeric drawn as a shaded
"biologically negligible" band so the reader can tell at a glance
which CIs sit outside it. If supplied, also colours points by
reliability via \code{\link[=flag_unreliable_loadings]{flag_unreliable_loadings()}}. Default \code{NULL}
(no band drawn).}

\item{sort_by}{Character, one of \code{"trait_order"} (the order of the
\code{trait} factor, the default) or \code{"magnitude"}. Note \code{"magnitude"}
applies a single \strong{global} trait ordering keyed by each trait's
largest absolute loading across axes; the shared discrete x-axis
cannot reorder traits within individual facets.}

\item{ylim}{Optional length-2 numeric vector clipping the y-axis
via \code{coord_cartesian()}. Useful when one degenerate loading
estimate would otherwise blow out the scale and crush the rest.
Default \code{NULL} (no clipping).}
}
\value{
A \code{ggplot} object.
}
\description{
A bar-and-whisker visualisation of the entries of \code{Lambda} with
point estimates and Wald (or other-method) confidence intervals from
\code{\link[=loading_ci]{loading_ci()}}. Default rendering uses the "Confidence Eye" contract:
a pale CI region (\code{geom_linerange}) and a hollow point estimate
(\code{geom_point}, shape = 21, fill white) so the visual weight rests on
the uncertainty rather than the point.
}
\details{
The closest published precedent for an uncertainty-aware per-species
loading display in JSDM is the site-score uncertainty plot of Hoegh
& Roberts (2020, doi:10.1002/ece3.5752), but that targets site
scores rather than species loadings. To my knowledge no equivalent
plot of per-species Lambda uncertainty exists in the ML/TMB JSDM
lineage (gllvm, sjSDM, ecoCopula).
}
\examples{
\dontrun{
plot_loadings_confidence_eye(fit, level = "unit",
                             null_region = c(-0.1, 0.1))
}

}
\seealso{
\code{\link[=loading_ci]{loading_ci()}}, \code{\link[=flag_unreliable_loadings]{flag_unreliable_loadings()}}.
}
