% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-rotated-loadings.R
\name{plot_rotated_loadings}
\alias{plot_rotated_loadings}
\title{Plot a rotated loading matrix}
\usage{
plot_rotated_loadings(
  x,
  level = "unit",
  method = c("varimax", "promax", "none"),
  order_axes = TRUE,
  sign_anchor = c("auto", "none"),
  anchor_traits = NULL,
  loading_scale = c("standardized", "raw"),
  sort = c("dominant", "abs_loading", "trait"),
  show_values = NULL,
  digits = 2L,
  limits = NULL,
  facet = c("level", "none")
)
}
\arguments{
\item{x}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}} or a data frame returned by
\code{\link[=extract_rotated_loadings_table]{extract_rotated_loadings_table()}}.}

\item{level}{\code{"unit"} (between-unit) or \code{"unit_obs"} (within-unit), passed
to \code{\link[=extract_rotated_loadings_table]{extract_rotated_loadings_table()}} when \code{x} is a fitted model.
Deprecated aliases \code{"B"} and \code{"W"} are still accepted with a warning.}

\item{method}{One of \code{"varimax"}, \code{"promax"}, or \code{"none"}; passed to
\code{\link[=extract_rotated_loadings_table]{extract_rotated_loadings_table()}} for fitted-model calls.}

\item{order_axes}{Logical. When \code{TRUE}, reorder rotated axes by decreasing
raw shared variance before plotting. Ignored when \code{method = "none"}.}

\item{sign_anchor}{One of \code{"auto"} or \code{"none"}. \code{"auto"} flips each rotated
axis so its anchor trait has a positive loading. Ignored when
\code{method = "none"}.}

\item{anchor_traits}{Optional character vector of trait names used for sign
anchoring. Supply one trait per axis after ordering. Axes without a
supplied anchor use the trait with the largest absolute loading.}

\item{loading_scale}{One of \code{"standardized"} or \code{"raw"} for fitted-model
calls. The default \code{"standardized"} puts loadings on a correlation-like
scale for figures. Ignored when \code{x} is already a data frame.}

\item{sort}{Trait ordering. \code{"dominant"} groups traits by the axis on which
they have their largest absolute loading. \code{"abs_loading"} orders by each
trait's largest absolute loading. \code{"trait"} preserves the incoming trait
order.}

\item{show_values}{Logical or \code{NULL}. When \code{NULL}, numeric tile labels are
shown for matrices with at most 80 cells.}

\item{digits}{Number of decimal places for tile labels.}

\item{limits}{Optional two-number colour-scale limits. When \code{NULL}, limits
are symmetric around zero and based on the plotted loadings.}

\item{facet}{One of \code{"level"} or \code{"none"}. Data frames containing more than
one covariance level are faceted by level by default.}
}
\value{
A \code{ggplot2} plot object with \code{gllvmTMB_meta} and \code{gllvmTMB_data}
attributes.
}
\description{
\code{plot_rotated_loadings()} turns the tidy rows from
\code{\link[=extract_rotated_loadings_table]{extract_rotated_loadings_table()}} into a publication-oriented loading
matrix. It can also extract the table from a fitted \code{\link[=gllvmTMB]{gllvmTMB()}} model.
Rows are traits, columns are latent axes, and colour shows the signed
loading after the requested rotation, axis ordering, sign anchoring, and
loading scale.
}
\details{
Scope: the helper plots point-estimate rotated loading rows from
\code{\link[=extract_rotated_loadings_table]{extract_rotated_loadings_table()}} for fitted \code{latent()} components
(this builds on the package's rotated-loadings extraction validation).
The plot does not compute or display loading uncertainty intervals
and does not make rotated axes uniquely biological. Bootstrap- or
simulation-aligned loading uncertainty remains a later inference
slice.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  rows <- data.frame(
    level = "unit",
    trait = rep(c("body", "wing", "bill"), times = 2),
    axis = rep(c("LV1", "LV2"), each = 3),
    loading = c(0.72, 0.61, -0.08, 0.05, -0.18, 0.66),
    abs_loading = abs(c(0.72, 0.61, -0.08, 0.05, -0.18, 0.66)),
    axis_variance = rep(c(1.1, 0.5), each = 3),
    axis_share = rep(c(0.69, 0.31), each = 3),
    rotation = "varimax",
    order_axes = TRUE,
    sign_anchor = "auto",
    anchor_trait = rep(c("body", "bill"), each = 3),
    loading_scale = "standardized"
  )
  plot_rotated_loadings(rows)
}
}
\seealso{
\code{\link[=extract_rotated_loadings_table]{extract_rotated_loadings_table()}}, \code{\link[=rotate_loadings]{rotate_loadings()}},
\code{\link[=plot.gllvmTMB_multi]{plot.gllvmTMB_multi()}}.
}
