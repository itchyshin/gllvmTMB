% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-gllvmTMB.R
\name{predict.gllvmTMB_multi}
\alias{predict.gllvmTMB_multi}
\title{Predict from a \code{gllvmTMB_multi} fit}
\usage{
\method{predict}{gllvmTMB_multi}(
  object,
  newdata = NULL,
  type = c("link", "response"),
  re_form = ~.,
  ...
)
}
\arguments{
\item{object}{A \code{gllvmTMB_multi} fit.}

\item{newdata}{Optional new data frame. If \code{NULL}, predictions are
produced for the training rows.}

\item{type}{One of \code{"link"} (default) or \code{"response"}.}

\item{re_form}{Random-effect formula controlling which random
effects are \emph{included} in the predicted linear predictor. Use
the default \code{~ .} to include all random effects when predicting
on training rows; pass \code{~ 0} (or \code{NA}) to predict the fixed-
effects-only / population-mean prediction. For \code{newdata} with
sites/species not present in the training data the random
effects cannot be drawn, so the prediction is fixed-effects-only
regardless of \code{re_form}.}

\item{...}{Unused.}
}
\value{
A data frame with the original row identifiers plus an \code{est}
column.
}
\description{
Returns the linear predictor (and conditional response) at each
observation in the training data, or the fixed-effects-only prediction
at user-supplied \code{newdata} (for sites / species not present in the
training data we cannot draw the corresponding random effects, so
predictions are returned on the population mean).
}
