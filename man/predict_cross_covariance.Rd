% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-sigma.R
\name{predict_cross_covariance}
\alias{predict_cross_covariance}
\title{Predict pair-specific cross-lineage covariance}
\usage{
predict_cross_covariance(
  fit,
  level,
  row_levels = NULL,
  col_levels = NULL,
  row_traits,
  col_traits
)
}
\arguments{
\item{fit}{A fitted \code{gllvmTMB_multi} object.}

\item{level}{Character scalar naming the fitted \verb{kernel_*()} tier.}

\item{row_levels, col_levels}{Character vectors naming row and column levels
in the fitted kernel matrix. If both are omitted for a kernel built by
\code{\link[=make_cross_kernel]{make_cross_kernel()}}, host and partner levels from the kernel metadata are
used.}

\item{row_traits, col_traits}{Character vectors of trait names defining the
rows and columns of the component-specific \code{Gamma_shape} block.}
}
\value{
A data frame with one row per level-pair and trait-pair. Columns
include \code{component}, \code{row_level}, \code{col_level}, \code{row_trait}, \code{col_trait},
\code{kernel_value}, \code{gamma_shape}, \code{covariance}, \code{rho}, and
\code{kernel_includes_rho}.
}
\description{
\code{predict_cross_covariance()} combines a component-specific
\code{Gamma_shape} block from \code{\link[=extract_Gamma]{extract_Gamma()}} with entries of the fitted
dense kernel matrix. For a fixed cross-lineage kernel built with
\code{\link[=make_cross_kernel]{make_cross_kernel()}}, the off-diagonal \code{K} entries already include the
supplied fixed \code{rho}, so the pair-specific covariance is
\code{Gamma_shape * K[row_level, col_level]}. This helper therefore uses the
shape-scale \code{Gamma} and does not multiply by \code{extract_Gamma(scale = "effect")}.

IN (\code{COE-03} / \code{COE-04}): fixed dense \code{kernel_latent()} tiers store their
aligned \code{K} matrices on the fit, and this helper returns point estimates for
named species/lineage pairs. PARTIAL: it does not estimate \code{rho}, produce
intervals, calibrate null thresholds, or combine components into a universal
total \code{Gamma}.
}
\examples{
\dontrun{
predict_cross_covariance(
  fit,
  level = "phy",
  row_levels = c("H1", "H2"),
  col_levels = c("P1", "P2"),
  row_traits = c("host_size", "host_defence"),
  col_traits = c("partner_size", "partner_attack")
)
}

}
