% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictive-diagnostics.R
\name{predictive_check}
\alias{predictive_check}
\title{Fitted-model predictive checks for a multivariate \code{gllvmTMB} fit}
\usage{
predictive_check(
  object,
  type = c("rq_qq", "rootogram", "stat_grouped", "dens_overlay"),
  nsim = NULL,
  ndraws = NULL,
  seed = NULL,
  trait = NULL,
  group = NULL,
  stat = c("mean", "median", "zero_fraction"),
  residual_type = c("randomized_quantile", "simulation_rank"),
  condition_on_RE = TRUE,
  max_count = NULL
)
}
\arguments{
\item{object}{A \code{gllvmTMB_multi} fit.}

\item{type}{Diagnostic plot type. \code{"rq_qq"} plots exact randomized-
quantile residuals when available; \code{"rootogram"} compares observed
count frequencies with fitted-model simulated count frequencies;
\code{"stat_grouped"} compares grouped summary statistics; \code{"dens_overlay"}
overlays observed and simulated densities and is mainly useful for
continuous responses.}

\item{nsim, ndraws}{Number of fitted-model draws. \code{ndraws} is accepted as
a bayesplot/brms-style alias; supply only one.}

\item{seed}{Optional RNG seed.}

\item{trait}{Optional character vector of trait names to keep.}

\item{group}{Row-metadata column used by \code{"stat_grouped"}. Default is
\code{"trait"}.}

\item{stat}{Grouped statistic for \code{"stat_grouped"}.}

\item{residual_type}{Residual type used by \code{"rq_qq"}. Defaults to exact
\code{"randomized_quantile"} residuals; \code{"simulation_rank"} is available as
a simulation-based fallback.}

\item{condition_on_RE}{Logical. Passed to \code{\link[=simulate.gllvmTMB_multi]{simulate.gllvmTMB_multi()}} for
simulation-based checks. The default \code{TRUE} checks the fitted response
distribution conditional on fitted random-effect modes.}

\item{max_count}{Optional upper count shown separately in \code{"rootogram"}.
Counts larger than this value are pooled into a final \code{">max_count"}
bin. Default \code{NULL} chooses a bounded automatic display range and pools
larger counts so that a single extreme simulated draw cannot create a
very wide rootogram.}
}
\value{
A \code{ggplot} object with diagnostic metadata attached in
\code{attr(plot, "gllvmTMB_diagnostic")}.
}
\description{
\code{predictive_check()} compares the observed stacked-trait response to
draws from the fitted model. It is the public version of the diagnostic
prototype from issue #222, but keeps the frequentist semantics explicit:
these are fitted-model predictive checks, not Bayesian posterior
predictive checks.
}
\details{
The returned object is a \code{ggplot}. Its plotted data, fit-health table
from \code{\link[=check_gllvmTMB]{check_gllvmTMB()}}, and \code{fit$fit_health} snapshot are also stored
in \code{attr(plot, "gllvmTMB_diagnostic")} so the figure can be audited
without reverse-engineering ggplot layers.

Scope boundary (DIA-11 / DIA-12): IN, fitted-model predictive plots
and residual Q-Q/rootogram helpers for \code{gllvmTMB_multi} fits, with
exact randomized-quantile residuals for Gaussian, Poisson, and NB2
rows and a simulation-rank fallback. PARTIAL, these are diagnostic
displays, not formal uniformity, dispersion, interval-calibration, or
Bayesian posterior-predictive tests. PLANNED, exact residual support
for delta, hurdle, truncated, ordinal, and mixture families remains
future validation work.
}
\examples{
\donttest{
set.seed(1)
n <- 24
df <- data.frame(
  unit = factor(rep(seq_len(n), each = 2)),
  trait = factor(rep(c("a", "b"), n)),
  value = rpois(2 * n, lambda = 2)
)
fit <- gllvmTMB(
  value ~ 0 + trait + latent(0 + trait | unit, d = 1),
  data = df,
  trait = "trait",
  unit = "unit",
  family = poisson()
)
predictive_check(fit, type = "rq_qq", seed = 1)
predictive_check(fit, type = "rootogram", ndraws = 20, seed = 1)
}
}
