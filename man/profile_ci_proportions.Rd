% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile-derived.R
\name{profile_ci_proportions}
\alias{profile_ci_proportions}
\title{Profile-likelihood CIs for per-trait variance proportions}
\usage{
profile_ci_proportions(fit, components = NULL, trait_idx = NULL, level = 0.95)
}
\arguments{
\item{fit}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{components}{Character vector of component names to include
(e.g. \code{c("shared_unit", "unique_unit")}). \code{NULL} (default) uses all
components present in \code{extract_proportions(fit)}.}

\item{trait_idx}{Integer vector of 1-based trait indices, or \code{NULL}
(default) for all traits.}

\item{level}{Confidence level. Default \code{0.95}.}
}
\value{
A data frame with one row per \verb{(trait, component)}:
\itemize{
\item \code{trait}: trait name (character).
\item \code{component}: component name (character).
\item \code{proportion}: point estimate (matches \code{\link[=extract_proportions]{extract_proportions()}}).
\item \code{lower}, \code{upper}: profile-likelihood bounds.
\item \code{method}: \code{"profile"} for successful inversions; \code{"(unavailable)"}
when the component is structurally pinned (e.g. \code{link_residual}
for fixed-scale families like Gaussian / binomial; the bounds
collapse to the point estimate); \code{NA} when uniroot could not
bracket.
}
}
\description{
For each \verb{(trait, component)} returned by \code{\link[=extract_proportions]{extract_proportions()}},
computes a profile-likelihood CI for the proportion of total trait
variance attributable to that component. The constraint is applied
to the proportion itself (Lagrange-style fix-and-refit), so the
denominator floats across all parameter blocks during the
constrained refit (i.e. the CI on \verb{p_c,t = sigma2_c,t / sum_\{c'\} sigma2_c',t} correctly accounts for the fact that fixing the
numerator shifts the denominator as nuisance parameters re-optimise).
}
\section{Implementation notes}{

\verb{shared_*} and \verb{unique_*} components are profiled via the shared
\code{\link[=profile_ci_communality]{profile_ci_communality()}} driver \code{.profile_ci_via_refit()} applied
to a custom target function that returns the (trait, component)
proportion as a function of all model parameters. For
\code{link_residual}, fixed-scale families (Gaussian, binomial with any
standard link, probit) give a constant-in-theta numerator and a
point-estimate denominator: bounds collapse to the point with
\code{method = "(unavailable)"}. Mean-dependent residuals (Poisson,
Gamma, Tweedie, ...) are not yet profiled and return \code{NA} bounds
with \code{method = "(unavailable)"}.
}

\seealso{
\code{\link[=extract_proportions]{extract_proportions()}}, \code{\link[=profile_ci_communality]{profile_ci_communality()}},
\code{\link[=profile_ci_correlation]{profile_ci_correlation()}}.
}
\keyword{internal}
