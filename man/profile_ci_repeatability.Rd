% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile-derived.R
\name{profile_ci_repeatability}
\alias{profile_ci_repeatability}
\title{Profile-likelihood CI for per-trait diag-only repeatability}
\usage{
profile_ci_repeatability(fit, trait_idx = NULL, level = 0.95)
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} fit.}

\item{trait_idx}{Integer index of the trait (1-based). \code{NULL} (the
default) returns CIs for all traits.}

\item{level}{Confidence level. Default 0.95.}
}
\value{
A data frame with columns \code{trait}, \code{R} (point estimate),
\code{lower}, \code{upper}, \code{method}.
}
\description{
Computes the profile-likelihood CI for the \strong{diag-only} per-trait
repeatability
\eqn{R^\text{diag}_t = \sigma^2_{\text{diag},B,t} /
                      (\sigma^2_{\text{diag},B,t} +
                       \sigma^2_{\text{diag},W,t})}
— i.e. only the trait-specific unique components, \emph{not} the standard
full-\eqn{\boldsymbol\Sigma} repeatability. For the
Bell-2009 / Nakagawa-Schielzeth-2010 standard
\eqn{R = \boldsymbol\Sigma_{B,tt} /
       (\boldsymbol\Sigma_{B,tt} + \boldsymbol\Sigma_{W,tt})}
(which includes the shared latent contributions
\eqn{(\boldsymbol\Lambda_B\boldsymbol\Lambda_B^\top)_{tt}} and
\eqn{(\boldsymbol\Lambda_W\boldsymbol\Lambda_W^\top)_{tt}}), use
\code{\link[=extract_repeatability]{extract_repeatability()}} instead.
}
\details{
Internally profiles the linear contrast
\eqn{2 (\theta_{\text{diag},B,t} - \theta_{\text{diag},W,t}) =
     \log(\sigma^2_{\text{diag},B} / \sigma^2_{\text{diag},W})} via
\code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} and back-transforms the bounds via
\eqn{\mathrm{plogis}}. Fast and accurate for that specific quantity.
}
\section{Boundary behaviour}{

When the within-unit variance is near zero (R close to 1) the
constrained log-likelihood is flat as \code{theta_diag_W -> -Inf} and the
upper bound on R is reported as \strong{1} (the natural boundary). Same
applies symmetrically for R close to 0 → lower bound \strong{0}. \code{NA} is
reserved for genuine profile failure (e.g. tmbprofile() error or too
few points to bracket the threshold).
}

\keyword{internal}
