% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-helpers.R
\name{profile_cross_rho}
\alias{profile_cross_rho}
\title{Profile fixed cross-lineage rho values}
\usage{
profile_cross_rho(
  A_H,
  A_P,
  W,
  rho,
  refit,
  metrics = NULL,
  eps = 1e-08,
  keep_fits = FALSE,
  ...
)
}
\arguments{
\item{A_H, A_P, W}{Inputs passed to \code{\link[=make_cross_kernel]{make_cross_kernel()}}.}

\item{rho}{Numeric vector of fixed \code{rho} values to evaluate.}

\item{refit}{Function called as \code{refit(K = K_rho, rho = rho_i, ...)}. It
should return a fitted object with a \code{logLik()} method.}

\item{metrics}{Optional function called as
\code{metrics(fit = fit_i, K = K_rho, rho = rho_i)}. It may return a named
list or one-row data frame of additional scalar summaries, such as a
\code{Gamma} correlation or a component norm.}

\item{eps}{Positive numerical floor passed to \code{\link[=make_cross_kernel]{make_cross_kernel()}}.}

\item{keep_fits}{Logical; if \code{TRUE}, fitted objects are attached as a
\code{"fits"} attribute. Default \code{FALSE} keeps the returned object light.}

\item{...}{Additional arguments passed to \code{refit}.}
}
\value{
A data frame of class \code{gllvmTMB_cross_rho_profile} with columns
\code{rho}, \code{logLik}, \code{relative_logLik}, \code{delta_deviance}, \code{is_best},
\code{convergence}, \code{pd_hessian}, \code{status}, and \code{error}, plus any metric
columns returned by \code{metrics}.
}
\description{
\code{profile_cross_rho()} formalises the fixed-kernel sensitivity workflow for
Design 65 cross-lineage kernels. It rebuilds \code{K_star} with
\code{\link[=make_cross_kernel]{make_cross_kernel()}} over a user-supplied \code{rho} grid, calls a caller-supplied
\code{refit(K, rho, ...)} function for each grid value, and returns a tidy
likelihood table.

What is covered: this is a fixed-kernel profile workflow for
comparing defended \code{rho} values. What is only partially covered: \code{rho} is still not a TMB parameter,
this helper does not estimate \code{rho}, and it does not produce confidence
intervals or null calibration.
}
\examples{
A_H <- diag(2)
A_P <- diag(2)
rownames(A_H) <- colnames(A_H) <- c("H1", "H2")
rownames(A_P) <- colnames(A_P) <- c("P1", "P2")
W <- matrix(c(1, 0.2, 0.2, 1), 2, 2)
dimnames(W) <- list(rownames(A_H), rownames(A_P))
dat <- data.frame(y = c(1, 2, 3, 4), x = c(0, 1, 0, 1))
profile_cross_rho(
  A_H, A_P, W,
  rho = c(0, 0.25),
  refit = function(K, rho) stats::lm(y ~ x, data = dat)
)

}
