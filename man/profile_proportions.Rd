% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile-derived-curves.R
\name{profile_proportions}
\alias{profile_proportions}
\title{Profile-likelihood curves for per-trait variance proportions}
\usage{
profile_proportions(
  fit,
  components = NULL,
  trait_idx = NULL,
  n_grid = 21L,
  grid_extent = 4,
  conf_level = 0.95
)
}
\arguments{
\item{fit}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{components}{Character vector of component names to include
(e.g. \code{c("shared_unit", "unique_unit")}). \code{NULL} (default) uses
all profile-able components present in
\code{\link[=extract_proportions]{extract_proportions()}} except \code{link_residual} (which is
structurally constant and has no curve).}

\item{trait_idx}{Integer vector of trait indices, or \code{NULL} for all.}

\item{n_grid}{Integer; number of grid points. Default 21.}

\item{grid_extent}{Numeric; total grid width as a multiple of a
robust scale (clamped to \verb{(0, 1)}). Default 4.}

\item{conf_level}{Confidence level. Default 0.95.}
}
\value{
A data.frame of class \code{profile_proportions},
\code{profile_derived}, \code{data.frame}.
}
\description{
For each \verb{(trait, component)} produced by \code{\link[=extract_proportions]{extract_proportions()}},
sweep over a grid of candidate proportion values, refit under each
constraint, and return the LR curves. Parallel CURVE endpoint to
\code{\link[=profile_ci_proportions]{profile_ci_proportions()}}.
}
\seealso{
\code{\link[=profile_ci_proportions]{profile_ci_proportions()}}, \code{\link[=plot.profile_derived]{plot.profile_derived()}}.
}
