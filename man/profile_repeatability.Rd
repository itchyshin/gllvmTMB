% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile-derived-curves.R
\name{profile_repeatability}
\alias{profile_repeatability}
\title{Profile-likelihood curve(s) for per-trait diag-only repeatability}
\usage{
profile_repeatability(
  fit,
  trait_idx = NULL,
  n_grid = 21L,
  grid_extent = 4,
  conf_level = 0.95
)
}
\arguments{
\item{fit}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{trait_idx}{Integer vector of trait indices, or \code{NULL} for all.}

\item{n_grid}{Integer; number of grid points per trait. Default 21.}

\item{grid_extent}{Numeric; total grid width as a multiple of a
robust scale (default 4 -- estimate +/- 2 scale units on each side).}

\item{conf_level}{Confidence level for the eventual CI inversion;
stored on the output for downstream consumers.}
}
\value{
A data.frame of class \code{profile_repeatability},
\code{profile_derived}, \code{data.frame} with columns: \code{target},
\code{profile_value}, \code{objective}, \code{delta_deviance}, \code{estimate},
\code{conf_level}.
}
\description{
For each requested trait, sweep over a grid of candidate values of
the diag-only repeatability
\eqn{R^\text{diag}_t = \sigma^2_{\text{diag},B,t} /
                      (\sigma^2_{\text{diag},B,t} +
                       \sigma^2_{\text{diag},W,t})}
refit the model under each constraint (Lagrange-style fix-and-refit),
and return the resulting profile-likelihood curve as a tidy
data.frame. The output has the same columnar shape as
\code{\link[=loading_profile]{loading_profile()}} so it can be passed to the shared
\code{\link[=plot.profile_derived]{plot.profile_derived()}} S3 method.
}
\details{
This is the parallel CURVE endpoint to the existing bracket-bisect
INVERSION endpoint \code{\link[=profile_ci_repeatability]{profile_ci_repeatability()}}. The two are
independent: this returns the LR curve data, that returns CI bounds.
}
\seealso{
\code{\link[=profile_ci_repeatability]{profile_ci_repeatability()}} (CI bounds endpoint),
\code{\link[=plot.profile_derived]{plot.profile_derived()}} (LR-curve plot method).
}
