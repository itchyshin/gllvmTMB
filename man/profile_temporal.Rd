% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal-profile.R
\name{profile_temporal}
\alias{profile_temporal}
\title{Profile a temporal persistence or decay parameter}
\usage{
profile_temporal(object, level = 0.95, ...)
}
\arguments{
\item{object}{An unreplicated Gaussian \code{temporal_indep()} fit or a qualified
replicated Gaussian AR1 \code{temporal_dep() + spatial_indep()} or
\code{temporal_dep() + phylo_indep()} or \code{temporal_dep() + animal_indep()} fit.}

\item{level}{Likelihood-ratio confidence level.}

\item{...}{Passed to \code{\link[=tmbprofile_wrapper]{tmbprofile_wrapper()}}. \code{lincomb} is refused because
this helper only profiles the native temporal time parameter.}
}
\value{
Named numeric vector with \code{estimate}, \code{lower}, and \code{upper}, on the
AR1 persistence or OU decay-rate scale. \code{NA} bounds mean the profile did
not establish an endpoint.
}
\description{
Profiles the direct native temporal time parameter with all other TMB
parameters re-optimized. This is a fitted-parameter likelihood profile,
not a calibrated interval or a profile of conditional temporal states.
The helper supports temporal-only unreplicated Gaussian \code{temporal_indep()}
fits and separately qualified replicated Gaussian AR1
\code{temporal_dep()} routes with one fixed-mesh \code{spatial_indep()} or fixed
\code{phylo_indep()} or \code{animal_indep()} source. Other combinations and
replicated panels need their own lifecycle contracts.
}
