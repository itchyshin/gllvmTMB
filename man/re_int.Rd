% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/re-int.R
\name{re_int}
\alias{re_int}
\alias{bar_syntax}
\title{Generic random intercepts \code{(1 | group)} (bar syntax)}
\arguments{
\item{condition}{Bar expression of the form \code{1 | group}. The right-hand
side \code{group} must be the unquoted name of a column in \code{data}; if the
column is not already a factor it is coerced to one.}
}
\value{
A formula marker; never evaluated as a call. The token is
recognised by \code{\link[=gllvmTMB]{gllvmTMB()}}'s formula parser via the parens that wrap
\code{1 | group}.
}
\description{
A \code{gllvmTMB()} formula token, modelled on lme4 / glmmTMB syntax, that
adds a per-row random intercept indexed by \code{group}. For each row \code{o},
the linear predictor gains \code{u[group(o)]}, where \code{u} is a vector of
length \code{nlevels(group)} with i.i.d. prior \code{u_g ~ N(0, sigma^2)}.
Multiple bar terms are allowed, each with its own variance component.
}
\details{
\subsection{What is and isn't supported}{

Only the random-intercept form \code{(1 | group)} is implemented in this
release. The natural extensions are not yet available:
\itemize{
\item \code{(0 + x | group)} — random slopes (no intercept). \strong{Coming in a
future release.}
\item \code{(1 + x | group)} — correlated intercept + slope. \strong{Coming in a
future release.}
\item \code{(0 + trait | group)} — trait-specific random intercepts. Use the
existing \code{unique()} / \code{latent()} covstructs for that pattern.
}

If you write any of these unsupported forms, \code{gllvmTMB()} aborts with a
message naming the unsupported form rather than silently fitting the
wrong model.
}

\subsection{Usage}{

Inside a \code{gllvmTMB()} formula, alongside any of the existing covstruct
terms:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Add a study-level random intercept on top of a latent() block:
fit <- gllvmTMB(value ~ 0 + trait + latent(0 + trait | site, d = 2) +
                         (1 | study),
                data = df)

# Two random intercepts (e.g. study + dataset):
fit <- gllvmTMB(value ~ 0 + trait + (1 | study) + (1 | dataset),
                data = df)
}\if{html}{\out{</div>}}

The fitted variance components live at
\code{fit$report$log_sigma_re_int} (one entry per \code{(1|...)} term, in the
order the terms appear in the formula); take \code{exp()} to get
\code{sigma}. The fitted random-intercept BLUPs live at
\code{fit$tmb_obj$env$parList()$u_re_int}, packed by term in the same
order; offsets and per-term lengths are recorded in \code{fit$re_int}.
}
}
\keyword{internal}
