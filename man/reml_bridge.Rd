% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reml-bridge.R
\name{reml_bridge}
\alias{reml_bridge}
\title{ML-vs-REML variance-component bridge diagnostic}
\usage{
reml_bridge(object, rel_thresh = 0.1)
}
\arguments{
\item{object}{A fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}. Must be Gaussian for every
response row -- REML is only implemented for Gaussian-only fits (see
the \code{REML} argument of \code{\link[=gllvmTMB]{gllvmTMB()}}).}

\item{rel_thresh}{Relative-gap threshold, \code{abs(reml - ml) / abs(reml)},
above which a component's \code{flag} is \code{TRUE}. Default \code{0.10} (10\%); a
heuristic screening value, not a calibrated small-cluster-regime
cutoff.}
}
\value{
A data frame with one row per comparable random-effect standard
deviation component: \code{component} (name), \code{ml}, \code{reml} (fitted
standard deviations under each estimator), \code{gap_abs}
(\code{abs(reml - ml)}), \code{gap_rel} (\code{gap_abs / abs(reml)}), \code{threshold}
(\code{rel_thresh}), and \code{flag} (\code{gap_rel > rel_thresh}).
}
\description{
Design 80's "bridge diagnostic": refits a Gaussian \code{\link[=gllvmTMB]{gllvmTMB()}} fit with
the opposite \code{REML} flag and compares each reported random-effect
standard deviation between the ML and REML companion fits. Maximum
likelihood variance components are known to be downward-biased at small
cluster counts; a large ML-vs-REML relative gap flags that regime --
"turning 'we know it's biased' into an automatic warning" (Design 80
sec. "Bridge diagnostic (cheap, ship in the RE arc)").
}
\details{
\code{reml_bridge()} is an explicit opt-in diagnostic: it triggers exactly
one extra refit of \code{object} (with the opposite \code{REML} value) and is
deliberately not run inside \code{\link[=check_gllvmTMB]{check_gllvmTMB()}}'s default path, because a
refit is expensive. Pass the result to
\code{check_gllvmTMB(object, reml_bridge = ...)}, or attach it as
\code{object$reml_bridge} before calling \code{check_gllvmTMB(object)}, to surface
the flag on the fit-health table.

REML likelihoods are not comparable across fixed-effect structures;
this diagnostic only ever compares a model against itself under the two
estimators; it is not an LRT and says nothing about fixed-effect
structure choice.
}
\examples{
\dontrun{
df <- data.frame(
  study = factor(rep(1:8, each = 8)),
  trait = factor(rep(c("t1", "t2"), length.out = 64)),
  value = rnorm(64)
)
fit <- gllvmTMB(value ~ 0 + trait + (1 | study), data = df, unit = "study")
bridge <- reml_bridge(fit)
bridge
check_gllvmTMB(fit, reml_bridge = bridge)
}
}
\seealso{
\code{\link[=check_gllvmTMB]{check_gllvmTMB()}} for the fit-health surface this feeds.
}
