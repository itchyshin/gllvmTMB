% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{scalar}
\alias{scalar}
\title{One shared trait variance: \code{scalar(0 + trait | g)}}
\usage{
scalar(formula)
}
\arguments{
\item{formula}{\code{0 + trait | g} style formula (LHS is the trait factor,
typically \code{0 + trait}; RHS is the grouping factor).}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical no-prefix name for the \strong{one-shared-variance} trait covariance:
a single variance \eqn{\sigma^2} shared by every trait with zero cross-trait
covariance, giving \eqn{\boldsymbol\Sigma_T = \sigma^2 \mathbf I_T}. It is the
most parsimonious mode in the covariance grid.
}
\details{
\code{scalar()} desugars byte-identically to
\code{indep(0 + trait | g, common = TRUE)} -- the two spellings fit the same
model, tying all trait variances to one shared parameter. Use \code{scalar()}
when you want to commit explicitly to one shared trait-scale variance; use
\code{indep()} for a separate variance per trait, or \code{dep()} / \code{latent()} for
cross-trait covariance.

The shared variance still couples grouping levels through the source
(identity for the no-prefix row). The source-specific \code{phylo_scalar()},
\code{animal_scalar()}, and \code{spatial_scalar()} carry the same one-shared-variance
meaning on their respective relationship operators.
}
\examples{
\dontrun{
# One shared trait variance across all traits (identity cross-trait):
fit <- gllvmTMB(value ~ 0 + trait + scalar(0 + trait | site),
                data = df, trait = "trait", unit = "site")

# Equivalent longhand:
fit <- gllvmTMB(value ~ 0 + trait + indep(0 + trait | site, common = TRUE),
                data = df, trait = "trait", unit = "site")
}
}
\seealso{
\code{\link[=indep]{indep()}}, \code{\link[=dep]{dep()}}, \code{\link[=latent]{latent()}}, \code{\link[=phylo_scalar]{phylo_scalar()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}.
}
