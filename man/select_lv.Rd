% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select-lv.R
\name{select_lv}
\alias{select_lv}
\alias{print.gllvmTMB_select_lv}
\title{Select a latent-variable rank by information criterion}
\usage{
select_lv(formula, data, ..., d_max, criterion = c("bic", "aic", "aicc"))

\method{print}{gllvmTMB_select_lv}(x, ...)
}
\arguments{
\item{formula}{A gllvmTMB model formula containing \strong{exactly one}
ordinary \code{latent(...)} term (the between-unit reduced-rank ordination;
\code{latent(0 + trait | unit, ...)}). Any \verb{d = } argument already present in
that term is overwritten for each swept value; write it without \verb{d =},
or with any placeholder value, since it will be replaced. Structured
source-specific terms (\code{phylo_latent()}, \code{spatial_latent()},
\code{kernel_latent()}, \code{animal_latent()}) are not swept and are rejected if
present, because their own \code{d} is a different rank than the one this
function selects.}

\item{data}{A data frame, as passed to \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{...}{Currently unused.}

\item{d_max}{Single positive integer: the largest rank to try. Fitting is
only identifiable up to the number of traits \code{p} (a \code{p}-row loading
matrix cannot have rank greater than \code{p}); \code{d_max} greater than \code{p} is
rejected before any fitting is attempted, naming \code{p}.}

\item{criterion}{One of \code{"aic"}, \code{"bic"} (default), or \code{"aicc"}. Selects
the row that minimises this criterion among fits that converged with a
positive-definite Hessian; a fit that did not is excluded from selection
(and reported with a warning) even though its row still appears in the
table.}

\item{x}{A \code{"gllvmTMB_select_lv"} object.}
}
\value{
An object of class \code{"gllvmTMB_select_lv"}, a list with:
\describe{
\item{table}{A \code{data.frame} with one row per attempted \code{d}: \code{d},
\code{npar}, \code{logLik}, \code{aic}, \code{bic}, \code{aicc}, \code{converged} (optimizer
convergence flag), \code{pd_hessian}, \code{seconds}, and \code{error} (the error
message when a fit failed, else \code{NA}).}
\item{selected_d}{The chosen rank under \code{criterion}.}
\item{criterion}{The criterion used for selection.}
\item{fits}{A named list (names = \code{d}) of the fitted \code{\link[=gllvmTMB]{gllvmTMB()}}
objects that succeeded; \code{NULL} for \code{d} that failed.}
\item{selected_fit}{The fit at \code{selected_d}.}
}
}
\description{
Fits the same model at latent rank (number of ordination axes) \verb{d = 1, ..., d_max} by sweeping the \code{d} argument of the formula's single ordinary
\code{latent(...)} covstruct term, and reports AIC, BIC, and AICc for each. The
fit at the \code{criterion}-minimising rank is reported as \code{selected_d}.

\code{select_lv()} is a \strong{information-criterion workflow tool}, not a
hypothesis test: it says nothing about statistical significance and
carries no interval on the chosen \code{d}. For a likelihood-ratio test of one
additional latent dimension (with the appropriate boundary correction, and
an explicit refusal when that correction is not justified), see
\code{\link[=anova.gllvmTMB_multi]{anova.gllvmTMB_multi()}}.
}
\section{Scope}{
Every fit in the sweep uses ordinary maximum likelihood (\code{REML = TRUE} is
rejected — AIC/BIC comparisons across different random-effect structures
under REML are not meaningful when the induced conditioning changes with
\code{d}, and gllvmTMB's REML route is Gaussian-only regardless). A single
failing \code{k} (an error from \code{\link[=gllvmTMB]{gllvmTMB()}}, non-convergence, or a non-PD
Hessian) does not abort the sweep; it is recorded in the table with \code{NA}
criteria and excluded from selection, with a warning naming which \code{d}
failed and why.
}

\examples{
\donttest{
set.seed(7)
n_units <- 70L
traits <- paste0("t", 1:4)
units <- paste0("u", seq_len(n_units))
Lambda_true <- matrix(
  c(0.8, 0.5, -0.6, 0.4, 0.3, 0.7, -0.5, 0.6),
  nrow = 4, ncol = 2
)
scores <- matrix(rnorm(n_units * 2), n_units, 2)
eta <- tcrossprod(scores, Lambda_true)
dat <- do.call(rbind, lapply(seq_len(n_units), function(i) {
  data.frame(unit = units[i], trait = traits,
    value = eta[i, ] + rnorm(4, sd = 0.2))
}))
dat$unit <- factor(dat$unit, levels = units)
dat$trait <- factor(dat$trait, levels = traits)

sel <- select_lv(
  value ~ 0 + trait + latent(0 + trait | unit, d = 1),
  data = dat, unit = "unit", trait = "trait", d_max = 3,
  criterion = "bic",
  control = gllvmTMBcontrol(optimizer = "optim", optArgs = list(method = "BFGS"))
)
sel
sel$selected_d
}

}
\seealso{
\code{\link[=anova.gllvmTMB_multi]{anova.gllvmTMB_multi()}}, \code{\link[=AIC.gllvmTMB_multi]{AIC.gllvmTMB_multi()}},
\code{\link[=BIC.gllvmTMB_multi]{BIC.gllvmTMB_multi()}}
}
