% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-site-trait.R
\name{simulate_site_trait}
\alias{simulate_site_trait}
\title{Simulate a functional-biogeography GLLVM dataset (sites × species × traits)}
\usage{
simulate_site_trait(
  n_sites = 50,
  n_species = 20,
  n_traits = 3,
  mean_species_per_site = 5,
  n_predictors = 2,
  alpha = NULL,
  beta = NULL,
  sigma2_eps = 0.5,
  Lambda_B = NULL,
  Lambda_W = NULL,
  psi_B = NULL,
  psi_W = NULL,
  sigma2_phy = NULL,
  sigma2_sp = NULL,
  Cphy = NULL,
  spatial_range = NULL,
  sigma2_spa = NULL,
  coords = NULL,
  seed = NULL
)
}
\arguments{
\item{n_sites}{Integer; number of sites \eqn{S}.}

\item{n_species}{Integer; number of species \eqn{I} in the regional pool.}

\item{n_traits}{Integer; number of traits \eqn{T}.}

\item{mean_species_per_site}{Average number of species observed per site
(Poisson with this mean, truncated at \eqn{n_{species}}). Default 5.}

\item{n_predictors}{Integer; number of site-level predictors. Default 2.}

\item{alpha}{Optional length-\code{n_traits} vector of trait intercepts; random
if \code{NULL}.}

\item{beta}{Optional \code{n_traits} x \code{n_predictors} matrix of trait-specific
slopes; random if \code{NULL}.}

\item{sigma2_eps}{Residual variance (\code{psi_W} only term in fixed-effects-only
simulator). Default 0.5.}

\item{Lambda_B, Lambda_W}{Optional \code{n_traits} x \code{d_B} / \code{n_traits} x \code{d_W}
loading matrices for between-site / within-site reduced-rank components.
Set to \code{NULL} (default) to omit.}

\item{psi_B, psi_W}{Optional length-\code{n_traits} vectors of trait-specific
variances at the global / local level. (Lowercase psi matches the
factor-analysis convention -- \verb{\\boldsymbol\\Psi_B = diag(psi_B)},
\verb{\\boldsymbol\\Psi_W = diag(psi_W)}; see \code{decisions.md} 2026-05-14
notation reversal.)}

\item{sigma2_phy, sigma2_sp}{Optional length-\code{n_traits} vectors of
phylogenetic and non-phylogenetic species variance. \code{Cphy} is required
if \code{sigma2_phy} is supplied.}

\item{Cphy}{Optional \code{n_species} x \code{n_species} phylogenetic correlation
matrix. If supplied, used to draw \code{p_it}.}

\item{spatial_range, sigma2_spa}{Optional spatial range and per-trait
variance for an exponential spatial residual \code{r_st}. If both supplied
and \code{coords} is \code{NULL}, sites are placed uniformly in \verb{[0, 1]^2}.}

\item{coords}{Optional \code{n_sites} x 2 matrix of site coordinates (used when
\code{spatial_range} is supplied).}

\item{seed}{Optional RNG seed.}
}
\value{
A list with components:
\describe{
\item{\code{data}}{Long-format data frame with one row per (site, species,
trait) observation: columns \code{site}, \code{species}, \code{trait}, \code{value},
\code{site_species}, predictors \code{env_1}, …, \code{env_n_predictors}, plus \code{lon}
and \code{lat} if coords were generated.}
\item{\code{truth}}{Named list of true parameter values (alpha, beta,
Lambda_B, Lambda_W, psi_B, psi_W, sigma2_phy, sigma2_sp, sigma2_spa,
spatial_range, sigma2_eps).}
\item{\code{Cphy}}{The phylogenetic correlation matrix used (or \code{NULL}).}
\item{\code{coords}}{Site coordinates used (or \code{NULL}).}
}
}
\description{
Simulates a three-dimensional dataset from the Nakagawa et al. (in
prep) functional-biogeography model:
\deqn{y_{sit} = \alpha_t + x_s'\beta_t + r_{st} + u_{st} + e_{sit} + p_{it} + q_{it}}
}
\details{
This simulator is domain-specific: it produces a \verb{(site, species, trait)} cube, which is the canonical functional-biogeography
layout. The package as a whole works for any \verb{(unit, trait)}
stacked-trait GLLVM (\code{unit} may be site, individual, species,
paper, ...); this particular simulator targets the site × species
× trait special case used in the methods paper. For simpler
simulations from a generic \verb{(unit, trait)} design, build the data
inline as in the morphometrics article.

Each component can be turned on or off via the corresponding variance /
loading argument. The default settings produce a fixed-effects-only dataset
suitable for Stage-1 regression tests.
}
\examples{
set.seed(1)
sim <- simulate_site_trait(n_sites = 30, n_species = 8, n_traits = 3,
                           mean_species_per_site = 4)
head(sim$data)
sim$truth$alpha
}
