% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-unit-trait.R
\name{simulate_unit_trait}
\alias{simulate_unit_trait}
\title{Simulate a generic stacked-trait GLLVM dataset (units x observations x traits)}
\usage{
simulate_unit_trait(
  n_units = 50L,
  n_obs_per_unit = 3L,
  n_traits = 5L,
  alpha = NULL,
  Lambda_B = NULL,
  Lambda_W = NULL,
  psi_B = NULL,
  psi_W = NULL,
  sigma2_eps = 0.5,
  seed = NULL,
  ...
)
}
\arguments{
\item{n_units}{Integer; number of units \eqn{U}.}

\item{n_obs_per_unit}{Integer; number of within-unit observations \eqn{O}
per unit.}

\item{n_traits}{Integer; number of traits \eqn{T}.}

\item{alpha}{Optional length-\code{n_traits} vector of trait intercepts; random
if \code{NULL}.}

\item{Lambda_B}{Optional \code{n_traits} x \code{d_B} between-unit loading matrix for
the reduced-rank between-unit component. Set to \code{NULL} (default) to omit.}

\item{Lambda_W}{Optional \code{n_traits} x \code{d_W} within-unit loading matrix for
the reduced-rank observation-level component (matching a \code{unique()} or
\code{latent()} term on the within-unit grouping). Set to \code{NULL} (default) to
omit.}

\item{psi_B}{Optional length-\code{n_traits} vector of trait-specific
between-unit variances. (Lowercase psi matches the factor-analysis
convention -- \verb{\\boldsymbol\\Psi_B = diag(psi_B)}; see \code{decisions.md}
2026-05-14 notation reversal.)}

\item{psi_W}{Optional length-\code{n_traits} vector of trait-specific
within-unit (observation-level) variances --
\verb{\\boldsymbol\\Psi_W = diag(psi_W)}.}

\item{sigma2_eps}{Row-level residual variance. Default 0.5.}

\item{seed}{Optional RNG seed.}

\item{...}{Reserved for future extensions; currently ignored.}
}
\value{
A list with components:
\describe{
\item{\code{data}}{Long-format data frame with one row per (unit,
observation, trait): columns \code{unit}, \code{observation}, \code{trait}, \code{value},
and \code{unit_observation} -- the row id matching gllvmTMB's \code{unique()}
within-unit grouping contract (one level per unit-observation cell).}
\item{\code{truth}}{Named list of true parameter values: \code{alpha}, \code{Lambda_B},
\code{Lambda_W}, \code{psi_B}, \code{psi_W}, \code{sigma2_eps}.}
}
}
\description{
Simulates a long-format dataset from the generic \verb{(unit, observation, trait)} stacked-trait GLLVM that the package supports. A \code{unit} is any
grouping over which a between-unit covariance structure applies
(individual, site, species, paper, ...) and each unit carries
\code{n_obs_per_unit} within-unit observations:
\deqn{y_{uot} = \alpha_t + b_{ut} + w_{uot} + e_{uot}}
where \eqn{b_{ut}} is the between-unit signal (reduced-rank
\code{Lambda_B} plus diagonal \code{psi_B}), \eqn{w_{uot}} the within-unit
observation-level signal (reduced-rank \code{Lambda_W} plus diagonal
\code{psi_W}), and \eqn{e_{uot}} a row-level residual.
}
\details{
This is the generic sibling of \code{\link[=simulate_site_trait]{simulate_site_trait()}}. Use this
function when the design is an abstract \verb{(unit, observation, trait)}
layout with no phylogenetic or spatial structure. Use
\code{\link[=simulate_site_trait]{simulate_site_trait()}} when you need the domain-specific
functional-biogeography \verb{(site, species, trait)} cube with
phylogenetic species correlation and spatial site coordinates baked
into the DGP.

Each component can be turned on or off via the corresponding variance /
loading argument. The all-\code{NULL} default settings produce a
fixed-effects-only dataset (trait intercepts plus row-level residual)
suitable for fast regression fixtures.
}
\examples{
set.seed(1)
## Fixed-effects-only default (all structure NULL):
sim0 <- simulate_unit_trait(n_units = 20, n_obs_per_unit = 3, n_traits = 4)
head(sim0$data)
sim0$truth$alpha

## With a between-unit reduced-rank loading and diagonal within-unit term:
Lambda_B <- matrix(c(0.9, 0.7, -0.4, 0.2,
                     0.1, -0.2, 0.6, 0.8), nrow = 4, ncol = 2)
sim <- simulate_unit_trait(n_units = 40, n_obs_per_unit = 3, n_traits = 4,
                           Lambda_B = Lambda_B, psi_W = 0.3, seed = 1)
str(sim$truth)
}
\seealso{
\code{\link[=simulate_site_trait]{simulate_site_trait()}} for the domain-specific
\verb{(site, species, trait)} functional-biogeography simulator with
phylogenetic and spatial machinery.
}
