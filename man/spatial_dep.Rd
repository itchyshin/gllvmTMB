% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{spatial_dep}
\alias{spatial_dep}
\title{Full unstructured spatial trait covariance: \code{spatial_dep(0 + trait | coords)}}
\usage{
spatial_dep(formula, coords = NULL, mesh = NULL)
}
\arguments{
\item{formula}{\code{0 + trait | coords} style formula (LHS is the trait
factor \code{0 + trait}; RHS is the \code{coords} placeholder symbol that
points at the \code{\link[=make_mesh]{make_mesh()}} coordinate columns).}

\item{coords}{Character; the column-name pair of spatial coordinates
in \code{data} (e.g. \code{c("lon", "lat")}). Resolved by the parser when
supplied as keyword argument; \code{NULL} when the orientation expresses
the coordinates via the formula RHS.}

\item{mesh}{Optional \code{fmesher} mesh object built via \code{\link[=make_mesh]{make_mesh()}}. If
\code{NULL}, the engine constructs a default mesh from \code{coords}.}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for the \strong{full unstructured cross-trait spatial
covariance} \eqn{\boldsymbol\Sigma_{\text{spa}} \otimes \mathbf{Q}^{-1}},
with \eqn{T(T+1)/2} free parameters
(\eqn{\boldsymbol\Sigma_{\text{spa}}} parameterised via Cholesky).
Mathematically identical to \code{spatial_latent(0 + trait | coords, d = T)}
standalone; the keyword choice is documentary.
}
\details{
Use \code{spatial_dep()} when you want an explicit full-unstructured
cross-trait spatial covariance fit. Use \code{spatial_latent()} for the
rank-reduced K-factor model. Use \code{spatial_indep()} for the
marginal-only per-trait spatial fit.
}
\section{Formula orientation}{

Same convention as the rest of the \verb{spatial_*} keywords: the
canonical orientation is \code{0 + trait | coords} (LHS = trait factor,
RHS = the \code{coords} placeholder). \code{spatial_dep} is born with this
orientation; the legacy \code{coords | trait} form is not accepted.
\subsection{Mutual exclusion with \code{spatial_latent()} / \code{spatial_unique()} / \code{spatial_indep()}}{

Combining \code{spatial_dep(0 + trait | coords)} with \code{spatial_latent} is
\strong{over-parameterised}; combining with \code{spatial_unique} or
\code{spatial_indep} is \strong{redundant}. The parser raises
\code{cli::cli_abort()} in any of these cases.
}
}

\examples{
\dontrun{
  sim <- simulate_site_trait(
    n_sites = 30, n_species = 6, mean_species_per_site = 5,
    spatial_range = 0.4, sigma2_spa = rep(0.3, 6), seed = 1
  )
  fit <- gllvmTMB(
    value ~ 0 + trait +
            spatial_dep(0 + trait | site, coords = c("lon", "lat")),
    data  = sim$data,
    trait = "trait",
    unit  = "site"
  )
}
}
\seealso{
\code{\link[=spatial_latent]{spatial_latent()}}, \code{\link[=spatial_unique]{spatial_unique()}}, \code{\link[=spatial_indep]{spatial_indep()}},
\code{\link[=dep]{dep()}}, \code{\link[=phylo_dep]{phylo_dep()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}.
}
