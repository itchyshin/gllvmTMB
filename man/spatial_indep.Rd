% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{spatial_indep}
\alias{spatial_indep}
\title{Per-trait spatial marginal field: \code{spatial_indep(0 + trait | coords)}}
\usage{
spatial_indep(formula, coords = NULL, mesh = NULL, common = FALSE)
}
\arguments{
\item{formula}{\code{0 + trait | coords} style formula (LHS is the trait
factor \code{0 + trait}; RHS is the \code{coords} placeholder symbol that
points at the \code{\link[=make_mesh]{make_mesh()}} coordinate columns).}

\item{coords}{Character; the column-name pair of spatial coordinates
in \code{data} (e.g. \code{c("lon", "lat")}). Resolved by the parser when
supplied as keyword argument; \code{NULL} when the orientation expresses
the coordinates via the formula RHS.}

\item{mesh}{An \code{fmesher} mesh object built via \code{\link[=make_mesh]{make_mesh()}}. It may be
supplied here or through the top-level \verb{mesh =} argument to \code{\link[=gllvmTMB]{gllvmTMB()}}.
The engine does not construct a mesh automatically from \code{coords}.}

\item{common}{\code{FALSE} (default) for a separate spatial-field variance per
trait; \code{TRUE} ties all traits to one shared spatial variance
(intercept-only). \code{spatial_indep(0 + trait | coords, common = TRUE)} is the
canonical one-shared-variance spelling and fits the same model as the
soft-deprecated \code{spatial_scalar()}.}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for \strong{T per-trait spatial fields coupled by the SPDE
precision matrix \eqn{\mathbf Q}}; standalone =
T univariate spatial fits stacked.
}
\details{
Each trait \eqn{t} gets its own variance
\eqn{\tau^2_t} on a Matern \eqn{\nu = 1} GMRF, with a shared range
parameter \eqn{\kappa}.

Use \code{spatial_indep()} for an explicit marginal-only spatial fit (no
cross-trait spatial decomposition). Use \code{spatial_latent()} for
K shared spatial fields driving all T traits via a T x K loading
matrix.
}
\section{Formula orientation}{

Same convention as the rest of the \verb{spatial_*} keywords: the
canonical orientation is \code{0 + trait | coords} (LHS = trait factor,
RHS = the \code{coords} placeholder). Spatial keywords adopted this
orientation at gllvmTMB 0.1.4; \code{spatial_indep} is born with it
(no legacy \code{coords | trait} orientation is accepted).
\subsection{Mutual exclusion with \code{spatial_latent()}}{

Combining \code{spatial_indep(0 + trait | coords)} with
\code{spatial_latent(0 + trait | coords, d = K)} is \strong{over-parameterised}
and the parser raises a \code{cli::cli_abort()}.
}
}

\examples{
\dontrun{
  sim <- simulate_site_trait(
    n_sites = 20, n_species = 4, mean_species_per_site = 4,
    spatial_range = 0.4, sigma2_spa = rep(0.3, 4), seed = 1
  )
  mesh <- make_mesh(sim$data, c("lon", "lat"), cutoff = 0.1)
  fit <- gllvmTMB(
    value ~ 0 + trait +
            spatial_indep(0 + trait | site, mesh = mesh),
    data  = sim$data,
    trait = "trait",
    unit  = "site"
  )
}
}
\seealso{
\code{\link[=spatial_latent]{spatial_latent()}}, \code{\link[=indep]{indep()}},
\code{\link[=phylo_indep]{phylo_indep()}}, \code{\link[=extract_Sigma]{extract_Sigma()}}.
}
