% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{spatial_latent}
\alias{spatial_latent}
\title{Reduced-rank spatial latent factors: \code{spatial_latent(0 + trait | coords, d = K)}}
\usage{
spatial_latent(formula, d = 1, coords = NULL, mesh = NULL)
}
\arguments{
\item{formula}{\code{0 + trait | coords} style formula (LHS is the trait
factor \code{0 + trait}; RHS is the \code{coords} placeholder symbol).}

\item{d}{Integer; number of spatial latent factors (rank K). Defaults
to 1.}

\item{coords}{Character; the column-name pair of spatial coordinates
in \code{data} (e.g. \code{c("lon", "lat")}). Resolved by the parser when
supplied as keyword argument; \code{NULL} when the orientation expresses
the coordinates via the formula RHS.}

\item{mesh}{Optional \code{fmesher} mesh object built via \code{\link[=make_mesh]{make_mesh()}}. If
\code{NULL}, the engine constructs a default mesh from \code{coords}.}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for the reduced-rank spatial random effect: K shared
SPDE fields drive all T traits via a T x K loading matrix
\eqn{\boldsymbol\Lambda_{\mathrm{spa}}}. The spatial analogue of
\code{\link[=phylo_latent]{phylo_latent()}} and the third cell of the spatial column of the API
grid (alongside \code{\link[=spatial_scalar]{spatial_scalar()}} and \code{\link[=spatial_unique]{spatial_unique()}}).
}
\details{
Internally this rewrites to \code{spde(form, .spatial_latent = TRUE, d = K)}
and toggles the TMB template's \code{spde_lv_k} switch to K. The C++ kernel
then reads a packed lower-triangular \code{Lambda_spde} and K shared spatial
fields \code{omega_spde_lv} (each prior \eqn{\mathrm{N}(\mathbf{0},
\mathbf{Q}^{-1})} where \eqn{\mathbf{Q}} is the SPDE precision built
from the mesh), and accumulates
\eqn{\eta_o \mathrel{{+}{=}} \sum_k \Lambda_{\mathrm{spa},tk}\,
\omega_k(\mathbf{s}_o)} per observation. Per-field \eqn{\tau} is
absorbed into \eqn{\boldsymbol\Lambda_{\mathrm{spa}}} for
identifiability, mirroring the \code{\link[=phylo_latent]{phylo_latent()}} convention. Like all
rr loadings, \eqn{\boldsymbol\Lambda_{\mathrm{spa}}} is identified
only up to rotation; pin via \code{lambda_constraint = list(spde = ...)}
or post-hoc rotate via \code{\link[=rotate_loadings]{rotate_loadings()}}.
}
\section{Formula orientation}{

The canonical orientation is \code{0 + trait | coords} (parallel to
\code{\link[=latent]{latent()}} / \code{\link[=unique]{unique()}} and glmmTMB's spatial keywords). The
earlier orientation \code{coords | trait} is accepted as a deprecated
alias and emits a one-shot \code{lifecycle::deprecate_warn()} per session
(introduced at gllvmTMB 0.1.4).
}

\examples{
\dontrun{
  sim <- simulate_site_trait(
    n_sites = 30, n_species = 6, mean_species_per_site = 5,
    spatial_range = 0.4, sigma2_spa = rep(0.3, 6), seed = 1
  )
  fit <- gllvmTMB(
    value ~ 0 + trait +
            spatial_latent(0 + trait | site, d = 2,
                           coords = c("lon", "lat")),
    data  = sim$data,
    trait = "trait",
    unit  = "site"
  )
}
}
\seealso{
\code{\link[=spatial_unique]{spatial_unique()}}, \code{\link[=spatial_scalar]{spatial_scalar()}}, \code{\link[=phylo_latent]{phylo_latent()}}.
}
