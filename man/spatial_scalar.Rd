% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{spatial_scalar}
\alias{spatial_scalar}
\title{One shared spatial-field variance across traits: \code{spatial_scalar(0 + trait | coords)}}
\usage{
spatial_scalar(formula, coords = NULL, mesh = NULL)
}
\arguments{
\item{formula}{\code{0 + trait | coords} style formula (LHS is the trait
factor \code{0 + trait}; RHS is the \code{coords} placeholder symbol).}

\item{coords}{Character; the column-name pair of spatial coordinates
in \code{data} (e.g. \code{c("lon", "lat")}). Resolved by the parser when
supplied as keyword argument; \code{NULL} when the orientation expresses
the coordinates via the formula RHS.}

\item{mesh}{Optional \code{fmesher} mesh object built via \code{\link[=make_mesh]{make_mesh()}}. If
\code{NULL}, the engine constructs a default mesh from \code{coords}.}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for the SPDE / GMRF Matern spatial random field with
\strong{one shared variance \eqn{\tau^{2}}} across all traits and a shared
range parameter \eqn{\kappa}. Each trait carries its own independent
field draw on the mesh, but every trait's field has the same marginal
variance and the same correlation length. Implementation: the same
SPDE engine as \code{\link[=spatial_unique]{spatial_unique()}}, with the per-trait
\code{log_tau_spde} parameters tied via TMB's \code{map} mechanism so they
collapse to a single estimable scalar.
}
\details{
Use this when domain knowledge (or parsimony) says all traits should
share the same amount of spatial structure. Compare to
\code{\link[=spatial_unique]{spatial_unique()}} (D independent variances) and \code{\link[=spatial_latent]{spatial_latent()}}
(K-dim factor decomposition).
}
\section{Formula orientation}{

The canonical orientation is \code{0 + trait | coords} (parallel to
\code{\link[=latent]{latent()}} / \code{\link[=unique]{unique()}} and glmmTMB's spatial keywords). The
earlier orientation \code{coords | trait} is accepted as a deprecated
alias and emits a one-shot \code{lifecycle::deprecate_warn()} per session
(introduced at gllvmTMB 0.1.4).
}

\examples{
\dontrun{
  sim <- simulate_site_trait(
    n_sites = 20, n_species = 4, mean_species_per_site = 4,
    spatial_range = 0.4, sigma2_spa = rep(0.3, 4), seed = 1
  )
  fit <- gllvmTMB(
    value ~ 0 + trait +
            spatial_scalar(0 + trait | site, coords = c("lon", "lat")),
    data  = sim$data,
    trait = "trait",
    unit  = "site"
  )
}
}
\seealso{
\code{\link[=spatial_unique]{spatial_unique()}}, \code{\link[=spatial_latent]{spatial_latent()}}, \code{\link[=spde]{spde()}} (deprecated alias).
}
