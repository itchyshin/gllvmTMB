% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{spatial_scalar}
\alias{spatial_scalar}
\title{Deprecated alias for one shared spatial-field variance}
\usage{
spatial_scalar(formula, coords = NULL, mesh = NULL)
}
\arguments{
\item{formula}{\code{0 + trait | coords} style formula (LHS is the trait
factor \code{0 + trait}; RHS is the \code{coords} placeholder symbol).}

\item{coords}{Character; the column-name pair of spatial coordinates
in \code{data} (e.g. \code{c("lon", "lat")}). Resolved by the parser when
supplied as keyword argument; \code{NULL} when the orientation expresses
the coordinates via the formula RHS.}

\item{mesh}{An \code{fmesher} mesh object built via \code{\link[=make_mesh]{make_mesh()}}. It may be
supplied here or through the top-level \verb{mesh =} argument to \code{\link[=gllvmTMB]{gllvmTMB()}}.
The engine does not construct a mesh automatically from \code{coords}.}
}
\value{
A formula marker; never evaluated.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}
}
\details{
\code{spatial_scalar()} is soft-deprecated compatibility syntax in gllvmTMB
0.2.0. Write
\code{spatial_indep(0 + trait | coords, common = TRUE)} in new code. The two
spellings fit the same SPDE / GMRF Matérn model with
\strong{one shared variance \eqn{\tau^{2}}} across all traits and a shared
range parameter \eqn{\kappa}. Each trait carries its own independent
field draw on the mesh, but every trait's field has the same marginal
variance and the same correlation length. Implementation: the same
SPDE engine as \code{\link[=spatial_indep]{spatial_indep()}}, with the per-trait
\code{log_tau_spde} parameters tied via TMB's \code{map} mechanism so they
collapse to a single estimable scalar.

Use \code{spatial_indep(..., common = TRUE)} when domain knowledge (or parsimony)
says all traits should share the same amount of spatial structure. Omit
\code{common = TRUE} for separate trait-specific spatial variances, or use
\code{\link[=spatial_latent]{spatial_latent()}} for a reduced-rank cross-trait decomposition.
}
\section{Formula orientation}{

The canonical orientation is \code{0 + trait | coords} (parallel to
\code{\link[=latent]{latent()}} / \code{\link[=indep]{indep()}} and glmmTMB's spatial keywords). The
earlier orientation \code{coords | trait} is accepted as a deprecated
alias and emits a one-shot \code{lifecycle::deprecate_warn()} per session
(introduced at gllvmTMB 0.1.4).
}

\examples{
\dontrun{
  sim <- simulate_site_trait(
    n_sites = 20, n_species = 4, mean_species_per_site = 4,
    spatial_range = 0.4, sigma2_spa = rep(0.3, 4), seed = 1
  )
  mesh <- make_mesh(sim$data, c("lon", "lat"), cutoff = 0.1)
  fit <- gllvmTMB(
    value ~ 0 + trait +
            spatial_indep(0 + trait | site, mesh = mesh, common = TRUE),
    data  = sim$data,
    trait = "trait",
    unit  = "site"
  )
}
}
\seealso{
\code{\link[=spatial_indep]{spatial_indep()}}, \code{\link[=spatial_latent]{spatial_latent()}}, \code{\link[=spde]{spde()}} (deprecated alias).
}
