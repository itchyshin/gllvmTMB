% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms-sugar.R
\name{spatial_unique}
\alias{spatial_unique}
\title{Per-trait independent spatial random fields: \code{spatial_unique(0 + trait | coords)}}
\usage{
spatial_unique(formula, coords = NULL, mesh = NULL)
}
\arguments{
\item{formula}{\code{0 + trait | coords} style formula (LHS is the trait
factor \code{0 + trait}; RHS is the \code{coords} placeholder symbol that
points at the \code{\link[=make_mesh]{make_mesh()}} coordinate columns).}

\item{coords}{Character; the column-name pair of spatial coordinates
in \code{data} (e.g. \code{c("lon", "lat")}). Resolved by the parser when
supplied as keyword argument; \code{NULL} when the orientation expresses
the coordinates via the formula RHS.}

\item{mesh}{Optional \code{fmesher} mesh object built via \code{\link[=make_mesh]{make_mesh()}}. If
\code{NULL}, the engine constructs a default mesh from \code{coords}.}
}
\value{
A formula marker; never evaluated.
}
\description{
Canonical name for the SPDE / GMRF Matern spatial random field with
\strong{one independent field per trait} (per-trait variance
\eqn{\tau^{2}_t}, shared range parameter \eqn{\kappa}). This is the
"unique-rank" cell of the spatial column of the API grid.
}
\details{
Formerly \code{spde(0 + trait | coords)} and \code{spatial(0 + trait | coords)}
-- same engine, new name. See \code{\link[=spde]{spde()}} for the technical details on
the Matern \eqn{\nu = 1} kernel.

Compare to \code{\link[=spatial_scalar]{spatial_scalar()}} (ONE shared variance across traits)
and \code{\link[=spatial_latent]{spatial_latent()}} (K-dim spatial latent factors with a loading
matrix).
}
\section{Formula orientation}{

The canonical orientation is \code{0 + trait | coords} (LHS = trait factor,
RHS = the \code{coords} placeholder), parallel to \code{\link[=latent]{latent()}},
\code{\link[=unique]{unique()}}, and glmmTMB's \code{gau(0 + trait | pos)} / \code{exp(0 + trait | pos)}.
The earlier orientation \code{coords | trait} is accepted as a deprecated
alias: it emits a one-shot \code{lifecycle::deprecate_warn()} per session
(introduced at gllvmTMB 0.1.4) and is internally normalised to the
canonical orientation, so existing fits remain byte-identical.
}

\examples{
\dontrun{
  sim <- simulate_site_trait(
    n_sites = 20, n_species = 4, mean_species_per_site = 4,
    spatial_range = 0.4, sigma2_spa = rep(0.3, 4), seed = 1
  )
  fit <- gllvmTMB(
    value ~ 0 + trait +
            spatial_unique(0 + trait | site, coords = c("lon", "lat")),
    data  = sim$data,
    trait = "trait",
    unit  = "site"
  )
}
}
\seealso{
\code{\link[=spatial_scalar]{spatial_scalar()}}, \code{\link[=spatial_latent]{spatial_latent()}}, \code{\link[=spde]{spde()}} (deprecated alias).
}
