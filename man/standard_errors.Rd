% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standard-errors.R
\name{standard_errors}
\alias{standard_errors}
\title{Compute standard errors after fitting}
\usage{
standard_errors(fit)
}
\arguments{
\item{fit}{A fitted multivariate model returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}
}
\value{
The same fit, with its \code{sd_report} field populated (and
\code{sdreport_error} cleared). Standard-error consumers such as
\code{\link[=summary.gllvmTMB_multi]{summary.gllvmTMB_multi()}}, \code{\link[=getLV]{getLV()}}, \code{\link[=getREsd]{getREsd()}}, and
\code{confint(method = "wald")} work on the returned object.

R semantics matter here: the fit is \strong{returned}, not modified in place.
Assign the result -- \code{fit <- standard_errors(fit)} -- or the standard
errors are discarded.

If the fit already has an \code{sd_report}, it is returned unchanged; the
calculation is not repeated.
}
\description{
\code{\link[=gllvmTMB]{gllvmTMB()}} computes a TMB \code{\link[TMB:sdreport]{TMB::sdreport()}} at fitting time unless it is
told not to (\code{control = gllvmTMBcontrol(se = FALSE)}). Skipping it is
substantially faster, but it used to be a one-way door: the only way to get
standard errors afterwards was to fit the model again. \code{standard_errors()}
removes that door by computing the same \code{sdreport()} on demand from the
fitted object.
}
\details{
Fits made with non-unit likelihood weights use a weighted objective rather
than an ordinary likelihood. \code{standard_errors()} refuses those fits because
the package does not yet provide sandwich-calibrated uncertainty for them;
point estimates remain available from the fitted model.
}
\section{When this helps}{

The saving is real but bounded: it is worth using when a workflow fits many
models and needs standard errors for only some of them (model selection,
simulation loops, a grid of candidate structures). For a single fit whose
summary you intend to read, the default \code{se = TRUE} is simpler and costs
nothing extra.
}

\section{Same-session only}{

A TMB ADFun object holds external pointers into compiled memory. Those
pointers do not survive \code{saveRDS()} / \code{readRDS()} or a new R session, so
\code{standard_errors()} can only be called on a fit made in the \emph{current}
session. This is a pre-existing property of every function in this package
that reuses \code{fit$tmb_obj} (profiling, bootstrapping, \code{getLV()}), not a new
restriction -- but this function names it with a clear error rather than
failing obscurely. If you need standard errors from a saved fit, refit it
with \code{se = TRUE}.
}

\section{What these standard errors are}{

Exactly the ones the fit would have produced at fitting time -- the same
TMB \code{sdreport()} call, on the same converged parameter vector, to the bit.
Deferring the calculation does not change it, and does not change any
honesty caveat that already applies to Wald standard errors from this
package.
}

\examples{
\dontrun{
## Fit fast, decide later whether standard errors are needed.
fit <- gllvmTMB(
  traits(y1, y2) ~ 1 + latent(1 | site, d = 1),
  data = dat, family = gaussian(),
  control = gllvmTMBcontrol(se = FALSE)
)

fit <- standard_errors(fit)
summary(fit)
}
}
\seealso{
\code{\link[=gllvmTMBcontrol]{gllvmTMBcontrol()}} for the \code{se} argument that defers the
calculation; \code{\link[=getREsd]{getREsd()}} and \code{\link[=getLV]{getLV()}} for accessors that read the
resulting \code{sd_report}.
}
