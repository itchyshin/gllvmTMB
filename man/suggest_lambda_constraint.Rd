% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest-lambda-constraint.R
\name{suggest_lambda_constraint}
\alias{suggest_lambda_constraint}
\title{Suggest a \code{lambda_constraint} matrix for a reduced-rank fit}
\usage{
suggest_lambda_constraint(
  fit_or_formula,
  data = NULL,
  level = "unit",
  convention = c("lower_triangular", "pin_top_one", "none", "varimax_threshold",
    "wald_retention", "profile_retention"),
  trait = "trait",
  unit = NULL,
  threshold = 0.3,
  retention_prob = 0.9,
  sigma_d2 = 1,
  site = NULL
)
}
\arguments{
\item{fit_or_formula}{Either a fitted multivariate model returned by
\code{\link[=gllvmTMB]{gllvmTMB()}} or a formula. If a formula, \code{data} must also be supplied.}

\item{data}{A data frame. Required when \code{fit_or_formula} is a formula;
ignored when it is a fit.}

\item{level}{Which loading matrix to constrain: \code{"unit"} (between-unit,
default) or \code{"unit_obs"} (within-unit). Deprecated aliases \code{"B"} and
\code{"W"} are still accepted with a warning.}

\item{convention}{One of:
\describe{
\item{\code{"lower_triangular"} (default)}{Pin every upper-triangular
entry to 0, i.e. \code{M[i, j] = 0} for \code{j > i}. Removes the
rotational ambiguity completely. Pins \code{K(K-1)/2} entries.}
\item{\code{"pin_top_one"}}{Single-anchor convention: \code{M[1, 1] = 1},
rest \code{NA}. Sets the scale of factor 1; does NOT remove rotational
ambiguity.}
\item{\code{"none"}}{All-\code{NA} matrix -- no pins. Useful if you plan to
rotate the fitted loadings after fitting instead.}
}}

\item{trait, unit}{Name of the trait and unit (site) columns. Forwarded when
\code{fit_or_formula} is a formula and the data does not already use
defaults.}

\item{threshold}{Salience threshold on the standardised loading
\eqn{\rho_{tk} = \Lambda_{tk} / \sqrt{\Sigma_{total,tt}}}. Used by
\code{"varimax_threshold"} and \code{"wald_retention"}. Comrey-Lee 1992
convention: 0.30 = "fair" salience (default). Must be a finite scalar
in \verb{[0, 1)}. Raise to 0.40 for "good" / 0.50 for "very good".}

\item{retention_prob}{Retention probability for \code{"wald_retention"}
and \code{"profile_retention"}:
\itemize{
\item{\code{"wald_retention"}: pin entry if
\verb{Pr(|rho| > threshold) < retention_prob}.}
\item{\code{"profile_retention"}: pin entry if a profile LRT fails to
reject \code{H0: Lambda = 0} at significance level
\code{1 - retention_prob}.}
}
Must be a finite scalar in \verb{(0, 1)}. Default 0.90 follows the applied-EFA
bootstrap convention; the stricter BSEM 0.95 often over-prunes at
moderate sample sizes and produces non-PD refits.}

\item{sigma_d2}{Deprecated compatibility argument; ignored by the
standardised-loading conventions. Their denominators now come from the
model-implied total variance returned by \code{\link[=extract_Sigma]{extract_Sigma()}}.}

\item{site}{Deprecated alias for \code{unit}. Emits a one-shot warning and maps
to \code{unit}.}
}
\value{
A list with components:
\describe{
\item{\code{constraint}}{A \verb{T x K} matrix with \code{NA} in free entries and
0 (or 1, for \code{"pin_top_one"}) in pinned entries. Has trait names
as rownames and \verb{"f1", ..., "fK"} as colnames.}
\item{\code{convention}}{The chosen convention.}
\item{\code{d}}{The number of factors \code{K}.}
\item{\code{n_pins}}{Number of pinned entries.}
\item{\code{note}}{A short explanation of what was pinned and why.}
\item{\code{usage_hint}}{An example call as a string showing how to use
the returned matrix with \code{\link[=gllvmTMB]{gllvmTMB()}}.}
}
}
\description{
Produces a default constraint matrix \code{M} for the reduced-rank loadings
Lambda in a fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}} or in a formula-data pair that will
be passed to \code{\link[=gllvmTMB]{gllvmTMB()}}. The matrix is returned in the format expected by
the \code{lambda_constraint} argument: \code{NA} in free entries, numeric values in
pinned entries. This function supplies an identification convention -- an
orientation/scale scaffold that resolves rotational ambiguity -- and does
not discover biological factor structure; a low or pinned loading is a
constraint choice or a result to interpret, not by itself grounds to
exclude a trait.
}
\examples{
\dontrun{
sug <- suggest_lambda_constraint(
  value ~ 0 + trait + latent(0 + trait | site, d = 2),
  data = my_data
)
fit <- gllvmTMB(
  value ~ 0 + trait + latent(0 + trait | site, d = 2),
  data              = my_data,
  trait             = "trait",
  unit              = "site",
  lambda_constraint = list(unit = sug$constraint)
)
}
}
