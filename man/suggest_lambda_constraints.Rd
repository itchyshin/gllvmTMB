% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest-lambda-constraint.R
\name{suggest_lambda_constraints}
\alias{suggest_lambda_constraints}
\title{Compare several \code{lambda_constraint} suggestions}
\usage{
suggest_lambda_constraints(
  fit_or_formula,
  data = NULL,
  level = "unit",
  methods = c("varimax_threshold", "wald_retention"),
  trait = "trait",
  unit = "site",
  threshold = 0.3,
  retention_prob = 0.9,
  sigma_d2 = 1,
  site = NULL
)
}
\arguments{
\item{fit_or_formula}{Either a fitted multivariate model returned by
\code{\link[=gllvmTMB]{gllvmTMB()}} or a formula. If a formula, \code{data} must also be supplied.}

\item{data}{A data frame. Required when \code{fit_or_formula} is a formula;
ignored when it is a fit.}

\item{level}{Which loading matrix to constrain: \code{"unit"} (between-unit,
default) or \code{"unit_obs"} (within-unit). Deprecated aliases \code{"B"} and
\code{"W"} are still accepted with a warning.}

\item{methods}{Character vector of conventions to run. Accepted values are
the same as the \code{convention} argument of \code{\link[=suggest_lambda_constraint]{suggest_lambda_constraint()}}.}

\item{trait, unit}{Name of the trait and unit (site) columns. Forwarded when
\code{fit_or_formula} is a formula and the data does not already use
defaults.}

\item{threshold}{Salience threshold on the standardised loading
\eqn{\rho = \Lambda / \sqrt{\Lambda^2 + \sigma_d^2}}. Used by
\code{"varimax_threshold"} and \code{"wald_retention"}. Comrey-Lee 1992
convention: 0.30 = "fair" salience (default). Raise to 0.40 for
"good" / 0.50 for "very good".}

\item{retention_prob}{Retention probability for \code{"wald_retention"}
and \code{"profile_retention"}:
\itemize{
\item{\code{"wald_retention"}: pin entry if
\verb{Pr(|Lambda| > threshold) < retention_prob}.}
\item{\code{"profile_retention"}: pin entry if a profile LRT fails to
reject \code{H0: Lambda = 0} at significance level
\code{1 - retention_prob}.}
}
Default 0.90 follows the applied-EFA bootstrap convention; the
stricter BSEM 0.95 often over-prunes at moderate sample sizes and
produces non-PD refits.}

\item{sigma_d2}{Link-implicit residual variance on the link scale,
used by \code{"wald_retention"} to compute \verb{Pr(|Lambda| > threshold)}
from the standardised \eqn{\rho}. Defaults to \code{1} (probit /
ordinal_probit). Use \eqn{\pi^2/3} for logit, \eqn{\pi^2/6} for
cloglog, or the fitted residual variance for Gaussian.}

\item{site}{Deprecated alias for \code{unit}. Emits a one-shot warning and maps
to \code{unit}.}
}
\value{
A list of class \code{gllvmTMB_lambda_constraint_suggestions} with:
\describe{
\item{\code{summary}}{A data frame with one row per method and columns for
decision basis, cost, number of pinned/free entries, and the helper
note.}
\item{\code{suggestions}}{A named list of the full
\code{\link[=suggest_lambda_constraint]{suggest_lambda_constraint()}} return objects, one per method.}
\item{\code{recommended_method}}{The highest-evidence method among the
requested methods. By default this is \code{"wald_retention"}; if
\code{"profile_retention"} is requested, it is recommended because it uses
likelihood-ratio refits rather than the baseline Hessian.}
\item{\code{recommended}}{The full suggestion object for
\code{recommended_method}.}
}
}
\description{
Runs \code{\link[=suggest_lambda_constraint]{suggest_lambda_constraint()}} under several conventions and returns a
compact comparison table. This is the convenience layer for exploratory
loading workflows where the analyst wants to see the cheap point-threshold
suggestion beside uncertainty-aware Wald or profile-retention suggestions
before choosing the matrix to pass to \code{lambda_constraint}.
}
\details{
The default compares \code{varimax_threshold} and \code{wald_retention}. Add
\code{"profile_retention"} to \code{methods} when you want the likelihood-ratio
version; it is slower because it refits once per testable loading entry.
}
\examples{
\dontrun{
cmp <- suggest_lambda_constraints(
  fit,
  methods = c("varimax_threshold", "wald_retention"),
  threshold = 0.30,
  retention_prob = 0.90
)
cmp$summary
fit_con <- gllvmTMB(
  value ~ 0 + trait + latent(0 + trait | site, d = 2),
  data = df,
  family = binomial(),
  lambda_constraint = list(unit = cmp$recommended$constraint)
)

# Expensive: one likelihood-ratio refit per testable loading.
cmp_profile <- suggest_lambda_constraints(
  fit,
  methods = "profile_retention"
)
}
}
