% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile-ci.R
\name{tmbprofile_wrapper}
\alias{tmbprofile_wrapper}
\title{Profile-likelihood CI for one parameter or linear combination}
\usage{
tmbprofile_wrapper(
  fit,
  name = NULL,
  which = 1L,
  lincomb = NULL,
  level = 0.95,
  transform = identity,
  ystep = 0.5,
  ytol = 2,
  parm.range = c(-Inf, Inf)
)
}
\arguments{
\item{fit}{A \code{gllvmTMB_multi} fit returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{name}{Either:
\itemize{
\item A single character string giving the parameter name (matched
against \code{names(fit$opt$par)}; for vector parameters use \code{which}
to pick the entry), OR
\item a single integer giving the index into \code{fit$opt$par}.
}}

\item{which}{For vector-valued parameters (e.g. \code{theta_diag_B} is
length T), the index within the named-block. Default \code{1}.}

\item{lincomb}{Optional numeric vector of length \code{length(fit$opt$par)}
giving a linear combination of fixed-effect parameters to profile.
When supplied, \code{name} is ignored. Used internally for total-variance
contrasts (e.g. \code{theta_diag_B[t] + theta_diag_W[t]} for a per-trait
total log-variance).}

\item{level}{Confidence level in (0, 1). Default 0.95.}

\item{transform}{Optional function applied to the profile-CI bounds
before returning. Pass \code{exp} to convert log-SD to SD; pass
\verb{\\(x) exp(2 * x)} for variance from log-SD. Default \code{identity}.}

\item{ystep, ytol}{Passed through to \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}}. The defaults
give a fast, robust profile for variance components.}

\item{parm.range}{Passed through to \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}}. Default
\code{c(-Inf, Inf)}; for log-scale parameters with a hard lower bound,
constrain via e.g. \code{c(-15, Inf)} so the profile does not chase
variance to zero.}
}
\value{
A length-3 named numeric vector (\code{estimate}, \code{lower}, \code{upper}).
\code{lower} or \code{upper} may be \code{NA} when the profile is one-sided
(variance pinned at boundary).
}
\description{
Wraps \code{TMB::tmbprofile()} with a chi-square root-finding step to return
a 95\% (or other-level) confidence interval for a single parameter (or
a fixed linear combination of parameters) on a fitted gllvmTMB_multi
model. The profile is computed in TMB's C++ inner optim warm-started
from the joint MLE — typically order-of-magnitude faster than refitting
under a constraint in pure R.
}
\details{
For non-linear derived quantities (ICC, communality, repeatability,
correlations, phylogenetic signal H^2), see the helpers in
\code{R/profile-derived.R}.
}
\section{Boundary behaviour}{

When a variance component is near zero, the profile likelihood becomes
one-sided: the parameter can decrease to negative infinity in log-SD
space (variance to zero) without an additional likelihood penalty.
In that case the bound is at the natural boundary of the parameter
space, not unknown. \code{tmbprofile_wrapper()} therefore returns the
transformed boundary (e.g. \code{lower = 0} for \code{transform = exp},
\code{lower = 0} for \code{transform = plogis}, \code{lower = -1} for
\code{transform = tanh}, \code{lower = -Inf} for \code{transform = identity}).
\code{NA} is reserved for genuine profile failure (e.g. tmbprofile()
threw an error or returned too few points).
}

\keyword{internal}
