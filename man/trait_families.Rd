% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output-methods.R
\name{trait_families}
\alias{trait_families}
\alias{trait_families.gllvmTMB_multi}
\title{Per-trait response families of a fitted multivariate model}
\usage{
trait_families(object, ...)

\method{trait_families}{gllvmTMB_multi}(object, ...)
}
\arguments{
\item{object}{A fitted multivariate model returned by \code{\link[=gllvmTMB]{gllvmTMB()}}.}

\item{...}{Currently unused; present for S3 generic compatibility.}
}
\value{
A named character vector of length \code{n_traits}, one canonical
family name per trait, named by the trait levels in trait-factor order.
}
\description{
Returns the response family of every trait in a fit returned by
\code{\link[=gllvmTMB]{gllvmTMB()}}. For a mixed-family fit (built by passing a named or
selector-driven \code{family} list) the returned vector reports the family
assigned to each trait; for a single-family fit every trait shares the
one family. This complements \code{\link[stats:family]{stats::family()}} (singular), which returns
the one \code{family} object retained on the fit.
}
\details{
This is a pure accessor: it reads fields already stored on the fit
(\code{family_selector} / the per-row family ids) and does not refit. The
name \code{trait_families} is used rather than \code{families} because the latter
help topic is already taken by the family constructors (see
\link{Families}).
}
\examples{
\dontrun{
# Mixed-family fit (one trait gaussian, one poisson, ...):
trait_families(fit)
#>      trait_1      trait_2      trait_3
#>   "gaussian"   "binomial"    "poisson"
}
}
\seealso{
\code{\link[stats:family]{stats::family()}} for the single retained family object;
the per-trait family is also shown by \code{print()} for mixed-family fits.
}
