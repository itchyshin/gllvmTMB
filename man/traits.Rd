% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traits-keyword.R
\name{traits}
\alias{traits}
\title{Wide-format trait marker for the \code{gllvmTMB()} formula LHS}
\usage{
traits(...)
}
\arguments{
\item{...}{Column-selection expression(s) passed verbatim to
\code{tidyr::pivot_longer(cols = ...)}. Bare names or any tidyselect
verb (\code{all_of()}, \code{starts_with()}, \code{matches()}, etc.) are accepted.}
}
\value{
A formula marker; never evaluated as a function call. The
parser recognises \code{traits(...)} on the LHS of a \code{gllvmTMB()}
formula and dispatches to the wide-format pivot pre-pass.
}
\description{
Formula-LHS marker that lets \code{\link[=gllvmTMB]{gllvmTMB()}} accept a wide data frame
(one row per unit, one column per trait) without making the user
pivot first to one row per \verb{(unit, trait)} observation.
}
\details{
The package teaches \strong{two shapes}, long or wide data-frame:
\itemize{
\item \strong{long}: \code{gllvmTMB(value ~ ..., data = df_long, ...)} -- one
row per \verb{(unit, trait)} observation.
\item \strong{wide data frame}: \code{gllvmTMB(traits(t1, t2, ...) ~ ..., data = df_wide, ...)} -- one row per unit, one column per trait,
with compact formula syntax.
}

The soft-deprecated \code{gllvmTMB_wide(Y, ...)} wrapper remains exported
for legacy matrix-wrapper workflows, but new examples should use
\code{traits(...)} through \code{\link[=gllvmTMB]{gllvmTMB()}}.

Both taught shapes reach the same stacked-trait model after internal
stacking; the user picks whichever shape matches their data on disk.

Because the LHS already names the response traits, the RHS can use a
compact wide shorthand. \code{1} expands to the trait-specific intercepts
\code{0 + trait}; ordinary predictors such as \code{env_temp} expand to
\code{(0 + trait):env_temp}; and \code{latent(1 | individual)} expands to
the long covariance syntax \code{latent(0 + trait | individual)}.
Ordinary \code{latent()} includes its \eqn{\boldsymbol\Psi} companion by
default. The same \code{1 | group} shorthand is recognised for \code{indep()},
\code{dep()}, bar-style \code{phylo_indep()} / \code{phylo_dep()}, and the
\verb{spatial_*()} keywords. Species-axis phylogenetic keywords such as
\code{phylo_latent(species, d = K)} already name their phylogenetic axis
and pass through unchanged. Ordinary random-intercept terms such as
\code{(1 | batch)} also pass through unchanged.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{gllvmTMB(
  traits(sleep, mass, lifespan, brain) ~ 1 + env_temp +
    latent(1 | individual, d = 2),
  data   = wide_df,
  unit   = "individual",
  family = gaussian()
)
}\if{html}{\out{</div>}}

Internally \code{traits()} is implemented as a \code{tidyr::pivot_longer()}
pre-pass: the wide data is pivoted to long format with \code{trait} as a
factor column (levels in the order the user supplied to \code{traits()})
and \code{.y_wide_} as the response column; the LHS of the formula is
rewritten from \code{traits(...)} to \code{.y_wide_}; and the compact RHS is
expanded to the trait-stacked long syntax before dispatch. The
explicit long RHS remains accepted, so existing calls that already
write \code{0 + trait} and \code{latent(0 + trait | group)} keep working.

Tidyselect verbs are supported because \code{traits()} forwards its
arguments to \code{tidyr::pivot_longer(cols = ...)}:
\code{traits(all_of(cols))}, \code{traits(starts_with("sp"))},
\code{traits(matches("^y[0-9]+$"))}, \code{traits(any_of(c("a", "b")))}, and
bare names all work.

Cells with \code{NA} responses are, by default, dropped via
\code{pivot_longer(values_drop_na = TRUE)} -- the canonical complete-case
behaviour. Passing \code{missing = miss_control(response = "include")} to
\code{\link[=gllvmTMB]{gllvmTMB()}} instead \strong{keeps} every \verb{(unit, trait)} cell and masks the
\code{NA} cells out of the likelihood (the observed-response mask), preserving
per-cell identity and original-row accounting in \code{fit$missing_data}. Users
who want strict listwise drop should pre-filter the wide data before
calling.

Mixed-family fits (\code{family = list(...)} keyed by trait) use the same
family handling after internal stacking; \code{traits()} does not
intercept the family argument. Per-row weight vectors of length
\code{nrow(data)} are also replicated across traits automatically. For
per-cell weight matrices, pivot to long format and pass a \code{weights}
column aligned with \verb{(unit, trait)} rows. The legacy matrix wrapper
\code{\link[=gllvmTMB_wide]{gllvmTMB_wide()}} still accepts matrix weights for migration code.
}
\examples{
\dontrun{
# Wide format: one column per trait; traits() stacks them internally.
# The LHS *is* the trait spec, so no `trait =` argument is needed.
set.seed(1)
df_wide <- data.frame(
  unit = factor(paste0("u", 1:40)),
  t1 = rnorm(40), t2 = rnorm(40), t3 = rnorm(40)
)
fit <- gllvmTMB(
  traits(t1, t2, t3) ~ 1 + latent(1 | unit, d = 2),
  data = df_wide, unit = "unit"
)
extract_Sigma(fit)
}
}
\seealso{
\code{\link[=gllvmTMB]{gllvmTMB()}} for model fitting. The legacy
matrix wrapper \code{gllvmTMB_wide(Y, ...)} is soft-deprecated in
0.2.0; wide-data examples now use the \code{traits(...)} LHS through
\code{\link[=gllvmTMB]{gllvmTMB()}}. The source-tree contract is
\code{docs/design/01-formula-grammar.md}.
}
